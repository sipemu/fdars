---
phase: 35-basis-system-completions
plan: 02
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - fdars-core/src/basis/exponential.rs
  - fdars-core/src/basis/power.rs
  - fdars-core/src/basis/polygonal.rs
  - fdars-core/src/basis/mod.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [REP-01]
estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "exponential_basis(argvals, rates) returns Ok(BasisSystem) whose eval_matrix equals exp(rate*t) on reference points; rate=0 column is constant 1 (REP-01 SC1)"
    - "power_basis(argvals, exponents) evaluates to t^exponent and rejects non-positive argvals when any exponent is non-integer/negative via FdarError (REP-01 SC1/SC4)"
    - "polygonal_basis(knots) produces hat functions that peak at 1 on their knot and satisfy partition-of-unity on interior points; non-monotone knots return FdarError (REP-01 SC1/SC4)"
    - "All three factories carry a penalty matrix inside BasisSystem (numeric Gram, reusing promoted smooth_basis helpers) (REP-01 SC1/SC4)"
    - "All three factories are crate-root re-exported; existing basis code unchanged; suite + clippy --all-targets green (REP-01 SC1/SC5)"
  artifacts:
    - fdars-core/src/basis/exponential.rs
    - fdars-core/src/basis/power.rs
    - fdars-core/src/basis/polygonal.rs
  key_links:
    - "each factory reuses crate::smooth_basis::{differentiate_basis_columns, integrate_symmetric_penalty} (promoted in 35-01) for its numeric Gram"
    - "basis/mod.rs + src/lib.rs re-export exponential_basis, power_basis, polygonal_basis"
---

<objective>
Expansion on the 35-01 tracer: implement the remaining three basis factories — `exponential_basis`, `power_basis`, `polygonal_basis` — each returning a `BasisSystem` (the struct proven in 35-01) with a penalty matrix, and register all three at crate root. Power basis reuses the analytic monomial Gram for integer exponents and a numeric Gram otherwise; exponential and polygonal use the numeric Gram via the `pub(crate)` helpers promoted in 35-01.

Purpose: Complete the four-factory family required by REP-01 SC1, reusing the proven BasisSystem shape and the smooth_basis numeric-penalty machinery (no new subsystem, no new dependency).
Output: exponential_basis, power_basis, polygonal_basis + registration + inline tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-basis-system-completions/35-CONTEXT.md
@.planning/phases/35-basis-system-completions/35-RESEARCH.md
@.planning/phases/35-basis-system-completions/35-01-SUMMARY.md

# Reuse analogs (verified on disk):
@fdars-core/src/basis/bspline.rs
@fdars-core/src/basis/monomial.rs
@fdars-core/src/basis/basis_system.rs
@fdars-core/src/smooth_basis.rs
@fdars-core/src/helpers.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: exponential_basis + power_basis factories</name>
  <files>fdars-core/src/basis/exponential.rs, fdars-core/src/basis/power.rs</files>
  <read_first>
    - fdars-core/src/basis/monomial.rs (from 35-01) — the BasisSystem construction + validation + col-major eval pattern to mirror; power_basis with integer exponents reuses this analytic Gram approach.
    - fdars-core/src/basis/basis_system.rs — BasisSystem field shape.
    - fdars-core/src/smooth_basis.rs — pub(crate) differentiate_basis_columns(basis, n_quad, nbasis, h, lfd_order) and integrate_symmetric_penalty(deriv_basis, weights, k, n_quad); and helpers::simpsons_weights for the quadrature weights argument.
    - 35-RESEARCH.md "Pattern 2: Exponential Basis" (eval exp(rate*t); recommended numeric Gram) and "Pattern 3: Power Basis" (eval t.powf(exp); strictly-positive-domain validation for non-integer/negative exponents; analytic Gram for integer exponents else numeric) and Pitfall 1 (zero/negative argvals with non-integer exponents -> Inf/NaN).
  </read_first>
  <behavior>
    - exponential_basis(&[0.0, 1.0], &[0.0, -1.0]): col0 == [1.0, 1.0] (exp(0)); col1 == [1.0, exp(-1.0)]; eval index [ti + j*n].
    - exponential_basis with rates=[0.0,...]: the rate=0 column is all 1.0 (constant).
    - power_basis with integer exponents [0,1,2] on t=[0,1,2] equals monomial_basis eval matrix (same [1,1,1,0,1,2,0,1,4]).
    - power_basis with a negative or non-integer exponent AND any argval <= 0.0 -> Err(InvalidParameter) (no NaN/Inf leaks into eval_matrix/penalty).
    - Both return a BasisSystem with a symmetric penalty_matrix of length nbasis*nbasis, lfd_order default 2.
    - Empty/short argvals or empty rates/exponents -> FdarError, never panic.
  </behavior>
  <action>
    Create exponential.rs: exponential_basis(argvals: &[f64], rates: &[f64]) -> Result<BasisSystem, FdarError>. nbasis = rates.len(). Validate argvals.len()>=2 and rates non-empty. eval_matrix[ti + j*n] = (rates[j]*t).exp(). Penalty: build a fine quad grid over [argvals[0], argvals.last()] (use the same sub-point density the smooth_basis bspline penalty uses — document the choice), evaluate the exponential basis on that grid, then call the pub(crate) differentiate_basis_columns + integrate_symmetric_penalty with simpsons_weights for lfd_order=2. Document in rustdoc that a numeric Gram is used (RESEARCH Open-Q3 recommendation).

    Create power.rs: power_basis(argvals: &[f64], exponents: &[f64]) -> Result<BasisSystem, FdarError>. nbasis = exponents.len(). Validate argvals.len()>=2 and exponents non-empty. If any exponent is non-integer OR negative, require argvals.iter().all(|&t| t > 0.0) else Err(InvalidParameter) (RESEARCH Pitfall 1). eval_matrix[ti + j*n] = t.powf(exponents[j]). Penalty: if all exponents are non-negative integers, reuse the analytic falling-factorial Gram (share the helper from monomial.rs or replicate — planner discretion; prefer a small crate-local helper); otherwise numeric Gram via the promoted helpers, guarding the strictly-positive domain. lfd_order default 2.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars &amp;&amp; TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel basis::exponential basis::power 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - exponential eval test: col0 all 1.0, col1 == [1.0, exp(-1.0)] within 1e-12.
    - exponential rate=0 column is constant 1.0.
    - power_basis integer-exponent eval matches monomial_basis for [0,1,2] on t=[0,1,2].
    - power_basis with negative exponent and a zero/negative argval returns Err(InvalidParameter); eval_matrix/penalty contain no NaN/Inf.
    - Both penalties symmetric, length nbasis*nbasis.
  </acceptance_criteria>
  <done>exponential_basis and power_basis return BasisSystem with correct eval + penalty; power-domain validation rejects NaN-producing inputs; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: polygonal_basis factory (hat functions, 1st-order penalty)</name>
  <files>fdars-core/src/basis/polygonal.rs</files>
  <read_first>
    - fdars-core/src/basis/basis_system.rs — BasisSystem shape.
    - fdars-core/src/smooth_basis.rs — pub(crate) differentiate_basis_columns + integrate_symmetric_penalty; helpers::simpsons_weights.
    - 35-RESEARCH.md "Pattern 4: Polygonal Basis" (hat_function eval; partition-of-unity reference; 1st-order penalty because 2nd derivative is 0 a.e.; strictly-increasing-knot validation) and Pitfall 5 (non-monotone/duplicate knots) and Code Examples "Polygonal hat function evaluation".
  </read_first>
  <behavior>
    - polygonal_basis over knots=[0.0,0.5,1.0] evaluated at argvals including interior points: at t=0.25, B0==0.5, B1==0.5, B2==0.0; at t=0.5, B0==0.0, B1==1.0, B2==0.0.
    - Partition of unity: for any interior argval, sum over j of B_j(t) == 1.0 within 1e-12.
    - B_j(knots[j]) == 1.0 (hat peaks at its own knot).
    - lfd_order default 1 (piecewise-linear; document that lfd_order>=2 yields an all-zero penalty since D^2 = 0 a.e.).
    - Non-monotone or duplicate knots (knots[i+1] <= knots[i]) -> Err(InvalidParameter). Knots.len() < 2 -> FdarError. Never panic.
  </behavior>
  <action>
    Create polygonal.rs: polygonal_basis(argvals: &[f64], knots: &[f64]) -> Result<BasisSystem, FdarError>. nbasis = knots.len(). Validate knots strictly increasing (knots.windows(2).all(|w| w[1] > w[0])) and knots.len()>=2 and argvals.len()>=2 else FdarError. eval_matrix[ti + j*n] via the hat-function formula (left ramp (t-knots[j-1])/(knots[j]-knots[j-1]) on [knots[j-1],knots[j]], right ramp (knots[j+1]-t)/(knots[j+1]-knots[j]) on [knots[j],knots[j+1]], boundary knots have one ramp, else 0). Penalty: numeric 1st-order Gram (lfd_order=1) via the promoted helpers on a fine grid over the knot span; document in rustdoc that a 1st-order roughness penalty is used because the 2nd derivative of a piecewise-linear basis is 0 a.e. (D — Polygonal). lfd_order default 1.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars &amp;&amp; TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel basis::polygonal 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Hat-peak test: B_j(knots[j]) == 1.0 for each interior j.
    - Partition-of-unity test: sum_j B_j(t) == 1.0 (within 1e-12) at interior argvals.
    - Midpoint test: at t=0.25 with knots [0,0.5,1], B0==0.5 and B1==0.5.
    - Non-monotone knots [0.0,0.5,0.5] returns Err(InvalidParameter); no panic.
    - penalty_matrix symmetric, length nbasis*nbasis; lfd_order==1.
  </acceptance_criteria>
  <done>polygonal_basis returns BasisSystem with correct hat-function eval, partition-of-unity, 1st-order penalty, and knot validation; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Register the three factories in basis/mod.rs and crate-root lib.rs; clippy gate</name>
  <files>fdars-core/src/basis/mod.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/basis/mod.rs — the pub mod block and pub use block extended in 35-01 (monomial + basis_system already present).
    - fdars-core/src/lib.rs — the pub use basis::{...} block extended in 35-01.
  </read_first>
  <action>
    In basis/mod.rs add pub mod exponential; pub mod power; pub mod polygonal; and extend the pub use block with pub use exponential::exponential_basis; pub use power::power_basis; pub use polygonal::polygonal_basis; consistent with existing ordering. In lib.rs extend the pub use basis::{...} block to add exponential_basis, power_basis, polygonal_basis. Do not remove or reorder existing exports. Then run the full clippy gate and fix any warnings this plan introduced.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars &amp;&amp; TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - basis/mod.rs declares + re-exports exponential, power, polygonal modules and their factories.
    - lib.rs pub use basis::{...} includes exponential_basis, power_basis, polygonal_basis.
    - cargo clippy --all-targets --features linalg,parallel -- -D warnings is clean.
    - All four factories (monomial from 35-01 + these three) callable at crate root.
  </acceptance_criteria>
  <done>All four basis factories are crate-root re-exported and the full clippy gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library caller -> basis factories | Numeric args (argvals, rates, exponents, knots); no network/file/deserialization surface. Invalid values rejected via FdarError; power basis guards the NaN/Inf-producing non-positive-domain case. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-02 | Tampering | power_basis eval on non-positive domain | low | mitigate | Reject non-positive argvals when any exponent is non-integer/negative (RESEARCH Pitfall 1) so no NaN/Inf silently poisons eval/penalty matrices. |
| T-35-03 | Denial of Service | polygonal_basis knot validation | low | mitigate | Reject non-monotone/duplicate knots at entry (zero-width interval would divide by zero); FdarError, no panic. |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | No new crate dependency (reuses promoted smooth_basis helpers + nalgebra). No package-install surface. |
</threat_model>

<verification>
- cargo test -p fdars-core --features linalg,parallel basis:: — all four-factory eval/penalty/validation tests green.
- cargo clippy --all-targets --features linalg,parallel -- -D warnings — clean.
- git diff shows only additive basis files + two additive re-export edits in mod.rs/lib.rs; existing basis factories untouched.
</verification>

<success_criteria>
exponential_basis, power_basis, and polygonal_basis exist, are crate-root re-exported, evaluate to their closed-form basis functions with a penalty carried in BasisSystem, reject invalid inputs via FdarError, and the full clippy gate is green — completing the four-factory family required by REP-01 SC1.
</success_criteria>

<output>
Create `.planning/phases/35-basis-system-completions/35-02-SUMMARY.md` when done
</output>
