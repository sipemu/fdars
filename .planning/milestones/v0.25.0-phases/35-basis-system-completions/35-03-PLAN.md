---
phase: 35-basis-system-completions
plan: 03
type: execute
wave: 3
depends_on: [35-02]
files_modified:
  - fdars-core/src/multi_fdata.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [REP-01]
estimate:
  tokens: 42000
  raw_tokens: 21000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "A MultiFunData multi-domain container lives in new fdars-core/src/multi_fdata.rs, holding several component FdMatrix blocks on possibly-different domains (REP-01 SC2)"
    - "MultiFunData::new enforces equal observation count (rows) across all components and argvals.len()==ncols per component, returning FdarError on violation (REP-01 SC2/SC4)"
    - "Accessors n_obs/n_components/component(k)/argvals(k) preserve each component's argvals; out-of-range index returns FdarError (REP-01 SC2)"
    - "MultiFunData + FdComponent are crate-root re-exported; pub mod multi_fdata; added to lib.rs; existing code unchanged; suite + clippy green (REP-01 SC2/SC5)"
  artifacts:
    - fdars-core/src/multi_fdata.rs
  key_links:
    - "src/lib.rs adds pub mod multi_fdata; and pub use multi_fdata::{MultiFunData, FdComponent}"
    - "MultiFunData components are crate::matrix::FdMatrix blocks (column-major)"
---

<objective>
Expansion: add the `MultiFunData` multivariate/multi-domain container in a new `fdars-core/src/multi_fdata.rs`, plus its `FdComponent` element type. The constructor enforces the two funData invariants (shared observation count across components; each component's argvals length matches its FdMatrix column count) and accessors preserve per-component argvals. Register both types at crate root.

Purpose: REP-01 SC2 — a composable multi-domain container so callers can hold several functional-data blocks (on different grids/domains) under one object with validated invariants.
Output: multi_fdata.rs (MultiFunData + FdComponent) + lib.rs registration + inline tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-basis-system-completions/35-CONTEXT.md
@.planning/phases/35-basis-system-completions/35-RESEARCH.md

# Reuse analogs (verified on disk):
@fdars-core/src/matrix.rs
@fdars-core/src/error.rs
@fdars-core/src/basis/pspline.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MultiFunData + FdComponent in new multi_fdata.rs</name>
  <files>fdars-core/src/multi_fdata.rs</files>
  <read_first>
    - fdars-core/src/matrix.rs — FdMatrix API: zeros(nrows, ncols), nrows(), ncols(), shape(), column(j), indexing data[(i,j)] (column-major i + j*nrows).
    - fdars-core/src/error.rs — FdarError::InvalidDimension { parameter, expected, actual } and InvalidParameter { parameter, message }.
    - fdars-core/src/basis/pspline.rs — the derive(Debug, Clone, PartialEq) + non_exhaustive + conditional serde derive pattern to mirror on the public structs.
    - 35-RESEARCH.md "MultiFunData Container" (struct design, invariants 1-4, constructor error cases, accessors) and 35-CONTEXT.md "MultiFunData container" locked decisions.
  </read_first>
  <behavior>
    - FdComponent { data: FdMatrix, argvals: Vec<f64> } — public fields.
    - MultiFunData::new(vec_of_components) with 2 components (5 obs x 10 cols argvals-len-10, and 5 obs x 4 cols argvals-len-4) -> Ok; n_obs()==5, n_components()==2.
    - Different domains allowed: component grids may differ (multi-domain feature); no cross-component argvals equality is required.
    - Mismatched observation count (component with 4 rows vs 5) -> Err(InvalidDimension).
    - argvals length != data.ncols() for any component -> Err(InvalidDimension).
    - Empty components vec -> Err(InvalidParameter).
    - component(k)/argvals(k) return Ok for valid k; out-of-range k -> Err(InvalidParameter). Never panic.
  </behavior>
  <action>
    Create multi_fdata.rs. Define pub struct FdComponent { pub data: FdMatrix, pub argvals: Vec<f64> } with derive(Debug, Clone, PartialEq) + conditional serde derive. Define pub struct MultiFunData holding components: Vec<FdComponent> (private field), derive(Debug, Clone, PartialEq), non_exhaustive, conditional serde. Implement new(components: Vec<FdComponent>) -> Result<Self, FdarError>: reject empty (InvalidParameter); let n_obs = components[0].data.nrows(); for each component enforce data.nrows()==n_obs (InvalidDimension) and argvals.len()==data.ncols() (InvalidDimension). Implement accessors: n_obs(&self)->usize (components[0].data.nrows()), n_components(&self)->usize, component(&self,k)->Result<&FdComponent,FdarError> (InvalidParameter on out-of-range), argvals(&self,k)->Result<&[f64],FdarError>. Document that components may live on different domains/grids (the multi-domain feature) and that the shared invariant is observation count only.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars &amp;&amp; TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel multi_fdata 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Two-component construction on different-length grids (same n_obs) returns Ok; n_obs and n_components correct.
    - Mismatched n_obs across components returns Err(InvalidDimension).
    - argvals.len() != ncols returns Err(InvalidDimension).
    - Empty components returns Err(InvalidParameter); out-of-range component(k)/argvals(k) returns Err; no panics.
    - MultiFunData is non_exhaustive; both structs derive Debug/Clone/PartialEq.
  </acceptance_criteria>
  <done>MultiFunData + FdComponent exist with validated invariants and accessors; inline tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Register multi_fdata module + crate-root re-exports; clippy gate</name>
  <files>fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/lib.rs — the pub mod declarations (e.g. pub mod basis; pub mod fts; pub mod covariance;) and the re-export sections; add multi_fdata alongside.
  </read_first>
  <action>
    In lib.rs add pub mod multi_fdata; (alongside the other pub mod declarations) and pub use multi_fdata::{MultiFunData, FdComponent}; in the crate-root re-export area, consistent with existing ordering. Do not touch other exports. Then run the full clippy gate and fix any warnings this plan introduced.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars &amp;&amp; TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - lib.rs contains pub mod multi_fdata; and pub use multi_fdata::{MultiFunData, FdComponent};.
    - fdars_core::MultiFunData and fdars_core::FdComponent are callable at crate root.
    - cargo clippy --all-targets --features linalg,parallel -- -D warnings is clean.
  </acceptance_criteria>
  <done>multi_fdata module + MultiFunData/FdComponent are crate-root re-exported; clippy gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library caller -> MultiFunData::new | Component FdMatrix blocks + argvals; no network/file/deserialization surface. Invariant violations rejected via FdarError. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-04 | Denial of Service | MultiFunData accessors on out-of-range index | low | mitigate | component(k)/argvals(k) return FdarError on out-of-range k instead of indexing-panic. |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | No new crate dependency (reuses existing FdMatrix). No package-install surface. |
</threat_model>

<verification>
- cargo test -p fdars-core --features linalg,parallel multi_fdata — invariant + accessor tests green.
- cargo clippy --all-targets --features linalg,parallel -- -D warnings — clean.
- git diff shows only the new multi_fdata.rs + two additive lib.rs lines.
</verification>

<success_criteria>
MultiFunData + FdComponent exist in a new multi_fdata.rs, enforce the shared-observation-count and argvals-length invariants, expose validated accessors, are crate-root re-exported, and the clippy gate is green — satisfying REP-01 SC2.
</success_criteria>

<output>
Create `.planning/phases/35-basis-system-completions/35-03-SUMMARY.md` when done
</output>
