---
phase: 13-parity-quick-wins-imputation-extrapolation-policy-scoring-me
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/helpers.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [FEAT-03, FEAT-04]
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "A caller can impute NaN entries in a regular-grid FdMatrix via impute_missing_values, choosing Linear, Mean, or Constant strategy (FEAT-03)"
    - "impute_missing_values returns Err(InvalidParameter) for an all-NaN curve and Err(InvalidDimension) when argvals.len() != data.ncols() (FEAT-03)"
    - "A caller can control out-of-range interpolation via fdata_interpolate_with_policy using ExtrapolationPolicy::{Boundary, Exception, Fill(v), Periodic} (FEAT-04)"
    - "ExtrapolationPolicy::Exception returns Err(InvalidParameter) for a query point outside the argvals domain (FEAT-04)"
    - "Existing fdata_interpolate, linear_interp, spline_interpolate, cubic_hermite_interp signatures are unchanged (additive, non-breaking)"
  artifacts:
    - "fdars-core/src/helpers.rs — ImputationMethod enum, impute_missing_values fn, ExtrapolationPolicy enum, fdata_interpolate_with_policy fn + inline tests"
    - "fdars-core/src/lib.rs — crate-root re-exports for the four new items"
  key_links:
    - "impute_missing_values (Linear) delegates to helpers::linear_interp between nearest non-NaN neighbors"
    - "fdata_interpolate_with_policy dispatches policy per query point then delegates to existing private linear_interp / cubic_hermite_interp"
    - "New items re-exported from lib.rs pub use helpers::{...} block"
---

<objective>
Close two additive scikit-fda parity gaps inside `fdars-core/src/helpers.rs`: FEAT-03 in-grid NaN imputation and FEAT-04 composable extrapolation policy. Both features touch the same file (`helpers.rs`) and are therefore SERIALIZED as sequential tasks in a single plan to avoid write collisions.

Purpose: Callers can (a) impute missing values in a regular-grid `FdMatrix` with mean/linear/constant strategies, and (b) control out-of-range interpolation behavior via an `ExtrapolationPolicy` enum — both per the LOCKED CONTEXT.md decisions.
Output: `ImputationMethod` enum + `impute_missing_values`, `ExtrapolationPolicy` enum + `fdata_interpolate_with_policy`, inline tests, and crate-root re-exports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

## Executor guardrails (MANDATORY — bake into every command)

1. **TMPDIR prefix on ALL cargo + git commands.** System `/tmp` is ~1.9G and doctest linking fills it, producing a bogus "No space left on device" that fails the pre-commit hook. Prefix EVERY cargo and git command with `TMPDIR=/home/simonm/projects/rust/fdars/.gsd-build-tmp` (the dir already exists). Example: `TMPDIR=/home/simonm/projects/rust/fdars/.gsd-build-tmp cargo test -p fdars-core --features linalg -q 2>&1 | tail -5`.
2. **Do NOT use `gsd-tools query commit` for code commits** — it does not inherit TMPDIR and the hook fails. Use plain `TMPDIR=/home/simonm/projects/rust/fdars/.gsd-build-tmp git commit -m "..."`.
3. **CI runs `clippy --all-targets --all-features -D warnings`** (stricter than the local hook). New code, INCLUDING `#[cfg(test)]` blocks, must be clean under it. Specifically: use `.to_vec()` not `iter().copied().collect()`; use `.and_then(...)` not `.map(...).flatten()`; no identity operations; no `needless map(|x| *x)`. Verify locally before commit: `TMPDIR=/home/simonm/projects/rust/fdars/.gsd-build-tmp cargo clippy -p fdars-core --features linalg --all-targets -- -D warnings 2>&1 | tail -20`.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-parity-quick-wins-imputation-extrapolation-policy-scoring-me/13-CONTEXT.md
@.planning/phases/13-parity-quick-wins-imputation-extrapolation-policy-scoring-me/13-RESEARCH.md

# Source files this plan modifies / reads (all VERIFIED in RESEARCH.md)
@fdars-core/src/helpers.rs
@fdars-core/src/matrix.rs
@fdars-core/src/error.rs
@fdars-core/src/lib.rs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: FEAT-04 ExtrapolationPolicy enum + fdata_interpolate_with_policy (all four variants, end-to-end)</name>
  <files>fdars-core/src/helpers.rs, fdars-core/src/lib.rs</files>
  <read_first>
    RESEARCH.md "Interpolation Path Detail (FEAT-04)" + "ExtrapolationPolicy Dispatch Skeleton" (the verified code skeleton) + Pitfall 3 (Periodic wrap) + Pitfall 6/7 (#[must_use], no #[non_exhaustive]).
    Existing helpers.rs: InterpolationMethod enum (~lines 342-350), fdata_interpolate (~365-391), linear_interp (172-191), cubic_hermite_interp (513-570). Do NOT modify any of these.
  </read_first>
  <behavior>
    This is the tracer: one new fn wired through the enum, all four policy branches, and the crate-root re-export, proven end-to-end by tests before expansion.
    - Boundary: an out-of-range query below t_min returns the interpolated value at t_min (clamp); above t_max returns value at t_max.
    - Exception: an out-of-range query returns Err(FdarError::InvalidParameter { parameter: "new_argvals", .. }).
    - Fill(v): an out-of-range query writes the constant v into the result cell.
    - Periodic: a query at t_min - delta returns the same value as t_max - delta (wrap over domain_len = t_max - t_min); uses ((t - t_min) % domain_len + domain_len) % domain_len (NOT bare %, which is negative for t < t_min).
    - In-range queries produce identical values to the existing fdata_interpolate path (Linear and CubicHermite branches).
    - Dimension guard: argvals.len() != data.ncols() -> Err(InvalidDimension { parameter: "argvals", .. }).
  </behavior>
  <action>
    Add to helpers.rs (new items only — no edits to existing fns):
    1. Define `ExtrapolationPolicy` enum with variants `Boundary`, `Exception`, `Fill(f64)`, `Periodic`. Derive `Debug, Clone, PartialEq` and add `#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]` (per crate convention — RESEARCH "Serde Convention"). Do NOT add `#[non_exhaustive]` (RESEARCH Pitfall 7). Doc-comment each variant.
    2. Define `#[must_use] pub fn fdata_interpolate_with_policy(data: &FdMatrix, argvals: &[f64], new_argvals: &[f64], method: InterpolationMethod, policy: ExtrapolationPolicy) -> Result<FdMatrix, FdarError>` following the VERIFIED skeleton in RESEARCH.md. Validate `argvals.len() == data.ncols()` first (InvalidDimension on mismatch). For each query point, test `t >= t_min && t <= t_max`; in-range delegates to the existing private `linear_interp` / `cubic_hermite_interp` (same file, no visibility change). Out-of-range dispatches per the policy exactly as in the skeleton. For `Periodic` use the guarded-modulo recipe. Do NOT thread policy into the low-level interpolators — the wrapper owns dispatch (RESEARCH Anti-Patterns).
    3. In lib.rs, extend the existing `pub use helpers::{ ... };` block (currently ends `... spline_interpolate, trapz, InterpolationMethod, DEFAULT_CONVERGENCE_TOL, NUMERICAL_EPS,`) to also re-export `ExtrapolationPolicy` and `fdata_interpolate_with_policy`. Keep list alphabetical-ish and comma-terminated as the existing block is.
    4. Inline `#[cfg(test)] mod tests` cases (use `crate::test_helpers::uniform_grid`): `test_extrapolation_boundary`, `test_extrapolation_exception`, `test_extrapolation_fill`, `test_extrapolation_periodic`, plus one in-range-equivalence assertion. Keep tests clippy-clean under `--all-targets -D warnings`.
    Commit message: `feat(13-01): add ExtrapolationPolicy enum + fdata_interpolate_with_policy (FEAT-04)`
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/projects/rust/fdars/.gsd-build-tmp cargo test -p fdars-core --features linalg test_extrapolation 2>&1 | tail -8</automated>
  </verify>
  <done>Four `test_extrapolation_*` tests pass; in-range equivalence assertion passes; existing helpers.rs signatures unchanged; new items re-exported from lib.rs; clippy `--all-targets -D warnings` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FEAT-03 ImputationMethod enum + impute_missing_values (Linear, Mean, Constant)</name>
  <files>fdars-core/src/helpers.rs, fdars-core/src/lib.rs</files>
  <read_first>
    RESEARCH.md "Imputation Detail (FEAT-03)" + "Imputation Skeleton" (VERIFIED code) + Pitfall 4 (use is_nan(), never == NaN). matrix.rs: `from_column_major` (50-63), `row` (146), `shape` (106). This task runs AFTER Task 1 (both edit helpers.rs — sequential).
  </read_first>
  <behavior>
    - Linear on a synthetic interior gap reproduces the exact linear-interpolated value between nearest non-NaN neighbors (assert against a hand-computed value).
    - Leading/trailing NaN (no neighbor on one side) are filled with the nearest valid value (boundary extension).
    - Mean replaces each NaN in a curve with that curve's mean of its non-NaN values.
    - Constant(c) replaces each NaN with c.
    - An all-NaN curve returns Err(InvalidParameter { parameter: "data", .. }); detect via is_nan() (NaN != NaN under IEEE-754, so equality checks silently miss).
    - argvals.len() != data.ncols() returns Err(InvalidDimension { parameter: "argvals", .. }).
    - Non-NaN entries are copied through unchanged.
  </behavior>
  <action>
    Add to helpers.rs (new items only):
    1. Define `ImputationMethod` enum with variants `Linear`, `Mean`, `Constant(f64)`. Derive `Debug, Clone, PartialEq` + conditional serde `#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]`. No `#[non_exhaustive]`. Doc-comment each variant, including that leading/trailing NaN use nearest-valid boundary extension for Linear.
    2. Define `#[must_use] pub fn impute_missing_values(data: &FdMatrix, argvals: &[f64], method: ImputationMethod) -> Result<FdMatrix, FdarError>` per the VERIFIED skeleton. Validate `argvals.len() == data.ncols()` first. Per curve (row), reject all-NaN via a finite/non-NaN count of 0 -> InvalidParameter. Impute per the skeleton's `impute_row` logic (Mean / Constant / Linear neighbor interpolation via `linear_interp`). Write results column-major and construct via `FdMatrix::from_column_major` (which itself returns a Result — propagate with `?`).
    3. In lib.rs, extend the same `pub use helpers::{ ... };` block to also re-export `ImputationMethod` and `impute_missing_values`.
    4. Inline tests: `test_impute_linear` (interior gap, hand-computed), `test_impute_mean`, `test_impute_constant`, `test_impute_all_nan` (expects Err InvalidParameter), `test_impute_boundary_nan` (leading/trailing fill). Use is_nan() to build NaN inputs (`f64::NAN`). Keep clippy-clean.
    Commit message: `feat(13-01): add ImputationMethod enum + impute_missing_values (FEAT-03)`
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/projects/rust/fdars/.gsd-build-tmp cargo test -p fdars-core --features linalg test_impute 2>&1 | tail -10</automated>
  </verify>
  <done>All five `test_impute_*` tests pass; all-NaN curve yields Err(InvalidParameter); argvals-mismatch yields Err(InvalidDimension); new items re-exported from lib.rs; existing signatures unchanged; clippy `--all-targets -D warnings` clean.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite + clippy gate for helpers.rs additions, then commit</name>
  <files>fdars-core/src/helpers.rs, fdars-core/src/lib.rs</files>
  <action>
    Final integration gate for both FEAT-03 and FEAT-04 additions (already committed per task). Run the full crate test suite and the strict CI-equivalent clippy. If either fails, fix and amend/add a follow-up commit `fix(13-01): resolve clippy/test findings in helpers additions`. This task adds no new behavior — it proves the two features coexist cleanly and match CI gating (D-FEAT-03, D-FEAT-04 non-breaking guarantee).
    Verify no existing helpers.rs public signature changed: `git diff` on committed range must show only additions (new enums, new fns, new re-exports, new tests) — no edits inside `fdata_interpolate`, `linear_interp`, `spline_interpolate`, `cubic_hermite_interp`.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/projects/rust/fdars/.gsd-build-tmp cargo test -p fdars-core --features linalg 2>&1 | tail -5 && TMPDIR=/home/simonm/projects/rust/fdars/.gsd-build-tmp cargo clippy -p fdars-core --features linalg --all-targets -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <done>Full `cargo test -p fdars-core --features linalg` green; `cargo clippy --all-targets -D warnings` clean; git diff confirms additive-only changes to helpers.rs (no existing signature edits).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → public fn | Untrusted `FdMatrix`, `argvals`, method/policy enums cross into the library; may contain NaN, mismatched dims, out-of-range queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | impute_missing_values NaN detection | medium | mitigate | Use `is_nan()` (never `== NaN`); reject all-NaN curves with InvalidParameter (V5 input validation) |
| T-13-02 | Tampering | fdata_interpolate_with_policy Periodic wrap | low | mitigate | Guarded modulo `((t - t_min) % L + L) % L` prevents negative index arithmetic for t < t_min |
| T-13-03 | Information Disclosure | out-of-range interpolation | low | mitigate | Exception policy returns Err instead of silently clamping/leaking boundary values when caller wants strictness |
| T-13-04 | Tampering | dimension mismatch (argvals vs ncols) | medium | mitigate | Entry-point InvalidDimension guard; no silent truncation (V5) |
| T-13-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new dependencies added this phase (RESEARCH: "no external crates are added") |
</threat_model>

<verification>
- `TMPDIR=/home/simonm/projects/rust/fdars/.gsd-build-tmp cargo test -p fdars-core --features linalg` passes (all new + existing tests).
- `TMPDIR=/home/simonm/projects/rust/fdars/.gsd-build-tmp cargo clippy -p fdars-core --features linalg --all-targets -- -D warnings` clean.
- `git diff` shows only additive changes to helpers.rs and lib.rs (no existing signature edits).
</verification>

<success_criteria>
- FEAT-03: `impute_missing_values` returns Result, supports Linear + Mean + Constant, rejects all-NaN curves, reproduces known values on synthetic gaps (inline tests green).
- FEAT-04: `ExtrapolationPolicy` { Boundary, Exception, Fill(f64), Periodic } threaded via `fdata_interpolate_with_policy`; each variant exercised incl. the Exception error path (inline tests green).
- Non-breaking: existing `fdata_interpolate`, `linear_interp`, `spline_interpolate`, `cubic_hermite_interp` signatures unchanged.
- Both new enums re-exported at crate root.
</success_criteria>

<output>
Create `.planning/phases/13-parity-quick-wins-imputation-extrapolation-policy-scoring-me/13-01-SUMMARY.md` when done.
</output>
