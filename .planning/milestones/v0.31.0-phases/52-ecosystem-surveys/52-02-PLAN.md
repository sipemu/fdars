---
phase: 52-ecosystem-surveys
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/survey-julia.md
autonomous: true
requirements: [JUL-01]
estimate:
  tokens: 70000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The survey file .planning/research/survey-julia.md exists (JUL-01)."
    - "It contains a capability-first inventory table with every reference row pinned pkg@version (actively-maintained JuliaStats / functional-data packages)."
    - "Julia performance/idiom patterns (type-stable/generic/GPU-friendly designs) are captured as candidate capability rows where they represent a capability fdars lacks."
    - "Every row carries an fdars status column valued present, partial, or absent."
    - "Every absent/partial row carries an explicit 'searched fdars for:' note recording the grep terms used against fdars-core/src/ and the capability reasoning."
    - "The file ends with a 'Net-New Gap List' table using the six standardized columns, filtered to rows verified absent from shipped fdars AND absent from both BACKLOG.md and R-BACKLOG.md."
    - "The file contains a reverse-parity note recording where fdars leads the Julia FDA ecosystem."
  artifacts:
    - .planning/research/survey-julia.md
  key_links:
    - "Standardized gap-table columns (Capability | Reference (pkg@ver) | fdars status | Searched-for | Net-new? | Notes) — the merge contract Phase 53 consumes."
  prohibitions:
    - "No fdars-core/src/ file is modified by this plan (audit-only fence). `git status --porcelain fdars-core/src/` attributable to this phase is empty."
  test_coverage_na: "This is an audit/documentation phase that writes zero code and adds zero tests. Nyquist / Dimension 8 (test coverage) is N/A; verification is document-content grep, not cargo test."
---

<objective>
Survey the Julia FDA ecosystem — the actively-maintained JuliaStats / functional-data packages — capability-first, map fdars present/partial/absent against it, de-duplicate every candidate gap against shipped fdars capabilities plus both prior backlogs, and emit a net-new gap list. Explicitly capture modern/performance-oriented Julia patterns (type-stable/generic/GPU-friendly designs) as candidate gaps where they represent a capability fdars lacks. Implements JUL-01.

Purpose: Produce the Julia slice of the raw material Phase 53 consolidates. The value gate is "genuinely net-new" — every emitted gap must be absent from shipped fdars AND from `BACKLOG.md` (v0.14.0) AND from `R-BACKLOG.md` (v0.18.0).
Output: `.planning/research/survey-julia.md`.

Audit-only fence (MANDATORY): this plan modifies ZERO `fdars-core/src/` files. The only file it creates is its own `.planning/research/survey-julia.md` deliverable. No git tag, no crate change.

Test coverage N/A: this milestone writes zero code and adds zero tests. Nyquist / Dimension 8 does not apply — verification is document-content grep, not `cargo test`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-ecosystem-surveys/52-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<artifacts_this_phase_produces>
- `.planning/research/survey-julia.md` — the Julia FDA capability survey (this plan).
- (siblings, other plans in this wave: `survey-matlab.md`, `survey-tidyfun.md`, `survey-pyx.md`)
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Enumerate the Julia FDA capability surface (JuliaStats / functional-data packages), version-pinned</name>
  <files>.planning/research/survey-julia.md</files>
  <read_first>
    - .planning/phases/52-ecosystem-surveys/52-CONTEXT.md (Julia scope: JuliaStats / functional-data packages + explicit capture of Julia performance/idiom patterns as candidate gaps; capability-first breadth; version-pinning rule; standardized columns)
    - .planning/research/R-AUDIT-REPORT.md (capability-first section ordering to mirror; do NOT overwrite)
    - .planning/PROJECT.md `### Validated` section (the fdars shipped-capability baseline)
  </read_first>
  <action>
    Web-research (WebSearch/WebFetch) the actively-maintained Julia packages relevant to functional data analysis and the surrounding modeling stack, and pin the latest stable release of each as of 2026-09 as `pkg@version` (e.g. FDA/functional-data packages, plus JuliaStats members providing FDA-adjacent numeric capability: smoothing/basis, PCA/dimension-reduction, GLM/mixed models, clustering, distances, time-series — selecting which packages qualify as in-scope and actively maintained is Claude's discretion per CONTEXT). Enumerate every MAJOR capability CATEGORY (not every function) into a single capability-first inventory table in `.planning/research/survey-julia.md` using the exact six standardized columns: `Capability | Reference (pkg@ver) | fdars status (present/partial/absent) | Searched-for | Net-new? (vs BACKLOG/R-BACKLOG) | Notes`. In this task populate Capability + Reference (pkg@ver) fully. ADDITIONALLY enumerate Julia performance/idiom patterns as candidate capability rows where they represent a capability fdars lacks (e.g. type-stable/generic-numeric-type FDA APIs, GPU-friendly batched kernels, allocation-free streaming designs) — mark these clearly in Notes as "idiom/perf pattern". Open the file with a header naming the surveyed packages + pinned versions + survey date (2026-09) + the audit-only/test-coverage-N-A note. Group by area mirroring R-AUDIT-REPORT.md. Exclude plotting/visualization and data/IO (out-of-scope).
  </action>
  <verify>
    <automated>test -f .planning/research/survey-julia.md && grep -Eq '\.jl@|@[0-9]' .planning/research/survey-julia.md && grep -q 'Reference (pkg@ver)' .planning/research/survey-julia.md</automated>
    <fails_when>file absent, no `pkg@version` pins, or the standardized column header row is missing (non-zero exit).</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/research/survey-julia.md` exists with a header naming surveyed packages + pinned versions + survey date + the audit-only/test-coverage-N-A note.
    - A single capability inventory table uses the exact six standardized column headers.
    - At least one Julia performance/idiom pattern is captured as a candidate capability row marked "idiom/perf pattern" in Notes.
    - No plotting/visualization or data/IO rows (out-of-scope respected).
  </acceptance_criteria>
  <done>The Julia capability surface (incl. perf/idiom patterns) is enumerated capability-first with version pins in the standardized table.</done>
</task>

<task type="auto">
  <name>Task 2: Map fdars present/partial/absent with per-row "searched fdars for:" evidence</name>
  <files>.planning/research/survey-julia.md</files>
  <read_first>
    - .planning/PROJECT.md `### Validated` section (shipped-capability baseline)
    - fdars-core/src/ module tree (grep target: regression.rs, fpca_variants.rs, classification/, clustering.rs, clustering_advanced.rs, distance.rs, metric/, fts/, smoothing.rs, basis/, parallel.rs, streaming_depth/, etc.)
  </read_first>
  <action>
    For each capability row from Task 1 (including the perf/idiom pattern rows), determine the fdars status (present / partial / absent) by capability, NOT by API name. Grep `fdars-core/src/` for capability evidence (e.g. clustering → grep clustering.rs clustering_advanced.rs gmm/; distances/metrics → grep distance.rs metric/; parallelism/perf idioms → grep parallel.rs and reason about the feature-gated rayon model + column-major FdMatrix layout). Cross-check against the PROJECT.md Validated list. Fill the `fdars status` column. For EVERY absent/partial row, fill `Searched-for` with "searched fdars for: <grep terms> — <capability reasoning>". For perf/idiom rows, reason explicitly about whether fdars' feature-gated parallelism + column-major layout already covers the pattern (present/partial) or lacks it (absent, e.g. no GPU backend). Mark partial rows and describe the missing slice in Notes. Leave the `Net-new?` column for Task 3. Modify ONLY `.planning/research/survey-julia.md`.
  </action>
  <verify>
    <automated>grep -Eq 'present|partial|absent' .planning/research/survey-julia.md && grep -q 'searched fdars for:' .planning/research/survey-julia.md && git status --porcelain fdars-core/src/ | grep -q . && echo FENCE_VIOLATION || echo FENCE_OK</automated>
    <fails_when>no present/partial/absent verdicts, no "searched fdars for:" evidence, or the fence check prints FENCE_VIOLATION.</fails_when>
  </verify>
  <acceptance_criteria>
    - Every inventory row has a present/partial/absent verdict.
    - Every absent/partial row has a "searched fdars for: <terms> — <reasoning>" note.
    - Perf/idiom rows include reasoning about fdars' feature-gated parallelism / column-major layout.
    - `git status --porcelain fdars-core/src/` is empty.
  </acceptance_criteria>
  <done>fdars parity is mapped by capability with grep-evidenced notes on every absent/partial row (perf/idiom rows reasoned against the parallelism model), no source file touched.</done>
</task>

<task type="auto">
  <name>Task 3: De-dup against prior backlogs, emit the Net-New Gap List + reverse-parity note</name>
  <files>.planning/research/survey-julia.md</files>
  <read_first>
    - .planning/research/BACKLOG.md (v0.14.0 de-dup baseline)
    - .planning/research/R-BACKLOG.md (v0.18.0 de-dup baseline)
    - .planning/phases/52-ecosystem-surveys/52-CONTEXT.md (hard de-dup rule + reverse-parity requirement)
  </read_first>
  <action>
    For each absent/partial-with-missing-slice row, check the candidate gap by CAPABILITY against both BACKLOG.md and R-BACKLOG.md. Fill `Net-new?`: `yes` only if verified absent from shipped fdars AND both prior backlogs; else `no — matches <ID/title>`. Append a `## Net-New Gap List` table (same six standardized columns) filtered to `Net-new? = yes` rows only. Append a short `## Reverse-Parity Note` recording where fdars leads the Julia ecosystem (re-vet against Julia specifically — do not copy the R/scikit-fda sweep uncritically; explainability + streaming depth + SPM + conformal + elastic are candidate moats). Keep one gap per distinct capability/method-family. Modify ONLY `.planning/research/survey-julia.md`.
  </action>
  <verify>
    <automated>grep -q 'Net-New Gap List' .planning/research/survey-julia.md && grep -Eq 'Reverse-Parity|Reverse parity|reverse-parity' .planning/research/survey-julia.md && grep -Eq 'R-BACKLOG|BACKLOG' .planning/research/survey-julia.md</automated>
    <fails_when>the "Net-New Gap List" heading is missing, the reverse-parity note is missing, or no de-dup evidence against BACKLOG/R-BACKLOG (non-zero exit).</fails_when>
  </verify>
  <acceptance_criteria>
    - Every absent/partial candidate row has a `Net-new?` value (`yes` or `no — matches <prior item>`).
    - A `## Net-New Gap List` table (six columns) contains only net-new-yes rows.
    - A `## Reverse-Parity Note` records where fdars leads the Julia ecosystem.
    - De-dup reasoning references BACKLOG.md and/or R-BACKLOG.md.
    - `git status --porcelain fdars-core/src/` is empty.
  </acceptance_criteria>
  <done>The Julia survey emits a de-duplicated Net-New Gap List + reverse-parity note, ready for Phase 53 merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web sources → survey file | Version pins and capability claims come from external docs/registries; risk is inaccuracy, not code execution (audit-only, no installs). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52.02-01 | Information Disclosure | version pins / capability claims | low | mitigate | Pin `pkg@version` from primary sources (Julia General registry / package docs) and record the survey date. |
| T-52.02-02 | Tampering | fdars-core/src/ (audit fence) | high | mitigate | Audit-only fence: Task verifies `git status --porcelain fdars-core/src/` is empty; no installs, so the package-legitimacy gate is N/A this phase. |
</threat_model>

<verification>
- `test -f .planning/research/survey-julia.md` — deliverable exists.
- `grep -q 'Net-New Gap List' .planning/research/survey-julia.md` — net-new gap list present.
- `grep -q 'searched fdars for:' .planning/research/survey-julia.md` — per-row evidence present.
- `grep -q 'idiom/perf pattern' .planning/research/survey-julia.md` — perf/idiom patterns captured.
- `git status --porcelain fdars-core/src/` empty — audit-only fence held.
- Test coverage N/A: no `cargo test` expected; this milestone adds zero code.
</verification>

<success_criteria>
- `.planning/research/survey-julia.md` exists with a version-pinned capability-first inventory of the JuliaStats / functional-data ecosystem, including Julia perf/idiom patterns as candidate gaps.
- Every row has an fdars verdict; every absent/partial row has a "searched fdars for:" note.
- The file ends with a Net-New Gap List (net-new-only, six columns) + a reverse-parity note.
- De-dup against BACKLOG.md and R-BACKLOG.md is evidenced.
- Zero `fdars-core/src/` edits.
</success_criteria>

<output>
Create `.planning/phases/52-ecosystem-surveys/52-02-SUMMARY.md` when done.
</output>
