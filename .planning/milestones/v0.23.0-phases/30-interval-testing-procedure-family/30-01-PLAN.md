---
phase: 30-interval-testing-procedure-family
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/inference/itp.rs
  - fdars-core/src/inference/mod.rs
  - fdars-core/src/lib.rs
  - fdars-core/src/basis/projection.rs
autonomous: true
requirements: [INF-03]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Private `pval_correct(pval_matrix, p)` implements the ITP interval-wise closure max-adjustment (adjusted p for component k = max joint permutation p-value over all contiguous intervals containing k), verified by a hand-computed ~4-component unit test (RESEARCH Open Question 1)."
    - "Public `itp_one_pop(...) -> Result<ItpResult, FdarError>` is wired end-to-end: fdata_to_basis projection -> inline sign-flip permutation loop -> raw per-component p-values -> pval_correct -> ItpResult, and is crate-root re-exported."
    - "On a one-sample localized-shift fixture the adjusted p-values are small on the shifted sub-interval; on a null (no-shift) fixture they are non-significant everywhere (seeded, tolerance-based)."
    - "Invalid inputs to itp_one_pop (n < 2, argvals.len() != m, nbasis < 2, n_perm == 0) return FdarError, never panic."
    - "Existing inference (INF-01/INF-02) and basis/ public signatures are unchanged; the crate compiles and the full suite + clippy --all-targets --features linalg,parallel -- -D warnings stays green after this plan."
  artifacts:
    - fdars-core/src/inference/itp.rs
    - "fdars-core/src/inference/mod.rs (mod itp; + pub use)"
    - "fdars-core/src/lib.rs (extended inference re-export block)"
  key_links:
    - "fdata_to_basis returns Option -> bridged with .ok_or_else to FdarError; proj.n_basis used as p (not requested nbasis)."
    - "ItpResult stores basis_type: ProjectionBasisType; conditional serde on ItpResult requires serde derives on ProjectionBasisType (additive attribute)."
    - "raw_pvalues use (n_ge + 1)/(n_perm + 1); Fisher combination clamps each p-value with .max(1e-300).ln()."
---

<objective>
Prove the entire ITP machinery end-to-end on the thinnest complete path: the interval-wise
closure-adjustment helper `pval_correct` (the trickiest index math in the phase) plus the
one-population interval-wise test `itp_one_pop`, fully wired from basis projection through the
inline permutation loop to `ItpResult` and crate-root re-export. This is the tracer slice: the
two-population and FLM tests in plan 02 follow the identical path, so proving it here — with a
hand-computed `pval_correct` unit test — de-risks the whole phase after one commit.

Purpose: Lock the closure-adjustment arithmetic and the projection -> permutation -> adjustment
pipeline before expanding to the other two entry points.
Output: `fdars-core/src/inference/itp.rs` (ItpResult + itp_one_pop + all private helpers +
inline tests), wired into `inference/mod.rs` and `lib.rs`; `ProjectionBasisType` gains conditional
serde derives so `ItpResult` can derive conditional serde.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-interval-testing-procedure-family/30-RESEARCH.md
@.planning/phases/30-interval-testing-procedure-family/30-PATTERNS.md
@.planning/phases/30-interval-testing-procedure-family/30-CONTEXT.md
</context>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan (plan 02 adds itp_two_pop, itp_flm reusing these):
- `ItpResult` struct — pub, in `inference/itp.rs`: fields `adjusted_pvalues: Vec<f64>`,
  `raw_pvalues: Vec<f64>`, `basis_type: ProjectionBasisType`, `n_basis: usize`, `n_perm: usize`;
  derives `Debug, Clone, PartialEq`, `#[non_exhaustive]`, conditional serde.
- `pub fn itp_one_pop(data: &FdMatrix, argvals: &[f64], mu0: Option<&[f64]>,
  basis_type: ProjectionBasisType, nbasis: usize, n_perm: usize, seed: u64) -> Result<ItpResult, FdarError>`.
- Private helpers in `itp.rs`: `pval_correct`, `build_pval_matrix`, `rank_transform`, `fisher_cf`,
  `shuffle_itp` (Fisher-Yates copy), `pool_coefficients_itp` (added/used by plan 02, may stub or add now).
- Re-exports: `inference/mod.rs` (`mod itp; pub use itp::{itp_one_pop, ItpResult}` — extended to add
  `itp_two_pop, itp_flm` in plan 02), and `lib.rs` inference block extended with `itp_one_pop, ItpResult`.
- `ProjectionBasisType` (basis/projection.rs) gains `#[cfg_attr(feature = "serde", derive(...))]`
  (additive attribute; no signature change) so `ItpResult` conditional serde compiles.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: pval_correct closure helper + hand-computed unit test (tracer core)</name>
  <files>fdars-core/src/inference/itp.rs</files>
  <read_first>
    - .planning/phases/30-interval-testing-procedure-family/30-RESEARCH.md — "Pattern 4: The Closure
      Adjustment Algorithm — PINNED EXACTLY" (Step A rank_transform, Step B build_pval_matrix,
      Step C pval_correct) and Pitfalls 3 (column reversal), 4 (log-safe Fisher), Open Question 1.
    - .planning/phases/30-interval-testing-procedure-family/30-PATTERNS.md — "Pitfall Reminders"
      table (Fisher log-safe, pval_correct reverse) and "iter_maybe_parallel! usage".
  </read_first>
  <behavior>
    - rank_transform(t_perm, p, n_perm): for each component k, largest permutation stat -> pseudo-p
      1/B, smallest -> B/B = 1.0 (descending rank / B). Returns L of shape (n_perm, p).
    - fisher_cf(vals): -2 * sum(v.max(1e-300).ln()) — never NaN/-inf on a 0.0 p-value.
    - build_pval_matrix(raw_pvalues, l, p, n_perm): row p-1 holds raw per-component p-values;
      rows for interval_len 2..=p hold each contiguous interval's joint permutation p-value via
      the doubled-array circular wrap; O(p^2) interval loop.
    - pval_correct(pval_matrix, p): adjusted p for component k = max joint p-value over all
      contiguous intervals containing k (cone walk on the doubled+reversed matrix), then reverse
      to restore natural component order.
    - Hand-computed test (RESEARCH Open Question 1): with a fixed small pval_matrix for p ~= 4,
      the returned adjusted_pvalues equal the by-hand max-over-containing-intervals values exactly.
  </behavior>
  <action>
    Create `fdars-core/src/inference/itp.rs`. Add imports per 30-PATTERNS.md
    ("Imports pattern" adapted): `use crate::basis::projection::{fdata_to_basis, ProjectionBasisType};`,
    `use crate::error::FdarError;`, `use crate::matrix::FdMatrix;`, `use rand::rngs::StdRng;`,
    `use rand::SeedableRng;` (and `use crate::iter_maybe_parallel;` for the O(p^2) loop; keep
    `use rand::Rng;` local to the future permutation/shuffle scope).
    Implement the four private helpers exactly per RESEARCH Pattern 4:
    `fn rank_transform(t_perm: &[Vec<f64>], p: usize, b: usize) -> Vec<Vec<f64>>` (descending
    rank/B); `fn fisher_cf` as a closure or fn applying `-2.0 * sum(v.max(1e-300).ln())`;
    `fn build_pval_matrix(raw_pvalues: &[f64], l: &[Vec<f64>], p: usize, n_perm: usize) -> Vec<Vec<f64>>`
    using the doubled `pval_2x` / `l_2x` circular arrays and the `interval_len in 2..=p`,
    `row_idx = p - interval_len` layout from RESEARCH Step B; and
    `fn pval_correct(pval_matrix: &[Vec<f64>], p: usize) -> Vec<f64>` using the `get_2x_rev`
    doubled+reversed cone walk from RESEARCH Step C, ending with `corrected.reverse()`.
    Parallelize the per-component rank loop and the O(p^2) interval-row build with
    `iter_maybe_parallel!(0..p)` where the computation is independent (per 30-PATTERNS.md);
    do NOT parallelize anything sharing a single RNG. Add an inline
    `#[cfg(test)] mod tests { use super::*; ... }` at the bottom containing
    `fn pval_correct_hand_computed()`: construct a fixed `pval_matrix` for p in {3,4}, compute the
    expected adjusted vector by hand (max over containing intervals per component), and assert the
    helper output equals it (exact f64 equality on the hand-picked matrix, or within 1e-12).
    Document in a rustdoc comment on pval_correct that the closure implements Pini & Vantini (2016)
    interval-wise closure matching fdatest::ITP1bspline's pval.correct, and note the deliberate
    `(n_ge + 1)/(n_perm + 1)` raw-p divergence from R's `/B` (RESEARCH Assumption A4). Do not add
    itp_one_pop yet (task 2). File need not be wired into mod.rs yet; use `#[allow(dead_code)]` on
    helpers only if the compiler flags them before task 2 wires them — remove the allow in task 2.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib inference::itp::tests::pval_correct_hand_computed</automated>
  </verify>
  <acceptance_criteria>
    - `fn pval_correct(pval_matrix: &[Vec<f64>], p: usize) -> Vec<f64>`, `fn build_pval_matrix(...)`,
      `fn rank_transform(...)`, and a Fisher combining function all exist in itp.rs.
    - `inference::itp::tests::pval_correct_hand_computed` exists and exits 0: the adjusted vector
      equals the hand-computed max-over-containing-intervals values for a fixed p~=4 pval_matrix.
    - fisher_cf clamps with `.max(1e-300)` (no NaN on a 0.0 input) — asserted or exercised by the test.
    - pval_correct ends with a `.reverse()` restoring natural component order.
  </acceptance_criteria>
  <done>The closure-adjustment helpers exist and the hand-computed pval_correct test passes, locking the trickiest index math before the entry point is wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: itp_one_pop end-to-end + ItpResult + serde + mod.rs/lib.rs wiring</name>
  <files>fdars-core/src/inference/itp.rs, fdars-core/src/inference/mod.rs, fdars-core/src/lib.rs, fdars-core/src/basis/projection.rs</files>
  <read_first>
    - .planning/phases/30-interval-testing-procedure-family/30-RESEARCH.md — "Code Examples"
      (Complete One-Population Entry Point Skeleton + ItpResult Struct), "Pattern 1"
      (observed/perm sign-flip stat), "Minimum-n and Basis-Parameter Guards", "Integration Edit
      Points (Exact)" sections 1-3, Pitfalls 1 (Option bridge), 2 (proj.n_basis as p).
    - .planning/phases/30-interval-testing-procedure-family/30-PATTERNS.md — "entry points" analog
      (validation, seeded loop, p-value formula), "ItpResult" derive stack + the note that
      ProjectionBasisType lacks serde, "mod.rs (edit)" and "lib.rs (edit)" exact blocks.
    - fdars-core/src/inference/itp.rs — the helpers created in task 1.
    - fdars-core/src/basis/projection.rs — ProjectionBasisType enum (derives) + fdata_to_basis signature.
  </read_first>
  <behavior>
    - itp_one_pop happy path: on a one-sample fixture with a constant shift confined to a known
      sub-interval of argvals, adjusted_pvalues are < 0.05 for components whose basis support
      overlaps the shifted interval and > 0.10 for components fully outside it (seeded, tolerance).
    - itp_one_pop null: no shift -> all adjusted_pvalues > 0.05 (or max adjusted p > 0.10), seeded.
    - Determinism: same (data, argvals, mu0, basis_type, nbasis, n_perm, seed) -> bit-identical ItpResult.
    - Error paths: n < 2 -> InvalidDimension; argvals.len() != m -> InvalidDimension; nbasis < 2 ->
      InvalidParameter; n_perm == 0 -> InvalidParameter. Never panics.
    - ItpResult.n_basis equals proj.n_basis (may differ from requested nbasis for B-spline).
  </behavior>
  <action>
    Add the `ItpResult` struct to itp.rs with the exact fields and derive stack from
    30-PATTERNS.md ("ItpResult" section): `adjusted_pvalues: Vec<f64>`, `raw_pvalues: Vec<f64>`,
    `basis_type: ProjectionBasisType`, `n_basis: usize`, `n_perm: usize`; derive
    `Debug, Clone, PartialEq`, `#[non_exhaustive]`, and
    `#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]`. Because
    `ProjectionBasisType` (basis/projection.rs) does NOT derive serde and is a field of ItpResult,
    add `#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]` to the
    `ProjectionBasisType` enum in basis/projection.rs (purely additive attribute — no signature or
    variant change; this is the minimal fix for the CONTEXT-locked conditional serde on ItpResult).
    Implement `pub fn itp_one_pop(data, argvals, mu0: Option<&[f64]>, basis_type, nbasis, n_perm, seed)
    -> Result<ItpResult, FdarError>` following the RESEARCH "Complete One-Population Entry Point
    Skeleton": validate in order (n < 2, argvals.len() != m, nbasis < 2, n_perm == 0) using the
    permutation.rs validation analog (InvalidDimension / InvalidParameter); subtract mu0 elementwise
    if Some (a private centering helper) else use data as-is; project once via
    `fdata_to_basis(&centered, argvals, nbasis, basis_type).ok_or_else(|| FdarError::InvalidParameter { parameter: "nbasis", message: ... })?`;
    set `p = proj.n_basis` and reference proj.coefficients for all loops (Pitfall 2). Compute the
    observed per-component statistic `t0[k] = |mean(coeff[:, k])|` (Pattern 1). Run a single
    sequential `for _ in 0..n_perm` loop with one `StdRng::seed_from_u64(seed)`, drawing per-curve
    signs (±1.0 from `rng.gen::<bool>()`, `use rand::Rng;` in scope) and pushing the per-component
    `|mean(coeff[i,k]*signs[i])|` row into `t_perm` (n_perm, p). Compute `raw_pvalues[k] =
    (n_ge + 1)/(n_perm + 1)` per Pattern's p-value formula. Call the task-1 helpers:
    `rank_transform` -> L, `build_pval_matrix` -> pval_matrix, `pval_correct` -> adjusted_pvalues.
    Return `ItpResult { adjusted_pvalues, raw_pvalues, basis_type, n_basis: p, n_perm }`. Remove any
    `#[allow(dead_code)]` added in task 1. Wire the module: in `inference/mod.rs` add `mod itp;` after
    `mod scb;` and `pub use itp::{itp_one_pop, ItpResult};` (plan 02 extends this to add itp_two_pop,
    itp_flm — leave room). In `lib.rs` extend the inference re-export block (lines ~225-229) to add
    `itp_one_pop` and `ItpResult` per 30-PATTERNS.md "lib.rs (edit)". Add inline tests in the same
    `#[cfg(test)] mod tests`: `one_population_localized` (localized shift on argvals ~0.4..=0.6,
    nbasis=15 B-spline, seed=42, n_perm=499, assert small adjusted p on overlapping components,
    large elsewhere), `one_population_null` (no shift, assert all adjusted p > 0.05 or max > 0.10),
    `one_population_deterministic` (two identical calls -> assert_eq! results), and
    `one_population_error_paths` (n<2, argvals mismatch, nbasis<2, n_perm==0 -> matches!(.., Err(...))).
    Use `crate::test_helpers::uniform_grid` for argvals per the PATTERNS test analog. Verify the
    serde feature still compiles (the conditional-serde ItpResult path).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib inference::itp && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --features serde && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `pub fn itp_one_pop(data: &FdMatrix, argvals: &[f64], mu0: Option<&[f64]>, basis_type: ProjectionBasisType, nbasis: usize, n_perm: usize, seed: u64) -> Result<ItpResult, FdarError>` exists.
    - `pub struct ItpResult` exists with fields adjusted_pvalues, raw_pvalues, basis_type, n_basis, n_perm and the derive stack (Debug, Clone, PartialEq, non_exhaustive, conditional serde).
    - `ProjectionBasisType` has `#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]`; `cargo build -p fdars-core --features serde` compiles.
    - `itp_one_pop` and `ItpResult` are re-exported from `inference/mod.rs` and from crate root (`fdars_core::itp_one_pop`, `fdars_core::ItpResult`).
    - `cargo test ... --lib inference::itp` exits 0 (localized, null, deterministic, error_paths all pass).
    - `cargo clippy --all-targets --features linalg,parallel -- -D warnings` exits 0; existing inference/basis public signatures unchanged.
  </acceptance_criteria>
  <reversibility rating="costly">ItpResult is a published API contract; its shape is fixed by CONTEXT/RESEARCH so this is a flag only, no checkpoint.</reversibility>
  <done>itp_one_pop runs end-to-end (projection -> permutation -> pval_correct -> ItpResult), is crate-root re-exported, passes localized/null/deterministic/error tests, and the crate + serde build + clippy are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> itp_one_pop | Untrusted matrix shapes / basis params cross here; validated at function entry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Denial of Service | itp_one_pop (bad dims / nbasis / n_perm) | low | mitigate | Entry-point guards return FdarError (n<2, argvals mismatch, nbasis<2, n_perm==0); never panic. Covered by one_population_error_paths test. |
| T-30-02 | Tampering | Fisher combination on 0.0 p-values | low | mitigate | Clamp with `.max(1e-300).ln()` to avoid NaN/-inf poisoning adjusted p-values. |
| T-30-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new crate dependency added this phase (RESEARCH Package Legitimacy Audit: none); no install step. |
</threat_model>

<verification>
- `cargo test -p fdars-core --features linalg,parallel --lib inference::itp` — all itp tests green (pval_correct hand-computed, one-population localized/null/deterministic/error).
- `cargo build -p fdars-core --features serde` — conditional serde on ItpResult + ProjectionBasisType compiles.
- `cargo clippy --all-targets --features linalg,parallel -- -D warnings` — green.
- Existing inference (INF-01/INF-02) and basis/ public signatures unchanged (additive only).
</verification>

<success_criteria>
- pval_correct hand-computed unit test passes (closure index math locked).
- itp_one_pop wired end-to-end and crate-root re-exported; localized-shift adjusted p small on true interval, null non-significant everywhere.
- Invalid inputs return FdarError; no panics.
- Crate + serde build + full clippy green.
</success_criteria>

<output>
Create `.planning/phases/30-interval-testing-procedure-family/30-01-SUMMARY.md` when done.
</output>
