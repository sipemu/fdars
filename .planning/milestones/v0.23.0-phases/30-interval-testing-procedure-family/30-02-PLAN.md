---
phase: 30-interval-testing-procedure-family
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - fdars-core/src/inference/itp.rs
  - fdars-core/src/inference/mod.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [INF-03]

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Public `itp_two_pop(...) -> Result<ItpResult, FdarError>` reuses the plan-01 pval_correct/rank_transform/build_pval_matrix helpers with a pool + Fisher-Yates relabel permutation null and the per-component two-sample statistic |colMean(a) - colMean(b)|."
    - "Public `itp_flm(...) -> Result<ItpResult, FdarError>` reuses the same closure helpers with a per-component simple-regression t-statistic and a response-permutation null (shuffle y)."
    - "On a two-population localized-difference fixture the adjusted p-values are small on the true differing sub-interval and non-significant elsewhere; on a null (equal-means) fixture non-significant everywhere; two_pop is seeded-deterministic (same seed -> identical ItpResult)."
    - "On an itp_flm fixture with a genuine functional effect the adjusted p-values are small in the coefficient region; on a null (y independent of X) fixture non-significant everywhere."
    - "Error paths for both entry points (n_a<2 || n_b<2, mismatched columns, argvals mismatch, nbasis<2, n_perm==0, and for itp_flm y.len() != n) return FdarError, never panic; existing signatures unchanged and full suite + clippy --all-targets --features linalg,parallel -- -D warnings green."
  artifacts:
    - "fdars-core/src/inference/itp.rs (itp_two_pop, itp_flm + pool/relabel + component_t_stat helpers)"
    - "fdars-core/src/inference/mod.rs (pub use extended with itp_two_pop, itp_flm)"
    - "fdars-core/src/lib.rs (inference re-export block extended with itp_two_pop, itp_flm)"
  key_links:
    - "Both entry points call the shared plan-01 helpers (rank_transform, build_pval_matrix, pval_correct, fisher_cf) — no new closure engine."
    - "Two-pop pools coefficient rows (shape (n_a+n_b, p)) and relabels via an inline Fisher-Yates (shuffle_itp); itp_flm shuffles the response vector y."
    - "component_t_stat guards sxx < 1e-30 and se2 <= 0.0 by returning 0.0 (FLM degrees-of-freedom guard)."
---

<objective>
Expand the proven plan-01 tracer path to the remaining two ITP entry points: the two-population
interval-wise test (`itp_two_pop`, pool + relabel null, mean-difference statistic) and the
interval-wise FLM coefficient test (`itp_flm`, response-permutation null, per-component regression
t-statistic). Both reuse the plan-01 closure helpers (rank_transform, build_pval_matrix,
pval_correct, fisher_cf) unchanged — this plan adds only the two entry points, their permutation
schemes, and their fixtures.

Purpose: Complete the ITP family (INF-03) so a user has all three interval-wise tests.
Output: `itp_two_pop` and `itp_flm` in `inference/itp.rs`, re-exported from `inference/mod.rs`
and `lib.rs`, with inline localized/null/deterministic/error tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-interval-testing-procedure-family/30-RESEARCH.md
@.planning/phases/30-interval-testing-procedure-family/30-PATTERNS.md
@.planning/phases/30-interval-testing-procedure-family/30-01-SUMMARY.md
@fdars-core/src/inference/itp.rs
</context>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan (reusing plan-01 ItpResult + closure helpers):
- `pub fn itp_two_pop(data_a: &FdMatrix, data_b: &FdMatrix, argvals: &[f64],
  basis_type: ProjectionBasisType, nbasis: usize, n_perm: usize, seed: u64) -> Result<ItpResult, FdarError>`.
- `pub fn itp_flm(data: &FdMatrix, y: &[f64], argvals: &[f64],
  basis_type: ProjectionBasisType, nbasis: usize, n_perm: usize, seed: u64) -> Result<ItpResult, FdarError>`.
- Private helpers in itp.rs: `shuffle_itp` (Fisher-Yates copy of permutation.rs shuffle_labels,
  if not already present), `pool_coefficients_itp` (pools two coefficient matrices, shape (n_a+n_b, p)),
  `component_t_stat` (simple-regression per-component t-statistic for itp_flm).
- Re-exports extended: `inference/mod.rs` `pub use itp::{itp_flm, itp_one_pop, itp_two_pop, ItpResult}`;
  `lib.rs` inference block adds `itp_flm, itp_two_pop`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: itp_two_pop (pool + relabel) + tests + wiring</name>
  <files>fdars-core/src/inference/itp.rs, fdars-core/src/inference/mod.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - .planning/phases/30-interval-testing-procedure-family/30-RESEARCH.md — "Pattern 2:
      Two-Population Pool + Relabel" (observed_stat_two_pop / perm_stat_two_pop), "Permutation
      Infrastructure: Reuse vs. In-File Loop" (shuffle inline), "Minimum-n and Basis-Parameter
      Guards" (n_a<2 || n_b<2).
    - .planning/phases/30-interval-testing-procedure-family/30-PATTERNS.md — "Input validation
      pattern" (validate_two_samples analog), "Pool helper" + "Fisher-Yates shuffle helper"
      (both private in permutation.rs — copy inline), "p-value formula", "Test structure".
    - fdars-core/src/inference/itp.rs — plan-01 ItpResult + rank_transform/build_pval_matrix/pval_correct/fisher_cf.
  </read_first>
  <behavior>
    - itp_two_pop localized: group B = group A + constant shift on argvals ~0.4..=0.6, nbasis=15
      B-spline, seed=42, n_perm=499 -> adjusted p < 0.05 on components overlapping the shift, > 0.10 elsewhere.
    - itp_two_pop null: A, B same distribution -> all adjusted p > 0.05 (or max > 0.10).
    - Determinism: same inputs+seed -> assert_eq! identical ItpResult.
    - Error paths: n_a<2 || n_b<2 -> InvalidDimension; m_a != m_b -> InvalidDimension;
      argvals mismatch -> InvalidDimension; nbasis<2 -> InvalidParameter; n_perm==0 -> InvalidParameter.
  </behavior>
  <action>
    Add `pub fn itp_two_pop(data_a, data_b, argvals, basis_type, nbasis, n_perm, seed)
    -> Result<ItpResult, FdarError>` to itp.rs. Validate with the `validate_two_samples` analog
    from 30-PATTERNS.md (m_a==0/m_b==0, m_a != m_b, argvals.len() != m_a, n_a<2 || n_b<2), then
    nbasis<2 and n_perm==0. Project each group separately via `fdata_to_basis(.., basis_type)
    .ok_or_else(..)?` and take `p = min` consistency by projecting on the same nbasis; pool the two
    coefficient matrices into one (n_a+n_b, p) matrix via a new private
    `fn pool_coefficients_itp(coeff_a, coeff_b, n_a, n_b, p) -> FdMatrix` copying the permutation.rs
    pool pattern but over coefficient columns. Use `proj_a.n_basis` (== proj_b.n_basis for the same
    nbasis/basis) as p and store it in ItpResult.n_basis. Compute observed
    `t0[k] = |colMean(coeff[0..n_a, k]) - colMean(coeff[n_a..n, k])|` (RESEARCH Pattern 2
    observed_stat_two_pop). Run a single sequential `for _ in 0..n_perm` loop with one
    `StdRng::seed_from_u64(seed)`: maintain an index vector `0..n`, relabel it in place with an
    inline `fn shuffle_itp(v: &mut [usize], rng: &mut StdRng)` (copy the 7-line Fisher-Yates from
    permutation.rs shuffle_labels; add it if plan 01 did not), then compute the per-component
    permuted statistic per RESEARCH perm_stat_two_pop and push the row into t_perm. Compute
    `raw_pvalues[k] = (n_ge + 1)/(n_perm + 1)`, then call the plan-01 helpers rank_transform ->
    build_pval_matrix -> pval_correct to get adjusted_pvalues. Return
    `ItpResult { adjusted_pvalues, raw_pvalues, basis_type, n_basis: p, n_perm }`. Wire the
    re-exports: extend `inference/mod.rs` `pub use itp::{...}` to include `itp_two_pop`, and extend
    the `lib.rs` inference block to add `itp_two_pop`. Add inline tests in the existing
    `#[cfg(test)] mod tests`: `two_population_localized`, `two_population_null`,
    `two_population_deterministic` (assert_eq! two identical calls), `two_population_error_paths`
    (n_a<2, m mismatch, nbasis<2, n_perm==0 -> matches!(.., Err(...))), using
    `crate::test_helpers::uniform_grid` and a seeded fixture builder per the PATTERNS test analog.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib inference::itp::tests::two_population && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `pub fn itp_two_pop(data_a: &FdMatrix, data_b: &FdMatrix, argvals: &[f64], basis_type: ProjectionBasisType, nbasis: usize, n_perm: usize, seed: u64) -> Result<ItpResult, FdarError>` exists and is re-exported from crate root (`fdars_core::itp_two_pop`).
    - Private `pool_coefficients_itp` and `shuffle_itp` (Fisher-Yates) exist in itp.rs; the permutation loop is a single sequential loop over one StdRng.
    - `cargo test ... --lib inference::itp::tests::two_population` exits 0 (localized small on true interval, null non-significant, deterministic assert_eq!, error paths return FdarError).
    - `cargo clippy --all-targets --features linalg,parallel -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>itp_two_pop runs end-to-end reusing the plan-01 closure helpers, is crate-root re-exported, passes localized/null/deterministic/error tests, and clippy is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: itp_flm (response permutation + per-component t-stat) + tests + wiring</name>
  <files>fdars-core/src/inference/itp.rs, fdars-core/src/inference/mod.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - .planning/phases/30-interval-testing-procedure-family/30-RESEARCH.md — "Pattern 3:
      Interval-Wise FLM Coefficient Test" (component_t_stat + response-permutation), Open Question 2
      (raw (data, y, argvals) signature), Assumption A2 (response-permutation simplification),
      "Minimum-n and Basis-Parameter Guards" (y.len() != n, n < p+2 df guard).
    - .planning/phases/30-interval-testing-procedure-family/30-PATTERNS.md — entry-point validation
      + seeded loop + p-value formula analogs; "mod.rs (edit)" / "lib.rs (edit)" final target blocks.
    - fdars-core/src/inference/itp.rs — plan-01 closure helpers + plan-02 task-1 shuffle_itp.
  </read_first>
  <behavior>
    - itp_flm effect: y = linear functional of X with signal localized to a coefficient region ->
      adjusted p < 0.05 on components in that region, > 0.10 elsewhere (seeded, tolerance).
    - itp_flm null: y independent of X (shuffled / random y) -> all adjusted p > 0.05 (or max > 0.10).
    - Error paths: y.len() != n -> InvalidDimension; n<2 -> InvalidDimension; argvals mismatch ->
      InvalidDimension; nbasis<2 -> InvalidParameter; n_perm==0 -> InvalidParameter.
    - component_t_stat returns 0.0 when sxx < 1e-30 or se2 <= 0.0 (degrees-of-freedom / degenerate guard).
  </behavior>
  <action>
    Add `pub fn itp_flm(data, y, argvals, basis_type, nbasis, n_perm, seed) -> Result<ItpResult, FdarError>`
    to itp.rs, accepting raw `(data, y, argvals, ...)` per RESEARCH Open Question 2 (not a pre-fitted
    FregreLmResult). Validate n<2, argvals.len() != m, `y.len() != n` (InvalidDimension), nbasis<2,
    n_perm==0. Project X once via `fdata_to_basis(.., basis_type).ok_or_else(..)?`; set p = proj.n_basis.
    Add a private `fn component_t_stat(y: &[f64], coeff: &FdMatrix, k: usize) -> f64` implementing the
    simple-regression |beta_hat / se| from RESEARCH Pattern 3 with the guards (sxx < 1e-30 -> 0.0,
    se2 <= 0.0 -> 0.0). Observed `t0[k] = component_t_stat(y, &coeff, k)`. Permutation null =
    response permutation: single sequential `for _ in 0..n_perm` loop with one
    `StdRng::seed_from_u64(seed)`, build a permuted index vector via the plan-02 `shuffle_itp`, form
    `y_perm[i] = y[perm[i]]`, then push the per-component `component_t_stat(&y_perm, &coeff, k)` row
    into t_perm. Compute `raw_pvalues[k] = (n_ge + 1)/(n_perm + 1)`, then reuse the plan-01 helpers
    rank_transform -> build_pval_matrix -> pval_correct. Return the ItpResult. In the rustdoc, document
    (RESEARCH Assumption A2) that this uses the response-permutation simplification testing the global
    null "y independent of the functional predictor" rather than R ITPlmbspline's partial-residual
    method. Finalize the re-exports so the whole family is exported: `inference/mod.rs`
    `pub use itp::{itp_flm, itp_one_pop, itp_two_pop, ItpResult};` and the `lib.rs` inference block
    includes `itp_flm, itp_one_pop, itp_two_pop, ItpResult` (per 30-PATTERNS.md final "lib.rs (edit)"
    block). Add inline tests `flm_effect`, `flm_null`, `flm_error_paths` (y.len() mismatch, nbasis<2,
    n_perm==0 -> matches!(.., Err(...))) using a seeded fixture where the response is a known linear
    functional of a localized region of X.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib inference::itp && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --features serde && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `pub fn itp_flm(data: &FdMatrix, y: &[f64], argvals: &[f64], basis_type: ProjectionBasisType, nbasis: usize, n_perm: usize, seed: u64) -> Result<ItpResult, FdarError>` exists and is crate-root re-exported (`fdars_core::itp_flm`).
    - Private `component_t_stat` exists with the sxx/se2 guards returning 0.0; the null uses response permutation (shuffled y) via shuffle_itp.
    - All three entry points (itp_one_pop, itp_two_pop, itp_flm) + ItpResult are exported from both `inference/mod.rs` and crate root.
    - `cargo test ... --lib inference::itp` exits 0 (full itp module: pval_correct, one/two-population, flm effect/null/error).
    - `cargo build -p fdars-core --features serde` compiles and `cargo clippy --all-targets --features linalg,parallel -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>itp_flm runs end-to-end reusing the shared closure helpers, all three ITP entry points are re-exported, the full itp test module + serde build + full clippy are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> itp_two_pop / itp_flm | Untrusted matrix shapes, group sizes, response length, basis params cross here; validated at entry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-03 | Denial of Service | itp_two_pop (n_a<2 / mismatched columns / bad params) | low | mitigate | validate_two_samples-style guards return FdarError; covered by two_population_error_paths. |
| T-30-04 | Denial of Service | itp_flm (y.len() != n / degenerate regression) | low | mitigate | y-length guard returns FdarError; component_t_stat guards sxx<1e-30 and se2<=0.0 -> 0.0 (no divide-by-zero / NaN). Covered by flm_error_paths. |
| T-30-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new crate dependency added this phase (RESEARCH Package Legitimacy Audit: none); no install step. |
</threat_model>

<verification>
- `cargo test -p fdars-core --features linalg,parallel --lib inference::itp` — full itp module green (pval_correct, one/two-population, flm).
- `cargo build -p fdars-core --features serde` — conditional serde still compiles.
- `cargo clippy --all-targets --features linalg,parallel -- -D warnings` — green.
- Existing inference/basis public signatures unchanged (additive only).
</verification>

<success_criteria>
- itp_two_pop and itp_flm implemented reusing the plan-01 closure helpers; all three entry points crate-root re-exported.
- Localized-difference adjusted p small on true interval, null non-significant everywhere, for both two-pop and flm.
- Invalid inputs return FdarError; no panics.
- Full suite + serde build + full clippy green.
</success_criteria>

<output>
Create `.planning/phases/30-interval-testing-procedure-family/30-02-SUMMARY.md` when done.
</output>
