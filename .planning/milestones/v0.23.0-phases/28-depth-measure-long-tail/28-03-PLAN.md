---
phase: 28-depth-measure-long-tail
plan: 03
type: execute
wave: 3
depends_on: [28-02]
files_modified:
  - fdars-core/src/depth/tvd.rs
  - fdars-core/src/depth/mod.rs
  - fdars-core/src/depth/dispatch.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [DEPTH-01]
estimate:
  tokens: 55000
  raw_tokens: 27000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "total_variation_depth_1d returns Result<TvdMssResult, FdarError> with tvd and mss vectors each of length nrows (per DEPTH-01)"
    - "TvdMssResult { tvd: Vec<f64>, mss: Vec<f64> } derives Debug, Clone, PartialEq, is #[non_exhaustive], has conditional serde, and is crate-root re-exported — PINNED interface for Phase 29's tvdmss reuse"
    - "functional_depth(data, DepthMethod::TotalVariation) dispatches to total_variation_depth_1d(data, data)?.tvd; existing variants + signature unchanged"
    - "The median-ranked-at-every-t curve gets maximum TVD (≈0.25); magnitude outliers get low TVD; shape outliers get low MSS on synthetic data (inline tests)"
    - "A flat curve (zero total variation) yields MSS == 0.0 (guarded division), not NaN"
    - "Empty matrix returns FdarError; n<3 returns FdarError (never panic) for total_variation_depth_1d and the TotalVariation dispatch arm"
    - "All 9 DepthMethod variants round-trip through functional_depth; existing FraimanMuniz/Band/ModifiedBand/RandomProjection keep working unchanged (regression test)"
    - "No new crate dependency added; full suite + cargo clippy --all-targets --features linalg,parallel -- -D warnings stays green"
  artifacts:
    - fdars-core/src/depth/tvd.rs
  key_links:
    - "TvdMssResult is re-exported from mod.rs + lib.rs; DepthMethod::TotalVariation projects the .tvd field; Phase 29 consumes total_variation_depth_1d directly for the full struct"
---

<objective>
Add the two-component total-variation depth with MSSI in `tvd.rs`, returning a dedicated `TvdMssResult { tvd, mss }` struct (NOT a plain Vec — Phase 29's `tvdmss` outlier detector consumes both fields). Wire `DepthMethod::TotalVariation` (projecting `.tvd`) and complete the crate-root re-exports, then verify all 9 measures round-trip through the dispatcher.

The `TvdMssResult` struct is the one deliberately-pinned public interface of this phase: Phase 29 calls `total_variation_depth_1d(data, data)?` directly for the full struct, so its shape (two named `Vec<f64>` fields, `#[non_exhaustive]`, conditional serde) is a forward contract.

Purpose: Complete the fdaoutlier depth catalog and pin the TVD+MSSI interface for Phase 29 (DEPTH-01).
Output: `tvd.rs` (`total_variation_depth_1d` + `TvdMssResult`), `DepthMethod::TotalVariation` arm, mod.rs + lib.rs re-exports, all-9-variant round-trip test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-depth-measure-long-tail/28-CONTEXT.md
@.planning/phases/28-depth-measure-long-tail/28-RESEARCH.md
@.planning/phases/28-depth-measure-long-tail/28-PATTERNS.md
@fdars-core/src/depth/dispatch.rs
@fdars-core/src/depth/mod.rs
</context>

<artifacts_produced>
## Artifacts this plan produces
- `total_variation_depth_1d(&FdMatrix, &FdMatrix) -> Result<TvdMssResult, FdarError>` (tvd.rs) + private `column_ranks` helper
- `TvdMssResult { tvd: Vec<f64>, mss: Vec<f64> }` struct — PINNED for Phase 29
- DepthMethod variant: `TotalVariation` (dispatches `.tvd`)
- file: tvd.rs
(See 28-01-PLAN.md for the full phase artifact list.)
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tvd.rs — TvdMssResult struct + total_variation_depth_1d (TVD magnitude + MSS shape)</name>
  <files>fdars-core/src/depth/tvd.rs, fdars-core/src/depth/mod.rs, fdars-core/src/depth/dispatch.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/depth/dispatch.rs (`FunctionalBoxplotResult` struct derive/serde/non_exhaustive pattern to mirror for TvdMssResult; Band n<2 guard arm to mirror for n<3)
    - 28-RESEARCH.md §"Measure Definitions" item 10 (TVD 3-step + MSS construction + v_weights) + Pattern 5 (TvdMssResult) + Pattern 6 (column_ranks) + Common Pitfalls 5 (÷n not ÷n+1) and 6 (flat-curve MSS guard) + Open Question 1 (MSS shape_variation recommendation) + Assumption A1
    - 28-PATTERNS.md §"src/depth/tvd.rs" (struct pattern, TVD rank pass, n≥3 guard, dispatcher usage)
  </read_first>
  <behavior>
    - Test: on a 3-curve toy case with column ranks [1,2,3], p = [1/3, 2/3, 1.0], TV = [2/9, 2/9, 0], so TVD for the median-ranked curve ≈ 2/9 and outliers lower — assert TVD values within 1e-9 of hand-computed.
    - Test: the curve that is median-ranked at every t gets the maximum TVD (≈0.25) on a larger synthetic sample.
    - Test: a magnitude outlier gets low TVD; a shape outlier gets low MSS.
    - Test: a flat curve (constant value → zero total variation) yields MSS == 0.0 (not NaN).
    - Test: tvd.len() == mss.len() == data.nrows().
    - Test: empty matrix → Err; n<3 → Err.
  </behavior>
  <action>
    Create `fdars-core/src/depth/tvd.rs` with module doc `//! Total variation depth with modified shape similarity index (TVD + MSSI).` and imports `use crate::iter_maybe_parallel; use crate::matrix::FdMatrix; use crate::error::FdarError;` + `#[cfg(feature = "parallel")] use rayon::iter::ParallelIterator;`.

    Define the pinned struct exactly:
    `#[derive(Debug, Clone, PartialEq)] #[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))] #[non_exhaustive] pub struct TvdMssResult { pub tvd: Vec<f64>, pub mss: Vec<f64> }` with rustdoc on each field (tvd = magnitude component, range (0,0.25], higher=central; mss = shape component, higher=shape-central). Do NOT change these field names/types later — Phase 29 depends on them.

    Add the private `column_ranks` helper (same as extremal/erl per PATTERNS.md per-file decision).

    Implement `#[must_use] pub fn total_variation_depth_1d(data_obj: &FdMatrix, data_ori: &FdMatrix) -> Result<TvdMssResult, FdarError>`. `let (n, m) = (data_ori.nrows(), data_ori.ncols());`. Entry guard + n<3 guard → FdarError.
      TVD: for each column t, `column_ranks`, normalized `p[i][t] = rank / n` (Pitfall 5: divide by n, NOT n+1). `tvd[i] = (1/m) Σ_t p[i][t]*(1 - p[i][t])`.
      MSS (per RESEARCH Open Question 1 recommendation + A1): compute first differences `Δ[i][k] = data_ori[(i,k+1)] - data_ori[(i,k)]` for k in 0..m-1. For each interval k, rank the n curves' Δ[·][k] (column_ranks-style over the difference column) → `p_deriv[i][k] = rank/n`; `shape_variation[i][k] = p_deriv[i][k]*(1 - p_deriv[i][k])`. Curve weights `v_weights[i][k] = |Δ[i][k]| / Σ_k |Δ[i][k]|`; if the curve's total variation `Σ_k |Δ[i][k]|` is 0 (flat curve) set `mss[i] = 0.0` (Pitfall 6 guard — no division). Else `mss[i] = Σ_k shape_variation[i][k] * v_weights[i][k]`.
      Rustdoc: formulas, `[CITED: fdaoutlier::total_variation_depth]`, `[ASSUMED]` note on the MSS shape_variation derivation (per Open Question 1) and any fdars divergence from the R C++ internals.
      Return `Ok(TvdMssResult { tvd, mss })`.

    Parallelize the per-curve accumulation with `iter_maybe_parallel!` where the ranks are precomputed into buffers first (rank passes are shared across curves — compute `p` and `p_deriv` matrices sequentially, then map per-curve).

    WIRE: mod.rs `pub mod tvd;` + `pub use tvd::{total_variation_depth_1d, TvdMssResult};`. dispatch.rs: add `total_variation_depth_1d` to import; add `TotalVariation` variant (`/// Total variation depth + MSSI (Huang & Sun 2019). Returns TVD component. Requires n ≥ 3.`); add match arm with `if n < 3` InvalidDimension guard calling `total_variation_depth_1d(data, data)?.tvd`. lib.rs: add `total_variation_depth_1d, TvdMssResult,` to the depth re-export block.

    Inline `#[cfg(test)] mod tests` implementing the behaviors (include the hand-computed 1e-9 toy case and the flat-curve MSS==0 case).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel depth::tvd</automated>
  </verify>
  <acceptance_criteria>
    - `pub struct TvdMssResult { pub tvd: Vec<f64>, pub mss: Vec<f64> }` present with Debug/Clone/PartialEq + non_exhaustive + conditional serde.
    - Signature `pub fn total_variation_depth_1d(&FdMatrix, &FdMatrix) -> Result<TvdMssResult, FdarError>` present.
    - `DepthMethod::TotalVariation` dispatches `total_variation_depth_1d(data, data)?.tvd` with an n<3 guard.
    - Inline test asserts the 3-curve hand-computed TVD within 1e-9 AND flat-curve MSS == 0.0.
    - `cargo test -p fdars-core --features linalg,parallel depth::tvd` exits 0.
    - `cargo clippy --all-targets --features linalg,parallel -- -D warnings` green.
  </acceptance_criteria>
  <done>TVD+MSS compute correctly (TVD via ÷n rank pass, MSS with flat-curve guard), the pinned TvdMssResult is re-exported at crate root, and DepthMethod::TotalVariation projects the tvd field.</done>
  <reversibility rating="costly">TvdMssResult is a published struct = a forward contract Phase 29 consumes. Field names/types (tvd, mss) are pinned by CONTEXT/RESEARCH; changing them post-publication breaks Phase 29. No user checkpoint needed (deliberately pinned) — flagged only.</reversibility>
</task>

<task type="auto">
  <name>Task 2: All-9-variant dispatcher round-trip + full-suite regression sweep</name>
  <files>fdars-core/src/depth/dispatch.rs</files>
  <read_first>
    - fdars-core/src/depth/dispatch.rs (existing `#[cfg(test)] mod tests` fixture `sample(n,m)` and the enum — now carrying all 9 new variants after Task 1 + waves 1/2)
    - 28-CONTEXT.md §"Testing & Validation" (round-trip + error-path expectations)
    - 28-VALIDATION.md §"Per-Task Verification Map" (task 28-03-02 = dispatch coverage)
  </read_first>
  <action>
    Extend the existing `#[cfg(test)] mod tests` in `dispatch.rs` (do NOT modify non-test code — the enum/arms are already complete after Task 1). Add a test that iterates every new parameter-free `DepthMethod` variant (`HalfRegion`, `ModifiedHalfRegion`, `HypographIndex`, `ModifiedHypographIndex`, `EpigraphIndex`, `Extremal`, `ExtremeRankLength`, `LInfinity`, `TotalVariation`) against a shared `sample(n, m)` fixture with `n >= 3`, asserting each `functional_depth(&data, method)` returns `Ok` with a vec of length `data.nrows()`.

    Add a regression test asserting the pre-existing variants (`FraimanMuniz { scale: false }`, `Band`, `ModifiedBand`, `RandomProjection { nproj, seed }`) still return `Ok` on the same fixture (proves the additive change did not alter existing dispatch behavior).

    Add an error-path test: for a single-curve matrix (n=1), the variants that require ≥2 (Band, HalfRegion, ModifiedHalfRegion, HypographIndex, EpigraphIndex, ExtremeRankLength) and ≥3 (Extremal, TotalVariation) return `Err`; and that an empty matrix returns `Err` for a representative variant. Confirm no panic anywhere (assertions use `.is_err()`).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel depth::dispatch</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel</automated>
  </verify>
  <acceptance_criteria>
    - A dispatch test round-trips all 9 new variants returning Ok(vec of len nrows).
    - A regression test confirms the 4 pre-existing variants still return Ok.
    - An error-path test confirms min-n guards return Err (no panic) for single-curve and empty inputs.
    - `cargo test -p fdars-core --features linalg,parallel depth::dispatch` exits 0.
    - `cargo test -p fdars-core --features linalg,parallel` (FULL suite) exits 0.
    - `cargo clippy --all-targets --features linalg,parallel -- -D warnings` green.
  </acceptance_criteria>
  <done>All 9 new DepthMethod variants and the 4 existing ones round-trip through functional_depth; error paths return Err without panic; the full test suite and clippy are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → depth fn | Untrusted `FdMatrix` dimensions cross here; validated at entry |
| tvd.rs → Phase 29 | `TvdMssResult` struct is a forward interface consumed by a later phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-06 | Denial of Service | tvd entry point | low | mitigate | Empty / n<3 guard returns FdarError before indexing (no panic) |
| T-28-07 | Tampering | MSS v_weights division by total variation | low | mitigate | Flat-curve guard (total variation == 0 → MSS 0.0), no division-by-zero/NaN (Pitfall 6) |
| T-28-08 | Tampering | TVD rank normalization ÷n | low | mitigate | n==0 guarded at entry; divide by n (not n+1) per fdaoutlier source (Pitfall 5) |
| T-28-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages added this phase |
</threat_model>

<verification>
- `cargo test -p fdars-core --features linalg,parallel` (full suite) exits 0.
- `cargo clippy --all-targets --features linalg,parallel -- -D warnings` green.
- No new dependency in `fdars-core/Cargo.toml`.
- `TvdMssResult` re-exported at crate root (`fdars_core::TvdMssResult` resolves).
</verification>

<success_criteria>
- `total_variation_depth_1d` returns the pinned `TvdMssResult { tvd, mss }`; DepthMethod::TotalVariation projects `.tvd`.
- All 9 new DepthMethod variants + 4 existing round-trip through functional_depth.
- Central/median curve deepest, outliers shallowest; flat-curve MSS == 0.0.
- Error paths return FdarError, never panic.
- Full suite + clippy green; no new dependency; existing signatures untouched.
</success_criteria>

<output>
Create `.planning/phases/28-depth-measure-long-tail/28-03-SUMMARY.md` when done
</output>
