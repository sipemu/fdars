---
phase: 28-depth-measure-long-tail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/depth/hypo_epi.rs
  - fdars-core/src/depth/half_region.rs
  - fdars-core/src/depth/mod.rs
  - fdars-core/src/depth/dispatch.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [DEPTH-01]
estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "hypograph_index_1d, modified_hypograph_index_1d, epigraph_index_1d each return Result<Vec<f64>, FdarError> over a column-major FdMatrix (per DEPTH-01)"
    - "half_region_depth_1d and modified_half_region_depth_1d return Result<Vec<f64>, FdarError> computing min(EI,HI) and min(MEI,MHI) respectively (per DEPTH-01)"
    - "functional_depth(data, DepthMethod::HypographIndex|ModifiedHypographIndex|EpigraphIndex|HalfRegion|ModifiedHalfRegion) dispatches to the new functions; existing DepthMethod variants (FraimanMuniz, Band, ModifiedBand, RandomProjection) and the functional_depth signature keep working unchanged"
    - "On synthetic data with a clearly-central curve plus injected magnitude/shape outliers, MHRD/MHI rank the central curve deepest and outliers shallowest (within tolerance) in inline tests"
    - "HI values are always integer multiples of 1/nori (global indicator, not pointwise average) — asserted in an inline test"
    - "Empty matrix returns FdarError (never panics) for every new entry point (hypo_epi + half_region)"
    - "Single-curve input (n<2) returns FdarError for HI/EI/HRD/MHRD entry points (never panics)"
    - "No new crate dependency added; cargo clippy --all-targets --features linalg,parallel -- -D warnings stays green"
  artifacts:
    - fdars-core/src/depth/hypo_epi.rs
    - fdars-core/src/depth/half_region.rs
  key_links:
    - "depth/mod.rs re-exports the 5 new functions; lib.rs re-exports them at crate root; dispatch.rs enum + match wire the 5 variants"
---

<objective>
Add the roahd index-measure family to `depth/`: the hypograph index (HI), modified-hypograph index (MHI), un-modified epigraph index (EI) in `hypo_epi.rs`, and the half-region depth (HRD) and modified half-region depth (MHRD) composites in `half_region.rs`. Wire all five as `DepthMethod` variants and crate-root re-exports.

This is the tracer plan: the first task fully wires ONE measure family end-to-end (signature → implementation → `DepthMethod` variant → match arm → `mod.rs`/`lib.rs` re-export → inline tests → green clippy), proving the entire additive integration path before the remaining measures are added the same way in later waves.

Purpose: Complete the roahd hypograph/epigraph index family (complementing the already-shipped MEI), and prove the depth-measure integration path end-to-end (DEPTH-01).
Output: `hypo_epi.rs` (3 fns), `half_region.rs` (2 fns), 5 new `DepthMethod` variants + match arms, mod.rs + lib.rs re-exports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-depth-measure-long-tail/28-CONTEXT.md
@.planning/phases/28-depth-measure-long-tail/28-RESEARCH.md
@.planning/phases/28-depth-measure-long-tail/28-PATTERNS.md
@fdars-core/src/depth/band.rs
@fdars-core/src/depth/dispatch.rs
@fdars-core/src/depth/mod.rs
</context>

<artifacts_produced>
## Artifacts this phase produces (full list across all 3 plans)

New functions (all `Result<Vec<f64>, FdarError>` except TVD):
- `hypograph_index_1d`, `modified_hypograph_index_1d`, `epigraph_index_1d` (hypo_epi.rs) — THIS PLAN
- `half_region_depth_1d`, `modified_half_region_depth_1d` (half_region.rs) — THIS PLAN
- `extremal_depth_1d` (extremal.rs) — plan 02
- `extreme_rank_length_depth_1d` (erl.rs) — plan 02
- `linfinity_depth_1d` (linf.rs) — plan 02
- `total_variation_depth_1d` → `Result<TvdMssResult, FdarError>` (tvd.rs) — plan 03

New struct:
- `TvdMssResult { tvd: Vec<f64>, mss: Vec<f64> }` (tvd.rs) — plan 03 — PINNED for Phase 29 reuse

New DepthMethod variants (parameter-free): `HalfRegion`, `ModifiedHalfRegion` (this plan), `HypographIndex`, `ModifiedHypographIndex`, `EpigraphIndex` (this plan), `Extremal`, `ExtremeRankLength`, `LInfinity` (plan 02), `TotalVariation` (plan 03).

New files (6): `hypo_epi.rs`, `half_region.rs` (this plan); `extremal.rs`, `erl.rs`, `linf.rs` (plan 02); `tvd.rs` (plan 03).

Sub-helpers: `column_ranks(data: &FdMatrix, col: usize) -> Vec<f64>` (private, defined per-file in extremal.rs/erl.rs/tvd.rs — plans 02/03).
</artifacts_produced>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — hypo_epi.rs (HI/MHI/EI) fully wired end-to-end through DepthMethod</name>
  <files>fdars-core/src/depth/hypo_epi.rs, fdars-core/src/depth/mod.rs, fdars-core/src/depth/dispatch.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/depth/band.rs (the MEI analog: `modified_epigraph_index_1d` loop structure, `iter_maybe_parallel!` usage, `<=` tie convention — mirror exactly)
    - fdars-core/src/depth/dispatch.rs (DepthMethod enum + functional_depth match + Band n<2 guard arm to mirror)
    - fdars-core/src/depth/mod.rs (pub mod + pub use blocks to extend)
    - 28-RESEARCH.md §"Measure Definitions" items 1 (HI), 2 (EI), 3 (MHI) — pinned formulas + tie convention + Code Examples Pattern 1 (HI) and Pattern 2 (MHI)
    - 28-PATTERNS.md §"src/depth/hypo_epi.rs" (HI global-indicator `continue 'outer`, EI complement, MHI pointwise-average patterns) and §"Shared Patterns"
  </read_first>
  <action>
    Create `fdars-core/src/depth/hypo_epi.rs` with module doc `//! Hypograph, modified-hypograph, and epigraph indices (HI, MHI, EI).` and imports `use crate::iter_maybe_parallel; use crate::matrix::FdMatrix; use crate::error::FdarError;` plus `#[cfg(feature = "parallel")] use rayon::iter::ParallelIterator;`.

    Implement three `#[must_use]` public functions, each `fn <name>(data_obj: &FdMatrix, data_ori: &FdMatrix) -> Result<Vec<f64>, FdarError>` with `let (nobj, nori, m) = (data_obj.nrows(), data_ori.nrows(), data_obj.ncols());` and an entry guard returning `FdarError::InvalidDimension { parameter: "data_obj", expected: "non-empty matrix".to_string(), actual: format!("{nobj}x{m}") }` when `nobj == 0 || nori == 0 || m == 0`:

    - `hypograph_index_1d`: GLOBAL indicator. For each object i, count reference curves j such that `data_ori[(j, t)] <= data_obj[(i, t)]` holds at EVERY t (use a labeled `'outer` loop that continues on the first violation `data_ori[(j,t)] > data_obj[(i,t)]`). Depth = count / nori. Add an n<2 guard returning FdarError (per RESEARCH assumption A3 — needs ≥2 for a meaningful reference).
    - `epigraph_index_1d`: GLOBAL indicator complement — count j where `data_ori[(j, t)] >= data_obj[(i, t)]` at every t (continue on `data_ori[(j,t)] < data_obj[(i,t)]`). Depth = count / nori. n<2 guard returning FdarError.
    - `modified_hypograph_index_1d`: POINTWISE average — exact mirror of MEI but with `>=`: `total += (count of t where data_obj[(i,t)] >= data_ori[(j,t)]) / m` summed over j, then `/ nori`. Allow n>=1 (no n<2 guard). Rustdoc must note MHI(X)+MEI(X)=1 without ties.

    All three use `iter_maybe_parallel!(0..nobj).map(|i| ...).collect()` mirroring MEI. Rustdoc each function: formula, `[CITED: roahd::HI/MHI/EI]`, tie convention (`<=`/`>=` per MEI), range [0,1]. Do NOT restate literal comparison tokens that a negative-grep test targets as bare prose duplicating an asserted invariant.

    WIRE END-TO-END (this is what makes it a tracer):
    - `depth/mod.rs`: add `pub mod hypo_epi;` (alphabetical in the new-file block) and `pub use hypo_epi::{epigraph_index_1d, hypograph_index_1d, modified_hypograph_index_1d};` after the `pub use band::{...}` line.
    - `depth/dispatch.rs`: extend the `use crate::depth::{...}` import to bring in the three fns; add three parameter-free variants `HypographIndex`, `ModifiedHypographIndex`, `EpigraphIndex` to the `DepthMethod` enum (with `///` doc each, after `RandomProjection`); add three match arms in `functional_depth` mirroring the `Band` arm — `HypographIndex`/`EpigraphIndex` with an `if n < 2` InvalidDimension guard calling `hypograph_index_1d(data, data)?` / `epigraph_index_1d(data, data)?`; `ModifiedHypographIndex` with no extra guard calling `modified_hypograph_index_1d(data, data)?`.
    - `lib.rs`: extend the `pub use depth::{...}` block (~line 422) inserting `epigraph_index_1d, hypograph_index_1d, modified_hypograph_index_1d,` after `modified_epigraph_index_1d,`.

    Inline `#[cfg(test)] mod tests`: build a `sample(n, m)` fixture (sine + per-curve offset, per PATTERNS.md). Assert: HI values are all integer multiples of 1/nori; MHI central curve ≈ 0.5 and ranks central deepest; EI is a valid global indicator; empty matrix → Err; single-curve (n=1) → Err for HI/EI; `functional_depth(&data, DepthMethod::HypographIndex)` round-trips and returns nrows values; existing `DepthMethod::Band` still returns Ok on the same fixture (proving no regression).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel depth::hypo_epi</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel depth::dispatch</automated>
  </verify>
  <acceptance_criteria>
    - Signatures `pub fn hypograph_index_1d(&FdMatrix, &FdMatrix) -> Result<Vec<f64>, FdarError>`, `modified_hypograph_index_1d(...)`, `epigraph_index_1d(...)` present.
    - `DepthMethod::HypographIndex`, `ModifiedHypographIndex`, `EpigraphIndex` variants exist and dispatch through `functional_depth`.
    - `cargo test -p fdars-core --features linalg,parallel depth::hypo_epi` exits 0.
    - `cargo test -p fdars-core --features linalg,parallel depth::dispatch` exits 0 (existing dispatcher tests unbroken).
    - Inline test asserts HI values are multiples of 1/nori AND MHI ranks the central curve deepest.
    - Empty-matrix and single-curve error-path tests pass (Err, no panic).
    - `cargo clippy --all-targets --features linalg,parallel -- -D warnings` green.
  </acceptance_criteria>
  <done>All three index functions compute correct depths, dispatch through the 3 new DepthMethod variants, are re-exported at crate root, existing dispatcher + Band tests still pass, and the crate compiles clean end-to-end.</done>
  <reversibility rating="reversible">New additive functions + non_exhaustive enum variants; removable without touching existing signatures.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: half_region.rs (HRD/MHRD) composing the hypo/epi indices, wired to DepthMethod</name>
  <files>fdars-core/src/depth/half_region.rs, fdars-core/src/depth/mod.rs, fdars-core/src/depth/dispatch.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/depth/hypo_epi.rs (the functions HRD/MHRD compose — created in Task 1)
    - fdars-core/src/depth/band.rs (`modified_epigraph_index_1d` — MEI, one half of MHRD)
    - fdars-core/src/depth/dispatch.rs (Band n<2 guard arm to mirror; enum/match already extended in Task 1)
    - 28-RESEARCH.md §"Measure Definitions" items 5 (HRD = min(EI,HI)) and 6 (MHRD = min(MEI,MHI)) + Common Pitfall 2 (avoid nested parallelism)
    - 28-PATTERNS.md §"src/depth/half_region.rs" (fused single-pass HRD to avoid nested parallelism; MHRD via zip of MEI+MHI)
  </read_first>
  <behavior>
    - Test: central curve in a synthetic sample gets the highest MHRD (deepest); injected magnitude and shape outliers get the lowest MHRD.
    - Test: MHRD central value ≈ 0.5 within tolerance.
    - Test: HRD = min(EI, HI) element-wise — verify against separately-computed epigraph_index_1d and hypograph_index_1d on the same fixture (within 1e-12).
    - Test: empty matrix → Err; single curve (n=1) → Err (n<2 guard) for both HRD and MHRD.
    - Test: functional_depth(&data, DepthMethod::HalfRegion) and ModifiedHalfRegion round-trip and return nrows values.
  </behavior>
  <action>
    Create `fdars-core/src/depth/half_region.rs` with module doc `//! Half-region depth and modified half-region depth (HRD, MHRD).` and the same imports as hypo_epi.rs.

    Implement two `#[must_use]` public functions `fn <name>(data_obj: &FdMatrix, data_ori: &FdMatrix) -> Result<Vec<f64>, FdarError>` with the non-empty entry guard AND an n<2 guard (expected "at least 2 curves for half-region depth"):

    - `half_region_depth_1d` (HRD = min(EI, HI)): to avoid nested parallelism (Pitfall 2), compute EI and HI in a FUSED single pass — one `iter_maybe_parallel!(0..nobj).map(|i| ...)` where per object i and reference j track two flags `j_above` (data_ori[(j,t)] never below data_obj[(i,t)]) and `j_below` (never above), short-circuiting via `continue 'outer` once both are false; accumulate `ei_count`/`hi_count`; return `f64::min(ei_count, hi_count) / nori`.
    - `modified_half_region_depth_1d` (MHRD = min(MEI, MHI)): call `crate::depth::modified_epigraph_index_1d(data_obj, data_ori)` (returns Vec, infallible) and `crate::depth::modified_hypograph_index_1d(data_obj, data_ori)?`, then `.iter().zip(...).map(|(&mei, &mhi)| f64::min(mei, mhi)).collect()`. The outer zip is sequential so the two internally-parallel calls do not nest.

    Rustdoc each: formula, `[CITED: roahd::HRD/MHRD]`, note HRD is strict (non-crossing curves), MHRD tolerates crossings and central ≈ 0.5.

    WIRE:
    - `depth/mod.rs`: add `pub mod half_region;` and `pub use half_region::{half_region_depth_1d, modified_half_region_depth_1d};`.
    - `depth/dispatch.rs`: add `half_region_depth_1d, modified_half_region_depth_1d` to the `use crate::depth::{...}` import; add `HalfRegion` and `ModifiedHalfRegion` parameter-free enum variants (after the index variants); add two match arms mirroring the Band arm (both with `if n < 2` InvalidDimension guard) calling `half_region_depth_1d(data, data)?` / `modified_half_region_depth_1d(data, data)?`.
    - `lib.rs`: add `half_region_depth_1d, modified_half_region_depth_1d,` to the `pub use depth::{...}` block.

    Inline `#[cfg(test)] mod tests` implementing the behaviors above.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel depth::half_region</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel depth::</automated>
  </verify>
  <acceptance_criteria>
    - Signatures `pub fn half_region_depth_1d(&FdMatrix, &FdMatrix) -> Result<Vec<f64>, FdarError>` and `modified_half_region_depth_1d(...)` present.
    - `DepthMethod::HalfRegion` and `ModifiedHalfRegion` dispatch through `functional_depth`.
    - Inline test proves HRD == element-wise min(EI, HI) within 1e-12 and MHRD ranks central curve deepest.
    - `cargo test -p fdars-core --features linalg,parallel depth::half_region` exits 0.
    - `cargo test -p fdars-core --features linalg,parallel depth::` (whole depth module) exits 0.
    - `cargo clippy --all-targets --features linalg,parallel -- -D warnings` green.
  </acceptance_criteria>
  <done>HRD and MHRD compute correctly (no nested parallelism), dispatch through their DepthMethod variants, are crate-root re-exported, and the whole depth test module passes green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → depth fn | Untrusted `FdMatrix` dimensions cross here; validated at entry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Denial of Service | hypo_epi/half_region entry points | low | mitigate | Empty/single-curve guard returns FdarError::InvalidDimension before any indexing (no panic) |
| T-28-02 | Tampering | column-major `data[(i,t)]` indexing | low | accept | FdMatrix encapsulates index arithmetic; no raw pointer math introduced |
| T-28-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages added this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `cargo test -p fdars-core --features linalg,parallel depth::` exits 0 (hypo_epi, half_region, dispatch).
- `cargo clippy --all-targets --features linalg,parallel -- -D warnings` green.
- No new dependency in `fdars-core/Cargo.toml` (git diff shows only src changes).
</verification>

<success_criteria>
- 5 new functions (HI/MHI/EI/HRD/MHRD) callable, `Result`-returning, crate-root re-exported.
- 5 new DepthMethod variants dispatch correctly; existing variants + signature unchanged.
- Central curve deepest / outliers shallowest for MHRD/MHI on synthetic data.
- Error paths (empty / single-curve) return FdarError, never panic.
- Full suite + clippy green.
</success_criteria>

<output>
Create `.planning/phases/28-depth-measure-long-tail/28-01-SUMMARY.md` when done
</output>
