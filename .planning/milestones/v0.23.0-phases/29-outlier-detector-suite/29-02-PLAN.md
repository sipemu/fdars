---
phase: 29-outlier-detector-suite
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - fdars-core/src/outliers.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [OUT-01]

estimate:
  tokens: 82000
  raw_tokens: 41000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "sequential_transform_outliers(data, sequence, config) returns Result<SeqTransformOutliers, FdarError> with per-transform flagged sets + a unioned outlier set, applying transforms cumulatively (each step feeds the next) and running functional_boxplot as the base detector per step (OUT-01, success-criteria 1+2)"
    - "depthgram(data, config) returns Result<DepthgramResult, FdarError> with the six MBD(MEI)/MEI(MBD) index vectors across the 3 (p=1-equivalent) representations plus numeric shape+magnitude outlier index sets — no rendering (OUT-01, success-criteria 1+2)"
    - "sequential_transform_outliers reuses functional_boxplot (no reimplementation); depthgram reuses modified_band_1d / modified_epigraph_index_1d and the outliergram parabola formula + functional_boxplot — no new crate dependency (OUT-01, success-criterion 3)"
    - "Both detectors reuse the shared iqr_fence helper from Plan 01 (depthgram uses the UPPER fence on the parabola distance); no second fence helper is introduced"
    - "Invalid inputs return FdarError at each entry point, never panic: seq-transform n<2 / D1 with m<2 / T2 zero-norm curve; depthgram n<2 (and documented n>=4 for meaningful parabola IQR) / empty (OUT-01, success-criterion 4)"
    - "Existing magnitude_shape_outlyingness / outliergram / DEPTH-01 signatures unchanged (additive/non-breaking); crate compiles after this plan and clippy --all-targets --features linalg,parallel -- -D warnings is green (OUT-01, success-criterion 5)"
  artifacts:
    - "fdars-core/src/outliers.rs — adds SeqTransform enum, SeqTransformConfig, SeqTransformOutliers, DepthgramConfig, DepthgramResult; seq_transform_apply + depthgram helpers; sequential_transform_outliers + depthgram public fns; inline #[cfg(test)] tests"
    - "fdars-core/src/lib.rs — extends the pub use outliers::{...} block with sequential_transform_outliers, depthgram, SeqTransform, SeqTransformOutliers, SeqTransformConfig, DepthgramResult, DepthgramConfig"
  key_links:
    - "sequential_transform_outliers -> functional_boxplot (depth/dispatch.rs) per transform step, cumulative input"
    - "depthgram -> modified_band_1d + modified_epigraph_index_1d (depth/band.rs); MBD/MEI re-applied to n×1 FdMatrix via from_column_major"
    - "depthgram -> iqr_fence UPPER fence on outliergram parabola distance (shared helper from Plan 01) + functional_boxplot on MBD for magnitude outliers"
    - "lib.rs re-export -> crate root: all four Plan-02 symbols callable as fdars_core::{...}"
---

<objective>
Add the remaining two OUT-01 detectors to `fdars-core/src/outliers.rs` additively, building on the
shared `iqr_fence` helper and re-export block established in Plan 01: `sequential_transform_outliers`
(the fdaoutlier T0/T1/T2/D1 cumulative-transform detector with a functional-boxplot base detector per
step) and `depthgram` (the roahd univariate depthgram statistic — MBD(MEI)/MEI(MBD) index pairs across
three representations that collapse for p=1, plus outliergram-parabola shape flags and
functional-boxplot magnitude flags).

Purpose: complete the four-detector OUT-01 suite; both detectors reuse existing depth + boxplot
machinery and the Plan-01 `iqr_fence` — no reimplementation, no new crate dependency.
Output: `SeqTransform`, `SeqTransformConfig`, `SeqTransformOutliers`, `DepthgramConfig`,
`DepthgramResult`, `sequential_transform_outliers`, `depthgram` in outliers.rs + extended lib.rs
re-export; inline tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-outlier-detector-suite/29-RESEARCH.md
@.planning/phases/29-outlier-detector-suite/29-PATTERNS.md
@.planning/phases/29-outlier-detector-suite/29-01-SUMMARY.md
@fdars-core/src/outliers.rs
@fdars-core/src/depth/band.rs
@fdars-core/src/depth/dispatch.rs
@fdars-core/src/lib.rs
</context>

<artifacts_produced>
## Artifacts this plan produces (new public symbols)

| Symbol | Kind | Location |
|--------|------|----------|
| `SeqTransform` | pub enum (`T0, T1, T2, D1, D2`, `#[non_exhaustive]`) | outliers.rs |
| `SeqTransformConfig` | pub struct (config) | outliers.rs |
| `SeqTransformOutliers` | pub struct (result) | outliers.rs |
| `DepthgramConfig` | pub struct (config) | outliers.rs |
| `DepthgramResult` | pub struct (result) | outliers.rs |
| `sequential_transform_outliers` | pub fn (detector) | outliers.rs |
| `depthgram` | pub fn (detector) | outliers.rs |
| private `seq_transform_apply` + depthgram parabola helper | private helpers | outliers.rs |
| re-export extension | `pub use outliers::{...}` block | lib.rs ~line 434 |

Reuses from Plan 01: the shared `iqr_fence` private helper (depthgram upper fence). Do NOT re-add it.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sequential_transform_outliers (T0/T1/T2/D1 cumulative transforms + functional_boxplot base + unioned set)</name>
  <files>fdars-core/src/outliers.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/outliers.rs — the outliergram skeleton (~L278-332) for the function/guard shape; the existing `#[cfg(test)] mod tests` block (~L449) to APPEND to; the iqr_fence + re-export block added in Plan 01 (do NOT duplicate iqr_fence).
    - .planning/phases/29-outlier-detector-suite/29-RESEARCH.md §"Algorithm Pinning → 3. sequential_transform_outliers" (exact transform table T0 identity / T1 row-mean centering / T2 L2 normalization / D1 lag-1 first difference m→m-1; CUMULATIVE application — each step feeds the next; default sequence [T0,T1,D1]; base detector functional_boxplot with DepthMethod::ModifiedBand; union is an fdars convenience; n>=2, m>=2-before-D1, T2 zero-norm -> ComputationFailed) + §"Common Pitfalls → Pitfall 3 (cumulative input)" + §"Open Questions → 1 (do NOT store transformed data in the struct)".
    - .planning/phases/29-outlier-detector-suite/29-PATTERNS.md §"sequential_transform_outliers" (entry validation, functional_boxplot call per step, FdMatrix::from_column_major for transformed data, T2 zero-norm ComputationFailed guard, union construction sort_unstable+dedup).
    - fdars-core/src/depth/dispatch.rs L205-247 — functional_boxplot(data, method, factor) -> Result + FunctionalBoxplotResult{outliers} + DepthMethod::ModifiedBand.
    - fdars-core/src/matrix.rs L50-58,L94-106 — from_column_major + nrows/ncols/shape (for building transformed matrices).
  </read_first>
  <behavior>
    - sequential_transform_outliers with the default sequence on data with an obvious magnitude outlier: at least one per-transform set is non-empty AND union_outliers contains that outlier index.
    - union_outliers == the sorted-deduped flatten of all per_transform_outliers sets.
    - Passing a sequence containing D1 when m < 2 -> Err(FdarError::InvalidDimension).
    - An all-zero curve under a T2 step -> Err(FdarError::ComputationFailed).
    - n == 1 -> Err(FdarError::InvalidDimension).
    - Transforms apply cumulatively: [T1, D1] differences the CENTERED data, not the raw data.
  </behavior>
  <action>
    Add a `#[non_exhaustive]` public enum `SeqTransform { T0, T1, T2, D1, D2 }` (derive Debug, Clone,
    Copy, PartialEq, Eq + conditional serde) with rustdoc mapping each variant to its formula (per the
    RESEARCH transform table; D2 identical to D1, re-differencing when applied after D1; `O` transform
    deferred/out-of-scope). Add `SeqTransformConfig { depth_method: DepthMethod, emp_factor: f64 }`
    (Default: ModifiedBand, 1.5). Add `SeqTransformOutliers { per_transform_outliers:
    Vec<(SeqTransform, Vec<usize>)>, union_outliers: Vec<usize> }` with the standard derive/attribute
    stack. Do NOT add a transformed-data field (RESEARCH Open Question 1).

    Add a private `seq_transform_apply(current: &FdMatrix, t: SeqTransform) ->
    Result<FdMatrix, FdarError>`: T0 clone; T1 subtract each row's mean; T2 divide each row by its L2
    norm (if any row norm < 1e-15 return FdarError::ComputationFailed{operation:"T2 normalization",
    detail with the row index}); D1/D2 build an (n × m-1) matrix of lag-1 differences (guard m<2 ->
    FdarError::InvalidDimension). Build outputs via FdMatrix::from_column_major, propagating its Result.

    Implement `#[must_use = "..."] pub fn sequential_transform_outliers(data: &FdMatrix, sequence:
    &[SeqTransform], config: SeqTransformConfig) -> Result<SeqTransformOutliers, FdarError>`. Guard
    n<2 (InvalidDimension). Maintain `current: FdMatrix` starting from data; for each transform in
    sequence: `current = seq_transform_apply(&current, t)?` (CUMULATIVE — mutate through the sequence,
    per Pitfall 3), then `functional_boxplot(&current, config.depth_method, config.emp_factor)?` and
    push `(t, fbp.outliers)` into per_transform_outliers. After the loop, build union_outliers by
    flatten + copied + sort_unstable + dedup. Document that the union is an fdars convenience not in
    the R baseline. Return Ok(SeqTransformOutliers{...}).

    Extend the lib.rs `pub use outliers::{...}` block to add `sequential_transform_outliers`,
    `SeqTransform`, `SeqTransformOutliers`, `SeqTransformConfig` (Edit, not Write).

    Append inline tests to the existing tests module: a default-sequence test on a magnitude-outlier
    fixture (union contains the outlier, at least one per-transform set non-empty), a union==flatten
    equality test, a D1-with-m==1 error case, a T2 all-zero-curve ComputationFailed case, and an n==1
    error case.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib outliers::seq_transform</automated>
  </verify>
  <acceptance_criteria>
    - `sequential_transform_outliers(data: &FdMatrix, sequence: &[SeqTransform], config: SeqTransformConfig) -> Result<SeqTransformOutliers, FdarError>` exists, is `#[must_use]`, crate-root re-exported.
    - `SeqTransform` is a `#[non_exhaustive]` enum with `T0, T1, T2, D1, D2`; `SeqTransformOutliers` has `per_transform_outliers: Vec<(SeqTransform, Vec<usize>)>` + `union_outliers: Vec<usize>` (no transformed-data field).
    - Transforms apply cumulatively via a single mutated `current` matrix; each step calls `functional_boxplot` (no reimplemented fence).
    - `cargo test ... --lib outliers::seq_transform` exits 0: default-sequence fixture flags the outlier in the union, union==sorted-deduped flatten, D1-with-m==1 -> InvalidDimension, T2 zero-norm -> ComputationFailed, n==1 -> InvalidDimension.
    - No existing signature changed; crate compiles.
  </acceptance_criteria>
  <reversibility rating="costly">SeqTransform/SeqTransformOutliers/SeqTransformConfig are published contracts; shape fixed by CONTEXT/RESEARCH — flag only.</reversibility>
  <done>sequential_transform_outliers returns per-transform + unioned outliers with cumulative transforms, errors on D1/m==1, T2 zero-norm, and n==1, is re-exported, and seq_transform tests are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: depthgram statistic (MBD/MEI index pairs across 3 p=1-equivalent representations + parabola shape flags + boxplot magnitude flags)</name>
  <files>fdars-core/src/outliers.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/outliers.rs — the outliergram parabola block (~L291-321) for the a0/a1/a2 coefficients + fence reuse; the OutligramResult derive stack; the existing tests module (~L449); the iqr_fence helper from Plan 01 (REUSE the UPPER fence).
    - .planning/phases/29-outlier-detector-suite/29-RESEARCH.md §"Algorithm Pinning → 4. depthgram" (three representations collapse for p=1; mbd_mei = MBD of the MEI column-vector, mei_mbd = MEI of the MBD column-vector; outliergram parabola dist = (a0 + a1·mei + a2·n²·mei²) − mbd with UPPER IQR fence dist>lim for shape outliers; functional_boxplot on MBD for magnitude outliers; A3 upper-tail direction; A5 p=1 equivalence; n>=2, n>=4 for meaningful parabola IQR — document) + §"Code Examples → depthgram" (exact formulas + n×1 FdMatrix wrapping) + §"Common Pitfalls → Pitfall 4 (MBD of a column vector)".
    - .planning/phases/29-outlier-detector-suite/29-PATTERNS.md §"depthgram" (depth calls return Vec<f64> no ?; n×1 FdMatrix wrapping; parabola+IQR reuse; UPPER-fence-vs-outliergram-lower-fence sign note; functional_boxplot on MBD; p=1 all three representations identical → clone; rustdoc note).
    - fdars-core/src/depth/band.rs L42-95 — modified_band_1d / modified_epigraph_index_1d signatures (return Vec<f64>, need n>=2 reference rows).
    - fdars-core/src/depth/dispatch.rs L205-247 — functional_boxplot + DepthMethod::ModifiedBand.
  </read_first>
  <behavior>
    - depthgram on a magnitude-outlier fixture: `result.magnitude_outliers.contains(&idx)`.
    - depthgram on a shape-outlier fixture: `result.shape_outliers.contains(&idx)`.
    - For p=1 (univariate): `result.mbd_mei_d == result.mbd_mei_t` and `== result.mbd_mei_t2` (exact equality); same for the mei_mbd_* triple.
    - `result.mbd.len() == n`, `result.mei.len() == n`, and each of the six index vectors has length n.
    - n == 1 -> Err(FdarError::InvalidDimension); empty -> Err(FdarError::InvalidDimension).
  </behavior>
  <action>
    Add `DepthgramConfig { outliergram_factor: f64, boxplot_factor: f64 }` (Default 1.5, 1.5) and
    `DepthgramResult { mbd_mei_d, mei_mbd_d, mbd_mei_t, mei_mbd_t, mbd_mei_t2, mei_mbd_t2: Vec<f64>,
    shape_outliers, magnitude_outliers: Vec<usize>, mbd, mei: Vec<f64> }` with the standard
    derive/attribute stack (per the locked struct in RESEARCH §depthgram Output).

    Implement `#[must_use = "..."] pub fn depthgram(data: &FdMatrix, config: DepthgramConfig) ->
    Result<DepthgramResult, FdarError>`. Guard n<2 with InvalidDimension (rustdoc-note that n>=4 is
    needed for a meaningful parabola IQR). Compute `mbd = modified_band_1d(data, data)` and `mei =
    modified_epigraph_index_1d(data, data)` (Vec<f64>, no ?). Wrap `mei` and `mbd` as n×1 matrices via
    FdMatrix::from_column_major(..., n, 1)? (Pitfall 4); `mbd_mei = modified_band_1d(&mei_mat,
    &mei_mat)`, `mei_mbd = modified_epigraph_index_1d(&mbd_mat, &mbd_mat)`. Shape outliers: compute
    outliergram parabola coefficients a2 = -2/(n(n-1)), a0 = a2, a1 = 2(n+1)/(n-1); dist[i] = (a0 +
    a1·mei[i] + a2·n²·mei[i]²) − mbd[i]; take the UPPER fence `let (_, upper) = iqr_fence(&dist,
    config.outliergram_factor);` and flag i where `dist[i] > upper` (A3 upper tail — note the sign is
    opposite to outliergram's lower fence). Magnitude outliers: wrap mbd as an n×1 matrix and call
    `functional_boxplot(&mbd_mat2, DepthMethod::ModifiedBand, config.boxplot_factor)?`; take
    `fbp.outliers`. For p=1 assign all three representations from the single computed pair
    (mbd_mei_d = mbd_mei_t = mbd_mei_t2 = mbd_mei.clone(); mei_mbd_* similarly). Add a rustdoc note:
    "For p=1, all three depthgram representations are equivalent; this implementation handles
    univariate functional data only" (RESEARCH A5 divergence). Return Ok(DepthgramResult{...}).

    Extend the lib.rs `pub use outliers::{...}` block to add `depthgram`, `DepthgramResult`,
    `DepthgramConfig` (Edit, not Write).

    Append inline tests to the existing tests module: a magnitude-outlier fixture (magnitude_outliers
    contains idx), a shape-outlier fixture (shape_outliers contains idx), a p=1-equivalence test
    (mbd_mei_d == mbd_mei_t == mbd_mei_t2 exactly), an n==1 error case, and an empty-matrix error case.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib outliers::depthgram</automated>
  </verify>
  <acceptance_criteria>
    - `depthgram(data: &FdMatrix, config: DepthgramConfig) -> Result<DepthgramResult, FdarError>` exists, is `#[must_use]`, crate-root re-exported.
    - `DepthgramResult` carries the six index vectors `mbd_mei_d/mei_mbd_d/mbd_mei_t/mei_mbd_t/mbd_mei_t2/mei_mbd_t2` + `shape_outliers`/`magnitude_outliers` + `mbd`/`mei`, all with the standard derive/attribute stack.
    - depthgram calls `modified_band_1d` + `modified_epigraph_index_1d` (no hand-rolled MBD/MEI), reuses the outliergram parabola formula with the shared `iqr_fence` UPPER fence, and `functional_boxplot` for magnitude outliers.
    - `cargo test ... --lib outliers::depthgram` exits 0: magnitude + shape fixtures flag the injected curve, p=1 equivalence holds exactly, n==1 and empty both return `FdarError::InvalidDimension`.
    - No existing signature changed; crate compiles.
  </acceptance_criteria>
  <reversibility rating="costly">DepthgramResult/DepthgramConfig are published contracts; shape fixed by CONTEXT/RESEARCH — flag only.</reversibility>
  <done>depthgram returns the six index vectors + numeric shape/magnitude outlier sets, the three p=1 representations are identical, errors on n==1/empty, is re-exported, and depthgram tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → sequential_transform_outliers/depthgram | Untrusted `FdMatrix` dimensions + `f64` values (incl. all-zero curves) cross into the detectors |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-04 | Denial of Service | seq-transform / depthgram entry | low | mitigate | n<2 (and D1 m<2, depthgram n<1) dimension guards return FdarError instead of panicking |
| T-29-05 | Tampering (numeric) | T2 normalization zero-norm curve | low | mitigate | Zero-L2-norm curve returns FdarError::ComputationFailed before dividing, preventing NaN/inf propagation |
| T-29-06 | Information Disclosure | crate deps | low | accept | No new crate dependency; no filesystem/network/shell reachable from numeric detectors (no package installs — no legitimacy gate needed) |
</threat_model>

<verification>
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib outliers::` (seq_transform + depthgram + Plan-01 tvdmss/muod all green)
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings` green (--all-targets mandatory — CI lints test/bench code, MEMORY.md pointer)
- `cargo build -p fdars-core` + `cargo build -p fdars-core --features serde` both compile (conditional serde on SeqTransform/SeqTransformOutliers/DepthgramResult + configs)
- Full suite: `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel` green
</verification>

<success_criteria>
- sequential_transform_outliers + depthgram are `Result`-returning public fns in outliers.rs, crate-root re-exported, numeric outputs only.
- Both reuse existing machinery (functional_boxplot; modified_band_1d/modified_epigraph_index_1d + outliergram parabola) and the Plan-01 iqr_fence; no new crate dependency.
- On synthetic fixtures depthgram flags magnitude + shape; seq_transform's union flags injected outliers; p=1 representations collapse identically.
- Invalid inputs (n<2, D1 m<2, T2 zero-norm, empty) return FdarError at each entry point, never panic.
- Existing outlier + DEPTH-01 signatures unchanged; full suite + clippy --all-targets green.
</success_criteria>

<output>
Create `.planning/phases/29-outlier-detector-suite/29-02-SUMMARY.md` when done
</output>
