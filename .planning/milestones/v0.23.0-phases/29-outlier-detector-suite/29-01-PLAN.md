---
phase: 29-outlier-detector-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/outliers.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [OUT-01]

estimate:
  tokens: 78000
  raw_tokens: 39000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "tvdmss(data, config) returns Result<TvdMssOutliers, FdarError> and flags magnitude outliers (via TVD functional boxplot) AND shape outliers (via lower IQR fence on MSS) as numeric index sets — no rendering (OUT-01, satisfies success-criteria 1+2)"
    - "tvdmss computes its detector from Phase 28 total_variation_depth_1d(data, data) (TvdMssResult{tvd, mss}) and reuses functional_boxplot for stage 2 — no reimplementation, no new crate dependency (OUT-01, success-criterion 3)"
    - "muod(data, config) returns Result<MuodResult, FdarError> with per-curve magnitude/amplitude/shape indices + flagged sets (upper IQR fence per index) — flags magnitude, amplitude, and shape outliers on synthetic fixtures (OUT-01, success-criteria 1+2)"
    - "Private iqr_fence(values, factor) -> (lower, upper) helper (Q1/Q3 via quantile_sorted + sort_nan_safe) exists once and is shared: tvdmss uses the LOWER fence on MSS, muod uses the UPPER fence per index"
    - "Empty / single-curve / n<3 / mismatched-dims / degenerate-column inputs return FdarError (InvalidDimension or ComputationFailed) at each entry point, never panic (OUT-01, success-criterion 4)"
    - "Existing magnitude_shape_outlyingness / outliergram / DEPTH-01 signatures are unchanged (additive/non-breaking); the crate compiles after this plan and clippy --all-targets --features linalg,parallel -- -D warnings is green (OUT-01, success-criterion 5)"
  artifacts:
    - "fdars-core/src/outliers.rs — adds TvdMssOutliers, MuodResult, TvdMssConfig, MuodConfig structs; iqr_fence + muod_indices private helpers; tvdmss + muod public fns; inline #[cfg(test)] tests"
    - "fdars-core/src/lib.rs — extends the pub use outliers::{...} block (~line 434) with tvdmss, muod, TvdMssOutliers, MuodResult (+ config structs)"
  key_links:
    - "tvdmss -> total_variation_depth_1d (depth/tvd.rs, Phase 28 pinned TvdMssResult) — the hard Phase-28 dependency"
    - "tvdmss stage 2 -> functional_boxplot (depth/dispatch.rs, DepthMethod::ModifiedBand) on reduced data, indices re-mapped to original via keep-vec"
    - "tvdmss + muod -> iqr_fence -> quantile_sorted + sort_nan_safe (helpers.rs)"
    - "lib.rs re-export -> crate root: tvdmss / muod / TvdMssOutliers / MuodResult callable as fdars_core::{...}"
---

<objective>
Add the first two OUT-01 outlier detectors to `fdars-core/src/outliers.rs` additively: `tvdmss` (the
TVD+MSSI two-stage detector — the tracer that wires the whole reuse chain end-to-end) and `muod`
(Massive Unsupervised Outlier Detection via per-curve regression on the pointwise mean). This plan
also introduces the shared private `iqr_fence` helper that all four detectors depend on.

`tvdmss` is the tracer: it is the ONE detector that exercises every layer this phase touches — the
Phase-28 `total_variation_depth_1d` dependency, the new `iqr_fence` helper, the `functional_boxplot`
reuse, a new `#[non_exhaustive]` result struct, and the `lib.rs` crate-root re-export path — proving
the full architecture before the remaining detectors follow.

Purpose: close the two regression-style gaps of OUT-01 (tvdmss + muod) and establish the shared
plumbing (iqr_fence + re-export block) for Plan 02.
Output: `TvdMssOutliers`, `MuodResult`, `TvdMssConfig`, `MuodConfig`, `iqr_fence`, `muod_indices`,
`tvdmss`, `muod` in `outliers.rs` + extended `lib.rs` re-export; inline tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-outlier-detector-suite/29-RESEARCH.md
@.planning/phases/29-outlier-detector-suite/29-PATTERNS.md
@fdars-core/src/outliers.rs
@fdars-core/src/depth/tvd.rs
@fdars-core/src/depth/dispatch.rs
@fdars-core/src/helpers.rs
@fdars-core/src/lib.rs
</context>

<artifacts_produced>
## Artifacts this plan produces (new public symbols)

| Symbol | Kind | Location |
|--------|------|----------|
| `TvdMssOutliers` | pub struct (result) | outliers.rs |
| `TvdMssConfig` | pub struct (config) | outliers.rs |
| `MuodResult` | pub struct (result) | outliers.rs |
| `MuodConfig` | pub struct (config) | outliers.rs |
| `tvdmss` | pub fn (TRACER detector) | outliers.rs |
| `muod` | pub fn (detector) | outliers.rs |
| `iqr_fence` | private helper (shared with Plan 02) | outliers.rs |
| `muod_indices` | private helper | outliers.rs |
| re-export extension | `pub use outliers::{...}` block | lib.rs ~line 434 |

Plan 02 adds: `SeqTransformOutliers`, `SeqTransform`, `SeqTransformConfig`, `DepthgramResult`,
`DepthgramConfig`, `sequential_transform_outliers`, `depthgram` (reusing this plan's `iqr_fence`).
</artifacts_produced>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: tvdmss two-stage detector (TRACER — wires TvdMssResult + iqr_fence + functional_boxplot + result struct + re-export end-to-end)</name>
  <files>fdars-core/src/outliers.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/outliers.rs — the OutligramResult struct (~L248-264) for the derive/attribute order to copy; the outliergram entry guard (~L279-286) for the n<3 InvalidDimension shape; the inline IQR block (~L313-319) that iqr_fence supersedes; the existing `#[cfg(test)] mod tests` block (~L449) to APPEND to (do NOT create a new mod tests); the `use crate::iter_maybe_parallel;` import (~L8).
    - .planning/phases/29-outlier-detector-suite/29-RESEARCH.md §"Algorithm Pinning → 1. tvdmss" (stage 1 lower fence on MSS + `< mean(mss)` restriction; stage 2 functional_boxplot on reduced TVD with central_region scaled by n_orig/n_reduced; defaults emp_factor_mss=1.5, emp_factor_tvd=1.5, central_region_tvd=0.5; Pitfall 1 central-region scaling) and §"Standard Stack → IQR Fence Helper".
    - .planning/phases/29-outlier-detector-suite/29-PATTERNS.md §"tvdmss" (entry-validation, depth-call `?` propagation, functional_boxplot call + re-map, Ok(Struct{...}) tail) and §"iqr_fence — Private IQR Helper" and §"pub use outliers::{…} — Re-Export Extension".
    - fdars-core/src/depth/tvd.rs L13-28,L87-90 — TvdMssResult{tvd, mss} fields + total_variation_depth_1d(data_obj, data_ori) -> Result signature.
    - fdars-core/src/depth/dispatch.rs L205-247 — FunctionalBoxplotResult{outliers, ...} + functional_boxplot(data, method, factor) -> Result signature + DepthMethod::ModifiedBand variant.
    - fdars-core/src/helpers.rs — sort_nan_safe and quantile_sorted signatures (called from iqr_fence).
    - fdars-core/src/matrix.rs L50-58,L94-106 — FdMatrix::from_column_major(data, nrows, ncols) -> Result, nrows/ncols/shape.
  </read_first>
  <behavior>
    - tvdmss on a sample with a magnitude outlier (one curve shifted by +10): `result.magnitude_outliers.contains(&outlier_idx)` is true.
    - tvdmss on a sample with a shape outlier (one high-frequency curve, ~same mean): `result.shape_outliers.contains(&shape_idx)` is true.
    - tvdmss returns `result.tvd.len() == n` and `result.mss.len() == n` (over original indices).
    - Empty matrix (0 rows) -> Err(FdarError::InvalidDimension).
    - n == 2 -> Err(FdarError::InvalidDimension) (inherited n>=3 guard).
    - The default config (TvdMssConfig::default() = emp_factor_mss 1.5, emp_factor_tvd 1.5, central_region_tvd 0.5) reproduces the fdaoutlier defaults.
  </behavior>
  <action>
    Introduce the shared private `iqr_fence(values: &[f64], factor: f64) -> (f64, f64)` helper FIRST,
    placed before the first detector that uses it: clone values, `crate::helpers::sort_nan_safe`,
    Q1 = `quantile_sorted(&sorted, 0.25)`, Q3 = `quantile_sorted(&sorted, 0.75)`, iqr = Q3-Q1, return
    `(q1 - factor*iqr, q3 + factor*iqr)`. Document in its rustdoc that it uses linear-interpolation
    quantiles (not R's floor-index quartiles), matching for n>20 (per RESEARCH divergence note).

    Add `TvdMssConfig { emp_factor_mss: f64, emp_factor_tvd: f64, central_region_tvd: f64 }` with a
    `Default` impl (1.5, 1.5, 0.5) and the standard derive/attribute stack. Add `TvdMssOutliers {
    magnitude_outliers: Vec<usize>, shape_outliers: Vec<usize>, tvd: Vec<f64>, mss: Vec<f64> }` with
    the exact derive order copied from OutligramResult: `#[derive(Debug, Clone, PartialEq)]`,
    `#[non_exhaustive]`, conditional serde.

    Implement `#[must_use = "..."] pub fn tvdmss(data: &FdMatrix, config: TvdMssConfig) ->
    Result<TvdMssOutliers, FdarError>`. Guard n<3 with FdarError::InvalidDimension (parameter "data",
    formatted expected/actual per the outliergram analog). Call `total_variation_depth_1d(data, data)?`
    for `tvd`/`mss`. Stage 1: `let (lower_mss, _) = iqr_fence(&depth.mss, config.emp_factor_mss);`
    mean_mss = sum(mss)/n; `shape_outliers` = indices i where `depth.mss[i] < lower_mss &&
    depth.mss[i] < mean_mss` (lower tail only, per RESEARCH). Stage 2: build `keep` = indices NOT in
    shape_outliers; if keep is empty or < 3, magnitude_outliers is empty (guard). Build a reduced
    FdMatrix from the kept rows via `FdMatrix::from_column_major` (assemble column-major: for each
    column j push data[(keep[r], j)]); call `functional_boxplot(&reduced, DepthMethod::ModifiedBand,
    config.emp_factor_tvd)?`; re-map `fbp.outliers` (reduced indices) back to original via `keep`.
    (Note: functional_boxplot recomputes ModifiedBand depth on the reduced envelope; the tvd vector is
    still returned in the result over original indices per the locked struct.) Handle the central-region
    scaling per RESEARCH Pitfall 1 with a rustdoc note that the scaling is `central_region_tvd *
    n_orig / n_reduced` — since fdars' `functional_boxplot` fixes the central region at 50%, document
    the divergence: fdars uses the standard 50% envelope on the reduced set rather than the R-scaled
    fraction, and note this in the tvdmss rustdoc. Return `Ok(TvdMssOutliers { magnitude_outliers,
    shape_outliers, tvd: depth.tvd, mss: depth.mss })`.

    Extend the `pub use outliers::{...}` block in lib.rs (~L434) to add `tvdmss`, `TvdMssOutliers`,
    `TvdMssConfig` (keep alphabetical-ish ordering matching the existing style; use Edit, not Write).

    Append inline tests to the EXISTING `#[cfg(test)] mod tests` block in outliers.rs (do not create a
    new one): a magnitude-outlier fixture, a shape-outlier fixture, an empty-matrix error case, and an
    n==2 error case. Build fixtures via FdMatrix::from_column_major following the RESEARCH §"Test
    Patterns" synthetic-data recipes. Do NOT put fenced implementation in comments; keep test assertions
    checkable (contains / matches!(..., Err(FdarError::InvalidDimension{..}))).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib outliers::tvdmss</automated>
  </verify>
  <acceptance_criteria>
    - `tvdmss(data: &FdMatrix, config: TvdMssConfig) -> Result<TvdMssOutliers, FdarError>` exists, is `#[must_use]`, crate-root re-exported (`fdars_core::tvdmss`, `fdars_core::TvdMssOutliers`, `fdars_core::TvdMssConfig` all resolve).
    - `TvdMssOutliers` has exactly the fields `magnitude_outliers: Vec<usize>, shape_outliers: Vec<usize>, tvd: Vec<f64>, mss: Vec<f64>` and derives `Debug, Clone, PartialEq` + `#[non_exhaustive]` + conditional serde.
    - Private `iqr_fence(&[f64], f64) -> (f64, f64)` exists once (Q1/Q3 via `quantile_sorted`) and is used by tvdmss's lower-fence stage.
    - tvdmss calls `total_variation_depth_1d(data, data)?` (no reimplementation of TVD/MSS) and `functional_boxplot(..., DepthMethod::ModifiedBand, ...)?` for stage 2.
    - `cargo test ... --lib outliers::tvdmss` exits 0: magnitude-outlier test flags the shifted curve, shape-outlier test flags the high-freq curve, empty + n==2 both return `FdarError::InvalidDimension`.
    - No existing public signature changed; crate compiles.
  </acceptance_criteria>
  <reversibility rating="costly">TvdMssOutliers/TvdMssConfig are published API contracts (costly to change post-release) but their shape is fixed by CONTEXT.md/RESEARCH — flag only, no checkpoint.</reversibility>
  <done>tvdmss flags magnitude + shape outliers on synthetic fixtures, returns FdarError on empty/n==2, is re-exported, and the crate compiles with tvdmss tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: muod detector (per-curve regression on pointwise mean, upper IQR fence per index)</name>
  <files>fdars-core/src/outliers.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/outliers.rs — the MagnitudeShapeResult struct (~L334-342) and magnitude_shape_outlyingness entry guard (~L353-360) for the (n,m) shape guard + InvalidDimension pattern; the column-mean loop (~L369-375) for the pointwise-mean pattern; the iter_maybe_parallel! usage; the iqr_fence helper added in Task 1 (REUSE it — do not add a second fence helper).
    - .planning/phases/29-outlier-detector-suite/29-RESEARCH.md §"Algorithm Pinning → 2. muod" (Fast-MUOD pointwise-mean regression; shape_index=|corr-1|, magnitude_index=|intercept|, amplitude_index=|slope-1|; boxplot cutoff = Q3+1.5·IQR upper whisker; n>=3 guard; A1 divergence to document) + §"Code Examples → muod" (the OLS formulas) + §"Common Pitfalls → Pitfall 2" (degenerate std guard).
    - .planning/phases/29-outlier-detector-suite/29-PATTERNS.md §"muod" (entry validation n<3 & m<2, iter_maybe_parallel! over curves, column-mean pattern, degenerate std guard 1e-15).
  </read_first>
  <behavior>
    - muod on a magnitude outlier (curve shifted by large constant): `result.magnitude_outliers.contains(&idx)`.
    - muod on an amplitude outlier (curve scaled by large factor): `result.amplitude_outliers.contains(&idx)`.
    - muod on a shape outlier (high-frequency curve): `result.shape_outliers.contains(&idx)`.
    - All six vectors (`shape_index`, `magnitude_index`, `amplitude_index` + the three outlier sets exist; the three index vectors have length n).
    - A constant curve does not produce NaN indices (degenerate-std guard sets shape index to 0.0).
    - n == 2 -> Err(FdarError::InvalidDimension); m < 2 -> Err(FdarError::InvalidDimension).
  </behavior>
  <action>
    Add `MuodConfig { factor: f64 }` (Default = 1.5) and `MuodResult { shape_outliers: Vec<usize>,
    magnitude_outliers: Vec<usize>, amplitude_outliers: Vec<usize>, shape_index: Vec<f64>,
    magnitude_index: Vec<f64>, amplitude_index: Vec<f64> }` with the standard derive/attribute stack
    copied from MagnitudeShapeResult.

    Add a private `muod_indices(data: &FdMatrix) -> (Vec<f64>, Vec<f64>, Vec<f64>)` returning
    (shape_index, magnitude_index, amplitude_index). Compute the pointwise mean `mu_t` (length m,
    iterate columns), `mu_mean`, `mu_var` = sum((mu_t-mu_mean)^2)/(m-1). Over curves via
    `iter_maybe_parallel!(0..n)`: per curve i compute xi_mean, cov_i = sum((X_i(t)-xi_mean)(mu_t-mu_mean))/(m-1),
    slope = cov_i/mu_var (guard mu_var<1e-15 -> slope 1.0), intercept = xi_mean - slope*mu_mean,
    xi_std and mu_std; corr = cov_i/(xi_std*mu_std) guarded (if either std<1e-15 -> corr 1.0);
    shape_index=|corr-1|, magnitude_index=|intercept|, amplitude_index=|slope-1| (per RESEARCH
    Code Example, benchmark c(1,0,1)). Collect and unzip into three Vec<f64>.

    Implement `#[must_use = "..."] pub fn muod(data: &FdMatrix, config: MuodConfig) ->
    Result<MuodResult, FdarError>`. Guard n<3 and m<2 with FdarError::InvalidDimension (parameter
    "data", formatted expected/actual). Call muod_indices; for EACH of the three index vectors take the
    UPPER fence `let (_, upper) = iqr_fence(&idx_vec, config.factor);` and flag indices where
    `idx_vec[i] > upper` (Q3+factor·IQR upper whisker per RESEARCH boxplot cutoff). Return
    Ok(MuodResult{...}). Add a rustdoc note documenting the A1 divergence (Fast-MUOD pointwise-mean
    regression vs R's pairwise-C++ block) and the `abs(index - benchmark)` convention (positive =
    more outlying); only the boxplot cutoff is implemented (tangent deferred to backlog).

    Extend the lib.rs `pub use outliers::{...}` block to add `muod`, `MuodResult`, `MuodConfig`
    (Edit, not Write).

    Append inline muod tests to the existing tests module: magnitude / amplitude / shape outlier
    fixtures (each asserting the right set contains the injected index), an n==2 error case, and an
    m==1 error case. Reuse the RESEARCH §"Test Patterns" synthetic recipes plus an amplitude fixture
    (one curve scaled).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib outliers::muod</automated>
  </verify>
  <acceptance_criteria>
    - `muod(data: &FdMatrix, config: MuodConfig) -> Result<MuodResult, FdarError>` exists, is `#[must_use]`, and `fdars_core::muod` / `MuodResult` / `MuodConfig` resolve.
    - `MuodResult` carries `shape_outliers, magnitude_outliers, amplitude_outliers: Vec<usize>` and `shape_index, magnitude_index, amplitude_index: Vec<f64>` (each index vector length n) with the standard derive/attribute stack.
    - `muod_indices` regresses each curve on the pointwise mean (no pairwise O(n²) block); iqr_fence UPPER fence flags each index; no second fence helper introduced.
    - `cargo test ... --lib outliers::muod` exits 0: magnitude/amplitude/shape fixtures each flag the injected curve; n==2 and m==1 both return `FdarError::InvalidDimension`; constant-curve test produces no NaN.
    - Existing signatures unchanged; crate compiles.
  </acceptance_criteria>
  <reversibility rating="costly">MuodResult/MuodConfig are published contracts; shape fixed by CONTEXT/RESEARCH — flag only.</reversibility>
  <done>muod flags magnitude, amplitude, and shape outliers on synthetic fixtures, returns FdarError on n==2 / m==1, is re-exported, and muod tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → tvdmss/muod | Untrusted `FdMatrix` dimensions + `f64` values cross into the detectors |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Denial of Service | tvdmss/muod entry | low | mitigate | n<3 / m<2 dimension guards at each entry return FdarError instead of panicking or looping on degenerate input |
| T-29-02 | Tampering (numeric) | muod_indices degenerate variance | low | mitigate | 1e-15 guards on xi_std/mu_std/mu_var prevent NaN propagation through the IQR fence |
| T-29-03 | Information Disclosure | crate deps | low | accept | No new crate dependency; no filesystem/network/shell reachable from numeric detectors (no package installs — no legitimacy gate needed) |
</threat_model>

<verification>
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib outliers::` (tvdmss + muod tests green)
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings` green (CI lints test/bench code — the --all-targets form is mandatory, MEMORY.md pointer)
- `cargo build -p fdars-core` (default features) + `cargo build -p fdars-core --features serde` both compile (conditional-serde derives on new structs)
- Existing tests still pass (no signature changes): `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib outliers`
</verification>

<success_criteria>
- tvdmss + muod are `Result`-returning public fns in outliers.rs, crate-root re-exported, numeric outputs only.
- tvdmss consumes Phase 28 `total_variation_depth_1d` and reuses `functional_boxplot`; muod reuses the shared `iqr_fence`; no new crate dependency.
- On synthetic fixtures tvdmss flags both magnitude and shape classes; muod flags magnitude, amplitude, and shape.
- Empty / n<3 / m<2 inputs return FdarError at each entry point, never panic.
- Existing outlier + DEPTH-01 signatures unchanged; full suite + clippy --all-targets green.
</success_criteria>

<output>
Create `.planning/phases/29-outlier-detector-suite/29-01-SUMMARY.md` when done
</output>
