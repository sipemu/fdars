---
phase: 06-conditional-svd-library-comparison
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/benches/audit_hotpaths.rs
  - .planning/research/AUDIT-REPORT.md
autonomous: true
requirements: [PERF-06]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC1: AUDIT-REPORT.md Phase 6 section states the go/no-go verdict (GO) and cites the Phase 4 SVD wall-clock share (~99.8-99.9%) and to_dmatrix() copy share (0.14-0.17%). (PERF-06 SC1)"
    - "SC2: A nalgebra-vs-faer SVD comparison table exists at fdars' real FPCA workload sizes (N in {100,500,1000} x M in {50,200} plus an M=500 crossover probe) with speedup, conversion-cost, and crossover columns; raw artifacts saved under .planning/research/bench/p6_svd_*. (PERF-06 SC2)"
    - "The numerical-equivalence #[test] confirms nalgebra and faer agree on singular values within tolerance and is sign-ambiguity-safe (compares singular VALUES only, not vectors). (PERF-06)"
    - "SC3: A faer adoption / maintenance-risk note in the Phase 6 report section assesses integration ROI (MSRV tension, API stability, bus factor, integration cost). (PERF-06 SC3)"
    - "SC4: A Phase 9 backlog item is drafted with function / current-cost / root-cause / candidate-fix / evidence / severity+effort fields, linking the p6_svd_* artifacts. (PERF-06 SC4)"
  artifacts:
    - "fdars-core/benches/audit_hotpaths.rs :: fn bench_p6_svd_comparison"
    - "fdars-core/benches/audit_hotpaths.rs :: fn generate_weighted_input"
    - "fdars-core/benches/audit_hotpaths.rs :: #[test] fn svd_equivalence"
    - ".planning/research/bench/p6_svd_nalgebra_linalg_run1.txt"
    - ".planning/research/bench/p6_svd_nalgebra_linalg_run2.txt"
    - ".planning/research/bench/p6_svd_faer_seq_linalg_run1.txt"
    - ".planning/research/bench/p6_svd_faer_seq_linalg_run2.txt"
    - ".planning/research/bench/p6_svd_conversion_linalg_run1.txt"
    - ".planning/research/AUDIT-REPORT.md :: ## Phase 6: Conditional SVD Library Comparison"
  key_links:
    - "bench_p6_svd_comparison is registered in criterion_group!(benches, ...) so cargo bench discovers it"
    - "generate_weighted_input replicates fdata_to_pc_1d's centering + Simpson-sqrt-weight scaling so the SVD comparison operates on the SAME matrix the shipped FPCA path feeds to SVD"
    - "faer sequential group is preceded by set_global_parallelism(Par::Seq) OUTSIDE b.iter() so faer(seq) vs nalgebra(always-seq) is apples-to-apples"
    - "AUDIT-REPORT Phase 6 section links each p6_svd_* artifact and the backlog item cites those artifacts as evidence"
---

<objective>
Perform the Phase 6 conditional SVD library comparison (nalgebra vs faer) at fdars' real FPCA
problem sizes, prove the two backends agree numerically, record the measured comparison table +
faer adoption note + Phase 9 backlog item in AUDIT-REPORT.md, and document that the go/no-go
decision is GO on Phase 4 evidence.

Purpose: PERF-06 — give the next milestone evidence-backed data on whether swapping nalgebra SVD
for faer in `fdata_to_pc_1d` is worth it, so the backlog item reflects integration ROI, not raw
speed alone.

Output: a throwaway comparison bench group `bench_p6_svd_comparison` (+ `generate_weighted_input`
helper + `svd_equivalence` #[test]) in the existing audit bench file; feature-tagged raw artifacts
under `.planning/research/bench/`; a new `## Phase 6` section appended to the single growing
AUDIT-REPORT.md.

AUDIT-ONLY: NO changes to `fdars-core/src`. The shipped `fdata_to_pc_1d` SVD path
(`regression.rs:298`) is NOT modified — the comparison lives only in the bench file + a throwaway
test.
</objective>

<phase_goal>
## Phase Goal (MVP user story)

**As a** milestone auditor, **I want to** measure faer vs nalgebra SVD at fdars' real FPCA sizes
and confirm they agree numerically, **so that** the Phase 9 backlog carries an evidence-backed
faer-adoption decision (speed + integration ROI), not a guess.

This phase is a single vertical slice: **measure -> verify equivalence -> record**. The tracer
(Task 1) proves the whole measure->record pipeline at one cell (N=500, M=200) before the grid fans
out.
</phase_goal>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-conditional-svd-library-comparison/06-RESEARCH.md
@.planning/phases/06-conditional-svd-library-comparison/06-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Every new symbol/file this phase creates (nothing in `fdars-core/src` is modified):

| Artifact | Location | Kind |
|----------|----------|------|
| `fn generate_weighted_input(n, m) -> (FdMatrix, Vec<f64>)` | `fdars-core/benches/audit_hotpaths.rs` | new bench helper |
| `fn bench_p6_svd_comparison(c: &mut Criterion)` | `fdars-core/benches/audit_hotpaths.rs` | new criterion bench fn |
| sub-group `audit_p6_svd_nalgebra` | inside `bench_p6_svd_comparison` | nalgebra baseline cells |
| sub-group `audit_p6_svd_faer_seq` | inside `bench_p6_svd_comparison` | faer sequential cells |
| sub-group `audit_p6_svd_conversion` | inside `bench_p6_svd_comparison` | conversion-cost attribution cells |
| `#[test] fn svd_equivalence()` | `fdars-core/benches/audit_hotpaths.rs` (`#[cfg(test)]`) | numerical-equivalence guard |
| `bench_p6_svd_comparison` entry in `criterion_group!(benches, ...)` | `fdars-core/benches/audit_hotpaths.rs` ~line 1043 | registration |
| `p6_svd_nalgebra_linalg_run{1,2}.txt`, `p6_svd_faer_seq_linalg_run{1,2}.txt`, `p6_svd_conversion_linalg_run1.txt` | `.planning/research/bench/` | raw bench artifacts |
| `## Phase 6: Conditional SVD Library Comparison` section | `.planning/research/AUDIT-REPORT.md` (appended) | report + backlog |
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end SVD-comparison tracer at one cell (helper + equivalence test + single-cell bench, registered)</name>
  <files>fdars-core/benches/audit_hotpaths.rs</files>
  <read_first>
    - fdars-core/benches/audit_hotpaths.rs (lines 1-57 imports + generate_curves; lines 760-840 bench_p4_fpca analog to mirror; lines 1040-1052 criterion_group! registration site)
    - fdars-core/src/regression.rs (lines 249-320 fdata_to_pc_1d — the centering + Simpson-sqrt-weight scaling at :283-296 the helper must replicate; SVD::new call at :298; DO NOT modify this file)
    - fdars-core/src/matrix.rs (line 291 `as_slice()` returns column-major `&[f64]`; line 50 `from_column_major`; line 422 `Index<(usize,usize)>` is read-only — there is NO IndexMut, so build the weighted matrix in a raw Vec)
    - .planning/phases/06-conditional-svd-library-comparison/06-RESEARCH.md (§"faer SVD API", §"Parallelism control", §"Numerical equivalence check", §"Code Examples" Pattern 1/2)
  </read_first>
  <action>
    Add three items to `fdars-core/benches/audit_hotpaths.rs`, wiring the full measure->verify->record
    path at the single cell N=500, M=200 before fanning out in Task 2.

    (1) Add helper `fn generate_weighted_input(n: usize, m: usize) -> (FdMatrix, Vec<f64>)` that
    reproduces the exact matrix `fdata_to_pc_1d` feeds to SVD. Steps: call `generate_curves(n, m)`
    to get `(data, argvals)`; center each column (subtract per-column mean) into a raw
    `Vec<f64>` in column-major order (index `i + j*n`) — replicating `center_columns` behavior since
    that fn is private in regression.rs; compute `simpsons_weights(&argvals)` (already re-exported
    from `fdars_core`) and `sqrt` each weight; multiply column j of the centered data by
    `sqrt_weights[j]`; build the result via `FdMatrix::from_column_major(weighted_vec, n, m).unwrap()`.
    Return `(weighted_fdmatrix, argvals)`. Do NOT use `weighted[(i,j)] *= ...` — FdMatrix has no
    IndexMut; mutate the raw Vec before constructing the FdMatrix.

    (2) Add `#[cfg(feature = "linalg")]` `#[test] fn svd_equivalence()` inside a `#[cfg(test)] mod`
    at the bottom of the file. Build the weighted input at N=500, M=200 via `generate_weighted_input`.
    Compute nalgebra singular values: `nalgebra::linalg::SVD::new(weighted.to_dmatrix(), true, true).singular_values`.
    Compute faer singular values: `faer::MatRef::<f64>::from_column_major_slice(weighted.as_slice(), n, m).thin_svd().unwrap()`,
    then extract via `svd.S().column_vector().as_slice()`. Compare the first `k = min(len_na, len_fa)`
    values element-wise (both are nonincreasing order) with relative error
    `(na_s[i] - fa_s[i]).abs() / na_s[i].max(1e-12) < 1e-10`. Compare singular VALUES only (never
    singular vectors — sign is arbitrary per column). Also assert the thin-SVD shape matches:
    `svd.U().nrows() == n` and `svd.U().ncols() == m` (for N > M, resolving RESEARCH Open Question 1).

    (3) Add `#[cfg(feature = "linalg")] fn bench_p6_svd_comparison(c: &mut Criterion)` with, for THIS
    tracer, ONLY the single cell N=500, M=200 across three sub-groups (mirroring the bench_p4_fpca
    group/sample_size/measurement_time/warm_up_time structure):
      - group `audit_p6_svd_nalgebra`: pre-build `let dm = weighted.to_dmatrix();` OUTSIDE `b.iter()`;
        inside iter: `black_box(nalgebra::linalg::SVD::new(black_box(dm.clone()), true, true))`.
      - group `audit_p6_svd_faer_seq`: call `faer::set_global_parallelism(faer::Par::Seq)` ONCE
        before `group.bench_function`, OUTSIDE `b.iter()`; pre-build `weighted` OUTSIDE iter; inside
        iter: `black_box(faer::MatRef::<f64>::from_column_major_slice(black_box(weighted.as_slice()), n, m).thin_svd())`.
      - group `audit_p6_svd_conversion`: inside iter: `black_box(faer::MatRef::<f64>::from_column_major_slice(black_box(weighted.as_slice()), n, m))`.
    Use `group.sample_size(20)`, `measurement_time(20s)`, `warm_up_time(5s)` for this cell.

    (4) Register `bench_p6_svd_comparison` in the `criterion_group!(benches, ...)` macro (the list
    ending at ~line 1052, after `bench_p5_streaming_payback_n`). Do NOT gate the criterion_group entry
    behind cfg — instead ensure the fn itself is `#[cfg(feature = "linalg")]` and add the registration
    line only inside a `#[cfg(feature = "linalg")]`-consistent path if the existing group already
    compiles without linalg; if the group is unconditional, keep the fn always-compiled by providing
    a `#[cfg(not(feature = "linalg"))]` empty stub `fn bench_p6_svd_comparison(_: &mut Criterion) {}`
    so the criterion_group! list always resolves. Verify against how existing bench fns are gated.

    Do NOT touch `fdata_to_pc_1d` or any file under `fdars-core/src/`.
  </action>
  <verify>
    <automated>export TMPDIR=/home/simonm/.cache/fdars-bench-tmp; mkdir -p "$TMPDIR"; cargo build -p fdars-core --features linalg --benches 2>&1 | tail -5; echo "BUILD_EXIT=${PIPESTATUS[0]}"</automated>
    <automated>export TMPDIR=/home/simonm/.cache/fdars-bench-tmp; cargo test -p fdars-core --features linalg --bench audit_hotpaths svd_equivalence 2>&1 | tail -15</automated>
    <automated>export TMPDIR=/home/simonm/.cache/fdars-bench-tmp; cargo bench -p fdars-core --features linalg --bench audit_hotpaths -- 'audit_p6_svd_faer_seq/n500_m200' 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p fdars-core --features linalg --benches` exits 0 (BUILD_EXIT=0).
    - `grep -n 'fn generate_weighted_input' fdars-core/benches/audit_hotpaths.rs` matches.
    - `grep -n 'fn bench_p6_svd_comparison' fdars-core/benches/audit_hotpaths.rs` matches.
    - `grep -n 'fn svd_equivalence' fdars-core/benches/audit_hotpaths.rs` matches.
    - `grep -n 'bench_p6_svd_comparison' fdars-core/benches/audit_hotpaths.rs` appears in the `criterion_group!` list.
    - `grep -n 'set_global_parallelism' fdars-core/benches/audit_hotpaths.rs` matches (faer seq fairness).
    - `cargo test ... svd_equivalence` reports `test result: ok` with 1 passed (nalgebra and faer singular values agree within 1e-10).
    - The single-cell `audit_p6_svd_faer_seq/n500_m200` bench runs to completion and criterion output references a `/release/` binary path (bench profile is opt-3, not debug).
    - No file under `fdars-core/src/` is modified (`git diff --name-only fdars-core/src/` is empty).
  </acceptance_criteria>
  <reversibility rating="reversible">New bench fn + throwaway test in an audit bench file; no shipped-code change — trivially removable.</reversibility>
  <done>generate_weighted_input, svd_equivalence (green), and a single-cell bench_p6_svd_comparison exist, compile under linalg, are registered in criterion_group!, and the equivalence test proves nalgebra==faer singular values — the full measure->verify pipeline works at N=500,M=200.</done>
</task>

<task type="auto">
  <name>Task 2: Expand bench to the full workload grid + M=500 crossover probe, run twice, save p6_svd_* artifacts</name>
  <files>fdars-core/benches/audit_hotpaths.rs</files>
  <read_first>
    - fdars-core/benches/audit_hotpaths.rs (the bench_p6_svd_comparison from Task 1; bench_p4_fpca lines 760-840 for the per-cell sample_size tiering pattern)
    - .planning/phases/06-conditional-svd-library-comparison/06-RESEARCH.md (§"Bench design" workload table lines 210-226; Pitfall F M=500 sample_size reduction; §"Artifact naming" Pattern 3)
    - .planning/research/bench/ (existing p4_fpca_*.txt for the artifact-format precedent)
  </read_first>
  <action>
    Expand each of the three sub-groups in `bench_p6_svd_comparison` from the single tracer cell to
    the full grid, iterating over the cell list
    `[(100,50,20),(100,200,20),(500,50,20),(500,200,20),(1000,50,20),(1000,200,10),(500,500,10)]`
    where the third tuple element is the criterion `sample_size` for that cell. For each cell call
    `group.sample_size(s_size)` before `group.bench_function(format!("n{n}_m{m}"), ...)`. The M=500
    crossover-probe cell (500,500) uses `sample_size(10)` and `measurement_time(30s)` per Pitfall F.
    Build the per-cell input (`weighted.to_dmatrix()` for nalgebra; `weighted` FdMatrix for faer/conversion)
    OUTSIDE `b.iter()` exactly as in the tracer. Keep `set_global_parallelism(Par::Seq)` once before the
    faer_seq group. Do NOT add a faer-parallel group (optional exploration only; skip to keep scope tight
    and the comparison fair-by-default).

    Then run the full `bench_p6_svd_comparison` group TWICE (run1, run2) for the +-5% variance rule
    (Pitfall 7) and capture output into the artifact files under `.planning/research/bench/`. Split the
    saved output by sub-group into the RESEARCH-specified artifact names. Use `tee` to persist raw
    criterion stdout. Concretely:
      run1: `cargo bench ... -- audit_p6 | tee .planning/research/bench/p6_svd_all_linalg_run1.txt`
      then extract/copy the nalgebra rows to `p6_svd_nalgebra_linalg_run1.txt`, the faer_seq rows to
      `p6_svd_faer_seq_linalg_run1.txt`, and the conversion rows to `p6_svd_conversion_linalg_run1.txt`
      (or, simpler and acceptable: run each sub-group filter separately with its own tee target — e.g.
      `-- audit_p6_svd_nalgebra | tee .../p6_svd_nalgebra_linalg_run1.txt`, likewise for
      `audit_p6_svd_faer_seq` and `audit_p6_svd_conversion`).
      run2: repeat the nalgebra and faer_seq filters into `*_run2.txt` (conversion needs only run1 per
      the RESEARCH artifact list).
    Every artifact file must contain the full set of grid cells it covers and a `/release/` path line
    proving release-profile execution. Always `export TMPDIR=/home/simonm/.cache/fdars-bench-tmp`
    before each cargo bench (Pitfall D /tmp exhaustion).

    Do NOT touch `fdars-core/src/`.
  </action>
  <verify>
    <automated>export TMPDIR=/home/simonm/.cache/fdars-bench-tmp; mkdir -p "$TMPDIR"; cargo build -p fdars-core --features linalg --benches 2>&1 | tail -3; echo "BUILD_EXIT=${PIPESTATUS[0]}"</automated>
    <automated>grep -c 'n500_m500\|n1000_m200\|n100_m50' fdars-core/benches/audit_hotpaths.rs</automated>
    <automated>ls -1 .planning/research/bench/p6_svd_nalgebra_linalg_run1.txt .planning/research/bench/p6_svd_nalgebra_linalg_run2.txt .planning/research/bench/p6_svd_faer_seq_linalg_run1.txt .planning/research/bench/p6_svd_faer_seq_linalg_run2.txt .planning/research/bench/p6_svd_conversion_linalg_run1.txt</automated>
    <automated>grep -l '/release/' .planning/research/bench/p6_svd_faer_seq_linalg_run1.txt</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p fdars-core --features linalg --benches` exits 0.
    - `bench_p6_svd_comparison` iterates all 7 cells: n100_m50, n100_m200, n500_m50, n500_m200, n1000_m50, n1000_m200, n500_m500 (grep the format!("n{n}_m{m}") loop cell list in the source).
    - The (500,500) crossover-probe cell uses `sample_size(10)` and `measurement_time` of 30s (grep the cell tuple + the M=500 sample tier in source).
    - All five artifacts exist: `p6_svd_nalgebra_linalg_run1.txt`, `p6_svd_nalgebra_linalg_run2.txt`, `p6_svd_faer_seq_linalg_run1.txt`, `p6_svd_faer_seq_linalg_run2.txt`, `p6_svd_conversion_linalg_run1.txt` under `.planning/research/bench/`.
    - Each nalgebra/faer_seq artifact contains all seven grid cell names and a `/release/` path line (`grep -c '/release/'` >= 1; `grep 'n500_m500'` matches).
    - No faer-parallel group was added (`grep 'Par::rayon\|Par::Rayon' fdars-core/benches/audit_hotpaths.rs | grep -v '^\s*//'` returns nothing).
    - No file under `fdars-core/src/` is modified (`git diff --name-only fdars-core/src/` empty).
  </acceptance_criteria>
  <reversibility rating="reversible">Bench grid expansion + text artifacts only; no shipped-code change.</reversibility>
  <done>bench_p6_svd_comparison covers the full 7-cell grid (incl. the M=500 crossover probe), and two-run nalgebra + faer_seq artifacts plus one conversion artifact are saved under .planning/research/bench/, all release-profile and feature-tagged.</done>
</task>

<task type="auto">
  <name>Task 3: Append the "## Phase 6" section to AUDIT-REPORT.md — go/no-go, comparison table, faer adoption note, Phase 9 backlog item</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - .planning/research/AUDIT-REPORT.md (lines 571-609 Phase 4 "Phase 6 Go/No-Go Decision" — the SVD-share/copy-share numbers to cite for SC1; the Phase 5 section format ~line 612 as the section-heading + table style to mirror; append AFTER the last line, do NOT create a new file per D-05)
    - .planning/research/bench/p6_svd_nalgebra_linalg_run1.txt, p6_svd_faer_seq_linalg_run1.txt, p6_svd_conversion_linalg_run1.txt (the measured numbers to transcribe into the table — Task 2 output)
    - .planning/phases/06-conditional-svd-library-comparison/06-RESEARCH.md (§"faer Adoption Note" lines 461-491 — the adoption-note fields and GSD-ready backlog phrasing to transcribe/refine with real numbers)
    - .planning/ROADMAP.md (Phase 6 four success criteria — the section must satisfy all four)
  </read_first>
  <action>
    Append a new top-level section `## Phase 6: Conditional SVD Library Comparison` to the END of
    `.planning/research/AUDIT-REPORT.md` (single growing file, D-05 — do NOT create a separate report
    file). The section MUST contain, in order:

    (1) Go/No-Go decision (SC1): a short paragraph stating the verdict is GO and citing the Phase 4
    evidence verbatim — SVD compute is ~99.8-99.9% of FPCA wall-clock AND the `to_dmatrix()` copy is
    0.14-0.17% of wall-clock — both SC1 conditions met, so the nalgebra-vs-faer comparison IS
    warranted. Reference the Phase 4 "Phase 6 Go/No-Go Decision" subsection as the source.

    (2) nalgebra-vs-faer SVD comparison table (SC2): one row per grid cell
    (n100_m50, n100_m200, n500_m50, n500_m200, n1000_m50, n1000_m200, n500_m500) with columns:
    Cell | nalgebra SVD time | faer(seq) thin_svd time | Speedup (nalgebra/faer) | FdMatrix->MatRef
    conversion cost | Notes/crossover. Transcribe the ACTUAL measured numbers from the p6_svd_*
    artifacts (do NOT invent). Add a one-line crossover observation identifying the M value (if any)
    where faer overtakes nalgebra, using the M=500 probe cell as the data point. Link each artifact:
    `[nalgebra run1](bench/p6_svd_nalgebra_linalg_run1.txt)`,
    `[faer_seq run1](bench/p6_svd_faer_seq_linalg_run1.txt)`,
    `[conversion](bench/p6_svd_conversion_linalg_run1.txt)` plus the run2 variance-check links. Note
    the numerical-equivalence test (`svd_equivalence`) passes within 1e-10 as the fairness guarantee.

    (3) faer adoption / maintenance-risk note (SC3): a table (adapting RESEARCH §"faer Adoption Note"
    lines 461-475) covering: performance vs nalgebra (now with real measured speedup, not just the
    ASSUMED 3-10x), API stability, breaking-change frequency, maintainer/bus factor, MSRV tension
    (faer 1.84 vs fdars 1.81 — already accepted since linalg is non-default), integration cost
    (~20 lines, zero-copy MatRef, faer already vendored), and test/correctness risk. End with a
    one-paragraph integration-ROI verdict.

    (4) Phase 9 backlog item (SC4): a GSD-ready backlog entry table with the exact fields used by the
    Phase 4/5 backlog entries: Function | Current cost (measured) | Root cause | Candidate fix |
    Evidence | Severity + Effort. Function = swap nalgebra SVD in `fdata_to_pc_1d` (regression.rs:298)
    for faer `thin_svd` behind the existing `linalg` feature via `MatRef::from_column_major_slice`
    (zero-copy). Current cost / evidence = the real Phase 6 measured speedup + artifact links. Set
    Severity + Effort from the measured result (if faer wins >=2x at M>=200: P2/S; if faer loses or is
    <1.2x at fdars' tall-thin sizes: downgrade to P3 per RESEARCH Assumption A1). Base the verdict on
    the ACTUAL numbers, not the ASSUMED range.

    Update the AUDIT-REPORT "last updated" line if present (append Phase 6). Do NOT modify prior phase
    sections.
  </action>
  <verify>
    <automated>grep -n '## Phase 6: Conditional SVD Library Comparison' .planning/research/AUDIT-REPORT.md</automated>
    <automated>grep -c 'bench/p6_svd_' .planning/research/AUDIT-REPORT.md</automated>
    <automated>grep -in 'GO\|99.8\|99.9' .planning/research/AUDIT-REPORT.md | tail -5</automated>
    <automated>grep -in 'MSRV\|1.84\|thin_svd\|regression.rs:298' .planning/research/AUDIT-REPORT.md | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/research/AUDIT-REPORT.md` contains exactly one new `## Phase 6: Conditional SVD Library Comparison` heading appended at the end (prior `## Phase 5` section unchanged — `git diff` shows only additions after the Phase 5 section).
    - SC1: the Phase 6 section states verdict GO and cites both the ~99.8-99.9% SVD share and the 0.14-0.17% copy share attributed to Phase 4.
    - SC2: a table with a row per grid cell (all 7 cells present) and columns for nalgebra time, faer(seq) time, speedup, conversion cost, and a crossover note; links to at least 3 distinct `bench/p6_svd_*` artifacts (`grep -c 'bench/p6_svd_'` >= 3).
    - SC3: a faer adoption note covers MSRV (1.84 vs 1.81), API stability, bus factor, and integration cost.
    - SC4: a Phase 9 backlog entry with Function / Current cost / Root cause / Candidate fix / Evidence / Severity+Effort fields references `fdata_to_pc_1d`/`regression.rs:298` and `thin_svd`, with severity set from the MEASURED speedup.
    - The numbers in the table are traceable to the on-disk p6_svd_* artifacts (not invented).
  </acceptance_criteria>
  <reversibility rating="reversible">Appends a report section to a docs file; no code change; revert = delete the section.</reversibility>
  <done>AUDIT-REPORT.md carries a complete "## Phase 6" section satisfying all four ROADMAP success criteria: GO decision citing Phase 4 evidence (SC1), a measured nalgebra-vs-faer table with speedup/conversion/crossover linking the artifacts (SC2), an evidence-backed faer adoption/risk note (SC3), and a GSD-ready Phase 9 backlog item (SC4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries cross in this phase. All computation is a local criterion benchmark + one
`#[test]` operating on deterministically-generated synthetic in-memory `FdMatrix` data. No network,
no untrusted/attacker-controlled input, no persistence beyond local text artifacts under
`.planning/research/bench/`, no authentication, no user input.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | N/A | local offline synthetic-data benchmark | low | accept | No applicable threats — local, offline, synthetic-data benchmark; no attacker-controlled input surface (ASVS L1: N/A). No new dependency introduced (faer already vendored under `linalg`). |
</threat_model>

<verification>
Phase-level checks (all must pass before the phase is complete):

1. `export TMPDIR=/home/simonm/.cache/fdars-bench-tmp && cargo build -p fdars-core --features linalg --benches` exits 0.
2. `cargo test -p fdars-core --features linalg --bench audit_hotpaths svd_equivalence` reports `test result: ok` (1 passed) — nalgebra and faer singular values agree within 1e-10.
3. `bench_p6_svd_comparison` is registered in `criterion_group!` and runs all 7 grid cells across the three sub-groups (`audit_p6_svd_nalgebra`, `audit_p6_svd_faer_seq`, `audit_p6_svd_conversion`).
4. Five artifacts present under `.planning/research/bench/`: p6_svd_nalgebra_linalg_run{1,2}.txt, p6_svd_faer_seq_linalg_run{1,2}.txt, p6_svd_conversion_linalg_run1.txt — each release-profile and feature-tagged.
5. `## Phase 6` section appended to AUDIT-REPORT.md satisfying ROADMAP SC1-SC4.
6. `git diff --name-only fdars-core/src/` is empty (audit-only: no shipped-code change).

Backstop (human_needed) predicates — the checker cannot confirm these without running the bench +
reading numbers; they abstain to human review rather than silently passing:
- { statement: "the nalgebra-vs-faer table numbers match the on-disk p6_svd_* artifacts within +-5% across run1/run2", verification: backstop }
- { statement: "the M=500 crossover observation correctly identifies whether/where faer overtakes nalgebra at fdars' tall-thin sizes", verification: backstop }
- { statement: "the backlog-item severity (P2/P3) is set from the MEASURED speedup, not the ASSUMED 3-10x range", verification: backstop }
</verification>

<success_criteria>
- PERF-06 SC1: documented GO decision in AUDIT-REPORT.md cites Phase 4 SVD-share (~99.8-99.9%) + copy-share (0.14-0.17%).
- PERF-06 SC2: nalgebra-vs-faer SVD table at all 7 workload cells with speedup + conversion-cost + crossover columns, artifacts under `.planning/research/bench/`.
- PERF-06 SC3: faer adoption / maintenance-risk note (MSRV, API stability, bus factor, integration cost) present.
- PERF-06 SC4: GSD-ready Phase 9 backlog item drafted with all six fields, severity set from measured data.
- Fairness guard: `svd_equivalence` #[test] green (nalgebra == faer singular values within 1e-10; sign-ambiguity-safe).
- Audit-only invariant: no modification to any `fdars-core/src/` file.
</success_criteria>

<output>
Create `.planning/phases/06-conditional-svd-library-comparison/06-01-SUMMARY.md` when done.
</output>