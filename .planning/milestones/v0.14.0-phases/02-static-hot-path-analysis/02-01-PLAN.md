---
phase: 02-static-hot-path-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/AUDIT-REPORT.md
autonomous: true
requirements: [PERF-01]
estimate:
  tokens: 55000
  raw_tokens: 30000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "AUDIT-REPORT.md contains a new `## Phase 2 — Static Hot-Path Analysis` section appended below the Phase 1 `§Workload Matrix` content, with Phase 1 content unmodified"
    - "The `### Complexity Table` has an Elastic Alignment row giving Big-O in N and M separately (unbanded O(max_iter·N·m²), banded O(max_iter·N·m·band)) and citing `karcher.rs`/`pairwise.rs` file:line anchors"
    - "The `### Allocation Hotspot List` enumerates all 6 `elastic_fpca.rs` + `alignment/nd.rs` `to_dmatrix()` SVD sites relevant to the elastic path (lines 214, 317, 483, 584, 930 in elastic_fpca.rs; 705 in nd.rs) each with a `[always]` feature-gate tag"
    - "The `### Parallelism Gap List` labels `karcher.rs:185` and `pairwise.rs:227` as ALREADY PARALLEL (not gaps) and flags the `elastic_fpca.rs` inner N-loops (701, 720, 764) as SEQUENTIAL gap candidates, plus notes banding opt-in (karcher_mean default band_frac=0.0)"
    - { statement: "The enclosing function name for the elastic_fpca.rs:930 to_dmatrix() site is confirmed by reading source (research Open Question 2) before the allocation entry is written", verification: backstop }
    - { statement: "Every elastic-slice finding carries a feature-gate annotation ([always] / [parallel-gated] / [sequential]); no macro-wrapped loop is mislabeled sequential", verification: backstop }
  artifacts:
    - .planning/research/AUDIT-REPORT.md
  key_links:
    - "The three sub-section headers (### Complexity Table, ### Allocation Hotspot List, ### Parallelism Gap List) established here are the format contract Plan 02 appends rows into"
---

<objective>
**As a** future-milestone planner, **I want to** see the zero-cost static hot-path map for the elastic alignment module proven end-to-end in the audit report, **so that** the report's three-list + complexity-table format is validated on the known worst case before it is expanded to the other five modules.

This is the **tracer slice** for Phase 2. It writes the `## Phase 2 — Static Hot-Path Analysis` section into `.planning/research/AUDIT-REPORT.md` with all three sub-sections (Complexity Table, Allocation Hotspot List, Parallelism Gap List) but populated ONLY for the elastic alignment module — one complexity row, the elastic `to_dmatrix()` SVD sites, and the elastic parallelism findings. This proves every format end-to-end on one module (research §7 recommendation) so Plan 02 can expand to the remaining 5 modules by adding rows into the same tables.

Purpose: De-risk the report format on the highest-complexity, most-fragile module (elastic alignment, O(max_iter·N·m²)) before expanding.
Output: The new Phase 2 section appended to AUDIT-REPORT.md with three sub-sections and their elastic-module content.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-static-hot-path-analysis/02-RESEARCH.md
@.planning/phases/02-static-hot-path-analysis/02-VALIDATION.md
@.planning/research/AUDIT-REPORT.md
</context>

<artifacts_this_phase_produces>
This is an **analysis-only phase**. No `fdars-core` source symbols, files, types, or functions are created or modified. The ONLY artifact is markdown appended to `.planning/research/AUDIT-REPORT.md`:

- `## Phase 2 — Static Hot-Path Analysis` (new top-level section, appended below Phase 1's `§Workload Matrix`)
  - `### Complexity Table` (this plan seeds the header + the Elastic Alignment row)
  - `### Allocation Hotspot List` (this plan seeds the header + the elastic `to_dmatrix()` SVD entries)
  - `### Parallelism Gap List` (this plan seeds the header + the elastic parallelism findings)

Plan 02 appends the remaining 5 modules' rows/entries into these same three sub-sections. No new files are created (D-05: single growing report).
</artifacts_this_phase_produces>

<schema_note>
N/A — no schema surface. fdars is a numeric Rust library with no ORM/database schema. No schema-push task applies.
</schema_note>

<tasks>

<task type="tracer">
  <name>Task 1: Append the Phase 2 section skeleton + all three sub-section headers</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - .planning/research/AUDIT-REPORT.md (READ the Phase 1 `## Phase 1`, `§Methodology`, `§Workload Matrix` sections and the closing italic line at the end so the new section matches the established heading style, table style, and file:line citation format; note the closing `*Full report sections ... Phases 2–9.*` line — append ABOVE or replace-around it appropriately, keeping Phase 1 content byte-for-byte intact)
    - .planning/phases/02-static-hot-path-analysis/02-RESEARCH.md §6 (append protocol + exact sub-section header names) and §Architecture Patterns (the ASCII layout showing table column shapes)
  </read_first>
  <action>
    Using the Edit tool (NOT Write — D-05 append-not-overwrite rule), append a new `## Phase 2 — Static Hot-Path Analysis` section to `.planning/research/AUDIT-REPORT.md` below the existing `§Workload Matrix` content. Place it before the trailing `*Full report sections ... Phases 2–9.*` italic line (move that line to the end of the new content, or append after it and leave one copy at the very bottom — do not duplicate it).

    Create the three empty-bodied sub-sections with their exact headers from RESEARCH §6: `### Complexity Table`, `### Allocation Hotspot List`, `### Parallelism Gap List`. Under each, write a one-line intro and the markdown table header row + separator row ONLY (no data rows yet — those come in Task 2 and Plan 02). Use these column contracts (match RESEARCH §Architecture ASCII):
    - Complexity Table columns: `Module | Primary function (file:line) | N complexity | M complexity | Feature gate | Fragile flag`
    - Allocation Hotspot List columns: `Site (file:line) | Category | Enclosing fn | Alloc size | Feature gate | Phase target`
    - Parallelism Gap List columns: `Loop (file:line) | Status | Parallelism macro | Feature gate tag | Gap candidate?`

    Also add a short lead paragraph under `## Phase 2` stating this section is a zero-runtime static analysis (no code changes, no benchmarks) and that the ROADMAP's "8 SVD-copy sites" claim is confirmed as **8 production sites** (one further `to_dmatrix()` call is test-only, under `#[cfg(test)]`, and is excluded from the allocation hotspot list — RESEARCH §2/Pitfall 2). Do NOT write the specific test-only line reference in this skeleton — the exclusion is stated authoritatively in Plan 02 Task 2. Do NOT paste RESEARCH tables verbatim; author fresh prose in the report's Phase 1 style. Do NOT put fenced code blocks in the tables.
  </action>
  <verify>
    <automated>test $(grep -c "^## Phase 2 — Static Hot-Path Analysis" .planning/research/AUDIT-REPORT.md) -eq 1 && grep -q "^### Complexity Table" .planning/research/AUDIT-REPORT.md && grep -q "^### Allocation Hotspot List" .planning/research/AUDIT-REPORT.md && grep -q "^### Parallelism Gap List" .planning/research/AUDIT-REPORT.md && grep -q "^## Phase 1 — Measurement Discipline" .planning/research/AUDIT-REPORT.md && grep -q "^## §Workload Matrix" .planning/research/AUDIT-REPORT.md</automated>
  </verify>
  <done>The Phase 2 section and all three sub-section headers exist; Phase 1 content (`## Phase 1`, `§Methodology`, `§Workload Matrix`) is still present and unmodified; three table header rows are in place with no data rows yet.</done>
</task>

<task type="auto">
  <name>Task 2: Populate all three sub-sections with the Elastic Alignment module content only</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - fdars-core/src/alignment/karcher.rs (READ lines ~170–432: confirm `iter_maybe_parallel!` at 185; confirm `karcher_mean` passes band_frac=0.0 → None at ~300; confirm `karcher_mean_impl` loop nesting at ~323 for the Big-O)
    - fdars-core/src/alignment/pairwise.rs (READ lines ~185–241: confirm `elastic_self_distance_matrix` full-DP no-band at 194 and `iter_maybe_parallel!(0..n)` at 227)
    - fdars-core/src/elastic_fpca.rs (READ around lines 214, 317, 483, 584, 705-analog, and 918–940 — the last to CONFIRM the enclosing function name for the `to_dmatrix()` at line 930, per RESEARCH Open Question 2, before writing its allocation entry; also READ 701, 720, 764 to confirm the sequential `for i in 0..n` inner loops)
    - fdars-core/src/alignment/nd.rs (READ around line 705: confirm `SVD::new(gram.to_dmatrix(), …)` for the ND elastic FPCA Gram matrix)
    - .planning/phases/02-static-hot-path-analysis/02-RESEARCH.md §2A, §3C, §3D, §3E, §3F, §4 Module 1 (the pre-verified anchors — lift the file:line data from here, but confirm each against the source reads above)
    - .planning/research/AUDIT-REPORT.md (re-read the just-written table headers to match column order)
  </read_first>
  <action>
    Using the Edit tool, add data rows to the three Phase 2 tables for the **Elastic Alignment module ONLY**:

    COMPLEXITY TABLE — one row: Module = "Elastic alignment"; Primary function = `karcher_mean` → `karcher_mean_impl` (`alignment/karcher.rs:323`), secondary `elastic_self_distance_matrix` (`alignment/pairwise.rs:194`); N complexity and M complexity stated SEPARATELY per RESEARCH Pitfall 4 — unbanded `O(max_iter·N·m²)`, banded `O(max_iter·N·m·band)`, and explicitly note N-scaling is unchanged by banding; Feature gate = `[parallel-gated]` (inner N-loop uses `iter_maybe_parallel!`); Fragile flag = "banding opt-in; default `karcher_mean()` uses O(m²) per pair".

    ALLOCATION HOTSPOT LIST — add the elastic `to_dmatrix()` SVD sites (Category = `to_dmatrix() SVD copy`, Feature gate = `[always]`, Phase target = Phase 4 dhat): `elastic_fpca.rs:214` (horiz_fpca), `:317` (joint_fpca), `:483` (vert_fpca), `:584` (joint_fpca 2nd path), `:930` (**use the enclosing fn name you confirmed from source, NOT a guess**), and `alignment/nd.rs:705` (ND elastic FPCA Gram matrix, size m×m). State each allocates a new `DMatrix<f64>` that is SVD'd then discarded.

    PARALLELISM GAP LIST — add the elastic findings:
    - `karcher.rs:185` — Status = ALREADY PARALLEL, Parallelism macro = `iter_maybe_parallel!`, tag = `[parallel-gated]`, Gap candidate? = No.
    - `pairwise.rs:227` — Status = ALREADY PARALLEL, macro = `iter_maybe_parallel!`, tag = `[parallel-gated]`, Gap = No.
    - `elastic_fpca.rs:701` (shooting_vectors_from_psis), `:720` (build_augmented_srsfs), `:764` (svd_scores_and_eigenvalues inner i-loop) — Status = SEQUENTIAL, macro = none, tag = `[sequential]`, Gap = Yes (candidates for Phase 5). Confirm each has NO `iter_maybe_parallel!` at its cited line before labeling it a gap (Manual-Only Verification in 02-VALIDATION.md).
    - Add a note line: banding is opt-in — `karcher_mean()` (`karcher.rs:~300`) passes `band_frac=0.0` → `None` (full DP); `karcher_mean_banded()` required to enable banding. Tag = `[always]`.

    Every row/entry MUST carry one of `[always]` / `[parallel-gated]` / `[sequential]` / `[linalg-gated]`. Do NOT include the `#[cfg(test)]` site `matrix.rs:682`. Point to RESEARCH §2A/§3/§4 for anchor data; author the prose fresh in the report's style. No fenced code blocks inside the tables.
  </action>
  <verify>
    <automated>test $(grep -c "elastic_fpca.rs:" .planning/research/AUDIT-REPORT.md) -ge 4 && grep -q "karcher.rs:185" .planning/research/AUDIT-REPORT.md && grep -q "pairwise.rs:227" .planning/research/AUDIT-REPORT.md && grep -Eq "O\(max_iter" .planning/research/AUDIT-REPORT.md && grep -qi "already parallel" .planning/research/AUDIT-REPORT.md && ! grep -q "matrix.rs:682" .planning/research/AUDIT-REPORT.md</automated>
    <human-check>Confirm the elastic_fpca.rs:930 allocation entry names the real enclosing function (read from source, not RESEARCH), and that no loop labeled SEQUENTIAL is actually wrapped in a parallelism macro at its cited line.</human-check>
  </verify>
  <done>The three Phase 2 tables each contain the Elastic Alignment content: one complexity row with N- and M-scaling stated separately, ≥5 elastic `to_dmatrix()` SVD allocation entries all `[always]`, and parallelism entries correctly labeling karcher.rs:185/pairwise.rs:227 as ALREADY PARALLEL and elastic_fpca inner loops as SEQUENTIAL gaps, with the banding-opt-in note. `matrix.rs:682` is absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan performs read-only static analysis of local `fdars-core/src/` files and writes markdown to a local file (`.planning/research/AUDIT-REPORT.md`). No network I/O, no user-supplied input, no secrets, no auth surface, no execution of `fdars-core` code, no package installs.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | — | Read-only local source analysis → local markdown | n/a | accept | No applicable threats (read-only local analysis; no attack surface). ASVS L1 categories V2–V6 all N/A per 02-RESEARCH.md Security Domain. |
</threat_model>

<verification>
Run the SC greps that apply to the elastic slice against `.planning/research/AUDIT-REPORT.md`:
- SC1 (partial): `grep -c "O(n\|O(max_iter" .planning/research/AUDIT-REPORT.md` returns ≥ 1 (elastic row present; full ≥6 satisfied by Plan 02).
- SC2 (partial): `grep -c "to_dmatrix" .planning/research/AUDIT-REPORT.md` ≥ 5 (elastic SVD sites present; full ≥8 in Plan 02).
- SC3/SC4 (partial): `grep -Eic "already parallel|sequential" AUDIT-REPORT.md` ≥ 3 and every elastic finding carries a `[…]` gate tag.
- Provenance backstop: for each cited `file:line`, `grep -n "<function>" fdars-core/src/<file>` confirms the line still exists.
</verification>

<success_criteria>
- Phase 2 section + three sub-section headers appended; Phase 1 content intact.
- Elastic alignment complexity row states N- and M-scaling separately with banded/unbanded forms.
- Elastic `to_dmatrix()` SVD sites listed with `[always]` gates; `matrix.rs:682` excluded.
- karcher.rs:185 & pairwise.rs:227 labeled ALREADY PARALLEL; elastic_fpca inner loops labeled SEQUENTIAL gaps; banding opt-in noted.
- elastic_fpca.rs:930 enclosing fn confirmed from source.
</success_criteria>

<output>
Create `.planning/phases/02-static-hot-path-analysis/02-01-SUMMARY.md` when done.
</output>
