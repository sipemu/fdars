---
phase: 08-capability-parity-matrix-categorization
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - .planning/research/AUDIT-REPORT.md
autonomous: true
requirements: [GAP-03, GAP-04]
estimate:
  tokens: 75000
  raw_tokens: 42000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Separate in-scope vs out-of-scope gap counts are reported, with plotting/IO excluded from the actionable count (SC2, GAP-03, Pitfall 14)"
    - "A reverse-parity strengths table documents every fdars capability with no scikit-fda 0.10.1 equivalent, at minimum model explainability, SPM/control charts, seasonal decomposition, and streaming depth (SC3, GAP-04, D-04)"
    - "The reverse sweep walks the STRUCTURE.md module map for fdars-only capabilities (conformal, tolerance, GMM, matrix profile, SSA, Hilbert/WIRE, FAMM, elastic changepoint, robust L1/Huber, multi-response, Andrews) — not only the four headline areas (D-04)"
    - "Draft gap-backlog entries carry area / current-gap / root-cause fields and stay UNRANKED (ranking is Phase 9) (SC4)"
    - "A drafted backlog item recommends a comparative numerical-accuracy validation pass for the fragile areas (D-02a)"
  artifacts:
    - ".planning/research/AUDIT-REPORT.md (appended: separated-counts subsection, reverse-parity strengths table, drafted gap-backlog subsection)"
  key_links:
    - "Separated counts are computed from the six area parity tables (Plans 01+02), out-of-scope carried from Phase-7 Relevance taxonomy"
    - "Reverse sweep verdicts (scikit-fda equivalent: none/partial) source-confirmed against fdars-core/src module map"
    - "Draft backlog entries feed Phase 9 (RPT-02) value-ranking; the D-02a accuracy-validation item captures the deferred smoke-test"
---

<objective>
Close out Phase 8 by (1) computing and reporting the separated in-scope vs out-of-scope gap
counts with plotting/IO excluded from the actionable total (GAP-03, Pitfall 14); (2) running
the full reverse-parity strengths sweep documenting every fdars capability with no scikit-fda
0.10.1 equivalent (GAP-04, D-04); and (3) drafting the UNRANKED gap-backlog with
area/current-gap/root-cause fields plus the D-02a deferred-accuracy-validation item (SC4).

Purpose: Turn the completed parity matrix into a categorized, strengths-aware, backlog-ready
gap picture that Phase 9 value-ranks. Output: three subsections appended into the same
`## Phase 8` section. Zero fdars-core/src edits (audit-only milestone).
</objective>

<user_story>
**As a** maintainer preparing the next milestone,
**I want** the gap counts separated (actionable vs out-of-scope), fdars' true strengths enumerated, and an unranked draft backlog with root causes,
**so that** Phase 9 can value-rank a complete, honest picture instead of an inflated "fdars is behind" list.
</user_story>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-capability-parity-matrix-categorization/08-CONTEXT.md
@.planning/research/AUDIT-REPORT.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONCERNS.md
@.planning/phases/08-capability-parity-matrix-categorization/08-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
No source symbols (audit-only). This plan appends three subsections into the existing
`## Phase 8 — Capability Parity Matrix & Categorization` section of
`.planning/research/AUDIT-REPORT.md`:
- `### Gap Counts (in-scope vs out-of-scope)` — separated tallies (Pitfall 14)
- `### Reverse-Parity Strengths Sweep (D-04)` — fdars-only capabilities table
- `### Drafted Gap Backlog (unranked)` — area/current-gap/root-cause entries + the D-02a
  accuracy-validation item
These complete the Phase 8 deliverable set. Ranking is Phase 9 (RPT-02) — not here.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Report separated in-scope vs out-of-scope gap counts (Pitfall 14)</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - The six area parity tables written by Plans 01+02 (§Area: * — Parity under `## Phase 8`) — the source of the gap tally
    - .planning/research/AUDIT-REPORT.md §"Phase 7" → §"Design-Goal Filter" (the 129 in-scope / 32 out-of-scope / 161 total counts + the out-of-scope taxonomy: plotting/IO/type-system)
    - .planning/phases/08-capability-parity-matrix-categorization/08-CONTEXT.md D-03 (categorization rubric) and Pitfall 14 (separated counts)
  </read_first>
  <action>Append a `### Gap Counts (in-scope vs out-of-scope)` subsection into the `## Phase 8` section. From the six area parity tables, tally: total in-scope capabilities mapped (129), how many are present / partial / absent, and — crucially — the ACTIONABLE gap count (partial + absent among in-scope rows) categorized by D-03 as table-stakes vs differentiator. Report the 32 out-of-scope (plotting/IO/type-system) capabilities as a SEPARATE count carried from the Phase-7 Design-Goal Filter, explicitly excluded from the actionable total so the report cannot read as "fdars is far behind" (Pitfall 14). Present as a small counts table plus one or two sentences of narrative. Prose and tables only — no fenced code, no fdars-core/src edits.</action>
  <verify>
    <automated>grep -q "### Gap Counts (in-scope vs out-of-scope)" .planning/research/AUDIT-REPORT.md && grep -Eq "actionable|out-of-scope" .planning/research/AUDIT-REPORT.md && git diff --name-only -- fdars-core/src | grep -qv .</automated>
  </verify>
  <acceptance_criteria>
    - `### Gap Counts (in-scope vs out-of-scope)` subsection exists under `## Phase 8`
    - In-scope present/partial/absent tallies are reported, plus an actionable gap count split table-stakes vs differentiator (D-03)
    - The 32 out-of-scope (plotting/IO/type-system) count is reported SEPARATELY and stated to be excluded from the actionable total (Pitfall 14)
    - `git diff --name-only -- fdars-core/src` is empty
  </acceptance_criteria>
  <done>Separated gap counts reported with the out-of-scope count excluded from the actionable total; no source edits.</done>
</task>

<task type="auto">
  <name>Task 2: Full reverse-parity strengths sweep (D-04, GAP-04)</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - .planning/codebase/STRUCTURE.md §"Directory Layout" + §"Directory Purposes" (the fdars module map — walk it for fdars-only capabilities)
    - .planning/codebase/ARCHITECTURE.md (module responsibilities — confirm each fdars-only verdict)
    - .planning/phases/08-capability-parity-matrix-categorization/08-CONTEXT.md D-04 (full reverse sweep; the candidate list: conformal prediction, tolerance bands, GMM clustering, matrix profile, SSA, Hilbert transform, WIRE, FAMM, elastic changepoint, robust L1/Huber regression, multi-response regression, Andrews curves)
    - .planning/research/AUDIT-REPORT.md §"Phase 7" area tables (to confirm no scikit-fda 0.10.1 equivalent exists for each fdars strength)
    - fdars-core/src/ (explain/, explain_generic/, spm/, seasonal/, streaming_depth/, conformal/, tolerance/, elastic_changepoint.rs, wire.rs, famm.rs, andrews.rs, scalar_on_function/ for L1/Huber/multi-response — confirm each capability exists)
  </read_first>
  <action>Append a `### Reverse-Parity Strengths Sweep (D-04)` subsection with a reverse table (columns: fdars Capability | fdars module | scikit-fda 0.10.1 equivalent: none / partial | Confidence). Walk the STRUCTURE.md module map and enumerate EVERY fdars capability with no scikit-fda equivalent, not just the four SC3 headline areas. At minimum include the four headliners (model explainability in explain/ + explain_generic/, SPM/control charts in spm/, seasonal decomposition in seasonal/, streaming depth in streaming_depth/) AND the D-04 candidate list (conformal prediction in conformal/, tolerance bands in tolerance/, GMM clustering in gmm/, matrix profile, SSA, Hilbert transform, WIRE in wire.rs, FAMM in famm.rs, elastic changepoint in elastic_changepoint.rs, robust L1/Huber regression + multi-response regression in scalar_on_function/, Andrews curves in andrews.rs). For each, confirm the capability exists by reading the named module and confirm scikit-fda 0.10.1 has no equivalent by checking the Phase-7 area tables; set the equivalent column to none or partial accordingly. Tag Confidence HIGH (source-grep both sides) or MEDIUM (inferred). Ordering/grouping is executor discretion (by module or by theme). Prose and tables only — no fenced code, no fdars-core/src edits.</action>
  <verify>
    <automated>grep -q "### Reverse-Parity Strengths Sweep" .planning/research/AUDIT-REPORT.md && grep -Eiq "explain|SPM|seasonal|streaming" .planning/research/AUDIT-REPORT.md && grep -Eiq "conformal|tolerance|changepoint|andrews|wire|famm" .planning/research/AUDIT-REPORT.md && git diff --name-only -- fdars-core/src | grep -qv .</automated>
  </verify>
  <acceptance_criteria>
    - `### Reverse-Parity Strengths Sweep (D-04)` subsection exists with a reverse table (fdars capability → scikit-fda equivalent none/partial)
    - The four SC3 headliners (explainability, SPM, seasonal decomposition, streaming depth) are all present
    - The D-04 candidate list beyond the headliners is enumerated (conformal, tolerance, GMM, matrix profile, SSA, Hilbert/WIRE, FAMM, elastic changepoint, robust L1/Huber, multi-response, Andrews)
    - Each row is source-confirmed on the fdars side and checked against Phase-7 for the scikit-fda side, with a confidence tag
    - `git diff --name-only -- fdars-core/src` is empty
  </acceptance_criteria>
  <done>The reverse-parity strengths sweep documents every fdars-only capability (headliners + full D-04 candidate list), source-confirmed; no source edits.</done>
</task>

<task type="auto">
  <name>Task 3: Draft the unranked gap backlog + D-02a accuracy-validation item (SC4)</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - The six area parity tables (partial/absent in-scope rows are the backlog source) + the `### Gap Counts` subsection (Task 1)
    - .planning/phases/08-capability-parity-matrix-categorization/08-CONTEXT.md D-02a (draft a comparative numerical-accuracy validation backlog item for the fragile areas) and D-03 discretion (one entry per gap vs grouped clusters)
    - .planning/codebase/CONCERNS.md §"Known Bugs" + §"Fragile Areas" (B-spline #33, elastic alignment #34, seasonal/Lomb-Scargle NaN, GMM over-split — the fragile areas the D-02a item must cover)
    - .planning/research/FEATURES.md §"Biggest Likely Gaps" (root-cause leads)
  </read_first>
  <action>Append a `### Drafted Gap Backlog (unranked)` subsection. Derive one entry per in-scope gap (partial + absent rows), OR sensibly grouped clusters (e.g. "add remaining basis systems" as one entry) per D-03 discretion — each entry MUST carry area / current-gap / root-cause fields. Keep entries UNRANKED and add an explicit note that value-ranking, severity, and effort estimates are Phase 9 (RPT-02/RPT-03) — do not rank, do not add value/effort here. Then add the D-02a entry: a backlog item recommending a comparative numerical-accuracy validation pass (fdars vs scikit-fda on shared datasets) for the four fragile/known-bug areas (B-spline round-trip & CV #33, elastic-alignment level encoding #34, seasonal/Lomb-Scargle NaN handling, GMM over-split), with area/current-gap/root-cause fields — so the deferred verification is captured, not lost. Close with a one-line pointer that this backlog + separated counts + strengths sweep feed Phase 9. Prose and tables only — no fenced code, no fdars-core/src edits.</action>
  <verify>
    <automated>grep -q "### Drafted Gap Backlog (unranked)" .planning/research/AUDIT-REPORT.md && grep -Eiq "root.cause" .planning/research/AUDIT-REPORT.md && grep -Eiq "accuracy.validation|numerical.accuracy" .planning/research/AUDIT-REPORT.md && grep -Eiq "Phase 9|unranked|ranking is" .planning/research/AUDIT-REPORT.md && git diff --name-only -- fdars-core/src | grep -qv .</automated>
  </verify>
  <acceptance_criteria>
    - `### Drafted Gap Backlog (unranked)` subsection exists; every entry carries area / current-gap / root-cause fields
    - Entries are explicitly UNRANKED with a note that ranking/severity/effort is Phase 9 (RPT-02/RPT-03)
    - A D-02a entry recommends a comparative numerical-accuracy validation pass covering all four fragile/known-bug areas
    - A closing pointer states the backlog + counts + strengths sweep feed Phase 9
    - `git diff --name-only -- fdars-core/src` is empty
  </acceptance_criteria>
  <done>The unranked gap backlog is drafted with area/current-gap/root-cause per entry, includes the D-02a accuracy-validation item, and defers ranking to Phase 9; no source edits.</done>
</task>

</tasks>

<verification>
- `### Gap Counts (in-scope vs out-of-scope)`, `### Reverse-Parity Strengths Sweep (D-04)`, and `### Drafted Gap Backlog (unranked)` all exist under `## Phase 8`
- Out-of-scope count reported separately and excluded from the actionable total (Pitfall 14)
- Reverse sweep covers the four headliners plus the full D-04 candidate list, source-confirmed
- Backlog entries carry area/current-gap/root-cause and stay unranked; D-02a accuracy-validation item present
- `git diff --name-only -- fdars-core/src` returns nothing (audit-only enforced)
</verification>

<success_criteria>
Phase 8 is complete: the parity matrix (Plans 01+02) is now accompanied by separated gap
counts, a full reverse-parity strengths sweep, and an unranked draft backlog with root causes
plus the deferred accuracy-validation item — all in one `## Phase 8` section, ready for Phase 9
value-ranking. Zero fdars-core/src edits.
</success_criteria>

<output>
Create `.planning/phases/08-capability-parity-matrix-categorization/08-03-SUMMARY.md` when done
</output>
