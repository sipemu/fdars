---
phase: 05-parallelism-gap-assessment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/benches/audit_hotpaths.rs
  - .planning/research/AUDIT-REPORT.md
  - .planning/research/bench/p5_karcher_linalg,parallel_run1.txt
  - .planning/research/bench/p5_karcher_linalg,parallel_run2.txt
  - .planning/research/bench/p5_karcher_linalg,parallel_run3.txt
  - .planning/research/bench/p5_env_info.txt
autonomous: true
requirements: [PERF-05]
estimate:
  tokens: 60000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "AUDIT-REPORT.md contains a section headed `## Phase 5: Parallelism Gap Assessment` (SC1 anchor)"
    - "A D-04 methodology note in that section documents the exact stability controls applied: taskset core set, cpupower/performance governor, 3-run median+spread (backstop)"
    - "A thread-scaling table row for karcher_mean at RAYON_NUM_THREADS ∈ {1,2,4,8} exists, reporting median + run-spread per thread count (SC1)"
    - "Raw criterion output for the karcher thread sweep is saved as p5_karcher_linalg,parallel_run{1,2,3}.txt under .planning/research/bench/ and linked from the report (D-05)"
    - "A p5_env_info.txt captures rustc/cargo version + the applied governor and taskset core set for reproducibility"
  artifacts:
    - ".planning/research/AUDIT-REPORT.md (## Phase 5 section appended)"
    - "fdars-core/benches/audit_hotpaths.rs (p5 karcher sweep bench cell + criterion_group registration)"
    - ".planning/research/bench/p5_karcher_linalg,parallel_run1.txt"
    - ".planning/research/bench/p5_karcher_linalg,parallel_run2.txt"
    - ".planning/research/bench/p5_karcher_linalg,parallel_run3.txt"
    - ".planning/research/bench/p5_env_info.txt"
  key_links:
    - "RAYON_NUM_THREADS env → rayon global pool size → iter_maybe_parallel! in karcher.rs:185 → measured thread scaling"
    - "bench artifact median → thread-scaling table row → ## Phase 5 report section"
  prohibitions:
    - "No edits to fdars-core/src/** — this is the milestone's defining scope fence; a src edit is a scope violation"
---

<objective>
**As the** audit consumer deciding where to invest future parallelization work, **I want to** see a proven, reproducible measure→capture→report pipeline that produces one real rayon thread-scaling row under a pinned-stability protocol, **so that** I can trust the full SC1 thread-scaling table that expands from it.

This is the phase's tracer slice. It wires ONE existing bench target (`karcher_mean`) end-to-end through the entire Phase-5 apparatus: a `p5` bench cell runnable across the thread grid {1,2,4,8} via `RAYON_NUM_THREADS`, executed under the full D-04 pinned-stability protocol (taskset core-pin + cpupower performance governor + 3 runs reporting median and spread), with `p5_*` artifacts captured and a first real thread-scaling row appended to a new `## Phase 5: Parallelism Gap Assessment` section of AUDIT-REPORT.md — including the D-04 methodology note the whole SC1 table depends on.

Purpose: Prove the architecture (env-driven thread sweep → pinned measurement → captured artifact → report row) end-to-end on the best early-context tokens, before expanding to the second sentinel and the payback-N sweep in Plan 02.
Output: A registered p5 karcher bench cell, three run artifacts, an env-info artifact, and the seeded `## Phase 5` report section with its methodology note and the first thread-scaling row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-parallelism-gap-assessment/05-CONTEXT.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Add p5 karcher thread-sweep bench cell (env-driven RAYON_NUM_THREADS, D-04/D-05 discipline)</name>
  <files>fdars-core/benches/audit_hotpaths.rs</files>
  <read_first>
    - fdars-core/benches/audit_hotpaths.rs (read the existing `bench_matrix_sentinel` at ~line 112 and `bench_p3_karcher` at ~line 283 for the karcher call signature, black_box pattern, sample_size/measurement_time convention, and the `generate_curves` helper at ~line 43; read the `criterion_group!(...)` registration at ~line 894)
    - .planning/phases/05-parallelism-gap-assessment/05-CONTEXT.md (D-01 heavy sentinel = karcher_mean; D-05 retains black_box + `linalg,parallel` build; Claude's-Discretion: exact bench-fn/group name and N grid)
    - fdars-core/src/parallel.rs (confirm `iter_maybe_parallel!` is gated on the `parallel` feature and reads the rayon global pool — RAYON_NUM_THREADS controls pool size at runtime with NO code change)
  </read_first>
  <action>
    Add ONE new bench function `bench_p5_karcher_threads` (group name `audit_p5_karcher_threads`) to audit_hotpaths.rs that benchmarks `karcher_mean` at a FIXED heavy cell N=100, M=50 (reuse `generate_curves(100, 50)`, matching the Phase-1/3 karcher sentinel size so the numbers are cross-comparable). Do NOT set the thread count in code — rayon's global pool reads RAYON_NUM_THREADS from the environment at first-use, so the SAME compiled cell is re-run once per thread value by the run script in Task 2. Keep the karcher call identical to `bench_p3_karcher`: `karcher_mean(black_box(&data), black_box(&argvals), black_box(10usize), black_box(1e-3), black_box(0.0))` wrapped in an outer `black_box(...)` on the result (D-05 black_box on inputs AND output). Use `group.sample_size(10)` and `group.measurement_time(Duration::from_secs(30))` with `group.warm_up_time(Duration::from_secs(5))` — karcher is seconds-scale, so long measurement time + reduced samples per the Claude's-Discretion tuning allowance. Name the single `group.bench_function` cell `n100_m50` (thread count is an env dimension, not a cell-name dimension — the artifact filename `run<N>` disambiguates runs, and the report row records the thread value). Register `bench_p5_karcher_threads` in the existing `criterion_group!(benches, ...)` list after `bench_p4_elastic_fpca`. Add a `///` doc comment stating: heavy sweep sentinel per D-01; thread count varied via RAYON_NUM_THREADS env (no recompile); N=100/M=50 matches p1/p3 karcher for cross-phase comparability. Do NOT edit any file under fdars-core/src/.
  </action>
  <verify>
    <automated>cd fdars-core && cargo build --release --features linalg --benches 2>&1 | tail -5 && grep -q "bench_p5_karcher_threads" benches/audit_hotpaths.rs && grep -c "bench_p5_karcher_threads" benches/audit_hotpaths.rs | grep -qx 2</automated>
  </verify>
  <acceptance_criteria>
    - `fdars-core/benches/audit_hotpaths.rs` defines `fn bench_p5_karcher_threads(c: &mut Criterion)` with group `audit_p5_karcher_threads` and cell `n100_m50`
    - The bench cell calls `karcher_mean(...)` with `black_box` on all inputs and the output, band_frac `0.0`, N=100, M=50
    - `bench_p5_karcher_threads` appears in the `criterion_group!` registration (grep count == 2: definition + registration)
    - `cargo build --release --features linalg --benches` compiles with zero errors
    - No file under `fdars-core/src/` is modified (git diff --name-only shows only benches/audit_hotpaths.rs)
  </acceptance_criteria>
  <done>The p5 karcher thread-sweep bench cell compiles under the `linalg` build and is registered; no src file touched.</done>
</task>

<task type="auto">
  <name>Task 2: Run the karcher thread sweep under D-04 pinned protocol, capture artifacts, seed the ## Phase 5 report section</name>
  <files>.planning/research/AUDIT-REPORT.md, .planning/research/bench/p5_karcher_linalg,parallel_run1.txt, .planning/research/bench/p5_karcher_linalg,parallel_run2.txt, .planning/research/bench/p5_karcher_linalg,parallel_run3.txt, .planning/research/bench/p5_env_info.txt</files>
  <read_first>
    - .planning/research/AUDIT-REPORT.md (read the Phase 1 §Methodology "±5% Two-Run Variance Rule" ~line 124, the "Artifact Naming Convention" ~line 148, and the tail of the file to find the correct append point AFTER the Phase 2/3/4 sections — single-file append convention, D-05)
    - .planning/research/bench/p1_env_info.txt (the env-capture format to mirror for p5_env_info.txt — rustc/cargo version block)
    - .planning/phases/05-parallelism-gap-assessment/05-CONTEXT.md (D-04 stability protocol: taskset + cpupower/performance governor + 3 runs median+spread, controls documented in report; D-05 artifact naming `p5_<target>_<features>_run<N>.txt`)
    - .planning/research/PITFALLS.md (Pitfall 7 machine-noise/pinning governing D-04; Pitfall 17 reproducible bench artifacts)
  </read_first>
  <action>
    Run the karcher thread sweep across RAYON_NUM_THREADS ∈ {1, 2, 4, 8} under the D-04 pinned-stability protocol, 3 independent runs of the full 4-thread grid.

    Setup (document exact values in p5_env_info.txt): pin worker cores with `taskset -c 0-7` (or the machine's available physical core range — record the exact core set used) and set the CPU governor to performance via `cpupower frequency-set -g performance` (if `cpupower` is unavailable or requires privileges that fail, fall back to `sudo cpupower ...`; if that also fails, RECORD the failure in p5_env_info.txt and note the governor as "not pinned — LOW CONFIDENCE" rather than silently skipping — do NOT block). Capture `rustc --version` and `cargo --version` into `.planning/research/bench/p5_env_info.txt` in the same block format as p1_env_info.txt, plus a `=== D-04 CONTROLS ===` block recording the taskset core set and the governor state actually applied.

    For each run N in {1,2,3}, execute the 4 thread values sequentially, appending all four thread results into ONE artifact file `.planning/research/bench/p5_karcher_linalg,parallel_run<N>.txt`. The exact per-thread command (from repo root):
    `for T in 1 2 4 8; do echo "=== RAYON_NUM_THREADS=$T ===" >> .planning/research/bench/p5_karcher_linalg,parallel_run<N>.txt; RAYON_NUM_THREADS=$T taskset -c 0-7 cargo bench --release -p fdars-core --features linalg --bench audit_hotpaths -- audit_p5_karcher_threads >> .planning/research/bench/p5_karcher_linalg,parallel_run<N>.txt 2>&1; done`
    (Confirm the binary path contains `/release/` in the criterion output — Phase-1 release-mode discipline. If `TMPDIR` /tmp exhaustion errors appear, per MEMORY set `TMPDIR=/home/simonm/.cache/fdars-bench-tmp`.) Repeat for run1, run2, run3 → three artifact files.

    Then APPEND a new section to `.planning/research/AUDIT-REPORT.md` (do NOT overwrite — single-file append after the Phase 4 section) with the exact heading `## Phase 5: Parallelism Gap Assessment`. Under it write:
    (a) a `### Methodology (D-04 pinned-stability protocol)` subsection documenting the EXACT controls applied — taskset core set, governor, 3-run median+spread — and stating this escalates the Phase-1 ±5%/2-run baseline (cite Phase-3's 34–58% karcher variance as the motivation); and
    (b) a `### Thread-Scaling Table` with the karcher_mean row: columns `Target | N | M | RAYON_NUM_THREADS | median (of 3 runs) | run spread | speedup vs 1-thread | Artifact`. Fill the 4 thread rows (1,2,4,8) from the median of the 3 captured runs; compute speedup vs the 1-thread median; link each to the run artifacts. Note if the 1→8 curve is still climbing steeply at 8 (deferred-idea flag for >8 threads). Mark any thread cell whose 3-run spread exceeds 10% as LOW CONFIDENCE (Phase-1 variance rule).
    Do NOT edit any file under fdars-core/src/.
  </action>
  <verify>
    <automated>test -f ".planning/research/bench/p5_karcher_linalg,parallel_run1.txt" && test -f ".planning/research/bench/p5_karcher_linalg,parallel_run2.txt" && test -f ".planning/research/bench/p5_karcher_linalg,parallel_run3.txt" && test -f .planning/research/bench/p5_env_info.txt && grep -q "^## Phase 5: Parallelism Gap Assessment" .planning/research/AUDIT-REPORT.md && grep -qi "RAYON_NUM_THREADS" .planning/research/AUDIT-REPORT.md && for T in 1 2 4 8; do grep -q "RAYON_NUM_THREADS=$T" ".planning/research/bench/p5_karcher_linalg,parallel_run1.txt" || exit 1; done && git diff --name-only | grep -qv "^fdars-core/src/"</automated>
  </verify>
  <acceptance_criteria>
    - Three artifacts `.planning/research/bench/p5_karcher_linalg,parallel_run{1,2,3}.txt` exist, each containing all four `=== RAYON_NUM_THREADS=<T> ===` blocks for T ∈ {1,2,4,8}
    - `.planning/research/bench/p5_env_info.txt` exists with rustc/cargo versions and a `=== D-04 CONTROLS ===` block recording the taskset core set and governor state actually applied
    - AUDIT-REPORT.md contains the exact heading `## Phase 5: Parallelism Gap Assessment`
    - That section contains a D-04 methodology subsection naming taskset, the governor, and the 3-run median+spread protocol
    - That section contains a Thread-Scaling Table with a karcher_mean row for each of RAYON_NUM_THREADS ∈ {1,2,4,8}, reporting median, run spread, and speedup vs 1-thread, each linked to a run artifact
    - `git diff --name-only` shows NO path under `fdars-core/src/`
  </acceptance_criteria>
  <precondition>`taskset` and `cpupower` are available (or their failure is recorded in p5_env_info.txt with a LOW-CONFIDENCE governor note); TMPDIR has space for release bench builds (MEMORY: set TMPDIR=/home/simonm/.cache/fdars-bench-tmp if /tmp exhausts).</precondition>
  <done>The measure→capture→report pipeline is proven end-to-end: karcher thread-sweep artifacts captured under the pinned protocol, and the `## Phase 5` section seeded with its methodology note and the first real thread-scaling row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none of security relevance) | This phase runs local Criterion benchmarks over seeded, in-process `FdMatrix` data and writes markdown/txt artifacts under `.planning/`. No network I/O, no untrusted input, no auth, no DB, no user-facing surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | bench harness edit scope | low | mitigate | Verify gates assert `git diff --name-only` excludes `fdars-core/src/**` — the milestone scope fence is machine-checked, not just asserted. |

No package installs occur in this plan (no npm/pip/cargo add). No network egress. The honest threat surface is minimal; no high/critical threats exist to block on.
</threat_model>

<verification>
- `cargo build --release --features linalg --benches` compiles cleanly with the new p5 cell.
- Three p5 karcher run artifacts + p5_env_info.txt exist under `.planning/research/bench/`.
- `## Phase 5: Parallelism Gap Assessment` section exists in AUDIT-REPORT.md with a D-04 methodology note and a 4-thread karcher scaling row.
- No `fdars-core/src/` file modified.
</verification>

<success_criteria>
The Phase-5 measure→capture→report pipeline is proven end-to-end on the karcher heavy sentinel: a registered p5 bench cell, three pinned-protocol run artifacts, an env-info artifact, and a seeded `## Phase 5` report section carrying the D-04 methodology note and the first real thread-scaling row — all with zero edits to `fdars-core/src/`.
</success_criteria>

<output>
Create `.planning/phases/05-parallelism-gap-assessment/05-01-SUMMARY.md` when done.

## Artifacts this plan produces
- New `## Phase 5: Parallelism Gap Assessment` section in `.planning/research/AUDIT-REPORT.md` (methodology note + first thread-scaling row) — expanded by Plans 02/03.
- `bench_p5_karcher_threads` bench function + `audit_p5_karcher_threads` group added to `fdars-core/benches/audit_hotpaths.rs` and registered in `criterion_group!`.
- `.planning/research/bench/p5_karcher_linalg,parallel_run{1,2,3}.txt` (thread sweep raw output).
- `.planning/research/bench/p5_env_info.txt` (toolchain + D-04 controls).
</output>
