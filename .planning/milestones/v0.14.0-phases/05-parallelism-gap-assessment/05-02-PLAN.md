---
phase: 05-parallelism-gap-assessment
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - fdars-core/benches/audit_hotpaths.rs
  - .planning/research/AUDIT-REPORT.md
  - .planning/research/bench/p5_streaming_linalg,parallel_run1.txt
  - .planning/research/bench/p5_streaming_linalg,parallel_run2.txt
  - .planning/research/bench/p5_streaming_linalg,parallel_run3.txt
  - .planning/research/bench/p5_karcher_paybackN_linalg,parallel_run1.txt
  - .planning/research/bench/p5_streaming_paybackN_linalg,parallel_run1.txt
autonomous: true
requirements: [PERF-05]
estimate:
  tokens: 70000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The Thread-Scaling Table in the ## Phase 5 section carries a second sentinel row set for StreamingFraimanMuniz::depth_batch across RAYON_NUM_THREADS ∈ {1,2,4,8} (SC1, D-01 light sentinel)"
    - "The report states a payback-threshold N for EACH sweep target: the smallest N at which the machine-default-thread parallel path first beats the RAYON_NUM_THREADS=1 single-thread run of the same build (SC1, D-02)"
    - "The payback-N method note explicitly states the baseline is RAYON_NUM_THREADS=1 (single-thread rayon), NOT --no-default-features (D-02)"
    - "Streaming and payback-N raw criterion artifacts are saved as p5_*_linalg,parallel_run*.txt under .planning/research/bench/ and linked (D-05)"
    - "The report notes pairwise/nadaraya_watson are NOT sweep targets, covered by the Phase-2 ALREADY-PARALLEL inventory (D-03) — backstop"
  artifacts:
    - "fdars-core/benches/audit_hotpaths.rs (p5 streaming thread-sweep cell + payback-N parameterized cells)"
    - ".planning/research/bench/p5_streaming_linalg,parallel_run{1,2,3}.txt"
    - ".planning/research/bench/p5_karcher_paybackN_linalg,parallel_run1.txt"
    - ".planning/research/bench/p5_streaming_paybackN_linalg,parallel_run1.txt"
    - ".planning/research/AUDIT-REPORT.md (Thread-Scaling Table completed + payback-N subsection)"
  key_links:
    - "RAYON_NUM_THREADS=1 single-thread baseline vs machine-default parallel across a downward N grid → payback-threshold N"
    - "streaming depth_batch iter_maybe_parallel!(0..nobj) at fraiman_muniz.rs:82 → light-sentinel overhead-dominated regime"
  prohibitions:
    - "No edits to fdars-core/src/** — a src edit is a scope violation"
---

<objective>
**As the** audit consumer, **I want** the SC1 thread-scaling assessment completed with a second (light) sentinel and a concrete payback-threshold N per target, **so that** I can see both the clean speedup regime and the point where rayon overhead stops paying off — the exact information needed to decide whether a given loop is worth parallelizing.

Expansion slice on the proven Plan-01 pipeline. Adds the light sentinel `StreamingFraimanMuniz::depth_batch` (D-01) to the thread grid, then runs the payback-N downward sweep (D-02): hold threads at the machine default and sweep N downward, comparing against a `RAYON_NUM_THREADS=1` single-thread run of the SAME build, reporting the smallest N at which the parallel path first wins — for BOTH karcher (heavy) and streaming (light) targets, which bracket the crossover.

Purpose: Complete SC1 — thread-scaling table + payback-threshold N.
Output: Streaming thread-sweep artifacts, payback-N artifacts, and the completed Thread-Scaling Table + payback-N subsection in the `## Phase 5` report section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-parallelism-gap-assessment/05-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add p5 streaming thread-sweep cell + payback-N parameterized cells (D-01 light sentinel, D-02 downward N grid)</name>
  <files>fdars-core/benches/audit_hotpaths.rs</files>
  <read_first>
    - fdars-core/benches/audit_hotpaths.rs (read `bench_streaming_sentinel` at ~line 221 for the `SortedReferenceState::from_reference` + `StreamingFraimanMuniz::new` + `depth_batch` construct-then-query pattern and imports; read `bench_p5_karcher_threads` added by Plan 01; read `generate_curves` at ~line 43 and the `criterion_group!` list at ~line 894)
    - .planning/phases/05-parallelism-gap-assessment/05-CONTEXT.md (D-01 light sentinel = StreamingFraimanMuniz::depth_batch; D-02 payback baseline = RAYON_NUM_THREADS=1; Claude's-Discretion: exact N grids per target, group/fn names)
    - .planning/codebase/CONCERNS.md (§Performance Bottlenecks "rayon overhead for n < ~100" note — informs the downward N grid range so it brackets the crossover)
  </read_first>
  <action>
    Add TWO groups of bench cells to audit_hotpaths.rs (thread count stays an RAYON_NUM_THREADS env dimension throughout — no thread-setting in code):

    1) `bench_p5_streaming_threads` (group `audit_p5_streaming_threads`): mirror `bench_streaming_sentinel`'s construct-then-query body (`SortedReferenceState::from_reference` → `StreamingFraimanMuniz::new(state, true)` → `fm.depth_batch(...)`, all `black_box`) at a FIXED light cell — reuse the sentinel's N=500, M=200 so it stays cross-comparable — single cell `n500_m200`. `group.sample_size(30)`, `group.measurement_time(Duration::from_secs(15))`, `group.warm_up_time(Duration::from_secs(3))` (light target keeps Phase-1 streaming defaults per Claude's-Discretion tuning).

    2) Payback-N cells — one bench function per target with MULTIPLE cells over a downward N grid so the run script can find the crossover:
       - `bench_p5_karcher_paybackN` (group `audit_p5_karcher_paybackN`): karcher_mean at M=50 fixed, N ∈ {10, 25, 50, 100} (bracket the karcher crossover — heavy target crosses at small N; grid chosen to straddle CONCERNS n≈100 overhead note). One `bench_function` per N named `n{N}_m50`. sample_size(10), measurement_time(15s).
       - `bench_p5_streaming_paybackN` (group `audit_p5_streaming_paybackN`): streaming construct+query at M=200 fixed, N_obj ∈ {1, 10, 50, 200, 500} (light target's overhead-dominated regime crosses much later; grid straddles it). One `bench_function` per N_obj named `n{N}_m200`. sample_size(30), measurement_time(10s).
       Reuse `generate_curves(N, M)` for all cells. black_box on inputs and outputs (D-05).

    Register `bench_p5_streaming_threads`, `bench_p5_karcher_paybackN`, `bench_p5_streaming_paybackN` in the `criterion_group!(benches, ...)` list. Add `///` docs citing D-01 (light sentinel) and D-02 (payback baseline = RAYON_NUM_THREADS=1, downward N grid). Do NOT edit any file under fdars-core/src/.
  </action>
  <verify>
    <automated>cd fdars-core && cargo build --release --features linalg --benches 2>&1 | tail -5 && for f in bench_p5_streaming_threads bench_p5_karcher_paybackN bench_p5_streaming_paybackN; do grep -c "$f" benches/audit_hotpaths.rs | grep -qx 2 || exit 1; done && git diff --name-only | grep -qv "^fdars-core/src/"</automated>
  </verify>
  <acceptance_criteria>
    - `audit_hotpaths.rs` defines `bench_p5_streaming_threads` (cell `n500_m200`), `bench_p5_karcher_paybackN` (cells `n10_m50`,`n25_m50`,`n50_m50`,`n100_m50`), and `bench_p5_streaming_paybackN` (cells `n1_m200`,`n10_m200`,`n50_m200`,`n200_m200`,`n500_m200`)
    - Each of the three new functions appears exactly twice (definition + criterion_group registration)
    - All bench bodies wrap inputs and outputs in `black_box`
    - `cargo build --release --features linalg --benches` compiles with zero errors
    - `git diff --name-only` shows only `fdars-core/benches/audit_hotpaths.rs` under fdars-core/
  </acceptance_criteria>
  <done>Light-sentinel thread-sweep cell and both payback-N downward-grid cells compile and are registered; no src file touched.</done>
</task>

<task type="auto">
  <name>Task 2: Run streaming thread sweep (3 pinned runs) + payback-N downward sweeps for both targets, capture artifacts</name>
  <files>.planning/research/bench/p5_streaming_linalg,parallel_run1.txt, .planning/research/bench/p5_streaming_linalg,parallel_run2.txt, .planning/research/bench/p5_streaming_linalg,parallel_run3.txt, .planning/research/bench/p5_karcher_paybackN_linalg,parallel_run1.txt, .planning/research/bench/p5_streaming_paybackN_linalg,parallel_run1.txt</files>
  <read_first>
    - .planning/research/bench/p5_env_info.txt (the D-04 controls established in Plan 01 — reuse the SAME taskset core set and governor for comparability)
    - .planning/phases/05-parallelism-gap-assessment/05-CONTEXT.md (D-04 pinned protocol reused; D-02 payback baseline = RAYON_NUM_THREADS=1 vs machine default; D-05 artifact naming)
    - .planning/research/PITFALLS.md (Pitfall 7 pinning; Pitfall: rayon overhead on small N is the payback-threshold signal)
  </read_first>
  <action>
    Reuse the EXACT D-04 controls from Plan 01 (same taskset core set, same performance governor — read p5_env_info.txt).

    (a) Streaming thread sweep — 3 runs of the 4-thread grid, same shape as Plan-01 karcher:
    For run N in {1,2,3}: `for T in 1 2 4 8; do echo "=== RAYON_NUM_THREADS=$T ===" >> .planning/research/bench/p5_streaming_linalg,parallel_run<N>.txt; RAYON_NUM_THREADS=$T taskset -c 0-7 cargo bench --release -p fdars-core --features linalg --bench audit_hotpaths -- audit_p5_streaming_threads >> .planning/research/bench/p5_streaming_linalg,parallel_run<N>.txt 2>&1; done`

    (b) Payback-N downward sweep (D-02) — for EACH target, run the whole N-grid group twice: once at RAYON_NUM_THREADS=1 (single-thread baseline) and once at the machine default (unset RAYON_NUM_THREADS, letting rayon use all cores), into a single artifact with labeled blocks:
    - karcher: `echo "=== RAYON_NUM_THREADS=1 ===" >> .../p5_karcher_paybackN_linalg,parallel_run1.txt; RAYON_NUM_THREADS=1 taskset -c 0-7 cargo bench --release -p fdars-core --features linalg --bench audit_hotpaths -- audit_p5_karcher_paybackN >> .../p5_karcher_paybackN_linalg,parallel_run1.txt 2>&1; echo "=== RAYON default (all cores) ===" >> ...; taskset -c 0-7 cargo bench --release -p fdars-core --features linalg --bench audit_hotpaths -- audit_p5_karcher_paybackN >> ... 2>&1`
    - streaming: same two-block pattern against `audit_p5_streaming_paybackN` → `p5_streaming_paybackN_linalg,parallel_run1.txt`.
    (If /tmp exhausts, set TMPDIR=/home/simonm/.cache/fdars-bench-tmp per MEMORY. Confirm `/release/` in criterion binary paths.)

    Do NOT edit AUDIT-REPORT.md in this task (report writing is Task 3). Do NOT edit any file under fdars-core/src/.
  </action>
  <verify>
    <automated>for N in 1 2 3; do test -f ".planning/research/bench/p5_streaming_linalg,parallel_run$N.txt" || exit 1; done && test -f ".planning/research/bench/p5_karcher_paybackN_linalg,parallel_run1.txt" && test -f ".planning/research/bench/p5_streaming_paybackN_linalg,parallel_run1.txt" && for T in 1 2 4 8; do grep -q "RAYON_NUM_THREADS=$T" ".planning/research/bench/p5_streaming_linalg,parallel_run1.txt" || exit 1; done && grep -q "RAYON_NUM_THREADS=1" ".planning/research/bench/p5_karcher_paybackN_linalg,parallel_run1.txt" && grep -q "RAYON default" ".planning/research/bench/p5_karcher_paybackN_linalg,parallel_run1.txt"</automated>
  </verify>
  <acceptance_criteria>
    - Three `p5_streaming_linalg,parallel_run{1,2,3}.txt` artifacts exist, each with all four `=== RAYON_NUM_THREADS=<T> ===` blocks (T ∈ {1,2,4,8})
    - `p5_karcher_paybackN_linalg,parallel_run1.txt` contains a `RAYON_NUM_THREADS=1` block and a `RAYON default` block, each covering the karcher N-grid {10,25,50,100}
    - `p5_streaming_paybackN_linalg,parallel_run1.txt` contains the same two blocks covering the streaming N-grid {1,10,50,200,500}
    - All runs used the same taskset/governor recorded in p5_env_info.txt
    - No `fdars-core/src/` file modified
  </acceptance_criteria>
  <precondition>Plan 01 completed (p5_env_info.txt exists with the D-04 controls to reuse; the p5 karcher thread cell is registered and the ## Phase 5 report section exists).</precondition>
  <done>Streaming thread-sweep and both payback-N downward sweeps are captured as labeled p5 artifacts under the same pinned protocol as Plan 01.</done>
</task>

<task type="auto">
  <name>Task 3: Complete the Thread-Scaling Table + write the payback-threshold-N subsection (SC1)</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - .planning/research/AUDIT-REPORT.md (the `## Phase 5: Parallelism Gap Assessment` section seeded by Plan 01 — its Thread-Scaling Table and Methodology note; append/extend, do not overwrite)
    - .planning/research/bench/p5_streaming_linalg,parallel_run{1,2,3}.txt, p5_karcher_paybackN_linalg,parallel_run1.txt, p5_streaming_paybackN_linalg,parallel_run1.txt (the numbers to tabulate)
    - .planning/phases/05-parallelism-gap-assessment/05-CONTEXT.md (D-02 payback method wording; D-03 pairwise/nadaraya_watson excluded note)
  </read_first>
  <action>
    Extend the existing `## Phase 5: Parallelism Gap Assessment` section (do NOT create a second one; grep for the heading first):

    1) Add the StreamingFraimanMuniz::depth_batch rows to the existing `### Thread-Scaling Table` — same columns as the karcher rows (Target | N | M | RAYON_NUM_THREADS | median-of-3 | run spread | speedup vs 1-thread | Artifact), one row per T ∈ {1,2,4,8}, medians from the 3 streaming runs, linked to p5_streaming artifacts. Flag cells with >10% 3-run spread as LOW CONFIDENCE (streaming is sub-ms and noisy — expect this; state it).

    2) Add a `### Payback-Threshold N (D-02)` subsection. For EACH target, present the downward N-grid as a small table (N | single-thread [RAYON_NUM_THREADS=1] median | machine-default-parallel median | parallel wins?) and state the payback-threshold N = the smallest N at which the machine-default parallel path first beats the single-thread path. State explicitly that the baseline is RAYON_NUM_THREADS=1 (single-thread rayon, same build/codegen), NOT --no-default-features (D-02), and that the --no-default-features rayon-off cost is reported separately under SC3 (Plan 03). Interpret the contrast: heavy karcher pays back at small N; light streaming pays back only at larger N_obj (or never within the grid — say so if so).

    3) Add one sentence noting `pairwise`/`nadaraya_watson` are NOT sweep targets — covered by the Phase-2 §Parallelism Gap List ALREADY-PARALLEL inventory and represented by the two chosen sentinels (D-03).

    Do NOT edit any file under fdars-core/src/.
  </action>
  <verify>
    <automated>grep -c "^## Phase 5: Parallelism Gap Assessment" .planning/research/AUDIT-REPORT.md | grep -qx 1 && grep -q "Payback-Threshold N" .planning/research/AUDIT-REPORT.md && grep -qi "StreamingFraimanMuniz" .planning/research/AUDIT-REPORT.md && grep -q "RAYON_NUM_THREADS=1" .planning/research/AUDIT-REPORT.md && grep -Eiq "not.*no-default-features|not --no-default-features|rather than .*no-default-features" .planning/research/AUDIT-REPORT.md && git diff --name-only | grep -qv "^fdars-core/src/"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly ONE `## Phase 5: Parallelism Gap Assessment` heading exists (section extended, not duplicated)
    - The Thread-Scaling Table now includes StreamingFraimanMuniz::depth_batch rows for all four RAYON_NUM_THREADS ∈ {1,2,4,8}, linked to p5_streaming artifacts
    - A `### Payback-Threshold N (D-02)` subsection states a payback-threshold N for karcher AND for streaming, each with its downward N-grid table
    - The subsection states the baseline is RAYON_NUM_THREADS=1 (not --no-default-features) and defers the rayon-off cost to SC3/Plan 03
    - A sentence records pairwise/nadaraya_watson excluded per D-03 (Phase-2 ALREADY-PARALLEL inventory)
    - No `fdars-core/src/` file modified
  </acceptance_criteria>
  <done>SC1 is complete: the thread-scaling table carries both sentinels across the full thread grid and a payback-threshold N is stated for each target with its D-02 method note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none of security relevance) | Local Criterion benchmarks over seeded in-process `FdMatrix` data; writes markdown/txt under `.planning/`. No network I/O, no untrusted input, no auth, no DB, no user-facing surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02 | Tampering | bench harness edit scope | low | mitigate | Verify gates assert `git diff --name-only` excludes `fdars-core/src/**`. |

No package installs, no network egress. No high/critical threats to block on.
</threat_model>

<verification>
- `cargo build --release --features linalg --benches` compiles with the new streaming + payback-N cells.
- Streaming thread-sweep (3 runs) and both payback-N artifacts exist under `.planning/research/bench/`.
- Single `## Phase 5` section carries both sentinels in the thread-scaling table + a payback-threshold-N subsection with the D-02 baseline note.
- No `fdars-core/src/` file modified.
</verification>

<success_criteria>
SC1 is complete: a rayon thread-scaling table covers both the heavy (karcher_mean) and light (StreamingFraimanMuniz::depth_batch) sentinels across RAYON_NUM_THREADS ∈ {1,2,4,8}, and a payback-threshold N is reported for each target against a RAYON_NUM_THREADS=1 single-thread baseline — with zero edits to `fdars-core/src/`.
</success_criteria>

<output>
Create `.planning/phases/05-parallelism-gap-assessment/05-02-SUMMARY.md` when done.

## Artifacts this plan produces
- `bench_p5_streaming_threads`, `bench_p5_karcher_paybackN`, `bench_p5_streaming_paybackN` added to `fdars-core/benches/audit_hotpaths.rs` and registered.
- `.planning/research/bench/p5_streaming_linalg,parallel_run{1,2,3}.txt` (light-sentinel thread sweep).
- `.planning/research/bench/p5_karcher_paybackN_linalg,parallel_run1.txt`, `.planning/research/bench/p5_streaming_paybackN_linalg,parallel_run1.txt` (payback-N downward sweeps).
- Completed Thread-Scaling Table (both sentinels) + `### Payback-Threshold N (D-02)` subsection in the `## Phase 5` section of AUDIT-REPORT.md.
</output>
