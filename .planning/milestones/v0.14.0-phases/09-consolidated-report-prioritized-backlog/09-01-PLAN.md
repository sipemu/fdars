---
phase: 09-consolidated-report-prioritized-backlog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/AUDIT-REPORT.md
  - .planning/research/BACKLOG.md
autonomous: true
requirements: [RPT-01, RPT-02, RPT-03]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A consolidated audit report combines the performance findings and the gap findings, with reproducible evidence attached to each finding (benchmark numbers, allocation counts, or doc/source references under .planning/research/bench/)"
    - "Every backlog item passes a completeness checklist: location/area, current cost or gap, root cause, proposed direction, severity (P1/P2/P3), effort estimate (S/M/L), and an evidence link"
    - "The report's methodology section documents build-mode/feature-flag discipline and infra-vs-code failure triage"
  artifacts:
    - .planning/research/BACKLOG.md
    - "AUDIT-REPORT.md ## Consolidated Findings section"
    - "AUDIT-REPORT.md ## Methodology (consolidated) — build-mode/feature-flag discipline + infra-vs-code triage pointer"
    - "BACKLOG.md ## Ranking Methodology section (value/sqrt(effort), value scale, S/M/L effort map, P1/P2/P3 severity scale)"
    - "BACKLOG.md ## Ranked Backlog table (with computed score column)"
  key_links:
    - "Every backlog item's evidence-link field resolves to a real file under .planning/research/bench/ or a phase SUMMARY / AUDIT-REPORT section"
    - "The ranked table score column equals value / sqrt(effort) using the documented value scale and S/M/L→effort map"
---

<objective>
Prove the full report+backlog consolidation pipeline end-to-end on ONE performance finding and ONE backlog item, and lay down the two deliverable scaffolds every later slice fills.

This is the FINAL phase of an audit-only milestone. **Deliverables are Markdown documents only — no `fdars-core` source, benchmark, or test code is edited.** Every task edits Markdown under `.planning/research/`.

MVP user story: **As a** fdars maintainer planning the next milestone, **I want to** read one consolidated audit report and a value-ranked backlog, **so that** I can promote the highest-leverage performance and gap work via `/gsd-new-milestone` without re-reading eight phases of raw findings.

Purpose: The audit ran across Phases 1–8 and left findings + draft backlog slices scattered through `AUDIT-REPORT.md`. This tracer proves the aggregate → evidence-link → prioritize → 7-field-checklist pipeline on one item before Plans 02/03 scale it to all findings. It de-risks the ranking formula, the checklist schema, and the two output-file structures on the maintainer's best early-context tokens.

Output: `BACKLOG.md` scaffold (ranking methodology + ranked-table header + one fully-worked ranked item); a consolidated `## Methodology` + `## Consolidated Findings` scaffold in `AUDIT-REPORT.md` carrying one fully-worked performance finding with a real benchmark-number evidence link.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# The single running report this phase consolidates (headings already exist for Phases 1-8):
@.planning/research/AUDIT-REPORT.md
</context>

<artifacts_this_phase_produces>
This section is the contract for what Plans 01–03 create. Plan 01 creates the scaffolds and the first worked item; Plans 02/03 fill them.

**New files:**
- `.planning/research/BACKLOG.md` — the standalone prioritized backlog deliverable (RPT-02/RPT-03).

**New sections created in `.planning/research/AUDIT-REPORT.md` (Plan 01):**
- `## Consolidated Findings` — top-level section aggregating performance + gap findings with per-finding evidence links (RPT-01). Plan 01 seeds it with the report status flip + one worked performance finding; Plan 02 adds the rest of the performance findings; Plan 03 adds the gap-findings summary + strengths summary.
- `## Methodology (Consolidated)` — a single methodology section (or a clearly-headed pointer to the existing `## §Methodology`) documenting build-mode/feature-flag discipline and the infra-vs-code failure-triage rule (RPT-02 SC4).

**New sections created in `.planning/research/BACKLOG.md` (Plan 01):**
- `## Ranking Methodology` — the `value / sqrt(effort)` formula, the value scale (1–5), the S/M/L→effort numeric map, and the P1/P2/P3 severity scale.
- `## Ranked Backlog` — the master table, ordered by descending score, with a computed-score column. Plan 01 seeds header + one row; Plans 02/03 append rows and Plan 03 does the final sort.
- `## Backlog Items` — the per-item detail blocks (each carrying all 7 checklist fields). Plan 01 seeds one; Plans 02/03 add the rest.

**Planner assumptions surfaced (see PROJECT.md decisions — these are Claude's-discretion choices made explicit, not silent):**
- Deliverable paths: `AUDIT-REPORT.md` is finalized in place (status flipped from "In progress" to "Complete"); the backlog lives in a dedicated `.planning/research/BACKLOG.md` (NOT an in-report section) so it can be handed to `/gsd-new-milestone` standalone.
- Ranking heuristic: `score = value / sqrt(effort)` where **value ∈ {1,2,3,4,5}** (5 = table-stakes capability blocking real workloads / P1 perf default-path cost; 1 = niche differentiator) and **effort maps S=1, M=3, L=9** (so sqrt(effort) = 1, 1.732, 3). This mapping is documented in `BACKLOG.md ## Ranking Methodology` and reused verbatim by Plans 02/03.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Create BACKLOG.md scaffold with ranking methodology + one fully-worked ranked item (P6-1 faer SVD swap)</name>
  <files>.planning/research/BACKLOG.md</files>
  <read_first>
    - .planning/research/AUDIT-REPORT.md lines 848–860 (the "SC4 — Phase 9 Backlog Item" / P6-1 draft entry, which already carries Function / Current cost / Root cause / Candidate fix / Evidence / Severity+Effort=P2/S)
    - .planning/research/AUDIT-REPORT.md lines 803–844 (Phase 6 SC2 comparison table + SC3 adoption note — the real speedup numbers and the borderline-1.8×-at-primary-cell rationale that sets P6-1 severity)
    - .planning/research/bench/p6_svd_nalgebra_linalg_run1.txt and .planning/research/bench/p6_svd_faer_seq_linalg_run1.txt (confirm the real N=500,M=200 numbers: nalgebra 41.026 ms vs faer 23.084 ms → 1.8× — cite the actual file the item links)
  </read_first>
  <action>
    Create `.planning/research/BACKLOG.md`. Write a top header block (title, milestone v0.14.0, source pointer to AUDIT-REPORT.md, "audit-only deliverable" note).

    Then write `## Ranking Methodology` documenting: the formula `score = value / sqrt(effort)`; the value scale 1–5 with the anchors from artifacts_this_phase_produces (5 = table-stakes / P1 default-path cost, 1 = niche differentiator); the effort map S=1 / M=3 / L=9 and therefore sqrt(effort)=1 / 1.732 / 3; and the severity scale P1 (default-path cost or table-stakes gap blocking real workloads) / P2 (meaningful but not blocking) / P3 (niche / cosmetic-adjacent). State that value is user value NOT ease (RPT-02).

    Then write `## Ranked Backlog` as a Markdown table with columns: Rank | ID | Title | Severity | Value(1-5) | Effort(S/M/L) | Score(value/sqrt(effort)) | Area/Location. Seed EXACTLY ONE row: the P6-1 faer-SVD-swap item. Assign value and effort by the documented scale (P6-1 is P2 / S-effort per the Phase-6 draft; pick value from the scale and compute the score — show the computed number in the Score column, do not leave it symbolic).

    Then write `## Backlog Items` and under it one fully-worked item block for P6-1 carrying ALL SEVEN checklist fields as a labelled list: **Location/area**, **Current cost or gap**, **Root cause**, **Proposed direction**, **Severity (P1/P2/P3)**, **Effort estimate (S/M/L)**, **Evidence link**. The evidence-link field MUST be a Markdown link to `.planning/research/bench/p6_svd_nalgebra_linalg_run1.txt` (and may add p6_svd_faer_seq_linalg_run1.txt) and cite the real 41.026 ms vs 23.084 ms → 1.8× numbers. Phrase the item as a candidate requirement/phase (GSD-ready wording, e.g. "Swap nalgebra SVD for faer thin_svd in fdata_to_pc_1d").

    Do NOT edit any file under fdars-core/. This is a docs deliverable.
  </action>
  <verify>
    <automated>test -f .planning/research/BACKLOG.md && grep -q 'value / sqrt(effort)' .planning/research/BACKLOG.md && grep -q '^## Ranked Backlog' .planning/research/BACKLOG.md && grep -q '^## Backlog Items' .planning/research/BACKLOG.md && grep -Eq 'Location/area|Location / area' .planning/research/BACKLOG.md && grep -q 'Evidence link' .planning/research/BACKLOG.md && grep -q 'bench/p6_svd_nalgebra_linalg_run1.txt' .planning/research/BACKLOG.md && test -f .planning/research/bench/p6_svd_nalgebra_linalg_run1.txt</automated>
  </verify>
  <acceptance_criteria>
    - BACKLOG.md contains a `## Ranking Methodology` section defining `score = value / sqrt(effort)`, a 1–5 value scale, and the S=1/M=3/L=9 effort map
    - BACKLOG.md `## Ranked Backlog` table has columns including Severity, Value, Effort, and a Score column showing a COMPUTED numeric value (not a symbolic `v/√e`)
    - The single P6-1 item block under `## Backlog Items` contains all 7 checklist fields (location/area, current cost or gap, root cause, proposed direction, severity, effort, evidence link)
    - The P6-1 evidence-link field is a Markdown link resolving to an existing file under `.planning/research/bench/`
    - The P6-1 current-cost field cites a real benchmark number (41.026 ms / 23.084 ms / 1.8×) drawn from the linked artifact, not an invented figure
    - No file under `fdars-core/` is modified
  </acceptance_criteria>
  <done>BACKLOG.md exists with ranking methodology, a ranked-table header seeded with one computed-score row, and one 7-field item block whose evidence link resolves to a real bench artifact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Consolidate the report header + Methodology and open the Consolidated Findings section with one fully-worked performance finding</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - .planning/research/AUDIT-REPORT.md lines 1–8 (current header: "Status: In progress (Phase 1 of 9)" — this must be flipped)
    - .planning/research/AUDIT-REPORT.md lines 57–181 (the existing `## §Methodology` — Release-Mode Discipline, Feature-Flag Matrix, black_box, Toolchain capture, ±5% variance, and the "Infrastructure vs. Code Failure Triage (SC4)" subsection at lines 165–181 — this is the infra-vs-code triage the RPT-02 SC4 criterion requires)
    - .planning/research/AUDIT-REPORT.md lines 536–583 (Phase 4 SVD-vs-copy split + Phase 6 go/no-go — the fully-derived FPCA finding with real 38.307 ms wall-clock and 0.14% copy-share numbers to carry as the tracer finding)
  </read_first>
  <action>
    Edit `.planning/research/AUDIT-REPORT.md` in place. Flip the header status line (line 6) from "In progress (Phase 1 of 9)" to reflect Phase 9 consolidation in progress (e.g. "Consolidating — Phase 9 of 9"). Do NOT delete any existing Phase 1–8 section.

    Add a new top-level `## Methodology (Consolidated)` section near the report top (after the header, before or alongside the existing `## §Methodology`). It must either (a) restate concisely OR (b) explicitly cross-link the existing `## §Methodology` subsections, and MUST name both required elements for the RPT-02 SC4 criterion: build-mode/feature-flag discipline (the `--release` check + the `""`/`parallel`/`linalg`/`linalg,parallel` matrix) AND the infra-vs-code failure-triage rule (bus errors classified as infrastructure, not defects — cross-reference the existing "Infrastructure vs. Code Failure Triage (SC4)" subsection).

    Add a new top-level `## Consolidated Findings` section. Under it add a `### Performance Findings` subsection and write ONE fully-worked finding end-to-end: "FPCA is dominated by SVD compute, not by the FdMatrix→DMatrix copy". State the finding, then attach reproducible evidence as a Markdown link to `.planning/research/bench/p4_fpca_linalg,parallel_run1.txt` and cite the real numbers (38.307 ms wall-clock at N=1000,M=200; copy-share ~0.14%; SVD share ~99.8–99.9%). Cross-link the P6-1 backlog item in BACKLOG.md as the promotion candidate this finding produces.

    Do NOT edit any file under fdars-core/. This is a docs deliverable.
  </action>
  <verify>
    <automated>grep -q '^## Consolidated Findings' .planning/research/AUDIT-REPORT.md && grep -Eq '^## Methodology \(Consolidated\)' .planning/research/AUDIT-REPORT.md && grep -q 'bench/p4_fpca_linalg,parallel_run1.txt' .planning/research/AUDIT-REPORT.md && grep -q '### Performance Findings' .planning/research/AUDIT-REPORT.md && ! grep -q 'In progress (Phase 1 of 9)' .planning/research/AUDIT-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - AUDIT-REPORT.md contains a `## Consolidated Findings` section with a `### Performance Findings` subsection
    - AUDIT-REPORT.md contains a `## Methodology (Consolidated)` section that names BOTH build-mode/feature-flag discipline AND the infra-vs-code failure-triage rule
    - The one worked performance finding links to an existing file under `.planning/research/bench/` and cites a real number from it (38.307 ms and/or ~0.14% copy-share)
    - The report header status line no longer reads "In progress (Phase 1 of 9)"
    - All eight prior `## Phase N` sections still exist (no deletion) — spot-check by grepping for `## Phase 8`
    - No file under `fdars-core/` is modified
  </acceptance_criteria>
  <done>The report has a consolidated methodology section (build-mode + infra-triage), a Consolidated Findings section opened with one evidence-linked performance finding, and the status header flipped — with all prior phase sections intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Tracer completeness self-check — assert the one item passes the full pipeline</name>
  <files>.planning/research/BACKLOG.md</files>
  <read_first>
    - .planning/research/BACKLOG.md (the file just written in Task 1 — re-read the P6-1 item block and ranked row)
  </read_first>
  <action>
    Add a `## Completeness Gate` section at the end of `BACKLOG.md` documenting the checklist every item MUST pass (the 7 fields) and the three phase-level assertions Plan 03 will finalize: (1) at least one P1 item exists; (2) no top-10 ranked item is a cosmetic convenience-only entry; (3) items are ordered by descending `value / sqrt(effort)`. For this tracer, record that the single P6-1 item passes the 7-field checklist and carries a computed score, and note that P1-existence and top-10-non-cosmetic are DEFERRED to Plan 03 (they need the full item set). This section is the checklist contract Plans 02/03 reuse.

    Do NOT edit any file under fdars-core/. This is a docs deliverable.
  </action>
  <verify>
    <automated>grep -q '^## Completeness Gate' .planning/research/BACKLOG.md && grep -Eiq 'at least one P1' .planning/research/BACKLOG.md && grep -Eiq 'descending' .planning/research/BACKLOG.md</automated>
  </verify>
  <acceptance_criteria>
    - BACKLOG.md contains a `## Completeness Gate` section listing the 7 checklist fields and the 3 phase-level assertions (P1 exists, no cosmetic top-10, descending-score order)
    - The gate section records that the tracer's single item passes the 7-field checklist
    - No file under `fdars-core/` is modified
  </acceptance_criteria>
  <done>The completeness-gate contract is written; the tracer item is confirmed to pass the 7-field checklist; phase-level assertions are explicitly deferred to Plan 03.</done>
</task>

</tasks>

<threat_model>
Docs-only audit deliverable — no code, no new attack surface; no applicable threats.
</threat_model>

<verification>
- BACKLOG.md exists with ranking methodology, a ranked table (computed score), one 7-field item block, and a completeness gate.
- AUDIT-REPORT.md has a consolidated methodology section (build-mode + infra-triage) and a Consolidated Findings section with one evidence-linked performance finding; status header flipped; all prior phase sections intact.
- Every evidence link written in this plan resolves to a real file under `.planning/research/bench/`.
- No `fdars-core/` file touched.
</verification>

<success_criteria>
The aggregate → evidence-link → prioritize → 7-field-checklist pipeline is proven end-to-end on one performance finding and one backlog item, and both deliverable scaffolds (BACKLOG.md, report Consolidated Findings + Methodology) exist for Plans 02/03 to fill.
</success_criteria>

<output>
Create `.planning/phases/09-consolidated-report-prioritized-backlog/09-01-SUMMARY.md` when done.
</output>
