---
milestone: v0.23.0
audited: 2026-08-20
status: passed
scores:
  requirements: 3/3
  phases: 3/3
  integration: 1/1
  flows: n/a (additive library milestone — no E2E user flows)
gaps: {}
tech_debt:
  - phase: all
    items:
      - "Nyquist: all three VALIDATION.md files are status: draft (NOT-VALIDATED) — validate-phase was not run this milestone. Coverage TODO, not a compliance failure; adjusted p / depth / detector outputs are already asserted by inline tests."
  - phase: 28-depth-measure-long-tail
    items:
      - "extremal & ERL are symmetric rank measures — a top magnitude outlier ties the lowest boundary curve for shallowest (documented in rustdoc + tests)."
      - "TVD uses ÷n normalization (fdaoutlier) making the highest curve score 0; MSS shape_variation reconstructed from Huang & Sun (2019) description (C++ internals not read) — [ASSUMED], documented."
  - phase: 29-outlier-detector-suite
    items:
      - "muod is the Fast-MUOD variant (regression vs pointwise mean, boxplot cutoff only; tangent deferred to backlog)."
      - "depthgram handles univariate (p=1) only; SeqTransformConfig is not serde-serializable (holds a DepthMethod)."
  - phase: 30-interval-testing-procedure-family
    items:
      - "itp_flm uses response-permutation (RESEARCH Assumption A2) rather than R's ITPlmbspline partial-residual method; raw p = (n_ge+1)/(n_perm+1) — both documented in rustdoc."
---

# Milestone v0.23.0 — Depth, Outliers & Interval Inference — Audit

**Status: PASSED** — 3/3 requirements satisfied, 3/3 phases verified, cross-phase integration confirmed.

## Requirements Coverage (3-source cross-reference)

| REQ | Phase | VERIFICATION.md | SUMMARY frontmatter | Traceability | Final |
|-----|-------|-----------------|---------------------|--------------|-------|
| DEPTH-01 | 28 | passed (5/5) | [DEPTH-01] (28-01/02/03) | `[ ]` → update `[x]` | **satisfied** |
| OUT-01 | 29 | passed (5/5) | [OUT-01] (29-01/02) | `[ ]` → update `[x]` | **satisfied** |
| INF-03 | 30 | passed (5/5) | INF-03 (in-prose; frontmatter minor) | `[x]` Complete | **satisfied** |

No unsatisfied, partial-blocking, or orphaned requirements.

## Phases

| Phase | Requirement | Verification | Notes |
|-------|-------------|--------------|-------|
| 28 Depth-Measure Long Tail | DEPTH-01 | passed 5/5 | 9 measures (HI/MHI/EI/HRD/MHRD, extremal, ERL, L∞, TVD+MSSI); TvdMssResult pinned |
| 29 Outlier-Detector Suite | OUT-01 | passed 5/5 | tvdmss / muod / sequential_transform_outliers / depthgram |
| 30 Interval Testing Procedure Family | INF-03 | passed 5/5 | itp_one_pop / itp_two_pop / itp_flm + pval_correct closure |

## Cross-Phase Integration

- **Phase 29 → Phase 28 (verified):** `tvdmss` (`outliers.rs:528`) calls `total_variation_depth_1d(data, data)?` and consumes both `tvd` and `mss` from the pinned `TvdMssResult` — the one hard cross-phase dependency, wired correctly.
- Phases 28 and 30 are independent (no cross-phase wiring required).
- Full lib suite (2178 tests) + `cargo clippy --all-targets --features linalg,parallel -- -D warnings` + `cargo build --features serde` all green after the milestone.

## Definition of Done

Milestone DoD (from ROADMAP): additive/non-breaking, `Result`-returning, inline `#[cfg(test)]` tests, crate-root re-exports, **zero changes to existing public signatures**, no new crate dependency, numeric outputs only. **All met** — the only edits to existing files are additive `pub use` re-exports, `DepthMethod`/inference module additions, and an additive `#[cfg_attr(feature=serde,...)]` on `ProjectionBasisType`.

## Tech Debt (non-blocking)

See frontmatter `tech_debt`. All items are either intentional documented divergences (R baselines matched by capability per the milestone constraint) or the Nyquist NOT-VALIDATED coverage TODO (run `/gsd-validate-phase 28|29|30` to reconcile). None block completion.

## Verdict

All requirements met, cross-phase integration verified, suite + clippy green. **Proceed to complete-milestone.**
