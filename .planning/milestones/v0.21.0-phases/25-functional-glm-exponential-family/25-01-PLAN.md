---
phase: 25-functional-glm-exponential-family
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/scalar_on_function/glm.rs
  - fdars-core/src/scalar_on_function/mod.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [REG-02]

estimate:
  tokens: 78000
  raw_tokens: 52000
  tasks: 4
  confidence: med

must_haves:
  truths:
    - "functional_glm(data, y, family, scalar_covariates, ncomp, max_iter, tol) returns Result<FunctionalGlmResult, FdarError>; GlmFamily{Binomial,Poisson,Gamma,Gaussian} each carries canonical link + variance (SC1)"
    - "functional_glm(..., GlmFamily::Binomial) reproduces functional_logistic coefficients + fitted_values within tolerance; functional_logistic signature unchanged (SC2)"
    - "Poisson (log link) and Gamma (inverse link) recover the known generative signal within stated tolerance; fitted_values all finite and positive (SC3)"
    - "IRLS runs over fdata_to_pc_1d FPC scores; out-of-domain response (negative/non-integer Poisson, non-positive Gamma, out-of-range Binomial) and dimension mismatch return FdarError, never panic (SC4)"
    - "full suite + cargo clippy --all-targets --features linalg,parallel -- -D warnings green; no new crate dependency (statrs already present) (SC5)"
  artifacts:
    - "fdars-core/src/scalar_on_function/glm.rs (functional_glm, predict_functional_glm, GlmFamily impl, irls step/loop, result assembly, inline #[cfg(test)] tests)"
    - "GlmFamily enum + FunctionalGlmResult struct (defined in glm.rs or mod.rs)"
    - "barrel pub use in scalar_on_function/mod.rs + crate-root pub use in src/lib.rs"
  key_links:
    - "GlmFamily::link_deriv separate from irls_weight — Gamma working response z=eta+(y-mu)*(-1/mu^2), NOT z=eta+(y-mu)/w (RESEARCH Pitfall 1)"
    - "Gamma intercept init = 1/mean(y) so eta_0 > 0; mu/eta clamping keeps inverse/log finite (RESEARCH Pitfalls 2,3)"
    - "Binomial arm of generic IRLS is bit-compatible with logistic.rs irls_step (same w and z) — parity gate SC2"
---

<objective>
Generalize the existing `functional_logistic` IRLS-over-FPC-scores path into a public
`functional_glm` entry point covering the four mainstream exponential-family families via a
`GlmFamily { Binomial, Poisson, Gamma, Gaussian }` enum (canonical link + variance per family),
plus a `FunctionalGlmResult` struct and a `predict_functional_glm` companion. Implements REG-02.

Purpose: closes the P1 table-stakes functional-GLM gap (REG-02) reusing existing scalar-on-function
design machinery — no new algorithm subsystem, no new crate dependency.
Output: new `fdars-core/src/scalar_on_function/glm.rs`; barrel + crate-root re-exports; inline tests.
Additive/non-breaking: `functional_logistic` retained verbatim.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-functional-glm-exponential-family/25-CONTEXT.md
@.planning/phases/25-functional-glm-exponential-family/25-RESEARCH.md
@fdars-core/src/scalar_on_function/logistic.rs
@fdars-core/src/scalar_on_function/mod.rs
</context>

## Artifacts this phase produces

New symbols (all additive; no existing signature changes):

| Symbol | Kind | Location |
|--------|------|----------|
| `fdars-core/src/scalar_on_function/glm.rs` | new file | scalar_on_function module |
| `functional_glm(data, y, family, scalar_covariates, ncomp, max_iter, tol) -> Result<FunctionalGlmResult, FdarError>` | pub fn (`#[must_use]`) | glm.rs |
| `predict_functional_glm(fit, new_data, new_scalar) -> Vec<f64>` | pub fn | glm.rs |
| `GlmFamily` enum + variants `Binomial`, `Poisson`, `Gamma`, `Gaussian` | pub enum (`#[non_exhaustive]`, `#[derive(Debug, Clone, Copy, PartialEq)]`, conditional serde) | glm.rs or mod.rs |
| `FunctionalGlmResult` struct — fields `{ intercept, beta_t, beta_se, gamma, fitted_values, linear_predictors, ncomp, coefficients, std_errors, log_likelihood, deviance, iterations, fpca, aic, bic, family }` | pub struct (`#[non_exhaustive]`, `#[derive(Debug, Clone, PartialEq)]`, conditional serde) | glm.rs or mod.rs |
| `mod glm;` + `pub use glm::{functional_glm, predict_functional_glm, GlmFamily, FunctionalGlmResult};` | barrel lines | scalar_on_function/mod.rs |
| `functional_glm, predict_functional_glm, GlmFamily, FunctionalGlmResult` appended | crate-root re-export | src/lib.rs (existing `pub use scalar_on_function::{…}` block) |

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end Gaussian tracer — functional_glm one path through every layer</name>
  <files>fdars-core/src/scalar_on_function/glm.rs, fdars-core/src/scalar_on_function/mod.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/scalar_on_function/logistic.rs (irls_step:14-49, irls_loop:64-84, build_logistic_result:87-157, functional_logistic:159+, predict_functional_logistic:257 — the IRLS scaffold and result-assembly pattern to generalize; `use super::{...}` import list at top)
    - fdars-core/src/scalar_on_function/mod.rs (FunctionalLogisticResult:136-169 struct template; `mod logistic;`:27; barrel `pub use logistic::{...}`:39; helpers build_design_matrix/recover_beta_t/compute_beta_se/compute_fitted/sigmoid/cholesky_factor/cholesky_solve/compute_ols_std_errors)
    - fdars-core/src/regression.rs (fdata_to_pc_1d signature + FpcaResult fields mean/rotation/weights)
    - fdars-core/src/error.rs (FdarError::{InvalidParameter{parameter:&'static str, message:String}, InvalidDimension{parameter,expected,actual}, ComputationFailed})
    - fdars-core/src/lib.rs (crate-root re-export block `pub use scalar_on_function::{...}` ending with SelectionCriterion — the additive insertion point)
  </read_first>
  <action>
    Create fdars-core/src/scalar_on_function/glm.rs. Define `GlmFamily` (pub enum, variants Binomial/Poisson/Gamma/Gaussian, `#[non_exhaustive]`, `#[derive(Debug, Clone, Copy, PartialEq)]`, conditional serde via the crate's `#[cfg_attr(feature = "serde", ...)]` convention) and `FunctionalGlmResult` (pub struct, `#[non_exhaustive]`, `#[derive(Debug, Clone, PartialEq)]`, conditional serde) with exactly the fields listed in "Artifacts this phase produces". Rustdoc every public item and every field.

    Implement the full pipeline for the Gaussian arm only, but wire every layer so the other families slot in without an architectural change: entry-point response-domain validation dispatched on family (Gaussian = no restriction; leave Binomial/Poisson/Gamma guards as complete match arms filled in Task 3), dimension checks (y.len()==n, n>=3, ncomp>=1), then `fdata_to_pc_1d` → `build_design_matrix` (reuse verbatim) → a generic `irls_step_glm(design, y, beta, family)` that computes eta, mu via `family.inv_link`, weights via `family.irls_weight`, working response `z_i = eta_i + (y_i - mu_i) * family.link_deriv(mu_i)`, then solves (X'WX)β=X'Wz via `cholesky_solve` → `irls_loop_glm` converging on deviance-change < tol (compute `family.deviance` each iteration; break on `|dev_new - dev_old| < tol`, cap at max_iter, break on singular step) → result assembly reusing `recover_beta_t`, `compute_beta_se`, `cholesky_factor`+`compute_ols_std_errors`, filling `fitted_values` (=mu), `linear_predictors` (=eta), `deviance`, `log_likelihood`, `aic`, `bic`, `family`.

    Add `GlmFamily` methods `inv_link`, `link_deriv`, `irls_weight`, `deviance(&[f64],&[f64])`, `log_likelihood(&[f64],&[f64])` with all four match arms present (Gaussian correct now; the other arms may be provisional but must compile and be exhaustive so clippy `--all-targets` stays green). Keep `link_deriv` STORED SEPARATELY from `irls_weight` — the working response must use `link_deriv`, never `1/weight` (this is the Gamma sign-safety invariant; see Task 3).

    Signature (exact): `pub fn functional_glm(data: &FdMatrix, y: &[f64], family: GlmFamily, scalar_covariates: Option<&FdMatrix>, ncomp: usize, max_iter: usize, tol: f64) -> Result<FunctionalGlmResult, FdarError>`. Mark it `#[must_use]`. Add `predict_functional_glm(fit: &FunctionalGlmResult, new_data: &FdMatrix, new_scalar: Option<&FdMatrix>) -> Vec<f64>` mirroring `predict_functional_logistic` but returning `family.inv_link(eta)` per row.

    Wire the barrel in mod.rs: add `mod glm;` near `mod logistic;` and `pub use glm::{functional_glm, predict_functional_glm, GlmFamily, FunctionalGlmResult};`. Append the same four names to the crate-root `pub use scalar_on_function::{...}` block in lib.rs (additive — do not reorder or remove existing entries). Add one inline `#[cfg(test)] mod tests` with a single Gaussian smoke test: a small synthetic n=30,m=40 fit that asserts the call is `Ok`, `fitted_values.len()==n`, `beta_t.len()==m`, `iterations>=1`, and all `fitted_values` finite.

    Do NOT special-case Gaussian as a separate non-IRLS branch — Gaussian runs through the same IRLS uniformly (weight ≡ 1, identity link → converges in one step). No fenced code in this action; identifiers above are the contract.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib glm -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `fdars-core/src/scalar_on_function/glm.rs` exists and compiles under `--features linalg,parallel`.
    - `GlmFamily` and `FunctionalGlmResult` are pub, `#[non_exhaustive]`, derive the project-standard traits with conditional serde.
    - `pub fn functional_glm(...)` has the exact locked signature and is `#[must_use]`; `predict_functional_glm` present.
    - Crate-root re-export in lib.rs exports `functional_glm, predict_functional_glm, GlmFamily, FunctionalGlmResult`; existing entries untouched.
    - The Gaussian smoke test passes: `cargo test ... --lib glm` reports the tracer test green.
  </acceptance_criteria>
  <reversibility rating="reversible">Additive new module + additive re-export lines; no existing signature touched.</reversibility>
  <done>Gaussian path works end-to-end through validation → FPCA → IRLS → result, is re-exported, and the smoke test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Binomial family + parity with functional_logistic (SC2)</name>
  <files>fdars-core/src/scalar_on_function/glm.rs</files>
  <read_first>
    - fdars-core/src/scalar_on_function/glm.rs (the Task-1 GlmFamily match arms + irls_step_glm to fill for Binomial)
    - fdars-core/src/scalar_on_function/logistic.rs (irls_step:14-49 — Binomial w=(mu*(1-mu)).max(1e-10), z=eta+(y-mu)/w; logistic_log_likelihood:52-61; build_logistic_result:87-157 for the deviance/aic/bic formulas to match)
    - fdars-core/src/scalar_on_function/mod.rs (sigmoid:561-568 — reuse the stable two-branch impl)
  </read_first>
  <behavior>
    - Binomial parity: functional_glm(data, y, GlmFamily::Binomial, None, 3, 25, 1e-6) coefficients agree with functional_logistic(data, y, None, 3, 25, 1e-6) coefficients within 1e-6.
    - Binomial parity: glm fitted_values (= P(Y=1)) agree with logistic probabilities within 1e-6.
    - Out-of-range Binomial response (a value neither 0.0 nor 1.0) returns FdarError::InvalidParameter, no panic.
  </behavior>
  <action>
    Complete the Binomial arms so the generic IRLS is mathematically identical to logistic.rs `irls_step`: `inv_link` = `sigmoid(eta)`; `irls_weight` = `(mu*(1-mu)).max(1e-10)`; `link_deriv` = `1/(mu*(1-mu)).max(1e-10)` so that `z_i = eta + (y-mu)*link_deriv = eta + (y-mu)/(mu(1-mu))` reproduces the logistic working response exactly. `log_likelihood` Binomial arm = `y*ln(mu) + (1-y)*ln(1-mu)` with mu clamped to [1e-15, 1-1e-15] (matches logistic_log_likelihood); Binomial `deviance` = `2*(xlogy(y, y/mu) + xlogy(1-y, (1-y)/(1-mu)))` with a private `xlogy(x,y)= if x==0.0 {0.0} else {x*y.ln()}` helper for the 0·log(0) convention. Fill the Binomial response guard: reject any `y_i` not equal to 0.0 or 1.0 with `FdarError::InvalidParameter { parameter: "y", message: ... }` (mirror logistic's guard).

    Reuse `sigmoid` from the parent module rather than re-implementing. Add inline tests: (1) a parity test constructing a 30×50 binary dataset, fitting both `functional_logistic` and `functional_glm(..., Binomial)`, asserting per-element coefficient agreement < 1e-6 and fitted-vs-probabilities agreement < 1e-6; (2) an out-of-range Binomial guard test asserting `matches!(err, FdarError::InvalidParameter { .. })`.

    Convergence note: use deviance-change as the IRLS stop criterion (per CONTEXT/RESEARCH); if the parity test is off by more than 1e-6 solely due to an iteration-count difference vs logistic's coefficient-change criterion, tighten by running enough iterations (both converge to the same fixed point) — do NOT loosen the parity tolerance below 1e-6 without documenting why in rustdoc. No fenced code in this action.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib glm -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - A test named for Binomial parity passes: glm-vs-logistic coefficients and fitted_values agree within 1e-6.
    - A test for the out-of-range Binomial guard passes, asserting `FdarError::InvalidParameter`.
    - No new import of any external crate; `sigmoid` reused from parent module.
  </acceptance_criteria>
  <done>Binomial family reproduces functional_logistic within 1e-6 and rejects out-of-range responses with FdarError.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Poisson + Gamma families, recovery + domain guards (SC3, SC4)</name>
  <files>fdars-core/src/scalar_on_function/glm.rs</files>
  <read_first>
    - fdars-core/src/scalar_on_function/glm.rs (GlmFamily arms, irls_loop_glm init, xlogy helper from Task 2)
    - fdars-core/src/scalar_on_function/logistic.rs (irls_loop:64-84 — beta init `vec![0.0; p_total]` — must be overridden for Gamma)
    - fdars-core/src/error.rs (FdarError::InvalidParameter variant field names)
  </read_first>
  <behavior>
    - Poisson recovery: on deterministic counts from log(mu)=a+b*score (n=100), all fitted_values are finite and strictly positive, and predictions Pearson-correlate with the true mu at > 0.9.
    - Gamma recovery: on deterministic responses from 1/mu = a+b*score with mu in [0.1,10] (n=100), all fitted_values are finite and strictly positive.
    - Negative Poisson count (y_i < 0) → FdarError::InvalidParameter, no panic.
    - Non-integer Poisson count (y_i = 1.5) → FdarError::InvalidParameter, no panic.
    - Non-positive Gamma response (y_i <= 0.0) → FdarError::InvalidParameter, no panic.
    - Dimension mismatch (y.len() != data.nrows()) → FdarError (InvalidDimension), no panic.
  </behavior>
  <action>
    Fill the Poisson and Gamma arms. Poisson: `inv_link(eta) = eta.min(500.0).exp().max(1e-10)` (RESEARCH Pitfall 2 — clamp before exp to avoid overflow); `irls_weight(mu) = mu.max(1e-10)`; `link_deriv(mu) = 1.0/mu.max(1e-10)`; `log_likelihood` arm = `y*mu.ln() - mu - ln_gamma(y+1.0)` using `statrs::function::gamma::ln_gamma` (already a crate dependency — no new dep); `deviance` arm = `2*(xlogy(y, y/mu) - (y-mu))`. Gamma: `inv_link(eta) = (1.0/eta.max(1e-10)).max(1e-10)` (Pitfall 3 — eta must stay > 0); `irls_weight(mu) = (1.0/mu.max(1e-10).powi(2)).max(1e-10)`; `link_deriv(mu) = -1.0/mu.max(1e-10).powi(2)` — NEGATIVE, the working response `z = eta + (y-mu)*link_deriv` therefore carries the correct sign (RESEARCH Pitfall 1: never derive z from the weight); `log_likelihood` arm = `-y/mu - mu.ln()`; `deviance` arm = `2*((y-mu)/mu - (y/mu).ln())`.

    Beta initialization: in `irls_loop_glm`, for `GlmFamily::Gamma` initialize `beta[0] = 1.0 / mean(y)` (with `mean(y).max(1e-10)`) so eta_0 > 0 and mu_0 = mean(y); all other families keep zero init (RESEARCH Pitfall 3 / Pattern 4).

    Fill the Poisson response guard (reject `y_i < 0.0 || y_i != y_i.floor()` → InvalidParameter, non-negative-integer message per RESEARCH Pitfall 5) and the Gamma guard (reject `y_i <= 0.0` → InvalidParameter, strictly-positive message). Guards run at entry, BEFORE `fdata_to_pc_1d` (early exit). Confirm the dimension check (`y.len() == data.nrows()`, else InvalidDimension) also fires before FPCA.

    Add inline tests matching every <behavior> bullet: Poisson recovery (positive + finite fitted_values, Pearson corr of predictions vs true mu > 0.9), Gamma recovery (positive + finite fitted_values), and four guard tests (negative Poisson, non-integer Poisson, non-positive Gamma, dimension mismatch) each asserting the appropriate `FdarError` variant via `matches!`. Use deterministic data (round mu to counts for Poisson; y=mu for Gamma) so tests are seed-free; keep recovery tolerances loose (±0.3–0.5 on coefficients) per RESEARCH. No fenced code in this action.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib glm -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Poisson recovery test passes: all fitted_values finite & > 0, prediction-vs-true-mu Pearson correlation > 0.9.
    - Gamma recovery test passes: all fitted_values finite & > 0.
    - Four guard tests pass: negative Poisson, non-integer Poisson, non-positive Gamma → InvalidParameter; dimension mismatch → InvalidDimension.
    - `statrs::function::gamma::ln_gamma` is used for Poisson log-likelihood; no new Cargo dependency added.
    - Gamma `link_deriv` is negative and the working response is `eta+(y-mu)*link_deriv` (not eta+(y-mu)/weight).
  </acceptance_criteria>
  <done>Poisson and Gamma families recover their generative signals within tolerance and all out-of-domain / dimension-mismatch inputs return FdarError without panicking.</done>
</task>

<task type="auto">
  <name>Task 4: Rustdoc, convention divergence notes, and full-suite + clippy gate (SC5)</name>
  <files>fdars-core/src/scalar_on_function/glm.rs</files>
  <read_first>
    - fdars-core/src/scalar_on_function/glm.rs (final state after Tasks 1-3)
    - fdars-core/src/scalar_on_function/logistic.rs (functional_logistic:159+ rustdoc style — Arguments / Returns / Errors sections to mirror)
  </read_first>
  <action>
    Finalize rustdoc: a module-level `//!` doc for glm.rs summarizing the functional GLM, the four families with their canonical link + variance, and the reuse of the logistic IRLS-over-FPC-scores path. Document `functional_glm` with Arguments / Returns / Errors sections mirroring `functional_logistic`. In rustdoc, note the divergences flagged in RESEARCH so reviewers see them resolved: (a) IRLS converges on deviance-change < tol (not coefficient-change); (b) canonical links only, Gamma = inverse link; (c) AIC uses `-2*log_likelihood + 2*p` and BIC `-2*log_likelihood + p*ln(n)` with the log-likelihood kernel per family (dispersion not separately estimated) — state that Gamma/Gaussian AIC magnitudes are therefore not directly comparable to R's `glm()`/`lm()`; (d) mu/eta clamping thresholds and the Gamma 1/mean(y) intercept init. Ensure at least one working doc example (`/// ```` `) for `functional_glm` that compiles (Gaussian or Binomial), respecting the TMPDIR doctest-link note.

    Run the full gate and fix any warnings surfaced by test/bench code: full suite `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel` and `cargo clippy --all-targets --features linalg,parallel -- -D warnings`. Because `GlmFamily` is `#[non_exhaustive]` yet defined in-crate, all in-crate matches must remain exhaustive with the four explicit arms (no `_` wildcard) — clippy `--all-targets` catches a missed arm. No fenced code in this action beyond the rustdoc doctest.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel && cargo clippy --all-targets --features linalg,parallel -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `cargo clippy --all-targets --features linalg,parallel -- -D warnings` exits 0.
    - Full `cargo test -p fdars-core --features linalg,parallel` suite is green (all prior tests + new glm tests).
    - glm.rs has a module `//!` doc, `functional_glm` rustdoc with Arguments/Returns/Errors + a compiling doc example, and rustdoc notes for the four RESEARCH divergences.
    - No new entry in Cargo.toml `[dependencies]` (statrs already present).
  </acceptance_criteria>
  <done>Documentation complete with convention divergences noted; full suite and clippy --all-targets both green; no new dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust caller → functional_glm | Numeric `&FdMatrix` / `&[f64]` from in-process caller. No network, no deserialization of untrusted bytes, no unsafe, no new dependency. Only threat class is malformed / out-of-domain numeric input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Denial of Service | functional_glm entry | medium | mitigate | Per-family response-domain guards at entry (Poisson y≥0 integer, Gamma y>0, Binomial y∈{0,1}) return FdarError::InvalidParameter before FPCA; dimension checks return FdarError::InvalidDimension — never panic (Task 3). |
| T-25-02 | Denial of Service | IRLS inverse/log links | medium | mitigate | μ/η clamping (Poisson η≤500 before exp, Gamma η≥1e-10 so μ=1/η finite, μ≥1e-10) + Gamma intercept init 1/mean(y) prevent ÷0 / overflow / NaN in the IRLS loop (Tasks 1,3). |
| T-25-03 | Tampering | dependencies | low | accept | No new crate dependency; `statrs` (ln_gamma) already vetted and present. No package-install task in this phase. |

No auth, crypto, or injection surface (pure in-process numeric library). ASVS V5 (input validation) is the only applicable category, satisfied by the entry-point guards.
</threat_model>

<verification>
- Per task: `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib glm -- --test-threads=1`
- Phase gate: `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel` (full suite) + `cargo clippy --all-targets --features linalg,parallel -- -D warnings`
- SC1: Gaussian tracer smoke test + per-family fits return Ok with generalized fields.
- SC2: Binomial parity test (< 1e-6) vs functional_logistic; functional_logistic signature untouched (git diff shows no change to logistic.rs signature).
- SC3: Poisson + Gamma recovery tests (positive finite fitted_values, correlation > 0.9).
- SC4: four guard tests → FdarError; no panic.
- SC5: full suite + clippy --all-targets green; Cargo.toml [dependencies] unchanged.
</verification>

<success_criteria>
- `functional_glm(data, y, family, scalar_covariates, ncomp, max_iter, tol)` and `predict_functional_glm` exist, are `Result`/`Vec`-returning, and are re-exported at the crate root (SC1).
- `GlmFamily{Binomial,Poisson,Gamma,Gaussian}` each carries canonical link + variance; Binomial reproduces `functional_logistic` within 1e-6 (SC1, SC2).
- Poisson and Gamma recover their generative signals within stated tolerance (SC3).
- All out-of-domain responses and dimension mismatches return `FdarError`, never panic (SC4).
- Full suite + `cargo clippy --all-targets --features linalg,parallel -- -D warnings` green; no new dependency (SC5).
- `functional_logistic` retained verbatim (additive/non-breaking).
</success_criteria>

<output>
Create `.planning/phases/25-functional-glm-exponential-family/25-01-SUMMARY.md` when done.
</output>
