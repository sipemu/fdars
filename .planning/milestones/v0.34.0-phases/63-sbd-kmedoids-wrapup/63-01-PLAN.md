# Phase 63 — Plan 63-01: SBD-based k-medoids & Wrap-up (KSH-05)

**Milestone:** v0.34.0 (FINAL phase)
**Requirement:** KSH-05
**Mode:** Additive / non-breaking. No new dependency. No crate version bump (stays 0.33.0).

## Goal

Deliver the `sbd_kmedoids` convenience (SBD distance matrix → existing
`kmedoids_from_distances`), finalize the crate-root public surface for SBD +
k-Shape, extend the prelude, and add a criterion benchmark for the SBD/k-Shape
pipeline. Completes milestone v0.34.0.

## Tasks

### T1 — `sbd_kmedoids` in `src/kshape.rs`
- Add `pub fn sbd_kmedoids(data: &FdMatrix, config: &KMedoidsConfig) -> Result<KMedoidsResult, FdarError>`, `#[must_use]`.
- Body: `let dist = crate::metric::sbd::sbd_distance_matrix(data)?; crate::alignment::clustering::kmedoids_from_distances(&dist, config)`.
- Reuse `KMedoidsConfig` / `KMedoidsResult` unchanged (imported from `crate::alignment::clustering`).
- Doctest showing the explicit SBD-matrix → k-medoids flow.

### T2 — Crate-root re-exports (`src/lib.rs`)
- Extend the existing `pub use metric::{…}` block (lib.rs:596) with `sbd, sbd_distance_matrix, SbdResult`.
- Add `pub use kshape::{kshape_fd, sbd_kmedoids, KShapeConfig, KShapeResult};` near the kernel_kmeans re-export (keep `pub mod kshape;`).

### T3 — Prelude (`src/prelude.rs`)
- Extend `pub use crate::metric::{…}` with `sbd, sbd_distance_matrix, SbdResult`.
- Add `pub use crate::kshape::{kshape_fd, sbd_kmedoids, KShapeConfig, KShapeResult};`.

### T4 — Benchmark
- New `fdars-core/benches/kshape.rs` (criterion, `harness = false`): bench `sbd_distance_matrix` and `kshape_fd` on a small synthetic set (n≈30, m≈64, k=3, n_init=2).
- Add `[[bench]] name = "kshape"` to `fdars-core/Cargo.toml` (mirror the `shapelet` entry).

### T5 — Tests (inline `#[cfg(test)] mod tests` in kshape.rs)
- `test_sbd_kmedoids_recovers_groups` — two shifted-shape groups recovered at high purity.
- `test_sbd_kmedoids_uses_sbd_matrix` — equals manual `sbd_distance_matrix` + `kmedoids_from_distances`.
- `test_sbd_kmedoids_validation` — k=0 / k>n → errors.
- `test_kshape_reexports` — crate-root `use` of all new items resolves.

## Gates (out-of-band; commit `--no-verify`)
1. `cargo test -p fdars-core --features linalg kshape` (iterate).
2. `cargo fmt` + `cargo fmt --check` clean.
3. `cargo clippy --all-targets --features linalg,parallel -- -D warnings` clean (compiles bench).
4. `cargo test -p fdars-core --features linalg kshape` + `cargo test -p fdars-core kshape` (default) green.

## Acceptance (4 ROADMAP criteria)
1. `sbd_kmedoids` is a real public fn (SBD matrix → k-medoids).
2. Provably uses SBD (integration test == manual composition; doctest shows flow).
3. Full v0.34.0 surface re-exported additively; prelude gains items; non-breaking.
4. Criterion bench added; whole-crate gates pass.
