---
milestone: v0.19.0
milestone_name: Functional Inference Suite
audited: 2026-08-16
status: passed
scores:
  requirements: 2/2
  phases: 2/2
  integration: 2/2
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: all
    items:
      - "Nyquist VALIDATION.md not produced for either phase (carried-forward draft posture from prior implementation milestones)."
      - "phase.complete emitted a benign false-positive warning: 21-01-SUMMARY.md 'references files not on disk' (inference/dist.rs, flm.rs, anova.rs) — these files DO exist (path-format mismatch in the checker, not a real gap)."
nyquist:
  overall: not-reconciled
  reason: "VALIDATION.md not seeded/reconciled; consistent with prior implementation milestones. Code is independently test-verified (2039 lib tests green)."
---

# Milestone v0.19.0 — Functional Inference Suite · Milestone Audit

**Verdict: PASSED** — 2/2 requirements satisfied, 2/2 phases verified (independently), cross-phase integration proven by a green combined build, additive/non-breaking.

## Definition of Done (from ROADMAP)

Give fdars its first standalone functional-inference surface — two-sample tests + FLM inference — closing the two P1 table-stakes items in R-parity Area 5 (Inference), previously 0/22 present.

**Achieved:** new `fdars-core/src/inference/` module (7 files) exposing 8 public entry points: `t_perm_test`, `f_perm_test`, `two_sample_mean_test`, `mean_scb`, `scb_two_sample_test` (INF-01) + `flm_f_test`, `flm_gof_test`, `oneway_anova_vstat` (INF-02), plus `TestResult`. 9 files changed, +2063/−4. All crate-root re-exported.

## Requirements Coverage (3-source cross-reference)

| REQ-ID | Phase | VERIFICATION | SUMMARY | Traceability | Final |
|--------|-------|--------------|---------|--------------|-------|
| INF-01 | 20 | passed | listed | Complete | **satisfied** |
| INF-02 | 21 | passed | listed | Complete | **satisfied** |

**2/2 satisfied.** No unsatisfied/partial/orphaned. INF-03 (ITP) correctly deferred to v2.

## Phase Verifications

| Phase | Status | SCs | Deliverable |
|-------|--------|-----|-------------|
| 20 Two-Sample Tests & `inference/` Module | passed | 5/5 | permutation + Hotelling + Degras-SCB two-sample tests; 17 tests |
| 21 Functional-Linear-Model Inference | passed | 4/4 | FLM F-test + GoF (Ramsey-RESET) + asymptotic ANOVA V-stat; 12 tests |

## Cross-Phase Integration

Strongest possible proof for a code milestone: the combined crate **compiles and passes the full suite together** — `cargo test --lib` = **2039 passed / 0 failed**, `cargo clippy --all-targets -D warnings` clean (orchestrator-run, 2026-08-16). INF-02 (Phase 21) builds directly on INF-01's (Phase 20) module: shares `TestResult`, and the distribution survival functions were consolidated into a common `inference/dist.rs` (χ² from Phase 20 + F from Phase 21). No breakage: only additive changes to existing code (`compute_group_means`/`integrated_f_statistic` widened to `pub(crate)`; `fanova` and all other public signatures byte-identical).

## Notes / Tech Debt

- Nyquist VALIDATION.md not produced (draft posture, prior-milestone convention). Mitigated: 29 inline correctness tests validate statistical behavior (reject real effects, fail-to-reject nulls, distribution SFs vs tabulated quantiles, determinism).
- `has_warnings` from phase.complete is a false positive (SUMMARY path-format mismatch; the referenced files exist).
- No new crate dependency (self-contained χ²/F survival functions).

## Verdict

**PASSED.** Ready to complete and archive. A crate release (version bump 0.17.0 → next + PR + tag) is a separate ship step — this milestone shipped real code, so unlike v0.18.0 it warrants a release when the operator chooses.
