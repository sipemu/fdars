---
milestone: v0.37.0
audited: 2026-09-04T21:36:45Z
status: passed
scores:
  requirements: 6/6
  phases: 3/3
  integration: 14/14
  flows: 4/4
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: pre-existing (NOT introduced by v0.37.0)
    items:
      - "cargo build --features serde fails on shapelet/classifier.rs (Phase 60, commit ea39c623): ShapeletTransformClassifier derives serde but embeds ClassifFit which lacks serde derives. Unrelated to WAV — new wavelet types (WaveletCoeffs, Wcr*/Wnet* configs+results) are serde-clean. Candidate GSD-ready backlog fix: add serde-gated derives to ClassifFit + its fitted sub-structs."
deferred:
  - "WAV-F1: binomial/logistic + GLM-family wavelet-domain regression (refund `family` argument) — Gaussian-only this milestone."
  - "WAV-F2: Symlets/Coiflets/biorthogonal wavelet families + wavelet packets; Daubechies orders beyond db10."
  - "WAV-F3: 2D/surface DWT + regression."
  - "Expose the additive wavelet surface (DWT + wcr/wnet) to the R + WASM bindings (issue fdars-j75) — follow-up, not this milestone."
  - "Operator ship step: crate version bump 0.36.0 → 0.37.0 + `v0.37.0` git tag + crates.io publish (release.yml publishes on the tag)."
---

# Milestone v0.37.0 — WAV: Wavelet-Domain Functional Regression — Audit

**Status: PASSED** · 6/6 requirements satisfied · 3/3 phases verified · integration SOUND (14/14 symbols wired) · 4/4 E2E flows complete.

Promotes **GAP-07** (wavelet-domain functional regression, `wcr`/`wnet`) from the v0.31.0 `GAP-BACKLOG.md`. Adds a from-scratch in-crate discrete wavelet transform plus wavelet-domain regularized scalar-on-function regression, for spiky/localized functional predictors in a sparse wavelet basis. Additive/non-breaking; no new crate dependency.

## Requirements Coverage (3-source cross-reference)

Each REQ-ID cross-referenced across VERIFICATION.md status, SUMMARY.md `requirements-completed` frontmatter, and the REQUIREMENTS.md traceability table — all three agree.

| REQ-ID | Description | Phase | VERIFICATION | SUMMARY | Traceability | Final |
|--------|-------------|-------|--------------|---------|--------------|-------|
| WAV-01 | Forward/inverse orthogonal DWT (Haar + db2–db10, multi-level, perfect reconstruction) | 69 | passed | listed | [x] | **satisfied** |
| WAV-02 | Boundary handling (periodic + symmetric, non-power-of-2, FdarError on invalid) | 69 | passed | listed | [x] | **satisfied** |
| WAV-03 | `wcr` — wavelet-domain PCR/PLS SoF regression (β(t), intercept, fitted) | 70 | passed | listed | [x] | **satisfied** |
| WAV-04 | `wnet` — wavelet-domain elastic-net (sparse recovery, CV-λ, selected coeffs) | 70 | passed | listed | [x] | **satisfied** |
| WAV-05 | Out-of-sample predict (self-consistent) + β(t)/fitted accessors | 71 | passed | listed | [x] | **satisfied** |
| WAV-06 | Crate-root + prelude re-exports of full surface + module doctest; additive/non-breaking | 71 | passed | listed | [x] | **satisfied** |

No unsatisfied requirements. No orphaned requirements (every REQ-ID appears in a phase VERIFICATION.md).

## Phases

| Phase | Name | Verification | Plans | Key gates |
|-------|------|--------------|-------|-----------|
| 69 | Discrete Wavelet Transform Primitive | passed (8/8 must-haves) | 2/2 | round-trip ≤1e-10 for Haar+db2–db10 across levels & both boundary modes on non-power-of-2 lengths; Haar known-answer; invalid→FdarError; 38 wavelet tests |
| 70 | Wavelet-Domain Regressors (`wcr` + `wnet`) | passed (7/7 must-haves) | 2/2 | wcr PCR+PLS β(t) recovery <1e-6 on full-rank spanning designs; wnet sparse-support recovery + deterministic CV-λ; invalid→FdarError; 74 wavelet tests |
| 71 | Prediction, Diagnostics & Integration | passed (6/6 must-haves) | 1/1 | predict self-consistency ≤1e-8 (both); accessors; full 14-symbol crate-root+prelude surface; module doctest under `--doc`; whole-crate 2794 lib + 199 doc tests green, clippy `--all-targets` clean, fmt clean |

## Cross-Phase Integration (checker verdict: SOUND)

- **69 → 70:** `curves_to_coeff_design` calls Phase 69 `decompose_matrix`; `coeff_weights_to_beta_t` calls Phase 69 `reconstruct`. Both `wcr` and `wnet` route through these shared seams — no stub, no bypass.
- **70 → 71:** `WcrResult::predict` / `WnetResult::predict` re-transform new curves via the **stored** fitted DWT config (`family`, `mode`, `Some(level)`) — no default fallback.
- **Public surface:** 14 symbols re-exported identically at crate root (`lib.rs`) and prelude (`prelude.rs`), no feature gates, no duplication.
- **E2E:** module doctest demonstrates decompose → fit `wcr` → `predict` → read β(t)/fitted via accessors, and passes under `cargo test --doc`.
- **Additive/non-breaking:** no existing public signature changed; crate + tests build clean; the 28 examples + R/WASM bindings unaffected.

## Code Review

Each phase's changed source was reviewed and findings resolved before completion:
- Phase 69: 0 Critical / 3 Warning / 2 Info — all resolved (QMF sign doc, length-validation guard, scoped dead_code allow, +db3/5/7/9 round-trip tests).
- Phase 70: 1 Critical / 3 Warning / 2 Info — CR-01 (small-n wcr `InvalidDimension` for n≤ncomp) fixed + 3 validation gaps closed; 2 Info deferred.
- Phase 71: 0 Critical / 2 Warning / 2 Info — all resolved (zero-row predict param name + `# Errors` docs, affine-intercept field doc, doctest now witnesses self-consistency, redundant must_use removed).

## Tech Debt

No tech debt introduced by v0.37.0. One **pre-existing, unrelated** carry-forward: `cargo build --features serde` fails on `shapelet/classifier.rs` (Phase 60) — the new wavelet types are serde-clean. Candidate backlog fix: serde-gate `ClassifFit` + its fitted sub-structs.

## Deferred (intentional scope decisions, not gaps)

WAV-F1 (GLM/binomial families), WAV-F2 (Symlets/Coiflets/biorthogonal + packets + db11–db20), WAV-F3 (2D/surface DWT), R+WASM binding exposure of the wavelet surface (issue fdars-j75), and the operator ship step (crate bump 0.36.0→0.37.0 + `v0.37.0` tag + crates.io publish).

## Verdict

Milestone v0.37.0 achieved its definition of done. Ready for `/gsd-complete-milestone v0.37.0`.
