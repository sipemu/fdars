---
phase: 41-spectral-functional-time-series
plan: 02
type: execute
wave: 2
depends_on: [41-01]
files_modified:
  - fdars-core/src/simulation.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [FTS-03-04, FTS-03-05]
estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "sim_fvarma generates n curves from the operator-kernel recurrence X_t = ΣA_k X_{t-k} + ε_t + ΣB_k ε_{t-k}; a zero AR operator yields pure i.i.d. innovations (near-zero lag-1 ACF) (FTS-03-04)"
    - "sim_fvarma output is bit-identical across two calls with the same seed (StdRng::seed_from_u64) (FTS-03-04)"
    - "a rank-1 AR operator with coefficient 0.8 produces non-trivial lag-1 serial dependence (‖C_1‖ > 0.1‖C_0‖) (FTS-03-04)"
    - "sim_farma combines AR+MA operator terms, is bit-identical for the same seed, and returns correct output shape (FTS-03-05)"
    - "both simulators return Result<_, FdarError>, validate kernel dimensions (each op is m×m) and argvals, and return ComputationFailed on NaN/Inf divergence during burn-in"
  artifacts:
    - "sim_fvarma, sim_farma functions in fdars-core/src/simulation.rs"
    - "FvarmaResult, FarmaResult structs in fdars-core/src/simulation.rs"
  key_links:
    - "innovations use rand_distr::Normal(0,1) per grid point with StdRng::seed_from_u64(seed) — deterministic"
    - "AR/MA kernel matrix-vector product uses column-major indexing a_k[j1 + j2*m] * x[j2]"
    - "NaN/Inf guard after burn-in returns ComputationFailed rather than emitting divergent curves"
---

<objective>
Add functional VAR/VMA (`sim_fvarma`, FTS-03-04) and functional ARMA (`sim_farma`, FTS-03-05) process simulators to `simulation.rs`. Both generate deterministic (seeded) curve series from user-supplied m×m operator kernels via a matrix-vector recurrence with Gaussian KL-style innovations and burn-in — additive/non-breaking, reusing the existing `rand`/`rand_distr` RNG pattern and `FdMatrix` column-major layout. No new crate dependency.

Purpose: completes the FTS-03 simulator half — principled operator-valued FAR/FMA/FARMA generation matching the `freqdom`/`ftsa` baseline, and provides the fixtures the spectral pipeline (Plan 41-01) validates against.
Output: `sim_fvarma`, `sim_farma`, `FvarmaResult`, `FarmaResult` in `simulation.rs`; crate-root re-exports; inline `#[cfg(test)]` oracle tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-spectral-functional-time-series/41-CONTEXT.md
@.planning/phases/41-spectral-functional-time-series/41-RESEARCH.md
@.planning/phases/41-spectral-functional-time-series/41-PATTERNS.md
@.planning/phases/41-spectral-functional-time-series/41-VALIDATION.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: sim_fvarma end-to-end — seeded recurrence, determinism verified</name>
  <files>fdars-core/src/simulation.rs</files>
  <read_first>
    - fdars-core/src/simulation.rs (module docstring lines 1-27, EFunType/EValType derive style lines 30-42, sim_kl RNG seeding lines 311-345)
    - .planning/phases/41-spectral-functional-time-series/41-RESEARCH.md (Functional VAR/VMA Simulator lines 312-357, Code Examples fvarma_step lines 643-682, Pitfall 5 divergence guard lines 529-537)
    - .planning/phases/41-spectral-functional-time-series/41-PATTERNS.md (simulation.rs section lines 247-361 — RNG, matrix-vector product, NaN guard, FvarmaResult struct, required tests)
  </read_first>
  <action>
    Append to `simulation.rs`. Define `FvarmaResult` per PATTERNS.md lines 332-341 (derive Debug/Clone/PartialEq, serde gate, `#[non_exhaustive]`): fields `curves: FdMatrix` (N×m), `ar_order: usize`, `ma_order: usize`, `burn_in: usize`.

    Implement `#[must_use] pub fn sim_fvarma(n: usize, argvals: &[f64], ar_ops: &[Vec<f64>], ma_ops: &[Vec<f64>], burn_in: usize, seed: u64) -> Result<FvarmaResult, FdarError>`. Let `m = argvals.len()`. Wire ONE full path: `let mut rng = StdRng::seed_from_u64(seed);` and `Normal::new(0.0, 1.0).expect("valid distribution parameters")`. Generate `burn_in + n` steps of the recurrence `X_t = Σ_k A_k·X_{t-k} + ε_t + Σ_k B_k·ε_{t-k}` where each `A_k`/`B_k` is a flat m×m column-major kernel applied via `s += ar_ops[k][j1 + j2*m] * history_x[k][j2]` accumulated into `x_new[j1]`, and `ε_t[j] = rng.sample(normal)` i.i.d. per grid point. Maintain ring buffers of the last `p=ar_ops.len()` curves and `q=ma_ops.len()` innovations. Discard the first `burn_in` curves. Assemble the kept N curves into `FdMatrix::from_column_major(data, n, m)` (rows=curves, cols=grid). Document in rustdoc: the recurrence, that stationarity (spectral radius < 1) is the user's responsibility, the burn-in default guidance, and the R divergence (identity innovation covariance vs freqdom's user σ).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel simulation::tests::fvarma_deterministic</automated>
  </verify>
  <acceptance_criteria>
    - `simulation.rs` compiles under `cargo build -p fdars-core --features linalg,parallel`.
    - Inline test `fvarma_deterministic` calls `sim_fvarma` twice with the same seed and asserts `res_a == res_b` (bit-identical `FvarmaResult`, PartialEq) — Oracle 5.
    - Output `curves` has shape n×m; all values finite for a moderate-norm AR operator.
  </acceptance_criteria>
  <done>sim_fvarma runs the full seeded recurrence end-to-end and produces bit-identical output for a fixed seed (FTS-03-04 core path).</done>
  <reversibility rating="reversible">Additive new function; innovation covariance convention documented and extensible without signature change.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sim_fvarma oracles + validation (zero-op white noise, rank-1 dependence, dim/NaN guards)</name>
  <files>fdars-core/src/simulation.rs</files>
  <read_first>
    - fdars-core/src/simulation.rs (Task 1 output — sim_fvarma)
    - fdars-core/src/fts/acf.rs (autocovariance_matrix pub(crate) — for lag-1 ACF oracle; mean_curve)
    - .planning/phases/41-spectral-functional-time-series/41-RESEARCH.md (Test Oracles 4 & 6 lines 719-729, Pitfall 5 lines 529-537, dimension-check pattern)
    - .planning/phases/41-spectral-functional-time-series/41-PATTERNS.md (error handling lines 306-325, required tests lines 358-361)
  </read_first>
  <behavior>
    - Test (Oracle 4): `sim_fvarma` with a single m×m zero AR operator and no MA ops → output has near-zero lag-1 autocovariance relative to lag-0 (‖C_1‖ ≪ ‖C_0‖); values finite.
    - Test (Oracle 6): rank-1 AR operator `A_1[j1,j2] = 0.8*phi[j1]*phi[j2]` (unit-norm phi) → lag-1 autocovariance ‖C_1‖ > 0.1·‖C_0‖ (uses `super::fts::acf::autocovariance_matrix` or a local Frobenius-norm helper).
    - Test: any `ar_ops[k].len() != m*m` (or ma_ops) → `Err(FdarError::InvalidDimension { parameter: "ar_ops"/"ma_ops", .. })`.
    - Test: argvals empty (m==0) → `Err(FdarError::InvalidDimension { parameter: "argvals" or "data", .. })`.
    - Test: a divergent AR operator (spectral radius ≥ 1, e.g. scaled identity ×2) → `Err(FdarError::ComputationFailed { operation: "sim_fvarma ..", .. })` via the NaN/Inf burn-in guard.
  </behavior>
  <action>
    Add entry-point validation to `sim_fvarma`: reject `m == 0`; reject any AR/MA kernel whose length != `m*m` with `InvalidDimension` naming the offending parameter. Inside the burn-in/generation loop, after computing each `x_new`, if any entry is non-finite return `FdarError::ComputationFailed { operation: "sim_fvarma burn-in", detail: "curve values diverged to NaN/Inf; ensure AR operators have spectral radius < 1".to_string() }`. Add the five inline tests in `<behavior>`. Compute lag-k Frobenius norms via `autocovariance_matrix` (call `super::fts::acf::autocovariance_matrix`) or a small inline `frobenius(flat_mxm)` helper. Do not weaken tolerances into vacuous assertions.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel simulation::tests::fvarma</automated>
  </verify>
  <acceptance_criteria>
    - Oracle-4 (zero-op ⇒ ‖C_1‖ < 0.15·‖C_0‖) and Oracle-6 (rank-1 ⇒ ‖C_1‖ > 0.1·‖C_0‖) tests pass with concrete numeric thresholds.
    - All three error-path tests assert the exact `FdarError` variant and `parameter`/`operation` field.
    - Divergence test triggers `ComputationFailed` (not a panic, not an Inf-filled result).
  </acceptance_criteria>
  <done>sim_fvarma validation, white-noise and serial-dependence oracles, and NaN-divergence guard are covered by passing tests (FTS-03-04 complete).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: sim_farma (combined AR+MA) + crate-root re-export</name>
  <files>fdars-core/src/simulation.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/simulation.rs (Task 1/2 output — sim_fvarma, FvarmaResult)
    - fdars-core/src/lib.rs (existing `pub use simulation::{EFunType, EValType};` line 235 — EXTEND, do not replace)
    - .planning/phases/41-spectral-functional-time-series/41-RESEARCH.md (Functional ARMA lines 359-374, Test Oracle 5 lines 723-725)
    - .planning/phases/41-spectral-functional-time-series/41-PATTERNS.md (FarmaResult struct lines 343-355, lib.rs re-export note lines 365-383)
  </read_first>
  <behavior>
    - Test: `sim_farma` with both non-empty AR and MA kernels returns curves of shape n×m, all finite; `ar_order`/`ma_order` match input lengths.
    - Test (Oracle 5): two `sim_farma` calls with the same seed → bit-identical `FarmaResult`.
    - Test: `sim_farma` and `sim_fvarma` with identical arguments + seed produce equal `curves` (shared recurrence), confirming FARMA = combined AR+MA.
  </behavior>
  <action>
    Define `FarmaResult` per PATTERNS.md lines 346-355 (same fields/derives as FvarmaResult). Implement `#[must_use] pub fn sim_farma(n, argvals, ar_ops, ma_ops, burn_in, seed) -> Result<FarmaResult, FdarError>` as a thin wrapper delegating to the shared `sim_fvarma` recurrence (call `sim_fvarma` internally and repackage into `FarmaResult`, or share a private core `fn fvarma_core(...)`), so both entry points are bit-identical for the same inputs. Add the three inline tests.

    In lib.rs, EXTEND the existing `pub use simulation::{EFunType, EValType};` (line 235) to also export `sim_fvarma, sim_farma, FvarmaResult, FarmaResult` — keep `EFunType, EValType`. Run `cargo fmt` on the touched files.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel simulation && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `sim_farma` tests pass: shape/order checks, seed-determinism (Oracle 5), and equality with `sim_fvarma` on identical inputs.
    - `fdars_core::sim_fvarma`, `fdars_core::sim_farma`, `fdars_core::FvarmaResult`, `fdars_core::FarmaResult` resolve at the crate root; `EFunType`/`EValType` remain exported.
    - Full `cargo clippy --all-targets --features linalg,parallel -- -D warnings` clean.
  </acceptance_criteria>
  <done>sim_farma delivers the combined AR+MA simulator, deterministic and shape-correct; all four simulation symbols re-exported at crate root; clippy clean (FTS-03-05 complete).</done>
  <reversibility rating="reversible">Additive re-export extension and thin wrapper; no existing signature touched.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → sim_fvarma/sim_farma | untrusted operator kernels (arbitrary norm/dimension), n, burn_in, argvals cross into a recurrence + index arithmetic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-05 | Denial of Service | non-stationary operator → geometric blow-up to NaN/Inf during burn-in | medium | mitigate | finiteness check each step → `ComputationFailed` (Pitfall 5); rustdoc states user owns spectral-radius<1 |
| T-41-06 | Tampering | kernel length != m*m → out-of-bounds index in matrix-vector product | high | mitigate | validate every `ar_ops`/`ma_ops` entry length == m*m at entry before any indexing |
| T-41-07 | Tampering | m == 0 / empty argvals → zero-size FdMatrix / div-by-zero in norms | medium | mitigate | reject m==0 at entry with InvalidDimension |
| T-41-08 | Repudiation | non-reproducible output undermines seeded-simulation contract | low | mitigate | single `StdRng::seed_from_u64(seed)`, no entropy fallback; determinism test asserts bit-identity |
</threat_model>

<verification>
- `cargo test -p fdars-core --features linalg,parallel simulation` — full simulation suite green (including new + existing sim_kl tests).
- `cargo clippy --all-targets --features linalg,parallel -- -D warnings` — clean (CI parity).
- `cargo fmt` clean before commit; `--no-verify` if /tmp doctest-link exhaustion occurs (MEMORY.md), whole-crate fmt sweep at phase end.
- `cargo doc --no-deps -p fdars-core` renders; stationarity + R-divergence notes present in simulator rustdoc.
</verification>

<success_criteria>
- FTS-03-04: `sim_fvarma` — seeded/deterministic; zero-op ⇒ white noise; rank-1 op ⇒ serial dependence; dim + NaN guards enforced.
- FTS-03-05: `sim_farma` — combined AR+MA, deterministic, shape-correct, equals sim_fvarma on identical inputs.
- All four simulation symbols re-exported at crate root; existing signatures unchanged; clippy + fmt clean.
</success_criteria>

<output>
Create `.planning/phases/41-spectral-functional-time-series/41-02-SUMMARY.md` when done.
</output>
