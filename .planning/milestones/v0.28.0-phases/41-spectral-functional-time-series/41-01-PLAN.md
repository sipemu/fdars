---
phase: 41-spectral-functional-time-series
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/fts/spectral.rs
  - fdars-core/src/fts/mod.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [FTS-03-01, FTS-03-02, FTS-03-03]
estimate:
  tokens: 92000
  raw_tokens: 46000
  tasks: 4
  confidence: med
must_haves:
  truths:
    - "spectral_density returns per-frequency m×m Hermitian operators; white-noise input yields a flat real spectrum ≈ C_0 (imaginary part ≈ 0) at every Fourier frequency (FTS-03-01)"
    - "dpca returns dynamic eigen-filters (shape (2L+1)×m per component) and dynamic scores (shape (N-2L)×ncomp), all finite and sign-aligned across frequencies (FTS-03-02)"
    - "dpca_reconstruct reconstruction error decreases monotonically as retained components K increase 1..ncomp; a rank-1 series reconstructs to <1e-4 error with K=1 (FTS-03-03)"
    - "spectral_density, dpca, dpca_reconstruct each return Result<_, FdarError>, validate inputs at entry, and error on empty data / argvals-length mismatch / out-of-range ncomp or filter_lag"
    - "new public symbols are re-exported from fts/mod.rs and crate-root lib.rs; existing fts re-export block is extended, not replaced"
  artifacts:
    - "fdars-core/src/fts/spectral.rs (new)"
    - "SpectralDensityResult, DpcaResult, DpcaReconstruction structs in fdars-core/src/fts/mod.rs"
  key_links:
    - "spectral.rs calls super::acf::autocovariance_matrix (pub(crate)) directly — no reimplementation"
    - "eigenvector sign-alignment before IFFT (largest-abs-entry positive) — prevents oscillatory filter taps"
    - "score/reconstruction operate only on valid interior t = L..N-1-L (valid_range) — no out-of-bounds curve access"
---

<objective>
Add frequency-domain functional time series analysis to `fdars-core` in a new `fts/spectral.rs` module: the spectral density operator estimator (FTS-03-01), dynamic functional PCA filters + scores (FTS-03-02), and curve reconstruction from dynamic scores with a monotone-decreasing error property (FTS-03-03). All additive/non-breaking, reusing the existing `rustfft`, `nalgebra`, and `fts/acf.rs` autocovariance machinery — no new crate dependency.

Purpose: closes ~9 absent Area-6 gaps vs the R `freqdom`/`ftsa` baseline; the highest-leverage frequency-domain FTS capability in the v0.28.0 backlog draw.
Output: `fts/spectral.rs` with `spectral_density`, `dpca`, `dpca_reconstruct`; `SpectralDensityResult`/`DpcaResult`/`DpcaReconstruction` structs in `fts/mod.rs`; crate-root re-exports; inline `#[cfg(test)]` oracle tests.
</objective>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Phase 41, both plans)

**New file:** `fdars-core/src/fts/spectral.rs`

**New public functions (all `Result<_, FdarError>`, `#[must_use]`):**
| Symbol | Plan | Location | Requirement |
|--------|------|----------|-------------|
| `spectral_density(data, argvals, bandwidth)` | 41-01 | `fts/spectral.rs` | FTS-03-01 |
| `dpca(data, argvals, ncomp, bandwidth, filter_lag)` | 41-01 | `fts/spectral.rs` | FTS-03-02 |
| `dpca_reconstruct(data, argvals, &dpca)` | 41-01 | `fts/spectral.rs` | FTS-03-03 |
| `sim_fvarma(n, argvals, ar_ops, ma_ops, burn_in, seed)` | 41-02 | `simulation.rs` | FTS-03-04 |
| `sim_farma(n, argvals, ar_ops, ma_ops, burn_in, seed)` | 41-02 | `simulation.rs` | FTS-03-05 |

**New public structs (derive `Debug, Clone, PartialEq`; serde-gated; `#[non_exhaustive]`):**
| Struct | Plan | Fields |
|--------|------|--------|
| `SpectralDensityResult` | 41-01 | `freqs: Vec<f64>`, `re: Vec<Vec<f64>>`, `im: Vec<Vec<f64>>`, `m: usize`, `n_curves: usize`, `bandwidth: usize` |
| `DpcaResult` | 41-01 | `filters: Vec<FdMatrix>`, `scores: FdMatrix`, `eigenvalues: Vec<Vec<f64>>`, `n_freqs: usize`, `filter_lag: usize`, `ncomp: usize`, `valid_range: (usize, usize)` |
| `DpcaReconstruction` | 41-01 | `fitted: FdMatrix`, `reconstruction_error: Vec<f64>`, `valid_range: (usize, usize)` |
| `FvarmaResult` | 41-02 | `curves: FdMatrix`, `ar_order: usize`, `ma_order: usize`, `burn_in: usize` |
| `FarmaResult` | 41-02 | `curves: FdMatrix`, `ar_order: usize`, `ma_order: usize`, `burn_in: usize` |

**Re-exports:** all ten symbols above re-exported from `fts/mod.rs` (spectral items) and at the crate root in `lib.rs` (extending the existing `pub use fts::{...}` and `pub use simulation::{...}` blocks — never replacing them).

**Edge-case predicates (test oracles, from RESEARCH.md):**
- White-noise spectrum is flat, real ≈ `C_0` (1/2π omitted), imaginary ≈ 0 at all Fourier frequencies.
- Rank-1 series reconstructs exactly (<1e-4) with K=1 dynamic component.
- Reconstruction error monotone non-increasing as retained components K grow.
- VAR(1) with zero AR operator ⇒ pure i.i.d. innovations (near-zero lag-1 ACF).
- Bit-identical simulator output for the same seed.
- Input-validation error paths: dimension mismatch, ncomp / filter_lag out of range, empty series, kernel-length ≠ m·m, NaN/Inf divergence ⇒ `ComputationFailed`.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-spectral-functional-time-series/41-CONTEXT.md
@.planning/phases/41-spectral-functional-time-series/41-RESEARCH.md
@.planning/phases/41-spectral-functional-time-series/41-PATTERNS.md
@.planning/phases/41-spectral-functional-time-series/41-VALIDATION.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end spectral pipeline skeleton — one path, white-noise verified</name>
  <files>fdars-core/src/fts/spectral.rs, fdars-core/src/fts/mod.rs</files>
  <read_first>
    - fdars-core/src/fts/acf.rs (validate_fts_input lines 25-42, mean_curve, autocovariance_matrix pub(crate) lines 73-95, bandwidth resolution + max_h guard lines 684-708)
    - fdars-core/src/fts/mod.rs (module head, mod/pub use pattern lines 20-26, FacfResult derive block lines 33-45)
    - fdars-core/src/metric/fourier.rs (FftPlanner + Complex import lines 7-8, forward-FFT pattern lines 13-36)
    - .planning/phases/41-spectral-functional-time-series/41-PATTERNS.md (spectral.rs pattern section, lines 22-151)
    - .planning/phases/41-spectral-functional-time-series/41-RESEARCH.md (Spectral Density Estimator section lines 184-231, Code Examples lines 553-580)
  </read_first>
  <action>
    Create `fdars-core/src/fts/spectral.rs`. Copy `validate_fts_input(data, argvals) -> Result<(usize, usize), FdarError>` verbatim from acf.rs (do NOT promote acf's visibility — forecast.rs re-implements it locally; follow that). Add `mod spectral;` to fts/mod.rs after `mod forecast;`, and a minimal `pub use spectral::spectral_density;`.

    Define `SpectralDensityResult` in fts/mod.rs with the derive/serde/`#[non_exhaustive]` block from PATTERNS.md lines 191-206: fields `freqs: Vec<f64>`, `re: Vec<Vec<f64>>`, `im: Vec<Vec<f64>>`, `m: usize`, `n_curves: usize`, `bandwidth: usize`. `re[k]`/`im[k]` are flat column-major m×m matrices for Fourier frequency k; element (j1,j2) at index `j1 + j2*m`.

    Implement `spectral_density(data: &FdMatrix, argvals: &[f64], bandwidth: Option<usize>) -> Result<SpectralDensityResult, FdarError>` marked `#[must_use = "..."]`. Wire ONE full path only: validate → resolve bandwidth (`None => (n as f64).cbrt().floor() as usize`) then `let max_h = resolved_bandwidth.min(n - 1)` guard → compute `xbar = mean_curve` and lag operators via `super::acf::autocovariance_matrix(data, &xbar, h, n, m)` for h=0..=max_h → for each (j1,j2) entry build a length-N complex buffer with Bartlett weights `w_h = 1.0 - (h as f64)/(resolved_bandwidth as f64)`, positive lags at index h, negative lags at index `N-h` using `C_{-h}[j1,j2] = C_h[j2,j1]` → forward FFT (one `FftPlanner` created outside all loops, `plan_fft_forward(N)` reused) → scatter real/imag into `re[k][j1+j2*m]` / `im[k][j1+j2*m]`. Set `freqs[j] = 2.0*std::f64::consts::PI*(j as f64)/(n as f64)`. Omit the 1/2π pre-factor (matches `long_run_covariance` which does not divide by 2π); document this divergence in rustdoc. Also document the Re-only-eigendecomposition divergence stub for later tasks.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel fts::spectral::tests::tracer_white_noise_flat</automated>
  </verify>
  <acceptance_criteria>
    - `fts/spectral.rs` exists; `cargo build -p fdars-core --features linalg,parallel` compiles.
    - An inline test `tracer_white_noise_flat` builds i.i.d. white-noise curves (e.g. via simulation::sim_kl with no serial dependence, or a seeded normal FdMatrix), calls `spectral_density`, and asserts: for every frequency k the real matrix `re[k]` is within 1e-6-scaled tolerance of `re[0]` (flat spectrum) and every `im[k]` entry has abs < 1e-6.
    - Test also asserts `re[0]` (DC) ≈ C_0 from `autocovariance_matrix(.., 0, ..)` within tolerance.
    - `freqs.len() == n`, `re.len() == n`, `re[0].len() == m*m`.
  </acceptance_criteria>
  <done>The full spectral-density path runs end-to-end and the white-noise flatness oracle (FTS-03-01) passes.</done>
  <reversibility rating="reversible">Additive new module; struct field layout can change before release since API is unpublished.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: spectral_density error paths + Hermitian-structure test</name>
  <files>fdars-core/src/fts/spectral.rs</files>
  <read_first>
    - fdars-core/src/fts/spectral.rs (Task 1 output)
    - fdars-core/src/fts/acf.rs (functional_acf error-path tests for the InvalidDimension message shape)
    - .planning/phases/41-spectral-functional-time-series/41-RESEARCH.md (Pitfall 1 lines 483-491, Test Oracle 1 lines 701-706)
  </read_first>
  <behavior>
    - Test: empty data (0 rows or 0 cols) → `Err(FdarError::InvalidDimension { parameter: "data", .. })`.
    - Test: argvals length != m → `Err(FdarError::InvalidDimension { parameter: "argvals", .. })`.
    - Test: `spectral_density` on the same deterministically-constructed data called twice returns equal `SpectralDensityResult` (PartialEq) — no hidden RNG.
    - Test: Hermitian symmetry — for a serially dependent series, `im[k][j1+j2*m] == -im[k][j2+j1*m]` within 1e-9 for a sampled (j1,j2).
  </behavior>
  <action>
    Add the input-validation branches (already routed through `validate_fts_input`) plus a bandwidth-range guard: if a user passes `Some(0)` bandwidth, return `FdarError::InvalidParameter { parameter: "bandwidth", message: "must be >= 1".to_string() }`. Add the four inline tests described in `<behavior>`. Do NOT weaken any assertion. Ensure the negative-lag scatter uses `C_h[j2 + j1*m]` (transpose) so the Hermitian relation holds.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel fts::spectral::tests::spectral_density</automated>
  </verify>
  <acceptance_criteria>
    - All four `<behavior>` tests pass (test names contain `spectral_density`).
    - Error-path tests assert the exact `FdarError` variant and `parameter` field value.
    - Determinism test uses `assert_eq!` on two `SpectralDensityResult` values.
  </acceptance_criteria>
  <done>spectral_density input validation, determinism, and Hermitian structure are covered by passing inline tests (FTS-03-01 complete).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: dpca — dynamic eigen-filters + scores</name>
  <files>fdars-core/src/fts/spectral.rs, fdars-core/src/fts/mod.rs</files>
  <read_first>
    - fdars-core/src/fts/spectral.rs (Task 1/2 output — spectral_density)
    - fdars-core/src/fts/acf.rs (SymmetricEigen pattern lines 337-348)
    - .planning/phases/41-spectral-functional-time-series/41-RESEARCH.md (Dynamic FPCA Filters lines 233-262, Scores lines 264-281, Pitfall 2 sign-alignment lines 493-507, Pitfall 4 boundary lines 519-527, Code Examples lines 582-641)
    - .planning/phases/41-spectral-functional-time-series/41-PATTERNS.md (DpcaResult layout lines 208-229, sign-alignment lines 114-124, Simpson weights lines 126-130)
  </read_first>
  <behavior>
    - Test: dpca on a small seeded series returns `filters.len() == ncomp`, each filter FdMatrix of shape (2L+1)×m; `scores` shape (N-2L)×ncomp; `eigenvalues.len() == ncomp` with each inner vec length n_freqs; `valid_range == (L, N-1-L)`. All values finite.
    - Test: white-noise input → per-frequency leading eigenvalues are approximately equal across frequencies (flat spectrum), within a loose tolerance.
    - Test: ncomp > m or ncomp == 0 → `Err(FdarError::InvalidParameter { parameter: "ncomp", .. })`; filter_lag >= n/2 → `Err(FdarError::InvalidParameter { parameter: "filter_lag", .. })`.
  </behavior>
  <action>
    Define `DpcaResult` in fts/mod.rs per PATTERNS.md lines 211-229: `filters: Vec<FdMatrix>` ((2L+1)×m, row l = lag l-L), `scores: FdMatrix` ((N-2L)×ncomp), `eigenvalues: Vec<Vec<f64>>` (ncomp × n_freqs), `n_freqs`, `filter_lag`, `ncomp`, `valid_range: (usize, usize)`. Add `dpca` to the `pub use spectral::{...}` line.

    Implement `dpca(data: &FdMatrix, argvals: &[f64], ncomp: usize, bandwidth: Option<usize>, filter_lag: Option<usize>) -> Result<DpcaResult, FdarError>`, `#[must_use]`. Default `filter_lag L = resolved_bandwidth` (document). Steps: call `spectral_density` → per frequency k, form `Re(f̂(θ_k))` as an m×m DMatrix, defensively symmetrize, `nalgebra::SymmetricEigen::new`, sort eigenpairs descending, take top `ncomp`, sign-align each eigenvector so its largest-abs entry is positive (Pitfall 2) → store `eigenvalues[c][k]`, clip negative eigenvalues to 0 for any reported variance but keep eigenvectors for filters (Pitfall 3) → for each component c and grid row j, assemble the length-N sequence `V_c_j = [v_c(θ_0)[j], .., v_c(θ_{N-1})[j]]`, inverse-FFT (`plan_fft_inverse(n)`, divide by n), take real parts for lags 0..=L with `a_c[j,-l]=a_c[j,l]`, and store into `filters[c]` as an FdMatrix ((2L+1)×m). Compute scores by time-domain convolution over the interior t=L..N-1-L: `s_c[t] = Σ_{l=-L}^{L} Σ_j filters[c][(l+L), j] * data[(t+l, j)] * weights[j]` where `weights = simpsons_weights(argvals)`. Set `valid_range = (L, n-1-L)`. Validate ncomp in 1..=m and filter_lag < n/2 at entry.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel fts::spectral::tests::dpca</automated>
  </verify>
  <acceptance_criteria>
    - `dpca` tests (names contain `dpca`, excluding reconstruct) pass: shape checks, finiteness, flat-eigenvalue white-noise check, and the two parameter-range error paths.
    - `filters[c]` has exactly `2*filter_lag + 1` rows and `m` columns; `scores` has `n - 2*filter_lag` rows and `ncomp` columns.
    - `valid_range == (filter_lag, n - 1 - filter_lag)`.
  </acceptance_criteria>
  <done>dpca returns correctly-shaped, finite, sign-aligned filters and scores with validated parameters (FTS-03-02 complete).</done>
  <reversibility rating="reversible">Filter/score layout is unpublished; the Re-only eigendecomposition is a documented approximation, swappable for the 2m embedding later without signature change.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 4: dpca_reconstruct + monotone-error / rank-1 oracles + crate-root re-export</name>
  <files>fdars-core/src/fts/spectral.rs, fdars-core/src/fts/mod.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/fts/spectral.rs (Task 3 output — dpca)
    - fdars-core/src/lib.rs (existing `pub use fts::{...}` block lines 259-264 — EXTEND, do not replace)
    - .planning/phases/41-spectral-functional-time-series/41-RESEARCH.md (Reconstruction lines 282-311, Test Oracles 2 & 3 lines 708-717)
    - .planning/phases/41-spectral-functional-time-series/41-PATTERNS.md (DpcaReconstruction struct lines 231-242, lib.rs re-export lines 365-383)
  </read_first>
  <behavior>
    - Test (Oracle 3, primary): on a seeded Gaussian AR(1)-driven series (N>=60, m~20, L small), `dpca_reconstruct` for K=1..ncomp yields `reconstruction_error[k] >= reconstruction_error[k+1]` for all k (monotone non-increasing, within a small numerical slack).
    - Test (Oracle 2): a rank-1 series `X_t[j] = a_t * phi[j]` with `a_t = 0.8 a_{t-1} + eps` reconstructs with K=1 to error < 1e-4 over the valid interior.
    - Test: `fitted` has shape (N-2L)×m; `valid_range` matches the DpcaResult valid_range.
  </behavior>
  <action>
    Define `DpcaReconstruction` in fts/mod.rs per PATTERNS.md lines 234-242: `fitted: FdMatrix` ((N-2L)×m), `reconstruction_error: Vec<f64>` (length ncomp, cumulative), `valid_range: (usize, usize)`. Add `dpca_reconstruct` and the three structs to the `pub use spectral::{...}` line, and define the structs' `pub use` in mod.rs.

    Implement `dpca_reconstruct(data: &FdMatrix, argvals: &[f64], dpca: &DpcaResult) -> Result<DpcaReconstruction, FdarError>`, `#[must_use]`. For each retained-component count K in 1..=ncomp, reconstruct over the interior: `X̂[t,j] = Σ_{c=0}^{K-1} Σ_{l=-L}^{L} dpca.filters[c][(l+L), j] * dpca.scores[(t-L)+l, c]` (adjoint = symmetric real filter), and compute integrated-L2 error `error(K) = (1/N') Σ_t Σ_j (X[t,j]-X̂_K[t,j])² * weights[j]` with `weights = simpsons_weights(argvals)`, N' = interior length. Store `reconstruction_error[K-1] = error(K)`; set `fitted` to the full-K reconstruction; `valid_range = dpca.valid_range`. Validate argvals length matches m and dpca dimensions are consistent; error on mismatch.

    In lib.rs, EXTEND the existing `pub use fts::{...}` block (lines 259-264) by appending `dpca, dpca_reconstruct, spectral_density, DpcaReconstruction, DpcaResult, SpectralDensityResult` — do not remove existing entries. Add the module-level and function-level rustdoc divergence notes (Re-only Hermitian eigendecomposition, 1/2π omission, score trimming) to `fts/spectral.rs`.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel fts::spectral && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Oracle-2 (rank-1, K=1 error < 1e-4) and Oracle-3 (monotone non-increasing error in K) tests pass.
    - `fdars_core::dpca`, `fdars_core::dpca_reconstruct`, `fdars_core::spectral_density`, `fdars_core::SpectralDensityResult`, `fdars_core::DpcaResult`, `fdars_core::DpcaReconstruction` resolve at the crate root (a doctest or the re-export line compiling proves it).
    - Full `cargo clippy --all-targets --features linalg,parallel -- -D warnings` is clean (CI lints test/bench code).
    - Existing `pub use fts::{...}` entries (functional_acf, ftsm, …) remain present.
  </acceptance_criteria>
  <done>dpca_reconstruct delivers monotone-decreasing error + exact rank-1 K=1 reconstruction; all new symbols re-exported at crate root; clippy clean (FTS-03-03 complete).</done>
  <reversibility rating="reversible">Additive re-export extension; documented approximations swappable pre-release.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → spectral_density/dpca/dpca_reconstruct | untrusted numeric input (data shape, argvals, bandwidth, ncomp, filter_lag) crosses into index arithmetic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-01 | Tampering | usize underflow in `n - h` / `n - 1 - L` interior bounds | medium | mitigate | `max_h = bandwidth.min(n-1)` guard (Pitfall 1); validate `filter_lag < n/2`; interior loop bounds checked before access |
| T-41-02 | Denial of Service | negative/indefinite spectral operator → NaN in variance | low | mitigate | clip negative eigenvalues to 0 for reported variance; keep eigenvectors for filters (Pitfall 3) |
| T-41-03 | Reliability | FFT planner `.plan_fft_*` on valid sizes | low | accept | sizes derive from n>0 (validated); planner never fails for n>=1 |
| T-41-04 | Tampering | out-of-bounds curve access during convolution at boundaries | medium | mitigate | scores/reconstruction only touch t ∈ [L, N-1-L]; `valid_range` returned |
</threat_model>

<verification>
- `cargo test -p fdars-core --features linalg,parallel fts::spectral` — full spectral module suite green.
- `cargo clippy --all-targets --features linalg,parallel -- -D warnings` — clean (CI parity).
- `cargo fmt` clean before commit; commit with `--no-verify` if /tmp doctest-link exhaustion occurs (MEMORY.md), then a whole-crate fmt sweep at phase end.
- `cargo doc --no-deps -p fdars-core` renders; divergence notes present in spectral.rs rustdoc.
</verification>

<success_criteria>
- FTS-03-01: `spectral_density` returns per-frequency Hermitian operators; white-noise oracle (flat, imaginary≈0) passes.
- FTS-03-02: `dpca` returns finite, correctly-shaped, sign-aligned filters + scores; parameter-range errors enforced.
- FTS-03-03: `dpca_reconstruct` error monotone-decreasing in K; rank-1 exact with K=1.
- All new symbols re-exported at crate root; existing signatures unchanged; clippy + fmt clean.
</success_criteria>

<output>
Create `.planning/phases/41-spectral-functional-time-series/41-01-SUMMARY.md` when done.
</output>
