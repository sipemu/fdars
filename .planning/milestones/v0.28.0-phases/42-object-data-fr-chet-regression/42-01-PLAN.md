---
phase: 42-object-data-fr-chet-regression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/frechet/spaces/mod.rs
  - fdars-core/src/frechet/spaces/spd.rs
  - fdars-core/src/frechet/spaces/correlation.rs
  - fdars-core/src/frechet/mod.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [FRE-02-01, FRE-02-02]
estimate:
  tokens: 62000
  raw_tokens: 34000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "SpdMatrixSpace::new(d, SpdMetric::Frobenius) then distance/weighted_frechet_mean returns numeric results with no panic (FRE-02-01)."
    - "SpdMetric::Power(1.0) distance equals SpdMetric::Frobenius distance element-for-element within 1e-10 (FRE-02-01)."
    - "SpdMetric::LogCholesky weighted mean of I and 4I (equal weights) in 2×2 equals 2I within 1e-8 (FRE-02-01)."
    - "Frobenius weighted mean of identical SPD matrices recovers that matrix within 1e-10 (FRE-02-01)."
    - "CorrelationMatrixSpace weighted mean of identical correlation matrices recovers that matrix; result has unit diagonal within 1e-10 (FRE-02-02)."
    - "distance of any object from itself is 0 within 1e-12 for both spaces."
    - "dimension mismatch, empty objects, weight-length mismatch, alpha<=0, and non-unit-diagonal/degenerate-diagonal inputs return FdarError (never panic)."
  artifacts:
    - fdars-core/src/frechet/spaces/mod.rs
    - fdars-core/src/frechet/spaces/spd.rs
    - fdars-core/src/frechet/spaces/correlation.rs
  key_links:
    - "frechet/mod.rs declares `mod spaces;` and re-exports SpdMatrixSpace, SpdMetric, CorrelationMatrixSpace."
    - "src/lib.rs `pub use frechet::{...}` block extended (not replaced) with SpdMatrixSpace, SpdMetric, CorrelationMatrixSpace."
    - "SpdMatrixSpace + CorrelationMatrixSpace each `impl MetricSpace` with `type Object = Vec<f64>`."
---

<objective>
Establish the `frechet/spaces/` submodule and deliver the two matrix-valued `MetricSpace`
backends: `SpdMatrixSpace` (with the `SpdMetric { Frobenius, Power(f64), LogCholesky }` enum)
and `CorrelationMatrixSpace`. This is the tracer-bearing plan that wires the new module into
`frechet/mod.rs` and the crate root — it OWNS all re-export edits for the phase so later plans
add only their own `pub use` lines without colliding on the same block.

Purpose: FRE-02-01 (SPD covariance-matrix response space under Frobenius / Power-α /
Log-Cholesky metrics) and FRE-02-02 (correlation-matrix response space). These are the
richest backends and prove the whole `spaces/` pattern end-to-end.

Output: `frechet/spaces/mod.rs`, `frechet/spaces/spd.rs`, `frechet/spaces/correlation.rs`;
extended re-exports in `frechet/mod.rs` and `src/lib.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-object-data-fr-chet-regression/42-RESEARCH.md
@.planning/phases/42-object-data-fr-chet-regression/42-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan produces: `frechet/spaces/mod.rs`, `SpdMatrixSpace`, `SpdMetric`,
`CorrelationMatrixSpace`. It also creates the `mod spaces;` declaration and the crate-root
re-export block that plans 42-02 and 42-03 extend. Later phase symbols (SphericalSpace,
NetworkSpace, PointProcessSpace, frechet_global_reg_space, frechet_local_reg_space,
frechet_anova_space, compute_global_weights, compute_local_weights, compute_tn_generic) are
produced by plans 42-02 and 42-03.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Module skeleton + SPD Frobenius backend end-to-end</name>
  <files>fdars-core/src/frechet/spaces/mod.rs, fdars-core/src/frechet/spaces/spd.rs, fdars-core/src/frechet/mod.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/frechet/space.rs (lines 24-121): `MetricSpace` trait; `WassersteinDensitySpace` struct + `new()` validation + `impl MetricSpace` — the exact analog to mirror (PATTERNS.md §spaces backends, lines 32-99).
    - fdars-core/src/frechet/mod.rs (lines 34-42): existing `mod`/`pub use` block to extend (PATTERNS.md §frechet/mod.rs, lines 446-486).
    - fdars-core/src/lib.rs (lines 152-156): existing `pub use frechet::{...}` block to EXTEND not replace (PATTERNS.md §src/lib.rs, lines 490-515).
    - 42-PATTERNS.md §"frechet/spaces/mod.rs" (lines 425-442): barrel re-export pattern, explicit `pub use` per item, no wildcards.
    - 42-RESEARCH.md §"SPD Matrix Space" Frobenius subsection (lines 181-187) and §"Module Re-Export Plan" (lines 693-726).
  </read_first>
  <action>
    Create `frechet/spaces/mod.rs` declaring `mod spd;` and `pub use spd::{SpdMatrixSpace, SpdMetric};` (explicit, no wildcard — add correlation in Task 3). In `frechet/mod.rs` add `mod spaces;` after the existing `mod space;` line and add `pub use spaces::{SpdMatrixSpace, SpdMetric};`. In `src/lib.rs` EXTEND the existing `pub use frechet::{...}` list (do not replace it) by appending `SpdMatrixSpace, SpdMetric`.
    Create `frechet/spaces/spd.rs` with `pub enum SpdMetric { Frobenius, Power(f64), LogCholesky }` deriving `Debug, Clone, PartialEq` + serde-gate via `#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]` (no `#[non_exhaustive]` — it is not a result struct). Define `pub struct SpdMatrixSpace { pub d: usize, pub metric: SpdMetric }` with the same derive + serde-gate. `SpdMatrixSpace::new(d: usize, metric: SpdMetric) -> Result<Self, FdarError>` validates `d >= 1` (else `InvalidParameter { parameter: "d", ... }`) and for `SpdMetric::Power(alpha)` validates `alpha > 0.0` (else `InvalidParameter { parameter: "alpha", ... }`).
    Implement `impl MetricSpace for SpdMatrixSpace { type Object = Vec<f64>; ... }`. This tracer wires ONLY the Frobenius path end-to-end: `distance` computes the element-wise Frobenius norm `sqrt(Σ (aᵢ − bᵢ)²)` over the flat `d*d` vectors after validating `a.len() == d*d` and `b.len() == d*d` (else `InvalidDimension`); `weighted_frechet_mean` validates non-empty `objects`, `weights.len() == objects.len()`, and each object length `== d*d`, then returns the element-wise weighted average `M = (Σ wᵢ aᵢ) / Σ wᵢ` (guard `Σ wᵢ.abs() < NUMERICAL_EPS` → `ComputationFailed`). Power and LogCholesky arms return an explicit `unimplemented`-free stub that dispatches to internal helpers `spd_power_mean`/`log_cholesky_mean` added in Task 2 — for THIS task, route both non-Frobenius arms to the Frobenius fallback so the file compiles and the tracer is green; Task 2 fills the real arms. Import `crate::error::FdarError`, `crate::frechet::space::MetricSpace`, `crate::helpers::NUMERICAL_EPS`.
    Rustdoc the module and `SpdMetric` noting the Frobenius/Power-α/Log-Cholesky geometry and that this diverges from R `frechet` 0.3.0's affine-invariant SPD geometry (document per 42-RESEARCH.md lines 187, 212).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel spd_ 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "mod spaces" fdars-core/src/frechet/mod.rs` returns a match; `grep -n "SpdMatrixSpace" fdars-core/src/lib.rs` returns a match inside the `pub use frechet::{...}` block.
    - Inline test `spd_frobenius_distance_of_identical_is_zero` passes (distance of a matrix from itself < 1e-12).
    - Inline test `spd_frobenius_mean_of_identical_recovers_matrix` passes (weighted mean of identical 2×2 SPD matrices equals input within 1e-10).
    - Inline test `spd_rejects_dimension_mismatch` asserts `distance` on wrong-length object returns `FdarError::InvalidDimension`.
    - Inline test `spd_new_rejects_zero_dim` asserts `SpdMatrixSpace::new(0, SpdMetric::Frobenius)` returns `FdarError::InvalidParameter`, and `SpdMatrixSpace::new(2, SpdMetric::Power(0.0))` returns `FdarError::InvalidParameter`.
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel spd_ 2>&1 | tail -8` shows 0 failures.
  </acceptance_criteria>
  <done>The `frechet/spaces/` module exists, is wired into `frechet/mod.rs` + `src/lib.rs`, and `SpdMatrixSpace` under `SpdMetric::Frobenius` supports distance + weighted mean end-to-end with passing inline tests.</done>
  <reversibility rating="reversible">Additive new module + additive re-export lines on an unpublished API; trivially removable.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SPD Power-α and Log-Cholesky metrics</name>
  <files>fdars-core/src/frechet/spaces/spd.rs</files>
  <read_first>
    - fdars-core/src/frechet/spaces/spd.rs (the file from Task 1 — the arms to fill).
    - fdars-core/src/fts/acf.rs (lines 337-348): `DMatrix::from_column_slice` + defensive symmetrize + `SymmetricEigen::new` + eigen reconstruction — the VERIFIED matrix-power pattern (PATTERNS.md §SymmetricEigen, lines 101-131; 42-RESEARCH.md code example lines 516-547).
    - fdars-core/src/linalg.rs (lines 85-108): `cholesky_factor(a: &[f64], p: usize) -> Result<Vec<f64>, FdarError>` — row-major L, element (i,j) at `l[i*d + j]`, `ComputationFailed` on non-PD (PATTERNS.md §Cholesky, lines 133-141).
    - 42-RESEARCH.md §"Power-α metric" (lines 189-213) and §"Log-Cholesky metric" (lines 214-242): exact formulas, the α=1≡Frobenius oracle, and the log-Cholesky 2×2 = 2I oracle (lines 642-646).
    - 42-RESEARCH.md §"Common Pitfalls" 1-3 (lines 462-482): column-major reconstruction, ascending eigenvalue order (no sort needed), exp() the diagonal on log-Cholesky back-map.
  </read_first>
  <behavior>
    - Test `spd_power_alpha_one_equals_frobenius`: for two distinct 2×2 SPD matrices, `SpdMetric::Power(1.0)` distance equals `SpdMetric::Frobenius` distance within 1e-10.
    - Test `spd_power_alpha_mean_of_identical_recovers`: Power(0.5) weighted mean of identical SPD matrices recovers the matrix within 1e-8.
    - Test `spd_log_cholesky_mean_identity_and_4i_is_2i`: equal-weight LogCholesky mean of `[1,0,0,1]` and `[4,0,0,4]` (2×2, column-major) equals `[2,0,0,2]` within 1e-8.
    - Test `spd_log_cholesky_mean_of_identical_recovers`: LogCholesky mean of two identical SPD matrices recovers the matrix within 1e-10.
    - Test `spd_log_cholesky_rejects_non_pd`: LogCholesky distance/mean on a non-PD input propagates `FdarError::ComputationFailed`.
  </behavior>
  <action>
    Add private helper `spd_power(mat_flat: &[f64], d: usize, alpha: f64) -> Vec<f64>` building `DMatrix::from_column_slice(d, d, mat_flat)`, defensively symmetrizing off-diagonal pairs, running `nalgebra::SymmetricEigen::new`, clamping each eigenvalue to `.max(0.0)` before `.powf(alpha)`, and reconstructing `result[i + j*d] += evecs[(i,k)] * lk_alpha * evecs[(j,k)]` (column-major; eigenvalues are ascending — no sort). Wire the `SpdMetric::Power(alpha)` distance arm to `‖spd_power(a,α) − spd_power(b,α)‖_F / α`, and the Power mean arm to `spd_power( element-wise weighted average of spd_power(aᵢ,α) , d, 1.0/α)` (weighted average with the same `Σ wᵢ` normalization + guard as Frobenius; clamp negative eigenvalues to 0 in the `1/α` back-map via the same `spd_power` helper).
    Add private helpers `log_cholesky_coords(mat_flat, d) -> Result<Vec<f64>, FdarError>` and `log_cholesky_reconstruct(coords, d) -> Vec<f64>`. Coords: call `crate::linalg::cholesky_factor(mat_flat, d)?` (propagate `ComputationFailed`), pack a length-`d*(d+1)/2` vector = strictly-lower entries `l[i*d + j]` for `i>j` (row-major within the lower triangle) followed by diagonal entries `l[i*d + i].ln()`. Reconstruct: unpack strictly-lower directly, diagonal via `.exp()`, form `L̄`, then `M[i + k*d] = Σⱼ L̄[i][j]·L̄[k][j]` (lower-triangular multiply, column-major output). Wire `SpdMetric::LogCholesky` distance = Euclidean norm between the two coordinate vectors; mean = `log_cholesky_reconstruct( Σ wᵢ · log_cholesky_coords(aᵢ) )` after the `Σ wᵢ` normalization guard.
    Update the module/enum rustdoc: document the log-Cholesky coordinate convention chosen (strictly-lower as-is, diagonal as log) and the α→0 note (users pick LogCholesky for the log-Euclidean analog) per 42-RESEARCH.md lines 212, 216.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel spd_ 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - The five `<behavior>` tests exist as `#[test]` fns in `spd.rs` and pass.
    - `grep -n "SymmetricEigen" fdars-core/src/frechet/spaces/spd.rs` returns a match; `grep -n "cholesky_factor" fdars-core/src/frechet/spaces/spd.rs` returns a match.
    - No fenced `unimplemented!`/`todo!` remain: `grep -nE "unimplemented!|todo!" fdars-core/src/frechet/spaces/spd.rs` returns nothing.
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel spd_ 2>&1 | tail -10` shows 0 failures.
  </acceptance_criteria>
  <done>All three SPD metrics (Frobenius, Power-α, Log-Cholesky) are implemented with passing hand-computed oracle tests, including α=1≡Frobenius and log-Cholesky(I,4I)=2I.</done>
  <reversibility rating="reversible">Fills stubbed arms in an unpublished new file.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Correlation-matrix backend</name>
  <files>fdars-core/src/frechet/spaces/correlation.rs, fdars-core/src/frechet/spaces/mod.rs, fdars-core/src/frechet/mod.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/frechet/space.rs (lines 51-121): struct + `new()` + `impl MetricSpace` analog (PATTERNS.md lines 32-99).
    - fdars-core/src/frechet/spaces/mod.rs (from Task 1): the barrel to extend.
    - fdars-core/src/frechet/mod.rs and fdars-core/src/lib.rs re-export blocks (from Task 1).
    - 42-RESEARCH.md §"Correlation Matrix Space" (lines 245-259): Frobenius distance, weighted average + unit-diagonal renormalization, degenerate-diagonal guard, R divergence note; §"Common Pitfalls" 6 (lines 496-500).
  </read_first>
  <behavior>
    - Test `correlation_distance_of_identical_is_zero`: distance of a correlation matrix from itself < 1e-12.
    - Test `correlation_mean_of_identical_recovers`: weighted mean of identical correlation matrices recovers the matrix within 1e-10.
    - Test `correlation_mean_has_unit_diagonal`: weighted mean of two distinct valid 2×2 correlation matrices has diagonal entries equal to 1.0 within 1e-10.
    - Test `correlation_rejects_non_positive_diagonal`: mean whose element-wise average yields a non-positive diagonal entry returns `FdarError::ComputationFailed`.
    - Test `correlation_rejects_dimension_mismatch`: distance on wrong-length object returns `FdarError::InvalidDimension`.
  </behavior>
  <action>
    Create `frechet/spaces/correlation.rs` with `pub struct CorrelationMatrixSpace { pub d: usize }` (derive `Debug, Clone, PartialEq` + serde-gate, no `#[non_exhaustive]`). `CorrelationMatrixSpace::new(d) -> Result<Self, FdarError>` validates `d >= 1`. `impl MetricSpace` with `type Object = Vec<f64>`: `distance` = element-wise Frobenius norm over `d*d` flat vectors after dimension validation; `weighted_frechet_mean` validates non-empty objects, weight-length match, and per-object length, computes the element-wise weighted average `M = Σ wᵢ aᵢ` (with `Σ wᵢ` normalization + `NUMERICAL_EPS` guard), then renormalizes to unit diagonal: guard every `M[i + i*d] > 0` (else `ComputationFailed { operation: "CorrelationMatrixSpace::weighted_frechet_mean", ... }`), and set `M_corr[i + j*d] = M[i + j*d] / (M[i+i*d]*M[j+j*d]).sqrt()`.
    Rustdoc: document the Frobenius + unit-diagonal-renormalization projection and that R `frechet` 0.3.0 may use a correlation-manifold geometry — this is a documented divergence (42-RESEARCH.md lines 258-259).
    Extend the re-exports (append only, do not rewrite the existing lines): `frechet/spaces/mod.rs` add `mod correlation;` + `pub use correlation::CorrelationMatrixSpace;`; `frechet/mod.rs` add `CorrelationMatrixSpace` to the `pub use spaces::{...}` list; `src/lib.rs` append `CorrelationMatrixSpace` to the `pub use frechet::{...}` block.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel correlation 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - The five `<behavior>` tests exist and pass.
    - `grep -n "CorrelationMatrixSpace" fdars-core/src/lib.rs` returns a match inside the `pub use frechet::{...}` block.
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel correlation 2>&1 | tail -8` shows 0 failures.
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy -p fdars-core --all-targets --features linalg,parallel -- -D warnings 2>&1 | tail -5` is clean for the touched files.
  </acceptance_criteria>
  <done>`CorrelationMatrixSpace` implements Frobenius distance + unit-diagonal-renormalized weighted mean with passing oracle + guard tests, and is re-exported from `frechet/mod.rs` and the crate root.</done>
  <reversibility rating="reversible">Additive new file + append-only re-exports on unpublished API.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → SpdMatrixSpace/CorrelationMatrixSpace public fns | Untrusted numeric matrix data (flat `Vec<f64>`, dimension `d`) crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-01 | Tampering | `spd_power` eigenvalue path on malformed (non-PSD) SPD input | medium | mitigate | Clamp eigenvalues to `.max(0.0)` before `powf`; validate `alpha > 0` on construction |
| T-42-02 | Tampering | Log-Cholesky on non-PD input | medium | mitigate | `cholesky_factor` returns `ComputationFailed` on non-PD — propagate, never panic |
| T-42-03 | Tampering | Correlation renormalization divides by `sqrt(M[i,i]·M[j,j])` | medium | mitigate | Guard all diagonal entries `> 0` before dividing; else `ComputationFailed` |
| T-42-04 | Denial of Service | Empty `objects` / weight-length mismatch | low | mitigate | `InvalidDimension` at function entry; never index into empty slice |
| T-42-05 | Tampering | Integer overflow in `i + j*d` index arithmetic | low | accept | Rust debug-mode overflow panics + safe `Vec` indexing; `d >= 1` validated, no realistic overflow for library-scale `d` |
| T-42-SC | Tampering | npm/pip/cargo installs | high | accept | No new dependencies added (RESEARCH Package Legitimacy Audit: none); reuses existing `nalgebra` 0.33 |
</threat_model>

<verification>
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel spd_ correlation 2>&1 | tail -12` — all backend tests green.
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings 2>&1 | tail -5` — clean.
- `cargo fmt -p fdars-core` run before each commit (avoid fmt drift per MEMORY.md).
</verification>

<success_criteria>
- FRE-02-01: `SpdMatrixSpace` supports all three metrics with passing α=1≡Frobenius and log-Cholesky(I,4I)=2I oracles.
- FRE-02-02: `CorrelationMatrixSpace` supports Frobenius distance + unit-diagonal-renormalized mean.
- Both spaces re-exported from `frechet/mod.rs` and crate root; existing re-exports untouched.
- No existing public signature changed.
</success_criteria>

<output>
Create `.planning/phases/42-object-data-fr-chet-regression/42-01-SUMMARY.md` when done.
</output>
