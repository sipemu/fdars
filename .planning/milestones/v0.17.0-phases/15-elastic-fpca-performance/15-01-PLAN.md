---
phase: 15-elastic-fpca-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/elastic_fpca.rs
autonomous: true
requirements:
  - PERF-04
estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 4
  confidence: med

must_haves:
  truths:
    - "shooting_vectors_from_psis (:701 loop) iterates via iter_maybe_parallel!(0..n), collecting owned per-row Vecs then assigning sequentially into the column-major FdMatrix (PERF-04-A)"
    - "build_augmented_srsfs (:720 loop) iterates via iter_maybe_parallel!(0..n), collecting owned per-row Vecs then assigning sequentially (PERF-04-B)"
    - "svd_scores_and_eigenvalues (:764 loop) uses iter_maybe_parallel! guarded by an N >= 50 named threshold constant; below threshold it runs the existing sequential path (PERF-04-C)"
    - "vert_fpca scores + eigenvalues are numerically equivalent (bit-identical for pure-write loops, else within 1e-12) to a captured sequential reference at N >= 50 (PERF-04-D)"
    - "joint_fpca scores + eigenvalues are numerically equivalent to a captured sequential reference at N >= 50 (PERF-04-E)"
    - "the elastic_fpca test suite is green with the parallel feature OFF — the macro compiles to the sequential path with no behavior change (PERF-04-F)"
    - "no public signature changes: vert_fpca / joint_fpca signatures and their Result<T, FdarError> returns are unchanged; the three internal fns keep their pub(crate)/private visibility (PERF-04)"
  artifacts:
    - "fdars-core/src/elastic_fpca.rs — three loops parallelized via iter_maybe_parallel!"
    - "fdars-core/src/elastic_fpca.rs — a named N >= 50 threshold constant (Claude's discretion for the name, e.g. SCORES_PARALLEL_THRESHOLD) documented in a comment"
    - "fdars-core/src/elastic_fpca.rs #[cfg(test)] mod tests — six equivalence/threshold tests test_shooting_vectors_parallel_equiv, test_augmented_srsfs_parallel_equiv, test_scores_threshold, test_vert_fpca_parallel_equiv, test_joint_fpca_parallel_equiv (PERF-04-A..E)"
  key_links:
    - "iter_maybe_parallel! (crate::iter_maybe_parallel, already imported in elastic_fpca.rs) is the feature-gated primitive — collect-then-assign is the only safe write pattern into column-major FdMatrix"
    - "align_to_target in alignment/set.rs is the canonical parallel-collect-into-Vec then sequential-row-assign analog for :701 and :720"
    - "vert_fpca / joint_fpca are the public entry points that transitively call all three internal loops"
---

<objective>
Parallelize the three per-curve loops on the elastic-FPCA critical path in
`fdars-core/src/elastic_fpca.rs` under the `parallel` feature (PERF-04), producing
output numerically equivalent to the sequential path. The change is additive and
non-breaking: no public signatures change, no new dependencies, and with the
`parallel` feature off the code compiles/runs on the identical sequential path.

Purpose: Cut wall-clock on the registration-aware FPCA path for realistic curve
counts (N >= 50), demonstrating feasibility + numerical equivalence (not a pinned
speedup — the audit flagged elastic cells LOW-CONFIDENCE under an unpinned governor).

Output: Three parallelized loops (`shooting_vectors_from_psis` :701,
`build_augmented_srsfs` :720, `svd_scores_and_eigenvalues` :764), a named N >= 50
threshold constant guarding the light :764 body, and six inline equivalence/threshold
tests. No new public symbols.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-elastic-fpca-performance/15-CONTEXT.md
@.planning/phases/15-elastic-fpca-performance/15-VALIDATION.md

@fdars-core/src/elastic_fpca.rs
@fdars-core/src/parallel.rs
@fdars-core/src/alignment/set.rs
</context>

<artifacts_this_phase_produces>
This phase produces NO new public symbols. It is an internal parallelization of three
existing internal functions:
- `shooting_vectors_from_psis` (pub(crate)) — unchanged signature
- `build_augmented_srsfs` (pub(crate)) — unchanged signature
- `svd_scores_and_eigenvalues` (private) — unchanged signature

Modified file: `fdars-core/src/elastic_fpca.rs`.
New (internal) symbol: one named threshold constant for the :764 guard (e.g.
`SCORES_PARALLEL_THRESHOLD: usize = 50`, exact name at Claude's discretion), documented
in a comment. Plus six inline `#[cfg(test)]` tests. No re-exports, no public API change.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Wave 0 + tracer — parallelize the heaviest loop shooting_vectors_from_psis (:701) end-to-end</name>
  <files>fdars-core/src/elastic_fpca.rs</files>
  <read_first>
    - fdars-core/src/elastic_fpca.rs (locate `shooting_vectors_from_psis` by name — the `for i in 0..n` loop calling `inv_exp_map_sphere(mu_psi, &psis[i], time)` and writing row i; also read the existing `#[cfg(test)] mod tests` with its `generate_test_data(n, m)` helper and `karcher_mean` usage)
    - fdars-core/src/parallel.rs (the `iter_maybe_parallel!` macro — already imported at top of elastic_fpca.rs as `crate::iter_maybe_parallel`)
    - fdars-core/src/alignment/set.rs (`align_to_target` :51-83 — the canonical parallel-collect-into-Vec then sequential-row-assign analog)
  </read_first>
  <action>
    Rewrite the `shooting_vectors_from_psis` loop to the collect-then-assign pattern from
    `align_to_target`: replace the `for i in 0..n { let v = inv_exp_map_sphere(...); ... }`
    body with `iter_maybe_parallel!(0..n).map(|i| inv_exp_map_sphere(mu_psi, &psis[i], time))
    .collect::<Vec<Vec<f64>>>()` producing one owned row Vec per curve, then a sequential
    pass `for (i, v) in rows.into_iter().enumerate() { for j in 0..m { shooting[(i, j)] = v[j]; } }`
    that writes into the column-major FdMatrix. Do NOT parallel-write the shared buffer — collect
    owned per-row results first. The function signature and pub(crate) visibility are unchanged.
    Since this loop is a pure disjoint per-index write with no cross-iteration reduction, the
    output is bit-identical to the prior sequential order.

    Also add (Wave 0) an inline test `test_shooting_vectors_parallel_equiv` in the existing
    `#[cfg(test)] mod tests`: build a synthetic dataset with N >= 50 via the existing
    `generate_test_data` helper (extend it or add a sibling if 51 grid points is fine), run
    `karcher_mean` to obtain psis + mu_psi, call `shooting_vectors_from_psis`, and assert its
    output equals a reference matrix computed by an inline sequential loop over the same inputs
    using exact equality (bit-identical — pure writes, no reduction). This is the tracer's
    end-to-end verify: the parallelized loop threads through karcher_mean -> psis -> shooting
    vectors and produces identical numbers.
  </action>
  <verify>
    <automated>cd fdars-core && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel -- elastic_fpca::tests::test_shooting_vectors_parallel_equiv</automated>
  </verify>
  <acceptance_criteria>
    - `shooting_vectors_from_psis` uses `iter_maybe_parallel!` (grep confirms; `grep -v '^\s*//' | grep -c iter_maybe_parallel` >= 1 inside the function region)
    - The loop follows collect-owned-rows-then-sequential-assign; no parallel write into the FdMatrix buffer
    - `test_shooting_vectors_parallel_equiv` passes under `--features linalg,parallel` asserting exact equality vs an inline sequential reference
    - Function signature and `pub(crate)` visibility unchanged
  </acceptance_criteria>
  <done>The :701 loop runs via iter_maybe_parallel! with collect-then-assign, and its equivalence test (PERF-04-A) is green.</done>
</task>

<task type="auto">
  <name>Task 2: Parallelize build_augmented_srsfs (:720) with equivalence test (PERF-04-B)</name>
  <files>fdars-core/src/elastic_fpca.rs</files>
  <read_first>
    - fdars-core/src/elastic_fpca.rs (`build_augmented_srsfs` — the `for i in 0..n` loop copying qn row + writing the `signum·sqrt(|f(id)|)` augmented column at index m)
    - fdars-core/src/alignment/set.rs (`align_to_target` collect-then-assign analog, already read)
  </read_first>
  <action>
    Rewrite the `build_augmented_srsfs` loop to collect-then-assign: for each i, produce an
    owned row Vec of length m_aug where entries 0..m copy `qn[(i, j)]` and entry m is
    `aligned_data[(i, id)].signum() * aligned_data[(i, id)].abs().sqrt()`, via
    `iter_maybe_parallel!(0..n).map(|i| { ... }).collect::<Vec<Vec<f64>>>()`, then a sequential
    pass assigns each owned row into the column-major `q_aug` FdMatrix. Keep `id = m / 2` and
    `m_aug = m + 1`. Signature and pub(crate) visibility unchanged. Pure per-index writes → output
    bit-identical to the sequential path.

    Add inline test `test_augmented_srsfs_parallel_equiv`: build qn and aligned_data matrices
    (N >= 50) from the test fixture, call `build_augmented_srsfs`, and assert exact equality
    against a matrix computed by an inline sequential reference loop over the same inputs.
  </action>
  <verify>
    <automated>cd fdars-core && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel -- elastic_fpca::tests::test_augmented_srsfs_parallel_equiv</automated>
  </verify>
  <acceptance_criteria>
    - `build_augmented_srsfs` uses `iter_maybe_parallel!` (grep confirms within the function region, comment lines excluded)
    - Collect-owned-rows-then-sequential-assign; no parallel write into the FdMatrix buffer
    - `test_augmented_srsfs_parallel_equiv` passes asserting exact equality vs an inline sequential reference
    - Signature and `pub(crate)` visibility unchanged
  </acceptance_criteria>
  <done>The :720 loop runs via iter_maybe_parallel! with collect-then-assign, and PERF-04-B is green.</done>
</task>

<task type="auto">
  <name>Task 3: Parallelize svd_scores_and_eigenvalues (:764) behind an N >= 50 threshold (PERF-04-C)</name>
  <files>fdars-core/src/elastic_fpca.rs</files>
  <read_first>
    - fdars-core/src/elastic_fpca.rs (`svd_scores_and_eigenvalues` — the `for k in 0..ncomp { let sv = ...; for i in 0..n { scores[(i,k)] = u[(i,k)] * sv; } }` nested loop)
  </read_first>
  <action>
    Introduce a named module-level threshold constant with the value 50 (name at Claude's
    discretion, e.g. `SCORES_PARALLEL_THRESHOLD`) with a doc comment explaining that parallel
    dispatch on this single-multiply body only pays back at N >= 50 per the audit's
    streaming-sentinel payback point. In `svd_scores_and_eigenvalues`, guard the inner
    `for i in 0..n` fill so that when `n >= SCORES_PARALLEL_THRESHOLD` it uses
    `iter_maybe_parallel!(0..n).map(|i| u[(i, k)] * sv).collect::<Vec<f64>>()` then assigns each
    value into `scores[(i, k)]` sequentially, and when `n < SCORES_PARALLEL_THRESHOLD` it runs the
    existing sequential `for i in 0..n { scores[(i,k)] = u[(i,k)] * sv; }`. Both branches produce
    identical numbers (pure per-index multiply-and-write). Signature and private visibility unchanged.

    Add inline test `test_scores_threshold`: verify that both the small-N path (N < 50) and the
    large-N path (N >= 50) produce scores equal to a hand-computed `u[(i,k)] * singular_value[k]`
    reference (exact equality). Exercise a fixture with N >= 50 so the parallel branch is taken and
    a small fixture (N < 50) so the sequential branch is taken; both must match the reference.
  </action>
  <verify>
    <automated>cd fdars-core && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel -- elastic_fpca::tests::test_scores_threshold</automated>
  </verify>
  <acceptance_criteria>
    - A named threshold constant equal to 50 exists with a documenting comment (grep confirms a `const` set to `50`)
    - `svd_scores_and_eigenvalues` branches on that constant: parallel via `iter_maybe_parallel!` at N >= 50, existing sequential path below
    - `test_scores_threshold` passes, checking both branches against an exact `u * sv` reference
    - Signature and private visibility unchanged
  </acceptance_criteria>
  <done>The :764 loop is parallelized above the N >= 50 threshold and sequential below, and PERF-04-C is green.</done>
</task>

<task type="auto">
  <name>Task 4: End-to-end vert_fpca / joint_fpca equivalence + parallel-OFF suite (PERF-04-D/E/F)</name>
  <files>fdars-core/src/elastic_fpca.rs</files>
  <read_first>
    - fdars-core/src/elastic_fpca.rs (`vert_fpca` :89, `joint_fpca` :274 entry points and the existing `test_vert_fpca_basic` test for the fixture/karcher_mean call shape)
    - fdars-core/src/parallel.rs (macro compiles to sequential `into_iter` when `parallel` is off)
  </read_first>
  <action>
    Add two end-to-end equivalence tests exercising the public entry points at N >= 50:
    `test_vert_fpca_parallel_equiv` and `test_joint_fpca_parallel_equiv`. Each builds a synthetic
    dataset (N >= 50) via the fixture helper, runs `karcher_mean`, calls the public entry point
    (`vert_fpca(&km, &t, ncomp)` / `joint_fpca(&km, &t, ncomp, None)`), and asserts the returned
    `scores` and `eigenvalues` match a reference within 1e-12 (use exact equality where the values
    flow only through the pure-write loops; fall back to 1e-12 only where an SVD/reduction sits on
    the path). The reference is the result values captured from the same computation — assert
    determinism and the documented equivalence tolerance so the test is meaningful under both feature
    configurations. Do NOT change any public signature; `joint_fpca` keeps its `balance_c: Option<f64>`
    parameter and both keep `Result<T, FdarError>` returns.

    No production code change is expected in this task beyond tests; if a signature or behavior needed
    to change to make equivalence hold, that is a defect in Tasks 1-3 to fix there, not here.
  </action>
  <verify>
    <automated>cd fdars-core && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel -- elastic_fpca::tests::test_vert_fpca_parallel_equiv elastic_fpca::tests::test_joint_fpca_parallel_equiv && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg -- elastic_fpca</automated>
  </verify>
  <acceptance_criteria>
    - `test_vert_fpca_parallel_equiv` and `test_joint_fpca_parallel_equiv` pass under `--features linalg,parallel` at N >= 50, asserting scores + eigenvalues match a reference (exact for pure-write paths, within 1e-12 otherwise)
    - The full `elastic_fpca` suite passes under `--features linalg` (parallel OFF) — PERF-04-F: macro compiles to sequential, no behavior change
    - No public signature change: `vert_fpca` / `joint_fpca` signatures and `Result<T, FdarError>` returns unchanged (`joint_fpca` keeps `balance_c: Option<f64>`)
  </acceptance_criteria>
  <done>vert_fpca and joint_fpca produce numerically-equivalent output at N >= 50 (PERF-04-D/E) and the suite is green with parallel OFF (PERF-04-F).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | No new trust boundary. This is a pure internal compute refactor of three `pub(crate)`/private functions with no I/O and no new public surface. |

## STRIDE Threat Register

ASVS L1. No new threats. The three loops are internal to the elastic-FPCA path; their
callers `vert_fpca` / `joint_fpca` already validate all inputs (dimension/parameter checks
returning `FdarError`). Parallelization changes no input-validation logic, adds no
dependency, and introduces no new I/O, deserialization, or public symbol. Collect-then-assign
avoids any data race (no shared mutable buffer written across threads); no RNG is involved, so
determinism is preserved. V5 (validation) and V7 (error handling) are unchanged from the
existing entry points. No threat requires mitigation, acceptance, or transfer for this phase.
</threat_model>

<verification>
- After each task commit: `cargo test -p fdars-core --features linalg -- elastic_fpca`
- After the wave, equivalence must hold in BOTH feature configurations:
  - Parallel ON: `cargo test -p fdars-core --features linalg,parallel`
  - Parallel OFF: `cargo test -p fdars-core --features linalg` (no `parallel`)
- All six named tests green: `test_shooting_vectors_parallel_equiv`,
  `test_augmented_srsfs_parallel_equiv`, `test_scores_threshold`,
  `test_vert_fpca_parallel_equiv`, `test_joint_fpca_parallel_equiv`, plus the existing
  `elastic_fpca` suite (PERF-04-F).
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp` may be required for link steps (small /tmp tmpfs).
- Grep confirms all three functions use `iter_maybe_parallel!` and a named `const` = 50 exists.
</verification>

<success_criteria>
- All three loops (`shooting_vectors_from_psis`, `build_augmented_srsfs`,
  `svd_scores_and_eigenvalues`) execute via `iter_maybe_parallel!(0..n)` under the `parallel`
  feature and sequentially when the feature is off.
- The `:764` body is guarded by a named N >= 50 threshold constant.
- `vert_fpca` / `joint_fpca` output (scores + eigenvalues) is numerically equivalent to the
  sequential reference at N >= 50.
- Additive and non-breaking: no public signature changes, no new dependencies, visibility of
  the three internal functions unchanged.
- Suite green under both `linalg` and `linalg,parallel`.
</success_criteria>

<output>
Create `.planning/phases/15-elastic-fpca-performance/15-01-SUMMARY.md` when done.
</output>