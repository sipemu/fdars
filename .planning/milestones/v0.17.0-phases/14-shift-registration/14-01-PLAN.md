---
phase: 14-shift-registration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/alignment/shift.rs
autonomous: true
requirements: [FEAT-06]
estimate:
  tokens: 55000
  raw_tokens: 32000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "least_squares_shift_registration(data, argvals, max_shift) returns ShiftRegistrationResult with registered curves + per-curve shift δᵢ for every input curve (SC1, FEAT-06)"
    - "On an already-aligned/constant set the estimated shifts are ≈ 0 (SC2, FEAT-06-A)"
    - "On identical curves offset by a known constant shift, recovered δᵢ match the injected offsets within tolerance (SC2, FEAT-06-B)"
    - "Registered curves are re-evaluated (linear-interp resampled) at the correct shifted argvals (FEAT-06-C)"
    - "Empty data returns Err(FdarError::InvalidDimension); argvals length mismatch returns Err(FdarError::InvalidDimension) (SC5, FEAT-06-D, FEAT-06-E)"
    - "least_squares_shift_registration returns Result<T, FdarError> and never panics on bad input (SC5)"
  artifacts:
    - "fdars-core/src/alignment/shift.rs (new file)"
    - "ShiftRegistrationResult { registered_data: FdMatrix, shifts: Vec<f64> } (public struct, per D-Area1)"
    - "least_squares_shift_registration (public fn, per D-Area1)"
  key_links:
    - "golden_section_search minimizes Simpson-weighted L2 objective ‖fᵢ(t−δ)−mean(t)‖² over δ ∈ [−max_shift, +max_shift]"
    - "linear_interp(argvals, row, t−δ) evaluates the shifted curve with built-in Boundary clamping"
    - "fdata::mean_1d(data) supplies the cross-sectional target"
---

<objective>
FEAT-06 tracer + full implementation: rigid horizontal shift registration in a new file `fdars-core/src/alignment/shift.rs`. Wire one entry point — `least_squares_shift_registration` — end-to-end through the cross-sectional mean, per-curve golden-section optimization, and registered-matrix assembly, verified by the already-aligned (FEAT-06-A) and offset-recovery (FEAT-06-B) tests first, then expand to the curve-value spot-check and the two error-path tests.

Purpose: Fills the "simplest registration method" gap (fdars currently jumps from landmark shifts straight to full elastic SRSF warping), per D-Area1 and ROADMAP Phase 14 SC1/SC2.
Output: New file `shift.rs` containing `ShiftRegistrationResult`, `least_squares_shift_registration`, a private golden-section search + L2-shift objective, and inline `#[cfg(test)] mod tests` covering FEAT-06-A…E. NOTE: this plan does NOT touch `mod.rs` or `lib.rs` — re-exports are consolidated into plan 14-02 to avoid merge collision.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-shift-registration/14-CONTEXT.md
@.planning/phases/14-shift-registration/14-PATTERNS.md
@.planning/phases/14-shift-registration/14-RESEARCH.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end least_squares_shift_registration — one path, verified</name>
  <files>fdars-core/src/alignment/shift.rs</files>
  <read_first>
    - fdars-core/src/alignment/set.rs (lines 57–83: `align_to_target` — the canonical parallel-collect-then-sequential-assemble template this file mirrors; imports at lines 1–9)
    - fdars-core/src/alignment/mod.rs (lines 131–141: `AlignmentSetResult` struct — the exact derive/`#[non_exhaustive]` pattern to model `ShiftRegistrationResult` on)
    - fdars-core/src/helpers.rs (lines 57–86 `simpsons_weights`; lines 172–191 `linear_interp` with built-in lower/upper Boundary clamp)
    - fdars-core/src/fdata.rs (lines 167–179: `mean_1d(&FdMatrix) -> Vec<f64>`)
    - .planning/phases/14-shift-registration/14-PATTERNS.md (section "fdars-core/src/alignment/shift.rs" — imports adaptation, result struct, parallel-collect template, error-validation block)
    - .planning/phases/14-shift-registration/14-RESEARCH.md (Pattern 1 golden-section sketch; Pattern 2 L2-shift objective; Exact Public API Surface FEAT-06)
  </read_first>
  <behavior>
    - test_shift_already_aligned (FEAT-06-A): a set of constant/already-aligned curves → every δᵢ ≈ 0.0 (within 1e-3) and registered_data ≈ input.
    - test_shift_recovers_injected_offset (FEAT-06-B): identical Gaussian bumps offset by a known constant δ_known (e.g. one curve at mu=0.5, another shifted to mu=0.5+δ_known) → recovered δᵢ align the shifted curve back toward the mean; the shift that best matches the sample mean is recovered within tolerance (~1e-2 given golden-section tol 1e-6 and grid resolution).
  </behavior>
  <action>
    Create new file `fdars-core/src/alignment/shift.rs` with a module doc comment ("//! Least-squares shift (rigid horizontal) registration."). Imports: `crate::error::FdarError`, `crate::helpers::{linear_interp, simpsons_weights}`, `crate::iter_maybe_parallel`, `crate::matrix::FdMatrix`, and `#[cfg(feature = "parallel")] use rayon::iter::ParallelIterator;` (mirror set.rs imports).

    Define public struct `ShiftRegistrationResult` with derives `Debug, Clone, PartialEq`, then `#[non_exhaustive]` and `#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]` (serde is locked ON for this struct per D-Area1, even though AlignmentSetResult omits it). Fields: `pub registered_data: FdMatrix` (n×m registered curves), `pub shifts: Vec<f64>` (per-curve δᵢ). Doc-comment each field.

    Define private `const DEFAULT_MAX_SHIFT_FRACTION: f64 = 0.25;`, `const GS_TOL: f64 = 1e-6;`, `const GS_MAX_ITER: usize = 100;`. Reference DEFAULT_MAX_SHIFT_FRACTION only in the rustdoc guidance (recommended caller value = 0.25 × domain range); Rust has no default args.

    Define private `fn golden_section_search<F: Fn(f64) -> f64>(f: F, lo, hi, tol, max_iter) -> f64` per the RESEARCH Pattern-1 sketch (PHI = 1.6180339887498949; bracket-narrowing loop; returns midpoint of final bracket).

    Define private `fn l2_shift_objective(row: &[f64], argvals: &[f64], mean: &[f64], weights: &[f64], delta: f64) -> f64` = Σⱼ (linear_interp(argvals, row, argvals[j] − delta) − mean[j])² · weights[j], per RESEARCH Pattern-2. No fenced code — follow the sibling `align_to_target`/`alignment_quality` idioms.

    Define public `pub fn least_squares_shift_registration(data: &FdMatrix, argvals: &[f64], max_shift: f64) -> Result<ShiftRegistrationResult, FdarError>`. Do NOT add `#[must_use]` (Result is already must_use; clippy::double_must_use fires under -D warnings). Body: extract `(n, m) = data.shape()`; validate (see Task 3 wires the full set, but include at least the shape+argvals checks now so the happy path is safe): compute `weights = simpsons_weights(argvals)` and `mean = crate::fdata::mean_1d(data)`. Then the parallel-collect-then-assemble pattern from set.rs: `let results: Vec<(f64, Vec<f64>)> = iter_maybe_parallel!(0..n).map(|i| { let row = data.row(i); let delta = golden_section_search(|d| l2_shift_objective(&row, argvals, &mean, &weights, d), -max_shift, max_shift, GS_TOL, GS_MAX_ITER); let shifted: Vec<f64> = argvals.iter().map(|&t| linear_interp(argvals, &row, t - delta)).collect(); (delta, shifted) }).collect();`. Sequentially assemble `registered_data = FdMatrix::zeros(n, m)` filling `registered_data[(i, j)] = row[j]` and pushing each delta into `shifts`. Return `Ok(ShiftRegistrationResult { registered_data, shifts })`. Because parallel iterators return in arbitrary order, results MUST be collected into a Vec first then assembled sequentially by enumerate index (never pushed from inside the parallel map).

    Rustdoc: document the unimodal-objective assumption for golden-section, the Boundary-clamping behavior inherited from linear_interp for off-domain t, and the recommended `max_shift = 0.25 * (argvals.last() - argvals.first())`. Include a `# Examples` doctest that constructs a small FdMatrix and calls the function (mirror set.rs doctest style).

    Add `#[cfg(test)] mod tests` with inline helpers `uniform_grid(n)` and `gaussian_bump(argvals, mu, sigma)` (per PATTERNS/VALIDATION fixtures), and the two behavior tests above. Assert with `assert!((delta).abs() < tol)` style using explicit tolerance constants chosen for the grid.
  </action>
  <verify>
    <automated>cargo test -p fdars-core --features linalg -- test_shift_already_aligned test_shift_recovers_injected_offset 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `fdars-core/src/alignment/shift.rs` exists and contains `pub fn least_squares_shift_registration` and `pub struct ShiftRegistrationResult` with fields `registered_data: FdMatrix` and `shifts: Vec<f64>`.
    - `cargo test -p fdars-core --features linalg -- test_shift_already_aligned` passes.
    - `cargo test -p fdars-core --features linalg -- test_shift_recovers_injected_offset` passes.
    - No `#[must_use]` attribute on `least_squares_shift_registration` (grep: the fn signature line is not preceded by `#[must_use]`).
  </acceptance_criteria>
  <done>The single happy path (mean → per-curve golden-section shift → registered matrix) works end-to-end and is committed; FEAT-06-A and FEAT-06-B pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Registered-curve-value spot-check test (FEAT-06-C)</name>
  <files>fdars-core/src/alignment/shift.rs</files>
  <read_first>
    - fdars-core/src/alignment/shift.rs (the file created in Task 1)
    - fdars-core/src/helpers.rs (lines 172–191: `linear_interp` — to hand-compute expected shifted values for the spot-check)
    - .planning/phases/14-shift-registration/14-VALIDATION.md (FEAT-06-C row)
  </read_first>
  <behavior>
    - test_shift_registration_curve_values (FEAT-06-C): after registration, spot-check that a chosen `registered_data[(i, j)]` equals `linear_interp(argvals, input_row_i, argvals[j] - shifts[i])` for a small case — i.e. the registered matrix is exactly the shifted re-evaluation, not a re-derived value.
  </behavior>
  <action>
    Add `test_shift_registration_curve_values` to the inline `#[cfg(test)] mod tests`. Build a small deterministic FdMatrix (e.g. 2×5 or 3×7 on a uniform grid), run `least_squares_shift_registration`, then for one or two `(i, j)` positions assert that `result.registered_data[(i, j)]` matches `linear_interp(argvals, &data.row(i), argvals[j] - result.shifts[i])` within 1e-9. This proves the registered curves are re-evaluated at the correct shifted argvals via linear-interpolation resampling (SC1). Choose input data where the recovered shift is nonzero so the spot-check is meaningful, or assert against the exact same interp call to be robust regardless of the optimizer's chosen δ.
  </action>
  <verify>
    <automated>cargo test -p fdars-core --features linalg -- test_shift_registration_curve_values 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p fdars-core --features linalg -- test_shift_registration_curve_values` passes.
    - The test asserts `registered_data[(i,j)]` equals `linear_interp(argvals, row_i, argvals[j] - shifts[i])` (source contains a `linear_interp(` call inside the test comparing against `registered_data`).
  </acceptance_criteria>
  <done>FEAT-06-C passes: registered curves confirmed to be the correct shifted re-evaluation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Input validation + error-path tests (FEAT-06-D, FEAT-06-E)</name>
  <files>fdars-core/src/alignment/shift.rs</files>
  <read_first>
    - fdars-core/src/alignment/shift.rs (the file from Tasks 1–2)
    - fdars-core/src/error.rs (FdarError variants: InvalidDimension { parameter, expected, actual }, InvalidParameter { parameter, message })
    - .planning/phases/14-shift-registration/14-PATTERNS.md (section "Error handling (input validation)" — the exact InvalidDimension construction shape)
    - .planning/phases/14-shift-registration/14-RESEARCH.md (Security Domain V5/V7 controls for least_squares_shift_registration)
  </read_first>
  <behavior>
    - test_shift_registration_empty_data (FEAT-06-D): empty data (n=0 or m=0) → returns Err(FdarError::InvalidDimension).
    - test_shift_registration_argvals_mismatch (FEAT-06-E): argvals length != m → returns Err(FdarError::InvalidDimension).
  </behavior>
  <action>
    At the entry of `least_squares_shift_registration`, add the full V5 dimension/parameter validation guard block BEFORE any computation (mean/weights): if `n == 0 || m == 0` return `Err(FdarError::InvalidDimension { parameter: "data", expected: "non-empty matrix".to_string(), actual: format!("{}x{}", n, m) })`; if `argvals.len() != m` return `Err(FdarError::InvalidDimension { parameter: "argvals", expected: m, actual: argvals.len() })` (match the exact variant field types used elsewhere in the crate — check error.rs for whether expected/actual are usize or String and construct accordingly); if `argvals.len() < 2` return an `InvalidParameter` error; if `max_shift <= 0.0` return `Err(FdarError::InvalidParameter { parameter: "max_shift", message: ... })`. No panic paths — these entry checks prevent index-out-of-bounds in the loops (V7). Add the two error-path tests asserting `matches!(result, Err(FdarError::InvalidDimension { .. }))`.
  </action>
  <verify>
    <automated>cargo test -p fdars-core --features linalg -- alignment::shift 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p fdars-core --features linalg -- test_shift_registration_empty_data` passes.
    - `cargo test -p fdars-core --features linalg -- test_shift_registration_argvals_mismatch` passes.
    - All 5 FEAT-06 tests green: `cargo test -p fdars-core --features linalg -- alignment::shift` reports 5 passed (plus the doctest).
    - `cargo clippy -p fdars-core --features linalg` is clean (no double_must_use).
  </acceptance_criteria>
  <done>All FEAT-06-A…E tests pass; input validation returns Result errors on empty data, argvals mismatch, argvals<2, and non-positive max_shift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → `least_squares_shift_registration` | Untrusted `&FdMatrix` dimensions, `&[f64]` argvals length, and `max_shift` scalar cross into the pure-numeric function; no I/O, auth, or network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering (V5 input validation) | `least_squares_shift_registration` entry | medium | mitigate | Validate `data.nrows()>0`, `data.ncols()==argvals.len()`, `argvals.len()>=2`, `max_shift>0.0` at entry; return `FdarError::InvalidDimension`/`InvalidParameter` (Task 3). Prevents index-out-of-bounds in per-curve loops. |
| T-14-02 | Denial of service (V7 error handling) | golden-section + interp loops | low | mitigate | No panic paths — entry checks guarantee loop indices in-bounds; `linear_interp` returns boundary values for off-domain t (never NaN). |
</threat_model>

<verification>
- `cargo test -p fdars-core --features linalg -- alignment::shift` → all 5 FEAT-06 tests + doctest green.
- `cargo clippy -p fdars-core --features linalg` → clean, no `double_must_use`.
- Full crate build unaffected (additive new file, no re-export edits in this plan).
</verification>

<success_criteria>
- New file `fdars-core/src/alignment/shift.rs` exists with `ShiftRegistrationResult` (fields `registered_data: FdMatrix`, `shifts: Vec<f64>`) and `least_squares_shift_registration` returning `Result<ShiftRegistrationResult, FdarError>`.
- FEAT-06-A…E all pass.
- No `#[must_use]` on the Result-returning fn; clippy clean.
- No existing `alignment/` signature modified; no new dependencies.
</success_criteria>

<output>
Create `.planning/phases/14-shift-registration/14-01-SUMMARY.md` when done. Note in the SUMMARY that `mod shift;` and crate-root re-exports are intentionally deferred to plan 14-02 (serialized re-export step).
</output>
