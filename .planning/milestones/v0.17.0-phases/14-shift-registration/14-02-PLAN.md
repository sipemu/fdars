---
phase: 14-shift-registration
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - fdars-core/src/alignment/quality.rs
  - fdars-core/src/alignment/mod.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [FEAT-07]
estimate:
  tokens: 60000
  raw_tokens: 34000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "least_squares_score(registered, argvals) = (1/n) Σᵢ ∫‖registeredᵢ − mean‖² dt (Simpson-weighted), returns Result<f64, FdarError> (SC3, FEAT-07)"
    - "pairwise_correlation_score(registered, argvals) = mean functional Pearson correlation over all n(n−1)/2 pairs via Simpson inner product, returns Result<f64, FdarError> (SC3, FEAT-07)"
    - "sobolev_least_squares_score(registered, argvals, lambda) = LS term + λ·(1/n) Σᵢ ∫(fᵢ′ − mean′)² dt, returns Result<f64, FdarError> (SC3, FEAT-07)"
    - "least_squares_score on identical constant curves = 0.0 (FEAT-07-A)"
    - "least_squares_score drops after least_squares_shift_registration on synthetic shifted bumps; pairwise_correlation_score rises after registration (SC4, FEAT-07-B, FEAT-07-C)"
    - "pairwise_correlation_score with n=1 returns Err(FdarError::InvalidParameter) (FEAT-07-D)"
    - "sobolev_least_squares_score with lambda=0 equals least_squares_score; with lambda>0 is ≥ the lambda=0 score (FEAT-07-E, FEAT-07-F)"
    - "All new public functions are re-exported at the crate root; existing alignment/ signatures untouched (SC5)"
  artifacts:
    - "least_squares_score (public fn in alignment/quality.rs, per D-Area2)"
    - "pairwise_correlation_score (public fn in alignment/quality.rs, per D-Area2)"
    - "sobolev_least_squares_score (public fn in alignment/quality.rs, per D-Area2)"
    - "alignment/mod.rs re-exports shift + three scores (per D-Area3)"
    - "lib.rs crate-root re-exports shift fn + ShiftRegistrationResult + three scores (per D-Area3)"
  key_links:
    - "crate::fdata::mean_1d + helpers::simpsons_weights supply the mean and integration weights for LS + Sobolev"
    - "helpers::gradient_uniform supplies the first-derivative for the Sobolev term (same idiom as warp_smoothness)"
    - "mod.rs `mod shift;` + `pub use shift::{...}` requires shift.rs from plan 14-01 to exist"
    - "single consolidated re-export task edits mod.rs AND lib.rs — never split across parallel tasks (merge-collision pitfall)"
---

<objective>
FEAT-07 expansion + integration: append the three registration-quality scores (`least_squares_score`, `pairwise_correlation_score`, `sobolev_least_squares_score`) to `alignment/quality.rs`, then a single consolidated task wires all new public API through `alignment/mod.rs` (`mod shift;` + re-exports) and `lib.rs` (crate-root re-exports) for BOTH FEAT-06 and FEAT-07.

Purpose: Enables post-hoc registration diagnostics (scikit-fda LeastSquares/PairwiseCorrelation/SobolevLeastSquares parity, standalone-energy form) and exposes the plan-14 API at the crate root, per D-Area2/D-Area3 and ROADMAP Phase 14 SC3/SC4/SC5.
Output: Three new public functions in `quality.rs` with inline tests FEAT-07-A…F, plus updated re-export chains in `mod.rs` and `lib.rs`. Depends on plan 14-01 (needs `shift.rs` present for `mod shift;` and needs `least_squares_shift_registration` for the "drops/rises after registration" tests).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-shift-registration/14-CONTEXT.md
@.planning/phases/14-shift-registration/14-PATTERNS.md
@.planning/phases/14-shift-registration/14-RESEARCH.md
@fdars-core/src/alignment/quality.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: least_squares_score + sobolev_least_squares_score (FEAT-07-A, E, F)</name>
  <files>fdars-core/src/alignment/quality.rs</files>
  <read_first>
    - fdars-core/src/alignment/quality.rs (lines 1–8 imports; lines 43–55 `warp_smoothness` gradient_uniform idiom; lines 63–161 `alignment_quality` mean_1d + simpsons_weights + l2_distance idiom)
    - fdars-core/src/helpers.rs (lines 57–86 `simpsons_weights`; lines 728–761 `gradient_uniform` 5-point stencil)
    - fdars-core/src/fdata.rs (lines 167–179 `mean_1d`)
    - fdars-core/src/error.rs (FdarError::InvalidDimension / InvalidParameter variants)
    - .planning/phases/14-shift-registration/14-PATTERNS.md (section "fdars-core/src/alignment/quality.rs" — least_squares_score expansion, sobolev gradient_uniform usage, FdarError import note)
    - .planning/phases/14-shift-registration/14-RESEARCH.md (Critical Semantic Divergence table — fdars standalone-energy form, NOT scikit-fda ratio; Pattern 4 Sobolev)
  </read_first>
  <behavior>
    - test_ls_score_identical_curves (FEAT-07-A): least_squares_score on identical constant curves = 0.0 (mean equals every curve → zero spread).
    - test_sobolev_score_lambda_zero (FEAT-07-E): sobolev_least_squares_score(reg, argvals, 0.0) equals least_squares_score(reg, argvals) within 1e-12.
    - test_sobolev_score_lambda_positive (FEAT-07-F): sobolev_least_squares_score with lambda>0 is ≥ the lambda=0 score (derivative penalty term is non-negative).
  </behavior>
  <action>
    Add `use crate::error::FdarError;` to the top of `quality.rs` (not currently imported — existing fns return raw types; the new fns return Result). Do not remove or modify any existing import or signature.

    Append `pub fn least_squares_score(registered: &FdMatrix, argvals: &[f64]) -> Result<f64, FdarError>`. No `#[must_use]` (Result already must_use). Validate at entry: `(n, m) = registered.shape()`; if `n == 0 || m == 0` return InvalidDimension; if `argvals.len() != m` return InvalidDimension (match the exact field types in error.rs). Compute `weights = simpsons_weights(argvals)`, `mean = crate::fdata::mean_1d(registered)`, then `score = (1/n) Σᵢ Σⱼ (registered[(i,j)] − mean[j])² · weights[j]` (expand the l2_distance-squared inline as in the alignment_quality idiom). Return `Ok(score)`. Rustdoc: state this is the fdars standalone-energy form (absolute L2 spread), NOT scikit-fda's ratio-based scorer, and that it returns Result (deviation from the raw-f64 neighbors) to enable dimension validation.

    Append `pub fn sobolev_least_squares_score(registered: &FdMatrix, argvals: &[f64], lambda: f64) -> Result<f64, FdarError>`. Same entry validation, plus: if `lambda < 0.0` return `Err(FdarError::InvalidParameter { parameter: "lambda", message: ... })`. Compute the LS term identically to least_squares_score. Then the Sobolev term: `h = (argvals[m-1] - argvals[0]) / (m-1) as f64`; `mean_prime = gradient_uniform(&mean, h)`; per curve `fi_prime = gradient_uniform(&registered.row(i).to_vec(), h)`; `sobol_term = (1/n) Σᵢ Σⱼ (fi_prime[j] − mean_prime[j])² · weights[j]`. Return `Ok(ls_term + lambda * sobol_term)`. Rustdoc: note the uniform-grid assumption for gradient_uniform (same as warp_smoothness) and the standalone-energy divergence from scikit-fda's ratio form.

    Add the three behavior tests to the module's `#[cfg(test)] mod tests` (extend the existing tests block if present, or add one). Use inline `uniform_grid`/`gaussian_bump` helpers as needed. For FEAT-07-E assert `(sobolev(...,0.0)? - ls(...)?).abs() < 1e-12`; for FEAT-07-F assert `sobolev(...,positive)? >= sobolev(...,0.0)? - 1e-12`.
  </action>
  <verify>
    <automated>cargo test -p fdars-core --features linalg -- test_ls_score_identical_curves test_sobolev_score_lambda_zero test_sobolev_score_lambda_positive 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `quality.rs` contains `pub fn least_squares_score` and `pub fn sobolev_least_squares_score`, both returning `Result<f64, FdarError>`.
    - `cargo test -p fdars-core --features linalg -- test_ls_score_identical_curves test_sobolev_score_lambda_zero test_sobolev_score_lambda_positive` reports 3 passed.
    - Neither new fn is preceded by `#[must_use]`.
  </acceptance_criteria>
  <done>FEAT-07-A, -E, -F pass; least_squares_score and sobolev_least_squares_score implemented as standalone-energy forms returning Result.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pairwise_correlation_score + direction tests (FEAT-07-B, C, D)</name>
  <files>fdars-core/src/alignment/quality.rs</files>
  <read_first>
    - fdars-core/src/alignment/quality.rs (the additions from Task 1; simpsons_weights usage)
    - fdars-core/src/alignment/shift.rs (from plan 14-01 — `least_squares_shift_registration` used by the direction tests)
    - .planning/phases/14-shift-registration/14-RESEARCH.md (Pattern 3 functional_pearson_corr via Simpson inner product; Pitfall 5 n=1 error; Synthetic Bump Generator fixture)
    - .planning/phases/14-shift-registration/14-VALIDATION.md (FEAT-07-B, C, D rows)
  </read_first>
  <behavior>
    - test_pairwise_corr_rises_after_registration (FEAT-07-C): on synthetic shifted Gaussian bumps, pairwise_correlation_score(registered) > pairwise_correlation_score(original).
    - test_ls_score_drops_after_registration (FEAT-07-B): on the same shifted bumps, least_squares_score(registered) < least_squares_score(original).
    - test_pairwise_corr_n1_error (FEAT-07-D): pairwise_correlation_score on a single-curve matrix (n=1) returns Err(FdarError::InvalidParameter).
  </behavior>
  <action>
    Append `pub fn pairwise_correlation_score(registered: &FdMatrix, argvals: &[f64]) -> Result<f64, FdarError>`. No `#[must_use]`. Validate at entry: `(n, m) = registered.shape()`; if `m == 0` return InvalidDimension; if `argvals.len() != m` return InvalidDimension; if `n < 2` return `Err(FdarError::InvalidParameter { parameter: "n", message: "pairwise correlation requires at least 2 curves" })` (Pitfall 5 — no pairs when n<2). Compute `weights = simpsons_weights(argvals)`. For each unordered pair (i, k) with i<k, compute the Simpson-weighted normalized inner product: `inner = Σⱼ row_i[j]·row_k[j]·weights[j]`, `norm_i = sqrt(Σⱼ row_i[j]²·weights[j])`, `norm_k = sqrt(Σⱼ ...)`; `corr = if norm_i*norm_k < 1e-15 { 0.0 } else { inner / (norm_i*norm_k) }` (NaN guard, Pitfall §NaN). Return `Ok(mean over the n(n-1)/2 pairs)`. Document O(n²·m) complexity and that this is the functional (L2 inner-product) correlation, standalone form (no ratio to original, unlike scikit-fda which takes original data). Iterate `(0..n).flat_map(|i| (i+1..n).map(move |k| ...))` — usize arithmetic for the pair count.

    Add the three direction/error tests. For FEAT-07-B and FEAT-07-C build a synthetic shifted-bumps set via inline `make_shifted_bumps(n, m, delta)` / `gaussian_bump` (per VALIDATION fixture), call `least_squares_shift_registration(&data, &argvals, max_shift)` (from plan 14-01, referenced through `crate::alignment::least_squares_shift_registration` or the sibling module path), then assert the score moves in the expected direction (LS drops, correlation rises). For FEAT-07-D assert `matches!(pairwise_correlation_score(&single_curve, &argvals), Err(FdarError::InvalidParameter { .. }))`.
  </action>
  <verify>
    <automated>cargo test -p fdars-core --features linalg -- alignment::quality 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `quality.rs` contains `pub fn pairwise_correlation_score` returning `Result<f64, FdarError>`.
    - `cargo test -p fdars-core --features linalg -- test_pairwise_corr_rises_after_registration test_ls_score_drops_after_registration test_pairwise_corr_n1_error` reports 3 passed.
    - All six FEAT-07 tests green under `cargo test -p fdars-core --features linalg -- alignment::quality`.
  </acceptance_criteria>
  <done>FEAT-07-B, -C, -D pass; pairwise_correlation_score implemented with n≥2 validation and NaN guard; direction tests confirm scores move correctly after registration.</done>
</task>

<task type="auto">
  <name>Task 3: Consolidated re-exports — mod.rs (mod shift + all new names) AND lib.rs</name>
  <files>fdars-core/src/alignment/mod.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/alignment/mod.rs (lines 14–40 `mod` declarations; lines 86–107 existing `pub use quality::{...}` and `pub use set::{...}` blocks)
    - fdars-core/src/lib.rs (lines 139–170 the single flat alphabetical alignment `pub use` block)
    - .planning/phases/14-shift-registration/14-PATTERNS.md (sections "alignment/mod.rs" and "lib.rs" — exact insertion points and alphabetical positions)
    - .planning/phases/14-shift-registration/14-RESEARCH.md (Pitfall 2 — mod.rs/lib.rs re-export edits MUST be a single serialized step)
  </read_first>
  <action>
    This is the single consolidated re-export step for BOTH FEAT-06 and FEAT-07 — it edits `mod.rs` AND `lib.rs` together so the two files are never touched by concurrent tasks (merge-collision pitfall).

    In `fdars-core/src/alignment/mod.rs`: add `mod shift;` in alphabetical order between `mod set;` and `mod shape;`. Extend the existing `pub use quality::{...}` block to add `least_squares_score`, `pairwise_correlation_score`, `sobolev_least_squares_score` (keep the block alphabetically ordered). Add a new `pub use shift::{least_squares_shift_registration, ShiftRegistrationResult};` block after the `pub use set::{...}` block.

    In `fdars-core/src/lib.rs`: insert the five new names into the single flat alphabetical alignment `pub use alignment::{...}` block (functions and types interleaved alphabetically as the existing block already mixes them): `least_squares_score` (after `lambda_cv`), `least_squares_shift_registration` (after `least_squares_score`), `pairwise_correlation_score` (after `pairwise_consistency`), `ShiftRegistrationResult` (after `ShapeMeanResult`), `sobolev_least_squares_score` (near `shape_self_distance_matrix` / snake_case ordering). Do not create a separate block — insert into the existing flat list.

    Do not modify any existing signature. After editing, the crate must compile and the full alignment test set must run.
  </action>
  <verify>
    <automated>cargo test -p fdars-core --features linalg -- alignment::shift alignment::quality 2>&1 && cargo build -p fdars-core --features linalg 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `fdars-core/src/alignment/mod.rs` contains `mod shift;` and `pub use shift::{least_squares_shift_registration, ShiftRegistrationResult};` and the three score names in the `pub use quality::{...}` block.
    - `fdars-core/src/lib.rs` alignment `pub use` block contains `least_squares_shift_registration`, `ShiftRegistrationResult`, `least_squares_score`, `pairwise_correlation_score`, `sobolev_least_squares_score`.
    - A doctest or ad-hoc check confirms `use fdars_core::least_squares_shift_registration;` and `use fdars_core::least_squares_score;` resolve (crate builds clean).
    - `cargo build -p fdars-core --features linalg` succeeds; `cargo clippy -p fdars-core --features linalg` clean.
  </acceptance_criteria>
  <done>All five new public items (shift fn + result struct + three scores) are re-exported through alignment/mod.rs and crate-root lib.rs in a single serialized edit; crate builds and all FEAT-06/FEAT-07 tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → score functions | Untrusted `&FdMatrix` dimensions, `&[f64]` argvals length, and `lambda` scalar cross into pure-numeric scorers; no I/O, auth, or network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03 | Tampering (V5 input validation) | least_squares_score / sobolev / pairwise entry | medium | mitigate | Validate `registered.ncols()==argvals.len()`, `nrows>=1` (`>=2` for pairwise), `lambda>=0.0`; return `FdarError::InvalidDimension`/`InvalidParameter` (Tasks 1–2). |
| T-14-04 | Information disclosure (V7) | pairwise correlation denom | low | mitigate | NaN guard `if norm_i*norm_k < 1e-15 { 0.0 }` prevents NaN propagation from degenerate all-zero curves. |
| T-14-05 | Denial of service (V7 error handling) | pairwise n=1 empty-pair set | low | mitigate | Validate `n>=2` at entry (Pitfall 5) — avoids division-by-zero / empty-mean over zero pairs. |
</threat_model>

<verification>
- `cargo test -p fdars-core --features linalg -- alignment::quality` → all 6 FEAT-07 tests green.
- `cargo test -p fdars-core --features linalg -- alignment::shift` → FEAT-06 tests still green (re-exports don't break plan 14-01).
- `cargo build -p fdars-core --features linalg` + `cargo clippy -p fdars-core --features linalg` → clean.
- Full suite `cargo test -p fdars-core --features linalg` → green before verify-work.
</verification>

<success_criteria>
- `least_squares_score`, `pairwise_correlation_score`, `sobolev_least_squares_score` exist in `alignment/quality.rs`, each returning `Result<f64, FdarError>`.
- FEAT-07-A…F all pass; scores move in the expected direction after registration (SC4).
- All five plan-14 public items re-exported at the crate root (SC5); no existing `alignment/` signature modified; no new dependencies.
</success_criteria>

<output>
Create `.planning/phases/14-shift-registration/14-02-SUMMARY.md` when done.
</output>
