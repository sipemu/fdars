---
phase: 23-functional-boxplot-depth-dispatcher
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/depth/dispatch.rs
  - fdars-core/src/depth/mod.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [T-02]

estimate:
  tokens: 55000
  raw_tokens: 30000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "functional_depth(data, DepthMethod::FraimanMuniz{scale}) returns exactly fraiman_muniz_1d(data,data,scale) (per T-02); Band/ModifiedBand/RandomProjection each equal their underlying self-depth call"
    - "functional_boxplot flags a planted gross-outlier curve and does not flag inliers (per T-02)"
    - "The boxplot median equals the deepest curve; the central region brackets the median at every t"
    - "RandomProjection dispatch and boxplot are bit-reproducible under a fixed seed"
    - "Invalid input (empty matrix / too few curves) returns Err(FdarError), never a panic"
    - "All existing depth/outlier public signatures are unchanged; the dispatcher only wraps them (per T-02)"
  artifacts:
    - fdars-core/src/depth/dispatch.rs
  key_links:
    - "DepthMethod enum variants map 1:1 to fraiman_muniz_1d / band_1d / modified_band_1d / random_projection_1d_seeded"
    - "functional_depth and functional_boxplot re-exported at crate root in lib.rs"
    - "functional_boxplot depends on functional_depth for ranking curves"
---

<objective>
Deliver requirement T-02: a unified `functional_depth(data, method: DepthMethod)` self-depth dispatcher over the existing depth functions, plus the canonical López-Pintado–Romo depth-fence `functional_boxplot` producing numeric outputs only (no plotting). Both additive, `Result`-returning, crate-root re-exported, with inline tests. Existing depth/outlier signatures stay unchanged.

Purpose: Closes the top-ranked (score 5.00) R-parity table-stakes gap — `roahd`/`fdaoutlier`/`fda.usc` functional boxplot fences + a general depth dispatcher — by wrapping infrastructure fdars already ships.
Output: New `fdars-core/src/depth/dispatch.rs` holding `DepthMethod`, `functional_depth`, `FunctionalBoxplotResult`, and `functional_boxplot`; wired into `depth/mod.rs` and re-exported at the crate root.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-functional-boxplot-depth-dispatcher/23-CONTEXT.md

# Depth functions the dispatcher wraps (signatures are load-bearing — do not change them)
@fdars-core/src/depth/fraiman_muniz.rs
@fdars-core/src/depth/band.rs
@fdars-core/src/depth/random_projection.rs

# Result-struct + serde-gate + crate-root re-export conventions to mirror
@fdars-core/src/outliers.rs
@fdars-core/src/depth/mod.rs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: DepthMethod enum + functional_depth self-depth dispatcher (end-to-end vertical slice)</name>
  <files>fdars-core/src/depth/dispatch.rs, fdars-core/src/depth/mod.rs, fdars-core/src/lib.rs</files>
  <behavior>
    - functional_depth(data, DepthMethod::FraimanMuniz{scale: true}) equals fraiman_muniz_1d(&data, &data, true) element-for-element
    - functional_depth(data, DepthMethod::FraimanMuniz{scale: false}) equals fraiman_muniz_1d(&data, &data, false)
    - functional_depth(data, DepthMethod::Band) equals band_1d(&data, &data)
    - functional_depth(data, DepthMethod::ModifiedBand) equals modified_band_1d(&data, &data)
    - functional_depth(data, DepthMethod::RandomProjection{nproj, seed}) equals random_projection_1d_seeded(&data, &data, nproj, Some(seed)); two calls with the same seed are bit-identical
    - functional_depth on an empty matrix (0 rows or 0 cols) returns Err(FdarError::InvalidDimension{..})
    - Returned Vec length equals data.nrows() for every valid method
  </behavior>
  <action>
    Create `fdars-core/src/depth/dispatch.rs`. Define `pub enum DepthMethod` deriving Debug, Clone, Copy, PartialEq and marked `#[non_exhaustive]`, with variants carrying per-method params: `FraimanMuniz { scale: bool }`, `Band`, `ModifiedBand`, `RandomProjection { nproj: usize, seed: u64 }` (per D — 23-CONTEXT decisions). Define `pub fn functional_depth(data: &FdMatrix, method: DepthMethod) -> Result<Vec<f64>, FdarError>` computing SELF-depth (pass `data` as both object and reference). Validate first: if `data.nrows() == 0` or `data.ncols() == 0` return `Err(FdarError::InvalidDimension{..})` with a descriptive parameter/expected/actual; for Band/ModifiedBand require `nrows() >= 2` (a band needs two reference curves) else `Err`. Then `match method` and delegate: `FraimanMuniz{scale}` -> `fraiman_muniz_1d(data, data, scale)`; `Band` -> `band_1d(data, data)`; `ModifiedBand` -> `modified_band_1d(data, data)`; `RandomProjection{nproj, seed}` -> `random_projection_1d_seeded(data, data, nproj, Some(seed))` (note the underlying fn takes `Option<u64>`; wrap the enum's `u64` in `Some` for determinism, and reject `nproj == 0` with `Err(FdarError::InvalidParameter{..})`). Return the wrapped `Vec<f64>` in `Ok`. Do NOT add `#[must_use]` on the `Result`-returning fn. Write rustdoc noting SELF-depth semantics (each curve's depth w.r.t. the sample). Register `pub mod dispatch;` in `depth/mod.rs` and add `pub use dispatch::{DepthMethod, functional_depth};` to the module's re-export block. In `lib.rs`, extend the existing `pub use depth::{...}` block (around line 413) to also export `functional_depth` and `DepthMethod`. Inline `#[cfg(test)] mod tests` proving each per-method equality against a direct call to the underlying function (build a small deterministic FdMatrix via `FdMatrix::from_column_major`), the RandomProjection fixed-seed reproducibility, and the empty-matrix/too-few-curves/`nproj==0` error paths. This is the tracer: one thin path (FraimanMuniz) must be wired all the way from `functional_depth` through the underlying fn and asserted end-to-end before the other variants are filled in.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel dispatch</automated>
  </verify>
  <done>functional_depth + DepthMethod compile, are re-exported at the crate root, each variant equals its underlying self-depth call in tests, RandomProjection is seed-reproducible, and invalid input returns Err.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: functional_boxplot depth-fence boxplot with planted-outlier detection</name>
  <files>fdars-core/src/depth/dispatch.rs, fdars-core/src/lib.rs</files>
  <behavior>
    - On a sample of tight inlier curves plus one planted gross-outlier curve, functional_boxplot returns `outliers` containing the planted curve's row index and none of the inlier indices
    - `median` equals the deepest curve (the data row whose depth is maximal)
    - central_lower[t] <= median[t] <= central_upper[t] for every t (central region brackets the median)
    - whisker_lower[t] <= central_lower[t] and whisker_upper[t] >= central_upper[t] for every t (fence contains the central region)
    - With DepthMethod::RandomProjection{..} at a fixed seed, two boxplot calls are bit-identical
    - functional_boxplot on an empty matrix or fewer than 2 curves returns Err(FdarError)
  </behavior>
  <action>
    In `dispatch.rs`, add `pub struct FunctionalBoxplotResult` deriving Debug, Clone, PartialEq and serde-gated via `#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]` (mirror the serde gate at `inference/mod.rs:48`), marked `#[non_exhaustive]`, with fields: `median: Vec<f64>`, `central_lower: Vec<f64>`, `central_upper: Vec<f64>`, `whisker_lower: Vec<f64>`, `whisker_upper: Vec<f64>`, `outliers: Vec<usize>`, `depths: Vec<f64>`. Add `pub fn functional_boxplot(data: &FdMatrix, method: DepthMethod, factor: f64) -> Result<FunctionalBoxplotResult, FdarError>`. Algorithm (canonical López-Pintado–Romo depth-fence, numeric only — per 23-CONTEXT decisions): (1) validate non-empty and `nrows() >= 2`, `factor >= 0.0`, else `Err`; (2) `depths = functional_depth(data, method)?`; (3) median row = the argmax of `depths`; `median` = that row's values across all columns; (4) select the deepest 50% of rows by depth (`ceil(n/2)` curves, ties broken by index for determinism); (5) `central_lower[t]` / `central_upper[t]` = pointwise min / max over those deepest-half rows at column `t`; (6) at each `t`, `width = central_upper[t] - central_lower[t]`, `whisker_lower[t] = central_lower[t] - factor*width`, `whisker_upper[t] = central_upper[t] + factor*width`; (7) `outliers` = row indices of any curve whose value at ANY column `t` falls below `whisker_lower[t]` or above `whisker_upper[t]`. Extract per-row/column values via `FdMatrix` indexing `data[(i, t)]`. Default recommendation is `method = ModifiedBand`, `factor = 1.5` — document in rustdoc (the caller passes these explicitly; do not add a separate defaulted overload). No `#[must_use]` on the `Result` fn. In `lib.rs`, extend the `pub use depth::{...}` block to also export `functional_boxplot` and `FunctionalBoxplotResult`. Inline tests: construct inlier curves (e.g. mild sinusoids on a uniform grid) plus one curve shifted far outside the band; assert the planted index is flagged and inliers are not; assert median-equals-deepest, central-brackets-median, fence-contains-central; assert RandomProjection fixed-seed reproducibility of the whole result; assert empty / single-curve / negative-factor inputs return Err.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel functional_boxplot</automated>
  </verify>
  <done>functional_boxplot + FunctionalBoxplotResult compile, are re-exported at the crate root, flag a planted outlier while sparing inliers, satisfy the median/central/fence orderings, are seed-reproducible for RandomProjection, and reject invalid input with Err.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library caller → functional_depth/functional_boxplot | Untrusted `FdMatrix` dimensions and `factor`/`nproj` values cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Denial of Service | functional_depth / functional_boxplot | low | mitigate | Validate matrix non-empty and `nrows() >= 2`, `nproj > 0`, `factor >= 0.0` at entry; return `Err(FdarError)` before any allocation or compute (no panic, no unbounded loop) |
| T-23-02 | Tampering | RandomProjection determinism | low | mitigate | Route seed through `random_projection_1d_seeded(.., Some(seed))` so results are reproducible and not silently non-deterministic |

No package installs in this plan (no `T-{phase}-SC` needed).
</threat_model>

<verification>
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel` is green (full suite, including the new inline tests)
- `cargo clippy --all-targets --features linalg,parallel -- -D warnings` is clean
- Every existing depth/outlier public signature is unchanged (grep confirms `fraiman_muniz_1d`, `band_1d`, `modified_band_1d`, `random_projection_1d_seeded`, `outliergram` untouched)
</verification>

<success_criteria>
- `functional_depth(data, method)` + `DepthMethod` and `functional_boxplot(data, method, factor)` + `FunctionalBoxplotResult` exist in `depth/dispatch.rs`, are `Result`-returning, and are re-exported at the crate root
- Per-method dispatcher equalities, planted-outlier detection, median/central/fence orderings, fixed-seed reproducibility, and input-validation error paths all pass as inline tests
- Additive/non-breaking: no existing signature changed; full suite + clippy stay green
</success_criteria>

<output>
Create `.planning/phases/23-functional-boxplot-depth-dispatcher/23-01-SUMMARY.md` when done
</output>
