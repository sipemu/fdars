---
milestone: v0.26.0
audited: 2026-08-21
status: passed
scores:
  requirements: 8/8
  phases: 2/2
  integration: 1/1
  flows: 2/2
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 37-specialized-fpca-variants
    items:
      - "VALIDATION.md status: draft (NOT-VALIDATED per #2117 — Nyquist coverage TODO; run /gsd-validate-phase 37 to reconcile). Carried-forward pattern from prior milestones."
      - "Intentional R-baseline divergences documented in rustdoc: fdars fpca_der differentiates curves-then-FPCA vs fdapace::FPCAder (differentiates eigenfunctions); fsvd uses Gram-matrix symmetric-eigendecomposition instead of nalgebra general SVD (the latter fails to converge on near-rank-deficient cross-covariance)."
  - phase: 38-sparse-fast-covariance-trajectory-bands
    items:
      - "VALIDATION.md status: draft (NOT-VALIDATED per #2117 — Nyquist coverage TODO; run /gsd-validate-phase 38 to reconcile)."
      - "Intentional R-baseline divergence documented in rustdoc: FACE/mfaces are kernel-sandwich approximations (K-FACE) of refund::face / mfaces penalized-spline (P-FACE) — matched by capability, dependency-free."
      - "Documented empirical tolerances calibrated during implementation: face_covariance dense-limit 0.30 (OU ridge smoothing bias), mface cross-structure 0.40, trajectory-band coverage ≥0.85 (pointwise 95% BLUP bands undercover in finite samples)."
nyquist:
  compliant_phases: 0
  partial_phases: 0
  not_validated_phases: 2
  missing_phases: 0
  overall: not-validated
---

# Milestone v0.26.0 — FPCA Breadth & Sparse Covariance — Audit

**Status: PASSED** — 8/8 requirements satisfied, 2/2 phases verified, cross-phase integration confirmed, no critical gaps.

## Definition of Done

Ship the two remaining top-ranked `R-BACKLOG.md` items (score 1.73, M-effort) — FPCA-02 (specialized FPCA variants) and SPARSE-01 (fast sparse/irregular covariance + trajectory bands) — as additive, `Result`-returning, crate-root-re-exported `fdars-core` code with inline `#[cfg(test)]` tests, **zero changes to existing public signatures**, and **no new crate dependency**.

## Requirements Coverage (3-source cross-reference)

| Req ID | Description | Phase | VERIFICATION | SUMMARY frontmatter | Status |
|--------|-------------|-------|--------------|---------------------|--------|
| FPCA-02-01 | `fpca_der` FPCA of derivatives | 37 | passed | 37-01 | ✅ satisfied |
| FPCA-02-02 | `fsvd` functional SVD / cross-FPCA | 37 | passed | 37-02 | ✅ satisfied |
| FPCA-02-03 | `cross_covariance` surface | 37 | passed | 37-01 | ✅ satisfied |
| FPCA-02-04 | `dynamical_correlation` scalar | 37 | passed | 37-02 | ✅ satisfied |
| FPCA-02-05 | `ssvd` sandwich-smoother FPCA | 37 | passed | 37-02 | ✅ satisfied |
| SPARSE-01-01 | `face_covariance` FACE sandwich | 38 | passed | 38-01 | ✅ satisfied |
| SPARSE-01-02 | `mface_covariance` multivariate | 38 | passed | 38-02 | ✅ satisfied |
| SPARSE-01-03 | `face_trajectory` fitted + bands | 38 | passed | 38-02 | ✅ satisfied |

**8/8 satisfied.** No unsatisfied, partial, or orphaned requirements. REQUIREMENTS.md checkboxes updated to `[x]` / traceability status `Complete`.

## Phase Verification Summary

| Phase | Plans | Verification | Score |
|-------|-------|--------------|-------|
| 37 — Specialized FPCA Variants | 2/2 | passed | 5/5 must-haves |
| 38 — Sparse Fast Covariance & Trajectory Bands | 2/2 | passed | 5/5 must-haves |

Both verifiers independently re-ran the full gate: `cargo test -p fdars-core --features linalg,parallel` (2414 lib + integration + doctests, 0 failures) and `cargo clippy --all-targets --features linalg,parallel -- -D warnings` (clean).

## Cross-Phase Integration

The two phases touch disjoint modules (`fpca_variants.rs` vs `irreg_fdata/face.rs`) — no runtime routes or E2E user flows (pure numeric library). One intra-crate integration link:

- **Phase 38 → Phase 37 reuse:** `irreg_fdata/face.rs` imports and calls `fpca_variants::gaussian_smooth_cov` (bumped to `pub(crate)` in Phase 37's file). Verified: import resolves (face.rs:25), used at face.rs:149, and the whole crate compiles + all 2414 tests pass.
- **Crate-root reachability:** all 8 new public symbols re-exported (`lib.rs:232` FACE family, `lib.rs:442` FPCA-variants family); `smoke_reexports` / `test_reexports` assert reachability.
- **Non-breaking:** `fdars-core/Cargo.toml` unchanged (no new dependency); no existing public signature modified (the only existing-item change is the private→`pub(crate)` visibility bump).

## Nyquist Coverage

| Phase | VALIDATION.md | status | Classification | Action |
|-------|---------------|--------|----------------|--------|
| 37 | exists | draft | NOT-VALIDATED | `/gsd-validate-phase 37` (optional) |
| 38 | exists | draft | NOT-VALIDATED | `/gsd-validate-phase 38` (optional) |

Both VALIDATION.md files are `status: draft` — seeded by plan-phase, not reconciled by validate-phase (NOT-VALIDATED per #2117, a coverage TODO, **not** a compliance failure). Every requirement already has automated inline `#[cfg(test)]` verification; this is a carried-forward advisory pattern (prior milestones' VALIDATION.md files also remain draft). Non-blocking.

## Tech Debt (non-blocking)

- Both phases' VALIDATION.md remain `status: draft` (Nyquist reconciliation TODO).
- Intentional, rustdoc-documented R-baseline divergences (K-FACE vs P-FACE; fpca_der curve-first convention; fsvd Gram-eigendecomposition robustness workaround for nalgebra SVD non-convergence).
- Documented empirically-calibrated test tolerances (face dense-limit 0.30, mface cross 0.40, trajectory coverage ≥0.85).

**Also pending (operator ship-step, not a v0.26.0 gap):** crate release for v0.23.0 + v0.24.0 + v0.25.0 + v0.26.0 — `fdars-core/Cargo.toml` still at 0.24.0; a version bump + `v*` tag triggers the crates.io publish. All four milestones shipped real code.

## Verdict

Milestone v0.26.0 achieved its definition of done. All 8 requirements satisfied with passing phase verifications, confirmed cross-phase reuse, and a green whole-crate gate. No critical gaps. Proceed to complete-milestone.
