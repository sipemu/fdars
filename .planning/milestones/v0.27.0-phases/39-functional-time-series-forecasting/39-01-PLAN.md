---
phase: 39-functional-time-series-forecasting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/fts/forecast.rs
  - fdars-core/src/fts/mod.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [FTS-01-01, FTS-01-02]
estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "A user can call ftsm(data, ncomp, argvals) and get an FtsmResult carrying mean, FPC loadings, score-time-series, reconstructed fitted curves, weights, ncomp, and per-component AR diagnostics (FTS-01-01)."
    - "Fitted curves reconstructed by ftsm recover the input series within a documented relative-L2 tolerance on synthetic data (FTS-01-01)."
    - "A user can call ftsm_forecast(fit, h, argvals) to obtain an h-step FPC-score forecast reconstructed into a forecast curve; on a synthetic AR-score series the forecast score recovers the AR one-step prediction and the forecast beats a naive last-curve baseline (FTS-01-02)."
    - "Invalid inputs (empty/short series, n <= ncomp, ncomp == 0, argvals mismatch, h < 1, degenerate columns) return FdarError, never panic."
    - "ftsm is deterministic — two identical calls return bit-identical FtsmResult (Yule-Walker AR fit)."
  artifacts:
    - "fdars-core/src/fts/forecast.rs (new file with inline #[cfg(test)] mod tests)"
    - "ftsm, ftsm_forecast public entry points"
    - "FtsmResult, FtsmForecastResult, ArModelResult public result structs"
    - "private scalar_acov, levinson_durbin_yw, ArModel::fit, ArModel::forecast helpers"
    - "mod forecast; + pub use forecast::{...} wiring in fts/mod.rs; crate-root re-export in src/lib.rs"
  key_links:
    - "ftsm delegates FPCA to regression::fdata_to_pc_1d and reconstruction to FpcaResult::reconstruct (no custom SVD)."
    - "AR score model fit via private ArModel using scalar_acov + levinson_durbin_yw + AIC order selection (no new dependency)."
    - "ftsm_forecast reconstructs the forecast curve via mean + Σ_k forecast_score_k · rotation_k truncated to ncomp."
---

<objective>
Deliver the end-to-end functional time-series tracer: a new `fdars-core/src/fts/forecast.rs` module wired into `fts/mod.rs` and the crate root, containing the FPCA-based `ftsm` fit (FTS-01-01) and the one-step FPC-score-AR `ftsm_forecast` (FTS-01-02), with the private Yule-Walker AR machinery (`scalar_acov`, `levinson_durbin_yw`, `ArModel`).

This is the tracer slice: it wires ONE forecasting path through every layer the phase touches — module scaffolding + wiring, FPCA delegation, AR score model, curve reconstruction — proven end-to-end by the AR(1) synthetic-series recovery test before Plan 02/03 expand to multi-step, dynamic update, and fplsr.

Purpose: Prove the architecture (FPCA delegate + per-score AR + reconstruction + module wiring) is sound on the agent's best early-context tokens, catching any dead-end after one plan rather than after all five entry points are written.
Output: New `forecast.rs`, wiring edits, `FtsmResult`/`FtsmForecastResult`/`ArModelResult` structs, `ftsm` + `ftsm_forecast` functions, inline tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-functional-time-series-forecasting/39-CONTEXT.md
@.planning/phases/39-functional-time-series-forecasting/39-RESEARCH.md
@.planning/phases/39-functional-time-series-forecasting/39-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols introduced across Phase 39 (this plan creates the file + the starred symbols):
- New file: `fdars-core/src/fts/forecast.rs` ★
- Public entry points: `ftsm` ★, `ftsm_forecast` ★, `ftsm_forecast_multistep` (Plan 02), `ftsm_update` (Plan 02), `fplsr` (Plan 03)
- Public result structs: `FtsmResult` ★, `FtsmForecastResult` ★, `ArModelResult` ★, `FplsrResult` (Plan 03)
- Private helpers: `validate_fts_input` ★, `mean_curve` ★, `scalar_acov` ★, `levinson_durbin_yw` ★ (Yule-Walker AR-coefficient solver), `ArModel` struct ★, `ArModel::fit` ★ (AIC order selection), `ArModel::forecast` ★ (iterative plug-in)
- Wiring: `mod forecast; pub use forecast::{...}` in `fts/mod.rs` ★; result-struct declarations in `fts/mod.rs` ★; crate-root re-export block in `src/lib.rs` ★
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Module scaffold + wiring + private Yule-Walker AR helpers</name>
  <files>fdars-core/src/fts/forecast.rs, fdars-core/src/fts/mod.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/fts/mod.rs (lines 1-24: module-doc + `mod acf; pub use acf::{...}` wiring pattern; lines 26-75: FacfResult/StationarityResult/LongRunCovResult struct declaration style to mirror)
    - fdars-core/src/lib.rs (lines 251-255: existing `pub use fts::{...}` crate-root re-export block to extend)
    - fdars-core/src/fts/acf.rs (lines 25-42: validate_fts_input verbatim; lines 47-58: mean_curve verbatim; lines 63-95: 1/n autocovariance normalization convention; lines 173-201: durbin_levinson_pacf structural pattern + `den.abs() < 1e-12` early-exit guard at 189-193)
    - fdars-core/src/helpers.rs (line 4: NUMERICAL_EPS constant)
    - .planning/phases/39-functional-time-series-forecasting/39-PATTERNS.md (lines 117-213: scalar_acov, levinson_durbin_yw, ArModel struct, AIC/p_max formulas — copy these concrete excerpts)
    - .planning/phases/39-functional-time-series-forecasting/39-RESEARCH.md (lines 244-299: Yule-Walker + Levinson-Durbin + AIC spec; lines 578-587: short-series divergence pitfall)
  </read_first>
  <precondition>Build/doctest linking requires TMPDIR=/home/simonm/.cache/fdars-bench-tmp (default /tmp tmpfs exhausts and produces a bogus "No space left"); export it before any cargo build/test/doctest invocation.</precondition>
  <action>
    Create `fdars-core/src/fts/forecast.rs` with the module-doc header from PATTERNS.md (module purpose, R baseline ftsa, divergences: no pre-smoothing, user-provided ncomp, AR-only score model, point forecasts only). Add imports: FdarError, FdMatrix, NUMERICAL_EPS, and `use crate::regression::fdata_to_pc_1d`. Wire the module into `fdars-core/src/fts/mod.rs` by adding `mod forecast;` after the existing `mod acf;` and declaring the public result structs there (`FtsmResult`, `FtsmForecastResult`, `ArModelResult` — the fplsr struct is added in Plan 03) with the exact derive chain used by FacfResult: `#[derive(Debug, Clone, PartialEq)]` + `#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]` + `#[non_exhaustive]`, in that order, with per-field doc comments. FtsmResult fields: mean (Vec<f64>), rotation (FdMatrix m x ncomp), scores (FdMatrix n x ncomp), fitted (FdMatrix n x m), weights (Vec<f64>), ncomp (usize), ar_models (Vec<ArModelResult>). ArModelResult fields: order (usize), phi (Vec<f64>), sigma2 (f64). FtsmForecastResult fields: forecast (FdMatrix h x m), h (usize). Extend the `pub use forecast::{...}` line in mod.rs and the crate-root `pub use fts::{...}` block in `src/lib.rs` to export ftsm, ftsm_forecast, FtsmResult, FtsmForecastResult, ArModelResult (leave placeholders for the Plan 02/03 symbols out until those plans add them). Re-implement `validate_fts_input` and `mean_curve` verbatim as private helpers in forecast.rs (do NOT modify acf.rs). Implement private `scalar_acov(series, mean, max_lag) -> Vec<f64>` using the 1/n normalization convention. Implement private `levinson_durbin_yw(gamma) -> Result<(Vec<f64>, f64), FdarError>` returning (phi coefficients, sigma2 residual variance): return `(vec![], gamma[0])` for order 0; return ComputationFailed with operation "levinson_durbin_yw" when `gamma[0].abs() < NUMERICAL_EPS`; guard the recursion with `nu[k-1].abs() < 1e-12` early-exit collapsing to the order-(k-1) model (mirror acf.rs guard). Implement the private `ArModel` struct (fields phi, sigma2, mean, order, history) with `#[derive(Debug, Clone, PartialEq)]`, plus `ArModel::fit(series, n) -> Result<ArModel, FdarError>` doing p_max = ((10*log10(n)).floor() as usize).min(n-1).min(n/4).max(1), centering the series, computing gamma via scalar_acov, selecting order by AIC = n*ln(sigma2_p) + 2*p over p in 0..=p_max, and storing the last `order` raw observations (oldest first) as history; and `ArModel::forecast(&self, h) -> Vec<f64>` doing the iterative plug-in recursion (mean-shift in, AR combination, mean-shift out, slide window mixing history with already-computed forecasts). Do not add rand/rand_distr imports — this path is fully deterministic.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --features linalg,parallel 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `fdars-core/src/fts/forecast.rs` exists and contains `fn scalar_acov`, `fn levinson_durbin_yw`, and `struct ArModel` with `fn fit` and `fn forecast`.
    - `fdars-core/src/fts/mod.rs` contains `mod forecast;` and declarations for `pub struct FtsmResult`, `pub struct FtsmForecastResult`, `pub struct ArModelResult` each with the four-attribute derive chain (Debug/Clone/PartialEq, serde cfg_attr, non_exhaustive).
    - `fdars-core/src/lib.rs` `pub use fts::{...}` block references `ftsm`, `ftsm_forecast`, `FtsmResult`, `FtsmForecastResult`, `ArModelResult` (compiles once Task 2 supplies the fns; this task may leave `ftsm`/`ftsm_forecast` as `pub fn` stubs returning todo!()-free minimal bodies OR add the exports in Task 2 — the executor keeps the crate compiling at each commit).
    - `levinson_durbin_yw` returns `Err(FdarError::ComputationFailed{ operation: "levinson_durbin_yw", .. })` when `gamma[0].abs() < NUMERICAL_EPS`.
    - `cargo build -p fdars-core --features linalg,parallel` (with TMPDIR set) exits 0.
  </acceptance_criteria>
  <behavior>
    - scalar_acov([c;n], mean, L) returns gamma[0..=L] with gamma[0] = variance and monotone-plausible decay.
    - levinson_durbin_yw on gamma from a strong AR(1) (gamma[1]/gamma[0] ≈ 0.8) returns phi ≈ [0.8] and sigma2 > 0.
    - ArModel::fit on a deterministic AR(1) series selects order >= 1 and phi[0] within ~0.1 of the true coefficient.
    - ArModel::forecast(1) on that series returns mean + phi·(last_centered) ≈ 0.8·last_value for a zero-mean series.
  </behavior>
  <reversibility rating="reversible">New additive private helpers + struct wiring on an unreleased-since-v0.24.0 crate; no public signature changes.</reversibility>
  <done>forecast.rs exists with AR helpers; module wired into mod.rs + lib.rs; crate builds green with linalg,parallel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ftsm fit — FPCA decompose + per-component AR + fitted reconstruction (FTS-01-01)</name>
  <files>fdars-core/src/fts/forecast.rs</files>
  <read_first>
    - fdars-core/src/regression.rs (lines 25-38: FpcaResult fields; lines 81-103: project; lines 142-170: reconstruct; lines 287-321: fdata_to_pc_1d signature + ncomp clamping)
    - fdars-core/src/fts/acf.rs (lines 25-42: validate_fts_input; lines 253/403 area: `#[must_use]` string convention)
    - .planning/phases/39-functional-time-series-forecasting/39-PATTERNS.md (lines 231-280: validation sequence + fdata_to_pc_1d call pattern + reconstruct call; lines 470-498: must_use + Simpson-weights-reuse rules)
    - .planning/phases/39-functional-time-series-forecasting/39-RESEARCH.md (lines 200-241: ftsm decomposition spec + FtsmResult; lines 626-649: T-01/T-02 test specs)
    - fdars-core/src/scoring.rs (lines 59-99: functional_mse signature for the fitted-recovery test)
  </read_first>
  <action>
    Implement `pub fn ftsm(data: &FdMatrix, ncomp: usize, argvals: &[f64]) -> Result<FtsmResult, FdarError>` with `#[must_use = "expensive computation whose result should not be discarded"]`. Validation order (return FdarError, never panic): validate_fts_input(data, argvals)?; `ncomp == 0` -> InvalidParameter; `n <= ncomp` -> InvalidParameter with message referencing ncomp and n; `n < 2` -> InvalidParameter (need >= 2 obs for AR). Delegate FPCA: `let fpca = fdata_to_pc_1d(data, ncomp, argvals)?;` and reconstruct fitted via `fpca.reconstruct(&fpca.scores, ncomp)?`. Use `fpca.rotation.ncols()` as the effective ncomp (fdata_to_pc_1d silently clamps ncomp = ncomp.min(n).min(m); store the clamped value). For each component k in 0..effective_ncomp, extract the score series with `fpca.scores.column(k)` (contiguous, length n — do NOT transpose), fit `ArModel::fit(column_k, n)?`, and map it to an ArModelResult { order, phi, sigma2 }. Store the private Vec<ArModel> for use by ftsm_forecast — since FtsmResult must derive PartialEq and expose only diagnostics, keep the private ArModel vector re-derivable: EITHER store the fitted `Vec<ArModel>` in a private field of FtsmResult (acceptable — ArModel derives PartialEq) OR have ftsm_forecast re-fit from fit.scores. Choose storing the private Vec<ArModel> on FtsmResult (add a non-pub field to the struct declared in mod.rs is not possible across modules — so declare FtsmResult to also carry a `pub ar_models: Vec<ArModelResult>` for diagnostics AND reconstruct ArModel from ArModelResult + fit.scores history inside ftsm_forecast). Populate FtsmResult { mean: fpca.mean, rotation: fpca.rotation, scores: fpca.scores, fitted, weights: fpca.weights (do not recompute Simpson weights), ncomp: effective_ncomp, ar_models }.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel fts::forecast::tests::ftsm_ 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `forecast.rs` contains `pub fn ftsm` returning `Result<FtsmResult, FdarError>`.
    - Inline tests include an `ftsm_`-prefixed test asserting fitted-curve recovery: `functional_mse(&data, &fit.fitted, &argvals) < 0.01 * data_variance` on synthetic full-rank data (T-02 tolerance 1%).
    - Error-path tests: empty data -> InvalidDimension; n <= ncomp -> InvalidParameter; argvals length mismatch -> InvalidDimension.
    - Determinism test: `ftsm(&data, ncomp, &argvals)? == ftsm(&data, ncomp, &argvals)?` (bit-identical, T-07).
    - `cargo test ... fts::forecast::tests::ftsm_` (with TMPDIR set) exits 0.
  </acceptance_criteria>
  <behavior>
    - Test ftsm_fitted_recovers_input: full-rank synthetic curves reconstructed within 1% relative-L2.
    - Test ftsm_deterministic: two identical calls produce equal FtsmResult.
    - Test ftsm_rejects_ncomp_ge_n: n=3, ncomp=5 returns InvalidParameter.
    - Test ftsm_rejects_empty: 0-row matrix returns InvalidDimension.
  </behavior>
  <reversibility rating="reversible">New additive public fn; no existing signature touched.</reversibility>
  <done>ftsm fits, reconstructs fitted curves within 1% tolerance, is deterministic, and rejects invalid inputs with FdarError; ftsm_ tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ftsm_forecast — one-step FPC-score AR forecast + curve reconstruction (FTS-01-02)</name>
  <files>fdars-core/src/fts/forecast.rs</files>
  <read_first>
    - fdars-core/src/regression.rs (lines 142-170: FpcaResult::reconstruct — replicate mean[j] + Σ_k score_k · rotation[(j,k)] for a 1-row forecast score matrix)
    - .planning/phases/39-functional-time-series-forecasting/39-RESEARCH.md (lines 303-347: AR forecasting recursion + ftsm_forecast reconstruction spec; lines 626-662: T-01 AR-recovery + T-03 naive-baseline test specs)
    - .planning/phases/39-functional-time-series-forecasting/39-PATTERNS.md (lines 217-227: must_use forecast string)
    - fdars-core/src/scoring.rs (lines 59-99: functional_mse for the baseline comparison)
  </read_first>
  <action>
    Implement `pub fn ftsm_forecast(fit: &FtsmResult, h: usize, argvals: &[f64]) -> Result<FtsmForecastResult, FdarError>` with `#[must_use = "returns forecast result; result should be examined"]`. Validation: `h == 0` -> InvalidParameter { parameter: "h", message: "h must be >= 1" }; `argvals.len() != fit.mean.len()` -> InvalidDimension. For this plan implement the h=1 forecast (h>1 is delegated in Plan 02, but keep the signature accepting general h — return the h=1 curve as the single row for now and note that multi-step is completed in Plan 02; alternatively reconstruct one ArModel per component from fit.ar_models + fit.scores.column(k) history and produce a single-step forecast). For each component k in 0..fit.ncomp: reconstruct the ArModel from fit.ar_models[k] (order, phi, sigma2) plus the trailing history taken from fit.scores.column(k), then compute the one-step score forecast beta_hat_k = ArModel::forecast(1)[0]. Assemble forecast_scores as a 1 x ncomp FdMatrix and reconstruct the forecast curve as forecast[j] = fit.mean[j] + Σ_k forecast_scores[k] * fit.rotation[(j,k)] (same arithmetic as FpcaResult::reconstruct). Return FtsmForecastResult { forecast: 1 x m FdMatrix, h: 1 }. Document in rustdoc that iterative h>1 is provided by ftsm_forecast_multistep (Plan 02) and that ftsm_forecast is the single-step convenience.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel fts::forecast 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `forecast.rs` contains `pub fn ftsm_forecast` returning `Result<FtsmForecastResult, FdarError>`.
    - T-01 test (forecast_ prefix): synthetic curves with AR(1) score (phi=0.8, n>=80) — `|forecast_score - 0.8 * fit.scores[(n-1,0)]| < 0.05`.
    - T-03 test: on AR-structured data (phi=0.7), `functional_mse(true_next, ftsm_forecast_curve) < functional_mse(true_next, naive_last_curve)`.
    - Error-path: `ftsm_forecast(&fit, 0, &argvals)` returns `Err(FdarError::InvalidParameter{ parameter: "h", .. })`.
    - `cargo test ... fts::forecast` (with TMPDIR set) exits 0.
  </acceptance_criteria>
  <behavior>
    - Test forecast_recovers_ar_one_step: forecast score within 0.05 of 0.8·last-score on AR(1) synthetic data.
    - Test forecast_beats_naive_baseline: model MSE strictly less than naive last-curve MSE on AR(0.7) data.
    - Test forecast_rejects_h_zero: h=0 returns InvalidParameter.
  </behavior>
  <reversibility rating="reversible">New additive public fn; deterministic; no existing signature touched.</reversibility>
  <done>ftsm_forecast recovers the AR one-step score within tolerance, beats the naive baseline, rejects h<1; full fts::forecast module tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → ftsm/ftsm_forecast | Untrusted numeric input (FdMatrix, ncomp, argvals, h) crosses into the library. No network, filesystem, deserialization of untrusted data, or process spawning occurs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Tampering | ftsm / ftsm_forecast entry points | low | mitigate | Entry-point validation returns FdarError (InvalidDimension/InvalidParameter) for empty/short series, n <= ncomp, ncomp == 0, argvals mismatch, h < 1 — never panic, no UB. |
| T-39-02 | Tampering | levinson_durbin_yw (division by gamma(0)) | low | mitigate | Check `gamma[0].abs() < NUMERICAL_EPS` before recursion; return ComputationFailed. Guard `nu[k-1].abs() < 1e-12` early-exit for near-unit-root series. |
| T-39-03 | Denial of Service | AR order candidate range | low | mitigate | p_max capped at min(n-1, floor(10*log10(n)), n/4); no unbounded loops; usize::min prevents overflow. |
| T-39-04 | Tampering | NaN/Inf propagation from degenerate score series | low | accept | fdata_to_pc_1d/reconstruct handle finite math; degenerate columns surface as ComputationFailed or produce finite output — no memory-safety impact for a pure numeric library. |

No high-severity threats: pure in-process numeric library, no network/filesystem/deserialization/process surface. Nothing to block on per ASVS L1 (block-on: high).
</threat_model>

<verification>
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel fts::forecast` green.
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings` clean (test code included — CI gate).
- No changes to existing public signatures (fdata_to_pc_1d, scoring.rs, fts/acf.rs untouched); no new crate dependency in Cargo.toml.
</verification>

<success_criteria>
- ftsm fits an FPCA-based model recovering fitted curves within 1% relative-L2 (FTS-01-01).
- ftsm_forecast recovers the AR one-step score within 0.05 and beats the naive baseline (FTS-01-02).
- All invalid inputs return FdarError; ftsm is deterministic.
- Module compiles and lints clean under linalg,parallel with --all-targets.
</success_criteria>

<output>
Create `.planning/phases/39-functional-time-series-forecasting/39-01-SUMMARY.md` when done.
</output>
