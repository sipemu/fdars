---
phase: 37-specialized-fpca-variants
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/fpca_variants.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [FPCA-02-01, FPCA-02-03]
estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "cross_covariance(x, y) returns a p×q FdMatrix whose entries equal the sample-centered empirical cross-covariance with a 1/(n-1) divisor"
    - "cross_covariance rejects mismatched sample sizes, <2 rows, and p*q overflow with FdarError (never panics)"
    - "fpca_der(data, ncomp, argvals, nderiv) returns an FpcaResult of the differentiated process; with nderiv=0 it equals fdata_to_pc_1d"
    - "fpca_der validates inputs BEFORE calling deriv_1d (which silently returns zeros on bad input) and returns FdarError on empty matrix / bad argvals / ncomp<1"
    - "The new fpca_variants module compiles, is declared in lib.rs, and cross_covariance + fpca_der are reachable from the crate root"
  artifacts:
    - "fdars-core/src/fpca_variants.rs"
    - "pub mod fpca_variants; declaration in fdars-core/src/lib.rs"
    - "crate-root re-export: pub use fpca_variants::{cross_covariance, fpca_der, FsvdResult};"
  key_links:
    - "fpca_der delegates to fdata::deriv_1d then regression::fdata_to_pc_1d"
    - "cross_covariance centers X and Y separately per-grid (NOT via functional_covariance on concatenated data)"
    - "FsvdResult struct is defined here (Wave 0 dependency) so Plan 02's fsvd can populate it"
---

<objective>
Create the new `fdars-core/src/fpca_variants.rs` module, wire it into the crate root, and land the two simplest FPCA variants end-to-end: `cross_covariance` (the tracer — proves the module compiles, re-exports resolve, and inline tests run) and `fpca_der` (trivial composition of two verified functions). Also define the `FsvdResult` struct here so Plan 02's `fsvd` can populate it in Wave 2.

Purpose: Establish the module skeleton and prove the full path (new file → crate-root re-export → inline `#[cfg(test)]` tests green) on the agent's best early-context tokens, so the three heavier variants in Plan 02 expand out from a proven slice.
Output: `fpca_variants.rs` with `cross_covariance`, `fpca_der`, the `FsvdResult` struct definition; `lib.rs` module declaration + re-export block.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-37-specialized-fpca-variants/37-CONTEXT.md
@.planning/phases/phase-37-specialized-fpca-variants/37-RESEARCH.md
@.planning/phases/phase-37-specialized-fpca-variants/37-VALIDATION.md

# Reused source (read the exact signatures before implementing):
@fdars-core/src/regression.rs
@fdars-core/src/fdata.rs
@fdars-core/src/helpers.rs
@fdars-core/src/lib.rs
</context>

<artifacts_this_phase_produces>
This phase (Phases 37, across Plans 01+02) creates ALL of the following NEW symbols. Plan 01 creates the file, the module wiring, `FsvdResult`, `cross_covariance`, and `fpca_der`. Plan 02 adds `dynamical_correlation`, `fsvd`, `ssvd`.

- New file: `fdars-core/src/fpca_variants.rs`
- `pub mod fpca_variants;` in `fdars-core/src/lib.rs`
- Crate-root re-export line(s):
  - Plan 01 adds: `pub use fpca_variants::{cross_covariance, fpca_der, FsvdResult};`
  - Plan 02 extends the same re-export block to: `pub use fpca_variants::{cross_covariance, dynamical_correlation, fpca_der, fsvd, ssvd, FsvdResult};`
- `pub fn cross_covariance(x: &FdMatrix, y: &FdMatrix) -> Result<FdMatrix, FdarError>` (Plan 01)
- `pub fn fpca_der(data: &FdMatrix, ncomp: usize, argvals: &[f64], nderiv: usize) -> Result<FpcaResult, FdarError>` (Plan 01)
- `pub struct FsvdResult` (Plan 01) with fields: `singular_values: Vec<f64>`, `left_functions: FdMatrix` (p×ncomp), `right_functions: FdMatrix` (q×ncomp), `left_scores: FdMatrix` (n×ncomp), `right_scores: FdMatrix` (n×ncomp). Per RESEARCH Open Question #1 / Assumption A2: OMIT the `cross_cov` field (caller can call `cross_covariance` separately).
- `pub fn dynamical_correlation(x: &FdMatrix, y: &FdMatrix, argvals: &[f64]) -> Result<f64, FdarError>` (Plan 02)
- `pub fn fsvd(x: &FdMatrix, argvals_x: &[f64], y: &FdMatrix, argvals_y: &[f64], ncomp: usize) -> Result<FsvdResult, FdarError>` (Plan 02)
- `pub fn ssvd(data: &FdMatrix, ncomp: usize, argvals: &[f64], bandwidth: f64) -> Result<FpcaResult, FdarError>` (Plan 02)
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Create fpca_variants module, wire it into lib.rs, define FsvdResult, and land cross_covariance end-to-end</name>
  <files>fdars-core/src/fpca_variants.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/fdata.rs:358-395 (functional_covariance — the 1/(n-1) Bessel divisor, the m.checked_mul overflow guard pattern, and center_1d usage to mirror)
    - fdars-core/src/fdata.rs:212-237 (center_1d — subtracts column means, returns centered FdMatrix)
    - fdars-core/src/regression.rs:25-38 (FpcaResult struct — imported for later tasks; FsvdResult mirrors its derive attributes)
    - fdars-core/src/lib.rs:101 (pub mod regression;), :129 (pub mod pace_fpca;), :436 (pub use regression::{...} re-export line — model the insertion point and one-pub-use-per-module convention)
    - fdars-core/src/matrix.rs (FdMatrix: column-major, .shape() -> (nrows, ncols), .column(j), indexing data[(i,j)])
  </read_first>
  <behavior>
    - test_cross_cov_shape: cross_covariance on X (n×p) and Y (n×q) with p != q returns an FdMatrix of shape (p, q).
    - test_cross_cov_self: for a hand-built X, cross_covariance(&X, &X) equals fdata::functional_covariance(&X) elementwise within 1e-12 (same 1/(n-1) sample-centered estimator when the two samples coincide).
    - test_cross_cov_hand_computed: for a tiny explicit X (n=3, p=2) and Y (n=3, q=2) with hand-computed column means, each C[(s,t)] matches the hand-computed 1/(n-1) Σ(x_is - x̄_s)(y_it - ȳ_t) within 1e-12.
    - test_cross_cov_errors: mismatched sample sizes (nx != ny) → FdarError::InvalidDimension; n < 2 → FdarError::InvalidDimension; p or q == 0 → FdarError (never panics).
    - smoke_reexports (partial, extended in Plan 02): fdars_core::cross_covariance and fdars_core::fpca_der resolve from the crate root (compile-level reachability).
  </behavior>
  <action>
Create `fdars-core/src/fpca_variants.rs` with a module doc comment (`//!`) describing the specialized FPCA variants family and stating the milestone constraint (additive, reuses fdata_to_pc_1d + covariance.rs, no new crate dependency). Import `crate::error::FdarError`, `crate::matrix::FdMatrix`, `crate::regression::FpcaResult`, `crate::fdata`.

Define `pub struct FsvdResult` with standard derives `#[derive(Debug, Clone, PartialEq)]`, conditional serde `#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]`, and `#[non_exhaustive]` (matching the codebase convention for public result structs). Fields with rustdoc on each: `singular_values: Vec<f64>` (length ncomp), `left_functions: FdMatrix` (p×ncomp, unit functional L2 norm on argvals_x), `right_functions: FdMatrix` (q×ncomp, unit functional L2 norm on argvals_y), `left_scores: FdMatrix` (n×ncomp), `right_scores: FdMatrix` (n×ncomp). Do NOT add a cross_cov field (RESEARCH Open Question #1 resolution: omit; caller calls cross_covariance separately). Add a rustdoc note on the struct that fsvd (Plan 02) populates it.

Implement `pub fn cross_covariance(x: &FdMatrix, y: &FdMatrix) -> Result<FdMatrix, FdarError>` per the locked CONTEXT decision (sample-centered empirical, 1/(n-1) Bessel divisor). Mark `#[must_use]`. Steps: read `(nx, p) = x.shape()`, `(ny, q) = y.shape()`; validate `nx == ny` else `FdarError::InvalidDimension { parameter: "y", expected: nx rows, actual: ny }`; validate `nx >= 2` else `InvalidDimension`; validate `p > 0 && q > 0` else `InvalidDimension`; guard `p.checked_mul(q)` returning `InvalidParameter` on overflow (mirror the m.checked_mul(m) guard in functional_covariance at fdata.rs:368-375). Center X and Y SEPARATELY using their own column means — reuse `crate::fdata::center_1d` on each (do NOT concatenate and call functional_covariance). Build a `p×q` FdMatrix where `C[(s,t)] = (Σ_i xc[(i,s)] * yc[(i,t)]) / (nx - 1)`. Return `Ok(cov)`.

Wire the module into `fdars-core/src/lib.rs`: add `pub mod fpca_variants;` alongside the other `pub mod` declarations (keep alphabetical grouping near `pub mod fof_regression;` / `pub mod fts;` region — place it so the module list stays readable). Add a crate-root re-export line in the re-export section (near the `pub use regression::{...}` line at lib.rs:436): `pub use fpca_variants::{cross_covariance, fpca_der, FsvdResult};` — this line will be extended in Plan 02, so add a `// Re-export specialized FPCA variants (Phase 37)` comment above it. NOTE: `fpca_der` is added in Task 2 of this same plan, so add its `pub fn` stub-then-fill in Task 2; if wiring the re-export before Task 2's fn exists causes a compile error, add the re-export incrementally — but since Task 2 is in the same plan/commit sequence, prefer to land Task 1 + Task 2 functions before compiling the full re-export line. Add inline `#[cfg(test)] mod tests` implementing the behavior tests above using synthetic `FdMatrix` data and documented tolerances.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel test_cross_cov -- --test-threads=4</automated>
  </verify>
  <acceptance_criteria>
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel test_cross_cov` passes (test_cross_cov_shape, test_cross_cov_self, test_cross_cov_hand_computed, test_cross_cov_errors all green).
    - `fdars-core/src/fpca_variants.rs` exists; `pub mod fpca_variants;` present in lib.rs; `FsvdResult` and `cross_covariance` are defined.
    - cross_covariance(&X, &X) matches fdata::functional_covariance(&X) within 1e-12 in the self-test.
  </acceptance_criteria>
  <done>The module exists, is declared in lib.rs, compiles under linalg+parallel, and cross_covariance is reachable and correct end-to-end with green inline tests. FsvdResult is defined (5 fields, no cross_cov).</done>
  <reversibility rating="reversible">Additive new module; no existing signature changes. FsvdResult shape is the only forward-facing decision and can be extended (#[non_exhaustive]).</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fpca_der (FPCA of curve derivatives) with divergence rustdoc</name>
  <files>fdars-core/src/fpca_variants.rs</files>
  <read_first>
    - fdars-core/src/fdata.rs:852-875 (deriv_1d signature + silent-zeros behavior: returns FdMatrix::zeros on n==0 || m<2 || argvals.len()!=m; nderiv==0 returns data.clone())
    - fdars-core/src/regression.rs:287-400 (fdata_to_pc_1d — signature, its own n==0 / ncomp validation, and FpcaResult it returns)
    - fdars-core/src/regression.rs:929-958 (existing FPCA reconstruction-test pattern to mirror for the derivative test)
  </read_first>
  <behavior>
    - test_fpca_der_nderiv0: fpca_der(&data, ncomp, &argvals, 0) returns an FpcaResult whose singular_values and rotation equal fdata_to_pc_1d(&data, ncomp, &argvals) within 1e-12 (deriv_1d with nderiv=0 clones data, so the two paths are identical).
    - test_fpca_der: on sine curves with identical phase x_i(t)=sin(2πt) for all i, the differentiated curves are identical → fpca_der(&data, 1, &argvals, 1) leading singular value ≈ 0 (documented tolerance, e.g. 1e-8) because centered derivatives have zero variance. Prefer a mode-of-variation variant: curves x_i(t)=a_i·sin(2πt) with varying a_i → the leading derivative component reconstructs the differentiated curves within a documented tolerance (mirror ROADMAP success criterion 2).
    - test_fpca_der_errors: empty matrix (n==0) → FdarError; argvals.len() != m → FdarError; ncomp < 1 → FdarError; nderiv>0 with m<2 → FdarError. None panic and none silently return a zero FpcaResult.
  </behavior>
  <action>
Implement `pub fn fpca_der(data: &FdMatrix, ncomp: usize, argvals: &[f64], nderiv: usize) -> Result<FpcaResult, FdarError>` per the locked CONTEXT decision (differentiate curves first, then FPCA on the derivatives; reuse FpcaResult). Mark `#[must_use]`. Default nderiv handling: the parameter is required (default of 1 is a caller convention documented in rustdoc, not an Option).

CRITICAL (RESEARCH Pitfall 1): validate ALL inputs BEFORE calling deriv_1d, because deriv_1d silently returns a zero matrix on bad input rather than erroring. Read `(n, m) = data.shape()`. Guard: `n == 0` → `FdarError::InvalidDimension`; `m == 0` → `InvalidDimension`; `argvals.len() != m` → `InvalidDimension { parameter: "argvals", expected: m, actual: argvals.len() }`; `ncomp < 1` → `FdarError::InvalidParameter { parameter: "ncomp", message: "must be >= 1" }`; `nderiv > 0 && m < 2` → `InvalidParameter { parameter: "data", message: "need >= 2 columns for numerical derivative" }`. After validation, call `let deriv = crate::fdata::deriv_1d(data, argvals, nderiv);` then `crate::regression::fdata_to_pc_1d(&deriv, ncomp, argvals)` and return its result directly.

Rustdoc MUST document the divergence from fdapace (RESEARCH State-of-the-Art + CONTEXT decision): fdars differentiates the CURVES first then runs FPCA (eigenfunctions of the derivative process), whereas fdapace::FPCAder differentiates the eigenfunctions of an existing FPCA fit. State that the returned FpcaResult's loadings/scores/mean are of the differentiated process. Extend the inline `#[cfg(test)] mod tests` with the behavior tests above.

Ensure the crate-root re-export line in lib.rs (added in Task 1) now correctly resolves `fpca_der` (it is now defined). If Task 1 deferred the fpca_der part of the re-export to avoid a compile error, complete it here so the line reads `pub use fpca_variants::{cross_covariance, fpca_der, FsvdResult};`.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel test_fpca_der -- --test-threads=4</automated>
  </verify>
  <acceptance_criteria>
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel test_fpca_der` passes (test_fpca_der, test_fpca_der_nderiv0, test_fpca_der_errors).
    - fpca_der with nderiv=0 equals fdata_to_pc_1d within 1e-12.
    - Rustdoc on fpca_der explicitly documents the fdapace FPCAder divergence.
    - fpca_der is reachable as fdars_core::fpca_der.
  </acceptance_criteria>
  <done>fpca_der validates inputs before deriv_1d, delegates to deriv_1d + fdata_to_pc_1d, returns an FpcaResult of the differentiated process, documents the fdapace divergence, and its inline tests are green.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-1 gate — full suite + clippy green for the new module</name>
  <files>fdars-core/src/fpca_variants.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - .planning/phases/phase-37-specialized-fpca-variants/37-VALIDATION.md (Sampling Rate — per-wave-merge full suite command + clippy gate)
  </read_first>
  <action>
Run the per-wave gate for the new module so Plan 02 (Wave 2) expands from a green base. Confirm: (1) the whole crate still compiles and all existing tests pass unchanged (additive/non-breaking — zero changes to existing public signatures), and (2) the CI-equivalent clippy gate is clean including test/bench code (a plain `-p ... -D warnings` false-greens per MEMORY.md, so `--all-targets` is mandatory). Fix any fmt/clippy drift in `fpca_variants.rs` before finishing. Use `TMPDIR=/home/simonm/.cache/fdars-bench-tmp` for build/doctest linking to avoid /tmp tmpfs exhaustion. If committing docs-only sub-steps, `--no-verify` is acceptable, but this CODE commit must pass fmt + clippy + tests (run `cargo fmt -p fdars-core` before committing to avoid the known --no-verify fmt-drift issue).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo fmt -p fdars-core --check && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt -p fdars-core --check` clean.
    - `cargo clippy --all-targets --features linalg,parallel -- -D warnings` clean (includes test code).
    - `cargo test -p fdars-core --features linalg,parallel` fully green (existing suite unchanged + new cross_covariance/fpca_der tests).
  </acceptance_criteria>
  <done>Whole crate compiles, full suite green, clippy --all-targets clean; Plan 02 can expand from a proven, green base.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → public fn args | The only untrusted input is malformed function arguments (empty matrix, mismatched sample sizes, bad argvals length, ncomp out of range, degenerate columns). No network / file / process I/O; no untrusted deserialization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Tampering (DoS via OOM) | cross_covariance p×q allocation | low | mitigate | `p.checked_mul(q).ok_or_else(...)` guard mirroring functional_covariance (fdata.rs:368-375) |
| T-37-02 | Tampering (silent wrong result) | fpca_der delegating to deriv_1d | low | mitigate | Validate n>0, m>=2 (when nderiv>0), argvals.len()==m, ncomp>=1 BEFORE calling deriv_1d (which silently returns zeros) |
| T-37-03 | Tampering (panic on bad dims) | cross_covariance / fpca_der | low | mitigate | Dimension checks at entry return FdarError; no unchecked indexing; no unsafe |
| T-37-04 | Information disclosure | zero-row input silently producing zeros | low | mitigate | Explicit n>0 / n>=2 check before any computation |

No threat reaches the block-on threshold (all low; pure in-process numerical library, no new dependency → no package-legitimacy gate).
</threat_model>

<verification>
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel` fully green.
- `cargo clippy --all-targets --features linalg,parallel -- -D warnings` clean.
- `cargo fmt -p fdars-core --check` clean.
- `fdars-core/src/fpca_variants.rs` exists with `cross_covariance`, `fpca_der`, `FsvdResult`; `lib.rs` declares `pub mod fpca_variants;` and re-exports the three symbols.
- No existing public signature changed (additive/non-breaking).
</verification>

<success_criteria>
- FPCA-02-01 (fpca_der) and FPCA-02-03 (cross_covariance) implemented, crate-root re-exported, and covered by green inline tests matching the VALIDATION.md test names (test_fpca_der, test_fpca_der_nderiv0, test_fpca_der_errors, test_cross_cov_self, test_cross_cov_shape, test_cross_cov_errors).
- FsvdResult struct defined (Wave 0 dependency for Plan 02's fsvd).
- Module compiles under linalg+parallel with clippy --all-targets clean.
</success_criteria>

<output>
Create `.planning/phases/phase-37-specialized-fpca-variants/37-01-SUMMARY.md` when done.
</output>