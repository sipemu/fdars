---
milestone: v0.15.0
name: Top-Backlog Quick Wins
audited: 2026-08-11
status: passed
closeout_type: verified_closeout
scores:
  requirements: 4/4
  phases: 2/2
  integration: 4/4
  flows: 4/4
gaps: {}
tech_debt:
  - phase: 11-performance-wins-parallel-cv-folds-faer-fpca-svd
    items:
      - "WR-01 (advisory, 11-REVIEW.md): MEWMA SPE-alarm assertion in spm/tests.rs is guarded against machine-noise reconstruction and is effectively dead on current test data — add a companion under-fit test to restore coverage."
      - "WR-02 (advisory, 11-REVIEW.md): fix_svd_signs silently no-ops on NaN rotation columns — add a NaN guard / debug_assert."
      - "IN-01 (advisory, 11-REVIEW.md): test_fclassif_cv_parallel_matches_sequential proves call-to-call determinism, not cross-mode equivalence — rename or add a clarifying comment."
nyquist:
  compliant_phases: 0
  partial_phases: 0
  not_validated_phases: 2
  missing_phases: 0
  overall: not-validated
  note: "Both phase VALIDATION.md files are status:draft (seeded by plan-phase, never reconciled by validate-phase, #2117). Coverage TODO — run /gsd-validate-phase 10 and 11 — not a compliance failure. Phase-goal verification (VERIFICATION.md) passed for both phases independently."
---

# Milestone v0.15.0 — Top-Backlog Quick Wins — Audit

**Verdict: PASSED (verified closeout).** All 4 requirements satisfied and integrated; both phases verified; cross-phase integration clean; full crate suite green under both feature configs. No critical gaps.

## Scope

| Phase | Name | Plans | Verification |
|-------|------|-------|--------------|
| 10 | Capability Gaps — Spline Interpolation & Functional Summary Statistics | 2/2 | passed (4/4 must-haves) |
| 11 | Performance Wins — Parallel CV Folds & faer FPCA SVD | 2/2 | passed (9/9 must-haves) |

## Requirements Coverage (3-source cross-reference)

Each requirement cross-checked against phase VERIFICATION.md (checked against live code), REQUIREMENTS.md traceability, and code evidence.

| REQ | Phase | VERIFICATION | REQUIREMENTS.md | Final | Evidence |
|-----|-------|--------------|-----------------|-------|----------|
| FEAT-01 | 10 | passed | `[x]` Complete | **satisfied** | `spline_interpolate` in `helpers.rs:416`, re-exported `lib.rs:174`; 5 inline tests (on-grid exact, off-grid cubic within 1e-10) |
| FEAT-02 | 10 | passed | `[x]` Complete | **satisfied** | 5 stat fns in `fdata.rs` (`functional_variance/std/covariance`, `depth_based_median`, `trim_mean`), re-exported `lib.rs:421-424`; tests vs hand-computed refs |
| PERF-01 | 11 | passed | `[x]` Complete | **satisfied** | `iter_maybe_parallel!(0..nfold)` in `cv.rs:77`; determinism test passes both feature configs |
| PERF-02 | 11 | passed | `[x]` Complete | **satisfied** | faer `Svd::new_thin` on zero-copy `MatRef` in `regression.rs` under `linalg`; nalgebra path retained; parity test within 1e-8·σ₁ |

**Coverage: 4/4 (100%). No orphaned requirements** — every REQ-ID appears in a phase VERIFICATION.md.

> Note: SUMMARY frontmatter `requirements_completed` was not populated on most plan summaries (template field unused; these summaries use `requires:` for dependencies). Requirement satisfaction is instead evidenced by the phase VERIFICATION.md files (produced by gsd-verifier, which checked must-haves against actual source) plus the REQUIREMENTS.md traceability table. Manually confirmed satisfied rather than downgraded to partial.

## Cross-Phase Integration

Spawned `gsd-integration-checker` — verdict **INTEGRATED**:
- All Phase-10 public functions reachable from `fdars_core::` crate root (verified re-exports in `lib.rs`).
- No API conflicts, duplicate symbols, or shadowing across the two phases.
- Feature-gated paths (`parallel`, `linalg`) compile with no dangling/unused symbols under `-D warnings`.
- Build + test matrix green: default (1941 tests), `--features linalg` (1948), `--features parallel`, `--features parallel,linalg`, `--no-default-features --features linalg` — all compile; clippy clean on every config.
- The two phases are independent (no compile-time coupling); both changes are additive or behavior-preserving.

## Tech Debt (non-blocking)

Three advisory quality items from the phase-11 code review (`11-REVIEW.md`) — none block the milestone; suitable for a small cleanup phase:
1. **WR-01** — MEWMA SPE-alarm assertion dead on current test data (companion under-fit test needed).
2. **WR-02** — `fix_svd_signs` no-ops on NaN rotation columns (add guard).
3. **IN-01** — `test_fclassif_cv_parallel_matches_sequential` name overstates scope.

## Nyquist Validation

Both phase `VALIDATION.md` files are `status: draft` (NOT-VALIDATED, #2117) — seeded at plan time, never reconciled by `/gsd-validate-phase`. This is a coverage TODO, not a compliance failure; phase-goal verification passed independently for both. Optional: run `/gsd-validate-phase 10` and `/gsd-validate-phase 11` to promote.

## Conclusion

Milestone v0.15.0 achieved its definition of done: the top-4 audit-backlog quick wins (2 capability gaps closed, 2 performance wins landed) shipped as real `fdars-core` code with tests and numerical verification. **Safe to complete.** Advisory tech debt tracked above.
