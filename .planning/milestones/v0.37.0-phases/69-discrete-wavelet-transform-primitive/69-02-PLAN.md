---
phase: 69-discrete-wavelet-transform-primitive
plan: 02
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - fdars-core/src/wavelet/mod.rs
autonomous: true
requirements: [WAV-01, WAV-02]
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Multi-level forward->inverse round-trip reconstructs the input within 1e-10 relative for Haar and db2-db10 across >=2 decomposition levels (SC1) — WAV-01"
    - "Multi-level decompose/reconstruct round-trip holds on a non-power-of-2 length under BOTH periodic and symmetric boundary modes (SC2) — WAV-01, WAV-02"
    - "The FdMatrix batch path transforms every row (curve) and each row's round-trip matches the per-row slice path (SC1) — WAV-01"
    - "Auto max level defaults to floor(log2(n/(filter_len-1))); an explicit level exceeding the max returns a descriptive FdarError, empty/degenerate input returns FdarError, no NaN/panic (SC4) — WAV-02"
  artifacts:
    - "fdars-core/src/wavelet/mod.rs — WaveletCoeffs result struct, decompose/reconstruct multi-level fns, decompose_matrix batch fn, max_level helper"
  key_links:
    - "decompose iterates single_level_analysis on the approximation band, storing per-level detail lengths — reconstruct MUST consume those exact lengths or the pyramid mis-aligns and reconstruction drifts"
    - "auto max_level formula floor(log2(n/(filter_len-1))) gates how deep the pyramid goes without a coefficient band collapsing below the filter length"
---

<objective>
Build the multi-level Mallat pyramid on top of Plan 01's single-level engine: the `WaveletCoeffs` result struct (per-level detail bands + final approximation + the metadata reconstruction needs), the `decompose`/`reconstruct` public functions with auto/explicit level handling and validation, and the `FdMatrix` batch path that transforms each row (curve) — the seam Phase 70's regressors consume.

Purpose: Turn the proven single-step adjoint pair into the full forward/inverse orthogonal DWT the milestone requires, and expose the whole-curve-set entry point so Phase 70 does not re-loop externally.
Output: `WaveletCoeffs`, `decompose`, `reconstruct`, `decompose_matrix`, `max_level` in `fdars-core/src/wavelet/mod.rs`, with the full round-trip / non-power-of-2 / both-boundary / invalid-input gates green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-discrete-wavelet-transform-primitive/69-CONTEXT.md

@fdars-core/src/matrix.rs
@fdars-core/src/wavelet/mod.rs
@fdars-core/src/wavelet/filters.rs
</context>

<artifacts_this_phase_produces>
This plan (69-02) adds the multi-level and batch surface on top of Plan 01. Still NO crate-root/prelude re-exports (deferred to Phase 71) — reachable as `crate::wavelet::...`.

- Struct `WaveletCoeffs` — `#[non_exhaustive]`, derives `Debug, Clone, PartialEq` (+ serde-gated). Fields (names at executor discretion, but these roles): `approx: Vec<f64>` (final coarse approximation), `details: Vec<Vec<f64>>` (per-level detail bands, coarsest-first or finest-first — document the order), `levels: usize`, `signal_len: usize` (original length, so reconstruct returns exactly it), `family: WaveletFamily`, `mode: BoundaryMode`. Provide `#[must_use]` accessors (`levels()`, `signal_len()`, `detail(level)`).
- Fn `max_level(signal_len: usize, family: &WaveletFamily) -> Result<usize, FdarError>` — returns `floor(log2(n / (filter_len - 1)))`; `InvalidParameter` if n==0 or the formula yields 0 useful levels.
- Fn `decompose(signal: &[f64], family: WaveletFamily, mode: BoundaryMode, level: Option<usize>) -> Result<WaveletCoeffs, FdarError>` — `#[must_use]`. `None` level -> auto `max_level`; explicit level validated `1..=max_level` else `InvalidParameter`.
- Fn `reconstruct(coeffs: &WaveletCoeffs) -> Result<Vec<f64>, FdarError>` — `#[must_use]`. Inverts `decompose` to exactly `signal_len` samples.
- Fn `decompose_matrix(data: &FdMatrix, family: WaveletFamily, mode: BoundaryMode, level: Option<usize>) -> Result<Vec<WaveletCoeffs>, FdarError>` — `#[must_use]`. One `WaveletCoeffs` per row (curve); each row is a signal of length `data.ncols()`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: max_level helper + WaveletCoeffs result struct</name>
  <files>fdars-core/src/wavelet/mod.rs</files>
  <read_first>
    - fdars-core/src/wavelet/mod.rs (WaveletFamily, BoundaryMode, single_level_analysis/synthesis from Plan 01)
    - fdars-core/src/wavelet/filters.rs (filter_bank + FilterBank::filter_len — max_level needs filter_len)
    - fdars-core/src/kshape.rs (lines 111-149: `#[non_exhaustive]` result struct + `#[must_use]` accessor house pattern to mirror for WaveletCoeffs)
  </read_first>
  <behavior>
    - Test: `max_level(1024, &Haar)` == floor(log2(1024/1)) == 10; `max_level(1024, &db4)` == floor(log2(1024/7)) (filter_len 8 -> 8-1=7).
    - Test: `max_level(0, &Haar)` returns `FdarError::InvalidParameter`.
    - Test: a signal shorter than one useful level (e.g. n=1) returns `FdarError::InvalidParameter` (no 0-level pyramids).
    - Test: `WaveletCoeffs` derives `PartialEq` — two decompositions of identical inputs compare equal (constructed via a small direct build in-test or via Task 2's decompose once available).
  </behavior>
  <action>
    Add the `WaveletCoeffs` struct with fields for the final approximation, the per-level detail bands (`Vec<Vec<f64>>`), `levels`, `signal_len`, `family`, `mode`, following the `#[non_exhaustive]` + serde-gated + `Debug, Clone, PartialEq` house pattern. Document the detail-band ordering convention (recommend finest-first: `details[0]` is the level-1 detail) in the field doc comment. Add `#[must_use]` accessors.

    Add `max_level(signal_len, family)`: look up `filter_len` via `filter_bank`, compute `floor(log2(signal_len as f64 / (filter_len - 1) as f64))` using integer-safe logic (guard signal_len==0 and filter_len<=1), return `InvalidParameter` when the result is < 1 (no useful decomposition). Keep the new wavelet types serde-clean (or serde-gated) — do not introduce a second serde build break (STATE.md notes a pre-existing one in shapelet).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && cargo test -p fdars-core --features linalg,parallel wavelet:: 2>&1 | tail -20</automated>
    <fails_when>`max_level` returns a wrong value or fails to error on n==0/degenerate input, the WaveletCoeffs struct does not compile, or any test prints "test result: FAILED" (non-zero exit).</fails_when>
  </verify>
  <acceptance_criteria>
    - `max_level` returns `floor(log2(n/(filter_len-1)))` for valid inputs and `InvalidParameter` for n==0 / sub-one-level signals (SC4).
    - `WaveletCoeffs` follows the `#[non_exhaustive]` + serde-gated house pattern and exposes `#[must_use]` accessors.
    - `cargo build -p fdars-core --features serde` is not newly broken by these types (serde-clean or serde-gated).
  </acceptance_criteria>
  <done>The pyramid-depth helper and the coefficient container exist, validated, and ready for decompose/reconstruct to populate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Multi-level decompose/reconstruct with auto + explicit level</name>
  <files>fdars-core/src/wavelet/mod.rs</files>
  <read_first>
    - fdars-core/src/wavelet/mod.rs (single_level_analysis/synthesis, WaveletCoeffs, max_level — this task chains the single step into the Mallat pyramid)
  </read_first>
  <behavior>
    - Test (round-trip, >=2 levels, periodic): for each family in {Haar, db2, db4, db6, db8, db10}, `decompose(signal, family, Periodic, Some(L))` with L>=2 then `reconstruct` recovers a pseudo-random signal within 1e-10 relative (SC1).
    - Test (round-trip, symmetric, non-power-of-2): same families, `Symmetric` mode, n=37 (non-power-of-2), L auto, reconstruct within 1e-10 relative (SC2).
    - Test (auto level): `decompose(signal, db4, Periodic, None)` uses `max_level(n, db4)` and its `coeffs.levels` equals that value.
    - Test (explicit-level validation): `decompose` with `level = Some(max_level+1)` returns `FdarError::InvalidParameter`; `level = Some(0)` returns `FdarError::InvalidParameter`.
    - Test (no NaN): every reconstruction above is finite (no NaN/inf).
    - Test (signal_len preserved): reconstruct returns exactly `signal_len` samples for odd n=37 and even n=64.
  </behavior>
  <action>
    Implement `decompose`: resolve the effective level (`None` -> `max_level`, else validate `1..=max_level` -> `InvalidParameter`). Build the filter bank once. Iterate: apply `single_level_analysis` to the current approximation band; push the detail band into `details` (record the convention chosen in Task 1); the approximation becomes the input to the next level. After `level` iterations, store the final approximation, `levels`, original `signal_len`, `family`, `mode` into `WaveletCoeffs`.

    Implement `reconstruct`: rebuild the filter bank from `coeffs.family`; starting from `coeffs.approx`, iterate the detail bands in reverse level order, calling `single_level_synthesis(approx, detail, fb, mode, target_len)` where `target_len` at each level is the length that level's approximation had during analysis (recover it from the stored `signal_len` and per-level halving, or store per-level lengths in Task 1 if simpler — whichever keeps reconstruction exact). The final step must return exactly `coeffs.signal_len` samples. This per-level length bookkeeping is the key link: if a target length is off by one, the pyramid mis-aligns and the round-trip drifts above tolerance.

    Validate: empty signal -> `InvalidParameter`; a `WaveletCoeffs` whose bands are internally inconsistent -> `InvalidDimension`. Keep `#[must_use]` on both fns. No NaN under any path.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && cargo test -p fdars-core --features linalg,parallel wavelet:: 2>&1 | tail -25</automated>
    <fails_when>Any multi-level round-trip test prints "test result: FAILED" (relative error > 1e-10 for any family, mode, or level), auto/explicit level handling is wrong, a reconstruction is not `signal_len` samples or contains NaN/inf (non-zero exit).</fails_when>
  </verify>
  <acceptance_criteria>
    - Multi-level decompose->reconstruct round-trip ≤1e-10 relative for Haar + db2..db10 across >=2 levels under periodic (SC1).
    - Same round-trip holds on non-power-of-2 n=37 under symmetric mode (SC2).
    - Auto level == `max_level`; explicit level outside `1..=max_level` returns `InvalidParameter` (SC4).
    - Every reconstruction is finite and exactly `signal_len` samples (SC4).
  </acceptance_criteria>
  <done>The full forward/inverse multi-level orthogonal DWT reconstructs perfectly across families, levels, boundary modes, and non-power-of-2 lengths — WAV-01/WAV-02's core gate is met at the slice API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FdMatrix batch path + full invalid-input gate</name>
  <files>fdars-core/src/wavelet/mod.rs</files>
  <read_first>
    - fdars-core/src/wavelet/mod.rs (decompose/reconstruct from Task 2)
    - fdars-core/src/matrix.rs (lines 40-160, 240-291: `FdMatrix` fields, `nrows`/`ncols`, `row(row)->Vec<f64>` / `row_to_buf`, `iter_rows`, `column`; rows = curves, columns = evaluation points)
  </read_first>
  <behavior>
    - Test (batch parity): build an FdMatrix with several rows (curves) of length n=48; `decompose_matrix(&m, db4, Periodic, None)` returns one WaveletCoeffs per row; each equals `decompose(&m.row(i), db4, Periodic, None)` for that row (batch == per-row slice path).
    - Test (batch round-trip): reconstruct each returned WaveletCoeffs and assert it matches the original row within 1e-10 relative for both boundary modes.
    - Test (empty matrix): `decompose_matrix` on a 0-row or 0-col FdMatrix returns `FdarError::InvalidDimension` (no panic).
    - Test (invalid family in batch): `decompose_matrix(..., WaveletFamily::from_db_order(11)?, ...)` path — an out-of-range order surfaced through decompose returns `FdarError::InvalidParameter`, not a panic.
    - Test (no NaN in batch): every reconstructed row is finite.
  </behavior>
  <action>
    Implement `decompose_matrix(data, family, mode, level)`: validate the matrix is non-empty (`nrows>0 && ncols>0`, else `InvalidDimension`). For each row (curve) of length `ncols`, extract it via `row(i)` / `row_to_buf` and call `decompose` with the same family/mode/level, collecting a `Vec<WaveletCoeffs>` (one per row). Reuse the single filter-bank resolution where practical, but correctness (batch result identical to the per-row slice path) is the gate. Respect the column-major layout — rows are curves, so iterate rows, not columns.

    Add the remaining invalid-input gate tests spanning the whole phase's SC4 surface: unsupported family order (via `from_db_order`), empty signal (`decompose`), level exceeding max (`decompose`), and empty matrix (`decompose_matrix`) — each returns a specific descriptive `FdarError` variant with no NaN and no panic. Confirm no reconstruction anywhere emits NaN/inf.

    Run `cargo fmt` and ensure `cargo clippy --all-targets --features linalg,parallel -- -D warnings` is clean before the executor commits (`--no-verify` after out-of-band gates per MEMORY.md).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && cargo test -p fdars-core --features linalg,parallel wavelet:: 2>&1 | tail -25 && cargo clippy -p fdars-core --all-targets --features linalg,parallel -- -D warnings 2>&1 | tail -5 && cargo fmt --check 2>&1 | tail -3</automated>
    <fails_when>Batch result differs from the per-row slice path, a batch round-trip exceeds 1e-10 relative, an empty matrix or invalid order panics instead of returning `FdarError`, a reconstruction contains NaN/inf, clippy emits any warning, or `fmt --check` reports drift (non-zero exit).</fails_when>
  </verify>
  <acceptance_criteria>
    - `decompose_matrix` returns one `WaveletCoeffs` per row and each equals the per-row `decompose` result (SC1).
    - Each row's batch round-trip reconstructs within 1e-10 relative under both boundary modes (SC1, SC2).
    - Empty matrix -> `InvalidDimension`; unsupported order -> `InvalidParameter`; level > max -> `InvalidParameter`; empty signal -> `InvalidParameter` — all with no NaN/panic (SC4).
    - `cargo clippy --all-targets --features linalg,parallel -- -D warnings` and `cargo fmt --check` are clean.
  </acceptance_criteria>
  <done>The FdMatrix batch path transforms a whole curve set identically to per-row calls, the full SC4 invalid-input surface returns descriptive `FdarError`s, and the phase's build gates are green — the Phase 70 seam is ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> wavelet API | untrusted signal slices, FdMatrix curve sets, family orders, levels, boundary modes cross here (in-process library call — no network/OS boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-04 | Denial of Service | `decompose`/`decompose_matrix` on empty/degenerate input or over-deep level | low | mitigate | `max_level` bounds the pyramid depth; entry validation returns `InvalidParameter`/`InvalidDimension`; no unbounded recursion (bounded level loop) |
| T-69-05 | Tampering | per-level length bookkeeping (silent off-by-one -> wrong-but-finite reconstruction) | medium | mitigate | Multi-level 1e-10 round-trip gate across families/levels/modes/non-power-of-2 lengths rejects any mis-aligned pyramid |
| T-69-06 | Denial of Service | large FdMatrix batch (many long rows) | low | accept | Bounded per-row work == single-slice cost x nrows; no pathological blowup; caller controls input size |
| T-69-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs — locked decision forbids new crate dependency |
</threat_model>

<verification>
- `cargo test -p fdars-core --features linalg,parallel wavelet::` — multi-level round-trip (>=2 levels, both modes, non-power-of-2), batch parity + round-trip, and full invalid-input gate all green.
- `cargo clippy -p fdars-core --all-targets --features linalg,parallel -- -D warnings` — clean.
- `cargo fmt --check` — clean (commit with `--no-verify` after out-of-band gates per MEMORY.md; the pre-commit hook runs the full suite and times out at 30s).
- Confirm `cargo build -p fdars-core --features serde` is not newly broken by the wavelet types (a pre-existing shapelet serde break is unrelated — do not add a second).
</verification>

<success_criteria>
- Multi-level forward->inverse round-trip ≤1e-10 relative for Haar + db2..db10 across >=2 levels (SC1).
- Round-trip holds on a non-power-of-2 length under both periodic and symmetric modes (SC2).
- FdMatrix batch path == per-row slice path; every row round-trips (SC1).
- Auto max level == `floor(log2(n/(filter_len-1)))`; invalid level/family/empty input returns descriptive `FdarError`, no NaN/panic (SC4).
</success_criteria>

<output>
Create `.planning/phases/69-discrete-wavelet-transform-primitive/69-02-SUMMARY.md` when done.
</output>