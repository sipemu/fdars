---
phase: 69-discrete-wavelet-transform-primitive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/wavelet/mod.rs
  - fdars-core/src/wavelet/filters.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [WAV-01, WAV-02]
estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Haar single-level forward+inverse round-trip reconstructs the input within 1e-10 relative (SC3, tracer) — WAV-01"
    - "A known-answer Haar single-level decomposition matches hand-computed sum/difference over sqrt(2) approximation and detail coefficients (SC3) — WAV-01"
    - "Single-level analysis+synthesis round-trips within 1e-10 relative for Haar and Daubechies db2-db10 under BOTH periodic and symmetric boundary modes on a non-power-of-2 length (SC1, SC2) — WAV-01, WAV-02"
    - "Requesting an unsupported family/order returns a descriptive FdarError (no panic, no NaN) (SC4) — WAV-02"
  artifacts:
    - "fdars-core/src/wavelet/mod.rs — module declaring WaveletFamily, BoundaryMode enums and single-level analysis/synthesis engine"
    - "fdars-core/src/wavelet/filters.rs — hardcoded db1..db10 orthonormal filter-coefficient tables + family->filter lookup"
    - "fdars-core/src/lib.rs — `pub mod wavelet;` declaration (NO crate-root re-exports — deferred to Phase 71)"
  key_links:
    - "filters.rs quadrature-mirror derivation (highpass g from lowpass h) — a sign/reversal error here silently breaks every round-trip downstream"
    - "single-level synthesis must be the exact adjoint of analysis under each boundary mode — the seam multi-level (Plan 02) builds on"
---

<objective>
Stand up the wavelet module and its single-level filter-bank engine: the hardcoded Daubechies filter tables (db1/Haar through db10), the `WaveletFamily` and `BoundaryMode` config enums, and one production-quality single-level DWT step (analysis: signal -> approximation+detail; synthesis: approximation+detail -> signal) that perfectly reconstructs under both periodic and symmetric boundary handling.

Purpose: This is the numerical make-or-break core of the milestone. Multi-level (Plan 02) is just repeated application of this single step, so proving analysis/synthesis is an exact adjoint pair here — with a hand-computed known-answer anchor — retires the perfect-reconstruction risk before anything builds on it.
Output: `fdars-core/src/wavelet/{mod.rs,filters.rs}`, the module declared in `lib.rs`, and passing round-trip + known-answer tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-discrete-wavelet-transform-primitive/69-CONTEXT.md

@fdars-core/src/error.rs
@fdars-core/src/matrix.rs
</context>

<artifacts_this_phase_produces>
This plan (69-01) establishes the following symbols. Plan 69-02 adds the multi-level/batch surface on top. NO crate-root or prelude re-exports are added in Phase 69 (deferred to Phase 71) — the module is reachable only as `crate::wavelet::...` internally.

- Module: `fdars-core/src/wavelet/mod.rs` (declared `pub mod wavelet;` in `lib.rs`)
- Submodule: `fdars-core/src/wavelet/filters.rs`
- Enum `WaveletFamily` — `#[non_exhaustive]`, derives `Debug, Clone, PartialEq` (+ serde-gated). Variants: `Haar`, `Daubechies(usize)` where the usize is the vanishing-moment order N in 2..=10 (db2..db10; db1 == Haar). Provide `WaveletFamily::from_db_order(order: usize) -> Result<Self, FdarError>` mapping 1 -> Haar, 2..=10 -> Daubechies(N), else `InvalidParameter`.
- Enum `BoundaryMode` — `#[non_exhaustive]`, derives `Debug, Clone, PartialEq` (+ serde-gated). Variants: `Periodic` (default), `Symmetric`. Provide a `Default` impl returning `Periodic`.
- Struct (private or `pub(crate)`) `FilterBank` in `filters.rs` — analysis lowpass `dec_lo`, analysis highpass `dec_hi`, synthesis lowpass `rec_lo`, synthesis highpass `rec_hi` (each `Vec<f64>` / `&'static [f64]`), plus `filter_len()`.
- Fn `filters::filter_bank(family: &WaveletFamily) -> Result<FilterBank, FdarError>` — returns the orthonormal filter bank for the family (hardcoded coefficients), `InvalidParameter` for out-of-range order.
- Fn (`pub(crate)`) `single_level_analysis(signal: &[f64], fb: &FilterBank, mode: BoundaryMode) -> Result<(Vec<f64>, Vec<f64>), FdarError>` — returns (approximation, detail).
- Fn (`pub(crate)`) `single_level_synthesis(approx: &[f64], detail: &[f64], fb: &FilterBank, mode: BoundaryMode, output_len: usize) -> Result<Vec<f64>, FdarError>` — reconstructs the original-length signal.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end Haar single-level round-trip — one path only</name>
  <files>fdars-core/src/wavelet/mod.rs, fdars-core/src/wavelet/filters.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/lib.rs (lines ~64-146: the `pub mod ...;` declaration block; add `pub mod wavelet;` in alphabetical position between `pub mod warping;`/`pub mod wire;` — pick the correct slot; do NOT add any `pub use wavelet::...` re-export)
    - fdars-core/src/error.rs (FdarError variants — use `InvalidParameter { parameter, message }` for bad orders/lengths, `InvalidDimension` for length mismatches)
    - fdars-core/src/kshape.rs (lines 67-149: the `#[derive(Debug, Clone, PartialEq)]` + `#[cfg_attr(feature = "serde", derive(...))]` + `#[non_exhaustive]` + `#[must_use]` house pattern for config/result types)
  </read_first>
  <action>
    Create `fdars-core/src/wavelet/mod.rs` and `fdars-core/src/wavelet/filters.rs`, and declare `pub mod wavelet;` in `lib.rs` (module-declaration only — no re-exports, per the Phase 71 deferral).

    In `filters.rs`: define the `FilterBank` struct (fields `dec_lo, dec_hi, rec_lo, rec_hi: Vec<f64>`) and a `filter_bank` fn. For THIS tracer, only the Haar (db1) bank must be correct: `dec_lo = [1/sqrt(2), 1/sqrt(2)]`, `dec_hi = [1/sqrt(2), -1/sqrt(2)]`, `rec_lo = [1/sqrt(2), 1/sqrt(2)]`, `rec_hi = [-1/sqrt(2), 1/sqrt(2)]` (standard orthonormal DWT convention where synthesis filters are the time-reverse of analysis). Other orders may return an `InvalidParameter` placeholder for now (Task 2 fills them).

    In `mod.rs`: define `WaveletFamily` (Haar variant is enough for the tracer; add the `Daubechies(usize)` variant and `from_db_order` now but its 2..=10 arm can defer to filters) and `BoundaryMode { Periodic, Symmetric }` with `Default = Periodic`. Implement `single_level_analysis` and `single_level_synthesis` for the PERIODIC path only on an EVEN-length signal: analysis = periodic circular convolution of the signal with `dec_lo`/`dec_hi` followed by downsample-by-2 (keep even-indexed outputs), producing two half-length coefficient vectors; synthesis = upsample-by-2 (zero-stuff), circular-convolve approximation with `rec_lo` and detail with `rec_hi`, sum. Use `InvalidParameter` if the signal is empty. Wire ONE happy path end-to-end; no db2-db10, no symmetric mode, no odd-length handling yet.

    Add an inline `#[cfg(test)] mod tests` with the tracer test: take an even-length signal, `single_level_analysis` then `single_level_synthesis`, assert reconstruction within 1e-10 relative. Include the hand-computed known-answer assertion: for input `[a, b, c, d]`, approximation == `[(a+b)/sqrt(2), (c+d)/sqrt(2)]` and detail == `[(a-b)/sqrt(2), (c-d)/sqrt(2)]` (SC3).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && cargo test -p fdars-core --features linalg,parallel wavelet:: 2>&1 | tail -20</automated>
    <fails_when>Any wavelet test prints "test result: FAILED", a round-trip assert fires (relative error > 1e-10), the known-answer Haar coefficients mismatch, or the crate fails to compile (non-zero exit).</fails_when>
  </verify>
  <acceptance_criteria>
    - `pub mod wavelet;` is declared in `lib.rs` with NO `pub use wavelet::...` re-export.
    - Haar single-level analysis of `[a,b,c,d]` yields approximation `[(a+b)/√2,(c+d)/√2]` and detail `[(a-b)/√2,(c-d)/√2]` (known-answer, SC3).
    - Haar analysis->synthesis reconstructs an even-length input within 1e-10 relative (SC1/SC3, one path).
    - Empty signal returns `FdarError::InvalidParameter` (no panic).
  </acceptance_criteria>
  <done>The Haar single-level forward+inverse round-trip works end-to-end under periodic boundary on even lengths and is committed with the known-answer test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hardcoded db2-db10 orthonormal filter tables</name>
  <files>fdars-core/src/wavelet/filters.rs</files>
  <read_first>
    - fdars-core/src/wavelet/filters.rs (the FilterBank + Haar bank from Task 1 — extend the `filter_bank` match to db2..db10)
    - fdars-core/src/wavelet/mod.rs (WaveletFamily::Daubechies(usize) and from_db_order — the order flows into filter_bank)
  </read_first>
  <behavior>
    - Test: for every family in {Haar, db2, db3, ..., db10}, the analysis lowpass `dec_lo` sums to sqrt(2) within 1e-12 (orthonormal-DWT normalization invariant).
    - Test: for every family, `dec_lo` has unit L2 norm within 1e-12 (sum of squares == 1).
    - Test: for every family, the analysis highpass `dec_hi` is the alternating-sign time-reverse (quadrature mirror) of `dec_lo`: `dec_hi[k] == (-1)^k * dec_lo[L-1-k]` within 1e-12, so `dec_lo · dec_hi == 0` (orthogonality) within 1e-12.
    - Test: for every family, synthesis filters satisfy `rec_lo[k] == dec_lo[L-1-k]` and `rec_hi[k] == dec_hi[L-1-k]` (synthesis == time-reverse of analysis) within 1e-12.
    - Test: `filter_len()` == 2*N for dbN (2 for Haar/db1, 4 for db2, ..., 20 for db10).
    - Test: `WaveletFamily::from_db_order(11)` and `from_db_order(0)` return `FdarError::InvalidParameter`; `from_db_order(1)` == Haar; `from_db_order(2)` == Daubechies(2).
  </behavior>
  <action>
    Populate the hardcoded Daubechies analysis-lowpass coefficient tables for db2 through db10 (the `dec_lo` filters; the standard orthonormal DWT convention used by PyWavelets `wavedec`/`waverec` with default normalization). Store each `dec_lo` as a `&'static [f64]` constant table keyed by order. Derive the other three filters PROGRAMMATICALLY from `dec_lo` (do NOT hand-type four tables per order — that multiplies the chance of a sign error): `dec_hi[k] = (-1)^k * dec_lo[L-1-k]`, `rec_lo = dec_lo` reversed, `rec_hi = dec_hi` reversed. Extend `filter_bank` to build the FilterBank for `Daubechies(N)` via table lookup, returning `InvalidParameter` for N outside 2..=10. Do not add a new crate dependency (locked decision — coefficients are hardcoded).

    Source the db2..db10 `dec_lo` numbers to full f64 precision from a standard reference table; the round-trip and normalization tests below are the correctness gate (a mistyped digit fails the L2-norm or round-trip assert). If any table fails the sum==√2 / unit-norm invariant, fix the digits — do not loosen the tolerance.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && cargo test -p fdars-core --features linalg,parallel wavelet::filters 2>&1 | tail -20</automated>
    <fails_when>Any filter-invariant test prints "test result: FAILED": a `dec_lo` table does not sum to √2 or lacks unit L2 norm, the QMF/time-reverse relations do not hold, `filter_len` is wrong, or `from_db_order` bounds are wrong.</fails_when>
  </verify>
  <acceptance_criteria>
    - db2..db10 `dec_lo` tables each sum to √2 and have unit L2 norm within 1e-12.
    - `dec_hi`, `rec_lo`, `rec_hi` are derived from `dec_lo` (single source of truth) and satisfy the QMF + time-reverse relations within 1e-12.
    - `from_db_order` maps 1->Haar, 2..=10->Daubechies(N), and returns `InvalidParameter` for 0 and 11.
    - No new entry in `Cargo.toml` (no dependency added).
  </acceptance_criteria>
  <done>All eleven families (Haar + db2..db10) expose a correct orthonormal filter bank verified by normalization/QMF invariant tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Single-level engine for all families under periodic + symmetric boundary, arbitrary length</name>
  <files>fdars-core/src/wavelet/mod.rs</files>
  <read_first>
    - fdars-core/src/wavelet/mod.rs (the Task 1 single-level periodic/even engine — generalize it to any filter length, any signal length, and add the symmetric mode)
    - fdars-core/src/wavelet/filters.rs (FilterBank fields consumed by the convolution)
  </read_first>
  <behavior>
    - Test (round-trip, periodic): for each family in {Haar, db2, db4, db6, db8, db10}, on a NON-power-of-2 odd length (e.g. n=37) of pseudo-random data, `single_level_analysis` then `single_level_synthesis` reconstructs within 1e-10 relative (SC1/SC2 under periodic).
    - Test (round-trip, symmetric): same families and n=37, `BoundaryMode::Symmetric`, reconstruct within 1e-10 relative (SC2 under symmetric — the mode must independently satisfy the gate).
    - Test (coefficient lengths): periodic analysis of length n yields approximation and detail each of length ceil(n/2); synthesis with `output_len = n` returns exactly n samples.
    - Test (no NaN): reconstruction of the n=37 signals contains no NaN/inf for any family/mode.
    - Test (odd/even both work): n=36 (even) and n=37 (odd) both round-trip for db4 under both modes.
  </behavior>
  <action>
    Generalize `single_level_analysis`/`single_level_synthesis` so they work for ANY filter length and ANY signal length under both boundary modes; keep the periodic even-length path from Task 1 as a special case (its known-answer test must still pass).

    Periodic mode: convolve with modular (wrap-around) index arithmetic `idx.rem_euclid(n)`; downsample-by-2; coefficient length = ceil(n/2). Synthesis inverts exactly (adjoint) so the periodic round-trip holds for any n and any dbN filter.

    Symmetric (half-point) mode: extend the signal by mirroring at the boundaries (reflect indices via the standard fold `i -> if i<0 {-i-1} else if i>=n {2n-1-i}`), convolve+downsample; synthesis applies the transpose of that boundary operator so the symmetric round-trip also reconstructs exactly to `output_len` samples. Ensure the perfect-reconstruction property holds independently of the periodic path (SC2).

    Validate inputs: empty signal -> `InvalidParameter`; in synthesis, mismatched approximation/detail lengths -> `InvalidDimension`. Never emit NaN — if a filter longer than the signal would require it, still produce a finite reconstruction (boundary extension covers this; assert finiteness in tests). Keep `#[must_use]` on the public-facing transform entry points.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && cargo test -p fdars-core --features linalg,parallel wavelet:: 2>&1 | tail -25 && cargo clippy -p fdars-core --all-targets --features linalg,parallel -- -D warnings 2>&1 | tail -5</automated>
    <fails_when>Any wavelet round-trip test prints "test result: FAILED" (relative error > 1e-10 under either boundary mode or on odd length), a reconstruction contains NaN/inf, coefficient lengths are wrong, or clippy emits any warning (non-zero exit).</fails_when>
  </verify>
  <acceptance_criteria>
    - Single-level round-trip within 1e-10 relative for Haar + db2..db10 on a non-power-of-2 length under BOTH periodic and symmetric modes (SC1, SC2).
    - Both even (n=36) and odd (n=37) lengths round-trip for db4 under both modes without panicking (SC2).
    - No reconstruction contains NaN/inf (SC4).
    - `cargo clippy --all-targets --features linalg,parallel -- -D warnings` is clean.
  </acceptance_criteria>
  <done>One single-level DWT step reconstructs perfectly for every in-scope family under periodic and symmetric boundaries on arbitrary lengths — the exact-adjoint core Plan 02 iterates into the Mallat pyramid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> wavelet API | untrusted signal slices, family orders, boundary modes cross here (in-process library call — no network/OS boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-01 | Denial of Service | `single_level_*` on empty/degenerate input | low | mitigate | Entry-point validation returns `FdarError::InvalidParameter`/`InvalidDimension`; no unbounded loops or index panics (rem_euclid / bounded fold) |
| T-69-02 | Tampering | Daubechies filter tables (silent numeric corruption -> wrong-but-finite output) | medium | mitigate | Normalization + QMF invariant tests (Task 2) and 1e-10 round-trip gate (Task 3) reject any mistyped coefficient |
| T-69-03 | Information Disclosure | none — pure numeric library, no secrets/IO | low | accept | No filesystem, network, or secret handling in this phase |
| T-69-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs — locked decision forbids new crate dependency; filter coefficients hardcoded in-crate |
</threat_model>

<verification>
- `cargo test -p fdars-core --features linalg,parallel wavelet::` — all single-level round-trip, known-answer, filter-invariant, and invalid-input tests green.
- `cargo clippy -p fdars-core --all-targets --features linalg,parallel -- -D warnings` — clean.
- `cargo fmt --check` before commit (commit with `--no-verify` after out-of-band gates per MEMORY.md; the pre-commit hook runs the full suite and times out).
</verification>

<success_criteria>
- Haar known-answer single-level coefficients match sum/difference over √2 (SC3).
- Single-level round-trip ≤1e-10 relative for Haar + db2..db10 across both boundary modes on non-power-of-2 lengths (SC1, SC2 at the single-level layer).
- Invalid family/order and empty signal return descriptive `FdarError`, no NaN/panic (SC4).
</success_criteria>

<output>
Create `.planning/phases/69-discrete-wavelet-transform-primitive/69-01-SUMMARY.md` when done.
</output>
