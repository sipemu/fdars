---
phase: 46-whole-crate-profiling-measurement
plan: 05
type: execute
wave: 3
depends_on: ["46-02", "46-03", "46-04"]
files_modified:
  - .planning/phases/46-whole-crate-profiling-measurement/PROF-00-summary.md
  - .planning/phases/46-whole-crate-profiling-measurement/46-VALIDATION.md
autonomous: true
requirements: [PROF-01, PROF-02, PROF-03]
estimate:
  tokens: 35000
  raw_tokens: 17500
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "PROF-00-summary.md exists, links all three inventories, and states the top targets from each (top hot-path from PROF-01, top dedup from PROF-02, top API item from PROF-03) so downstream phases have a single entry point."
    - "The summary maps each inventory to its consumer phase (PROF-01→47/51, PROF-02→49, PROF-03→50)."
    - "The final zero-behavior-change gate is confirmed: full suite green, no probe_* [[bench]] registered, no src/ edit across the whole phase."
    - "46-VALIDATION.md sign-off checkboxes are ticked and nyquist_compliant is set true (measure-only, baseline-green + inventories confirmed)."
  artifacts:
    - .planning/phases/46-whole-crate-profiling-measurement/PROF-00-summary.md
  key_links:
    - "The summary is the single index a downstream phase planner reads to find which inventory + which top-ranked target to consume."
---

<objective>
Write PROF-00-summary.md — the short document tying the three ranked inventories together — and
finalize the phase's validation sign-off. Confirm the phase's zero-behavior-change guarantee holds
crate-wide (full suite green, no throwaway bench registered, no `src/` edit anywhere in the phase).

Purpose: Downstream planners (Phases 47/49/50/51) need a single entry point that names the three
inventories, their consumer phases, and each inventory's top-ranked target — so they can jump
straight to the evidence they consume.

Output: The summary doc + a validated 46-VALIDATION.md. NO benches, NO `fdars-core/src/` edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/46-whole-crate-profiling-measurement/46-VALIDATION.md
@.planning/phases/46-whole-crate-profiling-measurement/PROF-01-hotpath-targets.md
@.planning/phases/46-whole-crate-profiling-measurement/PROF-02-dedup-inventory.md
@.planning/phases/46-whole-crate-profiling-measurement/PROF-03-api-inventory.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write PROF-00-summary.md tying the three inventories together</name>
  <files>.planning/phases/46-whole-crate-profiling-measurement/PROF-00-summary.md</files>
  <read_first>
    - .planning/phases/46-whole-crate-profiling-measurement/PROF-01-hotpath-targets.md (top-ranked hot-path target + Environment caveat)
    - .planning/phases/46-whole-crate-profiling-measurement/PROF-02-dedup-inventory.md (top-ranked dedup target)
    - .planning/phases/46-whole-crate-profiling-measurement/PROF-03-api-inventory.md (top-ranked API item)
  </read_first>
  <action>
    Write `.planning/phases/46-whole-crate-profiling-measurement/PROF-00-summary.md` as a SHORT index
    document (roughly one page). Required content:
      - `## The Three Inventories` — a table linking each inventory file to its consumer phase and its
        top-ranked target:
        `[Inventory] [File] [Requirement] [Consumer Phase] [Top-ranked target]`
        Rows: PROF-01 hot-path → PROF-01-hotpath-targets.md → Phase 47 (PERF) + Phase 51 (BENCH-01
        module list); PROF-02 dedup → PROF-02-dedup-inventory.md → Phase 49 (CONS); PROF-03 API →
        PROF-03-api-inventory.md → Phase 50 (API).
      - `## Headline Findings` — one short paragraph per inventory naming its single highest-leverage
        target with its anchor + number (the hot-path target's ms + `file:line`; the dedup target's
        call-site count; the API item's proposed canonical form).
      - `## Measurement Environment Caveat` — restate the governor (`powersave`, LOW-CONFIDENCE for
        multi-thread cells) and RAYON_NUM_THREADS honesty caveat from PROF-01 so downstream before/after
        comparisons stay honest.
      - `## Scope Confirmation` — state that this was measure-only: zero `fdars-core/src/` edits, no
        permanent `[[bench]]` registered (Phase 51 owns those), no new crate dependency.
    Do NOT restate the full inventories — link to them. No src edits.
  </action>
  <verify>
    <automated>test -f .planning/phases/46-whole-crate-profiling-measurement/PROF-00-summary.md && grep -Eic "PROF-01|PROF-02|PROF-03" .planning/phases/46-whole-crate-profiling-measurement/PROF-00-summary.md && grep -Eic "Phase 47|Phase 49|Phase 50" .planning/phases/46-whole-crate-profiling-measurement/PROF-00-summary.md</automated>
  </verify>
  <acceptance_criteria>
    - `PROF-00-summary.md` exists and references all three inventory files by name (grep for PROF-01/02/03 returns ≥3).
    - It maps each inventory to its consumer phase (grep for Phase 47/49/50 returns ≥3).
    - It contains the measurement-environment honesty caveat (governor + RAYON_NUM_THREADS).
    - It states the measure-only scope confirmation (zero src edits, no permanent bench, no new dep).
    - `git status --porcelain fdars-core/src/` prints `0`.
  </acceptance_criteria>
  <done>PROF-00-summary.md is a one-page index linking the three inventories to their consumer phases with headline targets.</done>
</task>

<task type="auto">
  <name>Task 2: Final zero-behavior-change gate + validation sign-off</name>
  <files>.planning/phases/46-whole-crate-profiling-measurement/46-VALIDATION.md</files>
  <read_first>
    - .planning/phases/46-whole-crate-profiling-measurement/46-VALIDATION.md (frontmatter status/nyquist_compliant + §Validation Sign-Off checklist)
  </read_first>
  <action>
    Run the final crate-wide zero-behavior-change gate and record it in 46-VALIDATION.md. Steps:
      (a) Confirm the full suite is green:
          `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel`
          → must end `test result: ok. … 0 failed`.
      (b) Confirm no throwaway probe survives: `grep "probe_" fdars-core/Cargo.toml` returns nothing;
          no `fdars-core/benches/probe_*.rs` and no `fdars-core/tests/alloc_audit_new_subsystems.rs`.
      (c) Confirm the clippy gate is clean (throwaway bench code is gone, so this should pass cleanly):
          `cargo clippy --all-targets --features linalg,parallel -- -D warnings`.
      (d) Confirm all four artifact docs exist: PROF-00-summary.md, PROF-01-hotpath-targets.md,
          PROF-02-dedup-inventory.md, PROF-03-api-inventory.md.
    Then edit 46-VALIDATION.md: tick the §Validation Sign-Off checkboxes (baseline green, green after
    probe removal, no throwaway bench registered, three inventory docs exist), set the frontmatter
    `status: validated` and `nyquist_compliant: true` (measure-only — no new test coverage required;
    the applicable validation is baseline-green + inventories-confirmed), and set `Approval:` to a
    dated pass note. Do NOT edit any `fdars-core/src/` file.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel 2>&1 | tail -3 && ! grep -q "probe_" fdars-core/Cargo.toml && grep -q "nyquist_compliant: true" .planning/phases/46-whole-crate-profiling-measurement/46-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Full suite output ends with `test result: ok.` and `0 failed`.
    - `grep "probe_" fdars-core/Cargo.toml` returns nothing (no throwaway bench registered).
    - `cargo clippy --all-targets --features linalg,parallel -- -D warnings` exits clean.
    - All four docs exist: PROF-00-summary.md, PROF-01-hotpath-targets.md, PROF-02-dedup-inventory.md, PROF-03-api-inventory.md.
    - 46-VALIDATION.md frontmatter has `status: validated` and `nyquist_compliant: true`; the sign-off checkboxes are ticked.
    - `git status --porcelain fdars-core/src/` prints `0`.
  </acceptance_criteria>
  <done>The phase's zero-behavior-change guarantee is confirmed crate-wide and 46-VALIDATION.md is signed off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifier → source tree | Read-only final gate (test run + grep) producing a summary doc + validation sign-off. No external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-02 | Tampering | Throwaway bench slips through to the finalized phase | high | mitigate | Task 2's gate asserts `grep "probe_" Cargo.toml` returns nothing and clippy `--all-targets` is clean before sign-off. |
| T-46-06 | Repudiation | Sign-off recorded without the suite actually being green | medium | mitigate | The sign-off edit is gated behind the live full-suite `test result: ok.` run in the same task's `<verify>`. |
| T-46-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs; no new dependency across the phase. |
</threat_model>

<verification>
- PROF-00-summary.md links all three inventories to their consumer phases with headline targets + environment caveat.
- Full suite green; no `probe_*` bench in Cargo.toml; clippy `--all-targets` clean; all four docs present.
- 46-VALIDATION.md signed off (`status: validated`, `nyquist_compliant: true`).
- No `fdars-core/src/` file changed anywhere in the phase.
</verification>

<success_criteria>
- The three ranked inventories (PROF-01/02/03) are tied together by a one-page index that names each consumer phase + top target.
- Crate-wide zero-behavior-change guarantee is confirmed and recorded; phase is validated.
</success_criteria>

<output>
Create `.planning/phases/46-whole-crate-profiling-measurement/46-05-SUMMARY.md` when done.
Note the three headline targets (one per inventory) so the milestone-level view has them at a glance.
</output>