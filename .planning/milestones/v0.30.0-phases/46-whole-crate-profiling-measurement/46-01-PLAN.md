---
phase: 46-whole-crate-profiling-measurement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/Cargo.toml
  - fdars-core/benches/probe_fpca_variants.rs
  - fdars-core/tests/alloc_audit_new_subsystems.rs
  - .planning/phases/46-whole-crate-profiling-measurement/PROF-01-hotpath-targets.md
autonomous: true
requirements: [PROF-01]
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Baseline `cargo test -p fdars-core --features linalg,parallel` is green BEFORE any probe bench is added (proves zero pre-existing breakage)."
    - "A single throwaway probe bench (fpca_variants) compiles, runs via `cargo bench`, and emits criterion wall-time numbers for at least 2 N×M cells."
    - "One dhat allocation probe for a fpca_variants hotspot runs under `--features dhat-heap,linalg` and prints total/peak heap bytes."
    - "PROF-01-hotpath-targets.md exists with an Environment section (governor, RAYON_NUM_THREADS, feature flags, date) and one measured fpca_variants row carrying a real ms number + file:line anchor."
  artifacts:
    - fdars-core/benches/probe_fpca_variants.rs
    - fdars-core/tests/alloc_audit_new_subsystems.rs
    - .planning/phases/46-whole-crate-profiling-measurement/PROF-01-hotpath-targets.md
  key_links:
    - "The throwaway `[[bench]]` entry in Cargo.toml is what makes `cargo bench --bench probe_fpca_variants` runnable (harness = false requires registration)."
    - "generate_curves() formula in the probe bench must match tests/alloc_audit_fpca.rs:generate_test_curves so bench and dhat numbers are comparable."
---

<objective>
Wave 0 setup + the end-to-end TRACER for the whole profiling pipeline: prove ONE probe bench
(for `fpca_variants`) works all the way through — author → temp-register in Cargo.toml → dry-run
compile → run → capture criterion numbers → capture one dhat allocation probe → write the first
row of the PROF-01 hot-path inventory doc.

Purpose: The remaining 8 subsystems in Plan 02 copy this exact pipeline. Proving it once, end to
end, on a single subsystem catches every harness/registration/measurement pitfall (TMPDIR,
target/ growth, temp `[[bench]]` registration, dhat gating) before it is repeated 8 times.

Output: A working throwaway probe bench, a new dhat integration-test file, the PROF-01 doc
skeleton with its Environment section and the first measured row, and a green baseline suite.
This is measure-only: ZERO edits to `fdars-core/src/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-whole-crate-profiling-measurement/46-CONTEXT.md
@.planning/phases/46-whole-crate-profiling-measurement/46-RESEARCH.md
@.planning/phases/46-whole-crate-profiling-measurement/46-VALIDATION.md

# Copy-adapt templates (read these before authoring):
@fdars-core/benches/audit_hotpaths.rs
@fdars-core/tests/alloc_audit_fpca.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Wave 0 setup + baseline-green sanity check</name>
  <files>(no files created — environment prep + verification only)</files>
  <read_first>
    - .planning/phases/46-whole-crate-profiling-measurement/46-RESEARCH.md §Operational Landmines
    - .planning/phases/46-whole-crate-profiling-measurement/46-VALIDATION.md §Wave 0 Requirements
  </read_first>
  <precondition>The bench TMPDIR cache dir `/home/simonm/.cache/fdars-bench-tmp` must exist (create with `mkdir -p` if absent) — bench/doctest linking exhausts the small /tmp tmpfs without it.</precondition>
  <action>
    Prepare the build environment and confirm a clean baseline BEFORE touching any bench code.
    Steps, in order:
    (a) `mkdir -p /home/simonm/.cache/fdars-bench-tmp` (idempotent; the /tmp tmpfs is too small for
        bench/doctest linking — MEMORY.md tmp-exhaustion pointer).
    (b) Free disk headroom so example/bench LINK does not die when target/ fills /home:
        `rm -rf /home/simonm/projects/rust/fdars/target/debug/{incremental,examples}`
        (MEMORY.md target-dir-fills-home-partition pointer).
    (c) Record the measurement environment for later use in the PROF-01 doc: capture the CPU governor
        (`cat /sys/devices/system/cpu/cpu0/cpufreq/scaling_governor` — known to be `powersave` on this
        host, a LOW-CONFIDENCE honesty caveat), the number of logical cores (for RAYON_NUM_THREADS
        default), and the feature combo `linalg,parallel`.
    (d) Run the baseline suite and confirm it is green (this is the phase's zero-behavior-change
        anchor — it must be green now and again at the end of Plan 02 after probes are removed).
    Do NOT edit any `fdars-core/src/` file in this task or anywhere in this phase.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `/home/simonm/.cache/fdars-bench-tmp` exists (`ls -d` succeeds).
    - The baseline test command's last lines contain `test result: ok.` with `0 failed`.
    - The governor value and logical-core count are captured (recorded in the SUMMARY for reuse in the PROF-01 Environment section).
    - `git status --porcelain fdars-core/src/` is empty (no src edits).
  </acceptance_criteria>
  <done>Baseline suite is green, TMPDIR cache exists, disk freed, environment recorded — ready to author probe benches.</done>
</task>

<task type="tracer">
  <name>Task 2: Author + run ONE throwaway probe bench (fpca_variants) end-to-end</name>
  <files>fdars-core/benches/probe_fpca_variants.rs, fdars-core/Cargo.toml</files>
  <read_first>
    - fdars-core/benches/audit_hotpaths.rs (lines 43-135 for `generate_curves`, sample_size/measurement_time tiers, `black_box` discipline; lines 1185-1208 for `criterion_group!`/`criterion_main!`)
    - fdars-core/src/fpca_variants.rs (public entry points `fsvd`, `ssvd`, `cross_covariance`; alloc hotspots at :488 `DMatrix::from_column_slice(g_dim, g_dim, &gram)` and :740 `DMatrix::from_column_slice(m, m, &c_scaled)`)
    - fdars-core/Cargo.toml (existing `[[bench]]` entries at lines 58-97, all `harness = false`)
  </read_first>
  <action>
    Create `fdars-core/benches/probe_fpca_variants.rs` as a `harness = false` criterion bench that
    copies the audit_hotpaths.rs pattern verbatim: a private `generate_curves(n, m)` helper
    producing a deterministic column-major FdMatrix (copy the formula from audit_hotpaths.rs:43 so it
    stays in sync with tests/alloc_audit_fpca.rs:generate_test_curves), inputs generated OUTSIDE
    `b.iter()`, `black_box()` around inputs and the returned value, and a `criterion_group!` +
    `criterion_main!` at the bottom. Bench `fsvd` (the HIGH-priority gram-matrix alloc path) over the
    locked N×M grid — at minimum the cells n50_m50, n200_m50, n50_m200 (3 cells is enough for the
    tracer; add n1000_m50 only if the small cell timed under ~200 ms). Encode each cell in the
    bench_function name as `n{N}_m{M}`. Tune `sample_size`/`measurement_time`/`warm_up_time` per the
    timing tier in audit_hotpaths.rs (research §Profiling Harness Pattern). Behind
    `#[cfg(feature = "linalg")]` guard the linalg-only calls, mirroring the empty-stub fallback at
    audit_hotpaths.rs:1181 so `criterion_group!` always resolves without the feature.
    Then TEMPORARILY register it in `fdars-core/Cargo.toml` by adding a `[[bench]]` block:
      `[[bench]]`
      `name = "probe_fpca_variants"   # THROWAWAY — Phase 46 profiling only; removed in Plan 02`
      `harness = false`
    Add the block adjacent to the existing bench entries. This temporary registration is what makes
    `cargo bench --bench probe_fpca_variants` runnable (the `harness = false` pattern requires a
    `[[bench]]` entry — research §Running a Single Bench). Do NOT leave this entry permanently
    registered — Plan 02 removes it (permanent bench coverage is Phase 51 / BENCH-01).
    Dry-run the compile first, then run the measurement.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo bench --bench probe_fpca_variants --features linalg,parallel --no-run 2>&1 | tail -3 && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo bench --bench probe_fpca_variants --features linalg,parallel 2>&1 | grep -E "n[0-9]+_m[0-9]+|time:" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `probe_fpca_variants` compiles cleanly under `--features linalg,parallel` (the `--no-run` step succeeds).
    - The run emits criterion output containing at least 2 lines matching `n[0-9]+_m[0-9]+` with a `time:` measurement.
    - `git status --porcelain fdars-core/src/` is empty (no src edits — only benches/ and Cargo.toml changed).
    - The added Cargo.toml `[[bench]]` block contains the literal comment marking it THROWAWAY.
  </acceptance_criteria>
  <done>One throwaway probe bench compiles, is temp-registered, runs, and produces per-cell wall-time numbers for fpca_variants::fsvd.</done>
</task>

<task type="tracer">
  <name>Task 3: One dhat alloc probe + PROF-01 doc skeleton with first measured row</name>
  <files>fdars-core/tests/alloc_audit_new_subsystems.rs, .planning/phases/46-whole-crate-profiling-measurement/PROF-01-hotpath-targets.md</files>
  <read_first>
    - fdars-core/tests/alloc_audit_fpca.rs (lines 1-120: `#[cfg(feature = "dhat-heap")]` global_allocator + `count_*_allocations` test pattern, `dhat::Profiler::builder().testing().build()`, `dhat::HeapStats::get()`, the "do NOT hard-assert" convention)
    - .planning/phases/46-whole-crate-profiling-measurement/46-RESEARCH.md §Three Inventory Document Specifications → Inventory 1 (required doc contents) and §Allocation Hotspots (the fpca_variants:488 gram-matrix site)
  </read_first>
  <action>
    Create `fdars-core/tests/alloc_audit_new_subsystems.rs` as a NEW dhat integration-test file
    (separate binary, so dhat's `#[global_allocator]` occupies its own process — copy the gating
    pattern from tests/alloc_audit_fpca.rs verbatim). Add ONE `#[cfg(feature = "dhat-heap")]`
    `#[global_allocator]` static and ONE `#[test] #[cfg(feature = "dhat-heap")]`
    `count_fsvd_allocations_n200_m50` that: builds inputs OUTSIDE the profiler scope with the same
    `generate_test_curves`-style formula, opens `dhat::Profiler::builder().testing().build()`, calls
    `fpca_variants::fsvd` once, then prints `total_blocks`, `total_bytes`, `max_bytes` from
    `dhat::HeapStats::get()`. Do NOT hard-assert on the numbers — this is a baseline measurement, per
    the alloc_audit_fpca.rs convention.
    Then create `.planning/phases/46-whole-crate-profiling-measurement/PROF-01-hotpath-targets.md`
    as the inventory-doc skeleton with the required sections from research §Inventory 1:
      - `## Environment` — governor (`powersave`, flagged LOW-CONFIDENCE / honesty caveat),
        RAYON_NUM_THREADS (default = logical cores; record the number), feature flags
        (`linalg,parallel`), measurement date. Use the values captured in Task 1.
      - `## Measured Cells` — a table with columns
        `[module] [function] [N] [M] [mean ms] [stddev ms] [allocs]`, and the fpca_variants::fsvd rows
        from Task 2's criterion output plus the dhat total_blocks/total_bytes from this task's probe.
      - `## Ranked Targets` — a placeholder heading noting "expanded in Plan 02 across all 9
        subsystems"; add fpca_variants::fsvd as the first entry with its `file:line` anchor
        `src/fpca_variants.rs:488` and its measured ms number.
      - `## N×M Scaling` — a one-line note that scaling analysis is completed in Plan 02.
    This is a SKELETON that Plan 02 fills out; it must already carry the Environment section and one
    real measured row so the tracer proves the full author→measure→document path.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features dhat-heap,linalg -- count_fsvd_allocations_n200_m50 --nocapture 2>&1 | grep -E "Total heap|Peak heap|total_bytes|max_bytes|blocks" | head && test -f .planning/phases/46-whole-crate-profiling-measurement/PROF-01-hotpath-targets.md && grep -q "src/fpca_variants.rs:488" .planning/phases/46-whole-crate-profiling-measurement/PROF-01-hotpath-targets.md && echo DOC_OK</automated>
  </verify>
  <acceptance_criteria>
    - The dhat test runs under `--features dhat-heap,linalg` and prints heap stats lines (total blocks/bytes, peak bytes).
    - `PROF-01-hotpath-targets.md` exists and contains an `## Environment` heading with governor + feature flags + date.
    - The doc contains the anchor string `src/fpca_variants.rs:488` and a real measured ms number for fpca_variants::fsvd.
    - `git status --porcelain fdars-core/src/` is empty (no src edits).
  </acceptance_criteria>
  <done>A dhat probe emits fsvd allocation counts, and PROF-01-hotpath-targets.md carries its Environment section + first grounded measured row — the full profiling pipeline is proven end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev harness → source crate | Probe benches/tests call public `fdars-core` APIs read-only; no untrusted external input. Local measure-only audit — no network, no user data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-01 | Tampering | Accidental `src/` edit while profiling | medium | mitigate | Every task asserts `git status --porcelain fdars-core/src/` is empty; measure-only scope enforced in each `<acceptance_criteria>`. |
| T-46-02 | Tampering | Throwaway `[[bench]]` left permanently registered | medium | mitigate | THROWAWAY comment marker on the Cargo.toml entry; Plan 02 removes it and asserts no `probe_*` entry survives (Phase 51 owns permanent benches). |
| T-46-SC | Tampering | npm/pip/cargo installs | high | accept | No new crate dependency added (criterion + dhat already dev-deps); no package installs occur in this phase. |
</threat_model>

<verification>
- Baseline suite green before any probe: `cargo test -p fdars-core --features linalg,parallel` → `test result: ok. 0 failed`.
- Probe bench compiles and runs, emitting per-cell `n{N}_m{M}` criterion times.
- dhat probe prints heap stats under `--features dhat-heap,linalg`.
- PROF-01 doc skeleton exists with Environment section + one grounded fpca_variants row (anchor `src/fpca_variants.rs:488`).
- No `fdars-core/src/` file changed.
</verification>

<success_criteria>
- Wave 0 complete (TMPDIR cache, disk freed, environment recorded, baseline green).
- The profiling pipeline is proven end-to-end on `fpca_variants`: bench authored → temp-registered → run → dhat probe → first PROF-01 row written.
- Zero behavior-changing edits to `fdars-core/src/`.
</success_criteria>

<output>
Create `.planning/phases/46-whole-crate-profiling-measurement/46-01-SUMMARY.md` when done.
Record in the SUMMARY: the captured governor value, logical-core count (RAYON_NUM_THREADS default),
and the fpca_variants::fsvd criterion + dhat numbers — Plan 02 reuses the Environment values and
appends to the same PROF-01 doc.
</output>