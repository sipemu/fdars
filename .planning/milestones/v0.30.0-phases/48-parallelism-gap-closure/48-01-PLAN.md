---
phase: 48-parallelism-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/Cargo.toml
  - fdars-core/benches/perf_parallelism.rs
  - fdars-core/tests/equivalence_phase48.rs
  - fdars-core/src/frechet/anova.rs
  - .planning/phases/48-parallelism-gap-closure/PERF-PARALLEL-RESULTS.md
autonomous: true
requirements: [PERF-03]
user_setup: []

estimate:
  tokens: 72000
  raw_tokens: 36000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "frechet_anova p_value_permutation is BIT-IDENTICAL parallel-ON vs parallel-OFF for the same seed (golden test passes under --features linalg,parallel AND --no-default-features --features linalg) (PERF-03)"
    - "frechet_anova parallel output is BIT-IDENTICAL to the CURRENT (pre-parallel) sequential output — determinism preserved because each perm reseeds StdRng::seed_from_u64(seed.wrapping_add(perm as u64)), independent of thread count (PERF-03)"
    - "A payback-threshold outer-if (const FRECHET_ANOVA_PERM_PARALLEL_THRESHOLD) routes n_perm below threshold to the sequential branch; both branches produce the same golden value (PERF-03)"
    - "frechet_anova public signature is byte-identical (no signature change); no new crate dependency added"
    - "A permanent perf_parallelism criterion bench + a permanent tests/equivalence_phase48.rs golden test exist and are wired end-to-end (tracer slice through bench + test + src + ledger)"
    - "The existing full suite stays green under BOTH feature configs and clippy --all-targets is clean"
  artifacts:
    - fdars-core/benches/perf_parallelism.rs
    - fdars-core/tests/equivalence_phase48.rs
    - .planning/phases/48-parallelism-gap-closure/PERF-PARALLEL-RESULTS.md
    - "fdars-core/Cargo.toml [[bench]] name = \"perf_parallelism\""
    - "const FRECHET_ANOVA_PERM_PARALLEL_THRESHOLD in src/frechet/anova.rs"
  key_links:
    - "per-perm reseeding (seed.wrapping_add(perm)) -> iter_maybe_parallel!(0..n_perm).map().sum() is order-independent -> bit-identical p-value"
    - "iter_maybe_parallel! OFF-config compiles to into_iter() (sequential) -> golden holds under both feature configs -> PERF-03 equivalence proof"
    - "perf_parallelism bench registered now -> becomes Phase 51 BENCH-02 regression guard"
---

<objective>
Wave 0 setup + the frechet_anova tracer: build the phase's proof pipeline (permanent `perf_parallelism` criterion bench, permanent `tests/equivalence_phase48.rs` golden-equivalence file, `PERF-PARALLEL-RESULTS.md` ledger) AND drive it end-to-end through the PRIMARY parallelization target — `frechet_anova` (`src/frechet/anova.rs`, PROF-01 133ms #4 hotspot). This is the thin vertical slice that touches every layer the phase uses — a src edit, a bit-identical golden captured under both feature configs, a thread-scaling criterion bench, and the results ledger — proving the whole architecture on one path before Wave 2 (`co_cluster`) expands horizontally.

Purpose: `frechet_anova` is the highest-wall-time, cleanest rewrite (RESEARCH Candidate A: already per-perm seeded, mechanical `iter_maybe_parallel!` swap). Proving the full golden/bench/both-config harness on it de-risks Wave 2.
Output: `benches/perf_parallelism.rs` (+ Cargo.toml `[[bench]]`), `tests/equivalence_phase48.rs`, the `frechet_anova` src edit in `anova.rs` (+ threshold const), and `PERF-PARALLEL-RESULTS.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/48-parallelism-gap-closure/48-CONTEXT.md
@.planning/phases/48-parallelism-gap-closure/48-RESEARCH.md
@.planning/phases/48-parallelism-gap-closure/48-VALIDATION.md

# Patterns to mirror (permanent bench + golden equivalence, both from Phase 47):
@fdars-core/tests/equivalence_phase47.rs
@fdars-core/benches/perf_hotpaths.rs
</context>

<operational_notes>
Honor MEMORY.md operational pointers on EVERY task (bake into commands):
- Prefix ALL build/test/bench with `TMPDIR=/home/simonm/.cache/fdars-bench-tmp` (create dir if absent; /tmp tmpfs exhaustion causes bogus "No space left").
- Wave-0 disk step (run once, first thing): `rm -rf target/debug/{incremental,examples}` to free space before bench builds.
- This is a BEHAVIOR-CHANGING phase: the suite must stay green at every commit under BOTH feature configs — the parallel-OFF build (`--no-default-features --features linalg`) must compile AND pass as part of the gate.
- Full lint gate is `cargo clippy --all-targets --features linalg,parallel -- -D warnings` (a plain `-p ... -D warnings` false-greens on test/bench code).
- Commit with `git commit --no-verify` (long-cargo pre-commit hook trips the watchdog) AND run `cargo fmt -p fdars-core` immediately before each commit (the --no-verify dodge otherwise leaves CI fmt drift).
</operational_notes>

<tasks>

<task type="tracer">
  <name>Task 1: Scaffold the permanent proof pipeline (perf_parallelism bench + equivalence_phase48.rs + PERF-PARALLEL-RESULTS.md) and baseline-green under BOTH feature configs</name>
  <files>fdars-core/Cargo.toml, fdars-core/benches/perf_parallelism.rs, fdars-core/tests/equivalence_phase48.rs, .planning/phases/48-parallelism-gap-closure/PERF-PARALLEL-RESULTS.md</files>
  <read_first>
    - fdars-core/benches/perf_hotpaths.rs (criterion pattern: build data OUTSIDE b.iter(), black_box inputs+outputs, group.sample_size(N), group.measurement_time(Duration), criterion_group!/criterion_main!)
    - fdars-core/tests/equivalence_phase47.rs lines 1-51 (module header + assert_rel_close + generate_curves helper conventions to mirror — for THIS phase, prefer EXACT bit-identical asserts on p_value_permutation, not tolerance)
    - fdars-core/Cargo.toml lines 58-100 (existing [[bench]] harness=false entries; the perf_hotpaths entry at 98-100 is the template)
    - fdars-core/src/frechet/anova.rs lines 122-128 (frechet_anova signature: responses: &FdMatrix, argvals: &[f64], group_labels: &[usize], n_perm: usize, seed: u64) and lines 353-361 (anova_permutation_is_seed_reproducible test — the two_group / uniform_grid data helpers to mirror for deterministic bench/golden data)
  </read_first>
  <action>
    First run `mkdir -p /home/simonm/.cache/fdars-bench-tmp` and `rm -rf target/debug/{incremental,examples}` (disk step).

    Create `fdars-core/benches/perf_parallelism.rs` mirroring `perf_hotpaths.rs` structure. Add ONE benched cell now: `bench_frechet_anova` at the PROF-01 cell (n=24 curves, m=81 argvals, n_perm=999, seed=42 — the RESEARCH thread-scaling cell). Build a deterministic two-group `FdMatrix` + argvals + labels generator OUTSIDE `b.iter()` (mirror the two_group phase/amplitude formula from anova.rs tests, or a simple deterministic sinusoid with a group-dependent shift — NO RNG in the data generator). Wrap inputs and the `frechet_anova(...)` output in `black_box`. Use `group.sample_size(10)` and `group.measurement_time(Duration::from_secs(60))` (n_perm=999 is expensive). Add a module-header comment documenting how to run the thread-scaling sweep: `RAYON_NUM_THREADS=1 cargo bench --bench perf_parallelism` vs `RAYON_NUM_THREADS=20 ...`, AND note that Task 4 will optionally add an in-bench `rayon::ThreadPoolBuilder` scoped-pool variant if the env-var sweep proves noisy. Register `criterion_group!(benches, bench_frechet_anova); criterion_main!(benches);` (the co_cluster cell is added by Wave 2).

    Add to `fdars-core/Cargo.toml` (after the existing perf_hotpaths entry at lines 98-100): a `[[bench]]` with name = "perf_parallelism" and harness = false, with an inline comment `# PERMANENT — Phase 48 PERF-03 thread-scaling proof / Phase 51 BENCH-02 regression guard`. Do NOT mark it throwaway.

    Create `fdars-core/tests/equivalence_phase48.rs` as the PERMANENT golden-equivalence module. Add a documented module header explaining: this is a BEHAVIOR-PRESERVING parallelism phase; each golden reference value is the CURRENT (pre-parallel) seeded output of the target fn, hardcoded as a const; the parallelized code must reproduce it BIT-IDENTICALLY (exact `assert_eq!`/`==`, NOT tolerance — the p-value is a rational `(n_ge+1)/(n_perm+1)` and co_cluster reductions are deterministic max over integer/f64), and the test must pass under BOTH `--features linalg,parallel` AND `--no-default-features --features linalg`. Add a deterministic `two_group_data(n_per_group, m)` helper (mirror anova.rs test helper) reused by the frechet golden. Include one compile-clean smoke test (e.g. `assert_eq!(2 + 2, 4)`). The per-fn golden tests are added by their owning tasks — this task establishes the file + helpers + header.

    Create `.planning/phases/48-parallelism-gap-closure/PERF-PARALLEL-RESULTS.md` with: an Environment block (governor, logical cores, RAYON_NUM_THREADS default, feature flags, host tmp, date) — attempt `sudo cpupower frequency-set -g performance`; if sudo unavailable, record governor=`powersave` with the LOW-CONFIDENCE caveat verbatim. Add empty 1-thread-vs-N-thread median tables for `frechet_anova` and `co_cluster` to be filled in Wave 3, plus a "Threshold constants" section (to record FRECHET_ANOVA_PERM_PARALLEL_THRESHOLD and CO_CLUSTER_INIT_PARALLEL_THRESHOLD) and a "Deferrals" section stub.

    Confirm baseline-green before any src edit: run the full suite under BOTH feature configs.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel 2>&1 | tail -5</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --no-default-features --features linalg 2>&1 | tail -5</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --benches --features linalg,parallel 2>&1 | tail -5</automated>
    <acceptance_criteria>
      - perf_parallelism bench compiles and is registered in Cargo.toml as harness=false
      - equivalence_phase48.rs compiles under both feature configs; smoke test passes
      - full existing suite green under --features linalg,parallel AND --no-default-features --features linalg
      - PERF-PARALLEL-RESULTS.md exists with Environment + empty result tables + Threshold + Deferrals stubs
    </acceptance_criteria>
  </verify>
  <done>perf_parallelism bench compiles and is registered; equivalence_phase48.rs compiles under both configs; PERF-PARALLEL-RESULTS.md scaffolded; full suite green under both feature configs (baseline confirmed before any src edit).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the frechet_anova golden equivalence test capturing current (pre-parallel) p-value</name>
  <files>fdars-core/tests/equivalence_phase48.rs</files>
  <read_first>
    - fdars-core/src/frechet/anova.rs lines 169-195 (the CURRENT sequential n_ge loop at 170-181 with per-perm StdRng::seed_from_u64(seed.wrapping_add(perm as u64)) at :172, and the p_permutation = (n_ge+1)/(n_perm+1) at :182; FrechetAnovaResult fields returned at 184-194)
    - fdars-core/src/frechet/anova.rs lines 353-361 (anova_permutation_is_seed_reproducible: two_group + uniform_grid data shape; asserts a.p_value_permutation == b.p_value_permutation — proves seed-reproducibility already)
    - fdars-core/tests/equivalence_phase48.rs (the two_group_data helper + module header from Task 1)
    - 48-RESEARCH.md Candidate A "Proof of equivalence" (golden asserts bit-identical p_value_permutation under both feature configs; the n_perm >= threshold branch AND the below-threshold branch must both reproduce it)
  </read_first>
  <behavior>
    - golden_frechet_anova_parallel: frechet_anova on deterministic two-group data (n=24 total, m=81, seed=42) with n_perm=999 (ABOVE threshold → parallel branch) produces a p_value_permutation exactly equal to the hardcoded reference captured from the CURRENT sequential code.
    - golden_frechet_anova_below_threshold: the SAME data with a small n_perm (e.g. 50, BELOW the threshold → sequential branch) produces a p-value exactly equal to its own reference — proving the below-threshold sequential branch is bit-identical too.
    - Both must hold under --features linalg,parallel AND --no-default-features --features linalg.
  </behavior>
  <action>
    Add `#[test] fn golden_frechet_anova_parallel()` and `#[test] fn golden_frechet_anova_below_threshold()` to `equivalence_phase48.rs`. Build deterministic data via the `two_group_data(12, 81)` helper (n=24 total across 2 groups, m=81 argvals) with `argvals = (0..81).map(|j| j as f64/80.0).collect()` (or a uniform grid mirroring the anova test). Call `fdars_core::frechet::frechet_anova(&data, &argvals, &labels, 999, 42).unwrap()` for the parallel case and `..., 50, 42` for the below-threshold case.

    Capture the reference the way Phase 47 did: run each test once against a placeholder const (it fails, prints the actual `p_value_permutation` and `statistic` at `{:.17e}`), then paste the printed values as `const FRECHET_ANOVA_P999: f64 = ...;` etc. Assert BIT-IDENTICAL: `assert_eq!(result.p_value_permutation, FRECHET_ANOVA_P999)` and `assert_eq!(result.statistic, FRECHET_ANOVA_STAT)` — NOT assert_rel_close (the p-value is an exact rational and `statistic` is deterministic; this phase requires bit-identity, and the golden is captured from the SAME code path the parallelization must reproduce). Add a comment: "captured from pre-parallel sequential frechet_anova; must remain bit-identical after iter_maybe_parallel! swap and under both feature configs."

    IMPORTANT: capture these constants from the CURRENT (pre-parallel) `anova.rs` — i.e. author and commit this test BEFORE Task 3's src edit, so the goldens pin the existing behavior.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel -- golden_frechet_anova --nocapture 2>&1 | tail -8</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --no-default-features --features linalg -- golden_frechet_anova --nocapture 2>&1 | tail -8</automated>
    <acceptance_criteria>
      - golden_frechet_anova_parallel and golden_frechet_anova_below_threshold pass against reference constants captured from the current (pre-parallel) code
      - both goldens pass under --features linalg,parallel AND --no-default-features --features linalg (identical constants under both configs)
      - the tests are committed BEFORE any anova.rs parallelization edit
    </acceptance_criteria>
  </verify>
  <done>Both frechet_anova goldens pass under both feature configs against constants captured from the current sequential code; committed before the Task 3 src edit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Parallelize frechet_anova — iter_maybe_parallel! over 0..n_perm with a payback-threshold outer-if; preserve per-perm seeding</name>
  <files>fdars-core/src/frechet/anova.rs</files>
  <read_first>
    - fdars-core/src/frechet/anova.rs lines 169-182 (the exact sequential loop to replace: `let mut n_ge = 0usize; for perm in 0..n_perm { let mut rng = StdRng::seed_from_u64(seed.wrapping_add(perm as u64)); let mut perm_labels = group_labels.to_vec(); perm_labels.shuffle(&mut rng); if let Ok((tn_perm, _, _, _, _)) = compute_tn_generic(&space, &objects, &perm_labels, k) { if tn_perm >= tn_obs { n_ge += 1; } } }`)
    - fdars-core/src/frechet/anova.rs lines 16-18 (existing `use rand::rngs::StdRng; use rand::seq::SliceRandom; use rand::SeedableRng;` — iter_maybe_parallel! is NOT yet imported)
    - 48-RESEARCH.md Candidate A "Exact iter_maybe_parallel! rewrite" (the map+sum form + threshold outer-if) and "Pitfall 2" (.sum::<usize>() is exact — NO Mutex/AtomicUsize) and "Pitfall 3" (add `use crate::iter_maybe_parallel;`)
    - fdars-core/src/elastic_fpca.rs lines 25-28 and 796-804 (the SCORES_PARALLEL_THRESHOLD const + outer-if guard precedent to mirror exactly)
  </read_first>
  <behavior>
    - After the swap, golden_frechet_anova_parallel (n_perm=999, parallel branch) still passes BIT-IDENTICALLY — because each perm reseeds StdRng::seed_from_u64(seed.wrapping_add(perm as u64)) so the RNG stream depends only on perm, never on thread/order; the .sum::<usize>() is exact.
    - golden_frechet_anova_below_threshold (n_perm=50, sequential branch) still passes.
    - The below-threshold sequential branch and the above-threshold parallel branch produce the same p-value for the same n_perm.
    - anova_permutation_is_seed_reproducible (existing test at :353) still passes UNCHANGED (values not re-baselined).
  </behavior>
  <action>
    Add `use crate::iter_maybe_parallel;` to the imports at the top of `anova.rs` (Pitfall 3).

    Add a module-level const near the top of the module (mirroring elastic_fpca.rs's SCORES_PARALLEL_THRESHOLD doc-comment style):
    `const FRECHET_ANOVA_PERM_PARALLEL_THRESHOLD: usize = 200;` with a doc-comment explaining n_perm is the work driver; below ~200 permutations rayon dispatch overhead can exceed the per-perm compute_tn_generic cost; the value is conservative and Wave 3 criterion measurement confirms/adjusts it.

    Replace the sequential `n_ge` loop (lines 170-181) with a payback-threshold outer-if that follows RESEARCH Candidate A verbatim:
    - `let n_ge: usize = if n_perm >= FRECHET_ANOVA_PERM_PARALLEL_THRESHOLD { iter_maybe_parallel!(0..n_perm).map(|perm| { let mut rng = StdRng::seed_from_u64(seed.wrapping_add(perm as u64)); let mut perm_labels = group_labels.to_vec(); perm_labels.shuffle(&mut rng); match compute_tn_generic(&space, &objects, &perm_labels, k) { Ok((tn_perm, _, _, _, _)) if tn_perm >= tn_obs => 1usize, _ => 0usize, } }).sum() } else { /* sequential branch identical to the current code, accumulating into a local count */ };`
    - The sequential else-branch MUST reproduce the exact current logic (per-perm reseed, degenerate-skip contributes 0). Use `.sum::<usize>()` — do NOT introduce Mutex/AtomicUsize (Pitfall 2).
    - Keep the degenerate-permutation skip (`Ok(...) if tn_perm >= tn_obs => 1, _ => 0`) so a compute error contributes 0 in BOTH branches (order-independent).

    CRITICAL CONSTRAINTS (acceptance-gated):
    - Do NOT change the `frechet_anova` public signature (byte-identical — grep-verify).
    - Preserve the per-perm seeding formula `StdRng::seed_from_u64(seed.wrapping_add(perm as u64))` EXACTLY (this is the determinism linchpin; the goldens must stay bit-identical, values NOT re-baselined).
    - Do NOT parallelize `frechet_anova_space<S: MetricSpace>` (lines 212+) in this task — that needs an `S: Sync` bound which is a public-generic signature change; it is DEFER-with-rationale (documented in Wave 3). Only the concrete `frechet_anova` is parallelized here; RESEARCH confirms this already covers the PROF-01 hotspot.
    - Add NO new crate dependency.

    Confirm both feature configs compile and the goldens + existing determinism test pass.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel -- golden_frechet_anova anova_permutation_is_seed_reproducible 2>&1 | tail -8</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --no-default-features --features linalg -- golden_frechet_anova anova_permutation_is_seed_reproducible 2>&1 | tail -8</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel 2>&1 | tail -5</automated>
    <acceptance_criteria>
      - golden_frechet_anova_parallel + golden_frechet_anova_below_threshold pass under --features linalg,parallel AND --no-default-features --features linalg (bit-identical, both configs)
      - anova_permutation_is_seed_reproducible passes with UNCHANGED values (no re-baseline)
      - full existing suite green under both feature configs
      - frechet_anova public signature byte-identical (grep the fn signature)
      - frechet_anova_space is NOT modified (still sequential; grep confirms no iter_maybe_parallel! added there)
      - no new dependency in Cargo.toml
    </acceptance_criteria>
  </verify>
  <done>frechet_anova parallelized via iter_maybe_parallel! with FRECHET_ANOVA_PERM_PARALLEL_THRESHOLD outer-if; per-perm seeding preserved; goldens bit-identical under both configs; existing determinism test green with unchanged values; signature unchanged; frechet_anova_space untouched.</done>
</task>

<task type="auto">
  <name>Task 4: Run the frechet_anova thread-scaling cell + wave-1 gate (both configs + clippy --all-targets); record ledger</name>
  <files>.planning/phases/48-parallelism-gap-closure/PERF-PARALLEL-RESULTS.md</files>
  <read_first>
    - fdars-core/benches/perf_parallelism.rs (the bench_frechet_anova cell from Task 1)
    - 48-VALIDATION.md "Sampling Rate" + "Validation Sign-Off" (per-wave: full suite ON + parallel-OFF + clippy --all-targets; phase gate: >=1 thread-scaling cell shows speedup at large input)
    - 48-RESEARCH.md "Thread-Scaling Bench Pattern" (RAYON_NUM_THREADS=1 vs =20; governor caveat) and "Payback threshold rationale" for frechet_anova
  </read_first>
  <action>
    Run the frechet_anova criterion cell under a 1-thread and a 20-thread sweep to capture the thread-scaling signal: `RAYON_NUM_THREADS=1 TMPDIR=... cargo bench -p fdars-core --features linalg,parallel --bench perf_parallelism -- frechet_anova` and again with `RAYON_NUM_THREADS=20`. Attempt governor pinning (`sudo cpupower frequency-set -g performance` before, `-g powersave` after); if sudo unavailable, record the powersave LOW-CONFIDENCE caveat.

    Append both medians to PERF-PARALLEL-RESULTS.md (frechet_anova n24_m81_nperm999 row: 1-thread median vs 20-thread median + the ratio). If the 20-thread median is NOT clearly below the 1-thread median (target: 20-thread < 0.6× 1-thread at n_perm=999), note it and flag for Wave 3 threshold review — but a real speedup is expected here (RESEARCH: ~10-20× ceiling). Record the chosen FRECHET_ANOVA_PERM_PARALLEL_THRESHOLD const in the Threshold section.

    Run the wave-1 merge gate: full suite under BOTH feature configs + `cargo clippy --all-targets --features linalg,parallel -- -D warnings` (must be clean — lints the new bench + test code).

    Commit sequence for this plan (each with `cargo fmt -p fdars-core` first, then `git commit --no-verify`): the scaffold (Task 1), the goldens (Task 2), the frechet_anova src edit (Task 3), and the ledger update (Task 4).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy -p fdars-core --all-targets --features linalg,parallel -- -D warnings 2>&1 | tail -5</automated>
    <automated>RAYON_NUM_THREADS=1 TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo bench -p fdars-core --features linalg,parallel --bench perf_parallelism -- frechet_anova 2>&1 | tail -10</automated>
    <automated>RAYON_NUM_THREADS=20 TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo bench -p fdars-core --features linalg,parallel --bench perf_parallelism -- frechet_anova 2>&1 | tail -10</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --no-default-features --features linalg 2>&1 | tail -5</automated>
    <acceptance_criteria>
      - clippy --all-targets clean (zero warnings, lints bench + test code)
      - perf_parallelism/frechet_anova cell runs and reports 1-thread and 20-thread medians
      - PERF-PARALLEL-RESULTS.md frechet_anova row records 1-vs-20-thread medians + ratio + governor note + the chosen threshold const
      - parallel-OFF suite (--no-default-features --features linalg) green
    </acceptance_criteria>
  </verify>
  <done>frechet_anova proven end-to-end (bit-identical golden under both configs + thread-scaling bench + ledger); clippy --all-targets clean; both feature configs green; the proof pipeline is established for Wave 2 to reuse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure in-process numerical parallelization — no external input, I/O, auth, or network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-01 | Tampering | frechet_anova p-value after parallelization (order/thread-dependence could silently change output) | high | mitigate | bit-identical golden captured pre-change, asserted under BOTH feature configs; per-perm reseed preserved; existing seed-reproducibility test kept with unchanged values |
| T-48-02 | Tampering | accidental frechet_anova_space signature widening (S: Sync) breaking external MetricSpace impls | medium | mitigate | frechet_anova_space explicitly NOT modified this plan (DEFER-with-rationale in Wave 3) |
| T-48-03 | Denial-of-Service | new bench/test code fails CI clippy --all-targets gate | low | mitigate | clippy --all-targets gate in Task 4 |

No package installs in this plan (rayon + criterion already deps; NO new dependency). No T-48-SC.
</threat_model>

<verification>
- frechet_anova goldens (n_perm=999 parallel branch + below-threshold sequential branch) pass BIT-IDENTICALLY under --features linalg,parallel AND --no-default-features --features linalg.
- anova_permutation_is_seed_reproducible passes with unchanged values (determinism preserved).
- payback-threshold outer-if routes below-threshold n_perm to sequential; both branches produce the same golden.
- frechet_anova public signature byte-identical; frechet_anova_space untouched; no new dependency.
- perf_parallelism bench registered + runnable (permanent — Phase 51 BENCH-02 guard); ≥1 thread-scaling cell shows speedup.
- full suite green under both feature configs + clippy --all-targets clean.
</verification>

<success_criteria>
- The permanent proof pipeline (perf_parallelism bench, equivalence_phase48.rs, PERF-PARALLEL-RESULTS.md) exists and is wired end-to-end through frechet_anova.
- frechet_anova parallelized behavior-preservingly: bit-identical output under both configs, per-perm seeding preserved, payback-threshold guard in place, thread-scaling speedup measured.
- No public signature changed; no new dependency; suite + clippy green under both feature configs.
</success_criteria>

<output>
Create `.planning/phases/48-parallelism-gap-closure/48-01-SUMMARY.md` when done. Record the frechet_anova 1-vs-20-thread medians, the chosen FRECHET_ANOVA_PERM_PARALLEL_THRESHOLD, confirmation the goldens are bit-identical under both configs, and the governor state.
</output>
