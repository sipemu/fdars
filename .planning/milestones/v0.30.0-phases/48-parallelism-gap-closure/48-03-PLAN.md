---
phase: 48-parallelism-gap-closure
plan: 03
type: execute
wave: 3
depends_on: [48-01, 48-02]
files_modified:
  - .planning/phases/48-parallelism-gap-closure/PERF-PARALLEL-RESULTS.md
  - .planning/phases/48-parallelism-gap-closure/48-VALIDATION.md
  - fdars-core/src/frechet/anova.rs
  - fdars-core/src/coclustering.rs
autonomous: true
requirements: [PERF-03]
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Thread-scaling measured for frechet_anova and co_cluster (1-thread vs 20-thread criterion medians); the parallel path is faster on large inputs — recorded in PERF-PARALLEL-RESULTS.md with the governor caveat (PERF-03)"
    - "The payback-threshold consts (FRECHET_ANOVA_PERM_PARALLEL_THRESHOLD, CO_CLUSTER_INIT_PARALLEL_THRESHOLD) are finalized against the measured break-even and documented (PERF-03)"
    - "The DEFERRED targets (t_perm_test/f_perm_test, frechet_anova_space S:Sync, explain/importance) are documented with rationale in PERF-PARALLEL-RESULTS.md — NOT silently dropped (PERF-03)"
    - "Final gate: both feature configs green, all Phase-48 goldens bit-identical, clippy --all-targets clean, no public signature changed"
    - "48-VALIDATION.md sign-off checklist satisfied; nyquist_compliant set true"
  artifacts:
    - ".planning/phases/48-parallelism-gap-closure/PERF-PARALLEL-RESULTS.md (finalized: thread-scaling + thresholds + deferrals)"
    - ".planning/phases/48-parallelism-gap-closure/48-VALIDATION.md (signed off)"
  key_links:
    - "measured 1-vs-N-thread medians -> finalized threshold consts -> payback guard prevents small-input regression"
    - "deferral rationale (t_perm/f_perm change output; frechet_anova_space needs S:Sync signature widening; explain low-fanout) -> satisfies CONTEXT.md 'defer marginal/unsafe with rationale'"
---

<objective>
Wave 3: measure the thread-scaling win for the two parallelized paths (frechet_anova, co_cluster),
finalize the payback-threshold consts against the measured break-even, document the DEFERRED targets
with rationale, finalize PERF-PARALLEL-RESULTS.md, and complete the 48-VALIDATION.md sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-parallelism-gap-closure/48-CONTEXT.md
@.planning/phases/48-parallelism-gap-closure/48-RESEARCH.md
@.planning/phases/48-parallelism-gap-closure/48-VALIDATION.md
@.planning/phases/48-parallelism-gap-closure/PERF-PARALLEL-RESULTS.md
</context>

<operational_notes>
Same as Plan 01: `TMPDIR=/home/simonm/.cache/fdars-bench-tmp`; clippy `--all-targets --features linalg,parallel -- -D warnings`; `cargo fmt -p fdars-core` + `git commit --no-verify` per commit; run the parallel-OFF suite in the gate. For thread scaling, prefer `RAYON_NUM_THREADS=1 cargo bench --bench perf_parallelism` vs `RAYON_NUM_THREADS=20 cargo bench --bench perf_parallelism` (each cell records a median); if the env sweep is noisy under `powersave`, the honest signal is the ratio. Attempt `sudo cpupower frequency-set -g performance`; record governor state either way.
</operational_notes>

<tasks>

<task type="auto">
  <name>Task 1: Measure thread-scaling (1 vs 20 threads) for frechet_anova + co_cluster; finalize thresholds</name>
  <files>.planning/phases/48-parallelism-gap-closure/PERF-PARALLEL-RESULTS.md, fdars-core/src/frechet/anova.rs, fdars-core/src/coclustering.rs</files>
  <read_first>
    - fdars-core/benches/perf_parallelism.rs (the bench_frechet_anova + bench_co_cluster cells)
    - .planning/phases/48-parallelism-gap-closure/PERF-PARALLEL-RESULTS.md (median tables + Threshold-constants section)
    - 48-RESEARCH.md payback-threshold estimates (FRECHET_ANOVA_PERM ~200, CO_CLUSTER_INIT ~3) and the note that criterion refines them
  </read_first>
  <action>
    Run each perf_parallelism cell at 1 thread and at 20 threads:
      `RAYON_NUM_THREADS=1 TMPDIR=... cargo bench -p fdars-core --features linalg,parallel --bench perf_parallelism -- frechet_anova`
      `RAYON_NUM_THREADS=20 TMPDIR=... cargo bench -p fdars-core --features linalg,parallel --bench perf_parallelism -- frechet_anova`
      (same for `co_cluster`). Record the medians in PERF-PARALLEL-RESULTS.md (1-thread vs 20-thread, ratio, governor caveat). The parallel path should be materially faster at the large cell (n_perm=999 / n_init=8).

    Finalize the threshold consts: if the measured break-even differs from the research estimates,
    update `FRECHET_ANOVA_PERM_PARALLEL_THRESHOLD` (src/frechet/anova.rs) and
    `CO_CLUSTER_INIT_PARALLEL_THRESHOLD` (src/coclustering.rs) to a conservative value at or above the
    measured crossover, and record the rationale. If the estimates hold, keep them and note "validated by
    thread-scaling". Do NOT change any numeric computation — only the threshold consts.
  </action>
  <verify>
    <automated>grep -E "frechet_anova|co_cluster|THRESHOLD" .planning/phases/48-parallelism-gap-closure/PERF-PARALLEL-RESULTS.md | head</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel -- golden_ 2>&1 | tail -4</automated>
    <acceptance_criteria>
      - PERF-PARALLEL-RESULTS.md records 1-thread vs 20-thread medians for both cells + the ratio + governor state
      - the two threshold consts are finalized (kept-or-updated) with a recorded rationale; goldens still pass (threshold change does not alter output above/below correctly)
    </acceptance_criteria>
  </verify>
  <done>Thread-scaling recorded; the parallel path is faster on large inputs; payback thresholds finalized against the measured break-even.</done>
</task>

<task type="auto">
  <name>Task 2: Document the deferred parallelization targets with rationale; finalize PERF-PARALLEL-RESULTS.md</name>
  <files>.planning/phases/48-parallelism-gap-closure/PERF-PARALLEL-RESULTS.md</files>
  <read_first>
    - 48-RESEARCH.md deferral findings (t_perm_test/f_perm_test shared advancing RNG; frechet_anova_space S:Sync bound; explain/importance low fan-out)
    - 48-CONTEXT.md <deferred> block + the "defer marginal/unsafe with rationale" decision
  </read_first>
  <action>
    Add/finalize a "Deferred (documented)" section in PERF-PARALLEL-RESULTS.md listing each NOT-parallelized target with its rationale:
      - `t_perm_test` / `f_perm_test` (src/inference/permutation.rs) — use a SINGLE shared advancing StdRng across permutations; parallelizing requires per-perm reseeding which CHANGES the returned p-values (a numeric-output change) → OUT OF SCOPE for a behavior-preserving milestone. (Could be revisited in a future milestone that accepts a re-baseline.)
      - `frechet_anova_space` (generic `<S: MetricSpace>`) — parallelizing needs an added `S: Sync` bound, a public-signature widening that could break external non-Sync MetricSpace impls → deferred (the concrete `frechet_anova` already captures the PROF-01 hotspot). Revisit if MetricSpace is confirmed sealed/internal.
      - `explain/importance.rs` (:131, :221) — low outer-loop fan-out (ncomp 3-10) + shared inner rng; Phase 49 CONS-02 will fold this into the already-parallel `explain_generic` path, gaining parallelism for free.
    Also finalize the results doc: consolidated thread-scaling table (frechet_anova, co_cluster), threshold-constants section, Environment block with governor caveat, and a note that perf_parallelism benches feed Phase 51 BENCH-02.
  </action>
  <verify>
    <automated>grep -Eic "t_perm_test|f_perm_test|frechet_anova_space|explain" .planning/phases/48-parallelism-gap-closure/PERF-PARALLEL-RESULTS.md</automated>
    <acceptance_criteria>
      - PERF-PARALLEL-RESULTS.md "Deferred (documented)" section names t_perm_test/f_perm_test, frechet_anova_space, and explain/importance, each with a one-line rationale
      - the doc has the consolidated thread-scaling table + threshold constants + Environment block
    </acceptance_criteria>
  </verify>
  <done>All deferred targets documented with rationale; PERF-PARALLEL-RESULTS.md finalized.</done>
</task>

<task type="auto">
  <name>Task 3: Final phase gate (both feature configs + clippy + goldens) + VALIDATION.md sign-off</name>
  <files>.planning/phases/48-parallelism-gap-closure/48-VALIDATION.md</files>
  <read_first>
    - .planning/phases/48-parallelism-gap-closure/48-VALIDATION.md (Validation Sign-Off checklist; nyquist_compliant/wave_0_complete frontmatter)
    - 48-CONTEXT.md <decisions> (bit-identical + threshold + determinism + both-config gates)
  </read_first>
  <action>
    Run the final phase gate:
      - Full suite (parallel ON): `cargo test -p fdars-core --features linalg,parallel` green.
      - Full suite (parallel OFF): `cargo test -p fdars-core --no-default-features --features linalg` green.
      - All Phase-48 goldens pass under BOTH configs: `golden_frechet_anova_parallel`,
        `golden_frechet_anova_below_threshold`, `golden_co_cluster_parallel`, `golden_co_cluster_below_threshold`.
      - Full clippy: `cargo clippy --all-targets --features linalg,parallel -- -D warnings` clean.
      - Confirm no public signature changed: grep `pub fn frechet_anova` and `pub fn co_cluster` — byte-identical to pre-phase.
    Update 48-VALIDATION.md: check off every sign-off item, mark the Per-Task Verification Map rows green,
    set frontmatter `status: validated`, `nyquist_compliant: true`, `wave_0_complete: true`, dated Approval.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel -- golden_ 2>&1 | tail -6</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --no-default-features --features linalg -- golden_ 2>&1 | tail -6</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy -p fdars-core --all-targets --features linalg,parallel -- -D warnings 2>&1 | tail -3</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel 2>&1 | tail -5</automated>
    <acceptance_criteria>
      - all Phase-48 goldens pass under BOTH feature configs (bit-identical parallel/sequential + ON/OFF)
      - full suite green under both configs; clippy --all-targets clean
      - 48-VALIDATION.md nyquist_compliant: true, wave_0_complete: true, all sign-off items checked
      - no public signature changed (frechet_anova + co_cluster byte-identical)
    </acceptance_criteria>
  </verify>
  <done>Phase 48 verified: goldens bit-identical under both configs, thread-scaling measured, deferrals documented, VALIDATION.md signed off; no signature changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure in-process numerical parallelization — no external input, I/O, auth, or network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-06 | Repudiation | deferred targets look like omissions at verification | low | mitigate | explicit "Deferred (documented)" section in PERF-PARALLEL-RESULTS.md per CONTEXT.md policy |
| T-48-07 | Repudiation | sign-off recorded without both configs actually green | medium | mitigate | the sign-off edit is gated behind the live both-config suite runs in the same task's verify |

No package installs. No new dependency.
</threat_model>

<verification>
- Thread-scaling recorded (1 vs 20 threads) for frechet_anova + co_cluster; parallel faster on large inputs.
- Payback thresholds finalized; deferrals (t_perm/f_perm, frechet_anova_space, explain) documented with rationale.
- Both feature configs green + clippy --all-targets clean; all Phase-48 goldens bit-identical; no signature change.
- 48-VALIDATION.md signed off (nyquist_compliant: true).
</verification>

<success_criteria>
- PERF-03 met: the two safe parallelizations show thread-scaling speedup with payback guards, bit-identical output under both configs; unsafe/marginal targets documented+deferred.
- Phase 48 validated; no public signature changed; no new dependency.
</success_criteria>

<output>
Create `.planning/phases/48-parallelism-gap-closure/48-03-SUMMARY.md` when done. Record the thread-scaling ratios, the finalized threshold consts, the governor state, and the deferral list.
</output>
