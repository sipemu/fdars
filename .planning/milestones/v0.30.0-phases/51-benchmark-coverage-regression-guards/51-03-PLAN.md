---
phase: 51-benchmark-coverage-regression-guards
plan: 03
type: execute
wave: 3
depends_on: [51-02]
files_modified:
  - fdars-core/benches/fem_smoothing_benchmarks.rs
  - fdars-core/benches/density_fda_benchmarks.rs
  - fdars-core/benches/fpca_variants_benchmarks.rs
  - fdars-core/benches/face_benchmarks.rs
  - fdars-core/Cargo.toml
autonomous: true
requirements: [BENCH-01]
estimate:
  tokens: 22000
  raw_tokens: 14000
  tasks: 4
  confidence: med
must_haves:
  truths:
    - "4 new benches (fem_smoothing::fem_smooth_gcv, density_fda::lqd_fpca, fpca_variants::fpca_der, irreg_fdata::mface_covariance) compile under `cargo build --benches --features linalg,parallel` (BENCH-01)"
    - "`cargo clippy --all-targets --features linalg,parallel -- -D warnings` clean with all 4 registered"
    - "each new bench runs its cell once (--quick) without panicking"
    - "all 9 BENCH-01 module benches now registered (5 from 51-01/51-02 + 4 here)"
  artifacts:
    - fdars-core/benches/fem_smoothing_benchmarks.rs
    - fdars-core/benches/density_fda_benchmarks.rs
    - fdars-core/benches/fpca_variants_benchmarks.rs
    - fdars-core/benches/face_benchmarks.rs
    - "fdars-core/Cargo.toml — 4 new [[bench]] harness=false entries (9 module benches total)"
  key_links:
    - "Each Cargo.toml [[bench]] name ↔ its benches/<module>_benchmarks.rs filename"
    - "face bench imports from fdars_core::irreg_fdata:: (mface_covariance + IrregFdata) — module path VERIFIED src/irreg_fdata/face.rs:263"
    - "fem_smooth_gcv uses SMALLER mesh (k=16 = 256 nodes) than the 576 already benched — GCV multiplies O(N³) by n_grid"
---

<objective>
BENCH-01 expansion (wave 3 of 3, final): Add the last 4 `[[bench]]` files — `fem_smoothing::fem_smooth_gcv` (fem_smooth already benched), `density_fda::lqd_fpca` (no coverage), `fpca_variants::fpca_der` (no coverage), `irreg_fdata::mface_covariance` (face_covariance already benched). Each mirrors the tracer (51-01) pipeline exactly. After this plan, all 9 BENCH-01 modules are covered.

Purpose: complete BENCH-01 — cover each remaining module's representative sibling entry, never duplicating an already-benched cell.
Output: 4 new bench files + 4 Cargo.toml `[[bench]]` entries; 9 module benches total, all green under the primary clippy gate.

NOTE: shares fdars-core/Cargo.toml with 51-01/51-02 → runs in SEPARATE wave (3) after 51-02 to serialize Cargo.toml edits.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-benchmark-coverage-regression-guards/51-RESEARCH.md

# Templates to mirror:
@fdars-core/benches/perf_hotpaths.rs
@fdars-core/benches/perf_parallelism.rs
@.planning/phases/51-benchmark-coverage-regression-guards/51-01-SUMMARY.md
</context>

<gates_shared>
Every task shares these GATES: prefix EVERY cargo command with `TMPDIR=/home/simonm/.cache/fdars-bench-tmp`. On LINK/disk failure ("linking with cc failed") run `rm -rf target/debug/{incremental,examples}` and retry. `cargo fmt` before each commit; `git commit --no-verify` per task. NO src/ edit; NO new dependency. Do NOT push a `v*` tag. Append [[bench]] entries adjacent to existing blocks; do NOT modify the PERMANENT perf_hotpaths/perf_parallelism blocks. Each bench copies its own deterministic non-RNG generator (separate compilation units): `generate_curves`/`grid_mesh` from perf_hotpaths.rs, `two_group_densities` from perf_parallelism.rs, or the `IrregFdata::from_lists` construction from perf_hotpaths.rs:52-65. Build data OUTSIDE `b.iter()`; `black_box` inputs and result. Primary gate after each task: `cargo clippy --all-targets --features linalg,parallel -- -D warnings`.
</gates_shared>

<tasks>

<task type="auto">
  <name>Task 1: benches/fem_smoothing_benchmarks.rs (fem_smooth_gcv) + Cargo entry</name>
  <files>fdars-core/benches/fem_smoothing_benchmarks.rs, fdars-core/Cargo.toml</files>
  <read_first>
    - fdars-core/src/fem_smoothing.rs:641-648 (VERIFIED: `pub fn fem_smooth_gcv(nodes: &[[f64;2]], triangles: &[[usize;3]], obs_xy: &[[f64;2]], y: &[f64], log_lambda_range: (f64,f64), n_grid: usize) -> Result<FemSmoothResult, FdarError>`; n_grid>=2 required)
    - fdars-core/benches/perf_hotpaths.rs:72-117 (grid_mesh(k) generator + how bench_fem_smooth builds obs_xy + y = sin(2πx)·cos(πy))
  </read_first>
  <action>Create benches/fem_smoothing_benchmarks.rs: import `fdars_core::fem_smoothing::fem_smooth_gcv`; copy `grid_mesh` verbatim + `use std::f64::consts::PI;`. Use a SMALLER mesh than the 576 already benched: `let (nodes, triangles) = grid_mesh(16);` (256 nodes — GCV multiplies the O(N³) solve by n_grid). `let obs_xy = nodes.clone();` `let y: Vec<f64> = nodes.iter().map(|p| (2.0*PI*p[0]).sin() * (PI*p[1]).cos()).collect();` `log_lambda_range=(-4.0, 0.0)`, `n_grid=5`. bench_group "fem_smooth_gcv", sample_size(10), measurement_time(60s), warm_up_time(3s) (slow — n_grid × O(N³)). Cell "nodes256_ngrid5": black_box the result. (fem_smooth single-λ at 576 nodes is already benched — fem_smooth_gcv λ-grid is the distinct entry.) Append `[[bench]] name="fem_smoothing_benchmarks" harness=false`. Follow gates_shared.</action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo bench -p fdars-core --features linalg,parallel --bench fem_smoothing_benchmarks -- --quick</automated>
  </verify>
  <done>fem_smoothing_benchmarks.rs compiles, lints clean, runs fem_smooth_gcv once (256 nodes, n_grid=5); Cargo entry present; committed --no-verify after fmt.</done>
</task>

<task type="auto">
  <name>Task 2: benches/density_fda_benchmarks.rs (lqd_fpca) + Cargo entry</name>
  <files>fdars-core/benches/density_fda_benchmarks.rs, fdars-core/Cargo.toml</files>
  <read_first>
    - fdars-core/src/density_fda.rs:563-568 (VERIFIED: `pub fn lqd_fpca(density_matrix: &FdMatrix, argvals: &[f64], ncomp: usize, n_quantile_pts: Option<usize>) -> Result<LqdFpcaResult, FdarError>`)
    - fdars-core/src/density_fda.rs:407-411 (VERIFIED optional cheap 2nd cell: `pub fn wasserstein_barycenter(...)`)
    - fdars-core/benches/perf_parallelism.rs:29-51 (two_group_densities — strictly-positive density rows, argvals in −3..3)
  </read_first>
  <action>Create benches/density_fda_benchmarks.rs: import `fdars_core::density_fda::lqd_fpca` (+ `wasserstein_barycenter` if adding the optional 2nd cell) + `fdars_core::matrix::FdMatrix`; copy `two_group_densities`. Inputs (n=100, m=81): `let (density_matrix, argvals, _) = two_group_densities(50, 81);` (100 strictly-positive density rows). Cell "n100_m81_ncomp3": `black_box(lqd_fpca(black_box(&density_matrix), black_box(&argvals), 3, None).unwrap())`. bench_group "density_lqd_fpca", sample_size(20), measurement_time(30s), warm_up_time(3s) (medium). OPTIONAL cheap 2nd cell "wasserstein_barycenter": `black_box(wasserstein_barycenter(black_box(&density_matrix), black_box(&argvals), None).unwrap())` (fast) — confirm its exact signature at src/density_fda.rs:407-411 before adding. Append `[[bench]] name="density_fda_benchmarks" harness=false`. Follow gates_shared.</action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo bench -p fdars-core --features linalg,parallel --bench density_fda_benchmarks -- --quick</automated>
  </verify>
  <done>density_fda_benchmarks.rs compiles, lints clean, runs lqd_fpca once; Cargo entry present; committed --no-verify after fmt.</done>
</task>

<task type="auto">
  <name>Task 3: benches/fpca_variants_benchmarks.rs (fpca_der) + Cargo entry</name>
  <files>fdars-core/benches/fpca_variants_benchmarks.rs, fdars-core/Cargo.toml</files>
  <read_first>
    - fdars-core/src/fpca_variants.rs:189-194 (VERIFIED: `pub fn fpca_der(data: &FdMatrix, ncomp: usize, argvals: &[f64], nderiv: usize) -> Result<FpcaResult, FdarError>`)
    - fdars-core/src/fpca_variants.rs:405-411 (VERIFIED optional 2nd cell: `pub fn fsvd(x, argvals_x, y, argvals_y, ncomp)` — cross-covariance SVD; confirm exact sig before adding)
    - fdars-core/benches/perf_hotpaths.rs:21-32 (generate_curves)
  </read_first>
  <action>Create benches/fpca_variants_benchmarks.rs: import `fdars_core::fpca_variants::fpca_der` (+ `fsvd` if adding the optional 2nd cell) + `fdars_core::matrix::FdMatrix`; copy `generate_curves`. Inputs (n=200, m=50): `let (data, argvals) = generate_curves(200, 50);`. Cell "n200_m50_ncomp5_nderiv1": `black_box(fpca_der(black_box(&data), 5, black_box(&argvals), 1).unwrap())`. bench_group "fpca_der", sample_size(20), measurement_time(30s), warm_up_time(3s) (medium — derivative + SVD). OPTIONAL 2nd cell fsvd: build two generate_curves sets, confirm the exact fsvd signature at src/fpca_variants.rs:405-411, black_box the result. (fsvd/ssvd were alloc-profiled in Phase 47 dhat but NOT wall-time benched; fpca_der has zero coverage.) Append `[[bench]] name="fpca_variants_benchmarks" harness=false`. Follow gates_shared.</action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo bench -p fdars-core --features linalg,parallel --bench fpca_variants_benchmarks -- --quick</automated>
  </verify>
  <done>fpca_variants_benchmarks.rs compiles, lints clean, runs fpca_der once; Cargo entry present; committed --no-verify after fmt.</done>
</task>

<task type="auto">
  <name>Task 4: benches/face_benchmarks.rs (mface_covariance) + Cargo entry</name>
  <files>fdars-core/benches/face_benchmarks.rs, fdars-core/Cargo.toml</files>
  <read_first>
    - fdars-core/src/irreg_fdata/face.rs:263-267 (VERIFIED: `pub fn mface_covariance(variables: &[IrregFdata], grids: &[Vec<f64>], bandwidth: f64) -> Result<MfaceCovResult, FdarError>`; requires variables.len()>=2 and grids.len()==variables.len())
    - fdars-core/src/irreg_fdata/mod.rs:61 (VERIFIED: `pub fn from_lists(argvals_list: &[Vec<f64>], values_list: &[Vec<f64>]) -> Self`)
    - fdars-core/benches/perf_hotpaths.rs:46-70 (bench_face_covariance — the IrregFdata::from_lists construction from a sinusoid grid, lines 52-65, to mirror)
  </read_first>
  <action>Create benches/face_benchmarks.rs: import `use fdars_core::irreg_fdata::{mface_covariance, IrregFdata};` (module path is irreg_fdata, filename is face_benchmarks). Copy the deterministic IrregFdata construction from perf_hotpaths.rs:52-65 (`use std::f64::consts::PI;`). Build 2 variables (n=100 curves, m=30 pts each): for each of the 2 variables construct an `IrregFdata::from_lists(&argvals_list, &values_list)` on a regular grid (vary the phase/amp per variable so they differ); collect into `let variables = vec![var0, var1];`. `let grids: Vec<Vec<f64>> = vec![grid0, grid1];` (per-variable regular grids matching each variable's m). `bandwidth=0.3`. bench_group "mface_covariance", sample_size(15), measurement_time(30s), warm_up_time(3s) (medium/slow — multivariate FACE covariance). Cell "vars2_n100_m30": `black_box(mface_covariance(black_box(&variables), black_box(&grids), 0.3).unwrap())`. (Univariate face_covariance is already benched in perf_hotpaths — mface_covariance multivariate is the distinct entry.) If mface_covariance proves too costly to construct/run, the RESEARCH-approved alternative is `face_trajectory(data: &IrregFdata, config: &PaceFpcaConfig)` at src/irreg_fdata/face.rs:407-410 with `PaceFpcaConfig::default()`. Append `[[bench]] name="face_benchmarks" harness=false`. Follow gates_shared.</action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --benches --features linalg,parallel && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo bench -p fdars-core --features linalg,parallel --bench face_benchmarks -- --quick</automated>
  </verify>
  <done>face_benchmarks.rs compiles, lints clean, runs mface_covariance (or face_trajectory fallback) once; Cargo entry present; all 9 module benches now build together; committed --no-verify after fmt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Deterministic hard-coded bench inputs; no external input, network, auth, or crypto. Attack surface nil. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-02 | Tampering | Phantom crates.io publish from a `v*` tag | medium | mitigate | Do NOT push a `v*` tag (MEMORY audit-milestone-no-git-tag) |
| T-51-SC | Tampering | npm/pip/cargo installs | high | accept | No package installed — criterion/dhat pre-existing dev-deps (locked "no new dependency") |
</threat_model>

<verification>
- `TMPDIR=… cargo build -p fdars-core --benches --features linalg,parallel` compiles all 9 module benches (5 prior + 4 here) plus the 2 PERMANENT perf benches.
- `TMPDIR=… cargo clippy --all-targets --features linalg,parallel -- -D warnings` clean.
- Each `--bench <name> -- --quick` runs its cell once without panic.
</verification>

<success_criteria>
BENCH-01 complete: all 9 module benches (fts, frechet, boosting_regression, coclustering, fem_smoothing, density_fda, inference, fpca_variants, face) compile, lint clean, and run — none duplicating an already-benched cell. No src/ change; no new dependency.
</success_criteria>

<output>
Create `.planning/phases/51-benchmark-coverage-regression-guards/51-03-SUMMARY.md` when done. Record: 4 bench files + Cargo entries landed; all 9 module benches confirmed building together; captured `--quick` baseline cells for BENCH-RESULTS.md; whether the face bench used mface_covariance or the face_trajectory fallback; any optional 2nd cells added (wasserstein_barycenter/fsvd) and any down-sizing (RESEARCH A1/A2).
</output>
