---
phase: 51-benchmark-coverage-regression-guards
plan: 04
type: execute
wave: 4
depends_on: [51-03]
files_modified:
  - .planning/phases/51-benchmark-coverage-regression-guards/BENCH-RESULTS.md
autonomous: true
requirements: [BENCH-02]
estimate:
  tokens: 8000
  raw_tokens: 5000
  tasks: 1
  confidence: high
must_haves:
  truths:
    - "BENCH-RESULTS.md exists and consolidates the Phase 47/48 before/after numbers as documented soft regression guards (BENCH-02)"
    - "the two PERMANENT perf benches (perf_hotpaths, perf_parallelism) are confirmed still [[bench]]-registered — no Cargo.toml change needed for them"
    - "the deterministic alloc_audit_dpca/alloc_audit_fpca hard-assert guards are confirmed intact and named in the ledger"
  artifacts:
    - .planning/phases/51-benchmark-coverage-regression-guards/BENCH-RESULTS.md
  key_links:
    - "BENCH-RESULTS.md ↔ PERF-RESULTS.md (Phase 47) + PERF-PARALLEL-RESULTS.md (Phase 48) — the source ledgers it references"
    - "documented soft guards ↔ criterion baseline compare; deterministic hard guards ↔ alloc_audit tests (dhat-heap)"
---

<objective>
BENCH-02: Land `.planning/phases/51-.../BENCH-RESULTS.md` — a consolidated ledger documenting the Phase 47/48 PERF wins as guarded baselines (soft guards: criterion measures, does NOT fail CI on regression — locked decision). Confirm the two PERMANENT perf benches stay registered (no Cargo.toml change needed) and the deterministic alloc_audit hard-assert guards stay intact. Fold in the new BENCH-01 module baseline cells captured in 51-01/02/03 SUMMARYs where available. Carry the governor/environment caveat verbatim.

Purpose: give future changes a documented before/after to detect regressions of the Phase 47/48 wins (via `cargo bench` + criterion baseline compare) plus a first-run baseline for the 9 newly-covered modules.
Output: BENCH-RESULTS.md.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-benchmark-coverage-regression-guards/51-RESEARCH.md

# Source ledgers to consolidate:
@.planning/phases/47-hot-path-allocation-performance/PERF-RESULTS.md
@.planning/phases/48-parallelism-gap-closure/PERF-PARALLEL-RESULTS.md
# New-module baseline cells captured this phase:
@.planning/phases/51-benchmark-coverage-regression-guards/51-01-SUMMARY.md
@.planning/phases/51-benchmark-coverage-regression-guards/51-02-SUMMARY.md
@.planning/phases/51-benchmark-coverage-regression-guards/51-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Land BENCH-RESULTS.md — consolidated regression-guard ledger (BENCH-02)</name>
  <files>.planning/phases/51-benchmark-coverage-regression-guards/BENCH-RESULTS.md</files>
  <read_first>
    - .planning/phases/47-hot-path-allocation-performance/PERF-RESULTS.md:30-78 (OPT-A dpca −54% blocks, OPT-E face_covariance −80.7%, OPT-B fsvd, OPT-C ssvd, OPT-D functional_acf, OPT-F fem_smooth) + :9-19 (governor caveat)
    - .planning/phases/48-parallelism-gap-closure/PERF-PARALLEL-RESULTS.md:25-48 (frechet_anova 9.9×, co_cluster 6.4×) + :8-23 (governor caveat)
    - fdars-core/Cargo.toml:98-104 (the two PERMANENT perf [[bench]] blocks — confirm still present)
    - fdars-core/tests/alloc_audit_dpca.rs + fdars-core/tests/alloc_audit_fpca.rs (confirm both still exist — deterministic hard-assert guards, feature dhat-heap)
    - the three 51-0x-SUMMARY.md files (new-module `--quick` baseline cells captured this phase)
  </read_first>
  <action>
    Create .planning/phases/51-benchmark-coverage-regression-guards/BENCH-RESULTS.md as a consolidated ledger with these sections:
    (1) HEADER: state the guard model — DOCUMENTED SOFT GUARDS (criterion measures; regressions caught via `cargo bench` + criterion baseline compare against the numbers below; they do NOT fail CI — locked decision) vs DETERMINISTIC HARD GUARDS (tests/alloc_audit_dpca.rs + tests/alloc_audit_fpca.rs, feature dhat-heap, hard-assert allocation-block counts — real CI failure surface, unchanged this phase). Explicitly note NO wall-time assert! tests were added (governor-sensitive — deferred/out of scope).
    (2) PHASE 47 WINS TABLE (from PERF-RESULTS.md): OPT-A dpca fts::dpca n200_m50 17,739→8,139 blocks (−54%, −20% bytes); OPT-E face_covariance perf_face_covariance/n200_m30 983.8ms→189.8ms (−80.7%); OPT-B fsvd 275→274 blocks; OPT-C ssvd 22→21 blocks; OPT-D functional_acf (−1 block + ~(m²−m) sqrt dropped); OPT-F fem_smooth perf_fem_smooth/nodes576 (alloc win; O(N³) solve DEFERRED). Include the "Guard bench cell" column (alloc_audit_dpca / perf_hotpaths::perf_dpca; perf_hotpaths::perf_face_covariance; alloc_audit_fpca; etc.).
    (3) PHASE 48 WINS TABLE (from PERF-PARALLEL-RESULTS.md): frechet_anova perf_parallelism_frechet_anova/n24_m81_nperm999 322.73ms(1-thread)→32.57ms(20-thread) = 9.9× (p<0.05); co_cluster perf_parallelism_co_cluster/n200_m50_ninit8 337.34ms→52.91ms = 6.4× (p<0.05). Include the 1-thread/20-thread env-var sweep command.
    (4) NEW-MODULE BASELINES (BENCH-01): a table of the 9 new module cells (fts::ftsm, frechet::frechet_global_reg, boosting_regression::boost_fosr, coclustering::co_cluster_select, fem_smoothing::fem_smooth_gcv, density_fda::lqd_fpca, inference::t_perm_test, fpca_variants::fpca_der, irreg_fdata::mface_covariance) with the `--quick` first-run median captured in the 51-0x SUMMARYs where available (mark "not captured / run `cargo bench --bench <name>` to populate" where a SUMMARY lacked a number). These are FIRST baselines, not before/after.
    (5) ENVIRONMENT / GOVERNOR CAVEAT (carry verbatim from RESEARCH §BENCH-02 / PERF-RESULTS.md:9-19): CPU governor `powersave` unpinned → all WALL-TIME cells LOW-CONFIDENCE (treat medians/ratios as indicative; thread-scaling DIRECTION is a valid signal); allocation wins (OPT-A..D) are governor-independent HIGH-confidence dhat counts; 20 logical cores (RAYON_NUM_THREADS default 20); features linalg,parallel (criterion) / dhat-heap,linalg (alloc); criterion 0.5, dhat 0.3; TMPDIR=/home/simonm/.cache/fdars-bench-tmp; re-run under a `performance` governor to supersede LOW-CONFIDENCE absolute medians.
    (6) GUARD INVENTORY: confirm perf_hotpaths + perf_parallelism remain [[bench]]-registered PERMANENT (Cargo.toml:98-104 — no Cargo change needed for them this phase; the 9 BENCH-01 entries are the only additions); confirm alloc_audit_dpca.rs + alloc_audit_fpca.rs remain as deterministic hard guards. Make NO Cargo.toml or src/ edit in this task (doc-only). Run `cargo fmt` (no-op for markdown but keeps the workflow uniform) and `git commit --no-verify`. Do NOT push a `v*` tag.
  </action>
  <verify>
    <automated>test -f .planning/phases/51-benchmark-coverage-regression-guards/BENCH-RESULTS.md && grep -q "powersave" .planning/phases/51-benchmark-coverage-regression-guards/BENCH-RESULTS.md && grep -q "alloc_audit" .planning/phases/51-benchmark-coverage-regression-guards/BENCH-RESULTS.md && grep -q "perf_hotpaths" .planning/phases/51-benchmark-coverage-regression-guards/BENCH-RESULTS.md && ls fdars-core/tests/alloc_audit_dpca.rs fdars-core/tests/alloc_audit_fpca.rs && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --benches --features linalg,parallel</automated>
  </verify>
  <done>BENCH-RESULTS.md exists with all 6 sections (soft/hard guard model, Phase 47 table, Phase 48 table, 9 new-module baselines, governor caveat, guard inventory); both alloc_audit tests confirmed present; both PERMANENT perf benches confirmed registered; all benches still build; committed --no-verify. No Cargo.toml or src/ edit in this task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Markdown ledger only — no code, no external input, no network. Attack surface nil. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-02 | Tampering | Phantom crates.io publish from a `v*` tag | medium | mitigate | Do NOT push a `v*` tag (MEMORY audit-milestone-no-git-tag); crate version unchanged (0.29.0) |
| T-51-01 | Tampering | dhat-heap allocator accidentally enabled in release | low | accept | Feature stays dev/test-only (existing Cargo comment); this doc-only task does not touch it |
</threat_model>

<verification>
- BENCH-RESULTS.md present with governor caveat, alloc_audit + perf_hotpaths references (grep gates above).
- Both alloc_audit tests still on disk; both PERMANENT perf benches still registered.
- `cargo build --benches` still green (whole bench suite intact).
</verification>

<success_criteria>
BENCH-02 complete: BENCH-RESULTS.md consolidates the Phase 47/48 before/after as documented soft guards + the 9 new-module first baselines, with the governor caveat; the two PERMANENT perf benches and the two deterministic alloc guards are confirmed intact. No src/ or Cargo change in this plan.
</success_criteria>

<output>
Create `.planning/phases/51-benchmark-coverage-regression-guards/51-04-SUMMARY.md` when done. Record: BENCH-RESULTS.md landed with all 6 sections; which new-module baseline cells were populated vs left "run to populate"; confirmation both PERMANENT perf benches + both alloc_audit guards are intact; no Cargo/src change.
</output>
