---
phase: 47-hot-path-allocation-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/Cargo.toml
  - fdars-core/benches/perf_hotpaths.rs
  - fdars-core/tests/equivalence_phase47.rs
  - fdars-core/tests/alloc_audit_dpca.rs
  - fdars-core/src/fts/spectral.rs
  - .planning/phases/47-hot-path-allocation-performance/PERF-RESULTS.md
autonomous: true
requirements: [PERF-01, PERF-02]
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "OPT-A: dpca output is provably-equivalent (golden test, rel <=1e-10) after the allocation refactor (PERF-01)"
    - "OPT-A: dpca heap block count drops from 17,739 to <1000 (dhat alloc-audit) (PERF-02)"
    - "A permanent perf_hotpaths criterion bench + a permanent equivalence golden test + a permanent dhat alloc-audit test all exist and are wired end-to-end (tracer slice through every layer)"
    - "The existing full suite stays green and clippy --all-targets is clean"
    - "No public signature changes; dpca signature is byte-identical"
  artifacts:
    - fdars-core/benches/perf_hotpaths.rs
    - fdars-core/tests/equivalence_phase47.rs
    - fdars-core/tests/alloc_audit_dpca.rs
    - .planning/phases/47-hot-path-allocation-performance/PERF-RESULTS.md
    - "fdars-core/Cargo.toml [[bench]] name = \"perf_hotpaths\""
  key_links:
    - "eigen_at_frequency (spectral.rs) index-sort refactor -> dpca output ordering preserved -> golden test passes"
    - "dpca alloc reduction -> dhat block count <1000 -> PERF-02 proof"
    - "perf_hotpaths bench registered now -> becomes Phase 51 BENCH-02 regression guard"
---

<objective>
Wave 0 setup + the OPT-A tracer: build the phase's proof pipeline (permanent criterion bench, permanent golden-equivalence test file, permanent dhat alloc-audit test) AND drive it end-to-end through the single highest-leverage optimization (OPT-A: `fts::dpca` allocation reduction). This is the thin vertical slice that touches every layer this phase will use — a src edit, a golden equivalence test, a dhat alloc-audit, a criterion bench, and the PERF-RESULTS.md ledger — proving the whole architecture on one path before Waves 2-4 expand horizontally.

Purpose: OPT-A is the clearest win in the phase (allocation #1 — 42 MB / 17,739 blocks, ~70x the next target). Proving the full test/bench/audit harness on it de-risks every later optimization.
Output: `benches/perf_hotpaths.rs` (+ Cargo.toml `[[bench]]`), `tests/equivalence_phase47.rs`, `tests/alloc_audit_dpca.rs`, the OPT-A src edit in `spectral.rs`, and `PERF-RESULTS.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/47-hot-path-allocation-performance/47-CONTEXT.md
@.planning/phases/47-hot-path-allocation-performance/47-RESEARCH.md
@.planning/phases/47-hot-path-allocation-performance/47-VALIDATION.md
@.planning/phases/46-whole-crate-profiling-measurement/PROF-01-hotpath-targets.md

# Patterns to copy verbatim:
@fdars-core/tests/alloc_audit_fpca.rs
@fdars-core/benches/audit_hotpaths.rs
</context>

<operational_notes>
Honor MEMORY.md operational pointers on EVERY task (bake into commands):
- Prefix all build/test/bench with `TMPDIR=/home/simonm/.cache/fdars-bench-tmp` (create dir if absent; /tmp tmpfs exhaustion causes bogus "No space left").
- Wave-0 disk step (run once, first thing): `rm -rf target/debug/{incremental,examples}` to free ~108G before bench builds.
- Full lint gate is `cargo clippy --all-targets --features linalg,parallel -- -D warnings` (a plain `-p ... -D warnings` false-greens on test/bench code).
- Commit with `git commit --no-verify` (long-cargo pre-commit hook trips the watchdog) AND run `cargo fmt -p fdars-core` immediately before each commit (the --no-verify dodge otherwise leaves CI fmt drift).
- dhat tests MUST run single-threaded in their own binary: `-- <test_name> --nocapture --test-threads=1`.
</operational_notes>

<tasks>

<task type="tracer">
  <name>Task 1: Scaffold the permanent proof pipeline (bench + golden test file + dhat test file + PERF-RESULTS.md) and baseline-green</name>
  <files>fdars-core/Cargo.toml, fdars-core/benches/perf_hotpaths.rs, fdars-core/tests/equivalence_phase47.rs, fdars-core/tests/alloc_audit_dpca.rs, .planning/phases/47-hot-path-allocation-performance/PERF-RESULTS.md</files>
  <read_first>
    - fdars-core/benches/audit_hotpaths.rs (criterion pattern: generate data OUTSIDE b.iter(), black_box inputs+outputs, sample_size(20), measurement_time(30s), criterion_group!/criterion_main!)
    - fdars-core/tests/alloc_audit_fpca.rs (dhat pattern: #[global_allocator] static ALLOC under #[cfg(feature="dhat-heap")], Profiler::builder().testing().build(), build data OUTSIDE profiler, print total_blocks/total_bytes/max_bytes)
    - fdars-core/Cargo.toml lines 58-96 (existing [[bench]] harness=false entries; dhat-heap feature at line 32; dhat dev-dep at line 54)
    - .planning/phases/46-whole-crate-profiling-measurement/PROF-01-hotpath-targets.md lines 44,51-58 (before-numbers: dpca 17,739 blocks/42MB @ n200_m50; face_covariance 984ms @ n200_m30; fem_smooth 452ms @ 576 nodes)
  </read_first>
  <action>
    First run `mkdir -p /home/simonm/.cache/fdars-bench-tmp` and `rm -rf target/debug/{incremental,examples}` (disk step).

    Create `fdars-core/benches/perf_hotpaths.rs` mirroring `audit_hotpaths.rs` structure. Add three benched cells at the PROF-01 measurement cells: `bench_dpca` (n200_m50, ncomp=3 — `fdars_core::fts::dpca`), `bench_face_covariance` (n200_m30 — `fdars_core::irreg_fdata::face_covariance`), `bench_fem_smooth` (576 nodes / 1058 triangles — `fdars_core::fem_smoothing::fem_smooth`). Build all inputs OUTSIDE `b.iter()`; wrap inputs and outputs in `black_box`. Use `group.sample_size(20)` and `group.measurement_time(Duration::from_secs(30))` per cell (these are expensive). Provide deterministic data generators (no RNG) — for the fts cell reuse the `generate_curves` phase/amplitude formula from `audit_hotpaths.rs`; for the face cell build an `IrregFdata` via `IrregFdata::from_lists(&argvals_list, &values_list)` with n=200 curves each observed at ~3 deterministic points; for the fem cell build a deterministic square-mesh grid of 576 nodes (24x24) with a Delaunay-style triangle list and deterministic `y`. Register `criterion_group!(benches, bench_dpca, bench_face_covariance, bench_fem_smooth); criterion_main!(benches);`.

    Add to `fdars-core/Cargo.toml` (after the existing audit_hotpaths entry): a `[[bench]]` with name = "perf_hotpaths" and harness = false. This is PERMANENT (becomes Phase 51 BENCH-02 guard) — do NOT mark it throwaway.

    Create `fdars-core/tests/equivalence_phase47.rs` as the permanent golden-equivalence test module. Add a shared `assert_rel_close(a: f64, b: f64, tol: f64)` helper asserting `(a-b).abs() <= tol * a.abs().max(1e-12)` and a deterministic `generate_curves(n, m)` helper (copy the phase/amplitude formula). Leave a documented module header explaining: golden reference values are captured from the CURRENT (pre-optimization) code and hardcoded as constants; each OPT's refactor must reproduce them within tolerance (exact for counting paths, rel <=1e-10 for float/SVD/eigen paths per 47-CONTEXT.md). The per-OPT golden tests are added by their owning tasks/plans — this task just establishes the file + helpers + one placeholder-free compile-clean smoke assertion (e.g. `assert_rel_close(1.0, 1.0 + 1e-13, 1e-10)`).

    Create `fdars-core/tests/alloc_audit_dpca.rs` mirroring `alloc_audit_fpca.rs` verbatim structure: `#[cfg(feature="dhat-heap")] #[global_allocator] static ALLOC: dhat::Alloc = dhat::Alloc;`, a `generate_test_curves(n, m)` helper (same formula), and a `#[test] #[cfg(feature="dhat-heap")] fn count_dpca_allocations_n200_m50()` that builds `(data, argvals)` OUTSIDE the profiler, then `let _profiler = dhat::Profiler::builder().testing().build();`, calls `fdars_core::fts::dpca(&data, &argvals, 3, None, None)`, reads `dhat::HeapStats::get()`, and prints total_blocks/total_bytes/max_bytes. Add a `// TODO(OPT-A): tighten to assert total_blocks < 1000 in Task 3` comment on the assertion line (Task 3 flips it to a hard assert once the refactor lands) — for THIS task leave it as a print-only baseline (no hard assert yet), matching the alloc_audit_fpca.rs baseline convention.

    Create `.planning/phases/47-hot-path-allocation-performance/PERF-RESULTS.md` with: an Environment block (copy the PROF-01 Environment table: governor, logical cores, RAYON_NUM_THREADS, feature flags, host tmp, date) — attempt `cpupower frequency-set -g performance` via sudo; if sudo unavailable, record governor=`powersave` with the LOW-CONFIDENCE caveat verbatim. Add empty before/after result tables for each OPT (A-F) to be filled by later tasks/plans.

    Confirm baseline-green before any src edit: run the full suite.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel 2>&1 | tail -5</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --benches --features linalg,parallel 2>&1 | tail -5</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features dhat-heap,linalg -- count_dpca_allocations_n200_m50 --nocapture --test-threads=1 2>&1 | tail -8</automated>
  </verify>
  <done>
    perf_hotpaths bench compiles and is registered in Cargo.toml; equivalence_phase47.rs and alloc_audit_dpca.rs compile; the dhat baseline prints a dpca block count near 17,739 (the pre-optimization number); full existing suite green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the OPT-A golden equivalence test capturing current dpca output</name>
  <files>fdars-core/tests/equivalence_phase47.rs</files>
  <read_first>
    - fdars-core/src/fts/spectral.rs lines 190-239 (eigen_at_frequency) and 265-309 (dpca signature + per-frequency loop)
    - fdars-core/src/fts/mod.rs lines 79-97 (DpcaResult fields: filters: Vec<FdMatrix>, scores: FdMatrix, eigenvalues: Vec<Vec<f64>>, n_freqs, filter_lag, ncomp, valid_range)
    - 47-RESEARCH.md "Equivalence / Golden Test Pattern" (relative <=1e-10; OPT-A should be bitwise-identical since eigendecomposition inputs/computation are unchanged — assert the stricter bound)
  </read_first>
  <behavior>
    - golden_dpca_n50_m10: dpca on deterministic n=50, m=10, ncomp=2 data produces the SAME filters (each FdMatrix), scores, and eigenvalue trajectories after OPT-A as the current code.
    - Reference values are captured NOW (pre-OPT-A) by running the test once, printing the outputs, and hardcoding a compact fingerprint of them.
  </behavior>
  <action>
    Add `#[test] fn golden_dpca_n50_m10()` to `equivalence_phase47.rs`. Build deterministic data via `generate_curves(50, 10)` and `argvals = (0..10).map(|j| j as f64/9.0).collect()`. Call `fdars_core::fts::dpca(&data, &argvals, 2, None, None).unwrap()`.

    Capture a stable fingerprint of the output that is cheap to hardcode yet catches ordering/sign/value regressions: the full `eigenvalues[c]` trajectories (both components), the first filter's `as_slice()` (filters[0]), and the scores' `as_slice()` — hash-compare is fragile, so instead hardcode a handful of representative scalar checkpoints: `result.eigenvalues[0][0]`, `result.eigenvalues[1][0]`, `result.eigenvalues[0][n_freqs/2]`, `result.filters[0].as_slice()[0]`, `result.filters[1].as_slice()[0]`, `result.scores.as_slice()[0]`, and `result.scores.as_slice().last()`. Also assert structural invariants: `result.ncomp == 2`, `result.filters.len() == 2`, `result.n_freqs == 50`.

    To capture the reference constants: run this test once (it will fail on the placeholder constants), read the printed actual values from a `println!("{:.17e}", ...)` for each checkpoint, then paste them in as `const` values and assert with `assert_rel_close(actual, EXPECTED, 1e-10)`. Because OPT-A is a pure copy-elimination refactor, prefer the tighter `1e-12` bound; fall back to `1e-10` only if FP reordering forces it (document which bound you used in a comment).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel -- golden_dpca_n50_m10 --nocapture 2>&1 | tail -8</automated>
  </verify>
  <done>golden_dpca_n50_m10 passes against reference constants captured from the current (pre-OPT-A) code; the test is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OPT-A — eliminate per-eigenvector Vec allocations in eigen_at_frequency; flip dhat gate to hard assert</name>
  <files>fdars-core/src/fts/spectral.rs, fdars-core/tests/alloc_audit_dpca.rs, .planning/phases/47-hot-path-allocation-performance/PERF-RESULTS.md</files>
  <read_first>
    - fdars-core/src/fts/spectral.rs lines 190-239 (eigen_at_frequency: `scaled` Vec at :197, `DMatrix::from_column_slice` at :203, the `Vec<(f64, Vec<f64>)>` pairs collect at :212-217 with `col.iter().copied().collect()` at :216 — the dominant alloc site; sort at :219; sign-align at :223-235)
    - fdars-core/src/fts/spectral.rs lines 291-304 (dpca: `sqrt_w` already precomputed at :291; the `for k in 0..n_freq` loop calling eigen_at_frequency at :299)
    - 47-RESEARCH.md OPT-A "Full fix" + "Code Examples/Example 1" (index-sort approach: pre-allocate idx buffer, sort idx by descending eigenvalue, read eigenvectors by idx WITHOUT collecting to Vec<Vec<f64>>) and "Common Pitfalls" 2 & 3 (idx[c] alignment; from_fn column-major convention)
    - 47-CONTEXT.md line 40 (tolerance: rel <=1e-10 float/eigen)
  </read_first>
  <behavior>
    - After the refactor, golden_dpca_n50_m10 (Task 2) still passes within tolerance — eigenvalues read in the same descending order, eigenvectors sign-aligned identically.
    - dpca @ n200_m50 heap blocks drop from 17,739 to < 1000 (dhat).
  </behavior>
  <action>
    Refactor `eigen_at_frequency` to remove the `Vec<(f64, Vec<f64>)>` pairs allocation, which is the dominant contributor to the 17,739 blocks (m x n_freq = 50 x 200 = ~10,000 `col.iter().copied().collect()` allocations). Apply the research's index-sort approach:

    1. Build the scaled matrix directly via `DMatrix::from_fn(m, m, |j1, j2| spec_real[j1 + j2*m] * sqrt_w[j1] * sqrt_w[j2])` (removes the intermediate `scaled: Vec<f64>` — verify column-major index convention `[j1 + j2*m]` matches, Pitfall 3). Keep the defensive symmetrization loop.
    2. `let eig = nalgebra::SymmetricEigen::new(mat);`
    3. Sort a pre-allocated `idx: Vec<usize>` (extend 0..m, then `idx.sort_by(|&a,&b| eig.eigenvalues[b].partial_cmp(&eig.eigenvalues[a]).unwrap_or(Equal))`) — descending, matching the current sort semantics.
    4. For only the top `ncomp` indices, push `eig.eigenvalues[idx[c]]` and read the eigenvector as ONE `Vec<f64>` per retained component (`eig.eigenvectors.column(idx[c]).iter().copied().collect()`) — this is `ncomp` Vecs per call instead of `m` Vecs per call. Apply the SAME sign-align (argmax-magnitude-positive) logic per retained eigenvector, using `idx[c]` consistently (Pitfall 2).
    5. Return `(eigenvalues, eigenvectors)` in the same shape/order as before.

    Optionally hoist the `idx` buffer into `dpca()` and pass `&mut Vec<usize>` if it cleanly reduces per-call allocation further — but do NOT change `eigen_at_frequency`'s external behavior or `dpca`'s public signature. Keep everything `#[cfg(feature="linalg")]`-agnostic (this path does not depend on linalg; confirm both feature configurations still compile equivalently).

    Then flip `tests/alloc_audit_dpca.rs` `count_dpca_allocations_n200_m50` to a HARD assertion: `assert!(stats.total_blocks < 1000, "dpca alloc regression: {} blocks (expected <1000 after OPT-A, was 17,739)", stats.total_blocks);` (remove the Task-1 TODO comment).

    Record OPT-A before/after in `PERF-RESULTS.md`: before = 17,739 blocks / 42,084,568 B / 8,637,712 peak; after = the measured post-refactor numbers; note the governor caveat.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel -- golden_dpca_n50_m10 2>&1 | tail -4</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features dhat-heap,linalg -- count_dpca_allocations_n200_m50 --nocapture --test-threads=1 2>&1 | tail -8</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel 2>&1 | tail -5</automated>
    <acceptance_criteria>
      - golden_dpca_n50_m10 passes (dpca output equivalent within documented tolerance)
      - count_dpca_allocations_n200_m50 passes with total_blocks < 1000 (was 17,739) — PERF-02 proof
      - full existing suite green
      - dpca public signature unchanged (grep the fn signature — byte-identical)
    </acceptance_criteria>
  </verify>
  <done>eigen_at_frequency no longer allocates one Vec per eigenvalue; dpca blocks <1000; golden test green; suite green; PERF-RESULTS.md OPT-A row filled.</done>
</task>

<task type="auto">
  <name>Task 4: Run the OPT-A criterion cell + wave-merge gate (clippy --all-targets + full suite)</name>
  <files>.planning/phases/47-hot-path-allocation-performance/PERF-RESULTS.md</files>
  <read_first>
    - fdars-core/benches/perf_hotpaths.rs (the bench_dpca cell from Task 1)
    - 47-VALIDATION.md "Sampling Rate" (per-wave-merge: full suite + clippy --all-targets; phase gate: >=1 before/after cell >=15% or >=25% alloc)
    - 47-RESEARCH.md "Governor Pinning" (sudo cpupower; else powersave LOW-CONFIDENCE caveat)
  </read_first>
  <action>
    Run the dpca criterion cell to capture a wall-time number for the ledger (the primary OPT-A proof is the >=25% allocation reduction from Task 3; the bench cell is the permanent guard + a secondary wall-time signal). Attempt governor pinning: `sudo cpupower frequency-set -g performance` before and `-g powersave` after; if sudo is unavailable, record the powersave LOW-CONFIDENCE caveat.

    Append the dpca criterion median to PERF-RESULTS.md (OPT-A row: allocation blocks 17,739 -> <1000 is the headline PERF-02 result; wall-time is secondary/informational for an allocation-bound path).

    Run the wave-merge gate: full suite + `cargo clippy --all-targets --features linalg,parallel -- -D warnings` (must be clean — lints the new bench + test code).

    Commit sequence for this plan (each with `cargo fmt -p fdars-core` first, then `git commit --no-verify`): the scaffold (Task 1), the golden test (Task 2), the OPT-A src edit + dhat hard-assert (Task 3), and the ledger update (Task 4).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy -p fdars-core --all-targets --features linalg,parallel -- -D warnings 2>&1 | tail -5</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo bench -p fdars-core --features linalg,parallel --bench perf_hotpaths -- perf_dpca 2>&1 | tail -12</automated>
    <acceptance_criteria>
      - clippy --all-targets clean (zero warnings)
      - perf_hotpaths/perf_dpca cell runs and reports a median
      - PERF-RESULTS.md OPT-A row records before/after alloc (17,739 -> <1000) + governor note
    </acceptance_criteria>
  </verify>
  <done>OPT-A proven end-to-end (golden + dhat + bench + ledger); clippy --all-targets clean; the full proof pipeline is established for Waves 2-4 to reuse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure in-process numerical optimization — no external input, I/O, auth, or network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-01 | Tampering | numeric output of dpca after refactor | high | mitigate | golden equivalence test (rel <=1e-10) captured pre-change; full suite green at every commit |
| T-47-02 | Denial-of-Service | new bench/test code fails CI clippy gate | low | mitigate | clippy --all-targets gate in Task 4 |

No package installs in this plan (criterion + dhat already dev-deps). No T-47-SC.
</threat_model>

<verification>
- OPT-A golden equivalence test passes (dpca output preserved within tolerance).
- dpca dhat block count < 1000 (was 17,739) — PERF-02 proof.
- Full existing suite green + clippy --all-targets clean.
- perf_hotpaths bench registered and runnable (permanent — Phase 51 BENCH-02 guard).
- dpca public signature unchanged.
</verification>

<success_criteria>
- The permanent proof pipeline (perf_hotpaths bench, equivalence_phase47.rs, alloc_audit_dpca.rs, PERF-RESULTS.md) exists and is wired end-to-end.
- OPT-A landed behavior-preservingly with a >=25% (in practice >90%) allocation-block reduction proven by dhat.
- No public signature changed; suite + clippy green.
</success_criteria>

<output>
Create `.planning/phases/47-hot-path-allocation-performance/47-01-SUMMARY.md` when done. Record the dpca before/after alloc numbers, the golden tolerance actually used, and the governor state.
</output>
