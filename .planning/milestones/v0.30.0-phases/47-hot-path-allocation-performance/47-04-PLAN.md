---
phase: 47-hot-path-allocation-performance
plan: 04
type: execute
wave: 4
depends_on: [47-01, 47-02, 47-03]
files_modified:
  - fdars-core/src/fem_smoothing.rs
  - fdars-core/tests/equivalence_phase47.rs
  - .planning/phases/47-hot-path-allocation-performance/PERF-RESULTS.md
  - .planning/phases/47-hot-path-allocation-performance/47-VALIDATION.md
autonomous: true
requirements: [PERF-01]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "OPT-F: fem_smooth builds phi_t_phi and a_mat in a single assembly pass, removing the load-bearing phi_t_phi.clone() N*N copy (PERF-01)"
    - "OPT-F: fem_smooth output (node_values, fitted_obs, edf, gcv, rss) is provably-equivalent (golden, rel <=1e-10) after the change"
    - "The fem_smooth O(N^3) Cholesky+GCV bottleneck is explicitly DOCUMENTED and DEFERRED with a written rationale (no safe behavior-preserving constant-factor win without sparse solvers = out of scope)"
    - "PERF-RESULTS.md consolidates before/after for all landed OPTs (A-F) with governor caveat; deferred items documented"
    - "47-VALIDATION.md sign-off checklist satisfied; nyquist_compliant set true"
    - "The existing full suite stays green and clippy --all-targets is clean; no public signature changes"
  artifacts:
    - "fdars-core/src/fem_smoothing.rs (single-pass phi_t_phi/a_mat build)"
    - "fdars-core/tests/equivalence_phase47.rs (golden_fem_smooth)"
    - ".planning/phases/47-hot-path-allocation-performance/PERF-RESULTS.md (finalized)"
  key_links:
    - "single-pass build produces identical phi_t_phi (for GCV trace) and a_mat (for solve) -> golden fem_smooth passes"
    - "O(N^3) defer rationale -> satisfies CONTEXT.md 'no-safe-win = documented+deferred' policy"
---

<objective>
Wave 4: land the final optimization (OPT-F: `fem_smoothing::fem_smooth` clone removal), explicitly document+defer the fem_smooth O(N^3) Cholesky/GCV bottleneck (no safe behavior-preserving constant-factor win without sparse solvers — out of scope per CONTEXT.md), finalize `PERF-RESULTS.md` (consolidated before/after for OPT-A..F + governor caveat + deferred items), and complete the 47-VALIDATION.md sign-off.

Purpose: OPT-F removes a load-bearing `phi_t_phi.clone()` (one N*N copy, ~2.6MB at N=576) by building both `phi_t_phi` and `a_mat` in a single assembly pass. The compute bottleneck is the O(N^3) dense Cholesky + column-by-column A^-1 for GCV — inherently superlinear, correctly out of scope for this behavior-preserving milestone; this plan records that decision so verification does not flag a "missing" fem_smooth compute win.
Output: fem_smooth single-pass edit; golden_fem_smooth test; finalized PERF-RESULTS.md; VALIDATION.md sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/47-hot-path-allocation-performance/47-CONTEXT.md
@.planning/phases/47-hot-path-allocation-performance/47-RESEARCH.md
@.planning/phases/47-hot-path-allocation-performance/47-VALIDATION.md
@.planning/phases/46-whole-crate-profiling-measurement/PROF-01-hotpath-targets.md
</context>

<operational_notes>
Same as Plan 01 (bake into every command): `TMPDIR=/home/simonm/.cache/fdars-bench-tmp`; full lint gate `cargo clippy --all-targets --features linalg,parallel -- -D warnings`; `cargo fmt -p fdars-core` then `git commit --no-verify` per commit.
Depends on Plans 01-03: reuse `assert_rel_close` in `equivalence_phase47.rs`; the `bench_fem_smooth` cell is already in `benches/perf_hotpaths.rs` (Plan 01 Task 1). This plan is the final wave — it also consolidates the ledger and signs off validation.
</operational_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: golden_fem_smooth + OPT-F single-pass phi_t_phi/a_mat build (remove clone)</name>
  <files>fdars-core/src/fem_smoothing.rs, fdars-core/tests/equivalence_phase47.rs</files>
  <read_first>
    - fdars-core/src/fem_smoothing.rs lines 475-606 (fem_smooth: signature at :475-481; phi_t_phi build at :523-538 (triangular, symmetric); `let mut a_mat = phi_t_phi.clone();` at :541 then `a_mat[ab] += lambda*k_global[ab]` at :542-544 and ridge `a_mat[a*big_n+a] += 1e-10` at :545-547; cholesky_solve at :558; A^-1 GCV column loop at :567-578; edf trace using phi_t_phi at :581-586; FemSmoothResult fields node_values/fitted_obs/edf/gcv/rss/lambda/n_nodes/n_triangles at :596-605)
    - 47-RESEARCH.md OPT-F "Proposed change" (build phi_t_phi AND a_mat in the SAME triangular pass: `phi_t_phi[a*big_n+b] += val; a_mat[a*big_n+b] += val;` and the symmetric mirror when a!=b; THEN add lambda*K + eps*I to a_mat ONLY, not phi_t_phi) and Pitfall 1 (do NOT mutate phi_t_phi into a_mat in place — the GCV trace at :581-586 needs pure phi_t_phi)
    - 47-CONTEXT.md line 40 (tolerance)
  </read_first>
  <behavior>
    - golden_fem_smooth: fem_smooth on a fixed deterministic small mesh (e.g. 64 nodes / ~98 triangles) with fixed lambda produces identical node_values, fitted_obs, edf, gcv, rss after OPT-F.
    - phi_t_phi still holds pure Phi'Phi at the GCV trace (Pitfall 1 avoided).
  </behavior>
  <action>
    Author `golden_fem_smooth_64nodes` first (capture reference constants from CURRENT code): build a deterministic square-grid mesh — nodes on a small NxN lattice (e.g. 8x8 = 64 nodes) in [0,1]^2, triangles via the standard two-per-cell split, obs_xy = the node coordinates (or a deterministic subset), y from a deterministic smooth surface formula (e.g. sin(2*pi*x)*cos(2*pi*y)), fixed `lambda` (e.g. 1e-2). Call `fdars_core::fem_smoothing::fem_smooth(&nodes, &triangles, &obs_xy, &y, 1e-2).unwrap()`. Assert checkpoints: `node_values` first/mid/last elements, `fitted_obs[0]`, `edf`, `gcv`, `rss` via `assert_rel_close(_, EXPECTED, 1e-10)` (fall back only if reordering forces it; document). Reuse this deterministic mesh builder from (or share it with) the bench_fem_smooth generator if convenient.

    Apply OPT-F (fem_smoothing.rs ~:523-547): build `phi_t_phi` and `a_mat` in ONE triangular pass. Allocate both `vec![0.0; big_n*big_n]` up front. In the existing `for i { for a { let phi_ia=...; if phi_ia==0.0 {continue;} for b in a..big_n { let val = phi_ia*phi[i*big_n+b]; phi_t_phi[a*big_n+b]+=val; a_mat[a*big_n+b]+=val; if a!=b { phi_t_phi[b*big_n+a]+=val; a_mat[b*big_n+a]+=val; } }}}`. Then add regularization to a_mat ONLY: `for ab in 0..(big_n*big_n) { a_mat[ab] += lambda*k_global[ab]; }` and `for a in 0..big_n { a_mat[a*big_n+a] += 1e-10; }`. Remove the `let mut a_mat = phi_t_phi.clone();` line. The downstream cholesky_solve, A^-1 GCV column loop, and the edf trace (which reads pure phi_t_phi) are unchanged — phi_t_phi still holds Phi'Phi exactly (Pitfall 1). FLOP count is identical; the win is eliminating one N*N allocation + memcpy.

    No public signature change (fem_smooth and FemSmoothResult untouched).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel -- golden_fem_smooth_64nodes --nocapture 2>&1 | tail -8</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel -- fem_smooth 2>&1 | tail -6</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel 2>&1 | tail -5</automated>
    <acceptance_criteria>
      - golden_fem_smooth_64nodes passes (all outputs equivalent within documented tolerance)
      - existing fem_smooth tests pass; full existing suite green
      - fem_smooth public signature unchanged
      - no `phi_t_phi.clone()` remains in fem_smoothing.rs
    </acceptance_criteria>
  </verify>
  <done>fem_smooth builds phi_t_phi + a_mat in one pass with no clone; golden + fem tests + full suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Document + defer the fem_smooth O(N^3) bottleneck; finalize PERF-RESULTS.md</name>
  <files>fdars-core/src/fem_smoothing.rs, .planning/phases/47-hot-path-allocation-performance/PERF-RESULTS.md</files>
  <read_first>
    - fdars-core/src/fem_smoothing.rs lines 557-586 (the O(N^3) cost centers: cholesky_solve at :558, cholesky_factor + column-by-column cholesky_forward_back A^-1 loop at :567-578, edf trace at :581-586)
    - 47-RESEARCH.md OPT-F "Structural bottleneck — DEFER" + "DEFER note" (O(N^3) dense Cholesky + A^-1 GCV column solve; no behavior-preserving constant-factor win without sparse assembly (new dep, out of scope) or skipping GCV (changes edf/gcv fields, API change, out of scope))
    - 47-CONTEXT.md lines 34-35 + <deferred> block (a target with no safe win is documented+deferred; asymptotic rewrites out of scope)
  </read_first>
  <action>
    Add a concise rustdoc DEFER note in `fem_smooth` (near the A^-1 GCV loop, ~:567) recording that the O(N^3) dense Cholesky factorization + column-by-column A^-1 computation for the GCV trace is the structural wall-time bottleneck (452ms @ 576 nodes) and is DEFERRED for Phase 47: no safe behavior-preserving constant-factor win exists without either sparse assembly (requires a new crate dependency — out of scope) or skipping the GCV computation (would change the returned edf/gcv fields — a breaking API change, out of scope). Reference the milestone behavior-preserving + no-new-dependency + additive-only constraints. Keep it a short comment, not a code change.

    Finalize `PERF-RESULTS.md`: consolidate the before/after table for every landed OPT — A (dpca: 17,739 -> <1000 blocks, PERF-02 headline), B (fsvd Vec removed), C (ssvd Vec removed), D (functional_acf Vec + m^2 sqrt removed), E (face_covariance: 983.8ms -> measured, % improvement, PERF-01 headline), F (fem_smooth: clone removed, ~N*N alloc saved; O(N^3) compute DEFERRED). Include the Environment block (governor state — powersave LOW-CONFIDENCE caveat if sudo pinning unavailable, else performance; RAYON_NUM_THREADS; date; feature flags). Add a "Deferred (documented)" section listing the fem_smooth O(N^3) bottleneck with its rationale. Note that PERF-RESULTS.md + the perf_hotpaths benches feed Phase 51 BENCH-02.
  </action>
  <verify>
    <automated>test -f .planning/phases/47-hot-path-allocation-performance/PERF-RESULTS.md && grep -c 'OPT-A\|OPT-B\|OPT-C\|OPT-D\|OPT-E\|OPT-F' .planning/phases/47-hot-path-allocation-performance/PERF-RESULTS.md</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel -- fem_smooth 2>&1 | tail -4</automated>
    <acceptance_criteria>
      - PERF-RESULTS.md contains before/after rows for OPT-A..F + Environment block + a "Deferred (documented)" section with the fem_smooth O(N^3) rationale
      - the fem_smooth DEFER rustdoc note is present (doc-only; fem_smooth tests still pass)
    </acceptance_criteria>
  </verify>
  <done>fem_smooth O(N^3) bottleneck documented+deferred with rationale; PERF-RESULTS.md finalized with all OPT rows + deferred section.</done>
</task>

<task type="auto">
  <name>Task 3: Phase validation sign-off — full gate + VALIDATION.md nyquist_compliant</name>
  <files>.planning/phases/47-hot-path-allocation-performance/47-VALIDATION.md</files>
  <read_first>
    - .planning/phases/47-hot-path-allocation-performance/47-VALIDATION.md (Validation Sign-Off checklist lines 72-82; Per-Task Verification Map; the `nyquist_compliant: false` / `wave_0_complete: false` frontmatter flags at lines 5-6)
    - 47-CONTEXT.md <decisions> (behavior-preservation + benchmark-evidence + no-signature-change gates)
  </read_first>
  <action>
    Run the full phase gate one final time across all landed work:
    - Full suite: `cargo test -p fdars-core --features linalg,parallel` green.
    - Full clippy: `cargo clippy --all-targets --features linalg,parallel -- -D warnings` clean.
    - All Phase-47 golden tests pass: dpca, ssvd, fsvd, functional_acf, face_covariance, fem_smooth.
    - dhat: dpca block count < 1000 (PERF-02 hard gate).
    - >=1 before/after criterion cell shows >=15% (face_covariance) OR >=25% allocation reduction (dpca) — both should hold.
    - Verify no public signature changed: grep the six target fn signatures (dpca, fsvd, ssvd, functional_acf, face_covariance/cov_irreg, fem_smooth) and confirm byte-identical to pre-phase.

    Update `47-VALIDATION.md`: check off every Validation Sign-Off item, mark the Per-Task Verification Map rows green, set frontmatter `nyquist_compliant: true`, `wave_0_complete: true`, `status: complete`, and set Approval accordingly.

    Commit sequence (each: `cargo fmt -p fdars-core` then `git commit --no-verify`): OPT-F edit + golden test (Task 1), defer-doc + PERF-RESULTS.md finalize (Task 2), VALIDATION sign-off (Task 3).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel -- golden_ 2>&1 | tail -10</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features dhat-heap,linalg -- count_dpca_allocations_n200_m50 --nocapture --test-threads=1 2>&1 | tail -6</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy -p fdars-core --all-targets --features linalg,parallel -- -D warnings 2>&1 | tail -4</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel 2>&1 | tail -5</automated>
    <acceptance_criteria>
      - all six golden_ tests pass; dpca dhat blocks < 1000
      - clippy --all-targets clean; full existing suite green
      - 47-VALIDATION.md nyquist_compliant: true, wave_0_complete: true, all sign-off items checked
      - no public signature changed (six target fns byte-identical)
    </acceptance_criteria>
  </verify>
  <done>Phase 47 fully verified: all golden + dhat + criterion evidence green; VALIDATION.md signed off; no signature changes; deferred items documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure in-process numerical optimization — no external input, I/O, auth, or network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-07 | Tampering | fem_smooth output after single-pass build (Pitfall 1: phi_t_phi corrupted into a_mat) | high | mitigate | golden_fem_smooth (rel <=1e-10) captured pre-change; phi_t_phi kept pure for the GCV trace; existing fem_smooth tests green |
| T-47-08 | Repudiation | deferred O(N^3) win looks like an omission at verification | low | mitigate | explicit documented+deferred rationale in fem_smooth rustdoc + PERF-RESULTS.md "Deferred" section per CONTEXT.md policy |

No package installs. No T-47-SC.
</threat_model>

<verification>
- golden_fem_smooth passes within documented tolerance; no phi_t_phi.clone() remains.
- All six Phase-47 golden tests pass; dpca dhat blocks < 1000; >=1 criterion cell >=15% (or >=25% alloc).
- Full suite green + clippy --all-targets clean; no public signature changed.
- fem_smooth O(N^3) bottleneck documented+deferred; PERF-RESULTS.md finalized; VALIDATION.md signed off.
</verification>

<success_criteria>
- OPT-F landed as a behavior-preserving clone-elimination proven by golden test.
- fem_smooth O(N^3) compute correctly documented+deferred per CONTEXT.md.
- PERF-RESULTS.md consolidates all OPT before/after + deferred items; VALIDATION.md nyquist_compliant: true.
- No public signature changed; suite + clippy green across the whole phase.
</success_criteria>

<output>
Create `.planning/phases/47-hot-path-allocation-performance/47-04-SUMMARY.md` when done. Record the fem_smooth golden tolerance, the consolidated OPT-A..F before/after summary, and the final governor state.
</output>
