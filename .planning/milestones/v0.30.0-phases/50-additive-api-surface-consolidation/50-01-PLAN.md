---
phase: 50-additive-api-surface-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/boosting_regression/mod.rs
autonomous: true
requirements: [API-01, API-03]
estimate:
  tokens: 44000
  raw_tokens: 22000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "BoostingConfig, BayesianConfig, StabilityConfig each gain an additive `impl Default` with the documented field values (API-01) — no existing signature changes, purely a new trait impl"
    - "StlConfig is NOT touched — it already `#[derive(Default)]` at detrend/stl.rs:47; adding an impl would E0119-conflict (RESEARCH correction to the PROF-03 inventory)"
    - "The full additive-safe pipeline is proven end-to-end on the smallest self-contained change: build + both-config suite + clippy --all-targets -D warnings + 28-example build + wasm compile + fmt + --no-verify commit (the TRACER for the whole phase)"
  artifacts:
    - fdars-core/src/boosting_regression/mod.rs
  key_links:
    - "BoostingConfig/BayesianConfig/StabilityConfig::default() ← the documented (doc-VERIFIED + representative-constructor [ASSUMED]) field values from RESEARCH item #1"
---

<objective>
TRACER SLICE (smallest, most self-contained, zero behavior change): add `impl Default` to the THREE
config structs in `src/boosting_regression/mod.rs` that genuinely lack it — `BoostingConfig`,
`BayesianConfig`, `StabilityConfig` — using the documented field defaults. This is item #1 of the
additive API pass and the phase tracer: a `Default` trait impl cannot change any existing output, so
it exercises the FULL additive+gate machinery (build → both-config test suite → clippy --all-targets
-D warnings → 28-example build → wasm compile → fmt → `--no-verify` commit) with the lowest risk,
proving the pipeline before the riskier deprecation-introducing items (#2 fanova, #4 Dim dispatch).

CRITICAL RESEARCH CORRECTION (do NOT re-expand to 4 structs): the PROF-03 inventory listed FOUR
missing-Default configs including `StlConfig`, but the live tree shows `StlConfig` ALREADY
`#[derive(Debug, Clone, PartialEq, Default)]` at `src/detrend/stl.rs:47`. Adding
`impl Default for StlConfig` would fail with `error[E0119]: conflicting implementations`. Only the
THREE boosting configs get an impl; `StlConfig` is untouched.

Purpose: give users `Config::default()` ergonomics for the 3 outlier configs (52/56 already have it),
and prove the additive+examples/clippy/wasm gate pipeline before deprecation-introducing work.
Output: 3 `impl Default` blocks + a one-line `::default()` smoke test asserting the documented values.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/50-additive-api-surface-consolidation/50-CONTEXT.md
@.planning/phases/50-additive-api-surface-consolidation/50-RESEARCH.md
@fdars-core/src/boosting_regression/mod.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Add `impl Default` for the 3 boosting configs (item #1 — additive) + prove the full gate pipeline</name>
  <files>fdars-core/src/boosting_regression/mod.rs</files>
  <read_first>
    - fdars-core/src/boosting_regression/mod.rs (BoostingConfig:44, BayesianConfig:76, StabilityConfig:103 + each struct's field doc-comments — the values below are pulled from those docs / representative constructors)
    - fdars-core/src/detrend/stl.rs:47 (CONFIRM StlConfig already derives Default — do NOT add an impl for it; adding one is an E0119 conflict)
  </read_first>
  <action>
    Add three `impl Default` blocks in src/boosting_regression/mod.rs, each placed immediately after
    its struct definition. Use these field values (doc-VERIFIED values are locked; the [ASSUMED]
    numerics adopt the value the most-representative existing constructor / doc-example uses, per
    RESEARCH item #1 — any in-range value is back-compat-safe since these configs only take effect when
    explicitly constructed, so there is NO behavior golden to break):

      impl Default for BoostingConfig — mstop: 100 [ASSUMED, FDboost convention], nu: 0.1 [doc],
        nbasis: 10 [doc-example], order: 4 [doc "typically 4"], lfd_order: 2 [doc "typically 2"],
        lambda: 1.0 [doc-example], ncomp_x: 3 [doc-example], seed: 0 [matches stability.rs default_boost].

      impl Default for BayesianConfig — ncomp: 4 [ASSUMED, bayesian.rs default_config], tau2: 100.0 [doc],
        ig_a0: 0.001 [doc], ig_b0: 0.001 [doc], n_iter: 400 [ASSUMED, test constructor],
        burn_in: 200 [ASSUMED, test constructor], thin: 1 [doc min], seed: 0 [ASSUMED, isolation-only].

      impl Default for StabilityConfig — n_resamples: 100 [doc "default: 100"], pi_thr: 0.9 [doc "default: 0.9"],
        seed: 0 [ASSUMED, isolation-only].

    Confirm the exact field set against the struct definitions in this session (do NOT invent fields).
    Since none of the three structs is `#[non_exhaustive]`, a struct-literal `Self { .. }` in the impl
    body is valid; adding `Default` is still purely additive (a new trait impl never breaks existing
    construction). Do NOT touch StlConfig, and do NOT add `#[non_exhaustive]` or change any derive.
    After editing, run `cargo fmt -p fdars-core` and commit with `git commit --no-verify`.
    Prefix EVERY cargo command with `TMPDIR=/home/simonm/.cache/fdars-bench-tmp`. If the examples build
    fails with "linking with cc failed" / "No space left on device", run
    `rm -rf target/debug/{incremental,examples}` and retry.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --features linalg,parallel && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    The 3 boosting configs compile with a `Default` impl; StlConfig is unchanged (no E0119). Crate builds
    under --features linalg,parallel and `clippy --all-targets -- -D warnings` is clean.
  </acceptance_criteria>
  <done>3 additive Default impls land; no StlConfig conflict; crate + clippy green.</done>
  <reversibility rating="reversible">Additive trait impls only; revert deletes the 3 blocks.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `::default()` smoke test + full phase-tracer gate (both configs, examples, wasm, fmt)</name>
  <files>fdars-core/src/boosting_regression/mod.rs</files>
  <read_first>
    - fdars-core/src/boosting_regression/mod.rs (the 3 new Default impls from Task 1; the existing `#[cfg(test)] mod tests` block, if any, to append to)
  </read_first>
  <behavior>
    - BoostingConfig::default() == BoostingConfig { mstop: 100, nu: 0.1, nbasis: 10, order: 4, lfd_order: 2, lambda: 1.0, ncomp_x: 3, seed: 0 }
    - BayesianConfig::default() == BayesianConfig { ncomp: 4, tau2: 100.0, ig_a0: 0.001, ig_b0: 0.001, n_iter: 400, burn_in: 200, thin: 1, seed: 0 }
    - StabilityConfig::default() == StabilityConfig { n_resamples: 100, pi_thr: 0.9, seed: 0 }
  </behavior>
  <action>
    Add a small `#[test]` in the boosting_regression test module asserting each config's `::default()`
    equals a struct-literal with the documented values above (all three structs derive PartialEq, so
    `assert_eq!(Config::default(), Config { .. })` works). This pins the documented defaults so a future
    edit that silently changes a default is caught. Then run the FULL phase-tracer gate below to prove
    the additive pipeline end-to-end (this is why item #1 is the tracer): both feature configs green, all
    28 examples build, wasm compiles. Prefix every cargo command with
    `TMPDIR=/home/simonm/.cache/fdars-bench-tmp`. If the 28-example build fails with a LINK / disk error,
    run `rm -rf target/debug/{incremental,examples}` and retry. Run `cargo fmt -p fdars-core` and commit
    with `git commit --no-verify` (doctest link needs /tmp; the milestone-end fmt sweep covers drift).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel boosting_regression && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --no-default-features --features linalg boosting_regression && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --examples --features linalg,parallel && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --target wasm32-unknown-unknown --features js --no-default-features && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    `::default()` smoke test passes under BOTH --features linalg,parallel AND --no-default-features
    --features linalg. All 28 examples build (`cargo build --examples`). wasm32-unknown-unknown compiles
    with --features js. clippy --all-targets -D warnings clean. fmt clean; committed --no-verify.
  </acceptance_criteria>
  <done>Documented defaults pinned by test; full additive+examples+wasm gate pipeline proven green (phase tracer complete).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure numerical library; additive trait impl only. No I/O, no untrusted input, no new dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-01 | Tampering | boosting_regression Default values | low | mitigate | `::default()` smoke test pins the documented values; no behavior golden exists (configs only take effect when explicitly constructed) |
| T-50-02 | Denial of Service | 28-example build fills /home (target/) | medium | mitigate | TMPDIR prefix on every cargo cmd; `rm -rf target/debug/{incremental,examples}` retry per MEMORY |
</threat_model>

<verification>
- `cargo build -p fdars-core --features linalg,parallel` + clippy `--all-targets -- -D warnings` clean.
- `::default()` smoke test green under BOTH --features linalg,parallel AND --no-default-features --features linalg.
- All 28 examples build (`cargo build -p fdars-core --examples --features linalg,parallel`).
- wasm compile: `cargo build -p fdars-core --target wasm32-unknown-unknown --features js --no-default-features`.
- `cargo fmt -p fdars-core` clean; commits via `git commit --no-verify`.
- StlConfig untouched (no E0119 conflict); no new dependency; no public signature change.
</verification>

<success_criteria>
- 3 additive `impl Default` blocks exist for BoostingConfig/BayesianConfig/StabilityConfig.
- StlConfig is NOT modified (already derives Default).
- `::default()` smoke test pins the documented values under both feature configs.
- Full additive+examples+wasm+clippy+fmt gate pipeline proven green (the phase tracer).
</success_criteria>

<output>
Create `.planning/phases/50-additive-api-surface-consolidation/50-01-SUMMARY.md` when done, mirroring
the Phase-49 SUMMARY format (frontmatter with provides/affects/key-files/key-decisions/coverage).
Record: (1) that item #1 shipped 3 Default impls (NOT 4 — StlConfig already derived it), (2) the exact
default values used and which were [ASSUMED] vs doc-VERIFIED, (3) that the additive+examples+wasm+clippy
gate pipeline is now proven for the deprecation-introducing plans 50-02 (fanova) and 50-03 (Dim dispatch).
</output>
