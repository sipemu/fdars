---
phase: 89-release-preparation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/Cargo.toml
  - CHANGELOG.md
  - fdars-core/CHANGELOG.md
  - documentation/ROADMAP-TO-1.0.md
autonomous: true
requirements: [REL-01]

estimate:
  tokens: 20000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "fdars-core is version 0.42.0 with a breaking-framed [0.42.0] entry in both root and crate CHANGELOG calling out sealed wire/config + the construction-idiom change + all renames/consolidations (REL-01 SC1)."
    - "documentation/ROADMAP-TO-1.0.md has AUD-09/12/13/19-23 checked off (REL-01 SC3)."
    - "Whole-crate gates green incl. cargo package: fmt, clippy --all-targets, test, serde build, examples, doctests (REL-01 SC2)."
    - "No git tag / publish performed — deferred operator step (REL-01 SC4)."
  artifacts:
    - "fdars-core/Cargo.toml (version 0.42.0)"
    - "CHANGELOG.md + fdars-core/CHANGELOG.md ([0.42.0] entry)"
    - "documentation/ROADMAP-TO-1.0.md (AUD items checked off)"
  key_links:
    - "The CHANGELOG must state the SEAL-02/03 construction-idiom change (default()+field-assign, not struct literals) — the subtle breaking change beyond 'sealed configs'."
---

<objective>
Prepare fdars-core v0.42.0 for release: bump the version, document all breaking API-shape changes from phases 86–88, check off the cleared 1.0-checklist API items, and prove release-readiness with the full gate set + `cargo package`. Leave the `git tag`/publish as the deferred operator step.
</objective>

<execution_context>
Repo notes (phases 86–88): pre-commit hook exceeds 30s → commit `--no-verify` + `cargo fmt` first; run gates individually foreground; /home ~99% full → route heavy builds to /tmp tmpfs. Audit/release milestone: do NOT `git tag` (release.yml publishes on any v* tag).
</execution_context>

<tasks>

<task type="tracer">
  <name>Task A: version bump + CHANGELOG [0.42.0] (root + crate)</name>
  <files>fdars-core/Cargo.toml, CHANGELOG.md, fdars-core/CHANGELOG.md</files>
  <read_first>
    - fdars-core/Cargo.toml (version line)
    - CHANGELOG.md + fdars-core/CHANGELOG.md (the [0.41.0] entry — match its style)
    - 86-01-SUMMARY.md, 87-01-SUMMARY.md, 88-01-SUMMARY.md (the exact breaking changes)
  </read_first>
  <action>
    Bump `version = "0.41.0"` → `version = "0.42.0"` in fdars-core/Cargo.toml. Add a breaking-framed `## [0.42.0] - 2026-09-08` entry above `[0.41.0]` in BOTH CHANGELOG.md (root) and fdars-core/CHANGELOG.md, with a `### Breaking` (or `### Changed` breaking) section covering: sealed `wire` `pub(crate)` (SEAL-01); `#[non_exhaustive]` on 38 config structs AND the construction-idiom change (external code must use `Config::default()` + field assignment — struct literals incl. `..Default::default()` no longer compile externally) (SEAL-02/03); `geometric_median`/`hausdorff_self`/`hausdorff_cross`/`functional_spatial`/`kernel_functional_spatial` consolidated onto `Dim` dispatch + `LpeerResult`→`LocalPeerResult` (NAME-01/02/03/05); the large Phase-88 batch — 41 lone `_1d` functions consolidated onto `Dim` dispatch and 5 genuinely-1D functions plain-renamed (`fdata_to_pc`, `fdata_to_pls`, `fourier_fit`, `pspline_fit`, `select_basis_auto` drop `_1d`) (NAME-04). Note explicitly: API-shape-only, no numeric/behavioral change.
  </action>
  <verify>
    <automated>grep -q '^version = "0.42.0"' fdars-core/Cargo.toml && grep -q '0.42.0' CHANGELOG.md && grep -q '0.42.0' fdars-core/CHANGELOG.md && grep -qi 'LocalPeerResult' fdars-core/CHANGELOG.md</automated>
    <fails_when>version not bumped, or a [0.42.0] entry missing from either CHANGELOG, or the entry omits the rename callouts (grep for LocalPeerResult).</fails_when>
  </verify>
  <acceptance_criteria>
    - fdars-core/Cargo.toml has `version = "0.42.0"`.
    - Both CHANGELOGs have a `[0.42.0]` breaking-framed entry covering SEAL + all NAME items + the construction-idiom change.
  </acceptance_criteria>
  <done>Version bumped and both CHANGELOGs document the breaking changes.</done>
</task>

<task type="auto">
  <name>Task B: check off ROADMAP-TO-1.0 API items</name>
  <files>documentation/ROADMAP-TO-1.0.md</files>
  <read_first>documentation/ROADMAP-TO-1.0.md (API surface section, AUD-09/12/13/19-23)</read_first>
  <action>Change the `- [ ]` to `- [x]` for AUD-09/AUD-13 (wire), AUD-12 (config non_exhaustive), AUD-19 (geometric_median), AUD-20 (hausdorff), AUD-21 (functional_spatial), AUD-22 (large suffix batch), AUD-23 (LpeerResult). Add a short "cleared in v0.42.0" note per item where the format allows. Do NOT check off non-API items (golden flake, SDTW-O1, diff-core, fdars-r, 1.0-CUT).</action>
  <verify>
    <automated>test $(grep -cE '^- \[x\] \*\*`AUD-(09|12|19|20|21|22|23)`' documentation/ROADMAP-TO-1.0.md) -ge 6</automated>
    <fails_when>fewer than the API AUD items are checked off (still `- [ ]`).</fails_when>
  </verify>
  <acceptance_criteria>AUD-09/12/13/19-23 are `- [x]`; non-API items untouched.</acceptance_criteria>
  <done>The 1.0 checklist reflects the cleared API section.</done>
</task>

<task type="auto">
  <name>Task C: full gate set + cargo package (release-readiness proof)</name>
  <files>(no edits — verification only)</files>
  <read_first>89-CONTEXT.md gate list</read_first>
  <action>Run each gate individually (foreground; route heavy builds to /tmp): `cargo fmt --check`; `cargo clippy --all-targets --features linalg,parallel -- -D warnings`; `cargo test --features linalg,parallel`; `cargo build --features serde`; `cargo build --examples`; `cargo test --doc --features linalg,parallel`; `cargo package -p fdars-core --no-verify` (or full `cargo package` if disk allows — it re-builds the packaged tarball). Fix any doc/version inconsistency surfaced (e.g. a doctest naming an old symbol). Do NOT `git tag` or publish.</action>
  <verify>
    <automated>cargo fmt --check && cargo package -p fdars-core --allow-dirty --no-verify 2>&1 | tail -1</automated>
    <fails_when>fmt diff, or cargo package errors (missing files, bad manifest, version issue).</fails_when>
  </verify>
  <acceptance_criteria>All gates green; `cargo package` produces a valid .crate; no git tag created.</acceptance_criteria>
  <done>v0.42.0 is release-ready and verified; the tag/publish remains the operator's step.</done>
</task>

</tasks>

<threat_model>Docs + version metadata only — no code, no new attack surface. N/A.</threat_model>

<success_criteria>
Maps to the 4 ROADMAP Phase 89 success criteria: version bump + breaking CHANGELOG (SC1), gates + cargo package (SC2), ROADMAP-TO-1.0 checkoff (SC3), tag/publish deferred (SC4).
</success_criteria>

<output>Create .planning/phases/89-release-preparation/89-01-SUMMARY.md when done.</output>
