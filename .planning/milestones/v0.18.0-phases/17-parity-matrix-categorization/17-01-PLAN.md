---
phase: 17-parity-matrix-categorization
plan: "17-01"
type: execute
wave: 1
requirements: [GAP-01, GAP-02]
audit_only: true
deliverable: .planning/research/R-AUDIT-REPORT.md
files_modified:
  - .planning/research/R-AUDIT-REPORT.md
status: ready
---

# Phase 17 · Plan 01 — Parity Matrix & Categorization

**Goal:** Append `§Parity-Matrix` (GAP-01) and `§Categorization` (GAP-02) to `.planning/research/R-AUDIT-REPORT.md`, mapping all 248 in-scope R capabilities against fdars. Audit-only — zero `fdars-core/src/` edits.

## Task 1 — Parity matrix (GAP-01)
For each of the 248 in-scope R capabilities in §R-Inventory, emit one row (grouped by the 9 areas) with: capability, source package(s), **verdict** (present / partial / absent) under the reused D-01 rubric, a **"searched fdars for:"** evidence note, and the **closest-match fdars reference** (module/function) or "no match found". Match by capability semantics, not API name. Reuse the v0.14.0 `AUDIT-REPORT.md` §Phase 8 fdars-side findings where an R capability overlaps an already-assessed scikit-fda one; confirm/extend by grepping `fdars-core/src/`. **Credit v0.15.0–v0.17.0 additions as present** (spline interp, summary stats, imputation, ExtrapolationPolicy, scoring metrics, shift registration + registration-quality scores, banded elastic, parallel CV, faer SVD, parallel elastic-FPCA). Total verdicts per area and overall; state the actionable-gap count (absent + partial) explicitly.

### Success checks (ROADMAP SC1–SC3)
- [ ] One row per in-scope R capability, present/partial/absent, matched by capability, single documented verdict rubric.
- [ ] Every row has a "searched fdars for:" note + closest-match reference or "no match found".
- [ ] Verdict counts per area + overall; actionable-gap count explicit.

## Task 2 — Categorization (GAP-02)
Categorize every absent/partial gap as **table-stakes / differentiator / out-of-scope** with a one-line rationale, under the reused D-03 rubric (documented once). Produce a `§Categorization` summary (counts by category; the table-stakes gaps are the priority signal for Phase 19).

### Success checks (ROADMAP SC4)
- [ ] Every absent/partial gap categorized with rationale, single documented category rubric.

**Constraints:** append to existing `R-AUDIT-REPORT.md` (do not overwrite Phase 16 sections; do not touch archived scikit-fda `AUDIT-REPORT.md`). Zero `fdars-core/src/` edits.

**Commit:** `docs(17): parity matrix + gap categorization (GAP-01/GAP-02)`
