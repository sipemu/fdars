---
phase: 16-r-ecosystem-inventory
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - .planning/research/R-AUDIT-REPORT.md
autonomous: true
requirements: [INV-02]
estimate:
  tokens: 30000
  raw_tokens: 16000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "The in/out-of-scope design-goal-filter rule is documented exactly once, with an explicit rationale (in-scope = numeric algorithm OR API-ergonomics; out-of-scope = plotting OR IO; numeric underpinnings of diagnostic plots stay in-scope)"
    - "A §Design-Goal Filter per-area count table exists: 9 area rows plus a TOTAL, columns In-Scope / Out-of-Scope / Total Rows, summing to 248 / 27 / 275"
    - "The table states the actionable in-scope comparison surface (248) that Phase 17's parity matrix will map against"
  artifacts:
    - ".planning/research/R-AUDIT-REPORT.md"
  key_links:
    - "The per-area in-scope count column is the exact set of rows Phase 17 iterates over — its totals must reconcile with the §R-Inventory per-area count headers written in 16-01"
---

<objective>
Append the `§Design-Goal Filter` section to `.planning/research/R-AUDIT-REPORT.md`: state the in/out-of-scope rule once with rationale, then present the per-area in-scope-vs-out-of-scope count table (9 areas + TOTAL = 248 / 27 / 275) that yields the actionable comparison surface for Phase 17. Covers INV-02.

Purpose: Convert the tagged inventory (from 16-01) into the actionable-surface count — the single table Phase 17 uses to scope its parity mapping.
Output: A `§Design-Goal Filter — Per-Area Summary` subsection appended to the existing R-AUDIT-REPORT.md, plus verification that its totals reconcile with the §R-Inventory per-area headers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md

# The completed survey — §Design-Goal Filter table + rule are lifted from here.
@.planning/phases/16-r-ecosystem-inventory/16-RESEARCH.md

# Resolved decisions — AUTHORITATIVE (in/out rule wording, per-area count requirement)
@.planning/phases/16-r-ecosystem-inventory/16-CONTEXT.md

# Depends on 16-01: the §R-Inventory section (9 areas + per-area count headers) must already exist in this file.
@.planning/phases/16-r-ecosystem-inventory/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the §Design-Goal Filter section — rule (documented once) + per-area count table</name>
  <files>.planning/research/R-AUDIT-REPORT.md</files>
  <action>
    Append a new subsection `## §Design-Goal Filter — Per-Area Summary` to the END of the existing R-AUDIT-REPORT.md (do NOT touch or re-emit the §R-Inventory section from 16-01; do NOT overwrite the file — append only). Author two parts:

    (1) A one-paragraph statement that the in/out-of-scope classification uses the rule already stated once in §Methodology, then restate that rule's rationale compactly for reference: in-scope = numeric algorithm OR API-ergonomics capability portable to a numeric Rust library; out-of-scope = plotting/visualization OR data/IO (dataset loaders, read/write round-trips); and the borderline ruling that the numeric underpinnings of graphical diagnostics (e.g. outliergram MO/MEI values, MS-plot / functional-boxplot fence statistics) are in-scope even though the renderer is not. Source this wording from 16-RESEARCH.md §Design-Goal Filter and 16-CONTEXT.md "In / Out-of-Scope Filter". Keep it as a single documented rule — do not introduce a second, conflicting definition.

    (2) The per-area count table lifted verbatim from 16-RESEARCH.md "§Design-Goal Filter — Per-Area Summary": one row per area (9 areas) with In-Scope / Out-of-Scope / Total Rows / Out-of-Scope Breakdown columns, plus a bold TOTAL row reading 248 in-scope / 27 out-of-scope / 275 total (breakdown: 25 plotting, 3 IO — note the survey's TOTAL breakdown line). Immediately follow the table with the bold statement: "Actionable in-scope capabilities for Phase 17 parity mapping: 248", and carry over the survey's note that some capabilities appear in multiple areas (logical grouping, matched by semantics not row count in Phase 17) and the rainbow-package all-out-of-scope note.

    Do NOT add fdars parity verdicts or gap categorization — Phase 17 material, deferred (16-CONTEXT.md Deferred Ideas).
  </action>
  <verify>
    <automated>grep -q "## §Design-Goal Filter" .planning/research/R-AUDIT-REPORT.md && grep -Eq "248" .planning/research/R-AUDIT-REPORT.md && grep -q "Actionable in-scope capabilities for Phase 17 parity mapping: 248" .planning/research/R-AUDIT-REPORT.md && grep -Eq "in-scope|In-Scope" .planning/research/R-AUDIT-REPORT.md</automated>
  </verify>
  <done>§Design-Goal Filter section appended with the in/out rule (documented once, with rationale) and the 9-area+TOTAL count table (248 / 27 / 275); the "248 actionable" surface line present; the §R-Inventory section from 16-01 is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the count table against §R-Inventory and confirm INV-02 completeness</name>
  <files>.planning/research/R-AUDIT-REPORT.md</files>
  <action>
    Cross-check the appended §Design-Goal Filter table against the §R-Inventory per-area count headers written by 16-01, and fix any mismatch (the design-goal table is the authoritative summary; if an area header and the table disagree, correct whichever copies the survey wrong so both match 16-RESEARCH.md). The nine per-area in-scope values must be 38, 22, 31, 59, 22, 24, 24, 10, 18 (sum 248) and out-of-scope 7, 0, 7, 2, 3, 2, 1, 2, 3 (sum 27), total 275. Confirm the design-goal-filter rule appears as a single documented rule (methodology statement + this section's compact restatement reference it — not a second divergent definition). No other edits.
  </action>
  <verify>
    <automated>python3 - <<'PY'
import re
t=open('.planning/research/R-AUDIT-REPORT.md').read()
# The Design-Goal Filter TOTAL row must assert 248 / 27 / 275
dgf=t.split('§Design-Goal Filter')[-1]
assert re.search(r'\*\*?248\*\*?', dgf) or '248' in dgf, "248 in-scope total missing from DGF section"
assert '275' in dgf, "275 total missing from DGF section"
assert '27' in dgf, "27 out-of-scope total missing from DGF section"
# per-area in-scope headers from R-Inventory still reconcile to 248
counts=re.findall(r'In-scope count:\s*(\d+)\s+Out-of-scope count:\s*(\d+)', t)
ins=sum(int(a) for a,_ in counts[:9]); outs=sum(int(b) for _,b in counts[:9])
assert (ins,outs)==(248,27), f"R-Inventory per-area headers {ins}/{outs} != 248/27"
assert "Actionable in-scope capabilities for Phase 17 parity mapping: 248" in t
print("OK: DGF table reconciles with R-Inventory; 248 in / 27 out / 275 total; actionable surface stated")
PY</automated>
  </verify>
  <done>The §Design-Goal Filter per-area table and the §R-Inventory per-area headers both reconcile to 248 in-scope / 27 out-of-scope / 275 total; the in/out rule is documented once; the 248 actionable-surface line is present. INV-02 satisfied.</done>
</task>

</tasks>

<verification>
- §Design-Goal Filter section exists with a per-area (9-row) + TOTAL count table summing to 248 in-scope / 27 out-of-scope / 275 total.
- The in/out-of-scope rule is documented once (with the numeric-underpinnings-of-plots borderline ruling), rationale included.
- The table's per-area in-scope column reconciles with the §R-Inventory per-area count headers from 16-01.
- The actionable in-scope comparison surface (248) is stated explicitly for Phase 17. (ROADMAP SC3 + SC4.)
</verification>

<success_criteria>
- INV-02 fully addressed: every capability carries an in/out-of-scope tag (from 16-01), the classifying rule is documented once, and a per-area in-scope-vs-out-of-scope count table yields the 248-capability actionable surface.
- ROADMAP Phase-16 SC3 (in/out tag + rule once) and SC4 (per-area count table → actionable surface) are TRUE in the written report.
- Zero `fdars-core/src/` edits.
</success_criteria>

<output>
Create `.planning/phases/16-r-ecosystem-inventory/16-02-SUMMARY.md` when done.
</output>
