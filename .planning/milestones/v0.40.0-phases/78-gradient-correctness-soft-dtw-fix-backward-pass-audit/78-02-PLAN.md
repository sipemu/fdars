---
phase: 78-gradient-correctness-soft-dtw-fix-backward-pass-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/78-gradient-correctness-soft-dtw-fix-backward-pass-audit/78-AUDIT.md
autonomous: true
requirements: [CORR-02]
estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Every one of the 9 hand-written backward/gradient passes named in CORR-02 has a recorded clean/fixed disposition with a traceable one-line rationale (CORR-02)"
    - "78-AUDIT.md exists in the phase directory and survives milestone archival as the CORR-02 audit trail (CORR-02)"
    - "Any NEW localized boundary-seed bug found during the sweep is fixed in-phase; anything needing a redesign is deferred to backlog with a logged item (CORR-02)"
  artifacts:
    - ".planning/phases/78-gradient-correctness-soft-dtw-fix-backward-pass-audit/78-AUDIT.md (CORR-02 per-module disposition table)"
  key_links:
    - "78-AUDIT.md disposition table -> referenced from the phase SUMMARY so the audit trail is discoverable after archival"
    - "metric/soft_dtw row in the audit table -> 'fixed' cross-references Plan 01 (CORR-01)"
---

<objective>
Complete the CORR-02 audit sweep: confirm the disposition of every hand-written backward/gradient pass named in CORR-02 and record a traceable clean/fixed disposition table in a dedicated `78-AUDIT.md`.

Research (78-RESEARCH.md, HIGH confidence, all claims `[VERIFIED: file:line]`) already read each of the 9 target modules and found NO additional endpoint-seed bugs: eight siblings use closed-form analytical gradients, forward-mode `Dual` AD, finite differences, or a scalar hill-climber — none has an overwriteable DP backward seed analogous to soft_dtw. The one bug (`metric/soft_dtw`) is fixed in Plan 01. This plan re-confirms each disposition against the source (a read-and-annotate task, not code) and writes the artifact.

Per CONTEXT decision: if the sweep re-confirms a NEW localized boundary-seed bug (analogous to the endpoint-seed one-liner), fix it in-phase; defer anything requiring an algorithm redesign to the backlog with a logged item. Based on research, none is expected.

Purpose: CORR-02 — every hand-written gradient pass audited and dispositioned clean/fixed with traceable rationale, recorded in a durable artifact.
Output: `.planning/phases/78-.../78-AUDIT.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md

This plan touches NO `fdars-core/src/` code unless the sweep finds a new localized bug (not expected). If it does, apply the CONTEXT decision: localized boundary-seed fix in-phase (mirror the CORR-01 pattern + add a regression test), redesign-scale issue deferred to backlog with a logged item. Build hazards from Plan 01's execution_context apply if any code change is made (fmt per commit, clippy --all-targets, --no-verify commits after out-of-band gates).
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-gradient-correctness-soft-dtw-fix-backward-pass-audit/78-RESEARCH.md
@.planning/phases/78-gradient-correctness-soft-dtw-fix-backward-pass-audit/78-CONTEXT.md

The 9 CORR-02 target modules (with the research disposition to confirm):
1. `fdars-core/src/metric/soft_dtw.rs` — `soft_dtw_backward` — **fixed** (CORR-01, Plan 01).
2. `fdars-core/src/alignment/differentiable.rs` — forward-mode `Dual` pipeline (interp/central-diff/L2 boundary handling are value-level, not gradient seeds) — **clean**.
3. `fdars-core/src/autodiff.rs` — the forward-mode AD substrate itself; no backward pass exists (reverse-mode deferred as DIF-F1) — **clean**.
4. `fdars-core/src/boosting_regression/gamlss.rs` — closed-form pointwise Gaussian log-likelihood negative gradients (`mu_neg_gradient`, `sigma_neg_gradient`); sigma_floor/eps are stability guards — **clean**.
5. `fdars-core/src/elastic_regression/logistic.rs` — closed-form logistic (BCE+L2) analytical gradient (`logistic_gradients`, `armijo_line_search_logistic`) — **clean**.
6. `fdars-core/src/explain_generic/counterfactual.rs` — finite-difference numerical gradient (`compute_gradient_finite_diff`, `counterfactual_gd_search`) — **clean**.
7. `fdars-core/src/regression.rs` — `project_scores_generic` linear projection, exact forward-mode `Dual` gradient — **clean**.
8. `fdars-core/src/seasonal/mod.rs` — `refine_period_gradient` is a 3-point-stencil scalar hill-climber (not a DP backward pass) — **clean**.
9. `fdars-core/src/smooth_basis.rs` — `differentiate_basis_columns` / `gradient_uniform` numerical differentiation for penalty-matrix construction; standard boundary stencils — **clean**.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-confirm each CORR-02 module disposition against source and note any new bug</name>
  <files>.planning/phases/78-gradient-correctness-soft-dtw-fix-backward-pass-audit/78-AUDIT.md</files>
  <read_first>
    - .planning/phases/78-gradient-correctness-soft-dtw-fix-backward-pass-audit/78-RESEARCH.md (CORR-02 Audit Sweep — Per-Module Map, sections 1-9, with file:line citations)
    - .planning/phases/78-gradient-correctness-soft-dtw-fix-backward-pass-audit/78-CONTEXT.md (Fix Scope & Audit Rigor decision; Deferred Ideas)
    - fdars-core/src/alignment/differentiable.rs (functions cited at lines 33-138, 200-236)
    - fdars-core/src/boosting_regression/gamlss.rs:53-101 (mu_neg_gradient, sigma_neg_gradient)
    - fdars-core/src/elastic_regression/logistic.rs:466-531 (logistic_gradients, armijo_line_search_logistic)
    - fdars-core/src/explain_generic/counterfactual.rs:1-83 (compute_gradient_finite_diff, counterfactual_gd_search)
    - fdars-core/src/regression.rs:232-251 (project_scores_generic)
    - fdars-core/src/seasonal/mod.rs:1111-1135 (refine_period_gradient)
    - fdars-core/src/smooth_basis.rs:984-1005 (differentiate_basis_columns) and fdars-core/src/helpers.rs:747-784 (gradient_uniform boundary stencils)
    - fdars-core/src/autodiff.rs:484-627 (diff/grad/jacobian/directional_derivative + Dual::seed)
  </read_first>
  <action>
    For each of the 9 modules, open the cited function(s) and scan specifically for the failure mode CORR-02 targets: a recurrence or DP pass where a pre-set boundary/endpoint value (a "seed") could be overwritten by a general update rule (the soft_dtw pattern). The research disposition for each is listed in this plan's `<context>`; your job is to VERIFY it against the current source, not to re-derive it from scratch — but do actually read the cited functions and satisfy yourself the rationale holds.

    For each module, capture: file path, the audited function name(s), the gradient mechanism (closed-form analytical / forward-mode Dual AD / finite-difference / scalar hill-climber / DP backward pass), whether an overwriteable boundary seed exists, disposition (`clean` or `fixed`), and a one-line rationale citing the mechanism.

    If — contrary to the research — you find a NEW localized boundary-seed bug (a recurrence overwriting a pre-set boundary value, analogous to soft_dtw), apply the CONTEXT decision: if it is a small localized behavior-preserving fix, fix it in-phase (mirror the CORR-01 endpoint-skip pattern, add a regression test, run the gates) and mark that module `fixed` with a reference to the fix; if it would require an algorithm redesign, do NOT fix it here — record it `deferred`, add a one-line backlog entry via the project backlog mechanism, and note the deferral in the audit table. Based on the HIGH-confidence research, no new bug is expected — do not invent one.

    Do not write the artifact file yet — collect the per-module findings for Task 2.
  </action>
  <acceptance_criteria>
    - Behavior: each of the 9 CORR-02 modules has been opened at its cited function(s) and its disposition confirmed against current source (not just copied from research).
    - Source (conditional): if any new localized boundary-seed bug is found, it is either fixed in-phase with a regression test (module marked `fixed`) OR deferred to backlog with a logged item (module marked `deferred`) per the CONTEXT decision — never silently omitted.
    - Traceability: every module's finding names a specific file path and function(s) and the gradient mechanism, ready to populate the disposition table.
  </acceptance_criteria>
  <verify>
    <automated>test -f fdars-core/src/alignment/differentiable.rs && test -f fdars-core/src/boosting_regression/gamlss.rs && test -f fdars-core/src/elastic_regression/logistic.rs && test -f fdars-core/src/explain_generic/counterfactual.rs && test -f fdars-core/src/seasonal/mod.rs && test -f fdars-core/src/smooth_basis.rs && test -f fdars-core/src/autodiff.rs && test -f fdars-core/src/regression.rs && echo AUDIT_SOURCES_PRESENT</automated>
    <fails_when>non-zero exit, or "AUDIT_SOURCES_PRESENT" absent from output (a cited audit source file is missing, so the disposition cannot be traced)</fails_when>
  </verify>
  <done>All 9 CORR-02 module dispositions are confirmed against current source; any new localized bug is fixed-in-phase or deferred-to-backlog per the CONTEXT decision (none expected).</done>
</task>

<task type="auto">
  <name>Task 2: Write 78-AUDIT.md with the per-module disposition table</name>
  <files>.planning/phases/78-gradient-correctness-soft-dtw-fix-backward-pass-audit/78-AUDIT.md</files>
  <read_first>
    - .planning/phases/78-gradient-correctness-soft-dtw-fix-backward-pass-audit/78-CONTEXT.md (Audit artifact decision — dedicated 78-AUDIT.md, referenced from SUMMARY)
    - .planning/phases/78-gradient-correctness-soft-dtw-fix-backward-pass-audit/78-RESEARCH.md (CORR-02 Per-Module Map — source of the rationale wording)
  </read_first>
  <action>
    Write `78-AUDIT.md` in the phase directory recording the CORR-02 sweep. Include: a short header (phase, requirement CORR-02, date, and a one-line statement of what was audited); then a disposition table with one row per module containing — Module (path), Audited function(s), Gradient mechanism, Overwriteable boundary seed? (yes/no), Disposition (`clean` / `fixed` / `deferred`), Rationale (one line). Populate all 9 rows from Task 1's findings. The `metric/soft_dtw` row is `fixed` and must cross-reference CORR-01 / Plan 01. Below the table, add a one-line note per any `fixed` or `deferred` row pointing to the fix commit or backlog item. Close with a one-line summary stating the sweep found no additional boundary-seed bugs beyond soft_dtw (or, if the sweep found otherwise, state what and how it was dispositioned).

    Keep rationale wording concise and traceable (cite file:line where useful); per-sibling wording is at Claude's discretion per the CONTEXT decision.

    Note for the phase SUMMARY author: the SUMMARY must reference this `78-AUDIT.md` so the audit trail survives milestone archival (CONTEXT decision).
  </action>
  <acceptance_criteria>
    - Artifact: `.planning/phases/78-gradient-correctness-soft-dtw-fix-backward-pass-audit/78-AUDIT.md` exists and is git-trackable.
    - Content: the disposition table has exactly 9 module rows, each with path + audited function(s) + mechanism + boundary-seed(yes/no) + disposition + one-line rationale; the `metric/soft_dtw` row is `fixed` and cross-references CORR-01/Plan 01.
    - Content: every row's disposition is one of `clean`/`fixed`/`deferred`; no module is missing or left blank.
    - Content: a closing summary line states whether any bug beyond soft_dtw was found.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/78-gradient-correctness-soft-dtw-fix-backward-pass-audit/78-AUDIT.md && grep -qi 'soft_dtw' .planning/phases/78-gradient-correctness-soft-dtw-fix-backward-pass-audit/78-AUDIT.md && [ "$(grep -Eic 'differentiable|autodiff|gamlss|logistic|counterfactual|regression|seasonal|smooth_basis|soft_dtw' .planning/phases/78-gradient-correctness-soft-dtw-fix-backward-pass-audit/78-AUDIT.md)" -ge 9 ] && echo AUDIT_OK</automated>
    <fails_when>non-zero exit, or "AUDIT_OK" absent from output (file missing, no soft_dtw row, or fewer than the 9 expected module names present)</fails_when>
  </verify>
  <done>`78-AUDIT.md` records all 9 module dispositions in a table with traceable one-line rationales; soft_dtw is `fixed` (cross-referencing CORR-01) and a closing summary states whether any further bug was found.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan produces a planning artifact and (only conditionally) a localized numerical fix; no external input boundary is introduced or changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-78-02 | Tampering (data integrity) | any sibling gradient pass found buggy during the sweep | low | mitigate | The audit itself is the mitigation: each hand-written gradient pass is checked for a boundary-seed defect; a new localized bug is fixed in-phase with a regression test, a redesign-scale issue is deferred to backlog with a logged item. |

No package-manager installs occur in this plan (no `T-78-SC` supply-chain threat). ASVS Level 1; no new input-validation surface.
</threat_model>

<verification>
Per-task: the `<automated>` checks above (audit source files present; 78-AUDIT.md exists with the 9 module rows).

Plan gate: if — and only if — Task 1 makes a code change (new localized fix, not expected), run the full out-of-band gate before committing with `--no-verify`:
- `cargo fmt` (apply) + `cargo fmt --check`
- `cargo clippy --all-targets --features linalg,parallel -- -D warnings`
- `cargo test -p fdars-core --features linalg,parallel` for the affected module

If no code change is made (the expected case), the artifact-only commit needs no cargo gate.
</verification>

<success_criteria>
- All 9 CORR-02 hand-written gradient passes are audited and dispositioned `clean`/`fixed`/`deferred` with traceable one-line rationale.
- `78-AUDIT.md` exists in the phase directory with the full disposition table; `metric/soft_dtw` is `fixed` (cross-referencing CORR-01).
- Any new localized boundary-seed bug (none expected) is fixed in-phase with a regression test or deferred to backlog with a logged item — never silently omitted.
- The phase SUMMARY (written at phase close) references `78-AUDIT.md`.
</success_criteria>

<output>
Create `.planning/phases/78-gradient-correctness-soft-dtw-fix-backward-pass-audit/78-02-SUMMARY.md` when done.
</output>