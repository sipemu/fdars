---
phase: 27-elastic-multinomial-regression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/elastic_regression/logistic.rs
  - fdars-core/src/elastic_regression/mod.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [REG-03]

estimate:
  tokens: 62000
  raw_tokens: 34000
  tasks: 4
  confidence: high

must_haves:
  truths:
    - "elastic_multinomial(data, y, argvals, ncomp_beta, lambda, max_iter, tol) returns Result<ElasticMultinomialResult, FdarError> for K>=2 classes (SC1, D: OvR)"
    - "predict_elastic_multinomial(fit, new_data, argvals) returns Vec<usize> predicted labels for new curves (SC2)"
    - "On well-separated K-class shape templates the fit recovers labels above a documented accuracy threshold, and the K=2 path agrees with binary elastic_logistic on separable data (SC3)"
    - "Invalid inputs (K<2, non-contiguous labels, y.len()!=n, empty) return FdarError and never panic (SC4)"
    - "Binary elastic_logistic + predict_elastic_logistic + ElasticLogisticResult are unchanged; new symbols re-exported at crate root; clippy --all-targets clean (SC5)"
  artifacts:
    - "fdars-core/src/elastic_regression/logistic.rs — elastic_multinomial, predict_elastic_multinomial, ElasticMultinomialResult"
    - "fdars-core/src/elastic_regression/mod.rs — barrel re-export of the 3 new symbols"
    - "fdars-core/src/lib.rs — crate-root re-export of the 3 new symbols"
  key_links:
    - "elastic_multinomial calls binary elastic_logistic K times with i8-remapped labels (class k -> +1, rest -> -1) — reuse-only, logistic.rs solver UNCHANGED"
    - "train_probabilities = K one-vs-rest sigmoid columns normalized row-wise to sum to 1 (class posterior)"
    - "predict_elastic_multinomial reuses predict_elastic_logistic per class model, row-normalizes, argmax -> label"
---

<objective>
Add elastic multinomial (multi-class, K>=2) logistic regression over SRSF/SRVF space to
`fdars-core/src/elastic_regression/logistic.rs`, implemented one-vs-rest (OvR) by fitting K binary
`elastic_logistic` models UNCHANGED, plus a `predict_elastic_multinomial` companion and an
`ElasticMultinomialResult` struct. Re-exported at the barrel and crate root. Closes REG-03 — the sole
multi-class partial in fdars' otherwise-complete elastic-regression family.

Purpose: Complete the elastic-regression family (REG-03) with maximal reuse of the existing
SRVF/warping/IRLS machinery, no new dependency, no changes to any existing public signature.
Output: `elastic_multinomial`, `predict_elastic_multinomial`, `ElasticMultinomialResult` (+fields),
barrel + crate-root re-export lines, inline `#[cfg(test)]` tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-elastic-multinomial-regression/27-CONTEXT.md
@fdars-core/src/elastic_regression/logistic.rs
@fdars-core/src/elastic_regression/mod.rs
</context>

## Artifacts this phase produces

New public symbols in `fdars-core/src/elastic_regression/logistic.rs`:

- `elastic_multinomial(data: &FdMatrix, y: &[usize], argvals: &[f64], ncomp_beta: usize, lambda: f64, max_iter: usize, tol: f64) -> Result<ElasticMultinomialResult, FdarError>` — `#[must_use]`.
- `predict_elastic_multinomial(fit: &ElasticMultinomialResult, new_data: &FdMatrix, argvals: &[f64]) -> Vec<usize>` — predicted labels.
- `ElasticMultinomialResult` — `#[derive(Debug, Clone, PartialEq)]`, `#[non_exhaustive]`, conditional serde via `#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]`. Fields:
  - `n_classes: usize`
  - `classes: Vec<usize>` (the sorted distinct class labels, 0..K)
  - `class_models: Vec<ElasticLogisticResult>` (one OvR fit per class)
  - `train_probabilities: FdMatrix` (n×K, row-normalized to sum to 1)
  - `predicted_classes: Vec<usize>` (length n)
  - `train_accuracy: f64`

Re-export lines to add:
- Barrel `fdars-core/src/elastic_regression/mod.rs`: extend the existing `pub use logistic::{ ... }` block with `elastic_multinomial, predict_elastic_multinomial, ElasticMultinomialResult`.
- Crate root `fdars-core/src/lib.rs`: extend the existing `pub use elastic_regression::{ ... }` block with `elastic_multinomial, predict_elastic_multinomial, ElasticMultinomialResult`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end OvR "fit K binary models, return result struct" — one path only</name>
  <files>fdars-core/src/elastic_regression/logistic.rs</files>
  <read_first>
    fdars-core/src/elastic_regression/logistic.rs (elastic_logistic signature at :53, ElasticLogisticResult at :14, predict_elastic_logistic at :174),
    fdars-core/src/elastic_regression/mod.rs (barrel + ElasticConfig),
    fdars-core/src/matrix.rs (FdMatrix: shape(), zeros(n,m), indexing (i,j)),
    fdars-core/src/error.rs (FdarError variants InvalidDimension, InvalidParameter)
  </read_first>
  <behavior>
    - Given n=6 curves, K=3 contiguous labels y in {0,1,2}, elastic_multinomial returns Ok.
    - result.n_classes == 3; result.classes == vec![0,1,2]; result.class_models.len() == 3.
    - result.train_probabilities has shape (6, 3); every row sums to 1.0 within 1e-9.
    - result.predicted_classes.len() == 6; result.train_accuracy is in 0.0..=1.0.
  </behavior>
  <action>
    Add ElasticMultinomialResult (fields per "Artifacts this phase produces", with the derives, #[non_exhaustive],
    and conditional-serde attribute matching ElasticLogisticResult's pattern) and elastic_multinomial to
    logistic.rs. Wire the single happy path end-to-end (this is production code, not a stub): derive K as the
    number of distinct labels; for each class k in 0..K build an i8 label vector mapping class k to +1 and all
    other classes to -1, then call the EXISTING binary elastic_logistic(data, &labels_k, argvals, ncomp_beta,
    lambda, max_iter, tol) UNCHANGED and push the ElasticLogisticResult into class_models. Build
    train_probabilities as an n×K FdMatrix whose column k is class_models[k].probabilities, then normalize each
    row to sum to 1 (divide by the row sum; guard a zero row sum by assigning uniform 1/K). Set predicted_classes
    to the per-row argmax of train_probabilities, classes to the sorted distinct labels, n_classes to K, and
    train_accuracy to the fraction of predicted_classes equal to y. Return Ok(ElasticMultinomialResult{..}).
    Mark elastic_multinomial #[must_use] with the same message string used on elastic_logistic. Do NOT edit the
    binary elastic_logistic / ElasticLogisticResult / predict_elastic_logistic. Do NOT reimplement any
    SRVF/warping/IRLS logic. Add an inline #[cfg(test)] test elastic_multinomial_shape_smoke covering the behavior
    above (write the assertions before the implementation so the test drives the shape contract).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib elastic_multinomial_shape_smoke</automated>
  </verify>
  <done>elastic_multinomial fits K OvR models end-to-end and returns a fully-populated ElasticMultinomialResult; elastic_multinomial_shape_smoke passes; binary path untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: predict companion + K-class recovery + K=2 binary agreement</name>
  <files>fdars-core/src/elastic_regression/logistic.rs</files>
  <read_first>
    fdars-core/src/elastic_regression/logistic.rs (elastic_multinomial from Task 1, predict_elastic_logistic at :174),
    fdars-core/src/matrix.rs (FdMatrix construction from column data),
    fdars-core/src/test_helpers.rs (uniform_grid)
  </read_first>
  <behavior>
    - predict_elastic_multinomial on new curves returns Vec<usize> of length n_new with labels drawn from result.classes.
    - Recovery: with K=3 well-separated per-class shape templates (e.g. Gaussian-like bumps centred at distinct locations, small per-curve noise), train_accuracy is high; documented threshold >= 0.8; new-curve predictions from templates recover the correct labels.
    - K=2 agreement: on linearly separable 2-class data, elastic_multinomial predicted_classes agree with binary elastic_logistic predicted_classes on all points (mapping binary 0/1 to the same labels).
  </behavior>
  <action>
    Add predict_elastic_multinomial(fit, new_data, argvals): for each class model call the EXISTING
    predict_elastic_logistic(&fit.class_models[k], new_data, argvals) to get that class's P(Y=1) column, assemble
    an n_new×K probability matrix, normalize each row to sum to 1 (same zero-row guard as Task 1), and return the
    per-row argmax mapped through fit.classes as the predicted label. Document the OvR construction and the
    row-normalization posterior convention in the rustdoc for both elastic_multinomial and
    predict_elastic_multinomial (note the fdasrvf capability parallel and the K=2 -> binary agreement property).
    Add inline #[cfg(test)] tests: elastic_multinomial_recovers_separated_classes (build K=3 templated synthetic
    data on a uniform_grid, assert train_accuracy >= 0.8 and that predict_elastic_multinomial on held-out
    templated curves returns the expected labels) and elastic_multinomial_k2_agrees_with_binary (build separable
    2-class data, fit both elastic_multinomial and elastic_logistic with identical ncomp_beta/lambda/max_iter/tol,
    assert predicted_classes match after mapping). Choose the accuracy threshold and any noise scale so the test is
    deterministic and robust; keep grid size modest for runtime.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib elastic_multinomial_recovers_separated_classes elastic_multinomial_k2_agrees_with_binary</automated>
  </verify>
  <done>predict_elastic_multinomial returns correct-shape labels; recovery test passes above documented threshold; K=2 path agrees with binary elastic_logistic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Input-guard validation (no panic on malformed input)</name>
  <files>fdars-core/src/elastic_regression/logistic.rs</files>
  <read_first>
    fdars-core/src/elastic_regression/logistic.rs (elastic_multinomial entry from Task 1),
    fdars-core/src/error.rs (FdarError::InvalidDimension { parameter, expected, actual }, FdarError::InvalidParameter { parameter, message })
  </read_first>
  <behavior>
    - y.len() != data n-rows -> Err(FdarError), no panic.
    - Fewer than 2 distinct classes (all labels equal / K<2) -> Err(FdarError), no panic.
    - Non-contiguous labels (distinct set is not exactly 0..K, e.g. {0,2}) -> Err(FdarError), no panic.
    - Empty input (n==0) -> Err(FdarError), no panic.
  </behavior>
  <action>
    At the top of elastic_multinomial, before any fitting, validate inputs and return the appropriate FdarError
    (never panic). Read (n, m) from data.shape(). Reject n==0 or y.len() != n with FdarError::InvalidDimension
    (parameter "data/y", expected "n >= 1, y.len() == n", actual describing n and y.len()). Compute the distinct
    sorted label set; reject fewer than 2 distinct classes and reject a distinct set that is not exactly the
    contiguous range 0..K with FdarError::InvalidParameter (parameter "y", message describing the requirement of
    at least 2 classes forming a contiguous 0..K label set). Do these guards before delegating to any binary fit
    so no downstream panic can occur. Add inline #[cfg(test)] tests: elastic_multinomial_rejects_count_mismatch,
    elastic_multinomial_rejects_single_class, elastic_multinomial_rejects_noncontiguous_labels,
    elastic_multinomial_rejects_empty — each asserting the call returns Err (matches!(result, Err(_))).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib elastic_multinomial_rejects</automated>
  </verify>
  <done>All four malformed-input cases return Err(FdarError) with no panic; the four reject tests pass.</done>
</task>

<task type="auto">
  <name>Task 4: Barrel + crate-root re-exports and full clippy/suite gate</name>
  <files>fdars-core/src/elastic_regression/mod.rs, fdars-core/src/lib.rs</files>
  <read_first>
    fdars-core/src/elastic_regression/mod.rs (pub use logistic::{ ... } block near :21),
    fdars-core/src/lib.rs (pub use elastic_regression::{ ... } block near :342)
  </read_first>
  <action>
    Extend the existing `pub use logistic::{ ... }` block in elastic_regression/mod.rs to also export
    elastic_multinomial, predict_elastic_multinomial, ElasticMultinomialResult (additive to the existing list —
    do not remove or reorder existing symbols). Extend the existing `pub use elastic_regression::{ ... }` block in
    src/lib.rs to also export the same three symbols (additive). Then run the full clippy gate and lib suite;
    resolve any warnings introduced by the new code (missing docs, needless clones, etc.). Confirm the binary
    elastic_logistic / predict_elastic_logistic / ElasticLogisticResult signatures are byte-for-byte unchanged.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib elastic</automated>
  </verify>
  <done>Three new symbols re-exported at barrel and crate root; clippy --all-targets exits 0; elastic lib tests green; existing elastic_logistic signature unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → elastic_multinomial | Untrusted `FdMatrix` data + `&[usize]` labels + numeric hyperparameters cross into the fit function. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Denial of Service | elastic_multinomial input handling | medium | mitigate | Entry-point guards (Task 3): reject n==0, y.len()!=n, K<2, and non-contiguous 0..K labels with FdarError before any fit — no panic, no unbounded work. |
| T-27-02 | Tampering | numeric integrity of probabilities | low | mitigate | Row-normalize OvR sigmoid outputs with an explicit zero-row-sum guard (uniform 1/K), so train_probabilities rows always sum to 1 and predicted_classes/argmax are well-defined. |
| T-27-03 | Tampering | supply chain (deps) | low | accept | No new crate dependency introduced (reuse-only extension); nothing to audit. |
</threat_model>

<verification>
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib elastic_multinomial` — all new module tests green.
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel` — full suite green (no regression to binary elastic path).
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings` — clean.
</verification>

<success_criteria>
- SC1: `elastic_multinomial(...)` returns `Result<ElasticMultinomialResult, FdarError>` for K>=2 and is crate-root re-exported.
- SC2: `predict_elastic_multinomial` returns predicted labels for new curves.
- SC3: K-class recovery above documented accuracy threshold; K=2 path agrees with binary `elastic_logistic`.
- SC4: reuses SRVF/warping (binary fit called unchanged); malformed inputs return `FdarError`, no panic.
- SC5: binary `elastic_logistic` signature unchanged; full suite + clippy `--all-targets` green; no new dependency.
</success_criteria>

<output>
Create `.planning/phases/27-elastic-multinomial-regression/27-01-SUMMARY.md` when done.
</output>
