---
phase: 33-model-based-density-functional-clustering
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/test_helpers.rs
  - fdars-core/src/gmm/subspace.rs
  - fdars-core/src/gmm/mod.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [CLUS-01]
estimate:
  tokens: 78000
  raw_tokens: 39000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "funhddC_cluster recovers two well-separated functional groups with ARI >= 0.90 (up to label permutation)"
    - "funhddC_cluster returns finite BIC/ICL and log-likelihood on valid input"
    - "funhddC_cluster returns FdarError (not panic) on empty data, k>n, k=0, d_k>=m, and mismatched argvals"
    - "adjusted_rand_index test helper returns 1.0 for identical labelings and ~0 for random labelings"
    - "fdars-core builds and existing tests stay green (strictly additive, no existing signature changed)"
  artifacts:
    - "fdars-core/src/gmm/subspace.rs (funHDDC: FunHddcConfig, FunHddcResult, funhddC_cluster + inline tests)"
    - "adjusted_rand_index in fdars-core/src/test_helpers.rs (pub(crate), #[cfg(test)])"
  key_links:
    - "gmm/subspace.rs consumes pub(super) e_step/compute_bic/compute_icl/hard_assignments/resp_to_membership from gmm/em.rs and kmeans_init_assignments/init_params_from_assignments from gmm/init.rs"
    - "gmm/mod.rs re-exports funhddC_cluster + FunHddcConfig + FunHddcResult; lib.rs gmm block re-exports them at crate root"
---

<objective>
Deliver the Phase 33 tracer: one production-quality functional clusterer (funHDDC) wired end-to-end
through the existing gmm/ EM machinery — fit + hard/soft assignments + BIC/ICL + inline recovery and
invalid-input tests — and the shared test-only `adjusted_rand_index` recovery metric that every
clusterer in this phase depends on. This proves the "reuse gmm/em.rs E-step + init.rs k-means++,
swap in a per-group SVD subspace M-step, re-export at crate root" architecture end-to-end before the
remaining four clusterers expand out from it in Waves 2-3.

Implements CLUS-01 (funHDDC portion) — the per-group subspace covariance model extending gmm/.

Purpose: De-risk the whole phase on the agent's best early-context tokens — if the gmm/ reuse path
or the subspace M-step is an architectural dead-end, catch it here after one plan, not after five.
Output: `adjusted_rand_index` helper; `gmm/subspace.rs` (funHDDC); gmm/mod.rs + lib.rs re-exports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-model-based-density-functional-clustering/33-RESEARCH.md
@.planning/phases/33-model-based-density-functional-clustering/33-PATTERNS.md

@fdars-core/src/gmm/mod.rs
@fdars-core/src/gmm/em.rs
@fdars-core/src/gmm/init.rs
@fdars-core/src/gmm/covariance.rs
@fdars-core/src/gmm/cluster.rs
@fdars-core/src/regression.rs
@fdars-core/src/test_helpers.rs
@fdars-core/src/matrix.rs
</context>

<execution_notes>
BUILD NOTE: export `TMPDIR=/home/simonm/.cache/fdars-bench-tmp` before ANY cargo command (doctests
link in a small /tmp tmpfs that fills and yields bogus "No space left").

ANTI-STALL: run ONLY lib-scoped filtered tests — `cargo test -p fdars-core --features linalg,parallel
--lib <filter>`. Do NOT run `--all-targets` or an unfiltered `cargo test`/`cargo build` (long fdars
builds trip the executor stream watchdog). The orchestrator runs the full gate. Commit early/often
with `git commit --no-verify` after each task.

VISIBILITY (verified this session): `gmm/em.rs` and `gmm/init.rs` helpers are `pub(super)`; because
`gmm/subspace.rs` is a sibling module under `gmm/`, `pub(super)` items ARE accessible from it. Import
via `use super::em::{e_step, compute_bic, compute_icl, hard_assignments, resp_to_membership};` and
`use super::init::{kmeans_init_assignments, init_params_from_assignments};`. `crate::linalg` is
pub(crate) and accessible. If a needed gmm helper is more restrictive than `pub(super)`, prefer
additive `pub(crate)` widening (non-breaking) and note it in the SUMMARY — never copy-paste large
helpers and never narrow an existing item.

DOCTEST RULE (learned Phase 31/32): any rustdoc `///` doctest that constructs a `#[non_exhaustive]`
config MUST use `let mut c = FunHddcConfig::default(); c.k = 2;` field-assignment form (struct-literal
with `..Default::default()` is E0639 from outside the crate in a doctest). But INLINE `#[cfg(test)]`
tests inside this crate MUST use struct-literal `FunHddcConfig { k: 2, ..Default::default() }` to
satisfy clippy `field_reassign_with_default`. Apply BOTH rules in their respective places.
</execution_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add adjusted_rand_index test helper (Wave 0 gap)</name>
  <files>fdars-core/src/test_helpers.rs</files>
  <behavior>
    - adjusted_rand_index(a, b) == 1.0 (within 1e-9) when a == b (identical labelings)
    - adjusted_rand_index(a, b) == 1.0 when b is a label-permutation of a (e.g. a=[0,0,1,1], b=[1,1,0,0])
    - adjusted_rand_index near 0 (|ari| < 0.3) for a fixed random-vs-structured labeling pair
    - Panics (debug assert) or asserts on differing lengths — a and b must be equal length
  </behavior>
  <action>Add a `#[cfg(test)] pub(crate) fn adjusted_rand_index(a: &[usize], b: &[usize]) -> f64`
    to test_helpers.rs implementing the Hubert and Arabie (1985) ARI: build the k_a x k_b contingency
    table n_ij (relabel each input's distinct labels to dense 0..k indices first), compute
    sum_ij C(n_ij,2), a_i = row sums, b_j = col sums, then
    ARI = (S - E) / (0.5*(sum C(a_i,2) + sum C(b_j,2)) - E) where S = sum C(n_ij,2),
    E = sum C(a_i,2) * sum C(b_j,2) / C(n,2). Handle the degenerate case where the denominator is 0
    (all points in one cluster in both) by returning 1.0. Use a local `choose2(x) = x*(x-1)/2` on
    f64 or u64. The whole test_helpers module is already `#[cfg(test)]`-gated at its lib.rs decl, so
    the fn is only compiled under test; keep it `pub(crate)` so every module's `#[cfg(test)] mod tests`
    can call it via `crate::test_helpers::adjusted_rand_index`.</action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib adjusted_rand</automated>
  </verify>
  <done>adjusted_rand_index compiles under test and its unit tests (identical=1.0, permutation=1.0, random~0) pass.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: funHDDC per-group subspace EM in new gmm/subspace.rs (end-to-end tracer)</name>
  <files>fdars-core/src/gmm/subspace.rs</files>
  <behavior>
    - test_funhddC_recovery: two vertically-separated sin-wave groups → cluster labels give ARI >= 0.90 vs ground truth
    - test_funhddC_bic_finite: on the recovery data, result.bic, result.icl, result.log_likelihood are all finite
    - test_funhddC_deterministic: same config (same seed) → identical cluster assignment across two runs
    - test_funhddC_invalid: empty data → Err; k=0 → Err; k>n → Err; d_k>=m → Err; argvals.len()!=m → Err (each is_err(), no panic)
  </behavior>
  <action>Create gmm/subspace.rs implementing the simplified funHDDC AkBk model per D-locked
    decisions and 33-RESEARCH.md "Algorithm Formulations §1". Public surface exactly:
    `FunHddcConfig { k, d_k, max_iter, tol, n_init, seed, ncomp_init }` with Default (k=2, d_k=2,
    max_iter=100, tol=1e-6, n_init=3, seed=42, ncomp_init=10);
    `FunHddcResult { cluster, membership, subspaces, within_vars, noise_vars, means, weights,
    log_likelihood, bic, icl, iterations, converged, k }`;
    `funhddC_cluster(data: &FdMatrix, argvals: &[f64], config: &FunHddcConfig) -> Result<FunHddcResult, FdarError>`.
    Config derives Debug/Clone/PartialEq + serde cfg_attr + #[non_exhaustive]; Result derives
    Debug/Clone + #[non_exhaustive]; funhddC_cluster is #[must_use]. Validate inputs first
    (n==0||m==0 -> InvalidDimension; k==0||k>n -> InvalidParameter; d_k==0||d_k>=m -> InvalidParameter;
    argvals.len()!=m -> InvalidDimension) using the exact error-construction pattern from gmm/em.rs.
    Init: for each of n_init restarts (seed = config.seed.wrapping_add(init as u64 * 1000)), build
    init features via global fdata_to_pc_1d(data, ncomp_init, argvals)? scores, run
    kmeans_init_assignments (super::init) to get warm-start hard labels. EM loop to max_iter:
    M-step per group k — gather member rows, subtract group mean, thin-SVD the centered member slice
    (nalgebra SVD, same conversion pattern as fdata_to_pc_1d) to get leading eigenvectors U_k
    (m x d_k_eff) and within-subspace variances a_k; d_k_eff = min(d_k, n_k, m) — USE THE ACTUAL
    returned subspace dimension (rotation columns), NOT config.d_k (fdata_to_pc_1d clamps
    ncomp.min(n).min(m); Pitfall 5). noise var b_k = (total weighted complement variance)/(m - d_k_eff),
    floored via super::covariance::data_scaled_reg to avoid b_k<=0. Empty-cluster guard: if effective
    weight < 1e-15, fall back to identity-style component (mirror gmm/em.rs identity_cov handling) so
    the M-step never divides by zero (Pitfall 1). E-step: compute per-observation per-group log-density
    with the subspace-plus-isotropic-noise Gaussian formula from 33-RESEARCH.md (project diff onto U_k,
    within-subspace term over a_k, complement squared norm ||diff||^2 - ||U_k^T diff||^2 over b_k, plus
    -0.5*m*ln(2*pi) constant), then normalize responsibilities via log-sum-exp — reuse
    super::em::e_step if its signature accepts a caller-supplied per-component log-density; otherwise
    inline a ~15-line log-sum-exp normalizer (do NOT change e_step's signature). Convergence: stop when
    log-likelihood delta < tol. Keep the best-of-n_init by log-likelihood. Assemble final result:
    hard_assignments + resp_to_membership (super::em); param count for BIC =
    k*(m*d_k_eff - d_k_eff*(d_k_eff-1)/2) + k*d_k_eff + k + (k-1); bic = compute_bic, icl = compute_icl
    (super::em). Module doc comment (`//!`) MUST document the deliberate divergence from the R funHDDC
    6-model akjbkqkdk family — this is the single AkBk model only. Add `#[cfg(test)] mod tests` using
    struct-literal config form and `crate::test_helpers::{uniform_grid, adjusted_rand_index}`, with a
    local two_separated_clusters(n_per, m) generator mirroring clustering.rs generate_two_clusters
    (cluster 0 = sin, cluster 1 = sin + 5.0 vertical offset). Do NOT put a runnable rustdoc doctest on
    funhddC_cluster unless it uses the field-assignment config form; a `no_run`/text example is safer.</action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib gmm::subspace</automated>
  </verify>
  <done>gmm/subspace.rs compiles; recovery (ARI>=0.90), BIC-finite, deterministic, and all invalid-input tests pass; module doc documents the AkBk-only divergence.</done>
</task>

<task type="auto">
  <name>Task 3: Wire funHDDC into gmm/mod.rs and crate-root lib.rs</name>
  <files>fdars-core/src/gmm/mod.rs, fdars-core/src/lib.rs</files>
  <action>In gmm/mod.rs add `pub mod subspace;` alongside the existing `pub mod cluster/covariance/em/init;`
    declarations, and add `pub use subspace::{funhddC_cluster, FunHddcConfig, FunHddcResult};` alongside
    the existing `pub use cluster::{...}` / `pub use em::gmm_em;` block. In lib.rs, extend the existing
    `pub use gmm::{ ... };` block (the one currently re-exporting gmm_cluster, gmm_em, CovType,
    GmmClusterConfig, GmmClusterResult, GmmResult) to also re-export `funhddC_cluster, FunHddcConfig,
    FunHddcResult`. Use scoped Edit on both files — do NOT rewrite either file. Strictly additive: no
    existing re-export line removed or reordered.</action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib gmm::subspace</automated>
  </verify>
  <done>`fdars_core::funhddC_cluster`, `fdars_core::FunHddcConfig`, `fdars_core::FunHddcResult` resolve at crate root; gmm/subspace tests still pass; no existing gmm re-export changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library caller → funhddC_cluster | Untrusted numeric inputs (data shape, argvals length, k, d_k) cross into the algorithm |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-01 | Denial of Service | funhddC_cluster input validation | low | mitigate | Validate n,m,k,d_k,argvals.len() at entry → FdarError (no panic, no unbounded alloc); covered by test_funhddC_invalid |
| T-33-02 | Tampering | numerical stability (b_k<=0, empty cluster, SVD failure) | low | mitigate | data_scaled_reg noise floor + empty-cluster identity fallback + Result on SVD failure; no NaN propagation into public result |
| T-33-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new crate dependency added this phase (milestone constraint) — no install surface |
</threat_model>

<verification>
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib adjusted_rand gmm::subspace` all green.
- funHDDC recovery ARI >= 0.90 on well-separated synthetic data; BIC/ICL/log-likelihood finite; deterministic under fixed seed; all invalid-input paths return Err (no panic).
- Crate-root re-exports resolve. No existing public signature in gmm/, clustering.rs, distance.rs, alignment/, or regression.rs changed.
- (Orchestrator full gate) full suite incl. doctests + `cargo clippy --all-targets --features linalg,parallel -- -D warnings` green.
</verification>

<success_criteria>
- adjusted_rand_index helper present and unit-tested (identical=1.0, permutation-invariant, random~0).
- funHDDC (FunHddcConfig/FunHddcResult/funhddC_cluster) implemented in gmm/subspace.rs, re-exported at crate root, with passing recovery + BIC + deterministic + invalid-input inline tests.
- Strictly additive/non-breaking; no new crate dependency; module doc records the AkBk-only divergence from R funHDDC.
</success_criteria>

<output>
Create `.planning/phases/33-model-based-density-functional-clustering/33-00-SUMMARY.md` when done.
</output>
