---
phase: 33-model-based-density-functional-clustering
plan: 02
type: execute
wave: 2
depends_on: ["33-00"]
files_modified:
  - fdars-core/src/clustering_advanced.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [CLUS-01]
estimate:
  tokens: 72000
  raw_tokens: 36000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "dbscan_fd forms the correct number of clusters on tight well-separated groups and flags injected outlier curves as None (noise)"
    - "dbscan_fd returns FdarError on eps<=0, min_points==0, empty data, and mismatched argvals"
    - "kcfc_cluster recovers two well-separated functional groups with ARI >= 0.90"
    - "kcfc_cluster returns FdarError on k=0, k>n, empty data, mismatched argvals"
    - "clustering_advanced module builds and existing tests stay green (strictly additive)"
  artifacts:
    - "fdars-core/src/clustering_advanced.rs (DbscanConfig/DbscanResult/dbscan_fd + KcfcConfig/KcfcResult/kcfc_cluster + inline tests)"
  key_links:
    - "dbscan_fd consumes distance::l2_distance_matrix; kcfc_cluster consumes regression::fdata_to_pc_1d + FpcaResult::project/reconstruct + helpers::simpsons_weights"
    - "lib.rs declares `pub mod clustering_advanced;` and re-exports dbscan_fd/kcfc_cluster + their config/result structs at crate root"
    - "clustering_advanced::tests use crate::test_helpers::adjusted_rand_index from plan 33-00"
---

<objective>
Expand from the funHDDC tracer to the two clusterers that need no gmm EM machinery: DBSCAN (density,
over the existing functional L2 distance matrix, with type-safe None-is-noise labels) and kCFC
(per-cluster-FPCA reassignment loop). This plan creates the shared `clustering_advanced.rs` module
that funFEM and align-and-cluster (plan 33-03) will append to, and wires the crate-root re-exports.

Implements CLUS-01 (DBSCAN + kCFC portions).

Purpose: Ship two independent, fully-tested clusterers built purely on already-proven primitives
(distance.rs, regression.rs FPCA), establishing the clustering_advanced.rs file + lib.rs wiring that
plan 33-03 extends.
Output: clustering_advanced.rs (DBSCAN + kCFC); lib.rs module decl + clustering-block re-export.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-model-based-density-functional-clustering/33-RESEARCH.md
@.planning/phases/33-model-based-density-functional-clustering/33-PATTERNS.md

@fdars-core/src/clustering.rs
@fdars-core/src/distance.rs
@fdars-core/src/regression.rs
@fdars-core/src/helpers.rs
@fdars-core/src/matrix.rs
@fdars-core/src/test_helpers.rs
</context>

<execution_notes>
BUILD NOTE: export `TMPDIR=/home/simonm/.cache/fdars-bench-tmp` before ANY cargo command.

ANTI-STALL: run ONLY lib-scoped filtered tests — `cargo test -p fdars-core --features linalg,parallel
--lib <filter>`. Never `--all-targets` or unfiltered. Commit early/often with `git commit --no-verify`.

DEPENDS ON 33-00: this plan reuses `crate::test_helpers::adjusted_rand_index` (added in plan 33-00)
and edits lib.rs (which 33-00 also edits) — hence Wave 2, strictly after 33-00.

DOCTEST RULE (learned Phase 31/32): rustdoc `///` doctests constructing a `#[non_exhaustive]` config
MUST use field-assignment form (`let mut c = DbscanConfig::default(); c.eps = 0.5;`); INLINE
`#[cfg(test)]` tests MUST use struct-literal form (`DbscanConfig { eps: 0.5, ..Default::default() }`)
to satisfy clippy field_reassign_with_default. Apply both in their respective places.

VERIFIED SIGNATURES (this session):
- `distance::l2_distance_matrix(data: &FdMatrix, argvals: &[f64]) -> FdMatrix` (n x n; #[must_use]).
- `regression::fdata_to_pc_1d(data: &FdMatrix, ncomp: usize, argvals: &[f64]) -> Result<FpcaResult, FdarError>`;
  clamps `ncomp = ncomp.min(n).min(m)` — use the ACTUAL returned dimension, not config.ncomp.
- `FpcaResult { singular_values, rotation, scores, mean, centered }`;
  `FpcaResult::project(&self, data: &FdMatrix) -> Result<FdMatrix, FdarError>` (data must be n_rows x m);
  `FpcaResult::reconstruct(&self, scores: &FdMatrix, ncomp: usize) -> Result<FdMatrix, FdarError>`.
- `helpers::simpsons_weights(argvals)` for L2 reconstruction-error integration weights.
- FdMatrix is column-major; use `data.row(i)` for row access and `data.to_row_major()` for a flat
  row-major buffer (Pitfall 7). Build a single-curve FdMatrix (1 x m) before FpcaResult::project (Pitfall 4).
</execution_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create clustering_advanced.rs with DBSCAN over functional L2 distances</name>
  <files>fdars-core/src/clustering_advanced.rs</files>
  <behavior>
    - test_dbscan_core_points: two tight well-separated groups (5 curves each), suitable eps → exactly 2 clusters, 0 noise
    - test_dbscan_noise_flagging: 2 tight groups + 2 isolated far outliers → the 2 outliers get None; n_noise==2
    - test_dbscan_zero_eps handled by validation: eps==0.0 → Err (documented as invalid; a tiny positive eps yields all-noise)
    - test_dbscan_invalid: eps<=0 → Err; min_points==0 → Err; empty data → Err; argvals.len()!=m → Err (all is_err, no panic)
  </behavior>
  <action>Create clustering_advanced.rs with a `//!` module doc summarizing the four advanced
    clusterers this file will hold (DBSCAN + kCFC here; funFEM + align-and-cluster added in plan 33-03).
    Implement DBSCAN per 33-RESEARCH.md "Algorithm Formulations §3". Public surface:
    `DbscanConfig { eps: f64, min_points: usize }` with Default (eps=0.5, min_points=3);
    `DbscanResult { cluster: Vec<Option<usize>>, n_clusters: usize, n_noise: usize, distances: FdMatrix }`;
    `dbscan_fd(data: &FdMatrix, argvals: &[f64], config: &DbscanConfig) -> Result<DbscanResult, FdarError>`.
    Config derives Debug/Clone/PartialEq + serde cfg_attr + #[non_exhaustive]; Result derives Debug/Clone
    + #[non_exhaustive]; dbscan_fd is #[must_use]. Validate first: n==0||m==0 -> InvalidDimension;
    argvals.len()!=m -> InvalidDimension; eps<=0.0 -> InvalidParameter; min_points==0 -> InvalidParameter.
    Compute `let dist = l2_distance_matrix(data, argvals);` then run standard DBSCAN: label vec of
    Option<usize> init None; iterate points, for each unvisited compute the eps-neighborhood
    { j : dist[(i,j)] <= eps, j != i }; if |neighborhood| + 1 < min_points (core-point rule, counting
    self) leave as noise; else start a new cluster id and BFS/DFS-expand, absorbing density-reachable
    core points and border points. Track visited separately from assigned so border points reachable
    from multiple cores land in the first cluster (standard DBSCAN determinism). Set n_clusters =
    number of distinct Some ids; n_noise = count of None. In the rustdoc, document that `eps` is in
    functional L2 distance units (same units as l2_distance_matrix, Simpson-integrated) and recommend
    choosing eps as a fraction of the dataset's median pairwise L2 distance (Pitfall 2). Add
    `#[cfg(test)] mod tests` (struct-literal config form) with local generators: two_tight_clusters(n_per, m)
    and clusters_with_noise(n_per, m) (2 tight sin groups + 2 far constant-offset outliers), plus the
    invalid-input tests. DBSCAN needs no ARI (assert cluster count + noise count directly).</action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib clustering_advanced::tests::dbscan</automated>
  </verify>
  <done>dbscan_fd forms 2 clusters on well-separated data, flags injected outliers as None (n_noise==2), and returns Err on eps<=0 / min_points==0 / empty / mismatched argvals; rustdoc documents eps units.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: kCFC per-cluster FPCA reassignment loop in clustering_advanced.rs</name>
  <files>fdars-core/src/clustering_advanced.rs</files>
  <behavior>
    - test_kcfc_recovery: two well-separated groups → ARI >= 0.90 vs ground truth
    - test_kcfc_errors: reconstruction error for a curve against its true cluster's FPCA is smaller than against the other cluster's FPCA
    - test_kcfc_deterministic: same seed → identical assignment across two runs
    - test_kcfc_invalid: k=0 → Err; k>n → Err; empty data → Err; argvals.len()!=m → Err
  </behavior>
  <action>Append kCFC to clustering_advanced.rs per 33-RESEARCH.md "Algorithm Formulations §4".
    Public surface: `KcfcConfig { k: usize, ncomp: usize, max_iter: usize, seed: u64 }` with Default
    (k=2, ncomp=3, max_iter=50, seed=42);
    `KcfcResult { cluster: Vec<usize>, fpca_models: Vec<Option<FpcaResult>>, reconstruction_errors: FdMatrix, iterations: usize, converged: bool }`;
    `kcfc_cluster(data: &FdMatrix, argvals: &[f64], config: &KcfcConfig) -> Result<KcfcResult, FdarError>`.
    Config derives Debug/Clone/PartialEq + serde cfg_attr + #[non_exhaustive]; Result derives Debug/Clone
    + #[non_exhaustive] (note FpcaResult must be serde-compatible only if you gate it; leave Result
    ungated since it holds FpcaResult/FdMatrix); kcfc_cluster is #[must_use]. Validate: n==0||m==0 ->
    InvalidDimension; k==0||k>n -> InvalidParameter; argvals.len()!=m -> InvalidDimension. Init k-means++
    -style hard labels using a seeded StdRng (seed = config.seed) over the raw row curves (mirror the
    k-means++ selection pattern; per-init seeding `StdRng::seed_from_u64(config.seed)`). Reassignment
    loop to max_iter: for each cluster, gather member rows into a (n_k x m) FdMatrix (via to_row_major +
    row gather), fit `fdata_to_pc_1d(data_k, config.ncomp, argvals)?` — use the ACTUAL returned ncomp
    (rotation columns), which is clamped to min(n_k, m) (Pitfall 5). Empty/degenerate cluster: keep the
    previous iteration's FPCA model (or None on the first iteration) and skip its update (Pitfall 1).
    For every curve i and every non-empty cluster k, wrap the curve as a 1 x m FdMatrix, project via
    fpca_k.project(&curve_mat)?, reconstruct via fpca_k.reconstruct(&scores, ncomp_eff)?, and compute
    the L2 reconstruction error ||x_i - recon||^2 using helpers::simpsons_weights (Pitfall 4). Fill the
    n x k reconstruction_errors matrix. Reassign cluster[i] = argmin_k err[i,k]. Converge when no label
    changes; record iterations + converged. Add `#[cfg(test)] mod tests` entries (struct-literal config
    form) using `crate::test_helpers::{uniform_grid, adjusted_rand_index}` and a local
    two_separated_clusters(n_per, m) generator.</action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib clustering_advanced::tests::kcfc</automated>
  </verify>
  <done>kcfc_cluster recovers well-separated groups (ARI>=0.90), true-cluster reconstruction error is smaller, deterministic under fixed seed, and all invalid-input paths return Err.</done>
</task>

<task type="auto">
  <name>Task 3: Declare clustering_advanced module and re-export DBSCAN + kCFC at crate root</name>
  <files>fdars-core/src/lib.rs</files>
  <action>In lib.rs add `pub mod clustering_advanced;` alongside the existing `pub mod clustering;`
    module declaration (keep alphabetical/grouped placement near clustering). Add a new re-export block
    (mirroring the existing `pub use clustering::{...}` block) re-exporting from clustering_advanced:
    `dbscan_fd, kcfc_cluster, DbscanConfig, DbscanResult, KcfcConfig, KcfcResult`. Use scoped Edit — do
    NOT rewrite lib.rs and do NOT touch the existing clustering / gmm re-export blocks (gmm block was
    already extended by plan 33-00). Strictly additive.</action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib clustering_advanced</automated>
  </verify>
  <done>`fdars_core::dbscan_fd`, `fdars_core::kcfc_cluster`, and their config/result structs resolve at crate root; all clustering_advanced tests pass; no existing re-export changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library caller → dbscan_fd / kcfc_cluster | Untrusted numeric inputs (data shape, argvals length, eps, min_points, k, ncomp) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-03 | Denial of Service | dbscan_fd input validation | low | mitigate | Validate n,m,argvals.len(),eps>0,min_points>0 at entry → FdarError (no panic); covered by test_dbscan_invalid |
| T-33-04 | Denial of Service | kcfc_cluster input validation + empty-cluster degeneracy | low | mitigate | Validate n,m,k,argvals; empty-cluster keeps prior FPCA (no divide-by-zero, no NaN in errors); covered by test_kcfc_invalid |
| T-33-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new crate dependency this phase (milestone constraint) — no install surface |
</threat_model>

<verification>
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib clustering_advanced` green.
- DBSCAN: correct cluster count + None-flagged noise; invalid inputs → Err. kCFC: ARI>=0.90 recovery, error-ordering, deterministic, invalid inputs → Err.
- Crate-root re-exports resolve; no existing public signature changed anywhere.
- (Orchestrator full gate) full suite incl. doctests + `cargo clippy --all-targets --features linalg,parallel -- -D warnings` green.
</verification>

<success_criteria>
- DBSCAN (DbscanConfig/DbscanResult/dbscan_fd) and kCFC (KcfcConfig/KcfcResult/kcfc_cluster) implemented in clustering_advanced.rs, re-exported at crate root, with passing inline tests.
- None-is-noise DBSCAN labeling; kCFC ARI>=0.90 recovery.
- Strictly additive/non-breaking; no new crate dependency.
</success_criteria>

<output>
Create `.planning/phases/33-model-based-density-functional-clustering/33-02-SUMMARY.md` when done.
</output>
