---
phase: 32-flexible-mixed-effects-regression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/famm.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [REG-05]
user_setup: []

estimate:
  tokens: 92000
  raw_tokens: 46000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "dense_flmm returns fixed-effect functions, random effects, variance components, and fitted curves from a column-major FdMatrix (REG-05)"
    - "dense_flmm on synthetic grouped data recovers the fixed-effect function (correlation with truth > 0.8) and positive random-intercept variance"
    - "multi_famm returns one DenseFlmmResult per response dimension plus stacked fitted/residuals"
    - "fast_fmm returns per-gridpoint smoothed fixed-effect functions with Wald t-statistics and p-values in [0,1]"
    - "empty data / mismatched subject_ids / mismatched grid all return FdarError, never panic"
    - "existing fmm / fmm_predict / fmm_test_fixed public signatures are unchanged and their tests still pass"
  artifacts:
    - "fdars-core/src/famm.rs (extended): dense_flmm, DenseFlmmConfig, DenseFlmmResult, multi_famm, MultiFammConfig, MultiFammResult, fast_fmm, FastFmmConfig, FastFmmResult + inline tests"
    - "fdars-core/src/lib.rs (extended famm re-export line)"
  key_links:
    - "dense_flmm -> fdata_to_pc_1d -> fit_scalar_mixed_model (per component) -> recover_beta_functions / recover_random_effects"
    - "six private famm.rs helpers promoted to pub(crate) for sibling-module reuse in Plan 02"
    - "crate-root re-exports so callers can `use fdars_core::{dense_flmm, ...}`"
---

<objective>
Extend `fdars-core/src/famm.rs` with three functional mixed-effects estimators — `dense_flmm` (dense FLMM over FPC scores, the tracer), `multi_famm` (multivariate/stacked variant), and `fast_fmm` (massively-univariate per-gridpoint inference) — each `Result<_, FdarError>`-returning with config + result structs, inline `#[cfg(test)]` tests, and crate-root re-exports. Also promote six private helpers to `pub(crate)` (non-breaking) so Plan 02's sibling `fof_regression.rs` can reuse them (REG-05).

Purpose: Take `famm.rs` beyond fixed-effect testing to full random-effects estimation, matching the capability of R's `denseFLMM` / `multifamm` / `fastFMM` per the milestone's reuse-first, additive/non-breaking mandate.
Output: Extended `famm.rs` (three new public fns + six configs/results) and one extended `pub use famm::{...}` line in `lib.rs`.

BUILD NOTE (executor): set `TMPDIR=/home/simonm/.cache/fdars-bench-tmp` before every cargo command — the system `/tmp` exhausts and produces bogus "No space left" link failures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-flexible-mixed-effects-regression/32-RESEARCH.md
@.planning/phases/32-flexible-mixed-effects-regression/32-PATTERNS.md

# Primary file to extend — read fully; it holds the REML-EM machinery being reused.
@fdars-core/src/famm.rs
# FPC decomposition entry + FpcaResult (rotation, mean, scores, singular_values, project).
@fdars-core/src/regression.rs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: denseFLMM end-to-end tracer + pub(crate) promotions + re-export</name>
  <files>fdars-core/src/famm.rs, fdars-core/src/lib.rs</files>
  <precondition>`cargo test -p fdars-core --features linalg,parallel famm` passes on the untouched tree (baseline green) before edits begin.</precondition>
  <behavior>
    Write these inline tests in the `#[cfg(test)] mod tests` block FIRST (they must fail before implementation):
    - test_dense_flmm_basic: `dense_flmm` on `generate_fmm_data(10, 3, 50)` with 1 covariate returns mean_function len 50, beta_functions 1x50, random_effects 10x50, fitted 30x50, residuals 30x50, n_subjects 10, sigma2_u len == ncomp.
    - test_dense_flmm_invariant: fitted[(i,t)] + residuals[(i,t)] == data within 1e-8 for all i,t.
    - test_dense_flmm_fixed_recovery: on grouped synthetic data with a known covariate effect, pearson correlation between recovered beta_functions row 0 and the true beta grid > 0.8 (helper: reuse the `generate_fmm_data` signal shape; add a local pearson helper).
    - test_dense_flmm_variance_positive: sigma2_eps >= 0.0, every sigma2_u entry >= 0.0, and at least one sigma2_u > 0 for grouped data; sigma2_slope all == 0.0 when random_slopes = false.
    - test_dense_flmm_empty_error: empty FdMatrix::zeros(0,0) -> FdarError::InvalidDimension { parameter: "data", .. }.
    - test_dense_flmm_ids_mismatch: subject_ids of wrong length -> FdarError::InvalidDimension { parameter: "subject_ids", .. }.
    - test_dense_flmm_converged: on well-conditioned data, result.converged == true and result.n_iter <= config.max_iter.
    - test_dense_flmm_reexport: `use crate::dense_flmm;` reference compiles (smoke).
  </behavior>
  <action>
    Add `DenseFlmmConfig` (fields per RESEARCH API Surface: ncomp=3, max_iter=50, tol=1e-10, random_slopes=false) with `#[derive(Debug, Clone, PartialEq)]`, `#[cfg_attr(feature = "serde", derive(...))]`, NO `#[non_exhaustive]`, and a `Default` impl. Add `DenseFlmmResult` (fields per RESEARCH API Surface: mean_function, beta_functions, random_effects, fitted, residuals, random_variance, sigma2_eps, sigma2_u, sigma2_slope, ncomp, n_subjects, eigenvalues, n_iter, converged) with `#[derive(Debug, Clone, PartialEq)]`, `#[non_exhaustive]`, conditional-serde, and a `///` doc comment on every field.

    Implement `dense_flmm(data, subject_ids, covariates, config)` (marked `#[must_use = "expensive computation whose result should not be discarded"]`) mirroring the existing `fmm` body: identical entry validation order (dimensions first via InvalidDimension, then ncomp>=1 via InvalidParameter), `build_subject_map`, uniform argvals, `fdata_to_pc_1d`, the score-scale-normalized per-component parallel loop via `iter_maybe_parallel!` over `fit_scalar_mixed_model`, then `recover_beta_functions` / `recover_random_effects` / `compute_random_variance` / `compute_fitted_residuals`. Additionally track REML convergence: capture whether every component's inner loop converged before max_iter and the max iteration count reached, populating `converged` and `n_iter` (RESEARCH Open Question (a) — resolve `sigma2_slope` as an always-present zero-filled `Vec<f64>` of length k when `random_slopes=false`, matching fdars' no-Option-in-results convention).

    Per RESEARCH Pitfall 6 / Assumption A1: when `config.random_slopes` is true, the two-random-effect scalar LMM is a separate concern; for THIS tracer keep the happy path to `random_slopes=false` (intercept-only), returning zero-filled sigma2_slope — this is a functionality gap that can be filled without an architectural change, NOT an architectural stub. Document in rustdoc that random-slope estimation is intercept-only in this release and that the FPC-score parametrization diverges from R denseFLMM's basis-coefficient / covariance-smoothing formulation (per D locked decision on parametrization).

    Promote exactly these six items in `famm.rs` from private to `pub(crate)` (RESEARCH Reuse Map — non-breaking, NOT a public-signature change): `fit_scalar_mixed_model`, `build_subject_map`, `SubjectStructure` (struct) + `SubjectStructure::new`, `ScalarMixedResult` (struct), `recover_random_effects`. Do NOT change `gls_update_gamma`, `reml_variance_update`, `compute_blup`, or any other private helper. Do NOT touch the signatures of `fmm`, `fmm_predict`, or `fmm_test_fixed`.

    Extend the `pub use famm::{...}` line in `lib.rs` (currently line 233) to also export `dense_flmm, DenseFlmmResult, DenseFlmmConfig`. Do not reorder or remove the existing `fmm, fmm_predict, fmm_test_fixed, FmmResult, FmmTestResult` exports.

    DOCTEST RULE: if you add any rustdoc doctest that constructs `DenseFlmmConfig`, use `let mut c = DenseFlmmConfig::default(); c.ncomp = ...;` — never struct-literal syntax (doctests compile as an external crate; a `#[non_exhaustive]`-adjacent field-init pattern triggers E0639). Prefer `DenseFlmmConfig::default()` in doctests.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel famm::tests::test_dense_flmm 2>/dev/null</automated>
  </verify>
  <done>All test_dense_flmm_* tests pass; the six helpers are `pub(crate)`; `dense_flmm`/`DenseFlmmResult`/`DenseFlmmConfig` are crate-root re-exported; existing `fmm` tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: multiFAMM stacked multivariate variant + re-export</name>
  <files>fdars-core/src/famm.rs, fdars-core/src/lib.rs</files>
  <behavior>
    Write these tests FIRST:
    - test_multi_famm_basic: given a `&[FdMatrix]` of D=2 response dimensions (each from `generate_fmm_data`, sharing n_total and m), returns `components.len() == 2`, each component a valid DenseFlmmResult, n_dims == 2, and stacked_fitted / stacked_residuals shape ((n_total * 2), m).
    - test_multi_famm_grid_mismatch: two dimensions with different ncols -> FdarError::InvalidDimension.
    - test_multi_famm_recovery: for each dimension, fitted + residuals == that dimension's data within 1e-8.
  </behavior>
  <action>
    Add `MultiFammConfig` (ncomp=3, max_iter=50, tol=1e-10; same derives/serde/Default rules as DenseFlmmConfig, no `#[non_exhaustive]`) and `MultiFammResult` (fields per RESEARCH API Surface: components: Vec<DenseFlmmResult>, stacked_fitted, stacked_residuals, n_dims) with `#[non_exhaustive]`, conditional-serde, per-field docs.

    Implement `multi_famm(data: &[FdMatrix], subject_ids, covariates, config)` (`#[must_use]`): validate the slice is non-empty; validate all dimensions share the same `ncols` (RESEARCH Pitfall 5 / Assumption A5 — return InvalidDimension otherwise) and the same `nrows`; then loop over dimensions calling `dense_flmm` with a `DenseFlmmConfig` built from the MultiFammConfig fields (`let mut c = DenseFlmmConfig::default(); c.ncomp = config.ncomp; c.max_iter = config.max_iter; c.tol = config.tol;`). Collect the per-dimension DenseFlmmResults; build `stacked_fitted` and `stacked_residuals` by row-stacking each dimension's fitted/residuals into an `(n_total*D) x m` FdMatrix via `FdMatrix::from_column_major` (validate `nrows*ncols == data.len()`).

    Document in rustdoc the RESEARCH divergence (Assumption A2): fdars runs D independent univariate FPCAs per dimension rather than a joint multivariate FPCA, so cross-dimension covariance kernels are NOT modeled — this is a documented capability divergence from R's multiFAMM.

    Extend the `lib.rs` famm re-export to add `multi_famm, MultiFammResult, MultiFammConfig`.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel famm::tests::test_multi_famm 2>/dev/null</automated>
  </verify>
  <done>All test_multi_famm_* tests pass; multi_famm reuses dense_flmm per dimension; grid-mismatch returns FdarError; re-exports present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: fastFMM massively-univariate per-gridpoint inference + re-export</name>
  <files>fdars-core/src/famm.rs, fdars-core/src/lib.rs</files>
  <behavior>
    Write these tests FIRST:
    - test_fast_fmm_basic: on `generate_fmm_data(12, 3, 40)` with 1 covariate, returns beta_matrix p x m, t_stats p x m, p_values p x m with every p-value in [0.0, 1.0], sigma2_eps len m, sigma2_u len m, n_grid == m.
    - test_fast_fmm_detects_effect: with a covariate carrying a real effect, the L2 norm of beta_matrix row 0 is > 0 and at least one gridpoint has |t_stat| meaningfully large.
    - test_fast_fmm_empty_error: empty data -> FdarError.
    - test_fast_fmm_no_smoothing: config with smooth_window = 1 produces finite beta_matrix identical-length output (smoothing disabled path).
  </behavior>
  <action>
    Add `FastFmmConfig` (smooth_window=3, max_iter=30, tol=1e-8, compute_inference=true; derives/serde/Default; no `#[non_exhaustive]`) and `FastFmmResult` (fields per RESEARCH API Surface: beta_matrix p x m, t_stats p x m, p_values p x m, sigma2_eps len m, sigma2_u len m, n_grid) with `#[non_exhaustive]`, conditional-serde, per-field docs.

    Implement `fast_fmm(data, subject_ids, covariates, config)` (`#[must_use]`) following RESEARCH fastFMM three-step path and Pitfalls 3 & 4:
    Step 1 — per gridpoint t in 0..m, fit a scalar mixed model on the contiguous column slice `data.column(t)` (column-major zero-copy per Pitfall 4) via the `pub(crate)` `fit_scalar_mixed_model`, using the parallel-safe collect pattern: `let per_point: Vec<PointwiseResult> = iter_maybe_parallel!(0..m).map(|t| { ... }).collect();` where `PointwiseResult` is a small immutable per-item struct (gamma vector, sigma2_u, sigma2_eps) — never write into a shared indexed Vec inside the closure (Pitfall 3).
    Step 2 — build the raw p x m beta matrix from per-point gammas, then apply a running-mean smoother of width `config.smooth_window` along the t axis (window 1 = identity/no smoothing). Document the RESEARCH divergence (Assumption A3, Open Question (b)): fdars uses a running-mean smoother configured by `smooth_window` rather than R fastFMM's mgcv thin-plate splines; note Savitzky-Golay as a future improvement.
    Step 3 — when `config.compute_inference`, compute Wald t-statistics `t = beta / se` with `se` derived from per-gridpoint sigma2_eps and the covariate cross-product, and two-sided p-values via a standard-normal survival approximation (reuse the crate's existing normal-CDF helper if present; otherwise a local `erf`-based approximation — no new dependency). When `compute_inference` is false, fill t_stats with zeros and p_values with ones. Document the divergence: Wald-only (Gaussian) inference, no bootstrap.

    Validate entry in the standard order (empty data / subject_ids mismatch -> InvalidDimension; smooth_window == 0 -> InvalidParameter). Extend the `lib.rs` famm re-export to add `fast_fmm, FastFmmResult, FastFmmConfig`.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel famm::tests::test_fast_fmm 2>/dev/null</automated>
  </verify>
  <done>All test_fast_fmm_* tests pass; p-values bounded in [0,1]; per-gridpoint fits use the collect-then-unpack parallel pattern; re-exports present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → public fn | typed `FdMatrix` / `&[usize]` / config structs cross here; only input-validation risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Denial of Service | dense_flmm / multi_famm / fast_fmm entry | medium | mitigate | Validate dimensions + subject_ids length + grid consistency + smooth_window at entry → FdarError, never panic (tests: *_empty_error, *_ids_mismatch, *_grid_mismatch) |
| T-32-02 | Denial of Service (Availability) | per-component / per-gridpoint Cholesky in reused solver | low | accept | `cholesky_factor` returns ComputationFailed and `reml_variance_update` clamps sigma2 to `.max(1e-15)` — inherited unchanged from existing famm.rs |
| T-32-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs — this phase adds zero external dependencies (RESEARCH: "installs zero new crates") |
</threat_model>

<verification>
- Per task: the task's own `cargo test ... famm::tests::<prefix>` command (see each `<verify>`).
- Per plan (wave merge): `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel famm 2>/dev/null` — all famm tests (new + existing fmm) green.
- Existing-signature guard: `fmm`, `fmm_predict`, `fmm_test_fixed` tests must still pass unchanged (additive/non-breaking).
</verification>

<success_criteria>
- `dense_flmm`, `multi_famm`, `fast_fmm` exist, are `Result`-returning, `#[must_use]`, and crate-root re-exported.
- Synthetic recovery: dense_flmm recovers the fixed-effect function (corr > 0.8) and positive random-intercept variance.
- Invalid inputs return `FdarError` (never panic) on all three estimators.
- Six private helpers are `pub(crate)` (ready for Plan 02); no existing public signature changed.
- `cargo test -p fdars-core --features linalg,parallel famm` fully green.
</success_criteria>

<output>
Create `.planning/phases/32-flexible-mixed-effects-regression/32-01-SUMMARY.md` when done.
</output>
