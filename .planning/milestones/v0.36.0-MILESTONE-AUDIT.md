---
milestone: v0.36.0
audited: 2026-09-04
status: passed
scores:
  requirements: 5/5
  phases: 3/3
  integration: 5/5
  flows: 2/2
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: all (66, 67, 68)
    items:
      - "Nyquist VALIDATION.md files are status: draft (NOT-VALIDATED) — validate-phase was never run to reconcile them. Coverage TODO, not a compliance failure (#2117): tests are green and comprehensive, but the formal Nyquist sign-off is unrun. Run /gsd-validate-phase 66|67|68 to promote to validated."
  - phase: 68 (lpeer)
    items:
      - "lpeer applies the structured penalty via a penalized-GLS re-solve in FPC-score space using famm's variance components (post-hoc, not a single joint REML fit of penalty + subject RE). Faithful and tested (λ genuinely regularizes), but a fully joint mixed-model formulation is a possible future refinement."
      - "GCV grid does a double Cholesky factorization per grid point (O(40·2) on m≈40 — negligible); could be unified. Cosmetic."
  - phase: pre-existing (not v0.36.0)
    items:
      - "cargo build --features serde is pre-broken (shapelet ShapeletTransformClassifier embeds non-serde ClassifFit, Phase 60). Independent of PEER; new PEER types are serde-clean. Backlog: add serde derives to ClassifFit."
nyquist:
  compliant_phases: 0
  partial_phases: 0
  not_validated_phases: 3
  missing_phases: 0
  overall: not-validated (draft — coverage TODO, tests green)
---

# v0.36.0 Milestone Audit — PEER: Structured-Penalty & Longitudinal Scalar-on-Function Regression

**Status: PASSED** — 5/5 requirements satisfied, 3/3 phases verified, cross-phase integration INTEGRATED (0 gaps), 2/2 E2E flows complete.

## Definition of Done

Ship PEER (Partially Empirical Eigenvectors for Regression) — structured-penalty scalar-on-function regression + the longitudinal `lpeer` variant — as an additive, non-breaking addition to `fdars-core`, matching the refund@0.1-38 `peer`/`lpeer` baseline. Publish on the v0.36.0 tag (crate 0.35.0 → 0.36.0).

## Requirements Coverage (3-source cross-reference)

| REQ | Phase | VERIFICATION | SUMMARY frontmatter | Traceability | Final |
|-----|-------|--------------|---------------------|--------------|-------|
| PER-01 | 66 | passed | listed | `[x]` | **satisfied** |
| PER-02 | 66 | passed | listed | `[x]` | **satisfied** |
| PER-03 | 67 | passed | listed | `[x]` | **satisfied** |
| PER-04 | 68 | passed | listed | `[x]` | **satisfied** |
| PER-05 | 68 | passed | listed | `[x]` | **satisfied** |

No unsatisfied, partial, or orphaned requirements.

## Phase Verifications

| Phase | Name | VERIFICATION | Score |
|-------|------|--------------|-------|
| 66 | Core PEER Estimator & Penalty Families | passed | 4/4 must-haves |
| 67 | Automatic λ Selection — GCV + REML | passed | 4/4 must-haves |
| 68 | Longitudinal PEER, Prediction & Integration | passed | 4/4 must-haves |

## Integration (cross-phase wiring)

Verdict: **INTEGRATED** (integration-checker, read-only). 8 exports connected, 0 orphaned, 0 missing.

- 66→67: `LambdaChoice` dispatch consumed by `peer()`; `select_lambda_gcv_peer`/`select_lambda_reml_peer` wired.
- 66→68: `PeerResult.w_bar` consumed by `predict`; self-consistency identity verified < 1e-9.
- 67→68: `lpeer()` uses the same `LambdaChoice` dispatch AND applies λ in the penalized-GLS re-solve (`a_mat += lambda·Φ'QΦ`) — λ genuinely regularizes β(t).
- 66→68: `build_q` penalty machinery reused by `lpeer` for all three families.
- Integration deliverable: all 8 symbols (`peer, lpeer, PeerConfig, PeerResult, LpeerResult, PeerPenalty, LambdaChoice, LambdaMethod`) exported at crate root AND prelude.

## E2E Flows

- **peer fit → predict:** GCV/REML/Fixed λ → β(t) → out-of-sample predict, self-consistent. Verified by module doctest + `test_peer_predict_self_consistent`.
- **lpeer fit → variance components → predict:** subject random effects via famm, penalized β(t), marginal predict. Verified by `lpeer` doctest + `test_lpeer_*`.

## Test Evidence

- Full lib suite: **2720 passed, 0 failed**.
- `peer::` module: **31 passed** (β recovery, 3 penalty families, decree-distinctness, GCV determinism, REML positivity + REML/GCV agreement, λ-regularizes, lpeer recovery + variance tracking, predict self-consistency, error/NaN surface).
- Doctests: **2 passed** (running end-to-end `peer` + `lpeer`).
- `cargo clippy --all-targets --features linalg,parallel -- -D warnings`: clean.
- `cargo fmt -- --check`: clean.

## Code Review

All three code phases went through a code-review gate:
- Phase 66: 1 critical (missing `w_bar` for prediction) + 3 warnings → all resolved.
- Phase 67: 4 warnings (REML EM robustness) → resolved.
- Phase 68: 1 critical (lpeer λ did not regularize) + 3 warnings → CR-01 fixed with a faithful penalized-GLS re-solve (λ now genuinely shapes β(t), proven by `test_lpeer_lambda_regularizes`); warnings resolved.

## Tech Debt (non-blocking)

1. **Nyquist NOT-VALIDATED** (all 3 phases): VALIDATION.md files are `status: draft` — validate-phase was never run. Tests are green and comprehensive; this is a formal-sign-off TODO (#2117), not a gap. Run `/gsd-validate-phase 66|67|68` to promote.
2. **lpeer penalty is post-hoc** (Phase 68): penalized-GLS re-solve using famm's variance components rather than one joint REML fit of penalty + subject RE. Faithful and tested; joint formulation is a future refinement.
3. **Pre-existing serde build break** (not v0.36.0): shapelet `ClassifFit` lacks serde derives → `cargo build --features serde` fails independently. PEER types are serde-clean. Backlog item.

## Verdict

**PASSED.** No critical blockers. All requirements satisfied and composed end-to-end. Tech debt is minor and tracked. Ready to complete the milestone and publish v0.36.0.
