---
milestone: v0.24.0
audited: 2026-08-20
status: passed
scores:
  requirements: 3/3
  phases: 3/3
  integration: 3/3
  flows: 3/3
gaps: {}
tech_debt:
  - phase: cross-milestone (Nyquist coverage)
    items:
      - "All three VALIDATION.md files are status: draft (NOT-VALIDATED). Run /gsd-validate-phase 31|32|33 to reconcile to status: validated. This is a standing project-wide coverage TODO (prior milestones' VALIDATION.md also remain draft), not a defect in delivered functionality."
  - phase: 31 (REG-04)
    items:
      - "Documented intentional R-baseline divergences (rustdoc): FAM/GSAM one-pass NW (GCV bandwidth, not PACE); scalar-on-function variable_selection direction; history-index marginal-integration (not pffr bivariate spline); GKAM/GSAM Gaussian identity link only (non-Gaussian links a documented known gap); variable_selection GroupLasso only (MCP/SCAD documented future work)."
  - phase: 32 (REG-05)
    items:
      - "denseFLMM random_slopes not implemented — random_slopes=true now returns FdarError::InvalidParameter (documented); sigma2_slope zero-filled."
      - "Documented divergences: multiFAMM uses D independent univariate FPCAs (not joint multivariate FPCA); fastFMM uses running-mean smoothing + Wald-only inference (not mgcv splines + bootstrap); FPC-score parametrization (not basis-coefficient)."
  - phase: 33 (CLUS-01)
    items:
      - "funHDDC simplified single AkBk-style model (intrinsic-dim + isotropic noise), documented divergence from the R funHDDC 6-model family."
      - "funFEM simplified Fisher-EM (documented); align-and-cluster shape-shift test uses amplitude-separable data (non-blocking test-framing note from verification)."
  - phase: carried-forward (not v0.24.0 work)
    items:
      - "Advisory tech-debt from prior milestones: weakened MEWMA test assertion; fix_svd_signs NaN no-op; over-broad Phase 11 test name. Unchanged this milestone."
      - "Crate release pending: Cargo.toml still 0.23.0. v0.23.0 and v0.24.0 both need version bump + PR + tag (operator ship-time step; audit milestones intentionally do not tag)."
---

# Milestone v0.24.0 — Audit Report

**Milestone:** v0.24.0 — Functional Regression & Clustering Breadth
**Audited:** 2026-08-20
**Status:** ✅ PASSED

## Definition of Done

Deliver the next three top-ranked P2 differentiator gaps from the v0.18.0 R-BACKLOG.md
(each score 1.73), as real, additive/non-breaking `fdars-core/src/` code with inline tests
and crate-root re-exports, **zero changes to existing public signatures**, and **no new crate
dependency**. Numeric outputs only.

## Requirements Coverage (3-source cross-reference)

| REQ-ID | Phase | VERIFICATION.md | SUMMARY frontmatter | REQUIREMENTS.md | Final |
|--------|-------|-----------------|---------------------|-----------------|-------|
| REG-04 | 31 | passed (5/5) | listed | [x] Complete | **satisfied** |
| REG-05 | 32 | passed (5/5) | listed | [x] Complete | **satisfied** |
| CLUS-01 | 33 | passed (5/5) | listed | [x] Complete | **satisfied** |

**3/3 requirements satisfied. No unsatisfied, no orphaned, no partial.**

## Phase Verification

| Phase | Name | Status | Score |
|-------|------|--------|-------|
| 31 | Additive Functional Regression & Variable Selection | passed | 5/5 |
| 32 | Flexible Mixed-Effects Regression | passed | 5/5 |
| 33 | Model-Based & Density Functional Clustering | passed | 5/5 |

Each phase went through: discuss → research → pattern-map → plan → plan-check → execute →
code-review (found + fixed real bugs) → verify. Code review found and fixed **5 critical + 12
warnings** across the milestone (P31: broken λ-selection + gradient fallback; P32: fastFMM inert
config fields; P33: funHDDC E-step complement clamp + align-cluster false-convergence).

## Integration (cross-phase wiring)

**Verdict: INTEGRATED.** The three phases touch disjoint source files (by design — no cross-phase
hard dependency). The combined crate:
- Re-exports all new public fns + config/result types at the crate root (verified symbol-by-symbol).
- Wires shared infrastructure correctly: DBSCAN→`distance::l2_distance_matrix`;
  align-and-cluster→`alignment/`; fof_re→`famm` pub(crate) helpers; all three→`regression::fdata_to_pc_1d`.
- Adds **no new crate dependency** (Cargo.toml unchanged).
- Is **non-breaking**: existing entry points untouched; 6 famm helpers widened private→pub(crate) only.
- `cargo build -p fdars-core --features linalg,parallel` clean; **`cargo test --lib` = 2268 passed, 0 failed**;
  `cargo clippy --all-targets --features linalg,parallel -- -D warnings` clean; doctests green.

## Nyquist Coverage

| Phase | VALIDATION.md | status | nyquist_compliant | Action |
|-------|---------------|--------|-------------------|--------|
| 31 | exists | draft | false | `/gsd-validate-phase 31` (NOT-VALIDATED — coverage TODO) |
| 32 | exists | draft | false | `/gsd-validate-phase 32` (NOT-VALIDATED — coverage TODO) |
| 33 | exists | draft | false | `/gsd-validate-phase 33` (NOT-VALIDATED — coverage TODO) |

All three VALIDATION.md files are `status: draft` — seeded by plan-phase but not yet reconciled by
validate-phase. Per audit policy, NOT-VALIDATED (draft) is a coverage TODO, **not a compliance
failure**, and is consistent with standing project policy (prior milestones' VALIDATION.md also
remain draft). Does not block milestone completion.

## Conclusion

Milestone v0.24.0 met its definition of done: REG-04, REG-05, and CLUS-01 delivered as additive,
non-breaking, verified `fdars-core` code with crate-root re-exports and zero new dependencies. All
per-phase gates and the whole-crate build/lint/test suite are green. Accumulated tech debt (Nyquist
validation reconciliation + documented intentional R-baseline divergences + a pending crate release)
is tracked above and carries no blockers.
