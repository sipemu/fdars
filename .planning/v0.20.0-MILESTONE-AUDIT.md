---
milestone: v0.20.0
milestone_name: Table-Stakes Quick Wins
audited: 2026-08-16
status: passed
scores:
  requirements: 2/2
  phases: 2/2
  integration: 2/2
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: all
    items:
      - "Nyquist VALIDATION.md not produced for either phase (carried-forward draft posture)."
nyquist:
  overall: not-reconciled
  reason: "VALIDATION.md not seeded/reconciled; code is independently test-verified (2061 lib tests green)."
---

# Milestone v0.20.0 — Table-Stakes Quick Wins · Milestone Audit

**Verdict: PASSED** — 2/2 requirements satisfied, 2/2 phases verified (independently), additive/non-breaking, combined build green.

## Definition of Done (from ROADMAP)

Ship the two top-ranked (score 5.00, P1 table-stakes, S-effort) R-parity quick wins, each wrapping existing infrastructure.

**Achieved:** T-01 (`constant_basis`, `CvCriterion::Aic` + `aic_smoother`, `smooth_basis_aic`) + T-02 (`DepthMethod` + `functional_depth` dispatcher, `functional_boxplot` + `FunctionalBoxplotResult`). New files `basis/constant.rs`, `depth/dispatch.rs`; 7 files changed, +867/−10 (additive).

## Requirements Coverage (3-source)

| REQ-ID | Phase | VERIFICATION | SUMMARY | Traceability | Final |
|--------|-------|--------------|---------|--------------|-------|
| T-01 | 22 | passed | listed | Complete | **satisfied** |
| T-02 | 23 | passed | listed | Complete | **satisfied** |

2/2 satisfied. No unsatisfied/partial/orphaned.

## Phase Verifications

| Phase | Status | Deliverable |
|-------|--------|-------------|
| 22 Constant Basis & AIC Smoothing | passed (5/5) | `constant_basis` + AIC criterion (kernel + basis paths); +10 tests |
| 23 Functional Boxplot & Depth Dispatcher | passed (5/5) | `functional_depth` dispatcher + López-Pintado–Romo `functional_boxplot`; +12 tests |

## Cross-Phase Integration

The two phases are independent (disjoint modules: `basis/`+`smoothing` vs `depth/`). Proof for a code milestone: the combined crate compiles + passes the full suite together — `cargo test --lib` = **2061 passed / 0 failed**, `cargo clippy --all-targets -D warnings` clean (orchestrator-run, 2026-08-16). Additive only: existing signatures unchanged (`gcv_smoother`/`smooth_basis_gcv`/depth fns byte-identical; `CvCriterion` gained a variant behind `#[non_exhaustive]`).

## Verdict

**PASSED.** Ready to archive. Crate release (bump 0.19.0 → 0.20.0 + push + tag → crates.io) is a separate operator-driven ship step — this milestone shipped real code.
