---
milestone: v0.35.0
audited: 2026-09-03
status: passed
scores:
  requirements: 5/5
  phases: 2/2
  integration: 5/5
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: pre-existing (not v0.35.0)
    items:
      - "cargo build --features serde fails: shapelet/classifier.rs ShapeletTransformClassifier (Phase 60, ea39c623) embeds non-serde ClassifFit (E0277 x4). Pre-existing since v0.33.0; NOT a v0.35.0 regression — the new FOptDes types are serde-clean. Backlog: add serde derives to ClassifFit + fitted sub-structs."
  - phase: 64 + 65 (deferred by design)
    items:
      - "FOD-BREADTH deferred to a future milestone: SR-criterion (FOD-B1), exhaustive/branch-and-bound search (FOD-B2), CV-ridge selection (FOD-B3), rank-1 Cholesky update for large grids (FOD-B4), off-grid interpolated candidates (FOD-B5)."
nyquist:
  compliant_phases: 0
  partial_phases: 0
  not_validated_phases: 2
  missing_phases: 0
  overall: not-validated
---

# Milestone v0.35.0 — Optimal Experimental Design for Sparse FDA (FOptDes) — Audit

**Status: PASSED** — 5/5 requirements satisfied, 2/2 phases verified passed, cross-phase integration verified end-to-end, non-breaking.

## Requirements Coverage (3-source cross-reference)

| REQ | Phase | VERIFICATION | Integration | Final |
|-----|-------|--------------|-------------|-------|
| FOD-01 (trajectory BLUP-MSE criterion) | 64 | passed (9/9) | WIRED | **satisfied** |
| FOD-02 (A-/D-optimality score criterion) | 64 | passed (9/9) | WIRED | **satisfied** |
| FOD-03 (public `design_criterion` evaluator + enums) | 64 | passed (9/9) | WIRED | **satisfied** |
| FOD-04 (deterministic greedy forward selection) | 65 | passed (6/6) | WIRED | **satisfied** |
| FOD-05 (two-stage `optimal_design` workflow + re-exports + benchmark) | 65 | passed (6/6) | WIRED | **satisfied** |

No unsatisfied, partial, or orphaned requirements. REQUIREMENTS.md traceability updated (all five `[x]`).

## Phase Verifications

| Phase | Name | Status | Must-haves |
|-------|------|--------|------------|
| 64 | Criterion Machinery Core | passed | 9/9 |
| 65 | Greedy Selection & Integration | passed | 6/6 |

## Integration & E2E

- Delegation verified: `optimal_design` (Phase 65) calls `design_criterion` (Phase 64) at `optimal_design.rs:401` inside the greedy loop; no duplicated math.
- Full 6-symbol public surface re-exported at crate root (`lib.rs:593-595`) and prelude (`prelude.rs:54-56`): `design_criterion, optimal_design, DesignCriterion, OptDesConfig, OptDesResult, OptimalityKind`.
- E2E flow (fit PACE → `optimal_design` → read `selected_argvals`) proven by the module doctest: `cargo test --doc optimal_design` → 1/1 ok.
- 32/32 optimal_design unit tests pass; determinism holds with AND without `--features parallel`.
- Additive/non-breaking: `cargo build` (default) clean; no existing public signature changed; benchmark registered (`Cargo.toml` `[[bench]] name = "optimal_design"`) and compiles.

## Quality Gates (whole-crate)

- `cargo fmt --check` — clean
- `cargo clippy --all-targets --features linalg,parallel -- -D warnings` — clean
- `cargo test --features linalg,parallel` — 2689 lib + all integration + 196 doctests, 0 failed
- Bench compiles (`cargo build --benches`); `cargo bench` intentionally NOT in CI.

Code review ran on both phases: Phase 64 — 0 blockers, 4 warnings (all fixed); Phase 65 — 1 blocker (panic on degenerate duplicate candidates) + 3 findings (all fixed, incl. true smallest-index tie-break and a defense-in-depth non-panic path).

## Tech Debt (non-blocking)

1. **Pre-existing serde-feature build break (NOT a v0.35.0 regression).** `cargo build --features serde` fails on `shapelet/classifier.rs` (Phase 60, v0.33.0, commit `ea39c623`) — `ShapeletTransformClassifier` derives serde but embeds non-serde `ClassifFit`. The FOptDes types are serde-clean and compile in isolation. This has been the published state since v0.33.0/v0.34.0. **Recommended follow-up backlog item:** add `#[cfg_attr(feature="serde", derive(Serialize, Deserialize))]` to `ClassifFit` and the fitted sub-structs it embeds, then gate `cargo build --features serde` in CI.
2. **FOD-BREADTH** (SR-criterion, exhaustive/branch-and-bound, CV-ridge selection, rank-1 Cholesky update, off-grid interpolated candidates) — intentionally deferred at milestone definition; carried in the STATE deferred-items table.

## Nyquist Coverage

| Phase | VALIDATION.md | status | Action |
|-------|---------------|--------|--------|
| 64 | exists | draft (seeded, not reconciled by validate-phase) | optional `/gsd-validate-phase 64` |
| 65 | exists | draft (seeded, not reconciled by validate-phase) | optional `/gsd-validate-phase 65` |

Both phases have a full VALIDATION.md with concrete automated gates and all listed tests green in the delivered code; the `draft` status only means `/gsd-validate-phase` has not formally reconciled the frontmatter (coverage TODO, not a compliance failure — #2117). Non-blocking.

## Verdict

**PASSED.** Milestone delivered its definition of done. Ready to complete + tag (publishes crate to crates.io on `v0.35.0`).
