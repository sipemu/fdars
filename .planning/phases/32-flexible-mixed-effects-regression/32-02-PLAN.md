---
phase: 32-flexible-mixed-effects-regression
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - fdars-core/src/fof_regression.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [REG-05]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "fof_re_regression fits a flexible random-effects function-on-function model over subject-grouped data and returns beta_surface, fitted, residuals, random_effects, and variance components (REG-05)"
    - "fof_re_regression fitted + residuals == y_data within 1e-6, and random_effects are non-zero when grouped structure is present"
    - "predict_fof_re produces fixed-effect-only predictions matching n_new x m_y, all finite"
    - "empty data / mismatched subject_ids / mismatched dimensions return FdarError, never panic"
    - "base fof_regression / predict_fof / fof_cv public signatures are unchanged and their tests still pass"
  artifacts:
    - "fdars-core/src/fof_regression.rs (extended): fof_re_regression, predict_fof_re, FofReConfig, FofReResult + inline tests"
    - "fdars-core/src/lib.rs (extended fof_regression re-export line)"
  key_links:
    - "fof_re_regression -> fdata_to_pc_1d (x and y) -> fpca.project -> famm::fit_scalar_mixed_model (per Y-score component) -> recover_random_effects"
    - "reuse of Plan 01's pub(crate) famm helpers from the sibling fof_regression module"
    - "crate-root re-exports so callers can `use fdars_core::{fof_re_regression, predict_fof_re, ...}`"
---

<objective>
Wire a flexible random-effects function-on-function estimator into `fdars-core/src/fof_regression.rs` (REG-05): `fof_re_regression` + `predict_fof_re` + `FofReConfig` + `FofReResult`, extending ONLY the flexible/RE variant. The base `fof_regression` / `predict_fof` / `fof_cv` capability is already at parity and its signatures stay untouched.

Purpose: Deliver the fourth REG-05 estimator — an FPCA-based functional random-intercept FoF model (R `refund::pffr` + `pcre()` capability) — by reusing the double-FPCA path already in this file plus the `pub(crate)` scalar-mixed-model helpers promoted in Plan 01.
Output: Extended `fof_regression.rs` and one extended `pub use fof_regression::{...}` line in `lib.rs`.

Depends on Plan 01: `famm::fit_scalar_mixed_model`, `famm::build_subject_map`, `famm::recover_random_effects` (and `SubjectStructure`/`ScalarMixedResult`) must already be `pub(crate)`. Do NOT re-promote them here.

BUILD NOTE (executor): set `TMPDIR=/home/simonm/.cache/fdars-bench-tmp` before every cargo command — the system `/tmp` exhausts and produces bogus "No space left" link failures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-flexible-mixed-effects-regression/32-RESEARCH.md
@.planning/phases/32-flexible-mixed-effects-regression/32-PATTERNS.md

# Primary file to extend — base double-FPCA FoF path + test helper `make_fof_data`.
@fdars-core/src/fof_regression.rs
# Plan 01 promoted these to pub(crate); this plan calls them across the module boundary.
@fdars-core/src/famm.rs
# FpcaResult.project / rotation / mean used for reconstruction.
@fdars-core/src/regression.rs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: fof_re_regression end-to-end tracer + re-export</name>
  <files>fdars-core/src/fof_regression.rs, fdars-core/src/lib.rs</files>
  <precondition>Plan 01 is complete: `famm::fit_scalar_mixed_model`, `famm::build_subject_map`, and `famm::recover_random_effects` are `pub(crate)` (verify with `grep -n "pub(crate) fn fit_scalar_mixed_model" fdars-core/src/famm.rs`). Baseline `cargo test -p fdars-core --features linalg,parallel fof_regression` is green before edits.</precondition>
  <behavior>
    Write these inline tests in `fof_regression.rs`'s `#[cfg(test)] mod tests` FIRST (must fail before implementation). Extend the existing `make_fof_data` helper (or add a sibling helper) to also produce `subject_ids` with a repeated-visits grouping structure:
    - test_fof_re_regression_dims: returns beta_surface m_y x m_x, fitted n x m_y, residuals n x m_y, random_effects n_subjects x m_y, coef_matrix ncomp_x x ncomp_y, intercept len m_y, sigma2_u len ncomp_y.
    - test_fof_re_regression_invariant: fitted[(i,j)] + residuals[(i,j)] == y_data within 1e-6 for all i,j.
    - test_fof_re_regression_re_nonzero: with a grouped structure carrying real subject-level shifts, the L2 norm of random_effects is > 0.
    - test_fof_re_regression_ids_mismatch: subject_ids length != n -> FdarError::InvalidDimension { parameter: "subject_ids", .. }.
    - test_fof_re_reexport: `use crate::fof_re_regression;` compiles (smoke).
  </behavior>
  <action>
    Add `FofReConfig` (fields per RESEARCH API Surface: ncomp_x=3, ncomp_y=3, max_iter=50, tol=1e-10) with `#[derive(Debug, Clone, PartialEq)]`, conditional-serde, NO `#[non_exhaustive]`, and a `Default` impl. Add `FofReResult` (fields per RESEARCH API Surface: intercept, beta_surface, fitted, residuals, r_squared_t, r_squared, ncomp_x, ncomp_y, fpca_x, fpca_y, coef_matrix, random_effects, sigma2_u, sigma2_eps, n_subjects) with `#[derive(Debug, Clone, PartialEq)]`, `#[non_exhaustive]`, conditional-serde, per-field `///` docs. It MUST carry `fpca_x: FpcaResult` and `fpca_y: FpcaResult` so `predict_fof_re` reuses the same projection path as `predict_fof`.

    Implement `fof_re_regression(x_data, y_data, subject_ids, x_argvals, y_argvals, config)` (`#[must_use = "expensive computation whose result should not be discarded"]`), signature per RESEARCH API Surface (subject_ids inserted between y_data and x_argvals; config replaces bare ncomp scalars). Follow RESEARCH "Flexible-RE Function-on-Function" three steps:
    Step 1 — same as base `fof_regression`: validate (n_x == n_y; n >= 3; argvals lengths; ncomp_x/ncomp_y >= 1) AND additionally `subject_ids.len() == n` (InvalidDimension { parameter: "subject_ids" }). Clamp ncomp as the base fn does. Run `fdata_to_pc_1d` on x and y; compute `x_scores = fpca_x.project(x_data)` and `y_scores = fpca_y.project(y_data)`.
    Step 2 (new — replaces OLS with scalar LMM): build the subject map via `famm::build_subject_map(subject_ids)`. For each Y-score component `l` in 0..ncomp_y, call `famm::fit_scalar_mixed_model(&y_scores.col(l), &subject_map, n_subjects, Some(&x_scores_as_covariates), ncomp_x)` to obtain `gamma_l` (len ncomp_x), `u_hat_l` (len n_subjects), `sigma2_u_l`, `sigma2_eps_l`. Per RESEARCH Pitfall 2: pass the projected `x_scores` directly as covariates WITHOUT re-applying the `h.sqrt()` score-scale normalization — the L² weighting is already embedded in `project`; add a comment marking this intentional divergence from `famm::fit_all_components`.
    Step 3 (reconstruction — same structure as base fof): `coef_matrix[k][l] = gamma_l[k]`; reconstruct `beta_surface(s,t)` from coef_matrix and the two rotations exactly as base `fof_regression` does; fitted curves add the mean, the fixed-effect score contribution, AND the subject random-effect contribution `u_hat_{subject(i),l}`; residuals = y_data - fitted; `random_effects` (n_subjects x m_y) via `famm::recover_random_effects(&u_hat_per_component, &fpca_y.rotation, n_subjects, m_y, ncomp_y)`; pointwise and overall R² as base fof does.

    Document in rustdoc the RESEARCH divergence from `refund::pffr`: fdars uses the FPC-score parametrization (double-FPCA + per-Y-component scalar LMM) rather than penalized-spline GAMM random effects — no basis penalties (per D locked decision on parametrization).

    Extend the `pub use fof_regression::{...}` line in `lib.rs` (currently line 242) to add `fof_re_regression, FofReResult, FofReConfig`. Do not reorder or remove `fof_cv, fof_regression, predict_fof, FofCvResult, FofResult`. Do NOT change the signatures of `fof_regression`, `predict_fof`, or `fof_cv`.

    DOCTEST RULE: any rustdoc doctest constructing `FofReConfig` MUST use `let mut c = FofReConfig::default(); c.ncomp_x = ...;` — never struct-literal syntax (E0639 in external-crate doctest compilation). Prefer `FofReConfig::default()`.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel fof_regression::tests::test_fof_re_regression 2>/dev/null</automated>
  </verify>
  <done>All test_fof_re_regression_* tests pass; fitted+residuals reconstruct y within 1e-6; random_effects non-zero under grouping; ids-mismatch returns FdarError; re-exports present; base fof tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: predict_fof_re fixed-effect prediction + re-export</name>
  <files>fdars-core/src/fof_regression.rs, fdars-core/src/lib.rs</files>
  <behavior>
    Write these tests FIRST:
    - test_predict_fof_re_shape: `predict_fof_re(&fit, &new_x)` returns n_new x m_y, all entries finite.
    - test_predict_fof_re_training_matches_fixed: on training x, prediction equals the fixed-effect part of fitted (mean + fixed-effect score contribution, no random effects) within 1e-6 — mirroring the `fmm_predict` / `predict_fof` fixed-effect-only convention.
  </behavior>
  <action>
    Implement `predict_fof_re(fit: &FofReResult, new_x: &FdMatrix) -> Result<FdMatrix, FdarError>` (`#[must_use = "prediction result should not be discarded"]`) mirroring `predict_fof`: project `new_x` onto `fit.fpca_x`, compute predicted Y-scores `X_scores * coef_matrix`, and reconstruct `Ŷ(s) = mean_y(s) + Σ_l score_l φ_y^l(s)`. Per RESEARCH API Surface note: prediction is fixed-effect-only (no random effects for new/unseen subjects) — same convention as `fmm_predict` and `predict_fof`. Document this in rustdoc.

    Extend the `lib.rs` fof_regression re-export to add `predict_fof_re`.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel fof_regression::tests::test_predict_fof_re 2>/dev/null</automated>
  </verify>
  <done>predict_fof_re returns finite n_new x m_y predictions matching the fixed-effect part on training data; re-export present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → public fn | typed `FdMatrix` / `&[usize]` / config cross here; only input-validation risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-03 | Denial of Service | fof_re_regression / predict_fof_re entry | medium | mitigate | Validate n_x==n_y, n>=3, argvals lengths, ncomp>=1, subject_ids length at entry → FdarError, never panic (tests: *_ids_mismatch + inherited base-fof validation) |
| T-32-04 | Denial of Service (Availability) | per-Y-component Cholesky / GLS in reused solver | low | accept | Inherited unchanged: `cholesky_factor` → ComputationFailed; `reml_variance_update` clamps sigma2 to `.max(1e-15)` |
| T-32-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs — zero new dependencies (RESEARCH) |
</threat_model>

<verification>
- Per task: the task's own `cargo test ... fof_regression::tests::<prefix>` command.
- Per plan (wave merge): `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel fof_regression 2>/dev/null` — all fof tests (new + existing base) green.
- Phase gate (before verify): full suite incl. doctests (`TMPDIR=... cargo test -p fdars-core --features linalg,parallel` and `TMPDIR=... cargo test -p fdars-core --features linalg,parallel --doc`) green + `TMPDIR=... cargo clippy --all-targets --features linalg,parallel -- -D warnings` clean.
- Existing-signature guard: `fof_regression`, `predict_fof`, `fof_cv` tests must still pass unchanged.
</verification>

<success_criteria>
- `fof_re_regression` + `predict_fof_re` exist, `Result`-returning, `#[must_use]`, crate-root re-exported.
- fitted + residuals reconstruct y within 1e-6; random_effects non-zero under grouping.
- predict_fof_re is fixed-effect-only, finite, correct shape.
- Invalid inputs return `FdarError` (never panic); base FoF signatures unchanged.
- `cargo test -p fdars-core --features linalg,parallel fof_regression` fully green.
</success_criteria>

<output>
Create `.planning/phases/32-flexible-mixed-effects-regression/32-02-SUMMARY.md` when done.
</output>
