---
phase: 80-release-hardening-ship-v0-40-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v0.39.0-phases/75-scalar-trait-forward-mode-dual-substrate/75-VALIDATION.md
  - .planning/milestones/v0.39.0-phases/76-differentiable-elastic-distance-fpca-scores/76-VALIDATION.md
  - .planning/milestones/v0.39.0-phases/77-gradient-api-composition-demo-integration/77-VALIDATION.md
autonomous: true
requirements: [REL-01]

estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Each of the three archived v0.39.0 VALIDATION.md files (75/76/77) carries frontmatter status: validated and nyquist_compliant: true, OR an explicitly recorded coverage gap where sign-off cannot hold (REL-01)."
    - "The phase's own tests are confirmed to exist and cover each phase's requirement via the VALIDATION.md per-task map — no new tests are synthesized into the archived phases (scope guard)."
  artifacts:
    - .planning/milestones/v0.39.0-phases/75-scalar-trait-forward-mode-dual-substrate/75-VALIDATION.md
    - .planning/milestones/v0.39.0-phases/76-differentiable-elastic-distance-fpca-scores/76-VALIDATION.md
    - .planning/milestones/v0.39.0-phases/77-gradient-api-composition-demo-integration/77-VALIDATION.md
  key_links:
    - "VALIDATION.md per-task Automated Command → an actually-passing test in the current suite (the sign-off evidence)."
---

<objective>
Lightweight Nyquist sign-off of the three outstanding v0.39.0 phases (75/76/77), per D REL-01 in 80-CONTEXT.md. For each archived VALIDATION.md, confirm the phase's tests exist and cover its requirement (read the per-task verification map + run the phase's named test command), then flip frontmatter `status: draft → validated` and `nyquist_compliant: false → true` where coverage holds. If a genuine coverage gap exists, RECORD it in the VALIDATION.md (and note it for backlog) rather than synthesizing new tests into an archived phase.

Purpose: Formally close out the v0.39.0 forward-mode AD phases so the v0.40.0 release folds them in with a clean validation record.
Output: Three edited VALIDATION.md files with updated frontmatter (or recorded gaps).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-release-hardening-ship-v0-40-0/80-CONTEXT.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Sign off Phase 75 VALIDATION.md end-to-end (prove-then-flip the one path)</name>
  <files>.planning/milestones/v0.39.0-phases/75-scalar-trait-forward-mode-dual-substrate/75-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v0.39.0-phases/75-scalar-trait-forward-mode-dual-substrate/75-VALIDATION.md (frontmatter + Per-Task Verification Map + Wave 0 Requirements)
    - .planning/phases/80-release-hardening-ship-v0-40-0/80-CONTEXT.md (REL-01 decision — lightweight sign-off, scope guard: no new tests into archived phases)
  </read_first>
  <action>
    This is the thin end-to-end sign-off path proving the whole REL-01 pattern on one phase before Task 2 repeats it for 76/77. Read the 75-VALIDATION.md Per-Task Verification Map row(s) — Phase 75 covers DIF-01 with the named automated command `cargo test -p fdars-core --features linalg,parallel autodiff`. Run that exact command and confirm the autodiff tests exist and pass (the sign-off evidence per D REL-01). Confirm `src/autodiff.rs` contains the `#[cfg(test)] mod tests` named in Wave 0 Requirements. If the tests exist and pass (coverage holds): edit the frontmatter, changing exactly `status: draft` to `status: validated` and `nyquist_compliant: false` to `nyquist_compliant: true`. Leave `wave_0_complete` and all other frontmatter keys unchanged. Do NOT add or modify any test file in the archived phase (scope guard per D REL-01). If a genuine coverage gap exists (a mapped requirement has no passing test), do NOT flip to validated — instead record the gap in the VALIDATION.md body under a new `## Coverage Gaps (REL-01 sign-off)` section naming the requirement and the missing coverage, leave `status: draft`, and note it for the SUMMARY backlog.
  </action>
  <verify>
    <automated>cargo test -p fdars-core --features linalg,parallel autodiff 2>&1 | tail -3; grep -q '^status: validated' .planning/milestones/v0.39.0-phases/75-scalar-trait-forward-mode-dual-substrate/75-VALIDATION.md && grep -q '^nyquist_compliant: true' .planning/milestones/v0.39.0-phases/75-scalar-trait-forward-mode-dual-substrate/75-VALIDATION.md</automated>
    <fails_when>non-zero exit — either the autodiff tests fail to run/pass, or the frontmatter was not flipped to validated + nyquist_compliant: true (only acceptable when a coverage gap was recorded and status intentionally left draft)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^status: validated' 75-VALIDATION.md` == 1 (unless a coverage gap was recorded, in which case a `## Coverage Gaps (REL-01 sign-off)` heading exists and `status: draft` remains)
    - `grep -c '^nyquist_compliant: true' 75-VALIDATION.md` == 1 (same gap exception)
    - `cargo test -p fdars-core --features linalg,parallel autodiff` exits 0
    - No file under `.planning/milestones/v0.39.0-phases/75-*/` other than 75-VALIDATION.md was modified; no source test file was touched.
  </acceptance_criteria>
  <done>Phase 75 VALIDATION.md frontmatter reflects the green autodiff test coverage: validated + nyquist_compliant: true, or an explicitly recorded coverage gap with status left draft.</done>
</task>

<task type="auto">
  <name>Task 2: Sign off Phase 76 and Phase 77 VALIDATION.md (expand the proven pattern)</name>
  <files>.planning/milestones/v0.39.0-phases/76-differentiable-elastic-distance-fpca-scores/76-VALIDATION.md, .planning/milestones/v0.39.0-phases/77-gradient-api-composition-demo-integration/77-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v0.39.0-phases/76-differentiable-elastic-distance-fpca-scores/76-VALIDATION.md (frontmatter + Per-Task Verification Map + Test Infrastructure quick-run command)
    - .planning/milestones/v0.39.0-phases/77-gradient-api-composition-demo-integration/77-VALIDATION.md (frontmatter + Per-Task Verification Map + Test Infrastructure quick-run command — this phase includes doctests)
    - .planning/phases/80-release-hardening-ship-v0-40-0/80-CONTEXT.md (REL-01 decision)
  </read_first>
  <action>
    Repeat the Task 1 sign-off pattern for Phase 76 (DIF-02, DIF-03 — differentiable elastic distance + FPCA scores) and Phase 77 (DIF-04 — gradient API + composition demo + integration). For EACH phase: read its VALIDATION.md Per-Task Verification Map to get the requirement→automated-command mapping and the Test Infrastructure quick-run command, then run that named command (Phase 77's map includes a `cargo test --doc` component — run the doctest command it names) and confirm the tests exist and pass. Where coverage holds, edit each file's frontmatter changing exactly `status: draft` to `status: validated` and `nyquist_compliant: false` to `nyquist_compliant: true`; leave all other keys unchanged. Do NOT add or modify any test in the archived phases (scope guard per D REL-01). If a genuine coverage gap exists for a mapped requirement, do NOT flip that file — record it under a `## Coverage Gaps (REL-01 sign-off)` section naming the requirement + missing coverage, leave `status: draft`, and note it for the SUMMARY backlog.
  </action>
  <verify>
    <automated>for p in 76-differentiable-elastic-distance-fpca-scores/76 77-gradient-api-composition-demo-integration/77; do f=".planning/milestones/v0.39.0-phases/$p-VALIDATION.md"; grep -q '^status: validated' "$f" && grep -q '^nyquist_compliant: true' "$f" || { echo "UNSIGNED (or gap-recorded): $f"; }; done; grep -rL '^status: validated' .planning/milestones/v0.39.0-phases/76*/76-VALIDATION.md .planning/milestones/v0.39.0-phases/77*/77-VALIDATION.md | grep -q . && echo "at least one not validated — confirm gap recorded" || echo "both 76+77 validated"</automated>
    <fails_when>a 76/77 VALIDATION.md is neither validated+nyquist_compliant:true nor carries a recorded `## Coverage Gaps (REL-01 sign-off)` section (i.e. silently left draft with no gap rationale)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^status: validated' 76-VALIDATION.md` == 1 and `grep -c '^nyquist_compliant: true' 76-VALIDATION.md` == 1 (unless a `## Coverage Gaps (REL-01 sign-off)` heading is present and status intentionally left draft)
    - Same two grep checks hold for 77-VALIDATION.md (same gap exception)
    - The Phase 76 and Phase 77 named test commands (including Phase 77's `--doc` component) run and pass
    - No source test file and no non-VALIDATION artifact under the 76/77 archived directories was modified.
  </acceptance_criteria>
  <done>Phases 76 and 77 VALIDATION.md frontmatter each reflect their green test coverage (validated + nyquist_compliant: true), or carry an explicitly recorded coverage gap with status left draft.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan edits only local `.planning/` documentation frontmatter and runs read-only test commands. No untrusted input, no network, no package installs, no source-code change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-80-01 | Tampering | Archived VALIDATION.md sign-off | low | mitigate | Sign-off is gated on an actually-passing named test command (verify runs it); a false "validated" flip cannot pass the verify gate because the gate re-runs the evidence command. |
| T-80-02 | Repudiation | False green (validated without coverage) | low | mitigate | Scope guard forbids synthesizing tests to manufacture coverage; genuine gaps must be recorded, not hidden, and status left draft. |
</threat_model>

<verification>
- `cargo test -p fdars-core --features linalg,parallel autodiff` passes (Phase 75 evidence).
- Phase 76 and Phase 77 named test commands (incl. doctests for 77) pass.
- All three VALIDATION.md files are either `status: validated` + `nyquist_compliant: true`, or carry a recorded `## Coverage Gaps (REL-01 sign-off)` section with status left draft.
- `git ls-files` confirms only the three VALIDATION.md files changed under `.planning/milestones/v0.39.0-phases/`.
</verification>

<success_criteria>
REL-01 satisfied: phases 75/76/77 VALIDATION.md are moved from draft to validated (Nyquist sign-off) reflecting the green test suite, or any genuine coverage gap is explicitly recorded. No new tests synthesized into archived phases.
</success_criteria>

<output>
Create `.planning/phases/80-release-hardening-ship-v0-40-0/80-01-SUMMARY.md` when done. The SUMMARY must list, per phase, the named test command run, its result, and whether the file was flipped to validated or a coverage gap was recorded (with the gap text if any, for backlog promotion).
</output>
