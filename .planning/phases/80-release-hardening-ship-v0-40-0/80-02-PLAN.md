---
phase: 80-release-hardening-ship-v0-40-0
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/Cargo.toml
  - CHANGELOG.md
  - README.md
  - documentation/ARCHITECTURE.md
  - documentation/DEVELOPMENT.md
  - documentation/GETTING-STARTED.md
autonomous: true
requirements: [REL-02]

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "fdars-core builds and its test suite passes under --features linalg,parallel,serde with the version bumped to 0.40.0 (REL-02)."
    - "fdars-core/Cargo.toml version is exactly 0.40.0 (REL-02)."
    - "CHANGELOG.md carries both a [0.39.0] entry (forward-mode AD core) and a [0.40.0] entry (CORR-01/02 + BUILD-01, noting the soft_dtw barycenter behavior change), above [0.38.0], in the existing keepachangelog format (REL-02)."
    - "Every tracked version string that read 0.38 now reads 0.40 in README.md and documentation/ (REL-02)."
    - "The whole-crate gates pass: cargo fmt --check, cargo clippy --all-targets, cargo test (REL-02)."
    - "The SUMMARY documents the operator's final manual ship steps (git tag v0.40.0 → push → release.yml publishes) — the phase itself does NOT tag or publish."
  artifacts:
    - fdars-core/Cargo.toml
    - CHANGELOG.md
    - README.md
  key_links:
    - "fdars-core/Cargo.toml version 0.40.0 → the [0.40.0] CHANGELOG heading (they must agree)."
    - "CHANGELOG [0.40.0] soft_dtw note → the actual Phase 78 behavior change (barycenter now converges)."
---

<objective>
Make fdars-core release-ready for v0.40.0 per D REL-02 in 80-CONTEXT.md: bump the crate version 0.38.0 → 0.40.0, add [0.39.0] and [0.40.0] CHANGELOG entries, refresh tracked version strings and highlights in README + documentation/, prove the whole-crate gates are green, and document the operator's final tag/publish steps in the SUMMARY. This phase PREPARES and VERIFIES release-readiness; it does NOT run `git tag` or `cargo publish` (locked Ship boundary in 80-CONTEXT.md).

Purpose: fold the unpublished v0.39.0 forward-mode AD core plus the v0.40.0 correctness/build fixes into fdars' first crates.io release since v0.38.0.
Output: bumped Cargo.toml, updated CHANGELOG, refreshed docs, green gates, and an operator ship-steps section in the SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-release-hardening-ship-v0-40-0/80-CONTEXT.md
@CHANGELOG.md
@fdars-core/Cargo.toml
@README.md
</context>

<tasks>

<task type="tracer" reversibility="reversible">
  <name>Task 1: Bump crate version to 0.40.0 and prove the crate still builds + tests green end-to-end</name>
  <files>fdars-core/Cargo.toml</files>
  <read_first>
    - fdars-core/Cargo.toml (line 3 version = "0.38.0"; the [features] block lines 18-30 for the feature names the gates use)
    - .planning/phases/80-release-hardening-ship-v0-40-0/80-CONTEXT.md (REL-02 version-bump decision: skip the never-published 0.39.0, go straight 0.38.0 to 0.40.0; root Cargo.toml is a virtual workspace with no version)
  </read_first>
  <action>
    This is the thin end-to-end release-readiness slice: change the one load-bearing fact (the crate version) and prove the whole crate still compiles and tests green under the full feature set the gates use, before the CHANGELOG/docs expansion tasks. In fdars-core/Cargo.toml change exactly the version key from 0.38.0 to 0.40.0 (per D REL-02 — skip the never-published 0.39.0). Do NOT touch the root workspace Cargo.toml (virtual workspace, no version). Do NOT edit any other Cargo.toml key. Then build the crate under the full gate feature set linalg,parallel,serde to prove the bump did not break the build end-to-end. Per MEMORY.md build hazards, run cargo fmt before committing and commit with git commit --no-verify (30s hook timeout) so no fmt drift is left.
  </action>
  <verify>
    <automated>grep -q '^version = "0.40.0"' fdars-core/Cargo.toml && ! grep -q '^version = "0.38.0"' fdars-core/Cargo.toml && cargo build -p fdars-core --features linalg,parallel,serde 2>&1 | tail -3</automated>
    <fails_when>non-zero exit — version not bumped to 0.40.0, the old 0.38.0 string still present, or the crate fails to compile under linalg,parallel,serde ("could not compile")</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^version = "0.40.0"' fdars-core/Cargo.toml` == 1
    - `grep -c '^version = "0.38.0"' fdars-core/Cargo.toml` == 0
    - `cargo build -p fdars-core --features linalg,parallel,serde` exits 0
    - root `Cargo.toml` unchanged (`git diff --name-only` does not list it)
  </acceptance_criteria>
  <done>fdars-core/Cargo.toml is versioned 0.40.0 and the crate compiles under the full gate feature set; the one release-critical fact is proven end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Add [0.39.0] and [0.40.0] CHANGELOG entries in keepachangelog format</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (lines 1-40: the additive/non-breaking preamble at lines 4-6 and the existing [0.38.0] - 2026-09-05 entry format with its Added subsections)
    - .planning/phases/80-release-hardening-ship-v0-40-0/80-CONTEXT.md (REL-02 CHANGELOG decision: two entries above [0.38.0]; the exact scope of [0.39.0] and [0.40.0]; note the soft_dtw barycenter behavior change)
    - .planning/ROADMAP.md (Phase 78/79 success criteria + the v0.39.0 folded-details block for accurate entry wording)
  </read_first>
  <action>
    Insert two new version entries directly above the existing [0.38.0] - 2026-09-05 heading, newest first, matching the existing keepachangelog heading style (## [x.y.z] - YYYY-MM-DD) with Added / Changed / Fixed subsections as appropriate. Use today's date 2026-09-07 for both. The [0.39.0] entry: an Added subsection describing the forward-mode Dual AD core — the Scalar trait plus the Dual value/tangent number, soft_dtw_distance_generic, differentiable elastic distance and FPCA scores, and the gradient-API composition demo (phases 75/76/77); note it was code-complete but unpublished and is folded in here. The [0.40.0] entry: a Fixed subsection for CORR-01 (the soft_dtw_backward endpoint-seed gradient bug — the reverse loop no longer overwrites the endpoint seed) and BUILD-01 (the serde-feature build compiles again, broken since Phase 60); a Changed subsection calling out the soft_dtw barycenter BEHAVIOR CHANGE (it now genuinely converges via the inverse-curvature step instead of silently returning the pointwise mean) plus the CORR-02 gradient-pass audit outcome (no other bugs found). IMPORTANT: the file preamble at lines 4-6 currently asserts the whole span is additive and non-breaking with no signature or behavior change — because [0.40.0] intentionally changes soft_dtw barycenter output, amend that preamble so it no longer over-claims (scope the additive/non-breaking claim to the public API and signatures, and explicitly except the intended soft_dtw gradient/barycenter correction). Exact wording is Claude's discretion per D REL-02. Do not alter any pre-existing [0.38.0] or earlier entry text.
  </action>
  <verify>
    <automated>grep -qE '^## \[0\.40\.0\]' CHANGELOG.md && grep -qE '^## \[0\.39\.0\]' CHANGELOG.md && grep -qE '^## \[0\.38\.0\]' CHANGELOG.md && awk '/^## \[0\.40\.0\]/{a=NR} /^## \[0\.39\.0\]/{b=NR} /^## \[0\.38\.0\]/{c=NR} END{exit !(a>0 && a<b && b<c)}' CHANGELOG.md && grep -iqE 'soft.?dtw' CHANGELOG.md</automated>
    <fails_when>non-zero exit — a [0.40.0]/[0.39.0]/[0.38.0] heading is missing, they are not ordered newest-first, or no soft_dtw behavior-change note is present in the CHANGELOG</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^## \[0\.40\.0\]' CHANGELOG.md` == 1 and `grep -cE '^## \[0\.39\.0\]' CHANGELOG.md` == 1
    - The [0.40.0] heading appears before [0.39.0], which appears before [0.38.0] (awk order check exits 0)
    - The [0.40.0] entry names CORR-01 (soft_dtw endpoint-seed fix), BUILD-01 (serde build), and the soft_dtw barycenter behavior change; the [0.39.0] entry names the forward-mode Dual AD core
    - The preamble no longer claims the whole span is behavior-preserving (the soft_dtw correction is excepted)
    - No pre-existing [0.38.0] or earlier entry body text changed (`git diff` shows only insertions above [0.38.0] plus the preamble edit)
  </acceptance_criteria>
  <done>CHANGELOG.md carries ordered [0.40.0] and [0.39.0] entries in keepachangelog format above [0.38.0], the [0.40.0] entry documents the soft_dtw behavior change, and the preamble no longer over-claims non-breakingness.</done>
</task>

<task type="auto">
  <name>Task 3: Bump tracked version strings and refresh AD/fix highlights in README + documentation/</name>
  <files>README.md, documentation/ARCHITECTURE.md, documentation/DEVELOPMENT.md, documentation/GETTING-STARTED.md</files>
  <read_first>
    - README.md (line 43 `fdars-core = "0.38"`, line 83 `fdars-core = { version = "0.38", default-features = false }`, and the Features table around lines 22-40 for where an AD/soft_dtw highlight fits naturally)
    - documentation/GETTING-STARTED.md (the five `version = "0.38"` install snippets at lines 33, 49, 56, 188, 196)
    - documentation/ARCHITECTURE.md (line 4 `(v0.38.0)`; line 307 the distance-metrics list that already mentions soft-DTW)
    - documentation/DEVELOPMENT.md (line 17 `fdars-core v0.38.0`)
    - .planning/phases/80-release-hardening-ship-v0-40-0/80-CONTEXT.md (REL-02 docs decision: bump version strings; add forward-mode AD + the two fixes to README highlights where natural; refresh only stale documentation/ spots; NO new guide pages)
  </read_first>
  <action>
    Update every tracked version string that reads 0.38 to 0.40 across README.md (lines 43 and 83), documentation/GETTING-STARTED.md (all five install snippets), documentation/ARCHITECTURE.md (the (v0.38.0) descriptor on line 4), and documentation/DEVELOPMENT.md (the fdars-core v0.38.0 label on line 17). Use the same string shape already present (e.g. the bare "0.38" install pins become "0.40"; the v0.38.0 prose descriptors become v0.40.0). Then, per D REL-02, add a natural mention of the newly-folded-in capability where it fits WITHOUT adding new guide pages: in the README Features table, extend an existing row (the Elastic Alignment or a new short highlight) to note the forward-mode differentiable core (Scalar/Dual, differentiable soft-DTW distance + elastic distance + FPCA scores, gradient API). A one-line highlight is sufficient — the AD feature shipped in the folded-in 0.39.0 work, so a mention suffices, not a tutorial. Do NOT touch anything under docs/ (gitignored) and do NOT create new files. Only edit stale spots; leave accurate prose alone. Which documentation/ spots beyond the version strings are stale is Claude's discretion per D REL-02.
  </action>
  <verify>
    <automated>test "$(git grep -c '0\.38' -- README.md 'documentation/*.md' | wc -l)" -eq 0 && grep -q '"0.40"' README.md && grep -qiE 'differentiable|forward-mode|autodiff|\bdual\b' README.md</automated>
    <fails_when>non-zero exit — any tracked README.md or documentation/*.md file still contains a 0.38 string, the README lacks a 0.40 pin, or no forward-mode/differentiable AD highlight was added to the README</fails_when>
  </verify>
  <acceptance_criteria>
    - `git grep -l '0\.38' -- README.md 'documentation/*.md'` returns nothing (no tracked doc still says 0.38)
    - `grep -c '"0.40"' README.md` >= 1 (both install pins updated)
    - README contains at least one of: differentiable / forward-mode / autodiff / Dual (the folded-in AD highlight)
    - No file under `docs/` changed; no new file created under `documentation/`
  </acceptance_criteria>
  <done>All tracked version strings read 0.40, and the README highlights the folded-in forward-mode differentiable core, with no new guide pages and no edits to the gitignored docs/ tree.</done>
</task>

<task type="auto">
  <name>Task 4: Green the whole-crate gates and document the operator's final ship steps</name>
  <files>.planning/phases/80-release-hardening-ship-v0-40-0/80-02-SUMMARY.md</files>
  <read_first>
    - fdars-core/Cargo.toml ([features] block — the exact feature names for the gate commands)
    - .planning/phases/80-release-hardening-ship-v0-40-0/80-CONTEXT.md (Ship boundary — LOCKED: the phase does NOT tag or publish; the git tag v0.40.0 → push → release.yml publish is the operator's manual final step, documented in the SUMMARY, gated on all prior phases green)
    - .planning/STATE.md (build hazards: clippy --all-targets, cargo fmt per commit, /tmp + target/ disk pressure, --no-verify commits)
  </read_first>
  <action>
    Final release-readiness gate + operator handoff. Run the whole-crate gates and confirm all three are green: cargo fmt --check; cargo clippy --all-targets --features linalg,parallel,serde with -D warnings and the CI allow-list (-A clippy::too_many_arguments -A clippy::useless_vec -A clippy::type_complexity -A clippy::manual_memcpy -A clippy::wildcard_in_or_patterns, matching rust-ci.yml); and cargo test -p fdars-core --features linalg,parallel,serde. If a gate is red because of a docs/version edit from Tasks 1-3 (e.g. a doctest pinning 0.38, or fmt drift), fix that stale spot and re-run — do NOT alter algorithmic source. Watch the MEMORY.md disk hazards: if a build dies with a bogus "No space left" (doctests link in a small /tmp tmpfs) or the target/ dir has filled /home, free space (rm -rf target/debug/{incremental,examples}) and re-run; these are environment issues, not code failures. Then write the SUMMARY's operator ship-steps: a clearly-labeled section documenting the LOCKED manual final steps the operator runs AFTER this phase — verify all prior phases (78, 79, 80) are green, then bump is already done, so: git tag v0.40.0, git push origin v0.40.0, which triggers release.yml to run cargo publish to crates.io. State explicitly that this phase does NOT run the tag or publish (Ship boundary, 80-CONTEXT.md). Also record in the SUMMARY the exact gate commands run and their pass/fail results, and (if Plan 01 recorded any REL-01 coverage gaps) surface them for backlog promotion.
  </action>
  <verify>
    <automated>cargo fmt --check && cargo clippy --all-targets --features linalg,parallel,serde -- -D warnings -A clippy::too_many_arguments -A clippy::useless_vec -A clippy::type_complexity -A clippy::manual_memcpy -A clippy::wildcard_in_or_patterns && cargo test -p fdars-core --features linalg,parallel,serde 2>&1 | tail -5</automated>
    <fails_when>non-zero exit — fmt not clean, clippy emits a warning under -D warnings, or the test suite has a failing test under the full feature set</fails_when>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --check` exits 0
    - `cargo clippy --all-targets --features linalg,parallel,serde -- -D warnings <CI allow-list>` exits 0
    - `cargo test -p fdars-core --features linalg,parallel,serde` exits 0
    - 80-02-SUMMARY.md contains an operator ship-steps section naming `git tag v0.40.0`, `git push origin v0.40.0`, and release.yml → cargo publish, and states the phase does NOT tag/publish
  </acceptance_criteria>
  <done>All three whole-crate gates are green under the full feature set and the SUMMARY documents the operator's locked manual tag/publish steps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release metadata → crates.io | The version + CHANGELOG this plan prepares become the published crate metadata once the operator tags. This phase stops short of the boundary (no tag, no publish). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-80-02 | Tampering | version/CHANGELOG mismatch | medium | mitigate | key_link asserts Cargo.toml 0.40.0 == the [0.40.0] CHANGELOG heading; both are grep-gated so a mismatch fails verify before the operator ever tags. |
| T-80-03 | Information Disclosure | premature publish | high | mitigate | Ship boundary LOCKED — the phase never runs `git tag`/`cargo publish`; publish is the operator's manual gated step documented in the SUMMARY. |
| T-80-04 | Tampering | over-claiming CHANGELOG (hidden behavior change) | medium | mitigate | Task 2 amends the additive/non-breaking preamble to except the intended soft_dtw behavior change; the verify gate requires a soft_dtw note in the CHANGELOG. |
</threat_model>

<verification>
- `grep -q '^version = "0.40.0"' fdars-core/Cargo.toml` and no `0.38.0` remains.
- CHANGELOG has ordered [0.40.0] → [0.39.0] → [0.38.0] headings; [0.40.0] documents the soft_dtw behavior change.
- No tracked README.md / documentation/*.md file still contains a `0.38` string; README highlights the folded-in AD core.
- `cargo fmt --check`, `cargo clippy --all-targets --features linalg,parallel,serde -- -D warnings <CI allow-list>`, and `cargo test -p fdars-core --features linalg,parallel,serde` all exit 0.
- 80-02-SUMMARY.md documents the operator's manual `git tag v0.40.0` → push → release.yml publish steps and states the phase itself does not tag/publish.
</verification>

<success_criteria>
REL-02 satisfied: fdars-core is versioned 0.40.0; CHANGELOG carries the [0.39.0] and [0.40.0] entries in keepachangelog format; README + documentation/ version strings and highlights are refreshed; all whole-crate gates pass; and the SUMMARY documents the operator's final tag/publish steps — release-ready, without the phase tagging or publishing.
</success_criteria>

<output>
Create `.planning/phases/80-release-hardening-ship-v0-40-0/80-02-SUMMARY.md` when done.

## Artifacts this phase produces

- `fdars-core/Cargo.toml` — version bumped to 0.40.0
- `CHANGELOG.md` — new [0.39.0] and [0.40.0] entries + amended preamble
- `README.md` + `documentation/{ARCHITECTURE,DEVELOPMENT,GETTING-STARTED}.md` — version strings 0.40; README AD highlight
- `80-02-SUMMARY.md` — gate results + the operator's locked manual ship steps (`git tag v0.40.0` → `git push origin v0.40.0` → release.yml runs `cargo publish`), stated as NOT performed by this phase

