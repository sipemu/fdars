---
phase: 49-code-consolidation-dedup
plan: 04
type: execute
wave: 4
depends_on: ["49-03"]
files_modified:
  - fdars-core/src/permutation_test.rs
  - fdars-core/src/lib.rs
  - fdars-core/src/frechet/anova.rs
  - fdars-core/src/inference/permutation.rs
  - fdars-core/src/explain/importance.rs
  - fdars-core/src/famm.rs
  - fdars-core/src/function_on_scalar.rs
  - fdars-core/tests/equivalence_phase49.rs
autonomous: true
requirements: [CONS-02]
estimate:
  tokens: 58000
  raw_tokens: 29000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "The permutation-test scaffold (per-perm reseed shuffle→recompute→count→(1+count)/(1+n_perm), feature-gated parallel) has ONE authoritative pub(crate) definition permutation_pvalue — the CONS-02 permutation consolidation (Plan A)"
    - "frechet_anova (both per-perm loops) is migrated onto permutation_pvalue and stays BIT-IDENTICAL: the Phase-48 goldens golden_frechet_anova_parallel + _below_threshold still pass, and the FRECHET_ANOVA_PERM_PARALLEL_THRESHOLD=200 parallel path is preserved"
    - "The five advancing-RNG / non-StdRng sites (t_perm, f_perm, explain/importance x2, famm, function_on_scalar::fanova) are NOT migrated (would change p-values / uses fixed-42 LCG) and each carries a one-line rationale comment; the explain/importance fold-in from the Phase-48 hand-off is documented as a behavior-CHANGING deferral, not implemented"
  artifacts:
    - fdars-core/src/permutation_test.rs
    - fdars-core/tests/equivalence_phase49.rs
  key_links:
    - "permutation_test.rs::permutation_pvalue ← frechet_anova (both loops)"
    - "equivalence_phase48.rs golden_frechet_anova_* (permanent) + new phase49 frechet golden ← migrated frechet_anova"
---

<objective>
Consolidate the permutation-test scaffold (CONS-02) via RESEARCH Plan A (lowest risk, operator-locked):
ship ONE authoritative pub(crate) `permutation_pvalue` helper (per-perm reseed, feature-gated parallel,
`(1+n_ge)/(1+n_perm)`) and migrate ONLY `frechet_anova` — the single site whose loop already matches the
contract exactly. Leave the five advancing-RNG / non-StdRng sites un-migrated with a documented rationale
(behavior-preservation outranks call-site count — CONTEXT deferred idea #3).

CRITICAL behavior-preservation linchpins (from CONTEXT + RESEARCH):
- t_perm_test / f_perm_test / explain/importance (x2) / famm use a SINGLE ADVANCING StdRng across the loop.
  Per-perm reseed WOULD change their p-values. Do NOT migrate them. famm is additionally multi-statistic
  (a `-> f64` closure cannot express it); function_on_scalar::fanova uses a hardcoded-42 LCG with NO seed
  param (outside the StdRng contract). Document each.
- frechet_anova was parallelized in Phase 48 with per-perm reseed seed.wrapping_add(perm) behind
  FRECHET_ANOVA_PERM_PARALLEL_THRESHOLD=200. Migration MUST keep the Phase-48 goldens bit-identical AND
  preserve the threshold/parallel path; frechet/anova.rs has a SECOND per-perm loop (~line 272, generic
  MetricSpace variant) — migrate it too or document.
- The Phase-48 hand-off "fold explain/importance into the already-parallel generic path" is
  behavior-CHANGING (advancing-RNG vs per-component reseed differ) — record as a correction/deferral, do
  NOT implement it here.

Purpose: one authoritative permutation scaffold; zero behavior change; explicit documented deferrals.
Output: pub(crate) permutation_pvalue; migrated frechet_anova; rationale comments; Phase-48 goldens re-green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/49-code-consolidation-dedup/49-CONTEXT.md
@.planning/phases/49-code-consolidation-dedup/49-RESEARCH.md
@fdars-core/tests/equivalence_phase48.rs
@fdars-core/src/frechet/anova.rs
@fdars-core/src/inference/permutation.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pub(crate) permutation_pvalue helper (per-perm reseed, threshold-gated parallel) + phase49 frechet golden</name>
  <files>fdars-core/src/permutation_test.rs, fdars-core/src/lib.rs, fdars-core/tests/equivalence_phase49.rs</files>
  <read_first>
    - fdars-core/src/frechet/anova.rs (lines ~118, 179-193 primary loop, ~272 second loop; FRECHET_ANOVA_PERM_PARALLEL_THRESHOLD:25; the iter_maybe_parallel! + per-perm reseed + (1+n_ge)/(1+n_perm) + "degenerate perm → count 0" conservative skip)
    - fdars-core/src/parallel.rs (iter_maybe_parallel! macro contract)
    - fdars-core/tests/equivalence_phase48.rs (golden_frechet_anova_parallel statistic 1.17320834419366224e3 / p 1.00000000000000002e-3; _below_threshold p 1.96078431372549017e-2)
    - fdars-core/src/lib.rs (mod region; add `pub(crate) mod permutation_test;` alphabetically — name chosen to avoid colliding with existing inference/permutation.rs)
  </read_first>
  <behavior>
    - permutation_pvalue(observed, n, n_perm, seed, stat) returns (n_ge + 1) as f64 / (n_perm + 1) as f64
    - each perm builds StdRng::seed_from_u64(seed.wrapping_add(perm as u64)) (reuse seed_for_thread from plan 49-03), shuffles 0..n, calls stat(&perm_idx); n_ge counts stat >= observed
    - parallel via iter_maybe_parallel!(0..n_perm).map(...).sum() when n_perm >= threshold (threshold param equals FRECHET_ANOVA_PERM_PARALLEL_THRESHOLD=200), else sequential — .sum() is order-independent because each perm reseeds (bit-identical to sequential)
    - a degenerate-permutation error path is expressible: stat returns f64::NEG_INFINITY on failure so the `>= observed` comparison yields 0 (matches frechet's conservative skip)
  </behavior>
  <action>
    Add `pub(crate) mod permutation_test;` to lib.rs. Create src/permutation_test.rs with
    `pub(crate) fn permutation_pvalue<F>(observed: f64, n: usize, n_perm: usize, seed: u64, threshold: usize, stat: F) -> f64 where F: Fn(&[usize]) -> f64 + Sync`.
    Body: for perm in 0..n_perm, let mut rng = seed_for_thread(seed, perm) (crate::helpers), build a shuffled
    Vec<usize> of 0..n via rng, compute stat(&idx), count how many are `>= observed`; combine via
    iter_maybe_parallel!(0..n_perm).map(count_ge_for_perm).sum::<usize>() behind
    `if n_perm >= threshold { parallel } else { sequential }` (mirror frechet's exact outer-if and the exact
    shuffle call so the draw sequence matches). Return (n_ge + 1) as f64 / (n_perm + 1) as f64. Pass a
    `threshold` param (frechet passes FRECHET_ANOVA_PERM_PARALLEL_THRESHOLD) so the payback point is caller-owned.
    Do NOT migrate any consumer yet. Extend equivalence_phase49.rs with a `frechet` golden (name contains
    `frechet`) mirroring the Phase-48 two_group_densities setup, asserting the SAME frechet_anova statistic +
    p_value_permutation bits as equivalence_phase48.rs (cheap insurance per RESEARCH A3) — captured against
    CURRENT frechet_anova, before Task 2's migration.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --test equivalence_phase49 frechet && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    permutation_pvalue compiles; the phase49 frechet golden passes against CURRENT frechet_anova under
    --features linalg,parallel; clippy --all-targets clean. No consumer migrated yet.
  </acceptance_criteria>
  <done>Authoritative permutation scaffold exists; frechet golden captured pre-migration.</done>
  <reversibility rating="reversible">Additive module; consumers unchanged.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Migrate frechet_anova (both per-perm loops) onto permutation_pvalue; keep Phase-48 goldens bit-identical (CONS-02)</name>
  <files>fdars-core/src/frechet/anova.rs</files>
  <read_first>
    - fdars-core/src/frechet/anova.rs (primary loop 179-193; second loop ~272 generic MetricSpace variant; FRECHET_ANOVA_PERM_PARALLEL_THRESHOLD:25; the degenerate-perm→count-0 skip at 184-187)
    - fdars-core/src/permutation_test.rs (permutation_pvalue signature from Task 1)
  </read_first>
  <action>
    Replace frechet_anova's primary n_ge permutation block (179-193) with a call to
    permutation_pvalue(observed_statistic, n, n_perm, seed, FRECHET_ANOVA_PERM_PARALLEL_THRESHOLD, stat)
    where `stat` shuffles-group-labels-and-recomputes the F-analog and returns f64::NEG_INFINITY on the
    compute_tn_generic error path (reproducing the conservative "degenerate perm → count 0" skip exactly).
    The helper already yields (1+n_ge)/(1+n_perm). Preserve the FRECHET_ANOVA_PERM_PARALLEL_THRESHOLD=200
    outer-if inside the helper (frechet passes the const). Do the SAME for the second per-perm loop (~272,
    generic MetricSpace variant) IF it fits the contract; otherwise leave it and add a one-line rationale
    comment (RESEARCH A2). NO public signature change; NO new dependency. cargo fmt -p fdars-core, then commit
    --no-verify. The PERMANENT Phase-48 goldens (equivalence_phase48.rs: golden_frechet_anova_parallel +
    _below_threshold) AND the new phase49 frechet golden MUST stay bit-identical under BOTH feature configs —
    they are the equivalence gate.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --test equivalence_phase48 golden_frechet_anova && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --test equivalence_phase49 frechet && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --no-default-features --features linalg --test equivalence_phase48 golden_frechet_anova && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    frechet_anova uses permutation_pvalue; both Phase-48 goldens + phase49 frechet golden bit-identical
    under BOTH feature configs; FRECHET_ANOVA_PERM_PARALLEL_THRESHOLD parallel path preserved; second loop
    migrated-or-documented. clippy clean. No signature change; no new dependency.
  </acceptance_criteria>
  <done>frechet_anova on the shared scaffold; Phase-48 + phase49 goldens bit-identical under both configs.</done>
  <reversibility rating="reversible">Code-motion; revert restores frechet's inline loop.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Document the un-migrated sites + the explain/importance fold-in deferral (CONS-02 rationale)</name>
  <files>fdars-core/src/inference/permutation.rs, fdars-core/src/explain/importance.rs, fdars-core/src/famm.rs, fdars-core/src/function_on_scalar.rs</files>
  <read_first>
    - fdars-core/src/inference/permutation.rs (t_perm_test:173, f_perm_test:236 — shared advancing StdRng)
    - fdars-core/src/explain/importance.rs (fpc_permutation_importance:125, _logistic:215 — advancing StdRng across ALL components)
    - fdars-core/src/famm.rs (permutation_test:861 — advancing StdRng, multi-statistic)
    - fdars-core/src/function_on_scalar.rs (fanova:~831 — hardcoded-42 LCG, no seed param)
  </read_first>
  <action>
    Add a concise one-line rationale comment at EACH un-migrated permutation site explaining why it stays on
    its own loop (behavior-preservation), citing the shared permutation_test::permutation_pvalue as the
    authoritative scaffold it deliberately does NOT use:
      - inference/permutation.rs t_perm_test + f_perm_test: "NOT migrated to permutation_pvalue — uses a single
        ADVANCING StdRng across permutations; per-perm reseed would change the p-value (Phase-49 CONS-02 Plan A)."
      - explain/importance.rs fpc_permutation_importance + _logistic: same advancing-RNG rationale; ALSO note the
        Phase-48 hand-off "fold into the already-parallel generic path" is behavior-CHANGING (generic path
        reseeds per-component seed+k; this advances one RNG) → DEFERRED to a future behavior-changing phase with
        a re-baselined golden, NOT done here.
      - famm.rs permutation_test: advancing-RNG AND multi-statistic (per-covariate) — the `-> f64` scaffold
        cannot express it.
      - function_on_scalar.rs fanova: hardcoded-42 LCG, no `seed` param — outside the StdRng contract.
    Comments/docs only — NO logic change at these sites. This satisfies the CONS-02 "documented rationale"
    requirement and records the Phase-48 hand-off correction. cargo fmt -p fdars-core, then commit --no-verify.
    Ensure the negated phrases (e.g. any literal an equivalence gate greps for) are NOT introduced as
    problematic comment text; these are plain rationale comments with no gate coupling.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --no-default-features --features linalg && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    Each of the 5 un-migrated sites carries a one-line rationale comment; the explain/importance fold-in is
    documented as a behavior-changing deferral. No logic change — full suite green under BOTH feature configs;
    clippy --all-targets clean.
  </acceptance_criteria>
  <done>Deferrals + fold-in correction documented in-source; suite bit-identical under both configs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Internal permutation-scaffold refactor; no I/O, no new dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-06 | Tampering | frechet_anova p-values after helper migration | high | mitigate | Permanent Phase-48 goldens + new phase49 frechet golden assert_eq! bit-identical under both configs; threshold path preserved |
| T-49-07 | Tampering | accidental migration of advancing-RNG / LCG sites | high | mitigate | Plan A: migrate ONLY frechet_anova; the 5 incompatible sites are documented-and-excluded, not touched |
</threat_model>

<verification>
- Phase-48 goldens (golden_frechet_anova_parallel + _below_threshold) + phase49 frechet golden: assert_eq! bit-identical, both configs.
- FRECHET_ANOVA_PERM_PARALLEL_THRESHOLD=200 parallel path preserved.
- 5 un-migrated sites each carry a documented rationale; explain/importance fold-in recorded as deferral.
- Full suite green under both feature configs; clippy --all-targets clean; cargo fmt clean.
- No public signature change; no new dependency.
</verification>

<success_criteria>
- pub(crate) permutation_pvalue is the one authoritative permutation scaffold.
- frechet_anova migrated (both loops migrated-or-documented); Phase-48 + phase49 goldens bit-identical, both configs.
- Advancing-RNG/LCG sites documented-and-excluded (Plan A); Phase-48 fold-in correction recorded.
</success_criteria>

<output>
Create `.planning/phases/49-code-consolidation-dedup/49-04-SUMMARY.md` when done (Phase-48 SUMMARY
format). Record Plan A rationale, that frechet_anova (both loops) migrated bit-identically with the
Phase-48 goldens re-green under both configs, and the explicit list of documented-and-excluded sites +
the explain/importance fold-in deferral (Phase-48 hand-off correction). Note phase-level completion:
CONS-01 (plans 01+02) + CONS-02 (plans 03+04) covered.
</output>
