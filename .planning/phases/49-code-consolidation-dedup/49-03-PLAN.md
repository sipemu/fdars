---
phase: 49-code-consolidation-dedup
plan: 03
type: execute
wave: 3
depends_on: ["49-02"]
files_modified:
  - fdars-core/src/helpers.rs
  - fdars-core/src/alignment/generative.rs
  - fdars-core/src/outliers.rs
  - fdars-core/src/scalar_on_function/bootstrap.rs
  - fdars-core/src/tolerance/fpca.rs
  - fdars-core/src/tolerance/equivalence.rs
  - fdars-core/src/spm/bootstrap.rs
  - fdars-core/src/elastic_explain.rs
  - fdars-core/tests/equivalence_phase49.rs
autonomous: true
requirements: [CONS-02]
estimate:
  tokens: 55000
  raw_tokens: 27000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "The per-thread determinism contract seed+k lives in ONE pub(crate) helper seed_for_thread(seed,k) in helpers.rs, body exactly StdRng::seed_from_u64(seed.wrapping_add(k as u64)) — the CONS-02 seeded-RNG consolidation"
    - "Every migrated thread-offset site produces a BIT-IDENTICAL RNG stream (and downstream numeric output) before and after, under both feature configs"
    - "Only the exact seed+k / seed.wrapping_add(k) thread-offset sites are migrated; the ~88 plain-`seed` and LCG-seed calls are NOT touched (different contract)"
  artifacts:
    - fdars-core/src/helpers.rs
    - fdars-core/tests/equivalence_phase49.rs
  key_links:
    - "helpers.rs::seed_for_thread ← the thread-offset sites (alignment/generative, outliers, scalar_on_function/bootstrap, tolerance/fpca, tolerance/equivalence, spm/bootstrap, elastic_explain)"
---

<objective>
Consolidate the per-thread seeded-RNG determinism contract (CONS-02). Ten-ish thread-offset sites
hand-roll `StdRng::seed_from_u64(seed + k)` / `seed.wrapping_add(k)` — the drift-prone determinism
contract. Centralize it in ONE pub(crate) helper `seed_for_thread(seed, k)` in helpers.rs whose body
is EXACTLY `StdRng::seed_from_u64(seed.wrapping_add(k as u64))` — nothing else. Per RESEARCH, use
`.wrapping_add` in the helper: it is bit-identical to `seed + k` for all non-overflowing inputs and
matches the `.wrapping_add` sites verbatim, so both spellings migrate safely.

CRITICAL (behavior-preservation linchpin): preserve the exact offset formula. Migrate ONLY the sites
using the exact per-iteration `seed + k` / `seed.wrapping_add(k)` thread-offset form. Do NOT migrate
the ~88 other `seed_from_u64` calls that use plain `seed` or `seed.wrapping_mul(...)` LCG seeds — those
are not the same contract. Explicitly EXCLUDE frechet/anova.rs:180,272 (owned by plan 49-04's
permutation migration) and the explain_generic per-component/per-i reseeds (importance.rs:66,
shap.rs:78) unless the executor confirms they are pure thread-offset with no other coupling — prefer
to leave the permutation/importance reseeds to their owning plans.

Purpose: one home for the determinism contract; zero behavior change.
Output: pub(crate) seed_for_thread in helpers.rs; migrated thread-offset sites; RNG-stream golden.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/49-code-consolidation-dedup/49-CONTEXT.md
@.planning/phases/49-code-consolidation-dedup/49-RESEARCH.md
@fdars-core/src/helpers.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pub(crate) seed_for_thread(seed,k) to helpers.rs + capture RNG-stream golden from CURRENT formula</name>
  <files>fdars-core/src/helpers.rs, fdars-core/tests/equivalence_phase49.rs</files>
  <read_first>
    - fdars-core/src/helpers.rs (module layout + where pub(crate) RNG/quadrature helpers live; add seed_for_thread here per CONTEXT)
    - fdars-core/tests/equivalence_phase49.rs (extend it — created by plan 49-01; this wave-3 plan has exclusive access)
    - fdars-core/src/frechet/anova.rs:180 and explain_generic/importance.rs:66 (reference `.wrapping_add` sites confirming the formula — do NOT migrate here)
  </read_first>
  <action>
    Add to helpers.rs: `pub(crate) fn seed_for_thread(seed: u64, k: usize) -> StdRng { StdRng::seed_from_u64(seed.wrapping_add(k as u64)) }`
    — body EXACTLY this, nothing else (determinism contract). Import rand::rngs::StdRng + rand::SeedableRng
    as the file's existing conventions require.
    Extend equivalence_phase49.rs with an RNG-stream golden under a name containing `rng_stream`: for a
    fixed (seed, k) pair (e.g. seed=42, k in {0,1,3}), assert that the first N (e.g. 8) u64 draws from
    `seed_for_thread(seed, k)` equal the draws from a freshly-constructed
    `StdRng::seed_from_u64(seed + k as u64)` (the pre-refactor formula). Hard-code the expected u64 draws
    as const captured from the current formula, and assert_eq!. This pins the contract independently of the
    downstream per-site goldens.
    Do NOT migrate any call site in this task (helper + golden only), so the golden gates the helper alone.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --test equivalence_phase49 rng_stream && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    seed_for_thread compiles; rng_stream golden asserts its draws equal StdRng::seed_from_u64(seed+k)
    for the fixed (seed,k) pairs. clippy --all-targets clean. No call site migrated yet.
  </acceptance_criteria>
  <done>Helper exists with the exact wrapping_add(seed,k) body; RNG-stream contract pinned.</done>
  <reversibility rating="reversible">Additive helper; no call site touched.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Migrate the thread-offset sites to seed_for_thread; prove bit-identical under both configs (CONS-02)</name>
  <files>fdars-core/src/alignment/generative.rs, fdars-core/src/outliers.rs, fdars-core/src/scalar_on_function/bootstrap.rs, fdars-core/src/tolerance/fpca.rs, fdars-core/src/tolerance/equivalence.rs, fdars-core/src/spm/bootstrap.rs, fdars-core/src/elastic_explain.rs</files>
  <read_first>
    - The confirmed thread-offset sites (grep to re-verify exact form before editing):
      alignment/generative.rs:108,276 (seed + i); outliers.rs:156 (.wrapping_add(b));
      scalar_on_function/bootstrap.rs:89,212 (.wrapping_add(b)); tolerance/fpca.rs:68,112 (seed + b);
      tolerance/equivalence.rs:29,56,83,108 (seed + b); spm/bootstrap.rs:253 (seed + rep);
      elastic_explain.rs:314,384 (.wrapping_add(p))
    - fdars-core/src/helpers.rs (seed_for_thread signature + import path from each site)
  </read_first>
  <action>
    Re-grep `seed_from_u64(seed` across src/ to reconfirm the exact per-iteration thread-offset sites
    (the list above is the verified candidate set; RESEARCH says ~10 sites). At EACH such site, replace
    `let mut rng = StdRng::seed_from_u64(seed + k as u64);` (or `.wrapping_add(k as u64)`) with
    `let mut rng = seed_for_thread(seed, k);` where `k` is that loop's index variable (i / b / rep / p),
    importing seed_for_thread from crate::helpers. Because the helper uses wrapping_add and the sites use
    either `+` or `.wrapping_add`, the result is bit-identical for all non-overflowing seeds.
    Do NOT touch: any plain `StdRng::seed_from_u64(seed)` (single-RNG) call; any LCG/`.wrapping_mul` seed;
    frechet/anova.rs (plan 49-04); explain_generic/importance.rs:66 + shap.rs:78 (their owning plans, unless
    the executor confirms pure thread-offset with no other coupling and migrates them — record the choice).
    This is code-motion: NO public signature change; NO new dependency. cargo fmt -p fdars-core, then commit
    --no-verify. Every existing seed-dependent test (bootstrap CIs, tolerance intervals, outlier detection,
    generative alignment) + the rng_stream golden must stay green under BOTH feature configs — those tests
    ARE the per-site equivalence gate (RESEARCH: rely on existing per-site goldens which pin downstream output).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --test equivalence_phase49 rng_stream && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --no-default-features --features linalg && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    All confirmed thread-offset sites call seed_for_thread; no plain-`seed` or LCG site touched. rng_stream
    golden + full suite green under BOTH feature configs; clippy --all-targets clean. No signature change;
    no new dependency.
  </acceptance_criteria>
  <done>Determinism contract centralized; all migrated sites bit-identical under both configs.</done>
  <reversibility rating="reversible">Mechanical substitution; revert restores inline seed_from_u64 calls.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Internal determinism-contract refactor; no I/O, no new dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-04 | Tampering | seed_for_thread RNG stream / migrated-site outputs | high | mitigate | rng_stream golden (draws == seed_from_u64(seed+k)) + full seed-dependent suite bit-identical under both configs |
| T-49-05 | Tampering | over-migration onto plain-`seed` / LCG sites | medium | mitigate | Migrate ONLY exact thread-offset form; explicitly exclude plain-seed, LCG, and permutation-owned sites |
</threat_model>

<verification>
- rng_stream golden: seed_for_thread draws == StdRng::seed_from_u64(seed+k) for fixed (seed,k), both configs.
- Full seed-dependent suite (bootstrap, tolerance, outliers, generative) green under both feature configs.
- clippy --all-targets clean; cargo fmt clean.
- Only exact thread-offset sites migrated; plain-seed/LCG/permutation sites untouched.
- No public signature change; no new dependency.
</verification>

<success_criteria>
- pub(crate) seed_for_thread(seed,k) in helpers.rs, body exactly StdRng::seed_from_u64(seed.wrapping_add(k as u64)).
- All confirmed thread-offset sites migrated; RNG stream + downstream output bit-identical under both configs.
</success_criteria>

<output>
Create `.planning/phases/49-code-consolidation-dedup/49-03-SUMMARY.md` when done (Phase-48 SUMMARY
format). List the exact set of migrated sites (and any explain_generic reseed sites deliberately left
to their owning plan), and confirm bit-identity under both feature configs.
</output>
