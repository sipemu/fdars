---
phase: 84-stability-deliverables
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - documentation/STABILITY.md
  - documentation/ROADMAP-TO-1.0.md
autonomous: true
requirements: [STAB-01, STAB-02, STAB-03]

estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A reader can learn from documentation/STABILITY.md what 'stable' means for fdars-core, how deprecations are staged, and the post-1.0 breaking-change policy (STAB-01)."
    - "documentation/STABILITY.md states the two-tier MSRV (crate 1.81, linalg 1.84) and that 1.81 matches fdars-core/Cargo.toml rust-version (STAB-02)."
    - "documentation/ROADMAP-TO-1.0.md enumerates every deferred item — all deferred AUD entries, the golden-test flake, SDTW-O1, DIF-F1/F2/F3, the 1.0-CUT, and fdars-j75 (STAB-03)."
    - "No file under fdars-core/src/ is modified by this phase."
  artifacts:
    - documentation/STABILITY.md
    - documentation/ROADMAP-TO-1.0.md
  key_links:
    - "STABILITY.md MSRV numbers (1.81) stay consistent with fdars-core/Cargo.toml rust-version."
    - "ROADMAP-TO-1.0.md deferred-item list stays faithful to the 81-AUDIT-INVENTORY.md ## Approval DEFERRED section."
---

<objective>
Author the three non-code stability deliverables that will govern the eventual (future, separate) fdars-core 1.0 cut, all as NEW tracked files under `documentation/`:
- STAB-01: a semver + API-stability policy (`documentation/STABILITY.md`).
- STAB-02: an MSRV policy (two-tier: crate 1.81, `linalg` 1.84), co-located in `documentation/STABILITY.md`, kept consistent with `fdars-core/Cargo.toml`.
- STAB-03: a 1.0 gap checklist (`documentation/ROADMAP-TO-1.0.md`) enumerating everything deferred out of this milestone.

Purpose: Settle the written stability contract now (while still in 0.x) so a future 1.0 milestone has an approved policy and a concrete, scoped gap checklist to clear.
Output: `documentation/STABILITY.md` and `documentation/ROADMAP-TO-1.0.md`. NO change to `fdars-core/src`, `Cargo.toml`, or any code.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-stability-deliverables/84-CONTEXT.md

# Source-of-truth for the deferred items STAB-03 must enumerate:
@.planning/phases/81-api-audit-deprecated-form-removal/81-AUDIT-INVENTORY.md

# Existing tracked docs — match their tone/format (header comment, # Title, --- separators, ## sections):
@documentation/DEVELOPMENT.md
@documentation/ARCHITECTURE.md

# MSRV source of truth (do NOT modify):
@fdars-core/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author documentation/STABILITY.md (STAB-01 semver/API-stability policy + STAB-02 MSRV policy)</name>
  <files>documentation/STABILITY.md</files>
  <read_first>
    - .planning/phases/84-stability-deliverables/84-CONTEXT.md §STAB-01 and §STAB-02 (the authoritative content spec)
    - fdars-core/Cargo.toml (confirm rust-version is 1.81 — the number the doc must cite for the crate MSRV)
    - documentation/DEVELOPMENT.md (match the doc house style: leading `<!-- generated-by: ... -->` comment optional, `# Title`, `---` separators, `##` section headings, fenced blocks for code/toml)
  </read_first>
  <action>
    Create the NEW file documentation/STABILITY.md as prose documentation matching the existing documentation/ house style. This is a policy document, not code — write it as clear reference prose.

    Cover, per D (CONTEXT §STAB-01):
    - A "What 'stable' means" section defining the public API that semver governs: re-exported items, public fn signatures, public types/enums/fields. Explicitly state that internal `pub(crate)` items AND the `wire` module (a public-but-unwired interchange seam) are OUTSIDE the stability guarantee until 1.0.
    - A "Deprecation process" section: breaking changes are staged via `#[deprecated(since = "...")]` then removed in a later release; note that under 0.x, breaking changes ship in MINOR bumps (as v0.41.0 did — the first breaking release after a long additive run).
    - A "Post-1.0 breaking-change policy" section: what constitutes breaking (signature / visibility / enum-variant / struct-field changes); the role of `#[non_exhaustive]` (applied across public enums + result structs in Phase 82) in keeping additions non-breaking; and that MSRV bumps are a minor-version event (link to the MSRV section).
    - A short "Stability surface conventions" section referencing existing crate conventions from CLAUDE.md as part of the stable surface: all public types derive Debug/Clone/PartialEq, `#[must_use]` on expensive computations, and the column-major FdMatrix invariant.

    Cover, per D (CONTEXT §STAB-02) — a dedicated "MSRV Policy" section in the SAME file:
    - The two-tier MSRV: crate MSRV = 1.81 (set for CRAN Windows compatibility), `linalg` feature MSRV = 1.84 (faer 0.23+ requirement).
    - Why each tier exists (CRAN Windows for 1.81; faer for 1.84).
    - The bump policy: an MSRV bump is a minor-version event, called out in the CHANGELOG.
    - A consistency statement: the crate MSRV documented here (1.81) matches `fdars-core/Cargo.toml` `rust-version`. The 1.84 `linalg` requirement lived only in CLAUDE.md until now — this doc surfaces it in the tracked docs. Do NOT edit Cargo.toml; rust-version stays 1.81.

    Write the literal tokens `1.81` and `1.84` into the MSRV section. Do NOT place fenced code blocks inside this plan; write them directly in the doc file.
  </action>
  <acceptance_criteria>
    - documentation/STABILITY.md exists and contains sections covering: definition of "stable"/public surface, deprecation process, post-1.0 breaking-change policy, and MSRV policy.
    - The doc explicitly names the `wire` module and `pub(crate)` items as outside the pre-1.0 stability guarantee.
    - The doc references `#[non_exhaustive]` and `#[must_use]` conventions.
    - The MSRV section contains both `1.81` and `1.84`, and `1.81` is stated as matching Cargo.toml rust-version.
    - No file under fdars-core/src/ and no Cargo.toml is modified.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && test -f documentation/STABILITY.md && grep -q '1.81' documentation/STABILITY.md && grep -q '1.84' documentation/STABILITY.md && grep -q "$(grep -oE 'rust-version = "[0-9.]+"' fdars-core/Cargo.toml | grep -oE '[0-9]+\.[0-9]+')" documentation/STABILITY.md && grep -qiE 'wire|non_exhaustive|non-exhaustive' documentation/STABILITY.md && grep -qiE 'deprecat' documentation/STABILITY.md && git diff --quiet -- fdars-core/src/ fdars-core/Cargo.toml</automated>
    <fails_when>documentation/STABILITY.md is missing; OR the literal `1.81` or `1.84` is absent; OR the crate rust-version from Cargo.toml does not appear verbatim in the doc; OR no mention of wire/non_exhaustive; OR no mention of deprecation; OR any change is staged under fdars-core/src/ or in Cargo.toml.</fails_when>
  </verify>
  <done>documentation/STABILITY.md is a complete, house-style policy document covering STAB-01 and STAB-02, MSRV numbers match Cargo.toml, and no code/manifest is touched.</done>
</task>

<task type="auto">
  <name>Task 2: Author documentation/ROADMAP-TO-1.0.md (STAB-03 — 1.0 gap checklist enumerating every deferred item)</name>
  <files>documentation/ROADMAP-TO-1.0.md</files>
  <read_first>
    - .planning/phases/84-stability-deliverables/84-CONTEXT.md §STAB-03 (the authoritative list of what MUST appear)
    - .planning/phases/81-api-audit-deprecated-form-removal/81-AUDIT-INVENTORY.md ## Approval → the "DEFERRED — route to STAB-03" block (source of truth for AUD-09/12/13, AUD-19..23)
    - documentation/DEVELOPMENT.md (match doc house style; checklist entries as markdown task-list items `- [ ]`)
  </read_first>
  <action>
    Create the NEW file documentation/ROADMAP-TO-1.0.md as a house-style markdown checklist of everything remaining before a real 1.0 cut. Each item is a `- [ ]` entry with a short description and, where it exists, its identifier token. Group into clear `##` sections. Write it as an enumeration only — this doc does NOT execute any item (that is the future 1.0 milestone).

    Include EVERY item below (the tokens in backticks MUST appear verbatim in the file):
    - Deferred audit items (from the 81 inventory ## Approval DEFERRED block):
      - `AUD-09` + `AUD-13`: the `wire` module — decide before 1.0 whether to wire it up (JS/R interchange) or seal it `pub(crate)`; currently public-but-unused.
      - `AUD-12`: add `#[non_exhaustive]` to the ~22 public config structs, paired with a builder / `Default` + `..default()` construction path (feature work, not cleanup).
      - `AUD-19` (geometric_median), `AUD-20` (hausdorff), `AUD-21` (functional_spatial), `AUD-22` (the large lone-`_1d`/`_2d` suffix batch across the crate + all 28 examples), `AUD-23` (`LpeerResult` → `LocalPeerResult`) — optional naming not done in Phase 83.
    - Known test/quality debt:
      - The co_cluster / svd_sign `golden`-test flake: `golden_co_cluster_parallel`, `golden_co_cluster_below_threshold` (equivalence_phase48), `svd_sign_fpca_two_matrix_bit_identical` (equivalence_phase49) — pass per-binary but flake under full parallel `cargo test`; make deterministic (tolerance vs bit-identity, or serialize) before 1.0.
    - Pre-existing deferred backlog:
      - `SDTW-O1`: `soft_dtw_barycenter` optimizer replacement (the MM-step / global-optimizer improvement — note the soft_dtw_backward zero-gradient bug itself was already FIXED in v0.40.0 / CORR-01).
      - Differentiable-core expansion: `DIF-F1` (reverse-mode / VJP), `DIF-F2` (broaden the differentiable subset), `DIF-F3` (generic f64 hot-path signatures).
    - The 1.0-CUT itself: bump to `1.0.0` and declare the API stable once this checklist clears (governed by STABILITY.md).
    - `fdars-r` FdMatrix migration (issue `fdars-j75`): external R wrapper migration — separate package, out of `fdars-core` scope, a 1.0-ecosystem gap.

    Do NOT list the serde/ClassifFit build as an open gap — it was repaired in v0.40.0 and is GREEN.
    Cross-link to documentation/STABILITY.md as the governing policy. Do NOT inline fenced code blocks in this plan; author the checklist directly in the doc.
  </action>
  <acceptance_criteria>
    - documentation/ROADMAP-TO-1.0.md exists as a markdown checklist grouped into `##` sections.
    - It contains every required token verbatim: `AUD-09`, `AUD-12`, `AUD-13`, `AUD-19`, `AUD-20`, `AUD-21`, `AUD-22`, `AUD-23`, `wire`, `golden`, `SDTW-O1`, `DIF-F1`, `DIF-F2`, `DIF-F3`, `1.0.0`, `fdars-j75`.
    - It does NOT list the serde/ClassifFit breakage as an open gap.
    - No file under fdars-core/src/ is modified.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && test -f documentation/ROADMAP-TO-1.0.md && for t in AUD-09 AUD-12 AUD-13 AUD-19 AUD-20 AUD-21 AUD-22 AUD-23 wire golden SDTW-O1 DIF-F1 DIF-F2 DIF-F3 1.0.0 fdars-j75; do grep -qF "$t" documentation/ROADMAP-TO-1.0.md || { echo "MISSING: $t"; exit 1; }; done && git diff --quiet -- fdars-core/src/</automated>
    <fails_when>documentation/ROADMAP-TO-1.0.md is missing; OR any required token (AUD-09, AUD-12, AUD-13, AUD-19, AUD-20, AUD-21, AUD-22, AUD-23, wire, golden, SDTW-O1, DIF-F1, DIF-F2, DIF-F3, 1.0.0, fdars-j75) is absent; OR any change is staged under fdars-core/src/.</fails_when>
  </verify>
  <done>documentation/ROADMAP-TO-1.0.md enumerates every deferred item named in CONTEXT §STAB-03 (each token present), lists no already-fixed items as open, and no code is touched.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

- `documentation/STABILITY.md` — semver + API-stability policy (STAB-01) and MSRV policy (STAB-02).
- `documentation/ROADMAP-TO-1.0.md` — 1.0 gap checklist enumerating every deferred item (STAB-03).

Both are NEW tracked files under `documentation/` (the tracked docs dir — NOT `docs/`, which is gitignored). No `fdars-core/src`, `Cargo.toml`, or other code is modified.
</artifacts_produced>

<verification>
Phase-level checks (docs-only — no whole-crate gates; those belong to Phase 85):
- `test -f documentation/STABILITY.md && test -f documentation/ROADMAP-TO-1.0.md`
- MSRV numbers `1.81` and `1.84` present in STABILITY.md; `1.81` matches `fdars-core/Cargo.toml` rust-version.
- Every STAB-03 required token present in ROADMAP-TO-1.0.md (see Task 2 verify).
- `git diff --quiet -- fdars-core/src/ fdars-core/Cargo.toml` — asserts no source/manifest change.
- Optional sanity: `cargo fmt --check` (no source touched — should be clean).

Commit hazard (MEMORY): the pre-commit hook runs the full test suite and times out (~30s / linking in a small /tmp tmpfs). Since this phase touches only `documentation/`, commit with `git commit --no-verify`.
</verification>

<success_criteria>
- STAB-01: documentation/STABILITY.md defines "stable", the public surface (with `wire`/`pub(crate)` excluded pre-1.0), the deprecation process, and the post-1.0 breaking-change policy.
- STAB-02: documentation/STABILITY.md documents the two-tier MSRV (1.81 crate / 1.84 linalg) consistent with Cargo.toml.
- STAB-03: documentation/ROADMAP-TO-1.0.md enumerates all deferred items (AUD-09/12/13, AUD-19..23, the co_cluster/svd_sign golden flake, SDTW-O1, DIF-F1/F2/F3, the 1.0-CUT, fdars-j75).
- No `fdars-core/src` or `Cargo.toml` change.
</success_criteria>

<output>
Create `.planning/phases/84-stability-deliverables/84-01-SUMMARY.md` when done.
</output>
