---
phase: 31-additive-functional-regression-variable-selection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/scalar_on_function/additive.rs
  - fdars-core/src/scalar_on_function/mod.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [REG-04]
estimate:
  tokens: 78000
  raw_tokens: 39000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "User can call fam(), fregre_gkam(), fregre_gsam() as crate-root re-exported Result-returning entry points (REG-04)"
    - "FAM recovers a known additive signal on synthetic FPC-score data (R2 above mean-only baseline) via a single forward pass, no backfitting loop"
    - "GKAM iteratively backfits NW smoothers over L2 curve distances and reports converged/iterations"
    - "GSAM with Gaussian identity link produces the same fitted values as FAM (within 1e-6)"
    - "Invalid inputs (empty matrix / mismatched y length / mismatched argvals / ncomp too large) return FdarError, never panic"
  artifacts:
    - fdars-core/src/scalar_on_function/additive.rs
  key_links:
    - "additive.rs -> regression::fdata_to_pc_1d (FPC scores for FAM/GSAM)"
    - "additive.rs -> smoothing::{nadaraya_watson, optim_bandwidth, CvCriterion}"
    - "additive.rs -> super::nonparametric::{compute_pairwise_distances, gaussian_kernel} (pub(super), no visibility change)"
    - "scalar_on_function/mod.rs pub use + lib.rs crate-root re-export"
---

<objective>
Establish the new `scalar_on_function/additive.rs` module with its shared config/result type conventions and wire in the first three additive estimators — FAM (the end-to-end tracer), GKAM, and GSAM — each `Result<T, FdarError>`-returning, crate-root re-exported, with inline `#[cfg(test)]` synthetic-recovery, invariant, and invalid-input tests.

Purpose: FAM proves the whole architecture end-to-end (new file → FPC scores → NW smooth → structured result → module wiring → crate re-export → green tests) on the agent's best early-context tokens, before expanding to the kernel (GKAM) and spectral (GSAM) siblings and, in Plan 02, to variable_selection / permutation / history-index.
Output: `additive.rs` containing `fam`/`fregre_gkam`/`fregre_gsam` plus their config/result types, with `mod.rs` and `lib.rs` re-exports in place.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/31-additive-functional-regression-variable-selection/31-RESEARCH.md
@.planning/phases/31-additive-functional-regression-variable-selection/31-PATTERNS.md

@fdars-core/src/scalar_on_function/mod.rs
@fdars-core/src/scalar_on_function/nonparametric.rs
@fdars-core/src/scalar_on_function/fregre_lm.rs
@fdars-core/src/smoothing.rs
@fdars-core/src/regression.rs
</context>

<build_note>
Doctest/build linking requires `TMPDIR=/home/simonm/.cache/fdars-bench-tmp` — the system /tmp is a tiny tmpfs that exhausts and yields a bogus "No space left on device". Export it before any `cargo test` / `cargo clippy` run in this plan:
`export TMPDIR=/home/simonm/.cache/fdars-bench-tmp`
If example linking fails with "No space left", `rm -rf target/debug/{incremental,examples}` frees space (this plan adds no examples, so risk is low).
</build_note>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: FAM tracer — new module wired end-to-end (file -> FPC scores -> NW smooth -> result -> re-exports -> green tests)</name>
  <files>fdars-core/src/scalar_on_function/additive.rs, fdars-core/src/scalar_on_function/mod.rs, fdars-core/src/lib.rs</files>
  <behavior>
    - fam_synthetic_recovery: curves projected via fdata_to_pc_1d give scores; y = sin(xi_1) + xi_2^2 + noise; fit fam(ncomp=2) recovers signal — assert r_squared > 0.75 and relative fitted error < 0.30.
    - fam_decomposition_identity: fitted_values + residuals == y elementwise (within 1e-9).
    - fam_output_shapes: component_fits.len() == result.ncomp; each inner Vec has length n; bandwidths.len() == ncomp.
    - fam_invalid_dimension: empty FdMatrix, y of wrong length, and argvals of wrong length each return FdarError (InvalidDimension), not a panic.
  </behavior>
  <action>
    Create `fdars-core/src/scalar_on_function/additive.rs`. Add file-level imports per 31-PATTERNS.md section 1: `crate::error::FdarError`, `crate::matrix::FdMatrix`, `crate::regression::{fdata_to_pc_1d, FpcaResult}`, `crate::smoothing::{nadaraya_watson, optim_bandwidth, CvCriterion}`. Do NOT place `use rand::prelude::*` at module top level (clippy unused-import on non-permutation paths — Pitfall 5).

    Define `FamConfig` and `FamResult` following 31-PATTERNS.md sections 2-3: `#[derive(Debug, Clone, PartialEq)]` + `#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]`; `#[non_exhaustive]` on both; `impl Default for FamConfig` with explicit field-by-field init (ncomp=0 meaning auto, bandwidth=0.0 meaning per-component GCV auto, kernel="gaussian", n_grid_bandwidth=20). Field set and doc comments per 31-RESEARCH.md "Config Structs" / "Result Structs" (FamResult carries fitted_values, residuals, component_fits, intercept, bandwidths, ncomp, r_squared, and embedded fpca).

    Implement `fam(data, y, argvals, scalar_covariates, config)` with the exact `#[must_use = "expensive computation whose result should not be discarded"]` string. Validate at entry (n, m from data.shape(); y.len()==n; argvals.len()==m; scalar_covariates rows==n) using the FdarError::InvalidDimension pattern from mod.rs. Resolve ncomp (0 => auto-select, capped at min(n,m)); return FdarError::InvalidParameter if an explicitly-provided ncomp exceeds min(n,m). Run fdata_to_pc_1d for FPC scores. Compute mu_y = mean(y). Fit each component with ONE forward pass (k=0..ncomp): build partial residual = y - mu_y - sum_{j!=k} component_fits[j]; pick per-component bandwidth via optim_bandwidth(..., CvCriterion::Gcv, ...).h_opt when config.bandwidth==0.0 else use config.bandwidth; component_fits[k] = nadaraya_watson(score_k, partial, score_k, h, kernel). Do NOT add a convergence loop — FPC scores are uncorrelated so one pass equals infinite-iteration backfitting (31-RESEARCH.md Pattern 1 / Pitfall 1). Assemble fitted = mu_y + sum_k component_fits[k]; residuals = y - fitted; r_squared via super::compute_r_squared(y, &residuals, ncomp).0. Fold scalar_covariates in as additional additive score columns (no separate overload — parameter-ordering convention). Every NW site guards `if den > 1e-15 { .. } else { fallback }` (denominator handled inside nadaraya_watson; guard any inline sites).

    Document R-baseline divergence in rustdoc: fdars uses `optim_bandwidth` GCV grid search (not fdapace PACE) and no backfitting iteration (per Müller & Yao FPC-uncorrelatedness).

    Wire the module: in `scalar_on_function/mod.rs` add `mod additive;` after the last existing `mod` and a `pub use additive::{ fam, FamConfig, FamResult };` block (alphabetic; will be extended by later tasks). In `src/lib.rs` extend the existing single `pub use scalar_on_function::{...}` block (lines ~251-261) by appending `fam, FamConfig, FamResult` — do NOT add a second `pub use scalar_on_function` block.

    Add the `#[cfg(test)] mod tests { use super::*; use crate::test_helpers::uniform_grid; }` module with the four tests from `<behavior>`. Keep any rand imports inside test functions or the test module only.
  </action>
  <verify>
    <automated>export TMPDIR=/home/simonm/.cache/fdars-bench-tmp && cargo test -p fdars-core --features linalg,parallel additive::tests::fam</automated>
  </verify>
  <done>`fam` compiles and is crate-root re-exported; all four fam_* tests pass; fitted_values + residuals reconstruct y; invalid inputs return FdarError.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GKAM — iterative hat-matrix backfitting over L2 curve distances</name>
  <files>fdars-core/src/scalar_on_function/additive.rs, fdars-core/src/scalar_on_function/mod.rs, fdars-core/src/lib.rs</files>
  <behavior>
    - gkam_r2_synthetic: two functional covariates with known Gaussian L2-kernel dependence; assert r_squared > 0.70.
    - gkam_convergence: on smooth synthetic data, result.converged == true and iterations <= config.max_iter.
    - gkam_invalid_inputs: mismatched predictor/response lengths and an empty predictor list return FdarError, not a panic.
  </behavior>
  <action>
    Add `GkamConfig` (bandwidth=0.0 => LOO-CV auto, kernel="gaussian", max_iter=50, epsilon=1e-6) and `GkamResult` (fitted_values, residuals, component_fits (q x n), intercept, bandwidths (len q), iterations, converged, r_squared) with the same derive/serde/non_exhaustive attribute stack as FAM types. `converged: bool` + `iterations: usize` mirror FregreRobustResult (31-PATTERNS.md section 3).

    Implement `fregre_gkam(predictors: &[&FdMatrix], y, argvals_list: &[&[f64]], scalar_covariates, config)` with the `#[must_use = ...]` string. Validate: non-empty predictors; predictors.len()==argvals_list.len(); every predictors[k].nrows()==y.len() (else FdarError::InvalidDimension); each argvals_list[k].len()==predictors[k].ncols(). Import `super::nonparametric::{compute_pairwise_distances, gaussian_kernel}` (pub(super) — confirmed accessible from this sibling file, no visibility change). Per covariate k, compute the L2 distance matrix once via compute_pairwise_distances; select per-covariate bandwidth by LOO-CV over the distance matrix when config.bandwidth==0.0. Initialize f_k = 0 for all k. Iterate up to max_iter: for each k set adjusted[i] = y[i] - mu - sum_{j!=k} f_j[i], then update f_k[i] via an inline NW-on-distance loop applying gaussian_kernel(dist_k[i*n+j], h_k) directly (31-PATTERNS.md section 6) with the `if den > 1e-15 { num/den } else { adjusted[i] }` guard. Track max component delta; set converged=true and break when delta < epsilon. Do NOT materialize `smoothing_matrix_nw` inside the inner loop (Pitfall / anti-pattern — apply weights directly, O(n) per point). Assemble fitted = mu + sum_k f_k; r_squared via compute_r_squared.

    Document R-baseline divergence in rustdoc: fdars performs the equivalent iterative update without materializing the full n×n hat matrix, and implements only the Gaussian identity link (logit/log require IRLS — documented as a known gap).

    Extend the `mod.rs` and `lib.rs` re-export lists to add `fregre_gkam, GkamConfig, GkamResult`. Add the three gkam_* tests to the inline test module.
  </action>
  <verify>
    <automated>export TMPDIR=/home/simonm/.cache/fdars-bench-tmp && cargo test -p fdars-core --features linalg,parallel additive::tests::gkam</automated>
  </verify>
  <done>`fregre_gkam` is crate-root re-exported; gkam_r2_synthetic (R2>0.70), gkam_convergence (converged, bounded iterations), and gkam_invalid_inputs (FdarError) all pass; no full hat matrix built inside the backfitting loop.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GSAM — FPC-score additive smooth, Gaussian identity link, FAM-equivalent</name>
  <files>fdars-core/src/scalar_on_function/additive.rs, fdars-core/src/scalar_on_function/mod.rs, fdars-core/src/lib.rs</files>
  <behavior>
    - gsam_matches_fam_identity: with Gaussian identity link and equivalent config, gsam fitted_values match fam fitted_values within 1e-6 on the same synthetic data.
    - gsam_ncomp_too_large: explicitly-provided ncomp > min(n,m) returns FdarError::InvalidParameter.
    - gsam_output_shapes: component_fits.len() == result.ncomp; fitted_values.len() == n.
  </behavior>
  <action>
    Add `GsamConfig` (ncomp=0 => auto, bandwidth=0.0 => per-component GCV auto, kernel="gaussian", n_grid_bandwidth=20) and `GsamResult` (fitted_values, residuals, component_fits (K x n), intercept, bandwidths, ncomp, r_squared, embedded fpca) with the standard attribute stack.

    Implement `fregre_gsam(data, y, argvals, scalar_covariates, config)` with the `#[must_use = ...]` string. GSAM is structurally FAM with an optional link; implement ONLY the Gaussian identity link. Reuse the same FPC-score additive-smooth path as `fam` (share a private helper if it reduces duplication; keep both public functions thin). Validate inputs identically to FAM, including the ncomp > min(n,m) => FdarError::InvalidParameter check exercised by gsam_ncomp_too_large.

    Document R-baseline divergence in rustdoc: R's fregre.gsam delegates to mgcv penalized splines; fdars uses Nadaraya-Watson smoothing on FPC scores (same model class, different smoother), numerically FAM-equivalent for the Gaussian identity case; non-Gaussian links are a documented known gap.

    Extend the `mod.rs` and `lib.rs` re-export lists to add `fregre_gsam, GsamConfig, GsamResult`. Add the three gsam_* tests to the inline test module.
  </action>
  <verify>
    <automated>export TMPDIR=/home/simonm/.cache/fdars-bench-tmp && cargo test -p fdars-core --features linalg,parallel additive::tests::gsam</automated>
  </verify>
  <done>`fregre_gsam` is crate-root re-exported; gsam_matches_fam_identity (within 1e-6), gsam_ncomp_too_large (FdarError), and gsam_output_shapes all pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → additive.rs public fn | Untrusted numeric input (matrix shape, y length, argvals, config bounds) crosses into computation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Tampering | fam/gkam/gsam input validation | medium | mitigate | Entry-point FdarError::InvalidDimension on n/m/y.len()/argvals mismatch; FdarError::InvalidParameter on ncomp > min(n,m); no silent truncation |
| T-31-02 | Tampering | NW denominator in FAM/GKAM/GSAM smooths | medium | mitigate | `if den > 1e-15 { num/den } else { fallback }` guard at every NW site; never emit NaN |
| T-31-03 | Denial of Service | GKAM backfitting loop | low | mitigate | GkamConfig.max_iter (default 50) bounds iteration; no recursion |
| T-31-04 | Tampering | history/index column arithmetic (Plan 02) | low | accept | Deferred to Plan 02 (history_index); tracked there |
| T-31-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages added this phase — Package Legitimacy Audit N/A per 31-RESEARCH.md |
</threat_model>

<verification>
- `export TMPDIR=/home/simonm/.cache/fdars-bench-tmp && cargo test -p fdars-core --features linalg,parallel additive` — all FAM/GKAM/GSAM tests green.
- `cargo test -p fdars-core --features linalg,parallel` — full suite green (no existing signature regressions; additive/non-breaking).
- `cargo clippy --all-targets --features linalg,parallel -- -D warnings` — clippy green including test-code.
</verification>

<success_criteria>
- `fam`, `fregre_gkam`, `fregre_gsam` and their config/result types are crate-root re-exported and `Result<T, FdarError>`-returning.
- FAM recovers a known additive signal via a single forward pass; GKAM converges on smooth data; GSAM matches FAM under the identity link.
- All invalid-input paths return FdarError, never panic.
- Existing `scalar_on_function/` and `fdata_to_pc_1d` signatures unchanged; full suite + `--all-targets` clippy green.
</success_criteria>

<output>
Create `.planning/phases/31-additive-functional-regression-variable-selection/31-01-SUMMARY.md` when done.
</output>
