---
phase: 33-model-based-density-functional-clustering
plan: 03
type: execute
wave: 3
depends_on: ["33-02"]
files_modified:
  - fdars-core/src/clustering_advanced.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [CLUS-01]
estimate:
  tokens: 74000
  raw_tokens: 37000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "funfem_cluster recovers two well-separated functional groups with ARI >= 0.90"
    - "funfem_cluster returns FdarError on k=0, k>n, empty data, mismatched argvals, and invalid ncomp/p_disc"
    - "align_cluster_fd recovers a shape-shifted (time-warped) group with ARI >= 0.90 where amplitude-naive k-means would fail"
    - "align_cluster_fd returns FdarError on k=0, k>n, empty data, mismatched argvals"
    - "full clusterer suite builds and existing tests stay green (strictly additive)"
  artifacts:
    - "funFEM (FunFemConfig/FunFemResult/funfem_cluster) appended to fdars-core/src/clustering_advanced.rs + inline tests"
    - "align-and-cluster (AlignClusterConfig/AlignClusterResult/align_cluster_fd) appended to fdars-core/src/clustering_advanced.rs + inline tests"
  key_links:
    - "funfem_cluster consumes regression::fdata_to_pc_1d + gmm/em log-sum-exp pattern + linalg Cholesky for W^{-1}B; align_cluster_fd consumes alignment::{karcher_mean, amplitude_distance, elastic_distance}"
    - "lib.rs clustering_advanced re-export block (created in 33-02) extended with funfem_cluster/align_cluster_fd + their config/result structs"
    - "clustering_advanced::tests use crate::test_helpers::adjusted_rand_index from plan 33-00"
---

<objective>
Complete CLUS-01 by adding the final two clusterers to clustering_advanced.rs: funFEM (Fisher-EM
discriminative-subspace clustering on FPC scores) and joint align-and-cluster (elastic k-means:
Karcher-mean templates + elastic-distance reassignment, the only shape-invariant clusterer in the
phase). Extend the crate-root re-export block created in plan 33-02.

Implements CLUS-01 (funFEM + align-and-cluster portions) — closing the five-clusterer set.

Purpose: Deliver discriminative-subspace and shape-invariant clustering — the two paths that reuse
the FPCA/Fisher machinery and the elastic alignment machinery respectively — completing the phase.
Output: clustering_advanced.rs (+funFEM +align-and-cluster); lib.rs re-export extension.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-model-based-density-functional-clustering/33-RESEARCH.md
@.planning/phases/33-model-based-density-functional-clustering/33-PATTERNS.md

@fdars-core/src/clustering_advanced.rs
@fdars-core/src/regression.rs
@fdars-core/src/linalg.rs
@fdars-core/src/gmm/em.rs
@fdars-core/src/alignment/mod.rs
@fdars-core/src/alignment/karcher.rs
@fdars-core/src/alignment/pairwise.rs
@fdars-core/src/test_helpers.rs
</context>

<execution_notes>
BUILD NOTE: export `TMPDIR=/home/simonm/.cache/fdars-bench-tmp` before ANY cargo command.

ANTI-STALL: run ONLY lib-scoped filtered tests — `cargo test -p fdars-core --features linalg,parallel
--lib <filter>`. Never `--all-targets` or unfiltered. Commit early/often with `git commit --no-verify`.
Align-and-cluster tests are the slowest in the phase (elastic DP + Karcher mean); keep n_per small
(e.g. 6-8) and m modest (e.g. 30-40) in tests to stay well under the feedback-latency budget.

DEPENDS ON 33-02: appends to clustering_advanced.rs (created in 33-02) and extends the lib.rs
re-export block (created in 33-02) — hence Wave 3, strictly after 33-02. Reuses
`crate::test_helpers::adjusted_rand_index` (from 33-00).

DOCTEST RULE (learned Phase 31/32): rustdoc `///` doctests constructing a `#[non_exhaustive]` config
MUST use field-assignment form; INLINE `#[cfg(test)]` tests MUST use struct-literal form. Apply both.

VERIFIED SIGNATURES (this session — DO NOT reinvent):
- `alignment::karcher_mean(data: &FdMatrix, argvals: &[f64], max_iter: usize, tol: f64, lambda: f64) -> KarcherMeanResult`
  — returns a struct (NOT Result); the template curve is the `.mean` field (length m). Also available:
  karcher_mean_with_band(.., band_frac: Option<f64>) for a speed knob (pass None for exact).
- `alignment::amplitude_distance(f1: &[f64], f2: &[f64], argvals: &[f64], lambda: f64) -> f64` (shape/amplitude distance after optimal warp).
- `alignment::elastic_distance(f1: &[f64], f2: &[f64], argvals: &[f64], lambda: f64) -> f64` (full elastic/Fisher-Rao distance).
  All three are already re-exported from `crate::alignment`.
- `regression::fdata_to_pc_1d(data, ncomp, argvals) -> Result<FpcaResult, FdarError>`; clamps ncomp.min(n).min(m).
  `FpcaResult { singular_values, rotation, scores, mean, centered }`.
- gmm/em.rs `e_step`/`compute_bic` etc. are `pub(super)` and NOT accessible from clustering_advanced.rs
  (different parent). For funFEM's log-sum-exp responsibility normalization, inline a ~15-line
  log-sum-exp normalizer here (do NOT try to import gmm pub(super) helpers; do NOT widen them).
- `linalg` is pub(crate): use `crate::linalg::{cholesky_d / cholesky_factor / cholesky_solve}` for the
  W_soft^{-1} B_soft computation. Confirm the exact linalg fn names by reading linalg.rs before use.
</execution_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: funFEM discriminative-subspace (Fisher-EM) clustering</name>
  <files>fdars-core/src/clustering_advanced.rs</files>
  <behavior>
    - test_funfem_recovery: two well-separated groups → ARI >= 0.90 vs ground truth
    - test_funfem_deterministic: same seed → identical assignment across two runs
    - test_funfem_invalid: k=0 → Err; k>n → Err; empty data → Err; argvals.len()!=m → Err; ncomp==0 → Err
  </behavior>
  <action>Append funFEM to clustering_advanced.rs per 33-RESEARCH.md "Algorithm Formulations §2".
    Public surface: `FunFemConfig { k: usize, ncomp: usize, p_disc: usize, max_iter: usize, tol: f64, seed: u64 }`
    with Default (k=2, ncomp=10, p_disc=0 meaning "auto = min(k-1, ncomp)", max_iter=50, tol=1e-6, seed=42);
    `FunFemResult { cluster: Vec<usize>, membership: FdMatrix, disc_subspace: FdMatrix, log_likelihood: f64, iterations: usize, converged: bool }`;
    `funfem_cluster(data, argvals, config) -> Result<FunFemResult, FdarError>`. Config derives
    Debug/Clone/PartialEq + serde cfg_attr + #[non_exhaustive]; Result derives Debug/Clone
    + #[non_exhaustive]; funfem_cluster is #[must_use]. Validate: n==0||m==0 -> InvalidDimension;
    k==0||k>n -> InvalidParameter; ncomp==0||ncomp>min(n,m) -> InvalidParameter; argvals.len()!=m ->
    InvalidDimension. Resolve p_disc_eff = if config.p_disc==0 { (k-1).max(1).min(ncomp_eff) } else
    { config.p_disc.min(ncomp_eff) } after fdata_to_pc_1d clamps ncomp. Steps: (1) global FPC scores
    via fdata_to_pc_1d(data, ncomp, argvals)? — use returned score matrix and actual ncomp_eff;
    (2) init hard labels via seeded k-means++-style selection over the score rows
    (StdRng::seed_from_u64(config.seed)); Fisher-EM outer loop to max_iter: (3) from current soft
    responsibilities compute between-cluster scatter B_soft and within-cluster scatter W_soft
    (ncomp_eff x ncomp_eff) — add a data-scaled diagonal regularization floor to W_soft before
    inverting (Pitfall 3: near-singular W); (4) form W_soft^{-1} B_soft via Cholesky solve
    (crate::linalg cholesky_d/cholesky_solve — confirm names in linalg.rs) and take its top p_disc_eff
    eigen/singular directions via nalgebra SVD (same DMatrix conversion pattern as fdata_to_pc_1d);
    fall back to identity discriminative directions if Cholesky fails; (5) project scores onto the
    discriminative subspace (n x p_disc_eff); (6) run a GMM E/M step in that subspace — inline a compact
    log-sum-exp responsibility normalizer (do NOT import gmm pub(super) helpers) and update
    per-cluster mean + diagonal covariance in the discriminative space; (7) converge on
    log-likelihood delta < tol. Multi-pass Fisher-EM (subspace re-estimated each outer iteration).
    Document in rustdoc that this is the simplified Fisher-EM (no generalized-eigenvalue crate; W^{-1}B
    via Cholesky + SVD) and diverges from the full R funFEM. Assemble FunFemResult (hard argmax of
    membership, membership FdMatrix, disc_subspace = final ncomp_eff x p_disc_eff directions). Add
    `#[cfg(test)] mod tests` entries (struct-literal config) using
    `crate::test_helpers::{uniform_grid, adjusted_rand_index}` + local two_separated_clusters generator.</action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib clustering_advanced::tests::funfem</automated>
  </verify>
  <done>funfem_cluster recovers well-separated groups (ARI>=0.90), deterministic under fixed seed, all invalid-input paths return Err; rustdoc records the simplified-Fisher-EM divergence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Joint align-and-cluster (elastic k-means) via Karcher-mean templates</name>
  <files>fdars-core/src/clustering_advanced.rs</files>
  <behavior>
    - test_align_cluster_shape_shift: cluster 0 = sin, cluster 1 = time-warped sin (phase shift) → ARI >= 0.90 (the phase-invariant grouping amplitude-naive k-means misses)
    - test_align_cluster_recovery: two amplitude-separated groups → ARI >= 0.90
    - test_align_cluster_invalid: k=0 → Err; k>n → Err; empty data → Err; argvals.len()!=m → Err
  </behavior>
  <action>Append align-and-cluster to clustering_advanced.rs per 33-RESEARCH.md "Algorithm
    Formulations §5". Public surface:
    `AlignClusterConfig { k: usize, max_iter: usize, seed: u64, use_amplitude_only: bool, elastic_lambda: f64, karcher_max_iter: usize, karcher_tol: f64 }`
    with Default (k=2, max_iter=20, seed=42, use_amplitude_only=true, elastic_lambda=0.0,
    karcher_max_iter=15, karcher_tol=1e-4);
    `AlignClusterResult { cluster: Vec<usize>, templates: Vec<Vec<f64>>, distances: FdMatrix, iterations: usize, converged: bool }`;
    `align_cluster_fd(data, argvals, config) -> Result<AlignClusterResult, FdarError>`. Config derives
    Debug/Clone/PartialEq + serde cfg_attr + #[non_exhaustive]; Result derives Debug/Clone
    + #[non_exhaustive]; align_cluster_fd is #[must_use]. Validate: n==0||m==0 -> InvalidDimension;
    k==0||k>n -> InvalidParameter; argvals.len()!=m -> InvalidDimension. Init k templates by seeded
    random distinct-curve selection (StdRng::seed_from_u64(config.seed)); take each template as that
    curve's values (length m). Elastic k-means loop to max_iter: (a) reassign — for each curve i and
    template t_k compute the distance via `amplitude_distance(&data.row(i), &t_k, argvals, config.elastic_lambda)`
    when use_amplitude_only else `elastic_distance(...)`; cluster[i] = argmin_k; fill the n x k distances
    matrix; (b) for each non-empty cluster, gather member rows into an (n_k x m) FdMatrix and update the
    template = `karcher_mean(&data_k, argvals, config.karcher_max_iter, config.karcher_tol, config.elastic_lambda).mean`
    (karcher_mean returns a struct, template is the `.mean` field — length m); (c) empty-cluster
    fallback: reinitialize that template to a random non-member curve (Pitfall 6 — karcher_mean needs
    >= 1 member). Converge when no label changes. Record iterations + converged. Add `#[cfg(test)] mod
    tests` (struct-literal config) using `crate::test_helpers::{uniform_grid, adjusted_rand_index}` and
    a local time_warped_clusters(n_per, m) generator (cluster 0 = sin(2*pi*t); cluster 1 = sin(2*pi*g(t))
    with a monotone phase-warp g so the two groups differ in phase, testing shape-invariant recovery).
    Keep n_per and m small for runtime (elastic DP is the phase's slowest path).</action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib clustering_advanced::tests::align_cluster</automated>
  </verify>
  <done>align_cluster_fd recovers the time-warped/shape-shifted group and amplitude-separated groups (ARI>=0.90 each) and returns Err on all invalid inputs; uses karcher_mean(.).mean + amplitude_distance/elastic_distance with the verified signatures.</done>
</task>

<task type="auto">
  <name>Task 3: Extend crate-root re-export with funFEM + align-and-cluster</name>
  <files>fdars-core/src/lib.rs</files>
  <action>Extend the `pub use clustering_advanced::{...};` block created in plan 33-02 to also
    re-export `funfem_cluster, align_cluster_fd, FunFemConfig, FunFemResult, AlignClusterConfig,
    AlignClusterResult`. Use scoped Edit on that single block — do NOT rewrite lib.rs, do NOT touch the
    clustering / gmm blocks, and do NOT remove the DBSCAN/kCFC re-exports already present. Strictly
    additive.</action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib clustering_advanced</automated>
  </verify>
  <done>`fdars_core::funfem_cluster`, `fdars_core::align_cluster_fd`, and their config/result structs resolve at crate root; all clustering_advanced tests (DBSCAN, kCFC, funFEM, align-cluster) pass; no existing re-export changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library caller → funfem_cluster / align_cluster_fd | Untrusted numeric inputs (data shape, argvals length, k, ncomp, p_disc, lambda) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-05 | Denial of Service | funfem_cluster input validation + singular within-scatter | low | mitigate | Validate n,m,k,ncomp,argvals; data-scaled reg floor on W_soft + identity fallback on Cholesky failure (no NaN, no panic); covered by test_funfem_invalid |
| T-33-06 | Denial of Service | align_cluster_fd input validation + empty-cluster Karcher call | low | mitigate | Validate n,m,k,argvals; empty-cluster template reinit before karcher_mean (which needs >=1 member); covered by test_align_cluster_invalid |
| T-33-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new crate dependency this phase (milestone constraint) — no install surface |
</threat_model>

<verification>
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --lib clustering_advanced` green (all four clusterers in this file).
- funFEM ARI>=0.90 recovery + deterministic + invalid-input Err. align-and-cluster shape-shift recovery ARI>=0.90 + amplitude recovery + invalid-input Err.
- Crate-root re-exports resolve; no existing public signature changed anywhere.
- (Orchestrator full gate) full suite incl. doctests + `cargo clippy --all-targets --features linalg,parallel -- -D warnings` green.
</verification>

<success_criteria>
- funFEM (FunFemConfig/FunFemResult/funfem_cluster) and align-and-cluster (AlignClusterConfig/AlignClusterResult/align_cluster_fd) implemented in clustering_advanced.rs, re-exported at crate root, with passing inline tests.
- All five CLUS-01 clusterers (funHDDC, funFEM, DBSCAN, kCFC, align-and-cluster) now present, tested, and crate-root re-exported.
- Strictly additive/non-breaking; no new crate dependency; divergences from R baselines documented in rustdoc.
</success_criteria>

<output>
Create `.planning/phases/33-model-based-density-functional-clustering/33-03-SUMMARY.md` when done.
</output>
