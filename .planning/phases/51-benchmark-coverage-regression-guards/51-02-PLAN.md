---
phase: 51-benchmark-coverage-regression-guards
plan: 02
type: execute
wave: 2
depends_on: [51-01]
files_modified:
  - fdars-core/benches/fts_benchmarks.rs
  - fdars-core/benches/frechet_benchmarks.rs
  - fdars-core/benches/boosting_regression_benchmarks.rs
  - fdars-core/benches/coclustering_benchmarks.rs
  - fdars-core/Cargo.toml
autonomous: true
requirements: [BENCH-01]
estimate:
  tokens: 22000
  raw_tokens: 14000
  tasks: 4
  confidence: med
must_haves:
  truths:
    - "4 new benches (fts::ftsm, frechet::frechet_global_reg, boosting_regression::boost_fosr, coclustering::co_cluster_select) compile under `cargo build --benches --features linalg,parallel` (BENCH-01)"
    - "`cargo clippy --all-targets --features linalg,parallel -- -D warnings` clean with all 4 registered"
    - "each new bench runs its cell once (--quick) without panicking"
  artifacts:
    - fdars-core/benches/fts_benchmarks.rs
    - fdars-core/benches/frechet_benchmarks.rs
    - fdars-core/benches/boosting_regression_benchmarks.rs
    - fdars-core/benches/coclustering_benchmarks.rs
    - "fdars-core/Cargo.toml — 4 new [[bench]] harness=false entries"
  key_links:
    - "Each Cargo.toml [[bench]] name ↔ its benches/<module>_benchmarks.rs filename"
    - "frechet_global_reg input contract: responses.nrows()==predictors.nrows(), argvals.len()==responses.ncols(), xout.ncols()==predictors.ncols() (VERIFIED src/frechet/regression.rs validate_reg_input)"
    - "boost_fosr uses BoostingConfig::default() (mstop=100) — VERIFIED src/boosting_regression/mod.rs:67"
---

<objective>
BENCH-01 expansion (wave 2 of 3): Add 4 new `[[bench]]` files covering the sibling entry points of 4 modules whose headline fn is already benched — `fts::ftsm` (dpca already benched), `frechet::frechet_global_reg` (frechet_anova already benched), `boosting_regression::boost_fosr` (no coverage), `coclustering::co_cluster_select` (co_cluster already benched). Each mirrors the tracer (51-01) pipeline exactly.

Purpose: cover the module's OTHER representative public entry — never duplicate the cells already in perf_hotpaths/perf_parallelism.
Output: 4 new bench files + 4 Cargo.toml `[[bench]]` entries, green under the primary clippy gate.

NOTE: this plan shares fdars-core/Cargo.toml with 51-01 and 51-03 → it runs in a SEPARATE wave (2) to serialize Cargo.toml edits.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-benchmark-coverage-regression-guards/51-RESEARCH.md

# Templates to mirror (structure + generators):
@fdars-core/benches/perf_hotpaths.rs
@fdars-core/benches/perf_parallelism.rs
# Proven pipeline reference (tracer):
@.planning/phases/51-benchmark-coverage-regression-guards/51-01-SUMMARY.md
</context>

<gates_shared>
Every task below shares these GATES (bake into each): prefix EVERY cargo command with `TMPDIR=/home/simonm/.cache/fdars-bench-tmp`. On a LINK/disk failure ("linking with cc failed") run `rm -rf target/debug/{incremental,examples}` and retry. Run `cargo fmt` before each commit; `git commit --no-verify` per task. NO src/ edit; NO new dependency. Do NOT push a `v*` tag. Cargo.toml [[bench]] entries are appended adjacent to existing blocks; do NOT modify the PERMANENT perf_hotpaths/perf_parallelism blocks. Each bench file copies its own deterministic non-RNG generator (benches are separate compilation units): sinusoid `generate_curves` from perf_hotpaths.rs:21-32, density `two_group_densities` from perf_parallelism.rs:29-51, or `co_cluster_curves` from perf_parallelism.rs:80-92. Build data OUTSIDE `b.iter()`; `black_box` inputs and the returned result. Primary gate after each task: `cargo clippy --all-targets --features linalg,parallel -- -D warnings`.
</gates_shared>

<tasks>

<task type="auto">
  <name>Task 1: benches/fts_benchmarks.rs (ftsm) + Cargo entry</name>
  <files>fdars-core/benches/fts_benchmarks.rs, fdars-core/Cargo.toml</files>
  <read_first>
    - fdars-core/src/fts/forecast.rs:267 (VERIFIED: `pub fn ftsm(data: &FdMatrix, ncomp: usize, argvals: &[f64]) -> Result<FtsmResult, FdarError>`)
    - fdars-core/benches/perf_hotpaths.rs:21-43 (generate_curves + bench_function shape)
  </read_first>
  <action>Create benches/fts_benchmarks.rs: import `fdars_core::fts::ftsm` + `fdars_core::matrix::FdMatrix`; copy `generate_curves` verbatim. bench_group "fts_ftsm", sample_size(20), measurement_time(30s), warm_up_time(3s) (medium). Data outside b.iter(): `let (data, argvals) = generate_curves(200, 50);`. Cell "n200_m50_ncomp3": `black_box(ftsm(black_box(&data), 3, black_box(&argvals)).unwrap())`. criterion_group!/criterion_main!. Append `[[bench]] name="fts_benchmarks" harness=false` to Cargo.toml. (dpca is already benched in perf_hotpaths — ftsm is the distinct forecast entry.) Follow gates_shared.</action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo bench -p fdars-core --features linalg,parallel --bench fts_benchmarks -- --quick</automated>
  </verify>
  <done>fts_benchmarks.rs compiles, lints clean, runs ftsm once; Cargo entry present; committed --no-verify after fmt.</done>
</task>

<task type="auto">
  <name>Task 2: benches/frechet_benchmarks.rs (frechet_global_reg) + Cargo entry</name>
  <files>fdars-core/benches/frechet_benchmarks.rs, fdars-core/Cargo.toml</files>
  <read_first>
    - fdars-core/src/frechet/regression.rs:236-241 (VERIFIED: `pub fn frechet_global_reg(predictors: &FdMatrix, responses: &FdMatrix, argvals: &[f64], xout: &FdMatrix) -> Result<FrechetGlobalRegResult, FdarError>`)
    - fdars-core/src/frechet/regression.rs validate_reg_input (input contract: responses.nrows()==predictors.nrows(); argvals.len()==responses.ncols(); xout.ncols()==predictors.ncols())
    - fdars-core/benches/perf_parallelism.rs:29-51 (two_group_densities generator to copy)
  </read_first>
  <action>Create benches/frechet_benchmarks.rs: import `fdars_core::frechet::frechet_global_reg` + `fdars_core::matrix::FdMatrix`; copy `two_group_densities` verbatim. Build inputs (n=24, m=81): `responses = two_group_densities(12, 81).0` (density rows); its argvals; `predictors` = a 1-column Euclidean FdMatrix with one deterministic scalar per row (e.g. `(i as f64 / n as f64)`), constructed via `FdMatrix::from_column_major(vec_of_len_n, n, 1).unwrap()`; `xout` = a 5-row 1-column FdMatrix of deterministic query points (e.g. evenly spaced in the predictor range), `FdMatrix::from_column_major(vec_of_len_5, 5, 1).unwrap()`. Use the CONCRETE `frechet_global_reg` (NOT the generic `_space<S: MetricSpace>` — avoids Sync/object-construction complication). bench_group "frechet_global_reg", sample_size(20), measurement_time(30s), warm_up_time(3s) (medium). Cell "n24_m81_xout5": black_box the result. Append `[[bench]] name="frechet_benchmarks" harness=false`. Follow gates_shared.</action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo bench -p fdars-core --features linalg,parallel --bench frechet_benchmarks -- --quick</automated>
  </verify>
  <done>frechet_benchmarks.rs compiles (all 4 input dims satisfy validate_reg_input), lints clean, runs frechet_global_reg once; Cargo entry present; committed --no-verify after fmt.</done>
</task>

<task type="auto">
  <name>Task 3: benches/boosting_regression_benchmarks.rs (boost_fosr) + Cargo entry</name>
  <files>fdars-core/benches/boosting_regression_benchmarks.rs, fdars-core/Cargo.toml</files>
  <read_first>
    - fdars-core/src/boosting_regression/boost_fosr.rs:263-268 (VERIFIED: `pub fn boost_fosr(data: &FdMatrix, predictors: &FdMatrix, argvals: &[f64], config: &BoostingConfig) -> Result<BoostFosrResult, FdarError>`) and :273-292 (validation: n>=3, m>0, predictors.nrows()==n, argvals.len()==m, p>=1)
    - fdars-core/src/boosting_regression/mod.rs:67 (VERIFIED `impl Default for BoostingConfig` → mstop=100, nu=0.1, nbasis=10, order=4, lfd_order=2, lambda=1.0, ncomp_x=3, seed=0)
    - fdars-core/benches/perf_hotpaths.rs:21-32 (generate_curves)
  </read_first>
  <action>Create benches/boosting_regression_benchmarks.rs: import `fdars_core::boosting_regression::{boost_fosr, BoostingConfig}` + `fdars_core::matrix::FdMatrix`; copy `generate_curves`. Inputs (n=100, m=50, p=2): `let (data, argvals) = generate_curves(100, 50);` (functional response); `predictors` = a deterministic n×2 Euclidean FdMatrix (2 sinusoid-of-index columns), built column-major `FdMatrix::from_column_major(vec_of_len_200, 100, 2).unwrap()`; `let config = BoostingConfig::default();`. bench_group "boosting_boost_fosr", sample_size(10), measurement_time(60s), warm_up_time(3s) (slow — mstop=100 penalized B-spline base-learners). Cell "n100_m50_p2_mstop100": black_box the result. If the default mstop=100 single-shot exceeds a few seconds/iter, ADD a second cell with a `mstop=50` config variant (clone default, set config.mstop=50) named "n100_m50_p2_mstop50" and keep both (RESEARCH A2 — mstop is an executor-tunable knob; keep the default cell for representativeness). Append `[[bench]] name="boosting_regression_benchmarks" harness=false`. Follow gates_shared.</action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo bench -p fdars-core --features linalg,parallel --bench boosting_regression_benchmarks -- --quick</automated>
  </verify>
  <done>boosting_regression_benchmarks.rs compiles, lints clean, runs boost_fosr once (default mstop=100, plus optional mstop=50 if the default cell was slow); Cargo entry present; committed --no-verify after fmt.</done>
</task>

<task type="auto">
  <name>Task 4: benches/coclustering_benchmarks.rs (co_cluster_select) + Cargo entry</name>
  <files>fdars-core/benches/coclustering_benchmarks.rs, fdars-core/Cargo.toml</files>
  <read_first>
    - fdars-core/src/coclustering.rs:1119-1125 (VERIFIED: `pub fn co_cluster_select(data: &FdMatrix, argvals: &[f64], k_range: &[usize], l_range: &[usize], config: &CoClusterConfig) -> Result<CoClusterSelectResult, FdarError>`)
    - fdars-core/src/coclustering.rs:171 (VERIFIED `impl Default for CoClusterConfig` → n_row_blocks=2, n_col_blocks=2, ncomp=5, max_iter=200, tol=1e-6, n_init=3, seed=42)
    - fdars-core/benches/perf_parallelism.rs:80-115 (co_cluster_curves generator + how co_cluster tunes CoClusterConfig fields)
  </read_first>
  <action>Create benches/coclustering_benchmarks.rs: import `fdars_core::coclustering::{co_cluster_select, CoClusterConfig}` + `fdars_core::matrix::FdMatrix`; copy `co_cluster_curves`. Inputs (n=120, m=40): `let (data, argvals) = co_cluster_curves(120, 40);`. Keep the sweep grid TINY: `let k_range = [2usize, 3]; let l_range = [2usize, 3];` (2×2). Config: `let mut config = CoClusterConfig::default(); config.n_init = 3; config.max_iter = 20;` (bound the grid×restart cost). bench_group "coclustering_co_cluster_select", sample_size(10), measurement_time(60s), warm_up_time(3s) (slow — sweeps K×L grid, each an internally-parallel co_cluster). Cell "n120_m40_grid2x2": `black_box(co_cluster_select(black_box(&data), black_box(&argvals), &k_range, &l_range, black_box(&config)).unwrap())`. (co_cluster single-fit is already benched in perf_parallelism — co_cluster_select model-selection sweep is the distinct entry; keep the grid 2×2 so the cell is not minutes-long.) Append `[[bench]] name="coclustering_benchmarks" harness=false`. Follow gates_shared.</action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo bench -p fdars-core --features linalg,parallel --bench coclustering_benchmarks -- --quick</automated>
  </verify>
  <done>coclustering_benchmarks.rs compiles, lints clean, runs co_cluster_select once (2×2 grid); Cargo entry present; committed --no-verify after fmt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Deterministic hard-coded bench inputs; no external input, network, auth, or crypto. Attack surface nil (RESEARCH Security Domain). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-02 | Tampering | Phantom crates.io publish from a `v*` tag | medium | mitigate | Do NOT push a `v*` tag (MEMORY audit-milestone-no-git-tag); crate version unchanged |
| T-51-SC | Tampering | npm/pip/cargo installs | high | accept | No package installed — criterion/dhat are pre-existing dev-deps (locked "no new dependency") |
</threat_model>

<verification>
- `TMPDIR=… cargo build -p fdars-core --benches --features linalg,parallel` compiles all 4 new benches.
- `TMPDIR=… cargo clippy --all-targets --features linalg,parallel -- -D warnings` clean.
- Each `--bench <name> -- --quick` runs its cell once without panic.
</verification>

<success_criteria>
4 module benches (ftsm, frechet_global_reg, boost_fosr, co_cluster_select) compile, lint clean, and run — none duplicating an already-benched cell. No src/ change; no new dependency.
</success_criteria>

<output>
Create `.planning/phases/51-benchmark-coverage-regression-guards/51-02-SUMMARY.md` when done. Record: 4 bench files + Cargo entries landed; captured `--quick` baseline cells for BENCH-RESULTS.md; whether boost_fosr needed the mstop=50 variant and whether any cell was down-sized (RESEARCH A1/A2).
</output>
