---
phase: 51-benchmark-coverage-regression-guards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/benches/inference_benchmarks.rs
  - fdars-core/Cargo.toml
autonomous: true
requirements: [BENCH-01]
estimate:
  tokens: 9000
  raw_tokens: 6000
  tasks: 1
  confidence: high
must_haves:
  truths:
    - "`cargo build -p fdars-core --benches --features linalg,parallel` compiles the new inference bench (BENCH-01 tracer)"
    - "`cargo clippy --all-targets --features linalg,parallel -- -D warnings` is clean with the new bench registered"
    - "`benches/inference_benchmarks.rs` runs t_perm_test once and returns a criterion result (baseline captured)"
  artifacts:
    - fdars-core/benches/inference_benchmarks.rs
    - "fdars-core/Cargo.toml — [[bench]] name=\"inference_benchmarks\" harness=false"
  key_links:
    - "Cargo.toml [[bench]] entry ↔ benches/inference_benchmarks.rs filename (criterion harness=false wiring)"
    - "bench imports fdars_core::inference::{t_perm_test, DEFAULT_N_PERM} — module path VERIFIED at src/inference/mod.rs:41"
---

<objective>
TRACER (BENCH-01): Add the first new criterion bench — `benches/inference_benchmarks.rs` covering `inference::t_perm_test` — and prove the full add-bench pipeline end-to-end: create the bench file (deterministic non-RNG generator copied in, `black_box` inputs+outputs, data built outside `b.iter()`) → register `[[bench]] harness=false` in Cargo.toml → `cargo build --benches` → `cargo clippy --all-targets -D warnings` → `cargo fmt` → `git commit --no-verify`.

Purpose: `inference::t_perm_test` is the cheapest, most self-contained target (two sinusoid `FdMatrix` from the existing generator, no feature gate, no generic bound, no multi-input assembly — per RESEARCH tracer pick). Proving this thin slice validates the exact pipeline every one of the remaining 8 module benches (plans 51-02, 51-03) will repeat.
Output: One new bench file + one Cargo.toml `[[bench]]` entry, both green under the primary clippy gate.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-benchmark-coverage-regression-guards/51-CONTEXT.md
@.planning/phases/51-benchmark-coverage-regression-guards/51-RESEARCH.md

# Template to mirror EXACTLY (structure, generator, sample_size/measurement_time tuning):
@fdars-core/benches/perf_hotpaths.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — benches/inference_benchmarks.rs (t_perm_test) + register in Cargo.toml + prove the full gate pipeline</name>
  <files>fdars-core/benches/inference_benchmarks.rs, fdars-core/Cargo.toml</files>
  <precondition>TMPDIR=/home/simonm/.cache/fdars-bench-tmp must exist and be writable (bench/doctest link into /tmp tmpfs → bogus "No space left" otherwise — MEMORY tmp-exhaustion-blocks-precommit). Create it with `mkdir -p /home/simonm/.cache/fdars-bench-tmp` if absent.</precondition>
  <read_first>
    - fdars-core/benches/perf_hotpaths.rs (lines 11-32: imports + `generate_curves(n,m)` sinusoid generator to copy verbatim; lines 34-43: bench_function structure — sample_size/measurement_time/warm_up_time + black_box on inputs and the returned result; lines 119-120: criterion_group!/criterion_main!)
    - fdars-core/src/inference/permutation.rs:152-158 (VERIFIED signature: `pub fn t_perm_test(data_a: &FdMatrix, data_b: &FdMatrix, argvals: &[f64], n_perm: usize, seed: u64) -> Result<TestResult, FdarError>`) and :18 (`pub const DEFAULT_N_PERM: usize = 999;`)
    - fdars-core/src/inference/mod.rs:41 (VERIFIED re-export: `pub use permutation::{f_perm_test, t_perm_test, DEFAULT_N_PERM};` — import path is `fdars_core::inference::{t_perm_test, DEFAULT_N_PERM}`)
    - fdars-core/Cargo.toml:98-104 (the two existing PERMANENT [[bench]] blocks — mirror the `name=… harness=false` shape; add the new block adjacent, keeping the PERMANENT comments intact)
  </read_first>
  <action>
    Create fdars-core/benches/inference_benchmarks.rs mirroring perf_hotpaths.rs structure exactly:
    (1) Imports: `use criterion::{black_box, criterion_group, criterion_main, Criterion};`, `use std::f64::consts::PI;`, `use std::time::Duration;`, `use fdars_core::inference::{t_perm_test, DEFAULT_N_PERM};`, `use fdars_core::matrix::FdMatrix;`.
    (2) Copy the deterministic `generate_curves(n, m) -> (FdMatrix, Vec<f64>)` sinusoid generator verbatim from perf_hotpaths.rs:21-32 (benches are separate compilation units — no shared helper module, so each bench file carries its own copy; NO RNG).
    (3) `fn bench_t_perm_test(c: &mut Criterion)`: benchmark_group("inference_t_perm_test"); `group.sample_size(20); group.measurement_time(Duration::from_secs(30)); group.warm_up_time(Duration::from_secs(3));` (medium cost class per RESEARCH). Build data OUTSIDE b.iter(): `let (a, argvals) = generate_curves(30, 50);` then `let (b_data, _) = generate_curves(30, 50);` (second sample shares argvals). bench_function("na30_nb30_m50_nperm999", |b| b.iter(|| black_box(t_perm_test(black_box(&a), black_box(&b_data), black_box(&argvals), black_box(DEFAULT_N_PERM), 42).unwrap()))). group.finish().
    (4) `criterion_group!(benches, bench_t_perm_test); criterion_main!(benches);`.
    Then add to fdars-core/Cargo.toml, adjacent to the existing [[bench]] blocks, a new entry `[[bench]]\nname = "inference_benchmarks"\nharness = false`. Do NOT modify the two PERMANENT perf_hotpaths/perf_parallelism blocks (keep their comments). Make NO src/ edit. Add NO new dependency. Run every cargo command prefixed with `TMPDIR=/home/simonm/.cache/fdars-bench-tmp`; on a LINK/disk failure ("linking with cc failed" — NOT a code bug) run `rm -rf target/debug/{incremental,examples}` and retry (MEMORY target-dir-fills-home-partition). Run `cargo fmt` before committing; commit with `git commit --no-verify` (the slow full hook stalls executor watchdogs — MEMORY executor-subagents-stall-on-long-cargo; fmt is run manually to avoid the drift MEMORY noverify-commits-leave-fmt-drift warns of). Do NOT push a `v*` tag (audit milestone — MEMORY audit-milestone-no-git-tag).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --benches --features linalg,parallel && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo bench -p fdars-core --features linalg,parallel --bench inference_benchmarks -- --quick</automated>
  </verify>
  <done>benches/inference_benchmarks.rs exists; Cargo.toml has `[[bench]] name="inference_benchmarks" harness=false`; the two build/clippy commands exit 0; the bench runs t_perm_test once (--quick) and completes; committed with `--no-verify` after `cargo fmt`. No src/ edit, no new dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Bench inputs are hard-coded deterministic generators — no external/untrusted input, no network, no auth/crypto. Attack surface is nil (RESEARCH Security Domain). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-01 | Tampering | `dhat-heap` `#[global_allocator]` accidentally enabled in release | low | accept | Feature stays dev/test-only, never in default CI (existing Cargo.toml comment); this plan does not touch it |
| T-51-02 | Tampering | Phantom crates.io publish from a `v*` tag on a no-code milestone | medium | mitigate | Do NOT push a `v*` tag (MEMORY audit-milestone-no-git-tag); crate version unchanged (0.29.0) |
| T-51-SC | Tampering | npm/pip/cargo installs | high | accept | No package installed this phase — criterion 0.5 + dhat 0.3 are pre-existing dev-deps (locked "no new dependency"); no legitimacy gate applies |
</threat_model>

<verification>
- `TMPDIR=… cargo build -p fdars-core --benches --features linalg,parallel` compiles all bench entries including the new inference bench.
- `TMPDIR=… cargo clippy --all-targets --features linalg,parallel -- -D warnings` clean (primary gate — lints bench code; a plain `-p … -D warnings` false-greens).
- `TMPDIR=… cargo bench … --bench inference_benchmarks -- --quick` runs the cell once.
</verification>

<success_criteria>
The inference bench compiles, lints clean, and runs; the full add-bench pipeline (create → register → build → clippy → fmt → commit --no-verify) is proven for the 8 expansion benches to follow. No src/ behavior change; no new dependency.
</success_criteria>

<output>
Create `.planning/phases/51-benchmark-coverage-regression-guards/51-01-SUMMARY.md` when done. Record: the bench file + Cargo entry landed; the captured `--quick` baseline cell (median if available) for BENCH-RESULTS.md; confirmation the pipeline (build/clippy/fmt/commit --no-verify) is proven for plans 51-02/51-03; any cell down-sizing if the medium cost estimate ran long (RESEARCH A1 — sizes are executor-tunable).
</output>