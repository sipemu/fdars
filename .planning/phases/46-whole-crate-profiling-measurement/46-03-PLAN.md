---
phase: 46-whole-crate-profiling-measurement
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/46-whole-crate-profiling-measurement/PROF-02-dedup-inventory.md
autonomous: true
requirements: [PROF-02]
estimate:
  tokens: 45000
  raw_tokens: 22500
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "PROF-02-dedup-inventory.md catalogs every RESEARCH-grounded duplication category (simpsons_weights, chi2/F survival, permutation loops, seeded-RNG, fix_svd_signs, FPCA scoring, Cholesky) with call-site counts."
    - "Each catalog entry carries file:line anchors verified by grep against the current tree (not stale)."
    - "Each entry is ranked by dedup leverage = (# call sites × complexity/drift-risk), with a proposed consolidation location and a CONS-0X target tag."
    - "The 9 new subsystems are explicitly confirmed to use canonical helpers (simpsons_weights, linalg Cholesky) rather than local reimplementations, or the exceptions are anchored."
  artifacts:
    - .planning/phases/46-whole-crate-profiling-measurement/PROF-02-dedup-inventory.md
  key_links:
    - "PROF-02's ranked list is the direct input to Phase 49 (CONS-01/CONS-02) — call-site anchors must be migration-ready."
---

<objective>
Produce PROF-02-dedup-inventory.md: a ranked catalog of numerical + statistical-test machinery
duplicated across the v0.19–v0.29 modules, each entry grounded by grep-verified `file:line`
anchors, ranked by dedup leverage = (# call sites × complexity/drift-risk), with a proposed
consolidation location per entry.

Purpose: This inventory drives Phase 49 (CONS-01 numerical machinery, CONS-02 statistical-test
scaffolding). Rankings and anchors must be concrete enough to migrate call sites directly.

Output: One ranked inventory document. This is a static-analysis task — grep + source reads only,
NO benches, NO `fdars-core/src/` edits. Runs independently of the profiling plans (own doc file).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/46-whole-crate-profiling-measurement/46-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep-verify every duplication candidate against the current tree</name>
  <files>(no files created — analysis pass; findings feed Task 2)</files>
  <read_first>
    - .planning/phases/46-whole-crate-profiling-measurement/46-RESEARCH.md §Duplication Candidates (Categories 1-7: the file:line anchors + call-site tables to re-verify)
  </read_first>
  <action>
    Re-verify each RESEARCH duplication category against the CURRENT source tree with grep (anchors
    may drift). For each category, run the relevant grep and record the exact current file:line hits.
    Use grep hygiene — strip comment lines before counting so header prose does not inflate counts
    (e.g. `grep -rn "simpsons_weights" fdars-core/src | grep -v '^\s*//'`). Categories to verify:
      - Category 1 — `simpsons_weights` call sites (RESEARCH lists 20+ across 9+ files). Confirm it is
        already centralized in `src/helpers.rs`; then grep the 9 NEW subsystems (inference, fts,
        frechet, density_fda, fpca_variants, face/irreg_fdata, boosting_regression, fem_smoothing,
        coclustering) for any LOCAL Simpson/quadrature reimplementation (search e.g. `fn.*weight`,
        `/ 3.0`, trapezoid patterns) — record whether each uses the canonical fn or rolls its own.
      - Category 2 — chi²/F survival: `chi_square_sf` (src/inference/dist.rs:99,118) vs `chi2_cdf` /
        `chi2_quantile` (src/spm/chi_squared.rs:164,189); enumerate call sites of each.
      - Category 3 — permutation-test loops: verify the 6 sites (inference/permutation.rs:175,238;
        frechet/anova.rs:171; function_on_scalar.rs:831,847; famm.rs:861; explain_generic/importance.rs:22;
        explain/importance.rs:59,127) and mark which are sequential vs parallel.
      - Category 4 — per-thread seeded-RNG `StdRng::seed_from_u64(...)`: verify the 8+ sites.
      - Category 5 — `fix_svd_signs` (src/regression.rs:180) vs the copy at src/pace_fpca.rs:219.
      - Category 6 — FPCA scoring / `fdata_to_pc_1d` usage + inline `fpca.project` repetition.
      - Category 7 — Cholesky (`cholesky_factor`/`cholesky_forward_back`/`cholesky_solve` in
        src/linalg.rs): confirm the new subsystems use `crate::linalg` rather than local Cholesky.
    Record for each: current call-site count, files, whether copies are identical or have DRIFTED
    (read the two implementations for chi2 and fix_svd_signs to assess drift risk). No src edits.
  </action>
  <verify>
    <automated>grep -rn "simpsons_weights\|fix_svd_signs\|seed_from_u64\|chi_square_sf\|chi2_cdf" fdars-core/src | grep -vc '^\s*//'</automated>
  </verify>
  <acceptance_criteria>
    - The grep pass returns a nonzero count (the duplication anchors resolve against the current tree).
    - For each of the 7 categories, the current file:line hits and call-site count are recorded (in working notes for Task 2).
    - The chi2 and fix_svd_signs implementations are read and their drift risk (identical vs diverged) is noted.
    - The 9 new subsystems are each classified as "uses canonical helper" or "has local reimpl @ anchor".
  </acceptance_criteria>
  <done>Every duplication candidate is re-verified against the live tree with current anchors and call-site counts.</done>
</task>

<task type="auto">
  <name>Task 2: Write the ranked PROF-02 duplication/consolidation inventory</name>
  <files>.planning/phases/46-whole-crate-profiling-measurement/PROF-02-dedup-inventory.md</files>
  <read_first>
    - .planning/phases/46-whole-crate-profiling-measurement/46-RESEARCH.md §Three Inventory Document Specifications → Inventory 2 (required doc contents: leverage table, verbatim quotes with anchors, proposed consolidation location)
  </read_first>
  <action>
    Write `.planning/phases/46-whole-crate-profiling-measurement/PROF-02-dedup-inventory.md` from the
    Task-1 findings. Required sections (research §Inventory 2):
      - `## Leverage Table` — one row per category:
        `[Category] [Call sites] [Files] [Complexity] [Drift risk] [Leverage score] [CONS target]`
        where Leverage score = (# call sites × complexity/drift-risk) per the locked ranking metric
        (NOT raw duplicate-LOC count). Tag each with CONS-01 (numerical machinery) or CONS-02
        (statistical-test scaffolding) so Phase 49 can consume directly.
      - `## Detailed Findings` — for each HIGH/MEDIUM-leverage item: a short verbatim quote of the
        duplicated pattern with its `file:line` anchor(s), and a one-line drift assessment.
      - `## Proposed Consolidation` — per item, the target location (`src/helpers.rs`, `src/linalg.rs`,
        or a new `src/distributions.rs` for chi²/F survival) and the proposed `pub(crate)` signature.
      - `## Already-Consolidated (No Action)` — note simpsons_weights + Cholesky are already
        centralized; record the 9-subsystem confirmation (each uses the canonical helper) or list any
        exception anchor found in Task 1.
    Rank the table descending by leverage score (permutation loops + chi²/F expected HIGH/MEDIUM;
    fix_svd_signs LOW-but-correctness-risk — note the silent sign-flip bug risk). No src edits.
  </action>
  <verify>
    <automated>test -f .planning/phases/46-whole-crate-profiling-measurement/PROF-02-dedup-inventory.md && grep -Eic "src/[a-z_/]+\.rs:[0-9]+" .planning/phases/46-whole-crate-profiling-measurement/PROF-02-dedup-inventory.md && grep -Eic "CONS-0[12]" .planning/phases/46-whole-crate-profiling-measurement/PROF-02-dedup-inventory.md</automated>
  </verify>
  <acceptance_criteria>
    - `PROF-02-dedup-inventory.md` exists with a Leverage Table, Detailed Findings, and Proposed Consolidation sections.
    - grep for `src/…\.rs:[0-9]+` anchors returns at least 8 hits (every category anchored).
    - grep for `CONS-0[12]` returns at least 2 hits (both CONS requirements have targets tagged).
    - The permutation-loop and chi²/F entries appear ranked HIGH/MEDIUM with a proposed consolidation location.
    - `git status --porcelain fdars-core/src/` prints `0` (no src edits).
  </acceptance_criteria>
  <done>PROF-02 is a ranked, anchored, CONS-tagged duplication inventory ready to drive Phase 49.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analyst → source tree | Read-only static grep/source analysis; produces a report doc. No code execution beyond grep, no external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-04 | Information disclosure | Stale/incorrect anchors misleading Phase 49 | medium | mitigate | Task 1 re-greps every anchor against the current tree; Task 2 asserts ≥8 live anchors resolve. |
| T-46-01 | Tampering | Accidental `src/` edit during analysis | low | mitigate | Both tasks assert `git status --porcelain fdars-core/src/` is empty; this is a read-only analysis plan. |
| T-46-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs; static analysis only; no new dependency. |
</threat_model>

<verification>
- Every duplication category is grep-verified against the live tree (current anchors).
- PROF-02-dedup-inventory.md exists with a leverage-ranked table, ≥8 file:line anchors, and CONS-01/CONS-02 tags.
- No `fdars-core/src/` file changed.
</verification>

<success_criteria>
- PROF-02 (duplication/consolidation inventory, ranked by dedup leverage, file:line anchors, proposed consolidation locations) is complete and drives Phase 49 concretely.
</success_criteria>

<output>
Create `.planning/phases/46-whole-crate-profiling-measurement/46-03-SUMMARY.md` when done.
Note the top-ranked dedup target and any surprise found (e.g. a 9-subsystem local reimplementation
that RESEARCH did not anticipate).
</output>
