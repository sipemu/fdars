---
phase: 46-whole-crate-profiling-measurement
plan: 02
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - fdars-core/Cargo.toml
  - fdars-core/benches/probe_inference.rs
  - fdars-core/benches/probe_fts.rs
  - fdars-core/benches/probe_frechet.rs
  - fdars-core/benches/probe_density_fda.rs
  - fdars-core/benches/probe_face.rs
  - fdars-core/benches/probe_boosting_regression.rs
  - fdars-core/benches/probe_fem_smoothing.rs
  - fdars-core/benches/probe_coclustering.rs
  - fdars-core/tests/alloc_audit_new_subsystems.rs
  - .planning/phases/46-whole-crate-profiling-measurement/PROF-01-hotpath-targets.md
autonomous: true
requirements: [PROF-01]
estimate:
  tokens: 95000
  raw_tokens: 47500
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Probe benches exist for all 8 remaining subsystems (inference, fts, frechet, density_fda, face, boosting_regression, fem_smoothing, coclustering) and each ran to produce criterion wall-time numbers."
    - "dhat allocation probes exist for the top allocation hotspots surfaced by RESEARCH (fts acf.rs:337 / spectral.rs:203, fpca_variants.rs:488/740, frechet spd.rs:154) and printed heap stats."
    - "PROF-01-hotpath-targets.md contains a Measured-Cells table covering all 9 subsystems, a ranked top-10 target list with file:line anchors + real ms numbers, and an N×M scaling note per subsystem."
    - "After probe removal: NO `probe_*` `[[bench]]` entry remains in Cargo.toml, and the full suite is green (proves zero behavior change)."
  artifacts:
    - .planning/phases/46-whole-crate-profiling-measurement/PROF-01-hotpath-targets.md
  key_links:
    - "Each probe bench must be temp-registered in Cargo.toml to run, then de-registered — the THROWAWAY comment marks every temporary entry."
    - "The 9-subsystem coverage of PROF-01 is what makes Phase 47 (PERF) and Phase 51 (BENCH-01 module list) plannable."
---

<objective>
Expand the Plan-01 tracer pipeline across the remaining 8 unbenchmarked subsystems, run every
probe bench + the targeted dhat allocation probes, then complete PROF-01-hotpath-targets.md as a
fully-ranked hot-path optimization target list. Finally, REMOVE all throwaway probe benches and
their temporary Cargo.toml registrations, and confirm the full suite is green with no permanent
`[[bench]]` left behind.

Purpose: PROF-01 is the evidence base that drives Phase 47 (hot-path/allocation PERF) and feeds
Phase 51's module list (BENCH-01). It must rank real bottlenecks with real numbers and anchors.

Output: The completed PROF-01 inventory doc + a clean tree (throwaway benches gone, suite green).
Measure-only: ZERO edits to `fdars-core/src/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-whole-crate-profiling-measurement/46-RESEARCH.md
@.planning/phases/46-whole-crate-profiling-measurement/46-01-SUMMARY.md

# Templates (already proven in Plan 01):
@fdars-core/benches/audit_hotpaths.rs
@fdars-core/tests/alloc_audit_fpca.rs
@.planning/phases/46-whole-crate-profiling-measurement/PROF-01-hotpath-targets.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author + run probe benches for the 8 remaining subsystems</name>
  <files>fdars-core/benches/probe_inference.rs, fdars-core/benches/probe_fts.rs, fdars-core/benches/probe_frechet.rs, fdars-core/benches/probe_density_fda.rs, fdars-core/benches/probe_face.rs, fdars-core/benches/probe_boosting_regression.rs, fdars-core/benches/probe_fem_smoothing.rs, fdars-core/benches/probe_coclustering.rs, fdars-core/Cargo.toml</files>
  <read_first>
    - fdars-core/benches/probe_fpca_variants.rs (the Plan-01 tracer — copy its structure verbatim for each new probe)
    - .planning/phases/46-whole-crate-profiling-measurement/46-RESEARCH.md §9 Unbenchmarked Subsystems → "Primary Timing Targets per Subsystem" (the exact functions + file:line to bench per subsystem) and §Profiling Harness Pattern → "N×M Scaling Grid" (the locked cell sizes + the O(N²·M²) elastic cap and the co_cluster small-cell-first rule)
    - fdars-core/Cargo.toml (existing `[[bench]]` blocks; the Plan-01 `probe_fpca_variants` THROWAWAY entry as the pattern)
  </read_first>
  <precondition>Disk headroom before the batch: re-run `rm -rf /home/simonm/projects/rust/fdars/target/debug/{incremental,examples}` if a build fails with a spurious "No space left" / LINK error (target/ growth — MEMORY.md pointer).</precondition>
  <action>
    Create one `harness = false` criterion probe bench per remaining subsystem, each copying the
    proven probe_fpca_variants.rs structure (private `generate_curves`, inputs outside `b.iter()`,
    `black_box` discipline, `#[cfg(feature = "linalg")]` guard + empty-stub fallback, `criterion_group!`
    + `criterion_main!`). Bench the RESEARCH-named primary target function(s) per subsystem over the
    locked N×M grid cells (n50_m50, n200_m50, n50_m200, and n1000_m50 / n200_m200 where the small cell
    timed tractably):
      - probe_inference.rs      → `itp_one_pop` / `itp_two_pop` and `t_perm_test` (inference/itp.rs:78, permutation.rs:175)
      - probe_fts.rs            → `stationarity_test`, `long_run_covariance`, `dpca` (fts/acf.rs:606,337; spectral.rs:203)
      - probe_frechet.rs        → `frechet_anova` / `frechet_anova_space`, `frechet_mean` (frechet/anova.rs:171,216; mean.rs)
      - probe_density_fda.rs    → `wasserstein_barycenter`, `lqd_fpca` (density_fda.rs:474)
      - probe_face.rs           → `face_covariance`, `mface_covariance` (irreg_fdata/face.rs)
      - probe_boosting_regression.rs → `boost_fosr` / `boost_fofr`, `bayesian_fosr` (boost_fosr.rs:413; boost_fofr.rs:328; bayesian.rs:167)
      - probe_fem_smoothing.rs  → `fem_smooth`, `fem_smooth_gcv` (fem_smoothing.rs:558)
      - probe_coclustering.rs   → `co_cluster` — measure the SMALL cell FIRST to estimate tractability
        before running large cells (nested l/k loops at coclustering.rs:238-333 can be slow at large N).
    Respect the elastic O(N²·M²) cap (N=100, M=50) if any target touches an elastic path. Tune
    sample_size/measurement_time per timing tier so each cell stays under ~60 s. TEMPORARILY register
    each probe in Cargo.toml with a `[[bench]] name = "probe_<subsystem>" harness = false` block, each
    carrying the literal comment `# THROWAWAY — Phase 46 profiling only; removed in Task 3`.
    Dry-run compile each probe (`--no-run`) before running measurements. Run each probe in a NAMED
    BATCH (one subsystem at a time) — do NOT run a single `cargo bench` across all probes + the 10
    registered benches (that exhausts target/ and /tmp — RESEARCH primary recommendation). Collect the
    per-cell mean ± stddev from criterion output for use in Task 3's doc.
    Do NOT edit any `fdars-core/src/` file.
  </action>
  <verify>
    <automated>for p in inference fts frechet density_fda face boosting_regression fem_smoothing coclustering; do TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo bench --bench probe_$p --features linalg,parallel --no-run 2>&1 | tail -1; done && git status --porcelain fdars-core/src/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - All 8 `probe_*` benches compile cleanly under `--features linalg,parallel` (each `--no-run` succeeds).
    - Each probe ran and produced criterion output with at least 2 `n{N}_m{M}` cells (captured in the SUMMARY / working notes for Task 3).
    - `git status --porcelain fdars-core/src/` prints `0` (no src edits).
    - Every temporary Cargo.toml `[[bench]]` block for a `probe_*` bench carries the THROWAWAY comment.
  </acceptance_criteria>
  <done>All 8 remaining subsystems have a compiled, run probe bench producing per-cell wall-time numbers; no src edits.</done>
</task>

<task type="auto">
  <name>Task 2: dhat allocation probes for the top hotspots + complete PROF-01 doc</name>
  <files>fdars-core/tests/alloc_audit_new_subsystems.rs, .planning/phases/46-whole-crate-profiling-measurement/PROF-01-hotpath-targets.md</files>
  <read_first>
    - fdars-core/tests/alloc_audit_new_subsystems.rs (the Plan-01 `count_fsvd_allocations_n200_m50` — copy its pattern for each new probe)
    - .planning/phases/46-whole-crate-profiling-measurement/46-RESEARCH.md §Allocation Hotspots → "Known FdMatrix↔DMatrix Copy Sites" table (the 6 sites with file:line + sizes) and §Three Inventory Document Specifications → Inventory 1 (required doc contents: Environment, Measured Cells table, Ranked list top-10, N×M scaling)
    - .planning/phases/46-whole-crate-profiling-measurement/PROF-01-hotpath-targets.md (the Plan-01 skeleton — append, keep the Environment section)
  </read_first>
  <action>
    Add `#[cfg(feature = "dhat-heap")]` `#[test]` allocation probes to
    tests/alloc_audit_new_subsystems.rs (copy the Plan-01 pattern) for the top hotspots RESEARCH
    grounded: `long_run_covariance` (fts/acf.rs:337), `dpca`/`spectral_density` (fts/spectral.rs:203),
    `ssvd` (fpca_variants.rs:740; fsvd@:488 already done in Plan 01), and `SpdSpace::frechet_mean`
    (frechet/spaces/spd.rs:154). Build inputs OUTSIDE the profiler scope; print total_blocks/
    total_bytes/max_bytes; do NOT hard-assert (baseline measurement). Run each under
    `--features dhat-heap,linalg`.
    Then COMPLETE `.planning/phases/46-whole-crate-profiling-measurement/PROF-01-hotpath-targets.md`,
    appending to the Plan-01 skeleton (keep its Environment section, updating RAYON_NUM_THREADS /
    governor if needed):
      - Fill the `## Measured Cells` table with every cell measured across all 9 subsystems:
        `[module] [function] [N] [M] [mean ms] [stddev ms] [allocs]` (allocs from the dhat probes
        where measured).
      - Complete `## Ranked Targets`: the top-10 optimization targets ranked by
        wall-time × representativeness (allocation count secondary), each carrying a real ms number
        and a `file:line` source anchor (e.g. `src/fpca_variants.rs:488`, `src/fts/acf.rs:337`,
        `src/coclustering.rs:238`).
      - Complete `## N×M Scaling`: for each subsystem note observed scaling (O(N·M), O(N·M²),
        O(N²·M²)) inferred from the grid cells.
      - Keep the honesty caveat: governor `powersave` (LOW-CONFIDENCE for multi-thread cells).
    Do NOT edit any `fdars-core/src/` file.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features dhat-heap,linalg -- count_ --nocapture 2>&1 | grep -Ec "blocks|bytes" && grep -Eic "src/[a-z_/]+\.rs:[0-9]+" .planning/phases/46-whole-crate-profiling-measurement/PROF-01-hotpath-targets.md</automated>
  </verify>
  <acceptance_criteria>
    - The dhat probes for fts/fpca_variants/frechet hotspots run and print heap-stat lines.
    - `PROF-01-hotpath-targets.md` contains a Measured-Cells table with rows for all 9 subsystems (grep for each module name in the module column returns a hit).
    - The Ranked Targets section lists at least 10 entries, and grep for `src/…\.rs:[0-9]+` anchors returns at least 10 hits.
    - `git status --porcelain fdars-core/src/` prints `0`.
  </acceptance_criteria>
  <done>PROF-01 is a complete, grounded, ranked hot-path inventory covering all 9 subsystems with real numbers + file:line anchors.</done>
</task>

<!-- planner-region-allow: probe_ -->
<task type="auto">
  <name>Task 3: Remove all throwaway probes, confirm clean tree + green suite</name>
  <files>fdars-core/Cargo.toml, fdars-core/benches/probe_inference.rs, fdars-core/benches/probe_fts.rs, fdars-core/benches/probe_frechet.rs, fdars-core/benches/probe_density_fda.rs, fdars-core/benches/probe_face.rs, fdars-core/benches/probe_boosting_regression.rs, fdars-core/benches/probe_fem_smoothing.rs, fdars-core/benches/probe_coclustering.rs, fdars-core/benches/probe_fpca_variants.rs, fdars-core/tests/alloc_audit_new_subsystems.rs</files>
  <read_first>
    - fdars-core/Cargo.toml (locate every `[[bench]]` block carrying the THROWAWAY comment)
    - .planning/phases/46-whole-crate-profiling-measurement/46-VALIDATION.md §Validation Sign-Off (the "no throwaway probe left registered" + "suite green after removal" gates)
  </read_first>
  <action>
    The MEASUREMENTS now live in PROF-01-hotpath-targets.md; the probe benches themselves are
    throwaway (permanent bench coverage is Phase 51 / BENCH-01). Delete all 9 `benches/probe_*.rs`
    files AND the new `tests/alloc_audit_new_subsystems.rs` file, and remove every THROWAWAY
    `[[bench]]` block from `fdars-core/Cargo.toml` (leave the 10 pre-existing registered benches
    untouched). Then confirm the tree is clean and the suite is green — this is the phase's
    zero-behavior-change proof: with all probes removed, the crate compiles and the full suite passes
    exactly as it did at the Plan-01 baseline.
    Do NOT delete `benches/audit_hotpaths.rs` or any of the 10 registered benches. Do NOT edit any
    `fdars-core/src/` file. Do NOT register any permanent probe bench.
  </action>
  <verify>
    <automated>! grep -q "probe_" fdars-core/Cargo.toml && [ -z "$(ls fdars-core/benches/probe_*.rs 2>/dev/null)" ] && [ ! -f fdars-core/tests/alloc_audit_new_subsystems.rs ] && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep "probe_" fdars-core/Cargo.toml` returns nothing (no throwaway `[[bench]]` entry survives).
    - No `fdars-core/benches/probe_*.rs` file exists; `fdars-core/tests/alloc_audit_new_subsystems.rs` is deleted.
    - The 10 pre-existing registered benches are still present in Cargo.toml (`grep -c "harness = false" fdars-core/Cargo.toml` still returns 10).
    - Full suite output ends with `test result: ok.` and `0 failed` (zero behavior change confirmed).
    - `git status --porcelain fdars-core/src/` prints `0`.
  </acceptance_criteria>
  <done>All throwaway probes removed, no permanent bench registered, full suite green — the measure-only guarantee is proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev harness → source crate | Probe benches/tests call public `fdars-core` APIs read-only; local measure-only audit — no network, no user data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-02 | Tampering | Throwaway `[[bench]]` left permanently registered | high | mitigate | Task 3 asserts `grep "probe_" Cargo.toml` returns nothing and the 10 registered benches remain — permanent bench coverage is Phase 51 (BENCH-01), not here. |
| T-46-03 | Tampering | Accidental deletion of a registered bench or a src edit | medium | mitigate | Task 3 only deletes `probe_*` + the new alloc file; asserts 10 registered benches survive and `git status fdars-core/src/` is empty. |
| T-46-SC | Tampering | npm/pip/cargo installs | high | accept | No new crate dependency; criterion + dhat already dev-deps; no package installs occur. |
</threat_model>

<verification>
- All 8 remaining probe benches compile + run, producing per-cell criterion numbers.
- dhat probes for the top hotspots print heap stats under `--features dhat-heap,linalg`.
- PROF-01-hotpath-targets.md is complete: 9-subsystem Measured-Cells table, ranked top-10 with file:line anchors + real ms, N×M scaling per subsystem.
- After removal: no `probe_*` bench in Cargo.toml, no probe files, 10 registered benches intact, full suite green.
- No `fdars-core/src/` file changed anywhere in the phase.
</verification>

<success_criteria>
- PROF-01 (ranked hot-path optimization target list, 9 subsystems, N×M-scaled, real numbers + anchors) is complete and drives Phase 47 + feeds Phase 51's module list.
- Throwaway probes fully removed; no permanent `[[bench]]` registered; full suite green (zero behavior change).
</success_criteria>

<output>
Create `.planning/phases/46-whole-crate-profiling-measurement/46-02-SUMMARY.md` when done.
Note the top-3 ranked hot-path targets and any subsystem whose large N×M cell was skipped as
intractable (with the small-cell number that justified skipping) — Phase 47 consumes this ranking.
</output>