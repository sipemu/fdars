---
phase: 46-whole-crate-profiling-measurement
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/46-whole-crate-profiling-measurement/PROF-03-api-inventory.md
autonomous: true
requirements: [PROF-03]
estimate:
  tokens: 45000
  raw_tokens: 22500
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "PROF-03-api-inventory.md catalogs every RESEARCH-grounded API-inconsistency category (Config structs missing Default, _1d/_2d/_nd families, result-field naming, permutation-test signatures) with file:line anchors verified against the current tree."
    - "Each item carries a proposed canonical form and is classified additive-safe (add + #[deprecated]) vs breaking (out of scope for v0.30.0)."
    - "Each item is ranked by user-facing impact + breadth and tagged with an API-0X target."
    - "The 4 Config structs missing Default are re-confirmed by grep (52 Default / 56 total) or the current count is corrected."
  artifacts:
    - .planning/phases/46-whole-crate-profiling-measurement/PROF-03-api-inventory.md
  key_links:
    - "PROF-03's proposed canonical forms are the direct input to Phase 50 (API-01/02/03) — each must be additive-safe to protect R/WASM bindings + 28 examples."
---

<objective>
Produce PROF-03-api-inventory.md: a ranked catalog of config/result patterns and redundant public
functions that are candidates for additive unification, each with a grep-verified `file:line`
anchor, a proposed canonical form, and an additive-safe vs breaking classification.

Purpose: This inventory drives Phase 50 (API-01 config/result unification, API-02 redundant-function
consolidation, API-03 zero-breakage). Every proposed canonical form must be additive (add unified
alternative + `#[deprecated]`, never remove) to protect the R/WASM bindings and 28 examples.

Output: One ranked inventory document. Static-analysis only — grep + source reads, NO benches, NO
`fdars-core/src/` edits. Runs independently (own doc file).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/46-whole-crate-profiling-measurement/46-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep-verify every API-inconsistency candidate against the current tree</name>
  <files>(no files created — analysis pass; findings feed Task 2)</files>
  <read_first>
    - .planning/phases/46-whole-crate-profiling-measurement/46-RESEARCH.md §API Inconsistency Candidates (Categories A-D: Config-missing-Default, _1d/_2d/_nd families, result-field naming, permutation-test signatures — with the anchors to re-verify)
  </read_first>
  <action>
    Re-verify each RESEARCH API-inconsistency category against the CURRENT tree with grep. Categories:
      - Category A — Config structs missing `Default`: enumerate all `pub struct *Config`
        (`grep -rn "pub struct [A-Za-z]*Config" fdars-core/src`) and all `impl Default for *Config`
        (`grep -rn "impl Default for [A-Za-z]*Config" fdars-core/src`); compute the set difference.
        RESEARCH claims 4 missing (`BoostingConfig` mod.rs:44, `BayesianConfig` mod.rs:76,
        `StabilityConfig` mod.rs:103, `StlConfig` detrend/stl.rs:49). Confirm the exact current count
        (52/56) or correct it; record each missing struct's fields + documented default values.
      - Category B — `_1d`/`_2d`/`_nd` families: `grep -rn "pub fn [a-z_]*_1d\|pub fn [a-z_]*_2d\|pub fn [a-z_]*_nd" fdars-core/src`;
        group into families (mean, deriv, geometric_median, modal, dtw, soft_dtw, hshift, deriv metric,
        basis projection). Flag which `_nd` variants are GENUINELY different algorithms (alignment/nd.rs:
        elastic_align_pair_nd, karcher_mean_nd, pca_nd) that must NOT be deprecated.
      - Category C — result-struct field naming: read FtsmResult, BoostFosrResult and compare field
        names (`fitted` vs `fitted_values`, `rotation` vs `components`) against the canonical
        FpcaResult (scores/rotation/mean/weights/singular_values); note the FpcPredictor trait
        unification layer in explain_generic/mod.rs.
      - Category D — permutation-test signature ordering: compare signatures of `t_perm_test`,
        `f_perm_test`, `frechet_anova`, `fanova`, `generic_permutation_importance` for the position of
        `n_perm` / `seed` (fanova lacks `seed`).
    Record current anchors + counts for each. No src edits.
  </action>
  <verify>
    <automated>echo "structs:$(grep -rc "pub struct [A-Za-z]*Config" fdars-core/src | awk -F: '{s+=$2} END{print s}') defaults:$(grep -rc "impl Default for [A-Za-z]*Config" fdars-core/src | awk -F: '{s+=$2} END{print s}')"</automated>
  </verify>
  <acceptance_criteria>
    - The Config-struct grep produces a total count and a Default-impl count; the difference (missing-Default set) is recorded with each struct's anchor.
    - The `_1d`/`_2d`/`_nd` grep hits are grouped into families, with the genuinely-different `_nd` algorithms flagged as do-not-deprecate.
    - The FtsmResult/BoostFosrResult field names are read and compared to FpcaResult (recorded in working notes).
    - The permutation-test signature positions of `n_perm`/`seed` are recorded per function.
  </acceptance_criteria>
  <done>Every API-inconsistency candidate is re-verified against the live tree with current anchors and counts.</done>
</task>

<task type="auto">
  <name>Task 2: Write the ranked PROF-03 API-inconsistency inventory</name>
  <files>.planning/phases/46-whole-crate-profiling-measurement/PROF-03-api-inventory.md</files>
  <read_first>
    - .planning/phases/46-whole-crate-profiling-measurement/46-RESEARCH.md §Three Inventory Document Specifications → Inventory 3 (required doc contents: item table, exact unified-replacement signature, additive-safe vs breaking classification)
  </read_first>
  <action>
    Write `.planning/phases/46-whole-crate-profiling-measurement/PROF-03-api-inventory.md` from the
    Task-1 findings. Required sections (research §Inventory 3):
      - `## Inventory Table` — one row per item:
        `[Item] [Current forms] [Proposed canonical form] [Impact] [Breadth] [API target]`, ranked by
        user-facing impact + breadth (the locked ranking metric). Tag each with API-01 (config/result
        pattern), API-02 (redundant function → single canonical entry point), or API-03 (zero-breakage
        surface).
      - `## Proposed Canonical Forms` — for each `#[deprecated]` candidate, the EXACT signature of the
        unified replacement (e.g. `fn mean(data: &FdMatrix, dim: Dim) -> ...` superseding
        `mean_1d`/`mean_2d`; `fanova_v2(data, groups, n_perm, seed)` superseding `fanova`;
        `impl Default for BoostingConfig` with the documented field defaults).
      - `## Additive-Safe vs Breaking` — classify every item: additive-safe (add new + `#[deprecated]`
        old, in scope for v0.30.0) vs breaking (renaming/removing an existing signature — OUT OF SCOPE,
        deferred to APIB-01). Explicitly mark the genuinely-different `_nd` alignment algorithms as
        "do not deprecate — different algorithm".
      - `## Config Default Gap` — the 4 (or corrected count) Config structs missing `Default`, each
        with anchor + the proposed `impl Default` field values.
    Rank descending: the 4 missing-Default configs are HIGH impact (public API, new subsystems); the
    30+ `_1d`/`_2d` family is large-breadth but Phase 50 should prioritize high-impact families (depth,
    regression, fdata) not unify all at once — note this. No src edits.
  </action>
  <verify>
    <automated>test -f .planning/phases/46-whole-crate-profiling-measurement/PROF-03-api-inventory.md && grep -Eic "src/[a-z_/]+\.rs:[0-9]+|BoostingConfig|StlConfig" .planning/phases/46-whole-crate-profiling-measurement/PROF-03-api-inventory.md && grep -Eic "API-0[123]" .planning/phases/46-whole-crate-profiling-measurement/PROF-03-api-inventory.md</automated>
  </verify>
  <acceptance_criteria>
    - `PROF-03-api-inventory.md` exists with an Inventory Table, Proposed Canonical Forms, Additive-Safe vs Breaking, and Config Default Gap sections.
    - grep for anchors/struct names returns at least 6 hits (every category grounded).
    - grep for `API-0[123]` returns at least 3 hits (all three API requirements have targets tagged).
    - Every proposed canonical form is classified additive-safe or breaking; the `_nd` alignment algorithms are flagged do-not-deprecate.
    - `git status --porcelain fdars-core/src/` prints `0` (no src edits).
  </acceptance_criteria>
  <done>PROF-03 is a ranked, anchored, API-tagged inconsistency inventory with additive-safe canonical forms, ready to drive Phase 50.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analyst → source tree | Read-only static grep/source analysis producing a report doc. No code execution beyond grep, no external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-05 | Information disclosure | Proposed canonical form that is secretly breaking (renames a live signature) misleading Phase 50 | high | mitigate | Task 2's Additive-Safe vs Breaking section forces every item to be classified; breaking items are marked OUT OF SCOPE (APIB-01), protecting R/WASM bindings + 28 examples. |
| T-46-01 | Tampering | Accidental `src/` edit during analysis | low | mitigate | Both tasks assert `git status --porcelain fdars-core/src/` is empty; read-only analysis. |
| T-46-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs; static analysis only; no new dependency. |
</threat_model>

<verification>
- Every API-inconsistency category is grep-verified against the live tree (current anchors + Config count).
- PROF-03-api-inventory.md exists with a ranked table, ≥6 anchors, API-01/02/03 tags, exact canonical-form signatures, and additive-safe/breaking classification.
- No `fdars-core/src/` file changed.
</verification>

<success_criteria>
- PROF-03 (API-inconsistency inventory, ranked by user-facing impact + breadth, proposed canonical form per item, additive-safe classification) is complete and drives Phase 50 concretely.
</success_criteria>

<output>
Create `.planning/phases/46-whole-crate-profiling-measurement/46-04-SUMMARY.md` when done.
Note the top-ranked API target and any correction to RESEARCH's counts (e.g. the missing-Default
Config set changed from the assumed 4).
</output>
