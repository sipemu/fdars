---
phase: 08-capability-parity-matrix-categorization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/AUDIT-REPORT.md
autonomous: false
requirements: [GAP-02, GAP-03]
estimate:
  tokens: 70000
  raw_tokens: 38000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "AUDIT-REPORT.md contains a top-level `## Phase 8 — Capability Parity Matrix & Categorization` section (D-05, SC1)"
    - "The three-value verdict rubric (present / partial / absent) is stated once with definitions and drives every parity row (D-01, SC1)"
    - "One complete capability area (Preprocessing, 29 in-scope rows) is parity-mapped end-to-end with every column populated (verdict, category, accuracy flag, searched-note, confidence) — proving the row schema (SC1, SC2)"
    - "Every partial/absent Preprocessing row carries a 'searched fdars for: [behavior]. Closest match: [fn/module]. Verdict: [...]' note mapped by capability not API name (Pitfall 9, Pitfall 11)"
    - "Preprocessing rows touching known-bug areas (B-spline round-trip GH #33, B-spline CV GH #33, elastic-alignment level encoding GH #34) are marked 'present — accuracy NOT verified' citing CONCERNS.md + fix commit, never a bare check (D-02, Pitfall 12, SC4)"
    - "Each Preprocessing gap carries a table-stakes / differentiator / out-of-scope category via the D-03 rubric (SC2, GAP-03)"
  artifacts:
    - ".planning/research/AUDIT-REPORT.md (appended `## Phase 8` section: verdict rubric subsection + category rubric subsection + Preprocessing parity table)"
  key_links:
    - "Phase 8 Preprocessing rows join 1:1 against Phase 7 §Area: Preprocessing in-scope rows (same left column, capability axis fixed)"
    - "D-02 accuracy flags cite CONCERNS.md §Known Bugs fix commits (2fb6d3c9 #33, 6ed62398 #34)"
    - "Verdicts source-confirmed by grep/read of fdars-core/src (STRUCTURE.md points the search, source confirms the row)"
---

<objective>
Establish the Phase 8 parity apparatus end-to-end by producing the COMPLETE parity treatment
for ONE Phase-7 capability area — Preprocessing (29 in-scope rows) — with every column
populated per the D-01 verdict rubric, D-03 category rubric, D-02 accuracy flag convention,
and the Pitfall-9/11/12 note conventions, all source-grep-confirmed against fdars-core/src.

Preprocessing is chosen as the tracer area because it is the largest algorithm-heavy area
(smoothing / registration / basis / dim-reduction) and it is the one that touches three of
the four known-bug areas (B-spline round-trip #33, B-spline CV #33, elastic-alignment level
encoding #34), so it exercises the full accuracy-flag path as well as the verdict path.

Purpose: Prove the row schema and rubric application are sound BEFORE expanding to the other
five areas. If the schema is wrong, this catches it after one area instead of after six.
Output: An appended `## Phase 8 — Capability Parity Matrix & Categorization` section in
AUDIT-REPORT.md containing the verdict rubric, the category rubric, and the fully-worked
Preprocessing parity table. Zero fdars-core/src edits (audit-only milestone).
</objective>

<user_story>
**As a** maintainer planning fdars' next milestone,
**I want to** see exactly which scikit-fda preprocessing capabilities fdars has, partially has, or lacks — mapped by capability and flagged where accuracy is unverified,
**so that** I can trust the parity picture and reuse the same proven schema for every other capability area.
</user_story>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-capability-parity-matrix-categorization/08-CONTEXT.md
@.planning/research/AUDIT-REPORT.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONCERNS.md
</context>

<artifacts_this_phase_produces>
This phase produces NO source symbols (audit-only). The sole artifact is the appended
`## Phase 8 — Capability Parity Matrix & Categorization` section in
`.planning/research/AUDIT-REPORT.md`. This plan (01) creates the section skeleton and these
subsections within it:
- `### Verdict Rubric (D-01)` — the three-value present/partial/absent definitions
- `### Categorization Rubric (D-03)` — table-stakes / differentiator / out-of-scope definitions
- `### Area: Preprocessing — Parity` — the fully-worked 29-row parity table
Plans 02 and 03 append the remaining five area tables, the separated counts, the reverse-parity
strengths sweep, and the drafted gap-backlog into the SAME section.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the D-01 three-value verdict rubric (costly reversibility)</name>
  <reversibility rating="costly">D-01 is the schema the whole 129-row matrix and the Phase 9 backlog key off; collapsing or re-defining it later forces re-mapping every row.</reversibility>
  <decision>Adopt the D-01 three-value verdict rubric (present / partial / absent) as the fixed schema for all 129 rows, with the D-01a "partial" bucket retained (not collapsed to binary)?</decision>
  <context>The rubric is locked in 08-CONTEXT.md D-01/D-01a. This checkpoint exists only because the rubric is a costly-reversibility decision: every subsequent row in this plan and in Plans 02/03 is written against it, and re-defining it after mapping begins forces re-mapping every row. Confirming here makes that cost visible before the tracer commits any rows.</context>
  <options>
    <option id="adopt-d01">
      <name>Adopt D-01 as written (recommended — it is the locked decision)</name>
      <pros>Matches locked CONTEXT decision; keeps partial vs absent separated per Pitfall 11; feeds Phase 9 backlog cleanly.</pros>
      <cons>None — this is the locked default.</cons>
    </option>
    <option id="collapse-binary">
      <name>Collapse to binary present/absent</name>
      <pros>Simpler table.</pros>
      <cons>Violates D-01a and Pitfall 11 (loses add-a-variant vs implement-from-scratch distinction). Do not choose unless the user overrides the locked decision.</cons>
    </option>
  </options>
  <resume-signal>Select: adopt-d01 (default) or collapse-binary</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: Seed the Phase 8 section + rubrics, and parity-map ALL of Preprocessing end-to-end</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - .planning/research/AUDIT-REPORT.md §"Phase 7 — scikit-fda Capability Enumeration" → §"Area: Preprocessing" (the 29 in-scope rows: the fixed left column — read every row's Task/Method/Relevance; do NOT re-enumerate or re-count)
    - .planning/research/AUDIT-REPORT.md §"Capability-Row Schema" (Collapse Rule / Relevance Taxonomy / Table Columns — mirror this style)
    - .planning/codebase/STRUCTURE.md §"Where to Add New Code" (the scikit-fda-task → fdars-module join: smoothing→smoothing.rs/smooth_basis.rs, registration→alignment/, basis→basis/, dim-reduction→regression.rs)
    - .planning/codebase/CONCERNS.md §"Known Bugs (Recent Fixes)" (B-spline round-trip #33 commit 2fb6d3c9; B-spline CV #33 commit 2fb6d3c9; elastic-alignment level encoding #34 commit 6ed62398)
    - .planning/research/PITFALLS.md (Pitfall 9 capability-not-API-name; Pitfall 11 searched-note + partial≠missing; Pitfall 12 accuracy flag)
    - .planning/research/FEATURES.md §"Biggest Likely Gaps" (leads only — verify against source)
    - fdars-core/src/smoothing.rs, fdars-core/src/smooth_basis.rs, fdars-core/src/basis/, fdars-core/src/alignment/, fdars-core/src/warping.rs, fdars-core/src/landmark.rs, fdars-core/src/regression.rs (grep/read to CONFIRM each present/partial/absent verdict)
  </read_first>
  <action>Append a new top-level `## Phase 8 — Capability Parity Matrix & Categorization` section to AUDIT-REPORT.md (D-05 single-file convention). Under it write, in order: (a) a `### Verdict Rubric (D-01)` subsection defining the three values — present (core capability exists in any call-shape; builder+single-call counts as fit/predict/transform per Pitfall 9), partial (core present but scikit-fda variants/options missing), absent (no fdars equivalent found) — plus the D-01a note that partial is retained; (b) a `### Categorization Rubric (D-03)` subsection defining table-stakes, differentiator, and out-of-scope; (c) a `### Area: Preprocessing — Parity` table with one row per Phase-7 Preprocessing in-scope capability (join 1:1 on the Phase-7 rows), columns Area | Task | Capability | Verdict | Category | Accuracy? | fdars equivalent | Confidence. For EVERY row, set Verdict by grepping/reading the named fdars-core/src modules (STRUCTURE.md points the search, source confirms — do not decide from the module map alone); map by capability not API name (a different call shape is present, not a gap). For every partial/absent row, put the mandatory note "searched fdars for: [behavior]. Closest match: [fn/module]. Verdict: [reason]" in the fdars-equivalent column. For rows covering B-spline basis round-trip, B-spline n_basis CV selection, or elastic-alignment level-encoding/registration, set Accuracy? to "present — accuracy NOT verified" and cite the CONCERNS.md entry plus its fix commit (2fb6d3c9 for #33 round-trip/CV, 6ed62398 for #34) — never a bare check-mark. Tag Confidence HIGH where source-grep confirmed, MEDIUM where inferred from the module map. Category per D-03 for every gap row. Write prose and tables only — no fenced code blocks, no fdars-core/src edits.</action>
  <verify>
    <automated>test -f .planning/research/AUDIT-REPORT.md && grep -q "^## Phase 8 — Capability Parity Matrix & Categorization" .planning/research/AUDIT-REPORT.md && grep -q "### Verdict Rubric" .planning/research/AUDIT-REPORT.md && grep -q "### Categorization Rubric" .planning/research/AUDIT-REPORT.md && grep -q "### Area: Preprocessing — Parity" .planning/research/AUDIT-REPORT.md && grep -q "accuracy NOT verified" .planning/research/AUDIT-REPORT.md && grep -Eq "2fb6d3c9|6ed62398" .planning/research/AUDIT-REPORT.md && grep -q "searched fdars for" .planning/research/AUDIT-REPORT.md && git diff --name-only -- fdars-core/src | grep -qv . </automated>
  </verify>
  <acceptance_criteria>
    - AUDIT-REPORT.md contains a `## Phase 8 — Capability Parity Matrix & Categorization` section with `### Verdict Rubric (D-01)` and `### Categorization Rubric (D-03)` subsections
    - The `### Area: Preprocessing — Parity` table has one row per Phase-7 Preprocessing in-scope capability (row count matches the Phase-7 Preprocessing in-scope rows), each with a present/partial/absent verdict
    - Every partial/absent row contains a "searched fdars for:" note naming the closest fdars fn/module (Pitfall 11)
    - Rows for B-spline round-trip, B-spline CV, and elastic-alignment level encoding read "present — accuracy NOT verified" with a CONCERNS.md + fix-commit citation (2fb6d3c9 or 6ed62398); no such row shows a bare check-mark (D-02, Pitfall 12)
    - Every gap row carries a table-stakes / differentiator / out-of-scope category (D-03)
    - Verdicts are mapped by capability not API name; builder+single-call fdars shapes counted as present (Pitfall 9)
    - `git diff --name-only -- fdars-core/src` is empty (zero source edits)
  </acceptance_criteria>
  <done>The Phase 8 section exists with both rubrics and a complete, source-confirmed Preprocessing parity table; every gap row has a searched-note; every known-bug row is accuracy-flagged with a fix-commit citation; no fdars-core/src file changed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verifies the tracer schema is sound before expansion</name>
  <what-built>The `## Phase 8` section skeleton, the D-01 verdict rubric, the D-03 category rubric, and the fully-worked Preprocessing parity table (all columns populated, gap notes present, known-bug rows accuracy-flagged).</what-built>
  <how-to-verify>Open .planning/research/AUDIT-REPORT.md and scroll to `## Phase 8`. Confirm: (1) the verdict rubric matches D-01/D-01a (three values, partial retained); (2) the category rubric matches D-03 (table-stakes / differentiator / out-of-scope); (3) the Preprocessing table has a row for each Phase-7 Preprocessing in-scope capability, mapped by capability not API name; (4) every partial/absent row has a "searched fdars for:" note; (5) the B-spline and elastic-alignment known-bug rows say "present — accuracy NOT verified" with a fix-commit citation, not a bare check; (6) confidence tags are present (HIGH/MEDIUM). This is the schema every other area will reuse — approve it here or request schema fixes now.</how-to-verify>
  <resume-signal>Type "approved" to unlock Plan 02 expansion, or describe the schema issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `## Phase 8 — Capability Parity Matrix & Categorization` section exists in AUDIT-REPORT.md
- Verdict rubric (D-01) and category rubric (D-03) subsections present and match CONTEXT
- Preprocessing parity table complete: one row per Phase-7 Preprocessing in-scope capability, all columns populated
- Every partial/absent row has a "searched fdars for:" note; known-bug rows accuracy-flagged with fix commit
- `git diff --name-only -- fdars-core/src` returns nothing (audit-only enforced)
</verification>

<success_criteria>
The tracer proves the parity row schema end-to-end on one full area: verdict rubric applied,
category rubric applied, accuracy flags applied to known-bug rows, searched-notes on every gap,
confidence tagged, all source-confirmed — and the human has approved the schema for reuse.
Zero fdars-core/src edits.
</success_criteria>

<output>
Create `.planning/phases/08-capability-parity-matrix-categorization/08-01-SUMMARY.md` when done
</output>
