---
phase: 08-capability-parity-matrix-categorization
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - .planning/research/AUDIT-REPORT.md
autonomous: true
requirements: [GAP-02, GAP-03]
estimate:
  tokens: 90000
  raw_tokens: 50000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "All five remaining Phase-7 areas (Representation 17, Exploratory 20, ML 20, Inference 5, Misc 38 in-scope) are parity-mapped using the SAME schema Plan 01 proved (SC1, GAP-02)"
    - "All 129 in-scope capabilities are now marked present / partial / absent across the six area tables, mapped by capability not API name (SC1, Pitfall 9)"
    - "Every partial/absent row across the five areas carries a 'searched fdars for:' note naming the closest fdars fn/module (Pitfall 11)"
    - "Rows touching the seasonal / Lomb-Scargle NaN fragile area and the GMM over-split known bug are accuracy-flagged 'present — accuracy NOT verified' citing CONCERNS.md + fix commit ec17d138 (GMM) / the seasonal fragile-area entry (D-02, Pitfall 12)"
    - "Every gap row across the five areas carries a table-stakes / differentiator / out-of-scope category (D-03, GAP-03)"
  artifacts:
    - ".planning/research/AUDIT-REPORT.md (five additional area parity tables appended into the `## Phase 8` section)"
  key_links:
    - "Each area table joins 1:1 against its Phase-7 §Area in-scope rows (fixed capability axis)"
    - "GMM over-split accuracy flag cites CONCERNS.md fix commit ec17d138 (v0.13.2)"
    - "Verdicts source-confirmed by grep/read of the corresponding fdars-core/src modules"
---

<objective>
Expand the proven parity schema from Plan 01 to the remaining FIVE capability areas —
Representation (17 in-scope), Exploratory (20), ML (20), Inference (5), Misc (38) — so that
all 129 in-scope scikit-fda capabilities are parity-mapped against fdars. Every row uses the
exact schema Plan 01 established and the human approved: verdict (D-01), category (D-03),
accuracy flag (D-02), searched-note (Pitfall 11), confidence tag, all source-grep-confirmed.

Purpose: Complete the parity matrix (GAP-02) and per-row categorization (GAP-03) across the
full capability surface. Output: five additional area tables appended into the same
`## Phase 8` section. Zero fdars-core/src edits (audit-only milestone).
</objective>

<user_story>
**As a** maintainer,
**I want** every one of scikit-fda's 129 in-scope capabilities marked present/partial/absent against fdars with a searched-note on each gap,
**so that** the parity picture is complete and consistent across all six capability areas.
</user_story>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-capability-parity-matrix-categorization/08-CONTEXT.md
@.planning/research/AUDIT-REPORT.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/08-capability-parity-matrix-categorization/08-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
No source symbols (audit-only). This plan appends five area parity tables into the existing
`## Phase 8 — Capability Parity Matrix & Categorization` section of
`.planning/research/AUDIT-REPORT.md`:
- `### Area: Representation — Parity`
- `### Area: Exploratory — Parity`
- `### Area: ML — Parity`
- `### Area: Inference — Parity`
- `### Area: Misc — Parity`
Each reuses the verdict rubric (D-01) and category rubric (D-03) that Plan 01 defined and the
human approved. Plan 03 then appends the separated counts, reverse-parity sweep, and backlog.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Parity-map Representation + Exploratory (37 in-scope rows)</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - .planning/research/AUDIT-REPORT.md §"Phase 7" → §"Area: Representation" (17 in-scope) and §"Area: Exploratory" (20 in-scope) — the fixed left column; do NOT re-enumerate
    - The `### Verdict Rubric (D-01)` and `### Categorization Rubric (D-03)` subsections Plan 01 wrote (reuse verbatim)
    - .planning/codebase/STRUCTURE.md §"Where to Add New Code" (basis→basis/, covariance→covariance.rs, interpolation→helpers.rs; depth→depth/, outlier→outliers.rs, exploratory stats)
    - .planning/codebase/CONCERNS.md §"Fragile Areas" (seasonal/Lomb-Scargle NaN handling, src/seasonal ~line 595)
    - fdars-core/src/basis/, fdars-core/src/covariance.rs, fdars-core/src/irreg_fdata/, fdars-core/src/helpers.rs, fdars-core/src/depth/, fdars-core/src/outliers.rs, fdars-core/src/andrews.rs, fdars-core/src/seasonal/ (grep/read to CONFIRM each verdict)
  </read_first>
  <action>Append `### Area: Representation — Parity` and `### Area: Exploratory — Parity` tables into the `## Phase 8` section, using the Plan-01 columns (Area | Task | Capability | Verdict | Category | Accuracy? | fdars equivalent | Confidence). One row per Phase-7 in-scope capability in each area (join 1:1 on the Phase-7 rows). Confirm each present/partial/absent verdict by grepping/reading the named fdars-core/src modules — the eight basis systems in basis/, covariance estimation in covariance.rs, spline interpolation and extrapolation policies, grid-to-basis conversion, irregular converters in irreg_fdata/ (Representation); depth measures in depth/, outlier detection in outliers.rs, MS-plot outlier algorithm, Andrews curves in andrews.rs, exploratory statistics (Exploratory). Map by capability not API name (Pitfall 9). For every partial/absent row write the "searched fdars for: [behavior]. Closest match: [fn/module]. Verdict: [reason]" note. If any Exploratory row rides on the seasonal / Lomb-Scargle NaN-handling fragile area, mark Accuracy? "present — accuracy NOT verified" and cite the CONCERNS.md §Fragile Areas seasonal entry. Tag Confidence HIGH (source-grep) or MEDIUM (module-map inferred). Category per D-03 for every gap. Prose and tables only — no fenced code, no fdars-core/src edits.</action>
  <verify>
    <automated>grep -q "### Area: Representation — Parity" .planning/research/AUDIT-REPORT.md && grep -q "### Area: Exploratory — Parity" .planning/research/AUDIT-REPORT.md && git diff --name-only -- fdars-core/src | grep -qv .</automated>
  </verify>
  <acceptance_criteria>
    - `### Area: Representation — Parity` and `### Area: Exploratory — Parity` tables exist under `## Phase 8`
    - Each has one row per Phase-7 in-scope capability for that area, each with a present/partial/absent verdict
    - Every partial/absent row has a "searched fdars for:" note naming the closest fdars fn/module
    - Any row on the seasonal/Lomb-Scargle fragile area reads "present — accuracy NOT verified" with a CONCERNS.md citation
    - Every gap row carries a D-03 category; verdicts mapped by capability not API name
    - `git diff --name-only -- fdars-core/src` is empty
  </acceptance_criteria>
  <done>Representation and Exploratory parity tables are complete and source-confirmed inside the Phase 8 section; no source edits.</done>
</task>

<task type="auto">
  <name>Task 2: Parity-map ML + Inference (25 in-scope rows)</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - .planning/research/AUDIT-REPORT.md §"Phase 7" → §"Area: ML" (20 in-scope) and §"Area: Inference" (5 in-scope) — fixed left column
    - The Plan-01 verdict + category rubric subsections (reuse)
    - .planning/codebase/STRUCTURE.md §"Where to Add New Code" (regression→scalar_on_function/, classification→classification/, clustering→clustering.rs/gmm/, inference→famm.rs)
    - .planning/codebase/CONCERNS.md §"Known Bugs" (GMM over-split fix commit ec17d138, v0.13.2)
    - fdars-core/src/scalar_on_function/, fdars-core/src/classification/, fdars-core/src/clustering.rs, fdars-core/src/gmm/, fdars-core/src/famm.rs, fdars-core/src/fof_regression.rs, fdars-core/src/function_on_scalar.rs, fdars-core/src/conformal/ (grep/read to CONFIRM each verdict)
  </read_first>
  <action>Append `### Area: ML — Parity` and `### Area: Inference — Parity` tables into the `## Phase 8` section using the Plan-01 columns. One row per Phase-7 in-scope capability per area (1:1 join). Confirm verdicts by grepping/reading the fdars-core/src modules — scalar-on-function regression (LDA/QDA/kNN/kernel classifiers in classification/, k-means and GMM clustering in clustering.rs/gmm/, function-on-function and function-on-scalar regression) for ML; functional ANOVA / hypothesis testing (famm.rs and related) for Inference. Map by capability not API name (Pitfall 9). Write the "searched fdars for:" note on every partial/absent row. For the GMM clustering row, mark Accuracy? "present — accuracy NOT verified" and cite CONCERNS.md §Known Bugs GMM over-split fix commit ec17d138 (v0.13.2) — never a bare check. Confidence HIGH/MEDIUM. Category per D-03 for every gap. Prose and tables only — no fenced code, no fdars-core/src edits.</action>
  <verify>
    <automated>grep -q "### Area: ML — Parity" .planning/research/AUDIT-REPORT.md && grep -q "### Area: Inference — Parity" .planning/research/AUDIT-REPORT.md && grep -q "ec17d138" .planning/research/AUDIT-REPORT.md && git diff --name-only -- fdars-core/src | grep -qv .</automated>
  </verify>
  <acceptance_criteria>
    - `### Area: ML — Parity` and `### Area: Inference — Parity` tables exist under `## Phase 8`
    - Each has one row per Phase-7 in-scope capability for that area with a present/partial/absent verdict
    - Every partial/absent row has a "searched fdars for:" note
    - The GMM clustering row reads "present — accuracy NOT verified" citing fix commit ec17d138 (not a bare check)
    - Every gap row carries a D-03 category; verdicts mapped by capability not API name
    - `git diff --name-only -- fdars-core/src` is empty
  </acceptance_criteria>
  <done>ML and Inference parity tables complete and source-confirmed; GMM row accuracy-flagged; no source edits.</done>
</task>

<task type="auto">
  <name>Task 3: Parity-map Misc (38 in-scope rows) — completes all 129 rows</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - .planning/research/AUDIT-REPORT.md §"Phase 7" → §"Area: Misc" (38 in-scope: 16 metrics/norms + covariance kernels + operators/regularization + data-generation helpers + scoring utilities) — fixed left column
    - The Plan-01 verdict + category rubric subsections (reuse)
    - .planning/codebase/STRUCTURE.md §"Where to Add New Code" (metrics→distance.rs/metric/, kernels/operators→covariance.rs/helpers.rs, simulation→simulation.rs, scoring→scalar_on_function/ result structs)
    - fdars-core/src/distance.rs, fdars-core/src/metric/, fdars-core/src/covariance.rs, fdars-core/src/helpers.rs, fdars-core/src/simulation.rs, fdars-core/src/utility.rs (grep/read to CONFIRM each verdict)
  </read_first>
  <action>Append `### Area: Misc — Parity` table into the `## Phase 8` section using the Plan-01 columns, one row per Phase-7 Misc in-scope capability (1:1 join, 38 in-scope rows). Confirm verdicts by grepping/reading distance.rs and metric/ (Lp / distance metrics), covariance.rs (covariance kernels), helpers.rs (operators / regularization / integration), simulation.rs (data-generation helpers), and the regression/classification result structs (scoring utilities like r2_score). Map by capability not API name (Pitfall 9). Write the "searched fdars for:" note on every partial/absent row; tag Confidence HIGH/MEDIUM; assign a D-03 category to every gap. After appending, add a short `### All-129 Coverage Check` note stating that all six area tables together cover the 129 in-scope Phase-7 rows (Representation 17 + Preprocessing 29 + Exploratory 20 + ML 20 + Inference 5 + Misc 38 = 129) and that none were re-enumerated. Prose and tables only — no fenced code, no fdars-core/src edits.</action>
  <verify>
    <automated>grep -q "### Area: Misc — Parity" .planning/research/AUDIT-REPORT.md && grep -q "### All-129 Coverage Check" .planning/research/AUDIT-REPORT.md && grep -q "= 129" .planning/research/AUDIT-REPORT.md && git diff --name-only -- fdars-core/src | grep -qv .</automated>
  </verify>
  <acceptance_criteria>
    - `### Area: Misc — Parity` table exists with one row per Phase-7 Misc in-scope capability
    - Every partial/absent row has a "searched fdars for:" note; every gap row has a D-03 category
    - `### All-129 Coverage Check` note confirms 17+29+20+20+5+38 = 129 in-scope rows mapped across the six tables
    - Verdicts mapped by capability not API name
    - `git diff --name-only -- fdars-core/src` is empty
  </acceptance_criteria>
  <done>Misc parity table complete; all 129 in-scope capabilities now marked present/partial/absent across the six area tables; coverage check present; no source edits.</done>
</task>

</tasks>

<verification>
- Five new area parity tables (Representation, Exploratory, ML, Inference, Misc) exist under `## Phase 8`
- All 129 in-scope capabilities marked present/partial/absent across the six tables (coverage check confirms 129)
- Every partial/absent row has a "searched fdars for:" note; GMM + seasonal fragile rows accuracy-flagged with citations
- Every gap row carries a D-03 category
- `git diff --name-only -- fdars-core/src` returns nothing (audit-only enforced)
</verification>

<success_criteria>
The parity matrix is complete: all 129 in-scope scikit-fda capabilities are marked
present/partial/absent against fdars using the Plan-01 schema, each gap carries a searched-note
and a category, and known-bug/fragile rows are accuracy-flagged with fix-commit citations.
Zero fdars-core/src edits.
</success_criteria>

<output>
Create `.planning/phases/08-capability-parity-matrix-categorization/08-02-SUMMARY.md` when done
</output>
