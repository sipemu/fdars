---
phase: 40-fr-chet-object-data-regression
plan: 03
type: execute
wave: 3
depends_on: [40-02]
files_modified:
  - fdars-core/src/frechet/anova.rs
  - fdars-core/src/frechet/mod.rs
  - fdars-core/src/inference/mod.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [FRE-01-08]
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A user can run a Fréchet ANOVA group-difference test on metric-space (density) responses and get a Dubey–Müller Tn statistic plus asymptotic and seeded-permutation p-values (FRE-01-08)."
    - "The test flags a genuine between-group shift (permutation p < 0.05) and does NOT flag a homogeneous sample (permutation p > 0.05)."
    - "The permutation p-value is reproducible for a fixed seed (per-thread StdRng::seed_from_u64(seed + k), 999 default)."
    - "Fewer than two groups returns FdarError; suite + clippy --all-targets stay green."
  artifacts:
    - fdars-core/src/frechet/anova.rs
    - "fdars-core/src/frechet/mod.rs (FrechetAnovaResult + re-export)"
    - "fdars-core/src/inference/mod.rs (expose chi_square_sf_df to crate)"
    - "fdars-core/src/lib.rs (frechet_anova re-export)"
  key_links:
    - "frechet_anova reuses frechet_mean/frechet_variance (Wave 1) for per-group and pooled Fréchet variances over a WassersteinDensitySpace."
    - "Asymptotic p-value reuses the in-crate inference::dist::chi_square_sf_df (NO new crate dependency, NOT statrs)."
    - "Permutation loop uses per-thread seeded RNG StdRng::seed_from_u64(seed.wrapping_add(k)) with DEFAULT_N_PERM = 999."
---

<objective>
Complete the phase with the Fréchet ANOVA group-difference test (Dubey–Müller Tn) in `frechet/anova.rs`, reusing the Wave-1 Fréchet mean/variance machinery and the crate's in-house chi-square survival function. Returns the combined statistic plus asymptotic χ²(k−1) and seeded-permutation p-values.

Purpose: Deliver the last FRE-01 capability (Fréchet ANOVA, R `frechet::DenANOVA`) reuse-first — no `statrs` dependency (the crate has `inference::dist::chi_square_sf_df`), per-thread seeded permutation matching INF-01's 999-perm convention.
Output: `frechet/anova.rs` with `frechet_anova`, `FrechetAnovaResult`, an additive `pub(crate)` exposure of the chi-square helper, and a crate-root re-export.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-fr-chet-object-data-regression/40-CONTEXT.md
@.planning/phases/40-fr-chet-object-data-regression/40-RESEARCH.md
@.planning/phases/40-fr-chet-object-data-regression/40-PATTERNS.md
@.planning/phases/40-fr-chet-object-data-regression/40-01-SUMMARY.md
@.planning/phases/40-fr-chet-object-data-regression/40-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New file:
- `fdars-core/src/frechet/anova.rs` — `frechet_anova`.

New public symbol (crate-root re-exported):
- `frechet_anova`, `FrechetAnovaResult`.

Modified files:
- `fdars-core/src/frechet/mod.rs` — declare `mod anova;`, define `FrechetAnovaResult`, add `pub use anova::{frechet_anova, FrechetAnovaResult};`.
- `fdars-core/src/inference/mod.rs` — change `mod dist;` to `pub(crate) mod dist;` OR add `pub(crate) use dist::chi_square_sf_df;` so `frechet/anova.rs` can reach the in-crate chi-square survival function (additive, non-breaking — no public signature changes).
- `fdars-core/src/lib.rs` — extend the frechet re-export block with `frechet_anova, FrechetAnovaResult`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fréchet ANOVA — Dubey–Müller Tn + asymptotic + seeded permutation p-value</name>
  <files>fdars-core/src/frechet/anova.rs, fdars-core/src/frechet/mod.rs, fdars-core/src/inference/mod.rs</files>
  <read_first>
    - fdars-core/src/frechet/anova.rs (new — does not exist yet)
    - fdars-core/src/frechet/mean.rs (Wave 1 — frechet_mean, frechet_variance)
    - fdars-core/src/frechet/space.rs (Wave 1 — WassersteinDensitySpace, distance)
    - fdars-core/src/inference/dist.rs lines 95-130 (chi_square_sf(x,k) @99, chi_square_sf_df(x,df) @118 — both pub(crate); the asymptotic p-value = chi_square_sf(Tn, k-1))
    - fdars-core/src/inference/mod.rs lines 29-36 (`mod dist;` is private to inference — must be exposed pub(crate))
    - fdars-core/src/inference/permutation.rs lines 18, 120-127, 183 (DEFAULT_N_PERM = 999 @18; Fisher-Yates shuffle; p = (n_ge+1)/(n_perm+1) @183)
    - fdars-core/src/elastic_explain.rs lines 313-314 (per-thread seeded RNG StdRng::seed_from_u64(seed.wrapping_add(k as u64)) — the required per-iteration pattern, NOT a single shared RNG)
    - fdars-core/src/helpers.rs line 4 (NUMERICAL_EPS = 1e-10 — σ̂ₗ² clamp)
    - fdars-core/src/fts/mod.rs lines 33-45 (result-struct template)
    - RESEARCH.md §7 Fréchet ANOVA (V̂ₗ, V̂ₚ, σ̂ₗ², Fₙ, Uₙ, Tₙ formulas; χ²(k-1); permutation p = (count≥ +1)/(B+1))
  </read_first>
  <behavior>
    - Two clearly-shifted groups (A = truncated N(-1,0.3), B = N(1,0.3)) → permutation p-value < 0.05.
    - Homogeneous sample (A = N(0,0.3), B = N(0,0.3)) → permutation p-value > 0.05.
    - Same seed → identical p_value_permutation across two calls (reproducibility).
    - Result carries statistic (Tn), p_value_asymptotic, p_value_permutation, n_perm, per-group Fréchet variances, pooled Fréchet variance, fn_statistic, un_statistic, group_labels.
    - Error: fewer than 2 distinct groups → InvalidParameter; group_labels length ≠ response rows → InvalidDimension; non-increasing argvals → InvalidParameter.
  </behavior>
  <action>
    Expose the chi-square helper: in `fdars-core/src/inference/mod.rs` change `mod dist;` to `pub(crate) mod dist;` (or add `pub(crate) use dist::{chi_square_sf, chi_square_sf_df};`). This is additive/non-breaking — no public API changes.

    Define `FrechetAnovaResult` in `frechet/mod.rs` (derive `Debug, Clone, PartialEq`, serde-gated, `#[non_exhaustive]`): fields `statistic: f64`, `p_value_asymptotic: f64`, `p_value_permutation: f64`, `n_perm: usize`, `group_frechet_variances: Vec<f64>`, `pooled_frechet_variance: f64`, `fn_statistic: f64`, `un_statistic: f64`, `group_labels: Vec<usize>`. Declare `mod anova;` and `pub use anova::{frechet_anova, FrechetAnovaResult};`.

    In `frechet/anova.rs`, implement `#[must_use = "..."] pub fn frechet_anova(responses: &FdMatrix, argvals: &[f64], group_labels: &[usize], n_perm: usize, seed: u64) -> Result<FrechetAnovaResult, FdarError>`:
    - Validate: responses.nrows() == group_labels.len() == n (InvalidDimension); responses.ncols() == argvals.len(); argvals strictly increasing; number of distinct groups k >= 2 (InvalidParameter{parameter:"group_labels", ...}).
    - Build a `WassersteinDensitySpace::new(argvals.to_vec())?`. Extract each response row as `Vec<f64>` object (density_matrix[(i,j)] loop / `.row(i)`).
    - Define a private `fn compute_tn(space, objects, labels, k) -> Result<(Tn, Fn, Un, group_vars, pooled_var), FdarError>` implementing the Dubey–Müller formulas (RESEARCH §7): per-group Fréchet mean μ̂ₗ (via frechet_mean over the group subset), V̂ₗ = (1/nₗ)Σ d²(Yᵢ,μ̂ₗ), pooled μ̂ and V̂ₚ; σ̂ₗ² = (1/nₗ)Σ [d²(Yᵢ,μ̂ₗ) − V̂ₗ]², clamped to NUMERICAL_EPS to avoid division blow-up (Pitfall 4); λₗ = nₗ/n; Fₙ = V̂ₚ − Σₗ λₗ V̂ₗ; Uₙ = Σⱼ<ₗ (λⱼλₗ/(σ̂ⱼ²σ̂ₗ²))(V̂ⱼ − V̂ₗ)²; Tₙ = n·Uₙ / Σₗ(λₗ/σ̂ₗ²) + n·Fₙ² / Σₗ(λₗ²σ̂ₗ²).
    - Observed Tn from `compute_tn`. Asymptotic p = `chi_square_sf(Tn_obs, k - 1)` (in-crate helper; χ²(k−1)).
    - Permutation loop: resolve `n_perm` (use DEFAULT_N_PERM=999 semantics if caller passes 0, or require > 0 — document the choice). For k in 0..n_perm: `let mut rng = StdRng::seed_from_u64(seed.wrapping_add(k as u64));` clone labels, `perm_labels.shuffle(&mut rng)` (rand::seq::SliceRandom); recompute Tn; count `n_ge` where Tn_perm >= Tn_obs. `p_value_permutation = (n_ge as f64 + 1.0) / (n_perm as f64 + 1.0)`. Optionally parallelize with `iter_maybe_parallel!` but keep per-iteration seeding (deterministic regardless of thread count).
    - Assemble and return the full `FrechetAnovaResult`.
    - Rustdoc: cite Dubey & Müller 2019 Biometrika; note the σ̂ₗ² formula is [ASSUMED] per RESEARCH Open Question 1 and permutation p is robust regardless; document the χ²(k−1) asymptotic and the 999-perm seeded convention.
    - Inline `#[cfg(test)] mod tests` covering the `<behavior>` items (reuse the truncated_gaussian helper; build multi-group response matrices).
  </action>
  <acceptance_criteria>
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel frechet::anova` exits 0.
    - Source: `frechet_anova` reuses `frechet_mean`/`frechet_variance` (grep) and `inference::dist::chi_square_sf` (grep) — NO `statrs` import anywhere in frechet/.
    - Source: permutation loop uses `StdRng::seed_from_u64(seed.wrapping_add(` per iteration (grep); `FrechetAnovaResult` derives + serde-gated + `#[non_exhaustive]`; function `#[must_use]`.
    - Behavior: shifted-group test asserts perm p < 0.05; homogeneous test asserts perm p > 0.05; two calls with same seed assert identical p_value_permutation.
    - Error paths: <2 groups → InvalidParameter; label/response length mismatch → InvalidDimension (no panic).
  </acceptance_criteria>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel frechet::anova</automated>
  </verify>
  <done>frechet_anova returns the Dubey–Müller Tn plus χ²(k−1) asymptotic and seeded-permutation p-values reusing frechet_mean/variance and the in-crate chi_square_sf; flags shifted groups, ignores homogeneous samples, is seed-reproducible, and validates inputs; no statrs dependency; tests pass.</done>
  <reversibility rating="reversible">Additive public API + a crate-internal `pub(crate)` module-visibility widening; no existing public signature changed.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Wave gate — re-exports, full suite + clippy green, phase close</name>
  <files>fdars-core/src/lib.rs, fdars-core/src/frechet/mod.rs</files>
  <read_first>
    - fdars-core/src/lib.rs lines 144-152 (frechet re-export block from Waves 1-2)
    - fdars-core/src/frechet/mod.rs (all waves)
  </read_first>
  <action>
    Extend the `src/lib.rs` frechet re-export block to include `frechet_anova, FrechetAnovaResult` (final block re-exports all of: `frechet_anova, frechet_global_reg, frechet_local_reg, frechet_mean, frechet_variance, FrechetAnovaResult, FrechetGlobalRegResult, FrechetLocalRegResult, MetricSpace, WassersteinDensitySpace`). Confirm `frechet/mod.rs` declares all four submodules (`anova, mean, regression, space`) and re-exports every public symbol. Run the full suite and the CI-equivalent clippy gate (lints test code); fix any warnings. Run `cargo fmt -p fdars-core`. Do NOT change any existing public signature (the only crate-internal change is the `pub(crate)` widening in inference/mod.rs).
  </action>
  <acceptance_criteria>
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel` exits 0.
    - `cargo clippy --all-targets --features linalg,parallel -- -D warnings` exits 0.
    - Source: `src/lib.rs` re-exports all ten frechet symbols; no existing public `pub fn`/`pub struct` signature changed.
    - `cargo fmt -p fdars-core -- --check` reports no drift.
  </acceptance_criteria>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel && cargo clippy --all-targets --features linalg,parallel -- -D warnings && cargo fmt -p fdars-core -- --check</automated>
  </verify>
  <done>Whole-crate suite green, clippy --all-targets clean, fmt clean, all ten frechet symbols re-exported at crate root, zero existing public signatures changed; phase FRE-01-01..08 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → frechet_anova | Caller supplies numeric response FdMatrix, argvals, integer group labels, n_perm, seed. Pure in-process numeric library; no external surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-08 | Tampering | frechet_anova (<2 groups, label/response mismatch, non-monotone grid) | low | mitigate | Entry-point validation → FdarError (never panic). |
| T-40-09 | Denial-of-Service | Tn divergence when a group is degenerate (σ̂ₗ² → 0) | low | mitigate | Clamp σ̂ₗ² to NUMERICAL_EPS before division (Pitfall 4). |
| T-40-10 | Repudiation | Non-reproducible permutation p-value undermining stated determinism | low | mitigate | Per-thread StdRng::seed_from_u64(seed.wrapping_add(k)); test asserts same-seed reproducibility. |
</threat_model>

<verification>
- `cargo test -p fdars-core --features linalg,parallel frechet::anova` green.
- Whole-crate suite + `cargo clippy --all-targets --features linalg,parallel -- -D warnings` green.
- No `statrs` import in frechet/ (in-crate chi_square_sf used instead).
- Zero changes to existing public signatures (only a crate-internal `pub(crate)` widening).
</verification>

<success_criteria>
- FRE-01-08 (Fréchet ANOVA) satisfied: Dubey–Müller Tn + asymptotic + seeded-permutation p-values, flags real differences, ignores homogeneous samples, seed-reproducible.
- All ten phase symbols crate-root re-exported.
- Additive/non-breaking; no new crate dependency.
</success_criteria>

<output>
Create `.planning/phases/40-fr-chet-object-data-regression/40-03-SUMMARY.md` when done.
</output>
