---
phase: 40-fr-chet-object-data-regression
plan: 02
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - fdars-core/src/frechet/regression.rs
  - fdars-core/src/frechet/mod.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [FRE-01-04, FRE-01-05, FRE-01-07]
estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A user can run global Fréchet regression with Euclidean predictors (Petersen–Müller global linear weights) and predict a conditional density response at new predictor values (FRE-01-04, FRE-01-07)."
    - "A user can run local (local-linear, Gaussian-kernel-weighted) Fréchet regression over Euclidean predictors (FRE-01-05)."
    - "On synthetic data from a known predictor→density-mean relationship, both regressions track the truth within a documented W₂ tolerance."
    - "Signed regression weights never reach wasserstein_barycenter — the signed_quantile_average path (sort-based monotone projection) is used, documented in rustdoc on both regression fns, and a predictor configuration yielding negative weights returns Ok with a finite monotone density (no InvalidParameter), per the RESEARCH KEY RISK."
    - "Invalid inputs (mismatched predictor/response counts, non-monotone/mismatched grids, h<=0, singular predictor covariance) return FdarError; suite + clippy --all-targets stay green."
  artifacts:
    - fdars-core/src/frechet/regression.rs
    - "fdars-core/src/frechet/mod.rs (FrechetGlobalRegResult, FrechetLocalRegResult + re-exports)"
    - "fdars-core/src/lib.rs (regression re-exports)"
  key_links:
    - "frechet_global_reg computes Σ̂⁻¹ via linalg::{cholesky_factor, cholesky_forward_back} with 1e-6 ridge, then signed_quantile_average (from Wave 1 space.rs)."
    - "frechet_local_reg computes a product Gaussian kernel via helpers::gaussian_kernel + local-linear mu0/mu1/mu2 correction, then signed_quantile_average."
    - "The density-response variant (FRE-01-07) is the same entry points specialized to WassersteinDensitySpace outputs (FdMatrix rows are densities)."
---

<objective>
Expand from the Wave-1 tracer to global and local Fréchet regression over Euclidean predictors, with the density-response variant, in `frechet/regression.rs`. Both use the signed quantile average from Wave 1 (never `wasserstein_barycenter`, because Petersen–Müller / local-linear weights can be negative — RESEARCH KEY RISK).

Purpose: Deliver the regression capability of the R `frechet` package (GloWassReg / LocWassReg) against the density/2-Wasserstein backend, reusing the crate's Cholesky solver and Gaussian kernel.
Output: `frechet/regression.rs` with `frechet_global_reg`, `frechet_local_reg`, result structs `FrechetGlobalRegResult` / `FrechetLocalRegResult`, and crate-root re-exports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-fr-chet-object-data-regression/40-CONTEXT.md
@.planning/phases/40-fr-chet-object-data-regression/40-RESEARCH.md
@.planning/phases/40-fr-chet-object-data-regression/40-PATTERNS.md
@.planning/phases/40-fr-chet-object-data-regression/40-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New file:
- `fdars-core/src/frechet/regression.rs` — `frechet_global_reg`, `frechet_local_reg`.

New public symbols (crate-root re-exported):
- `frechet_global_reg`, `frechet_local_reg`, `FrechetGlobalRegResult`, `FrechetLocalRegResult`.

New private helpers (crate-internal):
- predictor-moment helpers (X̄, Σ̂ row-major, ridge), product-kernel helper (inline), local-linear weight assembly.

Modified files:
- `fdars-core/src/frechet/mod.rs` — declare `mod regression;`, define the two result structs, add `pub use regression::{...}`.
- `fdars-core/src/lib.rs` — extend the frechet re-export block with the four regression symbols.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Global Fréchet regression (Petersen–Müller signed weights → signed quantile average)</name>
  <files>fdars-core/src/frechet/regression.rs, fdars-core/src/frechet/mod.rs</files>
  <read_first>
    - fdars-core/src/frechet/regression.rs (new — does not exist yet)
    - fdars-core/src/frechet/space.rs (Wave 1 — `signed_quantile_average` pub(crate), `density_to_quantile`, `WassersteinDensitySpace`)
    - fdars-core/src/linalg.rs lines 85-134 (cholesky_factor @85, cholesky_forward_back @113, cholesky_solve @131 — all pub(crate); v = Σ̂⁻¹(x−x̄))
    - fdars-core/src/elastic_changepoint.rs line 299 (ridge regularization `cov[(j,j)] += 1e-6` before Cholesky)
    - fdars-core/src/density_fda.rs lines 407-540 (n_q = m.max(101); back-map for reference)
    - fdars-core/src/matrix.rs lines 40-115 (from_column_major @50, zeros @84, row @row, ncols/nrows, Index)
    - fdars-core/src/fts/mod.rs lines 33-45 (result-struct derive/serde/non_exhaustive template)
    - fdars-core/src/error.rs lines 6-25 (FdarError field names)
    - RESEARCH.md §5 Global Fréchet Regression KEY RISK (sᵢ(x) = 1 + (Xᵢ−X̄)ᵀΣ̂⁻¹(x−X̄); gx = colMeans(qin*s); signed → sort-monotone, NOT wasserstein_barycenter)
  </read_first>
  <behavior>
    - On synthetic data (n=20 scalar predictors xᵢ ~ uniform[-1,1], response density = truncated N(xᵢ, 0.3) on a [-3,3] grid), prediction at x=0.5 yields a density with W₂ to N(0.5, 0.3) below 0.2.
    - The predicted FdMatrix has one row per xout row and m = argvals length columns; xout and x_bar are populated.
    - Signed weights are used: a predictor/xout configuration wide enough that some sᵢ go negative returns `Ok(..)` with a finite, monotone predicted quantile/density and does NOT raise InvalidParameter — proving wasserstein_barycenter is bypassed and the signed path accepts negative weights.
    - Error: predictor rows ≠ response rows → InvalidDimension; non-increasing argvals → InvalidParameter; all-identical predictors (singular Σ̂) handled by 1e-6 ridge (no panic); zero-range quantile average at an extreme extrapolation point → ComputationFailed.
  </behavior>
  <action>
    Define `FrechetGlobalRegResult` in `frechet/mod.rs` (derive `Debug, Clone, PartialEq`, serde-gated, `#[non_exhaustive]`): `predicted: FdMatrix` (n_out × m densities), `xout: FdMatrix` (n_out × p), `x_bar: Vec<f64>` (length p). Declare `mod regression;` and `pub use regression::{frechet_global_reg, FrechetGlobalRegResult};`.

    In `frechet/regression.rs`, implement `#[must_use = "..."] pub fn frechet_global_reg(predictors: &FdMatrix, responses: &FdMatrix, argvals: &[f64], xout: &FdMatrix) -> Result<FrechetGlobalRegResult, FdarError>`:
    - Validate: predictors.nrows() == responses.nrows() == n (InvalidDimension); responses.ncols() == argvals.len() == m; xout.ncols() == predictors.ncols() == p; argvals strictly increasing (InvalidParameter); n >= 1.
    - Compute X̄ (p-vector column means of predictors) and Σ̂ (p×p row-major sample covariance, divide by n-1; guard n==1). Ridge: `sigma_hat[j*p + j] += 1e-6` before factoring.
    - `let chol = cholesky_factor(&sigma_hat, p)?;` (propagate ComputationFailed on singular).
    - For each xout row `x_pred`: `diff_x = x_pred − X̄`; `v = cholesky_forward_back(&chol, &diff_x, p)` (= Σ̂⁻¹ diff_x); for each training obs i: `sᵢ = 1.0 + dot(Xᵢ − X̄, v)`. Note sᵢ may be negative — this is expected.
    - Normalize the signed weights to the R convention `gx = colMeans(qin*s)` i.e. divide by n (weights `sᵢ/n` summing to ~1). Then call `signed_quantile_average(responses, argvals, &weights, n_q)?` with `n_q = m.max(101)` → the predicted density row; write it into the predicted FdMatrix.
    - Assemble and return `FrechetGlobalRegResult { predicted, xout: xout.clone(), x_bar }`.
    - Rustdoc (REQUIRED on `frechet_global_reg`): cite Petersen & Müller 2019; explicitly state the function "uses the `signed_quantile_average` helper (sort-based isotonic projection), NOT `wasserstein_barycenter`, because Petersen–Müller global-linear weights sᵢ(x) = 1 + (Xᵢ−X̄)ᵀΣ̂⁻¹(x−X̄) can be negative (RESEARCH KEY RISK)"; document the sort-based monotone-projection divergence from R's osqp QP (reference the note in RESEARCH §5).
    - Inline `#[cfg(test)] mod tests` covering the `<behavior>` items; add a `truncated_gaussian` + `build_response_matrix` local test helper.
  </action>
  <acceptance_criteria>
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel frechet::regression::tests::global` exits 0.
    - Source: `frechet_global_reg` computes Σ̂⁻¹ via `cholesky_factor` + `cholesky_forward_back` (grep) and calls `signed_quantile_average` (grep) — it does NOT call `wasserstein_barycenter`.
    - Source: `frechet_global_reg` carries a rustdoc note citing the `signed_quantile_average` divergence and the negative-weight RESEARCH KEY RISK (grep for `signed_quantile_average` in the doc comment).
    - Source: `FrechetGlobalRegResult` derives Debug/Clone/PartialEq, serde-gated, `#[non_exhaustive]`; function is `#[must_use]`.
    - Behavior: predicted density at x=0.5 has W₂ to N(0.5,0.3) < 0.2 (test asserts using the public `wasserstein2_distance` from Wave 1).
    - Behavior (negative-weight non-error test): a predictor configuration wide enough that some sᵢ go negative still returns `Ok(..)` with a finite, monotone predicted quantile/density (no `InvalidParameter`) — asserts the signed path does not reject negative weights.
    - Error paths: mismatched predictor/response counts, non-increasing argvals return FdarError; wide predictor spread producing negative sᵢ does NOT error out.
  </acceptance_criteria>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel frechet::regression::tests::global</automated>
  </verify>
  <done>frechet_global_reg predicts conditional density responses via Petersen–Müller signed weights + signed_quantile_average (documented in rustdoc), tracks a known relationship within tolerance, handles singular Σ̂ via ridge, never routes signed weights to wasserstein_barycenter, and returns Ok (not InvalidParameter) when weights go negative; tests pass.</done>
  <reversibility rating="reversible">Additive public API; no existing signature touched.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Local (local-linear, kernel-weighted) Fréchet regression</name>
  <files>fdars-core/src/frechet/regression.rs, fdars-core/src/frechet/mod.rs</files>
  <read_first>
    - fdars-core/src/frechet/regression.rs (from Task 1)
    - fdars-core/src/helpers.rs line 247 (gaussian_kernel(d, h): returns 0 for h < 1e-15)
    - fdars-core/src/frechet/space.rs (signed_quantile_average)
    - fdars-core/src/linalg.rs lines 85-134 (μ₂⁻¹ for the local-linear correction via cholesky_solve)
    - RESEARCH.md §6 Local Fréchet Regression (product kernel Kᵢ = ∏ⱼ gaussian_kernel(Xᵢⱼ − x₀ⱼ, h); mu0/mu1/mu2; sᵢ = Kᵢ(1 − mu1ᵀ μ₂⁻¹ (Xᵢ − x₀)))
    - fdars-core/src/fts/mod.rs lines 33-45 (result-struct template)
  </read_first>
  <behavior>
    - On the same synthetic setup as Task 1, local regression at x=0.0 (near training points) yields a density with W₂ to N(0.0, 0.3) below 0.15 (tighter than global).
    - Product Gaussian kernel over p predictors is used; bandwidth h is user-supplied and applied per predictor dimension.
    - Local-linear correction weights sᵢ can be negative: a configuration that produces negative sᵢ returns `Ok(..)` with a finite, monotone predicted quantile/density (no InvalidParameter) — the signed path is exercised.
    - Error: h <= 0 or non-finite → InvalidParameter; predictor/response count mismatch → InvalidDimension; non-increasing argvals → InvalidParameter; singular μ₂ handled via 1e-6 ridge (no panic).
  </behavior>
  <action>
    Define `FrechetLocalRegResult` in `frechet/mod.rs` (same derives/serde/non_exhaustive): `predicted: FdMatrix`, `xout: FdMatrix`, `bandwidth: f64`. Add `pub use regression::{frechet_local_reg, FrechetLocalRegResult};`.

    In `frechet/regression.rs`, implement `#[must_use = "..."] pub fn frechet_local_reg(predictors: &FdMatrix, responses: &FdMatrix, argvals: &[f64], xout: &FdMatrix, bandwidth: f64) -> Result<FrechetLocalRegResult, FdarError>`:
    - Validate as in Task 1, plus `bandwidth > 0.0 && bandwidth.is_finite()` (InvalidParameter{parameter:"bandwidth", ...}) — do NOT rely on gaussian_kernel silently returning 0.
    - For each xout row `x0`:
      - `Kᵢ = (0..p).map(|j| gaussian_kernel(Xᵢⱼ − x0ⱼ, bandwidth)).product::<f64>()` per training obs i.
      - `mu0 = mean(K)` (scalar); `mu1 = (1/n) Σᵢ Kᵢ (Xᵢ − x0)` (p-vector); `mu2 = (1/n) Σᵢ Kᵢ (Xᵢ − x0)(Xᵢ − x0)ᵀ` (p×p row-major); ridge `mu2[j*p+j] += 1e-6`.
      - Solve `μ₂⁻¹ mu1` via `cholesky_solve(&mu2, &mu1, p)?` → vector `a`; then `sᵢ = Kᵢ · (1 − dot(Xᵢ − x0, a))` (local-linear correction; sᵢ may be negative).
      - Normalize signed weights to sum-1 convention (divide by Σᵢ sᵢ if non-degenerate; guard |Σ sᵢ| < NUMERICAL_EPS → ComputationFailed) then `signed_quantile_average(responses, argvals, &weights, n_q)?`.
    - Return `FrechetLocalRegResult { predicted, xout: xout.clone(), bandwidth }`.
    - Rustdoc (REQUIRED on `frechet_local_reg`): cite LocWassReg (Fan–Gijbels local-linear); explicitly state the function "uses the `signed_quantile_average` helper (sort-based isotonic projection), NOT `wasserstein_barycenter`, because local-linear correction weights sᵢ = Kᵢ(1 − (Xᵢ−x₀)ᵀμ₂⁻¹mu1) can be negative (RESEARCH KEY RISK)"; document the single-bandwidth product kernel (assumption A4) and the sort-monotone divergence from R's osqp QP.
    - Extend the inline test module with local-regression tests per `<behavior>` (reuse Task 1 helpers), the negative-weight non-error test, plus the h<=0 error path.
  </action>
  <acceptance_criteria>
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel frechet::regression::tests::local` exits 0.
    - Source: uses `helpers::gaussian_kernel` in a product over p predictors (grep) and `signed_quantile_average` (grep); validates `bandwidth > 0` explicitly at entry.
    - Source: `frechet_local_reg` carries a rustdoc note citing the `signed_quantile_average` divergence and the negative-weight RESEARCH KEY RISK (grep for `signed_quantile_average` in the doc comment).
    - Source: `FrechetLocalRegResult` derives + serde-gated + `#[non_exhaustive]`; function `#[must_use]`.
    - Behavior: local prediction at x=0.0 has W₂ to N(0,0.3) < 0.15 (test asserts).
    - Behavior (negative-weight non-error test): a configuration producing negative local-linear sᵢ returns `Ok(..)` with a finite, monotone predicted quantile/density (no InvalidParameter).
    - Error path: h=0 and h<0 return InvalidParameter (no panic; no silent all-zero weights).
  </acceptance_criteria>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel frechet::regression::tests::local</automated>
  </verify>
  <done>frechet_local_reg performs local-linear kernel-weighted regression via a product Gaussian kernel and mu0/mu1/mu2 correction, validates bandwidth, tracks the truth tighter than global near training points, uses signed_quantile_average (documented in rustdoc, negative weights return Ok not InvalidParameter); tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Wave gate — re-exports, full suite + clippy green</name>
  <files>fdars-core/src/lib.rs, fdars-core/src/frechet/mod.rs</files>
  <read_first>
    - fdars-core/src/lib.rs lines 144-152 (frechet re-export block from Wave 1)
    - fdars-core/src/frechet/mod.rs (Tasks 1-2)
  </read_first>
  <action>
    Extend the `src/lib.rs` frechet re-export block to include `frechet_global_reg, frechet_local_reg, FrechetGlobalRegResult, FrechetLocalRegResult`. Confirm `frechet/mod.rs` declares `mod regression;` and re-exports all four regression symbols. Run the full suite and the CI-equivalent clippy gate (lints test code); fix any warnings. Run `cargo fmt -p fdars-core`. Do NOT change any existing public signature. Density-response variant (FRE-01-07) is covered by the global/local functions specialized to the WassersteinDensitySpace density outputs — no separate function needed; note this in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel` exits 0.
    - `cargo clippy --all-targets --features linalg,parallel -- -D warnings` exits 0.
    - Source: `src/lib.rs` re-exports all four regression symbols; no existing public signature changed.
    - `cargo fmt -p fdars-core -- --check` reports no drift.
  </acceptance_criteria>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel && cargo clippy --all-targets --features linalg,parallel -- -D warnings && cargo fmt -p fdars-core -- --check</automated>
  </verify>
  <done>Whole-crate suite green, clippy --all-targets clean, fmt clean, regression symbols re-exported at crate root, density-response variant confirmed via the density space, zero existing public signatures changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → frechet regression fns | Caller supplies numeric predictor/response FdMatrix, argvals, xout, bandwidth. Pure in-process numeric library; no external surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-04 | Tampering | frechet_global_reg (singular predictor covariance Σ̂) | low | mitigate | 1e-6 ridge before Cholesky; ComputationFailed if still non-PD (no panic). |
| T-40-05 | Tampering | frechet_local_reg (h<=0, singular μ₂) | low | mitigate | Explicit `bandwidth > 0` validation at entry; 1e-6 ridge on μ₂; FdarError on degenerate. |
| T-40-06 | Denial-of-Service | signed_quantile_average at extreme extrapolation (near-cancelling signed weights) | low | mitigate | zero-range guard → ComputationFailed; fixed-size n_q grid (no unbounded work). |
| T-40-07 | Tampering | mismatched predictor/response/xout dimensions | low | mitigate | Entry-point dimension validation → InvalidDimension. |
</threat_model>

<verification>
- `cargo test -p fdars-core --features linalg,parallel frechet::regression` green.
- Whole-crate suite + `cargo clippy --all-targets --features linalg,parallel -- -D warnings` green.
- Signed weights never reach wasserstein_barycenter (source grep + no InvalidParameter on wide predictor spread).
- Zero changes to existing public signatures.
</verification>

<success_criteria>
- FRE-01-04 (global regression), FRE-01-05 (local regression), FRE-01-07 (density-response) satisfied with inline tests tracking a known predictor→density relationship within documented W₂ tolerance.
- Divergence from R osqp (sort-based monotone projection) documented in rustdoc.
- Additive/non-breaking; no new crate dependency.
</success_criteria>

<output>
Create `.planning/phases/40-fr-chet-object-data-regression/40-02-SUMMARY.md` when done.
</output>
