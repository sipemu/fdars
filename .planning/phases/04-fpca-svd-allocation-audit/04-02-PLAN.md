---
phase: 04-fpca-svd-allocation-audit
plan: 02
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - fdars-core/benches/audit_hotpaths.rs
  - fdars-core/tests/alloc_audit_fpca.rs
  - .planning/research/AUDIT-REPORT.md
  - .planning/phases/04-fpca-svd-allocation-audit/04-COVERAGE.md
  - ".planning/research/bench/p4_fpca_linalg,parallel_run1.txt"
  - .planning/research/bench/p4_dhat_fpca_n500_m200.txt
autonomous: true
requirements: [PERF-03, PERF-04]
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "One new criterion bench function bench_p4_fpca exists in audit_hotpaths.rs, registered in criterion_group!, covering the N=500,M=200 FPCA cell, and runs release + linalg,parallel + black_box"
    - "The N=500,M=200 dhat cell count_fpca_allocations_n500_m200 runs under --features dhat-heap,linalg and emits total_blocks/total_bytes/peak_bytes"
    - "A reproducible dhat baseline is saved under .planning/research/bench/p4_dhat_fpca_n500_m200.txt with a total_blocks value"
    - "AUDIT-REPORT.md contains a Phase-4 section header and one criterion results row for fdata_to_pc_1d N=500,M=200 linking to its bench artifact, plus one dhat allocation line derived from the baseline"
    - "AUDIT-REPORT.md states one derived copy-share % for the N=500,M=200 path, computed from the measured dhat bytes and measured criterion time (not a theoretical estimate)"
    - statement: "The stated copy-share % in the Phase-4 tracer row is arithmetically consistent with the measured dhat total_bytes in p4_dhat_fpca_n500_m200.txt and the measured criterion time in p4_fpca_linalg,parallel_run1.txt"
      verification: backstop
  artifacts:
    - fdars-core/benches/audit_hotpaths.rs
    - fdars-core/tests/alloc_audit_fpca.rs
    - .planning/research/AUDIT-REPORT.md
    - .planning/phases/04-fpca-svd-allocation-audit/04-COVERAGE.md
    - ".planning/research/bench/p4_fpca_linalg,parallel_run1.txt"
    - .planning/research/bench/p4_dhat_fpca_n500_m200.txt
  key_links:
    - "bench_p4_fpca registered in criterion_group! → runnable via cargo bench -- audit_p4_fpca"
    - "count_fpca_allocations_n500_m200 (from Plan 01) → run produces p4_dhat_fpca_n500_m200.txt baseline"
    - "criterion time + dhat total_bytes → derived copy-share % row in AUDIT-REPORT (SVD-compute vs copy split, Pitfall 5)"
---

<objective>
**As a** performance auditor feeding the Phase 6 go/no-go decision, **I want to** prove the whole Phase-4 measurement→report pipeline end-to-end on ONE FPCA cell — criterion bench cell → dhat allocation run → raw artifacts → one AUDIT-REPORT row with a derived copy-share % — **so that** the full N×M grid + elastic cells + backlog in Plan 03 expands a proven pipeline instead of discovering the criterion/dhat/report wiring on the last cell.

This is the tracer slice for the phase's single vertical slice (measure wall-clock → measure allocations → quantify copy-share → report). It touches every layer the phase will modify: the criterion bench (`audit_hotpaths.rs`), the dhat harness (`alloc_audit_fpca.rs`, wired in Plan 01), the raw artifacts (`bench/p4_*`), and the report (`AUDIT-REPORT.md`) — one cell only.

No `fdars-core/src` algorithm changes — measurement infrastructure only (audit phase).

Purpose: De-risk the whole slice on the agent's best early-context tokens; catch any criterion/dhat/report/copy-share wiring problem after ONE cell (N=500,M=200, the Phase-1 sentinel size with a known ~16 ms baseline) rather than after the full 6-cell grid.
Output: One new bench fn `bench_p4_fpca` (N=500,M=200 cell), one dhat baseline `p4_dhat_fpca_n500_m200.txt`, one criterion artifact `p4_fpca_linalg,parallel_run1.txt`, a Phase-4 AUDIT-REPORT section with one criterion row + one dhat line + one derived copy-share %, and the 04-COVERAGE.md stub.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fpca-svd-allocation-audit/04-RESEARCH.md
@.planning/phases/04-fpca-svd-allocation-audit/04-PATTERNS.md
@.planning/phases/04-fpca-svd-allocation-audit/04-VALIDATION.md

# The bench file to EXTEND (generate_curves, bench_fpca_sentinel analog, criterion_group!)
@fdars-core/benches/audit_hotpaths.rs

# The dhat harness wired in Plan 01 (to run + confirm baseline)
@fdars-core/tests/alloc_audit_fpca.rs

# The copy-site source-of-truth for the report/copy-share row
@fdars-core/src/regression.rs

# The report to APPEND to (single growing file per Phase-1 D-05)
@.planning/research/AUDIT-REPORT.md
</context>

<artifacts_this_phase_produces>
This plan (tracer) introduces these NEW symbols. Plan 03 adds the rest of the grid + elastic cells + full analysis.

**New bench functions (in `fdars-core/benches/audit_hotpaths.rs`):**
- `bench_p4_fpca` — `fdata_to_pc_1d` at N=500,M=200 (this cell only; Plan 03 expands to the full 6-cell grid and adds `bench_p4_elastic_fpca`)

**New criterion group name:** `"audit_p4_fpca"` (created here; Plan 03 adds cells to it plus `"audit_p4_elastic_fpca"`).

**New artifact files (under `.planning/research/bench/`):**
- `p4_fpca_linalg,parallel_run1.txt` — raw criterion output for this tracer cell (Plan 03 adds run2 + full-grid + no-parallel artifacts)
- `p4_dhat_fpca_n500_m200.txt` — dhat baseline for `fdata_to_pc_1d` (Plan 03 adds the vert/joint dhat baselines + optional .json profile)

**New files:**
- `.planning/phases/04-fpca-svd-allocation-audit/04-COVERAGE.md` — the benchmark-only API-coverage stub

**AUDIT-REPORT.md:** new `## Phase 4` section skeleton with one criterion row, one dhat line, one derived copy-share % (Plan 03 fills the rest of the grid, the go/no-go verdict, and the backlog).
</artifacts_this_phase_produces>

<precondition>
Plan 04-01 must be complete: `dhat` dev-dep + `dhat-heap` feature in Cargo.toml and the `alloc_audit_fpca.rs` harness with `count_fpca_allocations_n500_m200` must exist and compile. Also: bench/test linking requires a non-full temp dir — `mkdir -p /home/simonm/.cache/fdars-bench-tmp` and prefix every `cargo bench`/`cargo test` with `TMPDIR=/home/simonm/.cache/fdars-bench-tmp` (the /tmp tmpfs is near-full and causes linker bus-errors; STATE.md decision, MEMORY.md, PITFALLS Pitfall 8 — infrastructure, not a code defect).
</precondition>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Add the fdata_to_pc_1d tracer bench cell and run it release + linalg,parallel</name>
  <read_first>
    - fdars-core/benches/audit_hotpaths.rs (lines 38-52 generate_curves; lines 82-96 bench_fpca_sentinel — exact template; lines 736-751 criterion_group!)
    - fdars-core/src/regression.rs (lines 249-322: fdata_to_pc_1d signature (data, ncomp, argvals))
    - .planning/phases/04-fpca-svd-allocation-audit/04-PATTERNS.md (bench_p4_fpca template, lines 64-118; TMPDIR + artifact-naming shared patterns, lines 327-346)
    - .planning/research/bench/p1_fpca_linalg,parallel_run1.txt (Phase-1 artifact format to match — env header + /release/ confirmation)
  </read_first>
  <files>fdars-core/benches/audit_hotpaths.rs, .planning/research/bench/p4_fpca_linalg,parallel_run1.txt</files>
  <action>
    Add one new bench function `bench_p4_fpca(c: &mut Criterion)` to audit_hotpaths.rs mirroring `bench_fpca_sentinel` (lines 82-96) EXACTLY in structure: benchmark_group named `"audit_p4_fpca"`, build `(data, argvals)` via `generate_curves(500, 200)` OUTSIDE `b.iter()`, and inside `b.iter()` call `fdata_to_pc_1d` with all inputs wrapped in `black_box` (data, `5usize` ncomp, argvals). Cell name `"n500_m200"`. Set `group.sample_size(20)`, `group.measurement_time(Duration::from_secs(20))`, `group.warm_up_time(Duration::from_secs(5))` (sentinel defaults — this cell is ~16 ms). Register `bench_p4_fpca` in the `criterion_group!` macro after the existing `bench_p3_*` entries.

    Write a `///` doc comment on `bench_p4_fpca` in the sentinel style: state it is the Phase-4 FPCA cell, N=500,M=200, ncomp=5, and that Plan 03 expands it to the full N∈{100,500,1000}×M∈{50,200} grid. Do NOT modify or replace `bench_fpca_sentinel` — it is a separate Phase-1 group.

    Then run the cell: `mkdir -p /home/simonm/.cache/fdars-bench-tmp` and run cargo bench with TMPDIR set to that dir, features linalg,parallel, target `--bench audit_hotpaths`, filtered to the group `audit_p4_fpca`, teeing full stdout+stderr to the artifact path. Prepend an `=== ENVIRONMENT ===` header capturing `rustc --version` and `cargo --version` and the feature set `linalg,parallel`, matching the Phase-1/Phase-3 artifact convention; confirm the criterion output shows a `/release/` target path.

    If the run fails with a linker/allocator bus-error (SIGBUS, "No space left"), that is an INFRASTRUCTURE failure per Pitfall 8 — confirm TMPDIR is set / free /tmp and retry; do NOT edit the bench source to work around it.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --benches --features linalg,parallel 2>&1 | tail -3 && grep -c "fn bench_p4_fpca" fdars-core/benches/audit_hotpaths.rs && test -f ".planning/research/bench/p4_fpca_linalg,parallel_run1.txt" && grep -Eiq "time:|n500_m200" ".planning/research/bench/p4_fpca_linalg,parallel_run1.txt" && echo TRACER_BENCH_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fn bench_p4_fpca" fdars-core/benches/audit_hotpaths.rs` returns 1
    - `bench_p4_fpca` appears in the `criterion_group!` list after the `bench_p3_*` entries (grep confirms)
    - The `b.iter()` body calls `fdata_to_pc_1d` with `black_box`-wrapped inputs and cell name `"n500_m200"`
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --benches --features linalg,parallel` exits 0
    - File `.planning/research/bench/p4_fpca_linalg,parallel_run1.txt` exists, contains a criterion timing line (matches `time:` or `n500_m200`), records the toolchain version + feature set `linalg,parallel`, and confirms a `/release/` binary path (Pitfall 1)
  </acceptance_criteria>
  <done>The audit bench has a new bench_p4_fpca N=500,M=200 cell registered in criterion_group!, and its release+linalg,parallel criterion output is saved under bench/p4_* with toolchain+feature tagging.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run the dhat harness for the N=500,M=200 cell and save the reproducible baseline</name>
  <read_first>
    - fdars-core/tests/alloc_audit_fpca.rs (the count_fpca_allocations_n500_m200 cell wired in Plan 01)
    - .planning/phases/04-fpca-svd-allocation-audit/04-RESEARCH.md (Section 4B run command; Section 4C — total_blocks/total_bytes/peak_bytes to record; Section 4D baseline location)
    - .planning/phases/04-fpca-svd-allocation-audit/04-PATTERNS.md (dhat run command lines 188-193; artifact-naming shared pattern lines 337-346)
  </read_first>
  <files>.planning/research/bench/p4_dhat_fpca_n500_m200.txt</files>
  <action>
    Run the dhat integration test for the primary FPCA cell with TMPDIR set to `/home/simonm/.cache/fdars-bench-tmp`, features `dhat-heap,linalg`, targeting the `alloc_audit_fpca` test, filtered to `count_fpca_allocations_n500_m200`, with `--nocapture` so the printed HeapStats reach stdout. Tee full output to `.planning/research/bench/p4_dhat_fpca_n500_m200.txt`. Prepend an `=== ENVIRONMENT ===` header with `rustc --version`, `cargo --version`, and the feature set `dhat-heap,linalg`, matching the artifact convention.

    The baseline file MUST contain the printed `total_blocks`, `total_bytes`, and `peak_bytes` values for the `fdata_to_pc_1d` N=500,M=200 call. If dhat also writes a `dhat-heap.json` profile in the working directory, move/copy it to `.planning/research/bench/p4_dhat_fpca_n500_m200.json` (optional — Plan 03 may extend it); do not fail the task if the JSON is absent (the `testing()` builder may not emit it).

    Do NOT hard-assert a specific allocation count — this is a baseline, not a regression gate (RESEARCH §4B). If the test binary fails to link with SIGBUS/"No space left", that is a Pitfall-8 infrastructure failure — confirm TMPDIR and retry; do not edit source to work around it.
  </action>
  <verify>
    <automated>test -f .planning/research/bench/p4_dhat_fpca_n500_m200.txt && grep -Eiq "total_blocks|total_bytes|peak_bytes" .planning/research/bench/p4_dhat_fpca_n500_m200.txt && echo DHAT_BASELINE_OK</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/research/bench/p4_dhat_fpca_n500_m200.txt` exists
    - The file contains a numeric `total_blocks` value and a numeric `total_bytes` value for the `fdata_to_pc_1d` N=500,M=200 call (`grep -Ec "total_blocks|total_bytes|peak_bytes"` ≥ 3)
    - The file records the toolchain version and the feature set `dhat-heap,linalg`
    - The run used `--features dhat-heap,linalg` (feature-gated dhat, not the default build)
  </acceptance_criteria>
  <done>The dhat baseline for fdata_to_pc_1d at N=500,M=200 is saved under bench/p4_dhat_* with total_blocks/total_bytes/peak_bytes and toolchain+feature tagging — the reproducible allocation baseline exists.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Append the Phase-4 AUDIT-REPORT skeleton (one criterion row + one dhat line + one derived copy-share %) and create 04-COVERAGE.md</name>
  <read_first>
    - .planning/research/AUDIT-REPORT.md (tail — the Phase-3 section lines 337-360 for table/section markdown style; append target, D-05 append-only)
    - fdars-core/src/regression.rs (lines 167, 291, 298 — the three alloc sites to cite: center_columns, centered.clone(), weighted.to_dmatrix())
    - fdars-core/src/matrix.rs (lines 310-312 — to_dmatrix() = DMatrix::from_column_slice, column-major memcpy)
    - .planning/phases/04-fpca-svd-allocation-audit/04-PATTERNS.md (AUDIT-REPORT append section lines 237-297; results-table schema; copy-share method lines 293-297)
    - .planning/research/bench/p4_fpca_linalg,parallel_run1.txt AND .planning/research/bench/p4_dhat_fpca_n500_m200.txt (the two measured numbers feeding the copy-share %)
    - .planning/phases/03-elastic-alignment-hot-path/03-COVERAGE.md (exact one-line stub to mirror for 04-COVERAGE.md)
  </read_first>
  <files>.planning/research/AUDIT-REPORT.md, .planning/phases/04-fpca-svd-allocation-audit/04-COVERAGE.md</files>
  <action>
    APPEND (do NOT overwrite — single growing report per D-05) a new section to AUDIT-REPORT.md titled `## Phase 4: FPCA/SVD & Allocation Audit — Benchmark Results` with the sub-headers `### Results Table (criterion — full N×M grid)`, `### Allocation Audit (dhat — bytes/allocations per FPCA call)`, and `### SVD-Compute vs Copy Split`. Plan 03 will add `### Phase 6 Go/No-Go Decision` and `### Draft Backlog` — leave placeholders `[Plan 03: full grid]`, `[Plan 03: go/no-go verdict]`, `[Plan 03: backlog]` where those go.

    Under the Results Table, create the 8-column header `| Target | Cell (N×M) | Features | Mean time (run1) | Mean time (run2) | Variance | Confidence | Artifact |` + separator, then populate exactly ONE row from the tracer run: `fdata_to_pc_1d`, `500×200`, `linalg,parallel`, the mean time transcribed from `p4_fpca_linalg,parallel_run1.txt`, run2 `[Plan 03]`, variance `[Plan 03]`, confidence `PENDING (single run)`, and a markdown link to the `p4_fpca_linalg,parallel_run1.txt` artifact.

    Under the Allocation Audit, add one line transcribing the measured `total_blocks`/`total_bytes`/`peak_bytes` from `p4_dhat_fpca_n500_m200.txt`, and a prose note distinguishing the ONE copy site (`regression.rs:298` `weighted.to_dmatrix()` — `DMatrix::from_column_slice`, cite matrix.rs:310-312) from the two `FdMatrix` allocations (`regression.rs:167` `center_columns`, `:291` `centered.clone()` — the zero-copy candidate). Cite each file:line. Do NOT attribute the covariance-SVD elastic sites (elastic_fpca.rs:122/399, native DMatrix, no to_dmatrix copy) to the copy bucket.

    Under SVD-Compute vs Copy Split, state ONE derived copy-share % for the N=500,M=200 path computed from the two measured numbers: copy bytes = the `to_dmatrix()` allocation size (500×200×8 = 800 KB, cross-check against measured total_bytes), copy time estimated from those bytes and stated memory bandwidth, expressed as a % of the measured criterion wall-clock. Show the arithmetic inline (bytes → copy µs → % of wall-clock ms) so the number is traceable to both artifacts, NOT a bare theoretical estimate. Do NOT state the final Phase 6 go/no-go verdict here — that is Plan 03 after the full grid — but note the tracer copy-share direction (e.g. "copy appears negligible vs SVD compute — confirm across grid in Plan 03").

    Create `.planning/phases/04-fpca-svd-allocation-audit/04-COVERAGE.md` mirroring the Phase-3 stub exactly: one line stating this is a benchmark-only phase measuring internal fdars-core FPCA/SVD functions plus dhat allocation profiling, with no external API integration.
  </action>
  <verify>
    <automated>grep -q "Phase 4: FPCA/SVD & Allocation Audit" .planning/research/AUDIT-REPORT.md && grep -q "p4_fpca" .planning/research/AUDIT-REPORT.md && grep -Eq "regression.rs:(167|291|298)" .planning/research/AUDIT-REPORT.md && grep -Eiq "copy.*%|allocation.*share|copy.*share" .planning/research/AUDIT-REPORT.md && test -f .planning/phases/04-fpca-svd-allocation-audit/04-COVERAGE.md && echo REPORT_OK</automated>
  </verify>
  <acceptance_criteria>
    - AUDIT-REPORT.md contains a `## Phase 4: FPCA/SVD & Allocation Audit` section (appended; Phase 1/2/3 sections intact)
    - The Results Table has the 8-column header and exactly one populated `fdata_to_pc_1d` `500×200` row linking to `p4_fpca_linalg,parallel_run1.txt`
    - The Allocation Audit line transcribes measured total_blocks/total_bytes/peak_bytes and cites `regression.rs:167`, `:291`, `:298` distinguishing the one `to_dmatrix()` copy from the two FdMatrix allocations
    - The report does NOT attribute `elastic_fpca.rs:122`/`:399` to the copy-overhead bucket (grep confirms neither line is listed under the copy split as a to_dmatrix site)
    - The SVD-Compute vs Copy Split states one copy-share % with inline arithmetic tied to both `p4_fpca_linalg,parallel_run1.txt` and `p4_dhat_fpca_n500_m200.txt`
    - `.planning/phases/04-fpca-svd-allocation-audit/04-COVERAGE.md` exists with the benchmark-only stub
    - Prior AUDIT-REPORT content (Phase 1/2/3) unchanged (append-only)
  </acceptance_criteria>
  <done>AUDIT-REPORT.md has a Phase-4 section with one measured criterion row, one dhat allocation line citing the three regression.rs sites, and one derived copy-share % traceable to both artifacts; 04-COVERAGE.md exists — proving the report+copy-share end of the pipeline works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Benchmark + measurement-only phase. Bench and dhat code read only seeded synthetic data built in-process (`generate_curves`/`generate_test_curves`, no RNG, no I/O input). Report writes are to local markdown. No network, no auth, no untrusted input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Tampering | cargo bench/test dependency graph (criterion + dhat already dev-deps from Plan 01; no new packages this plan) | low | accept | No new packages installed this plan — dhat was added in Plan 01. Audit-only measurement code, feature-gated dhat. No package-legitimacy gate needed. |

N/A — no attack surface. At ASVS L1 this is a read-only local measurement phase: running benches + a feature-gated dhat profiler and appending to a local report introduces no network, input-handling, auth, or persistence attack surface. No `high` threats.
</threat_model>

<verification>
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --benches --features linalg,parallel` exits 0 with `bench_p4_fpca` present and registered.
- `p4_fpca_linalg,parallel_run1.txt` exists with a criterion timing line + toolchain/feature tag + `/release/` confirmation.
- `p4_dhat_fpca_n500_m200.txt` exists with total_blocks/total_bytes/peak_bytes + feature tag `dhat-heap,linalg`.
- AUDIT-REPORT.md Phase-4 section exists with one criterion row + one dhat line citing regression.rs:167/291/298 + one derived copy-share %; earlier sections intact.
- `04-COVERAGE.md` exists with the benchmark-only stub.
</verification>

<success_criteria>
The thin end-to-end slice works: one FPCA cell benched release+linalg,parallel → dhat baseline saved → one AUDIT-REPORT criterion row + dhat line + derived copy-share % → 04-COVERAGE.md stub. The pipeline (bench → dhat → artifacts → report → copy-share) is proven so Plan 03 can expand to the full grid + elastic cells + go/no-go verdict + backlog without re-discovering wiring.
</success_criteria>

<output>
Create `.planning/phases/04-fpca-svd-allocation-audit/04-02-SUMMARY.md` when done.
</output>
