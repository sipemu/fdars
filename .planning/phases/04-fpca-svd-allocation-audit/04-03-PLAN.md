---
phase: 04-fpca-svd-allocation-audit
plan: 03
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - fdars-core/benches/audit_hotpaths.rs
  - fdars-core/tests/alloc_audit_fpca.rs
  - .planning/research/AUDIT-REPORT.md
  - ".planning/research/bench/p4_fpca_linalg,parallel_run2.txt"
  - .planning/research/bench/p4_fpca_linalg_run1.txt
  - ".planning/research/bench/p4_elastic_fpca_vert_linalg,parallel_run1.txt"
  - ".planning/research/bench/p4_elastic_fpca_joint_linalg,parallel_run1.txt"
  - .planning/research/bench/p4_dhat_vert_fpca_n100_m50.txt
  - .planning/research/bench/p4_dhat_joint_fpca_n100_m50.txt
autonomous: true
requirements: [PERF-03, PERF-04]
estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "bench_p4_fpca covers the full N in {100,500,1000} x M in {50,200} grid (6 cells) and runs release + linalg,parallel with a second run for variance, plus one no-parallel (linalg-only) run confirming FPCA timings are parallel-invariant"
    - "A new bench_p4_elastic_fpca function benches vert_fpca and joint_fpca (balance_c=Some(1.0)) at representative cells with pre-computed KarcherMeanResult built outside b.iter()"
    - "The dhat harness has vert_fpca and joint_fpca cells and their baselines are saved under .planning/research/bench/"
    - "AUDIT-REPORT.md Results Table has 6 or more Phase-4 FPCA rows matching n{100,500,1000}_m{50,200} plus the elastic-FPCA rows"
    - "AUDIT-REPORT.md ranks the FPCA allocation hotspots citing regression.rs:167/291/298 and states the copy-share % across the grid"
    - statement: "AUDIT-REPORT.md's Phase-6 go/no-go verdict explicitly states whether the trigger fires (comparison only if SVD is a significant share of FPCA runtime AND copy is not the dominant cost), naming both the measured SVD share and the measured copy dominance and giving a fires/does-not-fire call backed by the grid numbers"
      verification: backstop
    - statement: "The report's SVD-compute-vs-copy split for the top FPCA path is a share of wall-clock derived from the measured dhat bytes and measured criterion time across the grid (not a theoretical-only estimate)"
      verification: backstop
    - "AUDIT-REPORT.md Draft Backlog has 2 or more FPCA/SVD entries (SVD-copy elimination, truncated-SVD candidate) each carrying Function / Current cost / Root cause / Candidate fix / Evidence fields"
  artifacts:
    - fdars-core/benches/audit_hotpaths.rs
    - fdars-core/tests/alloc_audit_fpca.rs
    - .planning/research/AUDIT-REPORT.md
    - ".planning/research/bench/p4_fpca_linalg,parallel_run2.txt"
    - .planning/research/bench/p4_fpca_linalg_run1.txt
    - ".planning/research/bench/p4_elastic_fpca_vert_linalg,parallel_run1.txt"
    - ".planning/research/bench/p4_elastic_fpca_joint_linalg,parallel_run1.txt"
    - .planning/research/bench/p4_dhat_vert_fpca_n100_m50.txt
    - .planning/research/bench/p4_dhat_joint_fpca_n100_m50.txt
  key_links:
    - "bench_p4_fpca 6-cell grid produces 6 criterion estimates that become 6 or more AUDIT-REPORT rows"
    - "bench_p4_elastic_fpca (vert+joint, pre-computed karcher) produces the elastic-FPCA rows (SC1 requires at least one elastic site)"
    - "grid criterion times + grid dhat bytes drive the copy-share % and the Phase 6 go/no-go verdict (ROADMAP Phase 6 SC1)"
    - "measured FPCA numbers + regression.rs alloc sites drive 2 or more GSD-ready backlog entries (SC4)"
---

<objective>
**As a** performance auditor feeding the Phase 6 go/no-go decision, **I want to** expand the proven tracer pipeline to the full FPCA measurement grid — 6-cell N x M criterion sweep + parallel-invariance run + elastic-FPCA cells + vert/joint dhat baselines, then complete the report with the SVD-vs-copy split, the Phase-6 go/no-go verdict, and the GSD-ready backlog — **so that** Phase 6 can make its faer-vs-nalgebra decision on complete measured evidence rather than a single cell.

This is the expansion of the single vertical slice proven in Plan 02. Every task builds on the wired-and-verified criterion group, dhat harness, and report section from the tracer; nothing here re-discovers wiring.

No `fdars-core/src` algorithm changes — measurement infrastructure only (audit phase). This plan reads the copy sites but does NOT modify them; SVD-copy elimination is Phase 5/6 backlog work.

Purpose: Deliver the complete Phase-4 report + backlog slice that answers the ROADMAP SC1–SC4 and the Phase-6 go/no-go trigger with full-grid numbers.
Output: The full 6-cell FPCA grid (2 runs + no-parallel run), elastic-FPCA bench cells + artifacts, vert/joint dhat baselines, and a completed AUDIT-REPORT Phase-4 section (full Results Table, allocation-hotspot ranking, SVD-vs-copy split, Phase-6 verdict, 2+ backlog entries).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fpca-svd-allocation-audit/04-RESEARCH.md
@.planning/phases/04-fpca-svd-allocation-audit/04-PATTERNS.md
@.planning/phases/04-fpca-svd-allocation-audit/04-VALIDATION.md

# The bench file to EXTEND (has bench_p4_fpca from Plan 02, generate_curves, criterion_group!)
@fdars-core/benches/audit_hotpaths.rs

# The dhat harness to EXTEND (has count_fpca_allocations_n500_m200 from Plan 01/02)
@fdars-core/tests/alloc_audit_fpca.rs

# Elastic-FPCA target signatures (vert_fpca, joint_fpca, karcher_mean)
@fdars-core/src/elastic_fpca.rs
@fdars-core/src/alignment/karcher.rs

# Copy-site source-of-truth for report/backlog
@fdars-core/src/regression.rs

# The report to COMPLETE (Phase-4 skeleton from Plan 02)
@.planning/research/AUDIT-REPORT.md
</context>

<artifacts_this_phase_produces>
This plan (expansion) completes the phase artifacts begun in the tracer.

**Bench functions extended/added (in `fdars-core/benches/audit_hotpaths.rs`):**
- `bench_p4_fpca` — extended from 1 cell to the full 6-cell grid: N in {100,500,1000} x M in {50,200}, per-cell timing tiers (N=1000,M=200 uses sample_size(10))
- `bench_p4_elastic_fpca` — NEW: `vert_fpca` and `joint_fpca` (balance_c=Some(1.0)) at representative cells, KarcherMeanResult built outside `b.iter()`, group `"audit_p4_elastic_fpca"`

**dhat test cells added (in `fdars-core/tests/alloc_audit_fpca.rs`):**
- `count_vert_fpca_allocations_n100_m50` — vert_fpca (elastic_fpca.rs:214 copy site)
- `count_joint_fpca_allocations_n100_m50` — joint_fpca with balance_c=Some(1.0) (elastic_fpca.rs:317 copy site, optimizer bypassed)

**New artifact files (under `.planning/research/bench/`):**
- `p4_fpca_linalg,parallel_run2.txt` (variance), `p4_fpca_linalg_run1.txt` (no-parallel invariance)
- `p4_elastic_fpca_vert_linalg,parallel_run1.txt`, `p4_elastic_fpca_joint_linalg,parallel_run1.txt`
- `p4_dhat_vert_fpca_n100_m50.txt`, `p4_dhat_joint_fpca_n100_m50.txt`

**AUDIT-REPORT.md:** completed Phase-4 section — full Results Table (6+ FPCA rows + elastic rows), allocation-hotspot ranking, SVD-vs-copy split across grid, `### Phase 6 Go/No-Go Decision` verdict, `### Draft Backlog` with 2+ entries.
</artifacts_this_phase_produces>

<precondition>
Plan 04-02 must be complete: `bench_p4_fpca` (N=500,M=200 cell) registered in criterion_group!, the `p4_fpca_linalg,parallel_run1.txt` + `p4_dhat_fpca_n500_m200.txt` baselines exist, and the AUDIT-REPORT Phase-4 skeleton section exists. Also: `mkdir -p /home/simonm/.cache/fdars-bench-tmp` and prefix every `cargo bench`/`cargo test` with `TMPDIR=/home/simonm/.cache/fdars-bench-tmp` (Pitfall 8 — /tmp near-full causes linker bus-errors; infrastructure, not a code defect).
</precondition>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Expand bench_p4_fpca to the full 6-cell grid, add bench_p4_elastic_fpca, and run all cells (2 runs + no-parallel)</name>
  <read_first>
    - fdars-core/benches/audit_hotpaths.rs (bench_p4_fpca from Plan 02; lines 278-345 bench_p3_karcher multi-cell grid template; lines 38-52 generate_curves; criterion_group! macro)
    - fdars-core/src/elastic_fpca.rs (lines 89-95 vert_fpca sig (karcher, argvals, ncomp); lines 274-282 joint_fpca sig (karcher, argvals, ncomp, balance_c))
    - fdars-core/src/alignment/karcher.rs (line 293 karcher_mean sig — for the elastic-cell setup built OUTSIDE b.iter())
    - .planning/phases/04-fpca-svd-allocation-audit/04-PATTERNS.md (timing-tier map lines 108-117; elastic iter_batched/pre-computed setup lines 119-128; Pitfall B balance_c=Some(1.0))
    - .planning/phases/04-fpca-svd-allocation-audit/04-RESEARCH.md (Section 2A grid cells; Section 2B elastic scope; Pitfall B/C)
  </read_first>
  <files>fdars-core/benches/audit_hotpaths.rs, .planning/research/bench/p4_fpca_linalg,parallel_run2.txt, .planning/research/bench/p4_fpca_linalg_run1.txt, .planning/research/bench/p4_elastic_fpca_vert_linalg,parallel_run1.txt, .planning/research/bench/p4_elastic_fpca_joint_linalg,parallel_run1.txt</files>
  <action>
    Extend `bench_p4_fpca` from the single N=500,M=200 cell to all 6 cells: N in {100,500,1000} x M in {50,200}. For each cell, re-declare its own `(data, argvals)` via `generate_curves(N, M)` OUTSIDE `b.iter()`, call `fdata_to_pc_1d(black_box(&data), black_box(5usize), black_box(&argvals))` inside, cell names `n100_m50`, `n100_m200`, `n500_m50`, `n500_m200`, `n1000_m50`, `n1000_m200`. Apply the timing tiers from PATTERNS.md timing-tier map: `sample_size(20)`/`measurement_time(20s)` for all cells except `n1000_m200` which uses `sample_size(10)`. Keep the existing `audit_p4_fpca` group.

    Add a NEW `bench_p4_elastic_fpca(c: &mut Criterion)` in group `"audit_p4_elastic_fpca"`. For `vert_fpca` at N=100,M=50: build the `KarcherMeanResult` via `karcher_mean(&data, &argvals, 10, 1e-3, 0.0)` (unwrap/expect) OUTSIDE `b.iter()` (it is setup, not the measured target — RESEARCH Section 2B), then `b.iter(|| vert_fpca(black_box(&karcher), black_box(&argvals), black_box(5usize)))`, cell `vert_fpca_n100_m50`. For `joint_fpca` at N=100,M=50: same karcher setup, `b.iter(|| joint_fpca(black_box(&karcher), black_box(&argvals), black_box(5usize), black_box(Some(1.0))))` with `balance_c=Some(1.0)` to bypass `optimize_balance_c_raw` (Pitfall B — main-SVD-path cell), cell `joint_fpca_n100_m50`. Extend the bench `use` block to import `vert_fpca`, `joint_fpca` from `fdars_core::elastic_fpca` and `karcher_mean` if not already imported. Register `bench_p4_elastic_fpca` in `criterion_group!` after `bench_p4_fpca`.

    Then run: (a) `p4_fpca_linalg,parallel_run2.txt` — the full grid a second time (features linalg,parallel, group audit_p4_fpca) for variance; (b) `p4_fpca_linalg_run1.txt` — the full grid with features `linalg` ONLY (no parallel) to confirm FPCA timings are parallel-invariant (formalizes Phase-1 D-04); (c) `p4_elastic_fpca_vert_linalg,parallel_run1.txt` and `p4_elastic_fpca_joint_linalg,parallel_run1.txt` — run group audit_p4_elastic_fpca (both cells land in one run; tee to both artifact names or split by cell filter — executor's choice, but each named file must contain its cell's timing). All runs: TMPDIR prefix, `--bench audit_hotpaths`, an `=== ENVIRONMENT ===` header with rustc/cargo versions + feature set, `/release/` path confirmed. Also ensure `p4_fpca_linalg,parallel_run1.txt` ultimately contains all 6 cells (re-run run1 of the full grid to overwrite the tracer's single-cell file, or append the 5 new cells) so run1 and run2 cover the same 6 cells.

    Pitfall C reminder: FPCA cells are sub-second even at N=1000,M=200 (~64-256 ms) — NO cell is infeasible; do not under-size the grid.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --benches --features linalg,parallel 2>&1 | tail -3 && grep -c "fn bench_p4_elastic_fpca" fdars-core/benches/audit_hotpaths.rs && grep -Ec "n(100|500|1000)_m(50|200)" ".planning/research/bench/p4_fpca_linalg,parallel_run1.txt" && test -f ".planning/research/bench/p4_fpca_linalg_run1.txt" && test -f ".planning/research/bench/p4_elastic_fpca_vert_linalg,parallel_run1.txt" && echo GRID_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fn bench_p4_elastic_fpca" fdars-core/benches/audit_hotpaths.rs` returns 1 and it is registered in `criterion_group!`
    - `bench_p4_fpca` contains all 6 cell names `n100_m50 n100_m200 n500_m50 n500_m200 n1000_m50 n1000_m200` (grep confirms 6 distinct cell strings)
    - `bench_p4_elastic_fpca` builds `karcher_mean` OUTSIDE `b.iter()` and benches `vert_fpca` + `joint_fpca(.., Some(1.0))`
    - `grep -Ec "n(100|500|1000)_m(50|200)" .planning/research/bench/p4_fpca_linalg,parallel_run1.txt` is 6 or more (run1 covers the full grid)
    - `p4_fpca_linalg,parallel_run2.txt`, `p4_fpca_linalg_run1.txt` (no-parallel), `p4_elastic_fpca_vert_linalg,parallel_run1.txt`, `p4_elastic_fpca_joint_linalg,parallel_run1.txt` all exist with criterion timing lines + toolchain/feature tags + `/release/` confirmation
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --benches --features linalg,parallel` exits 0
  </acceptance_criteria>
  <done>The full 6-cell FPCA grid runs twice (variance) + once no-parallel (invariance), and vert_fpca/joint_fpca elastic cells run — all artifacts saved under bench/p4_* with toolchain+feature tagging.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add vert_fpca/joint_fpca dhat cells and save their baselines</name>
  <read_first>
    - fdars-core/tests/alloc_audit_fpca.rs (the count_fpca_allocations_n500_m200 cell + generate_test_curves helper from Plan 01/02 — mirror for the new cells)
    - fdars-core/src/elastic_fpca.rs (lines 89-95 vert_fpca; lines 274-282 joint_fpca; lines 214/317 the to_dmatrix copy sites)
    - fdars-core/src/alignment/karcher.rs (line 293 karcher_mean — setup built before the profiler measures the fpca call)
    - .planning/phases/04-fpca-svd-allocation-audit/04-PATTERNS.md (three test cells lines 195-198; feature-gate discipline lines 348-352)
    - .planning/phases/04-fpca-svd-allocation-audit/04-RESEARCH.md (Section 4C ranking hotspots; Pitfall B balance_c=Some(1.0) for the main-SVD-path count)
  </read_first>
  <files>fdars-core/tests/alloc_audit_fpca.rs, .planning/research/bench/p4_dhat_vert_fpca_n100_m50.txt, .planning/research/bench/p4_dhat_joint_fpca_n100_m50.txt</files>
  <action>
    Add two new tests to `alloc_audit_fpca.rs`, both guarded by `#[test]` + `#[cfg(feature = "dhat-heap")]` and mirroring the existing `count_fpca_allocations_n500_m200` structure.

    `count_vert_fpca_allocations_n100_m50` — generate N=100,M=50 curves, build `karcher_mean(&data, &argvals, 10, 1e-3, 0.0)` FIRST, THEN build the profiler with `dhat::Profiler::builder().testing().build()`, THEN call `vert_fpca(&karcher, &argvals, 5)`, THEN read `dhat::HeapStats::get()`. Building the profiler AFTER the karcher setup isolates the vert_fpca allocation profile from the karcher setup allocations. Print total_blocks/total_bytes/peak_bytes.

    `count_joint_fpca_allocations_n100_m50` — same ordering, but call `joint_fpca(&karcher, &argvals, 5, Some(1.0))` with `balance_c=Some(1.0)` so the golden-section optimizer (`optimize_balance_c_raw`, up to 20 to_dmatrix allocations) is bypassed and the count reflects the MAIN SVD path only (Pitfall B). Print the three HeapStats fields.

    Do NOT hard-assert counts — baselines, not gates. Keep every dhat symbol under `#[cfg(feature = "dhat-heap")]`.

    Then run both cells with TMPDIR set, features `dhat-heap,linalg`, `--test alloc_audit_fpca`, filtered per test name, with `--nocapture`. Tee the vert run to `p4_dhat_vert_fpca_n100_m50.txt` and the joint run to `p4_dhat_joint_fpca_n100_m50.txt`. Each artifact gets an `=== ENVIRONMENT ===` header (rustc/cargo versions + feature set `dhat-heap,linalg`). These two baselines let the report RANK allocation hotspots across the three FPCA paths (fdata_to_pc_1d vs vert_fpca vs joint_fpca).
  </action>
  <verify>
    <automated>grep -Ec "count_(vert|joint)_fpca_allocations" fdars-core/tests/alloc_audit_fpca.rs && test -f .planning/research/bench/p4_dhat_vert_fpca_n100_m50.txt && test -f .planning/research/bench/p4_dhat_joint_fpca_n100_m50.txt && grep -Eiq "total_blocks|total_bytes" .planning/research/bench/p4_dhat_vert_fpca_n100_m50.txt && echo DHAT_EXPANSION_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec "count_vert_fpca_allocations_n100_m50|count_joint_fpca_allocations_n100_m50" fdars-core/tests/alloc_audit_fpca.rs` returns 2, both under `#[cfg(feature = "dhat-heap")]`
    - The joint cell calls `joint_fpca(.., Some(1.0))` (optimizer bypassed, Pitfall B)
    - Both cells build `karcher_mean` before building the profiler (isolating the fpca allocation profile from karcher setup)
    - `p4_dhat_vert_fpca_n100_m50.txt` and `p4_dhat_joint_fpca_n100_m50.txt` exist, each with numeric total_blocks/total_bytes/peak_bytes and feature tag `dhat-heap,linalg`
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --features dhat-heap,linalg --tests` exits 0
  </acceptance_criteria>
  <done>vert_fpca and joint_fpca (main SVD path, optimizer bypassed) dhat baselines exist under bench/p4_dhat_* — enabling the report's cross-path allocation-hotspot ranking.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Complete the AUDIT-REPORT Phase-4 section — full grid, hotspot ranking, SVD-vs-copy split, Phase-6 verdict, and 2+ backlog entries</name>
  <read_first>
    - .planning/research/AUDIT-REPORT.md (the Phase-4 skeleton from Plan 02 — fill the placeholders, do NOT overwrite prior sections; Phase-3 backlog shape for field parity)
    - fdars-core/src/regression.rs (lines 167/291/298 — the three alloc sites for hotspot ranking + backlog root-cause)
    - fdars-core/src/matrix.rs (lines 310-312 to_dmatrix = column-major memcpy)
    - .planning/phases/04-fpca-svd-allocation-audit/04-PATTERNS.md (results-table schema lines 241-246; backlog field shape lines 248-261; Phase-6 go/no-go template lines 280-296)
    - .planning/phases/04-fpca-svd-allocation-audit/04-RESEARCH.md (Section 5C the three go/no-go statements; Section 6C backlog fields; Pitfall A/D)
    - the six p4_* bench artifacts + the three p4_dhat_* baselines (the measured numbers feeding every row, the split, and the verdict)
  </read_first>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <action>
    Fill the Phase-4 section skeleton created in Plan 02 (APPEND/EDIT within it — prior Phase 1/2/3 content and the tracer row stay intact).

    Results Table: populate all 6 FPCA grid rows (`fdata_to_pc_1d` at each N x M) with run1/run2 mean times, per-cell variance %, and confidence (OK if variance is under about 5%, LOW otherwise), each linking to `p4_fpca_linalg,parallel_run{1,2}.txt`. Add the elastic-FPCA rows for `vert_fpca` and `joint_fpca` (N=100,M=50) linking to their artifacts. Add a note recording the no-parallel invariance result (from `p4_fpca_linalg_run1.txt`): FPCA timings identical across linalg vs linalg,parallel (D-04 formalized) — cite that FPCA SVD + center_columns are sequential (note `center_columns` at regression.rs:167 is the sequential one, NOT `center_1d` — RESEARCH anti-pattern).

    Allocation Audit / hotspot ranking: from the three dhat baselines, rank the allocation hotspots per FPCA path. For `fdata_to_pc_1d` list the three sites: `regression.rs:167` `center_columns` (FdMatrix alloc), `:291` `centered.clone()` (redundant FdMatrix copy — zero-copy candidate), `:298` `weighted.to_dmatrix()` (the DMatrix copy). Distinguish the ONE to_dmatrix copy from the two FdMatrix allocations. Do NOT attribute `elastic_fpca.rs:122`/`:399` (native-DMatrix covariance SVD, no to_dmatrix copy) to the copy bucket (Pitfall A). Note vert_fpca (elastic_fpca.rs:214) and joint_fpca (elastic_fpca.rs:317) each carry one to_dmatrix copy; the joint number is the main-SVD-path count (optimizer bypassed).

    SVD-Compute vs Copy Split: state the copy cost as a % of measured wall-clock for the TOP FPCA path (N=500,M=200 or N=1000,M=200), derived from measured dhat bytes divided by stated memory bandwidth versus the measured criterion time. Show the arithmetic. Add the Pitfall-D context note: FPCA (~16-256 ms) is 2-3 orders of magnitude cheaper than Phase-3 elastic alignment — FPCA is NOT the primary bottleneck; state this for Phase-9 prioritization.

    Phase 6 Go/No-Go Decision: state EXACTLY ONE of the three RESEARCH Section 5C verdicts as fired, naming both quantities the trigger depends on (the SVD share of runtime AND whether copy is the dominant cost) and backing it with the measured grid numbers. The ROADMAP Phase 6 SC1 condition is "comparison only if SVD is a significant share of FPCA runtime AND copy is not the dominant cost" — the verdict sentence must reference both. Given the tracer direction (copy negligible), the expected verdict is "SVD dominant, Phase 6 triggered", but state whatever the measured numbers support.

    Draft Backlog: add 2 or more GSD-ready entries, each with Function / Current cost (grid numbers) / Root cause (cite regression.rs:291 clone + :298 to_dmatrix; 3 allocations of O(n·m)) / Candidate fix / Evidence (link the p4 artifacts) / Severity+Effort `[TBD — Phase 9]`. Entry 1: `fdata_to_pc_1d` — eliminate redundant `centered.clone()` + zero-copy the to_dmatrix. Entry 2: truncated-SVD candidate (only ncomp=5 components needed while full SVD computes all — candidate for a thin/truncated SVD) OR the elastic-FPCA to_dmatrix copies. Match the Phase-3 backlog-entry field shape.
  </action>
  <verify>
    <automated>grep -Ec "n(100|500|1000)_m(50|200)" .planning/research/AUDIT-REPORT.md && grep -Eq "regression.rs:(167|291|298)" .planning/research/AUDIT-REPORT.md && grep -Eiq "go.?no.?go|triggered|not warranted" .planning/research/AUDIT-REPORT.md && grep -Ec "Current cost|Root cause|Candidate" .planning/research/AUDIT-REPORT.md && echo REPORT_COMPLETE</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec "n(100|500|1000)_m(50|200)" .planning/research/AUDIT-REPORT.md` is 6 or more (full FPCA grid rows present)
    - The Results Table has the 6 FPCA grid rows + `vert_fpca` + `joint_fpca` elastic rows, each linking to a p4_* artifact
    - The report cites `regression.rs:167`, `:291`, `:298` and distinguishes the one to_dmatrix copy from the two FdMatrix allocations; it does NOT list `elastic_fpca.rs:122`/`:399` as to_dmatrix copy sites (Pitfall A)
    - The SVD-vs-copy split states a copy-share % with inline arithmetic tied to measured dhat bytes + measured criterion time
    - The Phase 6 Go/No-Go verdict names BOTH the SVD share AND the copy dominance and gives a fires/does-not-fire call backed by the grid numbers
    - `grep -Ec "Current cost|Root cause|Candidate" .planning/research/AUDIT-REPORT.md` is 6 or more (2 entries x 3 fields), and there are 2 or more distinct backlog entries with Function + Evidence link
    - Prior AUDIT-REPORT content (Phase 1/2/3 + Plan-02 tracer row) unchanged (append/edit within Phase-4 section only)
  </acceptance_criteria>
  <done>AUDIT-REPORT.md Phase-4 section is complete: full 6-cell grid + elastic rows, allocation-hotspot ranking citing the three regression.rs sites, a measured SVD-vs-copy split, a Phase-6 go/no-go verdict naming both trigger quantities, and 2+ GSD-ready backlog entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Benchmark + measurement-only phase. Bench and dhat code read only seeded synthetic data built in-process (`generate_curves`/`generate_test_curves`, no RNG, no I/O input). Report writes are to local markdown. No network, no auth, no untrusted input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering | cargo bench/test dependency graph (criterion + dhat already dev-deps from Plan 01; no new packages this plan) | low | accept | No new packages installed this plan. Audit-only measurement code, feature-gated dhat. No package-legitimacy gate needed. |

N/A — no attack surface. At ASVS L1 this is a read-only local measurement phase: running benches + a feature-gated dhat profiler and appending to a local report introduces no network, input-handling, auth, or persistence attack surface. No `high` threats.
</threat_model>

<verification>
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --benches --features linalg,parallel` exits 0 with `bench_p4_fpca` (6 cells) + `bench_p4_elastic_fpca` registered.
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --features dhat-heap,linalg --tests` exits 0 with the two new dhat cells.
- All six p4_* bench artifacts + the two new p4_dhat_* baselines exist with timing/stats lines + toolchain/feature tags.
- AUDIT-REPORT.md Phase-4 section: 6+ FPCA rows, elastic rows, hotspot ranking citing regression.rs:167/291/298, measured copy-share %, Phase-6 verdict naming both trigger quantities, 2+ backlog entries; earlier sections intact.
</verification>

<success_criteria>
The full Phase-4 slice is delivered: the complete FPCA measurement grid (6 cells x 2 runs + no-parallel invariance + elastic vert/joint cells) and dhat baselines feed a completed AUDIT-REPORT section that states the SVD-vs-copy split, a Phase-6 go/no-go verdict backed by measured numbers, and 2+ GSD-ready backlog entries — answering ROADMAP SC1-SC4 and the Phase-6 trigger.
</success_criteria>

<output>
Create `.planning/phases/04-fpca-svd-allocation-audit/04-03-SUMMARY.md` when done.
</output>
