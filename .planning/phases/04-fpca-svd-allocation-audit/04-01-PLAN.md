---
phase: 04-fpca-svd-allocation-audit
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - fdars-core/Cargo.toml
  - fdars-core/tests/alloc_audit_fpca.rs
autonomous: true
requirements: [PERF-04]
estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "dhat is a dev-dependency and a dhat-heap feature exists in fdars-core/Cargo.toml, both feature-gated so dhat never activates in normal CI or release builds (per PERF-04 baseline requirement)"
    - "A fdars-core/tests/alloc_audit_fpca.rs integration test file exists with a #[global_allocator] and a dhat::Profiler::builder().testing() harness, all symbols under #[cfg(feature = \"dhat-heap\")]"
    - "cargo build -p fdars-core --features dhat-heap,linalg exits 0 (the dhat harness compiles)"
    - "cargo build -p fdars-core --features linalg (WITHOUT dhat-heap) still exits 0 — the dhat gate does not leak into the default build"
  artifacts:
    - fdars-core/Cargo.toml
    - fdars-core/tests/alloc_audit_fpca.rs
  key_links:
    - "dhat-heap feature in Cargo.toml → gates #[cfg(feature = \"dhat-heap\")] symbols in alloc_audit_fpca.rs"
    - "#[global_allocator] static dhat::Alloc → dhat::HeapStats::get() reads real per-call allocation counts"
    - "generate_test_curves column-major layout → FdMatrix::from_column_major matches fdata_to_pc_1d input contract"
---

<objective>
**As a** performance auditor (feeding the Phase 6 go/no-go decision), **I want to** wire dhat into fdars-core as a feature-gated dev-dependency with a compiling integration-test harness, **so that** Wave 1 can measure real per-call FPCA allocation counts without discovering the allocator-wiring problems mid-measurement.

This is the Wave 0 gap identified in 04-VALIDATION.md: `dhat` is not yet a dev-dependency and `fdars-core/tests/alloc_audit_fpca.rs` does not exist. dhat requires a `#[global_allocator]` in a **separate process** (integration test, not an inline `src/` `#[cfg(test)]` module), so both pieces are prerequisites for the dhat measurement in Plan 02/03.

No `fdars-core/src` algorithm changes — measurement scaffolding only (this is an audit phase).

Purpose: Establish the dhat measurement substrate and prove it compiles cleanly both with and without the `dhat-heap` gate, so Wave 1 measurement runs are pure execution, not wiring.
Output: `dhat` dev-dep + `dhat-heap` feature in Cargo.toml; a compiling `alloc_audit_fpca.rs` with the `#[global_allocator]` and one `fdata_to_pc_1d` dhat test cell.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fpca-svd-allocation-audit/04-RESEARCH.md
@.planning/phases/04-fpca-svd-allocation-audit/04-PATTERNS.md

# The manifest to EDIT (dev-deps at line 49, features at line 18)
@fdars-core/Cargo.toml

# Source-of-truth for the generate_test_curves layout + fdata_to_pc_1d signature
@fdars-core/benches/audit_hotpaths.rs
@fdars-core/src/regression.rs
</context>

<artifacts_this_phase_produces>
This plan (Wave 0) introduces the dhat measurement substrate. Plans 02/03 consume it.

**Cargo manifest changes (`fdars-core/Cargo.toml`):**
- `dhat = "0.3"` added to `[dev-dependencies]`
- `dhat-heap = []` added to `[features]`
- `[[test]] name = "alloc_audit_fpca"` entry added if the default test auto-discovery does not suffice (executor's judgment)

**New files:**
- `fdars-core/tests/alloc_audit_fpca.rs` — dhat integration-test harness with `#[global_allocator]`, `generate_test_curves` helper, and one `count_fpca_allocations_n500_m200` test cell (Plans 02/03 add the `vert_fpca` / `joint_fpca` cells and run them)
</artifacts_this_phase_produces>

<precondition>
`dhat` must resolve on crates.io at a 0.3.x version. RESEARCH.md marks the exact patch (`0.3.3`) as ASSUMED (A1); the executor confirms the current 0.3.x version via `cargo add dhat --dev` (which pins the resolved version) or `cargo search dhat` before committing. The `"0.3"` semver caret range is safe if any 0.3.x is current.
</precondition>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Add dhat dev-dependency and dhat-heap feature to Cargo.toml</name>
  <read_first>
    - fdars-core/Cargo.toml (lines 18-29 [features] block; lines 49-52 [dev-dependencies] block; lines 54-92 existing [[bench]] entries for [[test]] registration style)
    - .planning/phases/04-fpca-svd-allocation-audit/04-RESEARCH.md (Section 4A — dhat wiring; Assumption A1 version caveat)
    - .planning/phases/04-fpca-svd-allocation-audit/04-PATTERNS.md (Cargo.toml MODIFY section, lines 202-233)
  </read_first>
  <files>fdars-core/Cargo.toml</files>
  <action>
    Add `dhat` to the `[dev-dependencies]` block (lines 49-52) at semver `"0.3"`. Prefer running `cargo add dhat --dev -p fdars-core` so Cargo resolves and records the current 0.3.x version, then confirm the resulting entry reads `dhat = "0.3"` (or the resolved `0.3.x`). If offline, add `dhat = "0.3"` manually — the caret range accepts any current 0.3.x.

    Add `dhat-heap = []` to the `[features]` block (lines 18-29), alongside the existing `default`, `parallel`, `linalg`, `serde`, `js` features. It is an empty (no-dependency) feature whose sole purpose is to gate the `#[cfg(feature = "dhat-heap")]` symbols in `alloc_audit_fpca.rs`. Add a comment noting it gates dhat allocation profiling and must never be enabled in release/CI builds.

    Do NOT add `dhat-heap` to `default`. Do NOT wire `dhat` as a non-dev dependency. The gate keeps dhat entirely out of the normal build graph.

    If Rust's default integration-test auto-discovery (any file under `tests/` becomes a test target) is sufficient, no `[[test]]` entry is needed. If the executor finds an explicit entry is required to pass a feature-gated file, add `[[test]] name = "alloc_audit_fpca"` after the existing `[[bench]]` entries (this addresses PERF-04's reproducible-baseline requirement).
  </action>
  <verify>
    <automated>grep -Eq '^dhat = "0\.3' fdars-core/Cargo.toml && grep -Eq '^dhat-heap = \[\]' fdars-core/Cargo.toml && echo CARGO_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec '^dhat = "0\.3' fdars-core/Cargo.toml` returns 1 (dhat dev-dep present at 0.3.x)
    - `grep -Ec '^dhat-heap = \[\]' fdars-core/Cargo.toml` returns 1 (feature present)
    - `dhat-heap` does NOT appear inside the `default = [...]` array (grep the default line confirms it is absent)
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --features linalg` (WITHOUT dhat-heap) exits 0 — the new dev-dep and feature do not affect the default build
  </acceptance_criteria>
  <done>Cargo.toml carries the dhat dev-dep and the dhat-heap gate, and the default (non-dhat) build still compiles.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create the dhat integration-test harness alloc_audit_fpca.rs</name>
  <read_first>
    - fdars-core/benches/audit_hotpaths.rs (lines 38-52 generate_curves — replicate as generate_test_curves; lines 82-96 fdata_to_pc_1d call convention)
    - fdars-core/src/regression.rs (lines 249-322: fdata_to_pc_1d signature — (data, ncomp, argvals); the three alloc sites at :167/:291/:298)
    - .planning/phases/04-fpca-svd-allocation-audit/04-PATTERNS.md (alloc_audit_fpca.rs NEW section, lines 151-199 — full dhat harness pattern + feature-gate discipline)
    - .planning/phases/04-fpca-svd-allocation-audit/04-RESEARCH.md (Section 4B/4C — HeapStats fields to record; separate-process constraint)
  </read_first>
  <files>fdars-core/tests/alloc_audit_fpca.rs</files>
  <action>
    Create `fdars-core/tests/alloc_audit_fpca.rs` as a dhat integration test. Put a file-scope `#[global_allocator] static ALLOC: dhat::Alloc = dhat::Alloc;` guarded by `#[cfg(feature = "dhat-heap")]` — this is the critical separate-process allocator wiring that CANNOT live in an `src/` `#[cfg(test)]` module (it would contaminate the whole compilation unit's allocator).

    Add a private `generate_test_curves(n: usize, m: usize) -> (FdMatrix, Vec<f64>)` helper replicating the deterministic column-major generator from audit_hotpaths.rs lines 38-52 verbatim in logic: uniform argvals, per-curve sine with phase/amplitude variation, `data[i + j * n]` column-major indexing, built via `FdMatrix::from_column_major`. Import `FdMatrix`, `fdata_to_pc_1d`, and `std::f64::consts::PI`.

    Add ONE test `count_fpca_allocations_n500_m200` guarded by both `#[test]` and `#[cfg(feature = "dhat-heap")]`: build the profiler with `dhat::Profiler::builder().testing().build()`, generate the N=500,M=200 curves, call `fdata_to_pc_1d(&data, 5, &argvals)`, then read `dhat::HeapStats::get()` and `println!` its `total_blocks`, `total_bytes`, and `peak_bytes`. Do NOT hard-assert a specific count — this is a baseline, not a regression gate (RESEARCH §4B).

    Every dhat symbol (the `#[global_allocator]`, the test fn, any dhat import) must be under `#[cfg(feature = "dhat-heap")]` so `cargo test` without the feature compiles this file to nothing. Plans 02/03 add the `vert_fpca` and `joint_fpca` cells — this plan ships only the primary `fdata_to_pc_1d` cell to prove the harness compiles and the allocator wires up.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --features dhat-heap,linalg --tests 2>&1 | tail -5 && grep -c "global_allocator" fdars-core/tests/alloc_audit_fpca.rs</automated>
  </verify>
  <acceptance_criteria>
    - `fdars-core/tests/alloc_audit_fpca.rs` exists
    - `grep -c "global_allocator" fdars-core/tests/alloc_audit_fpca.rs` returns 1 and it is under a `#[cfg(feature = \"dhat-heap\")]` attribute (grep the surrounding lines)
    - `grep -c "Profiler::builder().testing()" fdars-core/tests/alloc_audit_fpca.rs` returns 1
    - `grep -Ec "total_blocks|total_bytes|peak_bytes" fdars-core/tests/alloc_audit_fpca.rs` returns 3 (all three HeapStats fields printed)
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --features dhat-heap,linalg --tests` exits 0 (harness compiles WITH the gate)
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --features linalg --tests` exits 0 (file compiles to nothing WITHOUT the gate)
  </acceptance_criteria>
  <done>The dhat integration harness compiles both with and without dhat-heap; the global allocator and one fdata_to_pc_1d dhat cell are wired and feature-gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Measurement-scaffolding phase. dhat is a dev-only, feature-gated allocation profiler; the harness reads only seeded synthetic data built in-process (`generate_test_curves`, no RNG, no I/O input). No network, no auth, no untrusted input crosses any boundary. No runtime attack surface is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | new `dhat` dev-dependency added to Cargo.toml | low | accept | `dhat` is a widely-used dev-only allocation profiler, feature-gated behind `dhat-heap` (never in default/release). Version added via `cargo add`/`cargo search` verification. No runtime attack surface — audit-only measurement code. |

N/A — no attack surface. At ASVS L1 this is a read-only local measurement phase: adding a dev-only dhat dependency + gated test harness introduces no network, input-handling, auth, or persistence attack surface. No `high` threats.
</threat_model>

<verification>
- `grep -Ec '^dhat = "0\.3' fdars-core/Cargo.toml` = 1 and `grep -Ec '^dhat-heap = \[\]' fdars-core/Cargo.toml` = 1.
- `dhat-heap` absent from the `default` feature array.
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --features dhat-heap,linalg --tests` exits 0.
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --features linalg --tests` exits 0 (gate does not leak).
- `alloc_audit_fpca.rs` has one feature-gated `#[global_allocator]`, a `testing()` profiler, and prints all three HeapStats fields.
</verification>

<success_criteria>
dhat is a feature-gated dev-dependency; the `alloc_audit_fpca.rs` integration harness compiles both with and without `dhat-heap`; the primary `fdata_to_pc_1d` dhat cell is wired. Wave 1 can now measure real allocation counts with a single `cargo test --features dhat-heap,linalg` invocation, no wiring left to discover.
</success_criteria>

<output>
Create `.planning/phases/04-fpca-svd-allocation-audit/04-01-SUMMARY.md` when done.
</output>
