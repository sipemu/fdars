---
phase: 48-parallelism-gap-closure
plan: 02
type: execute
wave: 2
depends_on: [48-01]
files_modified:
  - fdars-core/src/coclustering.rs
  - fdars-core/benches/perf_parallelism.rs
  - fdars-core/tests/equivalence_phase48.rs
  - .planning/phases/48-parallelism-gap-closure/PERF-PARALLEL-RESULTS.md
autonomous: true
requirements: [PERF-03]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "co_cluster n_init best-of loop is parallelized via iter_maybe_parallel! with a ?-safe collect::<Result<Vec<_>,_>>() and a SEQUENTIAL best-by-log_likelihood reduction (strict >, lowest-init-index tie-break) — output BIT-IDENTICAL parallel vs sequential and ON vs OFF (PERF-03)"
    - "Each init keeps its per-init seeding config.seed.wrapping_add(init*1000) so RNG streams are order-independent; result independent of thread count (PERF-03)"
    - "A payback-threshold outer-if (const CO_CLUSTER_INIT_PARALLEL_THRESHOLD) routes n_init below threshold to the sequential loop; both branches produce the same golden (PERF-03)"
    - "Error behavior preserved: the first init error in index order propagates (matching the sequential `?`); co_cluster public signature byte-identical"
    - "The existing full suite stays green under BOTH feature configs and clippy --all-targets is clean"
  artifacts:
    - "fdars-core/src/coclustering.rs (parallel n_init map + sequential reduce + threshold const)"
    - "fdars-core/tests/equivalence_phase48.rs (golden_co_cluster_parallel + below_threshold)"
    - "fdars-core/benches/perf_parallelism.rs (bench_co_cluster cell)"
  key_links:
    - "per-init seeding + sequential strict-> reduction over the index-ordered collected Vec -> identical best pick + tie-break -> bit-identical labels/log_likelihood"
    - "collect::<Result<Vec,_>>() returns the first Err in iteration order = the same error the sequential `?` short-circuited on -> error behavior preserved"
---

<objective>
Wave 2 expansion from the frechet_anova tracer: parallelize the `co_cluster` multi-restart CEM n_init
best-of loop (`src/coclustering.rs` ~:935-968, PROF-01 13ms candidate) with `iter_maybe_parallel!`.
Each init is already deterministically seeded (`config.seed.wrapping_add(init*1000)`), so the ONLY
correctness work is: (a) turn the in-loop `kmeans_fd(...)?` into a `?`-safe parallel map returning
`Result`, (b) do the best-by-log_likelihood reduction SEQUENTIALLY over the index-ordered results with
a strict `>` so the lowest-init-index wins ties exactly as today, and (c) guard small `n_init` with a
payback threshold. Guarded by a bit-identical golden authored before the change.

**Deferral acknowledgment (also in 48-CONTEXT.md + 48-VALIDATION.md; finalized in 48-03 Task 2):**
`t_perm_test`/`f_perm_test` (shared advancing RNG — parallelizing changes p-values, out of scope),
`frechet_anova_space` (`S: Sync` public-signature widening), and `explain/importance.rs` (low fan-out;
Phase 49 folds it into the already-parallel generic path) are DELIBERATELY NOT parallelized — not
silent omissions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-parallelism-gap-closure/48-CONTEXT.md
@.planning/phases/48-parallelism-gap-closure/48-RESEARCH.md
@.planning/phases/48-parallelism-gap-closure/48-VALIDATION.md
@fdars-core/tests/equivalence_phase48.rs
@fdars-core/benches/perf_parallelism.rs
</context>

<operational_notes>
Same as Plan 01 (bake into every command): `TMPDIR=/home/simonm/.cache/fdars-bench-tmp`; full lint gate
`cargo clippy --all-targets --features linalg,parallel -- -D warnings`; `cargo fmt -p fdars-core` then
`git commit --no-verify` per commit. The wave gate MUST run the suite under BOTH `--features linalg,parallel`
AND `--no-default-features --features linalg`. Reuse `iter_maybe_parallel!` (already used elsewhere in the crate).
Capture the golden BEFORE editing (co_cluster is currently sequential — capture now, then edit).
</operational_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author golden_co_cluster capturing current (pre-parallel) labels + log-likelihood</name>
  <files>fdars-core/tests/equivalence_phase48.rs</files>
  <read_first>
    - fdars-core/src/coclustering.rs:83-95 (`pub struct CoClusterResult` — CONFIRMED fields to assert on: `row_labels: Vec<usize>`, `col_labels: Vec<usize>`, `log_likelihood: f64`, plus `n_row_blocks`, `n_col_blocks`, `block_params: Vec<BlockParams>`). Assert `log_likelihood` (f64, bit-identical via assert_eq!), `row_labels`, `col_labels`.
    - fdars-core/src/coclustering.rs (the `co_cluster(data, argvals, config)` signature; CoClusterConfig fields incl. n_init, seed, n_row_blocks, n_col_blocks, ncomp, max_iter, tol)
    - fdars-core/src/coclustering.rs ~:935-972 (the current sequential n_init best-of loop: per-init seed, kmeans_fd?, cem_single_fit, strict `>` best pick)
    - fdars-core/tests/equivalence_phase48.rs (the Plan-01 file + helpers + bit-identity convention: assert_eq!, not tolerance)
  </read_first>
  <behavior>
    - golden_co_cluster_parallel: co_cluster on a fixed deterministic FdMatrix (n≈60, m≈40, two latent row groups) with CoClusterConfig{ n_init = 4 (ABOVE threshold), seed = 42, ... } produces log_likelihood + row_labels + col_labels EXACTLY equal to the reference captured from the CURRENT sequential code.
    - golden_co_cluster_below_threshold: the SAME data with n_init = 1 or 2 (BELOW threshold → sequential branch) produces its own reference exactly.
  </behavior>
  <action>
    Add `#[test] fn golden_co_cluster_parallel()` and `#[test] fn golden_co_cluster_below_threshold()`
    to `equivalence_phase48.rs`. Build a deterministic FdMatrix generator (reuse the phase47/probe
    sinusoid-with-group-shift formula: two latent row groups via `grp = i%2`, no RNG) at n≈60, m≈40 with
    `argvals = (0..40).map(|j| j as f64/39.0)`. Build a `CoClusterConfig` with `n_init: 4`, `seed: 42`,
    small `n_row_blocks`/`n_col_blocks` (e.g. 2/2), `ncomp` small (e.g. 3), a modest `max_iter` (e.g. 50),
    default `tol`. Call `fdars_core::coclustering::co_cluster(&data, &argvals, &config).unwrap()`.

    Capture references the Phase-47 way (see `fdars-core/tests/equivalence_phase47.rs` golden tests:
    add a temporary `println!("LL {:.17e}", r.log_likelihood)` + `println!("{:?}", r.row_labels)`, run once,
    paste the printed values as `const LL_REF: f64 = <pasted 17-digit literal>;` (add
    `#![allow(clippy::excessive_precision)]` at the file top as in equivalence_phase47.rs) and
    `const ROW_REF: [usize; N] = [...];`). Assert BIT-IDENTICAL: `assert_eq!(r.log_likelihood, LL_REF)` and
    `assert_eq!(r.row_labels, ROW_REF.to_vec())` / `assert_eq!(r.col_labels, COL_REF.to_vec())`. Use the
    EXACT field names found in Task-1 read (row_labels/col_labels may differ — verify). Add a comment:
    "captured from pre-parallel sequential co_cluster; must remain bit-identical after the parallel
    n_init map + sequential reduce, under both feature configs."
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel -- golden_co_cluster --nocapture 2>&1 | tail -8</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --no-default-features --features linalg -- golden_co_cluster --nocapture 2>&1 | tail -8</automated>
    <acceptance_criteria>
      - golden_co_cluster_parallel and golden_co_cluster_below_threshold pass against references captured from the current (pre-parallel) sequential code
      - both goldens pass under BOTH `--features linalg,parallel` AND `--no-default-features --features linalg`
    </acceptance_criteria>
  </verify>
  <done>Both co_cluster goldens pass under both feature configs against constants captured from the current sequential code; committed before the Task 2 src edit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parallelize the co_cluster n_init loop (iter_maybe_parallel! map→Result, sequential best reduce, payback threshold)</name>
  <files>fdars-core/src/coclustering.rs</files>
  <read_first>
    - fdars-core/src/coclustering.rs ~:934-972 (the exact loop to replace — `for init in 0..n_init { let seed = config.seed.wrapping_add(init as u64 * 1000); let km = kmeans_fd(...)?; let init_col_labels = col_kmeans_init(...); let (result, _) = cem_single_fit(...); if best.is_none() || result.log_likelihood > best.log_likelihood { best = Some(result); } }`)
    - fdars-core/src/parallel.rs lines 42-55 (iter_maybe_parallel! form: `iter_maybe_parallel!(0..n_init).map(|init| ...)` → into_par_iter when parallel ON, into_iter when OFF; the closure must be Send+Sync-safe and return an owned value)
    - 48-RESEARCH.md co_cluster candidate (the ?-restructure pattern: parallel map returns Result, collect::<Result<Vec<_>,_>>()? propagates the first error, then a SEQUENTIAL fold picks the best with strict `>`)
    - 48-CONTEXT.md (bit-identical + threshold-guard + determinism decisions)
  </read_first>
  <behavior>
    - golden_co_cluster_parallel still passes BIT-IDENTICALLY after the swap — per-init seeding is unchanged, and the sequential strict-`>` reduction over index-ordered results reproduces the exact best pick + lowest-index tie-break.
    - Below the threshold, the sequential loop runs (golden_co_cluster_below_threshold passes).
    - Error behavior unchanged: the first failing init in index order propagates (collect Result short-circuits on the first Err, matching the old `?`).
  </behavior>
  <action>
    Add `const CO_CLUSTER_INIT_PARALLEL_THRESHOLD: usize = 3;` near the top of the fn's module (documented:
    "below this init count, sequential dispatch avoids rayon overhead — payback threshold, v0.17.0
    SCORES_PARALLEL_THRESHOLD precedent; refined by perf_parallelism thread-scaling in Wave 3").

    Refactor the `for init in 0..n_init` best-of loop into:
    1. A closure `run_init(init: usize) -> Result<CoClusterResult, FdarError>` capturing `data, argvals,
       rotation, mean, weights, config, k_blocks, l_blocks, eff_ncomp, n, m` by reference, containing the
       current body: `let seed = config.seed.wrapping_add(init as u64 * 1000); let km = kmeans_fd(data,
       argvals, k_blocks, 100, 1e-4, seed)?; let init_col_labels = col_kmeans_init(...); let (result, _) =
       cem_single_fit(...); Ok(result)`. (This makes the `?` explicit and Send-safe.)
    2. Build the results with a threshold guard:
       `let results: Vec<CoClusterResult> = if n_init >= CO_CLUSTER_INIT_PARALLEL_THRESHOLD {
            iter_maybe_parallel!(0..n_init).map(|init| run_init(init)).collect::<Result<Vec<_>, _>>()?
        } else {
            (0..n_init).map(|init| run_init(init)).collect::<Result<Vec<_>, _>>()?
        };`
       (Both branches build the SAME Vec in init order; only the dispatch differs.)
    3. Reduce SEQUENTIALLY, preserving the exact strict-`>` semantics (lowest init index wins ties):
       `let best = results.into_iter().reduce(|acc, r| if r.log_likelihood > acc.log_likelihood { r } else { acc });`
       then `best.ok_or_else(|| FdarError::ComputationFailed { operation: "co_cluster", detail: "all initializations failed".to_string() })`.
       NOTE: `reduce` with strict `>` keeps `acc` on ties — since acc is the earlier (lower-index) element,
       this matches the old loop's "only replace when strictly greater" tie-break EXACTLY.

    Keep `co_cluster`'s public signature byte-identical. Ensure the closure/captured refs are `Sync`
    (they are plain `&` to owned data). Do NOT change per-init seeding or any numeric computation.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel -- golden_co_cluster co_cluster 2>&1 | tail -8</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --no-default-features --features linalg -- golden_co_cluster co_cluster 2>&1 | tail -8</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel 2>&1 | tail -5</automated>
    <acceptance_criteria>
      - golden_co_cluster_parallel + below_threshold pass under BOTH feature configs (bit-identical)
      - existing coclustering tests pass; full suite green
      - co_cluster public signature unchanged (grep the fn signature — byte-identical)
      - the reduction uses strict `>` (lowest-index tie-break preserved); per-init seeding unchanged
    </acceptance_criteria>
  </verify>
  <done>co_cluster n_init loop parallelized behavior-preservingly with a payback threshold; goldens bit-identical under both configs; suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Add the co_cluster thread-scaling bench cell + Wave-2 merge gate</name>
  <files>fdars-core/benches/perf_parallelism.rs, .planning/phases/48-parallelism-gap-closure/PERF-PARALLEL-RESULTS.md</files>
  <read_first>
    - fdars-core/benches/perf_parallelism.rs (the Plan-01 bench_frechet_anova cell + criterion_group! — add bench_co_cluster and register it)
    - 48-RESEARCH.md co_cluster thread-scaling recommendation (n_init≥ threshold; a scoped rayon ThreadPoolBuilder inside the bench for a 1-vs-N variant, or the RAYON_NUM_THREADS env sweep)
    - .planning/phases/48-parallelism-gap-closure/PERF-PARALLEL-RESULTS.md (the empty co_cluster median table + Threshold-constants section)
  </read_first>
  <action>
    Add a `bench_co_cluster` cell to `benches/perf_parallelism.rs` at a cell large enough to show thread
    scaling (e.g. n≈200, m≈50, n_init=8 — well above the threshold), deterministic data OUTSIDE `b.iter()`,
    `black_box` inputs+output, `sample_size(10)`, `measurement_time(30s)`. Register it in `criterion_group!`.
    Record the CO_CLUSTER_INIT_PARALLEL_THRESHOLD value in PERF-PARALLEL-RESULTS.md "Threshold constants".

    Run the Wave-2 merge gate: full suite under BOTH feature configs + `cargo clippy --all-targets
    --features linalg,parallel -- -D warnings`. (Actual 1-vs-N thread-scaling numbers are captured in Wave 3.)

    Commit sequence (each: `cargo fmt -p fdars-core` then `git commit --no-verify`): golden test (Task 1),
    parallel co_cluster edit (Task 2), bench cell + gate (Task 3).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --benches --features linalg,parallel 2>&1 | tail -3</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy -p fdars-core --all-targets --features linalg,parallel -- -D warnings 2>&1 | tail -3</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --no-default-features --features linalg 2>&1 | tail -3</automated>
    <acceptance_criteria>
      - perf_parallelism builds with the new bench_co_cluster cell registered
      - clippy --all-targets clean; full suite green under BOTH feature configs
      - PERF-PARALLEL-RESULTS.md records CO_CLUSTER_INIT_PARALLEL_THRESHOLD
    </acceptance_criteria>
  </verify>
  <done>co_cluster thread-scaling bench cell added + registered; Wave-2 gate green under both configs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure in-process numerical parallelization — no external input, I/O, auth, or network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-03 | Tampering | co_cluster best-pick tie-break changes under parallel reduce | high | mitigate | SEQUENTIAL reduce with strict `>` over the index-ordered collected Vec preserves lowest-index tie-break; golden asserts bit-identical labels + log_likelihood |
| T-48-04 | Tampering | error behavior changes (which init's error propagates) | medium | mitigate | collect::<Result<Vec,_>>() short-circuits on the first Err in iteration order = the sequential `?` behavior; preserved |
| T-48-05 | Tampering | non-Send/Sync capture breaks parallel-OFF or ON build | medium | mitigate | run_init captures only `&` to owned data; both feature configs compiled + tested in the wave gate |

No package installs. No new dependency (iter_maybe_parallel! + rayon already present behind the `parallel` feature).
</threat_model>

<verification>
- golden_co_cluster_parallel + below_threshold pass BIT-IDENTICALLY under both feature configs.
- co_cluster parallelized with a payback threshold; per-init seeding + strict-`>` tie-break preserved.
- Full suite green under both configs + clippy --all-targets clean; co_cluster signature unchanged.
- perf_parallelism bench_co_cluster cell registered (permanent — Phase 51 BENCH-02 guard).
</verification>

<success_criteria>
- co_cluster n_init loop parallelized behavior-preservingly (bit-identical, both configs), with a payback threshold and preserved determinism.
- No public signature changed; no new dependency; suite + clippy green under both feature configs.
</success_criteria>

<output>
Create `.planning/phases/48-parallelism-gap-closure/48-02-SUMMARY.md` when done. Record the golden bit-identity confirmation (both configs), the CO_CLUSTER_INIT_PARALLEL_THRESHOLD, and any tie-break/error-propagation notes.
</output>
