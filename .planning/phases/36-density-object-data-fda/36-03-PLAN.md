---
phase: 36-density-object-data-fda
plan: 03
type: execute
wave: 3
depends_on: [36-02]
files_modified:
  - fdars-core/src/density_fda.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [DENS-01]
estimate:
  tokens: 56000
  raw_tokens: 28000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "lqd_fpca reuses fdata_to_pc_1d in LQD space and returns FPC components plus an FVE vector (DENS-01, SC-3)"
    - "FVE is monotone non-decreasing in the number of components and reaches 1 at full rank (DENS-01, SC-3)"
    - "On a single-mode synthetic density family, the leading component captures (near-)all the variance (DENS-01, SC-3)"
    - "LqdFpcaResult embeds the reused FpcaResult and exposes fve: Vec<f64>; density-space modes are documented as deferred in rustdoc (DENS-01, SC-3)"
    - "lqd_fpca and LqdFpcaResult are crate-root re-exported (DENS-01, SC-1)"
    - "Existing public signatures unchanged; full suite + cargo clippy --all-targets --features linalg,parallel -- -D warnings stays green; no new crate dependency (DENS-01, SC-5)"
  artifacts:
    - "fdars-core/src/density_fda.rs (adds LqdFpcaResult + lqd_fpca + inline #[cfg(test)] tests; additive)"
    - "fdars-core/src/lib.rs (crate-root re-export extended with lqd_fpca, LqdFpcaResult)"
  key_links:
    - "each density row → lqd_transform onto the common uniform t-grid → assemble LQD FdMatrix (n × n_q, column-major)"
    - "fdata_to_pc_1d(&lqd_matrix, ncomp, &t_grid) → FpcaResult embedded in LqdFpcaResult"
    - "FVE = cumsum(singular_values²)/sum(singular_values²), monotone non-decreasing → 1 at full rank"
    - "crate-root pub use density_fda::{..., lqd_fpca, LqdFpcaResult} extended in lib.rs"
---

<objective>
Add LQD-FPCA — functional PCA of a sample of probability densities in log-quantile-density space — to the `density_fda` module, then close the phase with a full-suite + full-clippy gate. `lqd_fpca` transforms each density to LQD via the tracer's `lqd_transform` onto a common uniform quantile grid, assembles an LQD `FdMatrix`, and delegates the SVD to the existing `fdata_to_pc_1d` engine (no new SVD code), returning an `LqdFpcaResult` that embeds `FpcaResult` plus an FVE vector.

Purpose: LQD-FPCA is the last of the four DENS-01 capabilities (RESEARCH §Pattern 4). It composes the proven forward transform with the library's ship-tested FPCA engine, so the only genuinely new logic is the LQD-matrix assembly and the FVE computation. This plan also owns the phase-wide merge gate (full test suite + `--all-targets` clippy + Cargo.toml diff-clean).
Output: `fdars-core/src/density_fda.rs` gains `LqdFpcaResult` + `lqd_fpca` (+ inline tests); `fdars-core/src/lib.rs` re-export completed to the final DENS-01 surface; the full suite and clippy gate pass.

Artifacts this phase produces (cumulative — see 36-RESEARCH.md):
- normalize_density, lqd_transform, inverse_lqd — DELIVERED by plan 36-01 (tracer).
- wasserstein_barycenter — DELIVERED by plan 36-02.
- lqd_fpca, LqdFpcaResult — DELIVERED by THIS plan (36-03).
All crate-root re-exported from `fdars-core/src/lib.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-density-object-data-fda/36-RESEARCH.md
@.planning/phases/36-density-object-data-fda/36-PATTERNS.md

@fdars-core/src/density_fda.rs
@fdars-core/src/regression.rs
@fdars-core/src/matrix.rs
@fdars-core/src/lib.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add LqdFpcaResult + lqd_fpca (LQD-space FPCA via fdata_to_pc_1d) to density_fda.rs</name>
  <files>fdars-core/src/density_fda.rs</files>
  <read_first>
    - fdars-core/src/density_fda.rs (whole file, delivered by 36-01/36-02) — reuse `lqd_transform`; match module rustdoc voice, imports, error variants. Do NOT modify existing fns.
    - .planning/phases/36-density-object-data-fda/36-RESEARCH.md §Pattern 4 (LQD-FPCA exact chain: per-row lqd_transform → assemble n×n_q FdMatrix → fdata_to_pc_1d → FVE=cumsum(sv²)/sum(sv²)), §Code Examples (LqdFpcaResult struct + lqd_fpca_fve_monotone_and_bounded test), §Common Pitfalls (Pitfall 5 FVE reaches 1 only at ncomp==min(n,m))
    - .planning/phases/36-density-object-data-fda/36-PATTERNS.md "Result struct pattern" (LqdFpcaResult exact block), "FpcaResult reuse + FVE pattern", "Core function signatures" (lqd_fpca signature + #[must_use]), "Column-major FdMatrix row extraction"
    - fdars-core/src/regression.rs lines 22-38 (FpcaResult fields: singular_values, rotation, scores, mean, centered, weights) and 287-321 (fdata_to_pc_1d signature `(data: &FdMatrix, ncomp: usize, argvals: &[f64]) -> Result<FpcaResult, FdarError>`)
    - fdars-core/src/matrix.rs — FdMatrix::zeros, shape(), indexing `data[(i, j)]` (column-major)
  </read_first>
  <behavior>
    - lqd_fpca on 20 shifted truncated Gaussians (single mode of variation = mean shift), ncomp=5, n_quantile_pts=Some(101): returns LqdFpcaResult; `fve` is monotone non-decreasing (each fve[k] >= fve[k-1] - 1e-12) and every value in [0, 1+1e-9].
    - Leading-PC capture: on that single-mode family, `fve[0] > 0.80` (leading component captures near-all variance of a one-parameter family).
    - Full-rank FVE: with ncomp = min(n_dens, n_quantile_pts), `fve.last()` ≈ 1.0 (within 1e-9).
    - LqdFpcaResult embeds the reused FpcaResult (from regression.rs) and exposes `fve: Vec<f64>`; derives Debug, Clone, PartialEq + conditional serde + #[non_exhaustive].
    - Errors: empty density_matrix (0 rows) → FdarError; argvals length != ncols → FdarError; propagated fdata_to_pc_1d / lqd_transform errors surface as FdarError (no panic).
  </behavior>
  <action>
    Append to `fdars-core/src/density_fda.rs`: (a) the `LqdFpcaResult` struct exactly per 36-PATTERNS.md "Result struct pattern" — `#[derive(Debug, Clone, PartialEq)]`, `#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]`, `#[non_exhaustive]`, fields `pub fpca: FpcaResult` and `pub fve: Vec<f64>` with the documented rustdoc (all fields are in LQD space; note density-space modes are DEFERRED and a caller can obtain them by applying `inverse_lqd` to `fpca.mean ± scale * loading`); and (b) the public `lqd_fpca` fn per the 36-PATTERNS.md "Core function signatures" block, marked `#[must_use = "expensive computation whose result should not be discarded"]`.

    Ensure the module imports `use crate::regression::{fdata_to_pc_1d, FpcaResult};` and `use crate::matrix::FdMatrix;` (add to the existing import block if the tracer did not already need them).

    Implement `lqd_fpca(density_matrix: &FdMatrix, argvals: &[f64], ncomp: usize, n_quantile_pts: Option<usize>) -> Result<LqdFpcaResult, FdarError>` per DENS-01 RESEARCH §Pattern 4:
    1. Validate at entry (36-PATTERNS.md gates): `let (n, m) = density_matrix.shape();` reject n == 0 (InvalidDimension), reject `argvals.len() != m` (InvalidDimension).
    2. `let n_q = n_quantile_pts.unwrap_or(argvals.len().max(101));` build uniform `t_grid[k] = k as f64 / (n_q - 1) as f64` for k in 0..n_q.
    3. Assemble the LQD matrix: `let mut lqd_data = FdMatrix::zeros(n, n_q);` for each row i, extract `row = (0..m).map(|j| density_matrix[(i, j)])`, call `let psi = lqd_transform(&row, argvals, Some(n_q))?;` (reuse the tracer fn — propagate its error with `?`), and write it back column-major: `for (j, &val) in psi.iter().enumerate() { lqd_data[(i, j)] = val; }`.
    4. Delegate SVD: `let fpca = fdata_to_pc_1d(&lqd_data, ncomp, &t_grid)?;` — do NOT re-implement SVD.
    5. FVE: `sv_sq = fpca.singular_values.iter().map(|&s| s*s)`, `total = sum(sv_sq)`, running cumulative `fve[k] = cumsum(sv_sq[0..=k]) / total`. Guard `total < 1e-300` (degenerate) → FdarError::ComputationFailed rather than dividing by zero.
    6. Return `Ok(LqdFpcaResult { fpca, fve })`.

    Do NOT re-implement SVD (delegate to fdata_to_pc_1d, RESEARCH §Don't Hand-Roll). Do NOT ship density-space modes this phase — document the manual recipe in rustdoc per RESEARCH §Open Questions 1. Do NOT add a new crate dependency. No panic on bad input.

    Extend the inline `#[cfg(test)] mod tests` in `density_fda.rs` with: `lqd_fpca_fve_monotone_and_bounded` (use the RESEARCH §Code Examples test verbatim as the basis: 20 shifted truncated Gaussians, ncomp=5, assert fve monotone non-decreasing and all in [0,1+1e-9]), `lqd_fpca_leading_pc_captures_shift` (same single-mode family → assert `fve[0] > 0.80`), `lqd_fpca_full_rank_fve_reaches_one` (ncomp = min(n_dens, n_q) → assert `fve.last()` within 1e-9 of 1.0), `error_lqd_fpca_empty` (0-row matrix → FdarError). Reuse the module's `truncated_gaussian(argvals, mu)` test helper and the column-major FdMatrix write pattern. Deterministic analytic densities — seed nothing.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && cargo test -p fdars-core --features linalg,parallel density_fda:: 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `LqdFpcaResult` present with `pub fpca: FpcaResult`, `pub fve: Vec<f64>`, deriving Debug/Clone/PartialEq + conditional serde + #[non_exhaustive], with rustdoc noting density-space modes are deferred.
    - `lqd_fpca` present, `#[must_use]`, signature `(density_matrix: &FdMatrix, argvals: &[f64], ncomp: usize, n_quantile_pts: Option<usize>) -> Result<LqdFpcaResult, FdarError>`, delegating SVD to `fdata_to_pc_1d`.
    - `lqd_fpca_fve_monotone_and_bounded` passes (FVE monotone non-decreasing, all in [0,1+1e-9]).
    - `lqd_fpca_leading_pc_captures_shift` passes (fve[0] > 0.80 on single-mode family).
    - `lqd_fpca_full_rank_fve_reaches_one` passes (fve.last() ≈ 1.0 at full rank).
    - `error_lqd_fpca_empty` passes (FdarError, no panic).
    - Existing density_fda fns (normalize_density, lqd_transform, inverse_lqd, wasserstein_barycenter) unchanged.
    - Module-scoped test command is green.
  </acceptance_criteria>
  <done>lqd_fpca + LqdFpcaResult exist in density_fda.rs, reuse fdata_to_pc_1d in LQD space, and return an FVE that is monotone non-decreasing, reaches 1 at full rank, and is dominated by the leading component on a single-mode family; the slice is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Complete crate-root re-export in lib.rs (lqd_fpca, LqdFpcaResult)</name>
  <files>fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/lib.rs — the density_fda re-export block (after 36-02: `pub use density_fda::{inverse_lqd, lqd_transform, normalize_density, wasserstein_barycenter};`)
    - .planning/phases/36-density-object-data-fda/36-PATTERNS.md "fdars-core/src/lib.rs (modified — re-exports only)" — the final target re-export list
  </read_first>
  <action>
    Using Edit (scoped — do NOT rewrite lib.rs wholesale), extend the existing `pub use density_fda::{...}` re-export line to the final DENS-01 surface, adding `lqd_fpca` and `LqdFpcaResult` in the correct order (fns alphabetical, result type last per the 36-PATTERNS.md target block). Final line: `pub use density_fda::{inverse_lqd, lqd_fpca, lqd_transform, normalize_density, wasserstein_barycenter, LqdFpcaResult};`. The `pub mod density_fda;` declaration is already present; do not duplicate it.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && cargo build -p fdars-core --features linalg,parallel 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `fdars-core/src/lib.rs` re-exports all six DENS-01 symbols: `inverse_lqd, lqd_fpca, lqd_transform, normalize_density, wasserstein_barycenter, LqdFpcaResult`.
    - `cargo build -p fdars-core --features linalg,parallel` succeeds.
  </acceptance_criteria>
  <done>All DENS-01 public entry points and LqdFpcaResult are re-exported at the crate root; the crate builds.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — full suite + full clippy + Cargo.toml diff-clean (no new dependency)</name>
  <files>fdars-core/src/density_fda.rs</files>
  <read_first>
    - MEMORY.md pointer "CI clippy uses --all-targets" — the phase clippy gate must be `--all-targets` (lints test/bench code too)
    - MEMORY.md pointer "/tmp exhaustion blocks pre-commit" — doctests link in a small /tmp; set TMPDIR to /home/simonm/.cache/fdars-bench-tmp (create first) if linking reports a bogus "No space left"
    - MEMORY.md pointer "target/ fills /home partition" — if a full build dies at example LINK ("linking with cc failed"), it is disk pressure, not a code bug; free space with rm -rf target/debug/{incremental,examples}
  </read_first>
  <action>
    Run the phase-wide merge gate confirming the additive DENS-01 module broke nothing, introduced no lint warnings (including in test code, which `--all-targets` lints), and added no crate dependency:
    1. Full test suite: `cargo test -p fdars-core --features linalg,parallel` — must be green (existing public signatures across fdars-core, including fdata_to_pc_1d, keep working).
    2. Full clippy gate: `cargo clippy --all-targets --features linalg,parallel -- -D warnings` — must be clean. Fix any lint in density_fda.rs (no `#[allow]` suppressions unless matching an existing crate-level allow).
    3. Cargo.toml diff-clean: `git diff --exit-code fdars-core/Cargo.toml` — must exit 0 (no new dependency added; the constraint is locked).
    If the /tmp tmpfs blocks doctest linking, set `TMPDIR=/home/simonm/.cache/fdars-bench-tmp` (create it first) before the test run, per MEMORY.md. Do not finalize the phase with a red suite, dirty clippy, or a modified Cargo.toml.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && cargo test -p fdars-core --features linalg,parallel 2>&1 | tail -5 && cargo clippy --all-targets --features linalg,parallel -- -D warnings 2>&1 | tail -5 && git diff --exit-code fdars-core/Cargo.toml && echo CARGO_TOML_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - Full `cargo test -p fdars-core --features linalg,parallel` is green.
    - `cargo clippy --all-targets --features linalg,parallel -- -D warnings` is clean (zero warnings, test code included).
    - `git diff --exit-code fdars-core/Cargo.toml` exits 0 (no new dependency) and `CARGO_TOML_CLEAN` prints.
  </acceptance_criteria>
  <done>The complete DENS-01 module passes the full suite and the full `--all-targets` clippy gate, and Cargo.toml is unchanged; the phase is merge-ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → public fn | Numeric arrays (density matrix rows, argvals, ncomp) cross into the module; the only untrusted surface. No network, file, deserialization, or session I/O. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-06 | Tampering | lqd_fpca (FVE division by total variance) | low | mitigate | Guard `total < 1e-300` before dividing; return FdarError::ComputationFailed on degenerate (all-zero singular values) rather than producing NaN. |
| T-36-07 | Tampering | lqd_fpca (per-row lqd_transform of zero/degenerate density) | low | mitigate | Errors from lqd_transform (zero/negative density) propagate via `?` as FdarError; no panic, no silent NaN. |

No new external packages introduced (constraint: no new crate dependency; enforced by the Task 3 `git diff --exit-code fdars-core/Cargo.toml` gate) — no supply-chain threat (T-36-SC N/A).
</threat_model>

<verification>
- `cargo test -p fdars-core --features linalg,parallel density_fda::` green (FVE monotone → 1, leading-PC capture, error gates).
- Full `cargo test -p fdars-core --features linalg,parallel` green.
- `cargo clippy --all-targets --features linalg,parallel -- -D warnings` clean.
- `git diff --exit-code fdars-core/Cargo.toml` exits 0.
</verification>

<success_criteria>
- lqd_fpca + LqdFpcaResult exist, are crate-root re-exported, reuse fdata_to_pc_1d in LQD space, and are Result-returning.
- FVE is monotone non-decreasing, reaches 1 at full rank, and is dominated by the leading component on a single-mode family.
- Existing public signatures unchanged (additive/non-breaking); full suite + `--all-targets` clippy green; no new crate dependency.
</success_criteria>

<output>
Create `.planning/phases/36-density-object-data-fda/36-03-SUMMARY.md` when done
</output>