---
phase: 03-elastic-alignment-hot-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/benches/audit_hotpaths.rs
  - .planning/research/AUDIT-REPORT.md
  - .planning/research/bench/p3_karcher_linalg,parallel_run1.txt
autonomous: true
requirements: [PERF-03]
estimate:
  tokens: 55000
  raw_tokens: 28000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "A new criterion bench function for karcher_mean at one grid cell (N=100, M=50) exists in audit_hotpaths.rs and is registered in criterion_group!"
    - "cargo bench for that cell runs under release + linalg,parallel and emits timing output"
    - "Raw criterion output is saved under .planning/research/bench/ with the p3_ naming convention"
    - "AUDIT-REPORT.md contains a Phase-3 section header and at least one results-table row linking to its bench artifact"
    - "AUDIT-REPORT.md contains one draft backlog stub with function, current-cost, and root-cause fields for the elastic hot path"
  artifacts:
    - fdars-core/benches/audit_hotpaths.rs
    - .planning/research/AUDIT-REPORT.md
    - ".planning/research/bench/p3_karcher_linalg,parallel_run1.txt"
  key_links:
    - "bench fn bench_p3_karcher registered in criterion_group! → runnable via cargo bench"
    - "AUDIT-REPORT row → links to p3_karcher_*.txt artifact file"
    - "karcher_mean(band_frac=0.0 default) source fact → Anti-Pattern 2 backlog root-cause"
---

<objective>
**As a** performance auditor, **I want to** prove the Phase-3 elastic-benchmark apparatus end-to-end on a single karcher_mean cell — bench function → release run → raw artifact → one AUDIT-REPORT results row → one backlog stub — **so that** the full sweep in Plan 02 expands a proven pipeline instead of discovering the wiring on the last cell.

This is the tracer slice for the phase's single vertical slice (measure → report → backlog). It adds ONE new criterion bench cell (`karcher_mean` at N=100×M=50, the D-06-locked params) to the existing Phase-1 audit harness, runs it release + `linalg,parallel`, saves the raw output under `bench/p3_*`, and appends the Phase-3 section skeleton to AUDIT-REPORT.md with one populated row and one backlog stub.

No `fdars-core/src` algorithm changes — measurement infrastructure only (this is an audit phase).

Purpose: De-risk the whole slice on the agent's best early-context tokens; catch any bench-harness/run/artifact/report wiring problem after one cell, not after the full grid.
Output: One new bench fn `bench_p3_karcher`, one artifact `p3_karcher_linalg,parallel_run1.txt`, a Phase-3 AUDIT-REPORT section with one row + one backlog stub.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-elastic-alignment-hot-path/03-CONTEXT.md
@.planning/phases/03-elastic-alignment-hot-path/03-PATTERNS.md

# The bench file to EXTEND (has generate_curves, sentinel analogs, criterion_group!)
@fdars-core/benches/audit_hotpaths.rs

# Target-function signatures
@fdars-core/src/alignment/karcher.rs

# The report to APPEND to (single growing file per Phase-1 D-05)
@.planning/research/AUDIT-REPORT.md
</context>

<artifacts_this_phase_produces>
This plan (tracer) introduces these NEW symbols. Plan 02 adds the rest of the grid.

**New bench functions (in `fdars-core/benches/audit_hotpaths.rs`):**
- `bench_p3_karcher` — karcher_mean at N=100×M=50, D-06 params (max_iter=20, tol=1e-4, lambda=0.0)

**New criterion group name:** reuses group `"audit_p3_karcher"` (created here; Plan 02 adds `"audit_p3_elastic_self"`, `"audit_p3_elastic_cross"`, and banded variants).

**New artifact files (under `.planning/research/bench/`):**
- `p3_karcher_linalg,parallel_run1.txt` — raw criterion output for this tracer cell (Plan 02 adds run2 + the full grid artifacts)
</artifacts_this_phase_produces>

<precondition>
Bench linking requires a non-full temp dir: export `TMPDIR=/home/simonm/.cache/fdars-bench-tmp` (mkdir -p first) before running cargo bench — the /tmp tmpfs is near-full and causes linker bus-errors (STATE.md decision, MEMORY.md). This is infrastructure, not a code failure (PITFALLS Pitfall 8).
</precondition>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Add the karcher_mean tracer bench cell</name>
  <read_first>
    - fdars-core/benches/audit_hotpaths.rs (lines 30-48 generate_curves, 94-125 bench_matrix_sentinel analog, 260-270 criterion_group!)
    - fdars-core/src/alignment/karcher.rs (lines 293-321: karcher_mean + karcher_mean_banded signatures; :300 karcher_mean_impl(.., 0.0) default)
    - .planning/phases/03-elastic-alignment-hot-path/03-PATTERNS.md (karcher cell pattern, lines 91-108)
  </read_first>
  <files>fdars-core/benches/audit_hotpaths.rs</files>
  <action>
    Add one new bench function `bench_p3_karcher(c: &mut Criterion)` to audit_hotpaths.rs, mirroring the existing `bench_matrix_sentinel` (lines 103-125) EXACTLY in structure: benchmark_group named "audit_p3_karcher", build the `(data, argvals)` tuple via the existing `generate_curves(100, 50)` OUTSIDE `b.iter()`, and inside `b.iter()` call `karcher_mean` with all args wrapped in `black_box` AND the return wrapped in `black_box`.

    Use the D-06-locked parameters (NOT the sentinel's 10/1e-3): `black_box(20usize)` for max_iter, `black_box(1e-4)` for tol, `black_box(0.0)` for lambda. Bench cell name `"n100_m50"`.

    Set `group.sample_size(20)`, `group.measurement_time(Duration::from_secs(20))`, `group.warm_up_time(Duration::from_secs(5))` (sentinel defaults — this N=100×M=50 cell is small).

    Extend the alignment `use` statement (line 21) to also import `karcher_mean_banded`, `elastic_self_distance_matrix_banded`, `elastic_cross_distance_matrix`, `elastic_cross_distance_matrix_banded` now (all publicly re-exported from `fdars_core::alignment` per mod.rs:66,78-79) so Plan 02 needs no further import edits. Unused imports at this stage are acceptable — the compiler `#[allow(unused_imports)]` is not needed because Plan 02 consumes them; if the bench must compile clean at this wave, add the four Plan-02 imports inside a `#[allow(unused_imports)]` on the use block, or defer them to Plan 02 (executor's choice — prefer deferring the three unused-until-Plan-02 imports to keep this wave warning-clean, importing only `karcher_mean_banded` here since Task 3's backlog stub references it).

    Register `bench_p3_karcher` in the `criterion_group!` macro list (after `bench_smooth_sentinel`).

    Write a `///` doc comment on `bench_p3_karcher` following the sentinel doc-comment style: state the D-06 params, that it is the Phase-3 karcher unbanded cell, and that karcher_mean defaults band_frac=0.0 (unbanded full DP, Anti-Pattern 2 — D-05).
  </action>
  <verify>
    <automated>cargo build -p fdars-core --benches --features linalg,parallel 2>&1 | tail -5 && grep -c "bench_p3_karcher" fdars-core/benches/audit_hotpaths.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fn bench_p3_karcher" fdars-core/benches/audit_hotpaths.rs` returns 1
    - `bench_p3_karcher` appears in the `criterion_group!` list (grep confirms it after the sentinel entries)
    - The call uses `black_box(20usize)`, `black_box(1e-4)`, `black_box(0.0)` — the D-06 params, not the sentinel 10/1e-3
    - `cargo build -p fdars-core --benches --features linalg,parallel` succeeds
  </acceptance_criteria>
  <done>The audit bench compiles with a new bench_p3_karcher cell using D-06 params, registered in criterion_group!.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run the tracer cell release + linalg,parallel and save the raw artifact</name>
  <read_first>
    - .planning/research/bench/p1_karcher_linalg,parallel_run1.txt (Phase-1 artifact format to match)
    - .planning/research/PITFALLS.md (Pitfall 1 release-binary path; Pitfall 3 black_box; Pitfall 8 linker bus-error = infra; Pitfall 17 evidence under bench/)
    - .planning/phases/01-measurement-discipline-baselines/01-CONTEXT.md (bench artifact naming p<phase>_<target>_<features>_run<N>.txt)
  </read_first>
  <files>.planning/research/bench/p3_karcher_linalg,parallel_run1.txt</files>
  <action>
    Ensure `mkdir -p /home/simonm/.cache/fdars-bench-tmp` then run the tracer cell with TMPDIR pointing there (precondition), release build, features linalg,parallel, filtering to just this group:

    `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo bench -p fdars-core --features linalg,parallel --bench audit_hotpaths -- audit_p3_karcher`

    Capture the full stdout+stderr to `.planning/research/bench/p3_karcher_linalg,parallel_run1.txt` (use `tee` so the run is visible AND saved). Prepend or append a one-line header capturing the toolchain version (`rustc --version`) and the feature set `linalg,parallel` and the release confirmation (criterion prints the target path — confirm it contains `/release/`), matching the Phase-1 artifact convention.

    If the run fails with a linker/allocator bus-error ("No space left", SIGBUS), that is an INFRASTRUCTURE failure per Pitfall 8 — free /tmp or confirm TMPDIR is set, then retry; do NOT treat it as a code defect and do NOT edit the bench source to work around it.
  </action>
  <verify>
    <automated>test -f ".planning/research/bench/p3_karcher_linalg,parallel_run1.txt" && grep -Eiq "time:|n100_m50" ".planning/research/bench/p3_karcher_linalg,parallel_run1.txt" && echo ARTIFACT_OK</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/research/bench/p3_karcher_linalg,parallel_run1.txt` exists
    - The file contains a criterion timing line (matches `time:` or the cell name `n100_m50`)
    - The file records the toolchain version and feature set `linalg,parallel`
    - The criterion output confirms a `/release/` binary path was used (Pitfall 1)
  </acceptance_criteria>
  <done>The karcher tracer cell has run release + linalg,parallel and its raw criterion output is saved under bench/p3_* with toolchain + feature tagging.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Append the Phase-3 AUDIT-REPORT skeleton with one row and one backlog stub</name>
  <read_first>
    - .planning/research/AUDIT-REPORT.md (tail — the append target; match existing section/table markdown style)
    - .planning/phases/03-elastic-alignment-hot-path/03-CONTEXT.md (D-05 Anti-Pattern 2, D-07 backlog fields)
    - ".planning/research/bench/p3_karcher_linalg,parallel_run1.txt" (the measured number to transcribe into the row)
  </read_first>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <action>
    Append (do NOT overwrite — this is the single growing report per Phase-1 D-05) a new section to AUDIT-REPORT.md titled `### Phase 3: Elastic Alignment Hot Path — Benchmark Results`.

    Under it, create the results-table skeleton with the header row `| Target | N | M | Features | band_frac | Mean time | Artifact | Confidence |` and its markdown separator, then populate exactly ONE data row from the tracer run: `karcher_mean`, N=100, M=50, `linalg,parallel`, band_frac=0.0, the measured mean time transcribed from `p3_karcher_linalg,parallel_run1.txt`, a link to that artifact file, and confidence `PENDING (single run — Plan 02 adds run2 for ±5% variance)`.

    Add a short prose note recording the D-05 source fact: `karcher_mean()` calls `karcher_mean_impl(.., 0.0)` at karcher.rs:300 → band_frac=0.0 → full unbanded DP by default (Anti-Pattern 2 from the Phase-2 static analysis). Cite the file:line.

    Add a `#### Draft Backlog (elastic alignment) — Phase 3 slice` subsection with ONE stub entry carrying the D-07 SC4 fields: **Function:** `karcher_mean` / distance matrices default to unbanded full DP; **Current cost:** (reference the tracer number, note Plan 02 completes the grid + banded comparison); **Root cause:** Anti-Pattern 2 / banding opt-in — cite the AUDIT-REPORT Parallelism-Gap "BANDING OPT-IN" row and the elastic O(max_iter·N·m²) complexity row; **Candidate fix (one line):** default `karcher_mean`/distance matrices to a banded path or expose `band_frac` on the high-level API. Mark it `[STUB — Plan 02 finalizes with full banded-vs-unbanded numbers]`.

    Do NOT invent a banded-vs-unbanded reduction number here — that comes from Plan 02's measurements. This tracer only proves the row+stub wiring.
  </action>
  <verify>
    <automated>grep -q "Phase 3: Elastic Alignment Hot Path" .planning/research/AUDIT-REPORT.md && grep -q "p3_karcher" .planning/research/AUDIT-REPORT.md && grep -Eiq "backlog" .planning/research/AUDIT-REPORT.md && echo REPORT_OK</automated>
  </verify>
  <acceptance_criteria>
    - AUDIT-REPORT.md contains a `### Phase 3: Elastic Alignment Hot Path` section (appended, prior content intact)
    - The results table has the 8-column header and exactly one populated `karcher_mean` row linking to `p3_karcher_linalg,parallel_run1.txt`
    - The D-05 note cites `karcher.rs:300` and names Anti-Pattern 2
    - A draft backlog stub exists carrying Function / Current cost / Root cause / Candidate-fix fields (D-07)
    - Prior AUDIT-REPORT content (Phase 1 + Phase 2 sections) is unchanged (append-only)
  </acceptance_criteria>
  <done>AUDIT-REPORT.md has a Phase-3 section with one measured karcher row, the D-05 Anti-Pattern-2 note, and one D-07-shaped backlog stub — proving the report+backlog end of the pipeline works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Benchmark-only phase. Bench code reads seeded synthetic data generated in-process (`generate_curves`, no RNG, no I/O input). No network, no auth, no external/untrusted input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | cargo bench dependency graph (criterion already a dev-dep; no new deps added) | low | accept | No new packages installed this phase — audit-only measurement code. No package-legitimacy gate needed. |

At ASVS L1 this benchmark-only phase has no network/auth/input-handling attack surface: bench code reads only seeded synthetic data built in-process. No `high` threats. Threat model is minimal by design.
</threat_model>

<verification>
- `cargo build -p fdars-core --benches --features linalg,parallel` succeeds with the new bench fn.
- `bench_p3_karcher` present and registered (`grep -c "bench_p3_karcher"` ≥ 2: definition + criterion_group entry).
- Artifact `p3_karcher_linalg,parallel_run1.txt` exists with a criterion timing line + toolchain/feature tag + `/release/` confirmation.
- AUDIT-REPORT.md Phase-3 section exists with one populated row + D-05 note + one backlog stub; earlier sections intact.
</verification>

<success_criteria>
The thin end-to-end slice works: one karcher cell benched release+linalg,parallel → raw artifact saved → one AUDIT-REPORT row links it → one backlog stub drafted. The pipeline (bench → run → artifact → report → backlog) is proven so Plan 02 can expand to the full grid without re-discovering wiring.
</success_criteria>

<output>
Create `.planning/phases/03-elastic-alignment-hot-path/03-01-SUMMARY.md` when done.
</output>
