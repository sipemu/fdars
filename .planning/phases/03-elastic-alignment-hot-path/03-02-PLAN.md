---
phase: 03-elastic-alignment-hot-path
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - fdars-core/benches/audit_hotpaths.rs
  - .planning/research/AUDIT-REPORT.md
  - .planning/research/bench/
  - .planning/phases/03-elastic-alignment-hot-path/03-COVERAGE.md
autonomous: true
requirements: [PERF-03]
estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "Bench functions exist for all three targets (karcher_mean, elastic_self_distance_matrix, elastic_cross_distance_matrix) swept over N∈{100,500}×M∈{50,200}, each with an unbanded and a banded (band_frac=0.1) variant"
    - "elastic_cross_distance_matrix is benched square N×N (data1=data2), directly comparable to self-distance (D-02)"
    - "Every grid cell runs release + linalg,parallel with black_box on inputs AND outputs and saves raw criterion output twice (run1, run2) under .planning/research/bench/p3_*"
    - "AUDIT-REPORT.md Phase-3 results table is filled for all three targets × grid × banded/unbanded, each row tagged with feature set + toolchain and linking its bench artifact"
    - "The banded-vs-unbanded reduction is quantified per target against the ~7× expected / ~10× theoretical (m/band at M=200), and karcher_mean's default band_frac=0.0 (unbanded) is explicitly confirmed and recorded (D-05, Anti-Pattern 2)"
    - "Two-run variance is computed per cell; cells over ±5% noted, cells over 10% marked LOW CONFIDENCE (SC3)"
    - "Backlog entries for the elastic hot path carry function / current-cost (Phase-3 measured) / root-cause (cites AUDIT-REPORT anti-pattern + complexity row) / one-line candidate fix (D-07), GSD-ready for Phase 9"
    - { statement: "The observed banded reduction (~7×) and per-cell ±5% two-run variance are correctly derived from the produced criterion numbers", verification: backstop }
  artifacts:
    - fdars-core/benches/audit_hotpaths.rs
    - .planning/research/AUDIT-REPORT.md
    - .planning/phases/03-elastic-alignment-hot-path/03-COVERAGE.md
  key_links:
    - "each results-table row → its p3_<target>_<features>_run<N>.txt artifact"
    - "banded bench cell (band_frac=0.1) → band_radius(0.1, m) internal conversion → observed reduction claim"
    - "measured current-cost → backlog entry → root-cause cite of AUDIT-REPORT Anti-Pattern 2 / elastic complexity row"
---

<objective>
**As a** performance auditor, **I want to** complete the full elastic-alignment benchmark sweep (three targets × N∈{100,500}×M∈{50,200} × banded/unbanded), record the banded-vs-unbanded reduction, and finalize the Phase-3 report table + backlog, **so that** Phase 9 can rank the elastic hot path against every other module on comparable, reproducible evidence.

This expands the Plan-01 tracer (one karcher cell) into the complete grid. It adds the remaining bench cells to `audit_hotpaths.rs`, runs every cell twice release + `linalg,parallel`, saves all raw artifacts under `bench/p3_*`, fills the AUDIT-REPORT results table, quantifies the ~7× banded reduction across all three targets, confirms karcher's unbanded default (D-05), computes two-run variance (SC3), and writes the GSD-ready backlog entries (D-07).

No `fdars-core/src` algorithm changes — the banded API twins already exist as public functions; this plan only adds bench cases and report/backlog prose.

Purpose: Deliver all four ROADMAP Phase-3 success criteria (results table, banded-vs-unbanded ~7×, reproducible evidence, backlog slice).
Output: Full grid of bench fns, ~2 runs × all cells of `p3_*` artifacts, a complete AUDIT-REPORT Phase-3 section, elastic backlog entries, and 03-COVERAGE.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-elastic-alignment-hot-path/03-CONTEXT.md
@.planning/phases/03-elastic-alignment-hot-path/03-PATTERNS.md

# The bench file (tracer added bench_p3_karcher + imports; extend it)
@fdars-core/benches/audit_hotpaths.rs

# Target signatures (plain + banded)
@fdars-core/src/alignment/karcher.rs
@fdars-core/src/alignment/pairwise.rs

# The report with the Phase-3 skeleton from Plan 01 (append/fill; do not overwrite)
@.planning/research/AUDIT-REPORT.md

# Plan 01's summary — the tracer's karcher number + confirmed pipeline
@.planning/phases/03-elastic-alignment-hot-path/03-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Plan 01 produced `bench_p3_karcher` + `audit_p3_karcher` group + `p3_karcher_linalg,parallel_run1.txt`. This plan (expansion) adds the rest.

**New bench functions (in `fdars-core/benches/audit_hotpaths.rs`):**
- `bench_p3_karcher_banded` — karcher_mean_banded, band_frac=0.1, full grid
- `bench_p3_elastic_self` — elastic_self_distance_matrix, full grid (completes the Phase-1 N=100×M=50 cell to the full N∈{100,500}×M∈{50,200})
- `bench_p3_elastic_self_banded` — elastic_self_distance_matrix_banded, band_frac=0.1, full grid
- `bench_p3_elastic_cross` — elastic_cross_distance_matrix, square N×N (data1=data2), full grid
- `bench_p3_elastic_cross_banded` — elastic_cross_distance_matrix_banded, band_frac=0.1, square N×N, full grid
- (bench_p3_karcher from Plan 01 is EXTENDED to cover the remaining 3 grid cells N∈{100,500}×M∈{50,200} beyond the tracer's N=100×M=50)

**New criterion group names:** `audit_p3_karcher_banded`, `audit_p3_elastic_self`, `audit_p3_elastic_self_banded`, `audit_p3_elastic_cross`, `audit_p3_elastic_cross_banded` (plus the tracer's `audit_p3_karcher`).

**New artifact files (under `.planning/research/bench/`), p<phase>_<target>_<features>_run<N>.txt convention:**
- `p3_karcher_linalg,parallel_run2.txt` (+ run1 completed for full grid)
- `p3_karcher_banded_linalg,parallel_run{1,2}.txt`
- `p3_elastic_self_linalg,parallel_run{1,2}.txt`
- `p3_elastic_self_banded_linalg,parallel_run{1,2}.txt`
- `p3_elastic_cross_linalg,parallel_run{1,2}.txt`
- `p3_elastic_cross_banded_linalg,parallel_run{1,2}.txt`

**New doc file:**
- `.planning/phases/03-elastic-alignment-hot-path/03-COVERAGE.md` (API-coverage gate; benchmark-only phase)
</artifacts_this_phase_produces>

<precondition>
Bench linking requires `TMPDIR=/home/simonm/.cache/fdars-bench-tmp` (mkdir -p first) set on every cargo bench invocation — the /tmp tmpfs is near-full and produces linker bus-errors otherwise (STATE.md decision, MEMORY.md, PITFALLS Pitfall 8 = infra failure, not code). Free /tmp if the cache dir alone is insufficient.
</precondition>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add the full grid of bench cells for all three targets, banded and unbanded</name>
  <read_first>
    - fdars-core/benches/audit_hotpaths.rs (current state after Plan 01: bench_p3_karcher + extended alignment imports; generate_curves at 34-48; sentinel analogs)
    - fdars-core/src/alignment/pairwise.rs (194 self, 205 self_banded, 266 cross, 278 cross_banded signatures)
    - fdars-core/src/alignment/karcher.rs (293 karcher_mean, 312 karcher_mean_banded)
    - .planning/phases/03-elastic-alignment-hot-path/03-PATTERNS.md (distance-matrix + karcher cell patterns, band_frac semantics, criterion cell discipline)
  </read_first>
  <files>fdars-core/benches/audit_hotpaths.rs</files>
  <action>
    Add the remaining Phase-3 bench cells so all three targets are swept over the full grid N∈{100,500}×M∈{50,200}, each with an unbanded and a band_frac=0.1 banded variant. Mirror the tracer/sentinel structure exactly: build each `(data, argvals)` via `generate_curves(n, m)` OUTSIDE `b.iter()`, wrap all inputs AND the return value in `black_box`.

    Six group functions total (the tracer's `bench_p3_karcher` already exists — EXTEND it to add its remaining 3 grid cells, then add the five new fns):
    - `bench_p3_karcher` (extend): add cells n100_m200, n500_m50, n500_m200 alongside the tracer's n100_m50 — `karcher_mean(&data, &argvals, 20, 1e-4, 0.0)` (D-06 params).
    - `bench_p3_karcher_banded` (new, group "audit_p3_karcher_banded"): all 4 grid cells — `karcher_mean_banded(&data, &argvals, 20, 1e-4, 0.0, 0.1)` (D-03/D-04, band_frac=0.1 as 6th arg).
    - `bench_p3_elastic_self` (new, group "audit_p3_elastic_self"): all 4 cells — `elastic_self_distance_matrix(&data, &argvals, 0.0)` (lambda=0.0).
    - `bench_p3_elastic_self_banded` (new, "audit_p3_elastic_self_banded"): all 4 cells — `elastic_self_distance_matrix_banded(&data, &argvals, 0.0, 0.1)`.
    - `bench_p3_elastic_cross` (new, "audit_p3_elastic_cross"): all 4 cells, SQUARE N×N per D-02 — generate ONCE and pass as BOTH args: `elastic_cross_distance_matrix(&data, &data, &argvals, 0.0)`.
    - `bench_p3_elastic_cross_banded` (new, "audit_p3_elastic_cross_banded"): all 4 cells, square — `elastic_cross_distance_matrix_banded(&data, &data, &argvals, 0.0, 0.1)`.

    Bench-cell names within each group: `n100_m50`, `n100_m200`, `n500_m50`, `n500_m200`.

    Per-cell criterion tuning (Claude's discretion within Phase-1 methodology): the borderline N=500×M=200 cells warrant `measurement_time = 60s` (workload matrix). Set the borderline elastic cells' groups to `sample_size(10..20)` + `measurement_time(60s)`; smaller cells may keep sentinel defaults (`sample_size(20)`, `measurement_time(20s)`, `warm_up_time(5s)`). Do NOT exceed the Phase-1 cap (N≤500, M≤200) — add no extra cells. Document the applied tuning in each group fn's `///` doc comment (matching sentinel doc style).

    Register all six group fns in `criterion_group!` (the tracer already registered `bench_p3_karcher` — add the five new ones after it). If the four Plan-02 imports were deferred by Plan 01, add `elastic_self_distance_matrix_banded, elastic_cross_distance_matrix, elastic_cross_distance_matrix_banded` to the `fdars_core::alignment` use statement now.
  </action>
  <verify>
    <automated>cargo build -p fdars-core --benches --features linalg,parallel 2>&1 | tail -5 && grep -Eoc "fn bench_p3_(karcher|karcher_banded|elastic_self|elastic_self_banded|elastic_cross|elastic_cross_banded)\b" fdars-core/benches/audit_hotpaths.rs</automated>
  </verify>
  <acceptance_criteria>
    - All six bench fns present: `grep -c "fn bench_p3_karcher_banded"`, `..._elastic_self`, `..._elastic_self_banded`, `..._elastic_cross`, `..._elastic_cross_banded` each return 1 (bench_p3_karcher already existed)
    - `bench_p3_elastic_cross` and `_banded` pass `&data, &data` (square N×N per D-02) — grep confirms the doubled arg
    - karcher cells use `20, 1e-4, 0.0` (D-06); banded karcher appends `0.1`; distance targets use `0.0` lambda (banded append `0.1`)
    - Each group covers the 4 grid cells `n100_m50/n100_m200/n500_m50/n500_m200` and none outside N≤500,M≤200
    - All six fns registered in `criterion_group!`; `cargo build -p fdars-core --benches --features linalg,parallel` succeeds
  </acceptance_criteria>
  <done>The audit bench compiles with the complete Phase-3 grid: three targets × {unbanded, banded band_frac=0.1} × N∈{100,500}×M∈{50,200}, cross benched square N×N.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run the full grid twice (release + linalg,parallel) and save all raw artifacts</name>
  <read_first>
    - .planning/research/PITFALLS.md (Pitfall 1 release path, 3 black_box, 7 ±5% two-run variance, 8 linker=infra, 17 evidence under bench/)
    - .planning/phases/01-measurement-discipline-baselines/01-CONTEXT.md (artifact naming, variance rule)
    - ".planning/research/bench/p3_karcher_linalg,parallel_run1.txt" (tracer artifact format to match)
  </read_first>
  <files>.planning/research/bench/</files>
  <action>
    With `mkdir -p /home/simonm/.cache/fdars-bench-tmp` and `TMPDIR=/home/simonm/.cache/fdars-bench-tmp` set (precondition), run each of the six criterion groups TWICE (run1, run2) in release + `--features linalg,parallel`, saving raw output to `.planning/research/bench/p3_<target>_<features>_run<N>.txt` per the Phase-1 naming convention:

    For each group G in {audit_p3_karcher, audit_p3_karcher_banded, audit_p3_elastic_self, audit_p3_elastic_self_banded, audit_p3_elastic_cross, audit_p3_elastic_cross_banded}, for N in {1,2}:
    `TMPDIR=... cargo bench -p fdars-core --features linalg,parallel --bench audit_hotpaths -- <G> | tee .planning/research/bench/p3_<target>_linalg,parallel_run<N>.txt`

    Map group→target-filename: audit_p3_karcher→`karcher`, audit_p3_karcher_banded→`karcher_banded`, audit_p3_elastic_self→`elastic_self`, audit_p3_elastic_self_banded→`elastic_self_banded`, audit_p3_elastic_cross→`elastic_cross`, audit_p3_elastic_cross_banded→`elastic_cross_banded`. (The tracer's `p3_karcher_..._run1.txt` may be regenerated to cover the now-4-cell group; keep both runs.)

    Each artifact records the toolchain (`rustc --version`) and feature set `linalg,parallel`, and the criterion output must show a `/release/` binary path (Pitfall 1). Total runtime is significant (the N=500×M=200 elastic cells are the workload-matrix's 60s cells × 4 targets × 2 runs) — this is expected; run it, do not shortcut by dropping cells.

    Any SIGBUS/"No space left" during linking = infrastructure failure (Pitfall 8): re-assert TMPDIR / free /tmp and retry; never edit bench source to dodge it.
  </action>
  <verify>
    <automated>ls .planning/research/bench/p3_*_run1.txt .planning/research/bench/p3_*_run2.txt 2>/dev/null | wc -l | grep -qx 12 && for f in .planning/research/bench/p3_*_run*.txt; do grep -Eiq "time:|n100_m50|n500_m200" "$f" || { echo "MISSING TIMING: $f"; exit 1; }; done && echo ALL_ARTIFACTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - 12 artifact files exist (6 targets × run1/run2): `karcher`, `karcher_banded`, `elastic_self`, `elastic_self_banded`, `elastic_cross`, `elastic_cross_banded` each with `_run1` and `_run2`
    - Every artifact contains criterion timing output (matches `time:` / a cell name) and records toolchain + `linalg,parallel`
    - Criterion output confirms `/release/` binary path (Pitfall 1)
    - No cell outside the N≤500,M≤200 cap was run
  </acceptance_criteria>
  <done>All six Phase-3 groups have run twice release+linalg,parallel; 12 raw artifacts saved under bench/p3_* with toolchain + feature tagging and /release/ confirmation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fill the AUDIT-REPORT Phase-3 results table, banded-vs-unbanded analysis, and variance</name>
  <read_first>
    - .planning/research/AUDIT-REPORT.md (the Phase-3 skeleton from Plan 01; the elastic complexity row + Anti-Pattern 2 / BANDING OPT-IN parallelism row to cite)
    - all 12 .planning/research/bench/p3_*_run*.txt artifacts (the numbers to transcribe + variance-check)
    - .planning/phases/03-elastic-alignment-hot-path/03-CONTEXT.md (D-01..D-06; ~7× expected / ~10× theoretical framing)
    - .planning/phases/01-measurement-discipline-baselines/01-CONTEXT.md (±5% / >10% LOW CONFIDENCE rule)
  </read_first>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <action>
    Fill the Phase-3 section of AUDIT-REPORT.md (append/extend the Plan-01 skeleton — do NOT overwrite prior Phase 1/2/tracer content).

    (a) **Results table:** complete the 8-column table (`Target | N | M | Features | band_frac | Mean time | Artifact | Confidence`) with every measured cell: three targets × 4 grid cells × {band_frac=0.0 unbanded, band_frac=0.1 banded} = 24 rows. Transcribe each mean time from its run1 artifact; each row links its `p3_*` artifact. Tag every row `linalg,parallel` + toolchain (state the toolchain once in a section header if identical across rows).

    (b) **Two-run variance (SC3):** for each cell compute the run1-vs-run2 percent difference. Cells within ±5% → confidence OK; cells 5–10% → note the variance; cells >10% → mark `LOW CONFIDENCE` in that row's Confidence column. Record the variance computation method (e.g. `|r2-r1|/r1`) in a one-line note.

    (c) **Banded-vs-unbanded analysis (SC2, D-03/D-04):** for each of the three targets, state the observed reduction factor (unbanded mean ÷ banded mean) at a representative cell (use N=500×M=200 where the corridor bites hardest). Compare each observed factor to the ~7× expected (post-overhead) and the ~10× theoretical (m/band = 200/20 at M=200, band_frac=0.1 → band_radius(0.1,200)=20). Note that at M=50 the band is ceil(0.1·50)=5 → theoretical 10× still but smaller absolute DP so overhead dominates more; caveat alignment-quality implications lightly (fix framing is Phase-9 territory).

    (d) **D-05 confirmation:** finalize the note (upgraded from the Plan-01 stub) that `karcher_mean()` → `karcher_mean_impl(.., 0.0)` at karcher.rs:300 → band_frac=0.0 → `band_radius` returns `None` → full unbanded DP by default = Anti-Pattern 2. This is the measured root cause: the unbanded karcher rows ARE the default-path cost.

    Do NOT change any Phase 1/2 numbers. Every quantitative claim links to a bench artifact.
  </action>
  <verify>
    <automated>grep -c "p3_" .planning/research/AUDIT-REPORT.md | awk '$1>=6{print "LINKS_OK"; exit} {print "TOO_FEW_LINKS"; exit 1}' && grep -Eiq "banded|reduction|~7|7×|7x" .planning/research/AUDIT-REPORT.md && grep -Eiq "variance|LOW CONFIDENCE|±5" .planning/research/AUDIT-REPORT.md && grep -q "karcher.rs:300" .planning/research/AUDIT-REPORT.md && echo REPORT_FILLED</automated>
  </verify>
  <acceptance_criteria>
    - Results table has all three targets × grid × {unbanded, banded} rows, each linking a `p3_*` artifact (grep finds ≥6 `p3_` references; ideally 24 rows)
    - Banded-vs-unbanded reduction factor stated per target and compared to ~7× expected / ~10× theoretical (grep finds the banded/reduction discussion)
    - Two-run variance computed per cell; >10% cells marked LOW CONFIDENCE; variance method noted
    - D-05 note finalized citing `karcher.rs:300` + Anti-Pattern 2
    - Prior AUDIT-REPORT content unchanged (append-only)
  </acceptance_criteria>
  <done>AUDIT-REPORT.md Phase-3 section carries the full results table (3 targets × grid × banded/unbanded, artifact-linked), the per-target observed banded reduction vs ~7×/~10×, per-cell two-run variance with LOW-CONFIDENCE flags, and the finalized D-05 Anti-Pattern-2 root-cause note.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Write the GSD-ready elastic backlog entries and the phase COVERAGE gate file</name>
  <read_first>
    - .planning/research/AUDIT-REPORT.md (the just-filled Phase-3 table + banded analysis — the current-cost numbers + anti-pattern/complexity rows to cite)
    - .planning/phases/03-elastic-alignment-hot-path/03-CONTEXT.md (D-07 SC4 fields + candidate-fix examples; deferred items to keep OUT)
    - .planning/ROADMAP.md (Phase 9 backlog-ranking context these must be GSD-ready for)
  </read_first>
  <files>
    .planning/research/AUDIT-REPORT.md
    .planning/phases/03-elastic-alignment-hot-path/03-COVERAGE.md
  </files>
  <action>
    (a) **Finalize backlog entries** in the AUDIT-REPORT Phase-3 `#### Draft Backlog (elastic alignment)` subsection (replace the Plan-01 `[STUB]`). Write the GSD-ready elastic-alignment perf backlog entries, each with the D-07 SC4 fields:
    - **Function** (the target),
    - **Current cost** — the Phase-3 MEASURED numbers (cite the unbanded default-path times from the table),
    - **Root cause** — cite the AUDIT-REPORT Anti-Pattern 2 (banding opt-in) note AND the elastic complexity row (O(max_iter·N·m²) unbanded / O(max_iter·N·m·band) banded); do NOT re-derive the complexity, cite Phase 2's static analysis,
    - **Candidate fix (one line)** — e.g. "default `karcher_mean` to a banded path", "expose `band_frac` on the high-level distance-matrix API", quantified by this phase's observed reduction.

    Minimum entries: the top item (default-to-banded / expose band_frac, justified by the measured ~N× reduction), plus a distance-matrix entry. Keep entries GSD-ready for Phase-9 ranking — candidate requirements/phases, NOT fully-specified fixes. Do NOT include deferred items (implementing the fix, dhat profiling, FPCA-loop parallelization, thread sweep, final cross-module ranking) — those are downstream phases.

    (b) **Write `03-COVERAGE.md`** (API-coverage gate — required for seal-time). Content exactly: `No external API integration: benchmark-only phase measuring internal fdars-core elastic-alignment functions.`
  </action>
  <verify>
    <automated>grep -Eiq "candidate fix|expose .band_frac|default.*band" .planning/research/AUDIT-REPORT.md && grep -Eiq "root cause|Anti-Pattern 2" .planning/research/AUDIT-REPORT.md && test -f .planning/phases/03-elastic-alignment-hot-path/03-COVERAGE.md && grep -q "benchmark-only phase" .planning/phases/03-elastic-alignment-hot-path/03-COVERAGE.md && echo BACKLOG_AND_COVERAGE_OK</automated>
  </verify>
  <acceptance_criteria>
    - Backlog subsection has ≥2 entries, each carrying Function / Current-cost (Phase-3 measured, artifact-backed) / Root-cause (cites Anti-Pattern 2 + elastic complexity row) / one-line Candidate-fix (D-07)
    - No deferred item (dhat, FPCA-loop parallelization, thread sweep, implementing the fix, cross-module ranking) appears as a Phase-3 backlog entry
    - `.planning/phases/03-elastic-alignment-hot-path/03-COVERAGE.md` exists with the exact benchmark-only-phase statement
    - Backlog entries are phrased as candidate requirements/phases (GSD-ready for Phase 9), not fully-specified fixes
  </acceptance_criteria>
  <done>The Phase-3 elastic backlog entries are finalized with all D-07 SC4 fields (measured cost + cited root cause + one-line fix), GSD-ready for Phase 9, and 03-COVERAGE.md records the benchmark-only API-coverage disposition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Benchmark-only phase. All bench inputs are seeded synthetic data built in-process by `generate_curves` (deterministic, no RNG, no file/network input). No untrusted input crosses any boundary; report/backlog edits are local markdown. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | cargo bench dependency graph | low | accept | No new packages installed — criterion is an existing dev-dep; audit-only measurement code. No package-legitimacy gate applicable. |
| T-03-02 | Information Disclosure | bench artifacts + AUDIT-REPORT under .planning/ | low | accept | Artifacts contain only timing numbers + toolchain version of a public OSS crate. No secrets. |

At ASVS L1 this benchmark-only phase has no network/auth/input-handling attack surface. No `high` threats — the threat model is minimal by design.
</threat_model>

<verification>
- `cargo build -p fdars-core --benches --features linalg,parallel` succeeds with all six Phase-3 bench fns registered.
- 12 raw artifacts (`p3_<target>_linalg,parallel_run{1,2}.txt`) exist, each with a criterion timing line + toolchain/feature tag + `/release/` confirmation.
- AUDIT-REPORT Phase-3 table: three targets × grid × {unbanded, banded}, each row artifact-linked; banded-vs-unbanded reduction stated per target vs ~7×/~10×; per-cell two-run variance with LOW-CONFIDENCE flags; D-05 note citing karcher.rs:300 + Anti-Pattern 2.
- Backlog subsection ≥2 D-07-shaped entries; no deferred item present.
- 03-COVERAGE.md exists with the benchmark-only statement.
- Human backstop (flat-scalar must-have): a reviewer confirms the observed ~7× reduction and ±5% variance were derived correctly from the produced numbers (the verifier cannot recompute criterion timings).
</verification>

<success_criteria>
All four ROADMAP Phase-3 success criteria are delivered: (1) criterion results table for the three targets across N∈{100,500}×M∈{50,200}, release+linalg,parallel+black_box, feature/toolchain-tagged; (2) banded-vs-unbanded at band_frac=0.1 for all three targets quantifying the ~7× reduction and confirming karcher's unbanded default (Anti-Pattern 2); (3) reproducible evidence under bench/p3_* with per-finding artifact links and ±5%/>10% variance handling; (4) GSD-ready elastic backlog entries with function/current-cost/root-cause/candidate-fix.
</success_criteria>

<output>
Create `.planning/phases/03-elastic-alignment-hot-path/03-02-SUMMARY.md` when done.
</output>
