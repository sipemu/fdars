---
phase: 52-ecosystem-surveys
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/survey-tidyfun.md
autonomous: true
requirements: [TDY-01]
estimate:
  tokens: 70000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The survey file .planning/research/survey-tidyfun.md exists (TDY-01)."
    - "It contains a capability-first inventory of the tidyfun data-representation & workflow slice plus refund methods pinned pkg@version."
    - "refund rows are restricted to methods NOT already captured in v0.18.0 (R-AUDIT-REPORT.md / R-BACKLOG.md), with an explicit note when a refund method is excluded because v0.18.0 already covered it."
    - "Every row carries an fdars status column valued present, partial, or absent."
    - "Every absent/partial row carries an explicit 'searched fdars for:' note recording the grep terms used against fdars-core/src/ and the capability reasoning."
    - "The file ends with a 'Net-New Gap List' table using the six standardized columns, filtered to rows verified absent from shipped fdars AND absent from both BACKLOG.md and R-BACKLOG.md."
    - "The file contains a reverse-parity note recording where fdars leads the tidyfun/refund slice."
  artifacts:
    - .planning/research/survey-tidyfun.md
  key_links:
    - "Standardized gap-table columns (Capability | Reference (pkg@ver) | fdars status | Searched-for | Net-new? | Notes) — the merge contract Phase 53 consumes."
    - "v0.18.0 refund-coverage boundary: R-AUDIT-REPORT.md / R-BACKLOG.md define what is already captured; TDY-01 touches refund only beyond that boundary."
  prohibitions:
    - "No fdars-core/src/ file is modified by this plan (audit-only fence). `git status --porcelain fdars-core/src/` attributable to this phase is empty."
  test_coverage_na: "This is an audit/documentation phase that writes zero code and adds zero tests. Nyquist / Dimension 8 (test coverage) is N/A; verification is document-content grep, not cargo test."
---

<objective>
Survey the R tidyfun/refund slice — the tidyfun data-representation & workflow layer PLUS refund methods NOT already captured in v0.18.0 — capability-first, map fdars present/partial/absent against it, de-duplicate every candidate gap against shipped fdars capabilities plus both prior backlogs, and emit a net-new gap list. Implements TDY-01.

Purpose: Produce the tidyfun/refund slice of the raw material Phase 53 consolidates. The v0.18.0 R-ecosystem audit already covered the core R FDA ecosystem (`fda`/`fda.usc`/`refund`/`fdapace`/`roahd`/`ftsa`/`frechet`); this survey touches refund ONLY where NOT already captured there, plus the tidyfun tidyverse-style representation/workflow slice which v0.18.0 did not survey. The value gate is "genuinely net-new" — every emitted gap must be absent from shipped fdars AND from `BACKLOG.md` (v0.14.0) AND from `R-BACKLOG.md` (v0.18.0).
Output: `.planning/research/survey-tidyfun.md`.

Audit-only fence (MANDATORY): this plan modifies ZERO `fdars-core/src/` files. The only file it creates is its own `.planning/research/survey-tidyfun.md` deliverable. No git tag, no crate change.

Test coverage N/A: this milestone writes zero code and adds zero tests. Nyquist / Dimension 8 does not apply — verification is document-content grep, not `cargo test`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-ecosystem-surveys/52-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<artifacts_this_phase_produces>
- `.planning/research/survey-tidyfun.md` — the tidyfun/refund (R) capability survey (this plan).
- (siblings, other plans in this wave: `survey-matlab.md`, `survey-julia.md`, `survey-pyx.md`)
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Enumerate the tidyfun representation/workflow slice + refund-not-in-v0.18.0 capability surface, version-pinned</name>
  <files>.planning/research/survey-tidyfun.md</files>
  <read_first>
    - .planning/phases/52-ecosystem-surveys/52-CONTEXT.md (TDY scope: tidyfun representation/workflow slice + refund only where not in v0.18.0; capability-first breadth; version-pinning rule; standardized columns)
    - .planning/research/R-AUDIT-REPORT.md AND .planning/research/R-BACKLOG.md (the v0.18.0 coverage boundary — what refund capability is ALREADY captured; anything already here is EXCLUDED from this survey's refund rows; also the table shape to mirror; do NOT overwrite)
    - .planning/PROJECT.md `### Validated` section (fdars shipped-capability baseline)
  </read_first>
  <action>
    Web-research (WebSearch/WebFetch) the tidyfun package (tidyverse-style functional-data representation: the `tf` / `tfd` / `tfb` vectors, tidy workflow verbs, integration with tibbles/ggplot data prep) and pin its latest stable release as of 2026-09 as `pkg@version`; likewise pin `refund@version`. Enumerate the tidyfun representation & workflow capability CATEGORIES capability-first into a single table in `.planning/research/survey-tidyfun.md` using the exact six standardized columns: `Capability | Reference (pkg@ver) | fdars status (present/partial/absent) | Searched-for | Net-new? (vs BACKLOG/R-BACKLOG) | Notes`. For refund, FIRST read R-AUDIT-REPORT.md / R-BACKLOG.md to establish what v0.18.0 already covered, THEN add refund capability rows ONLY for methods NOT already captured there; when you consider a refund method and exclude it because v0.18.0 covered it, record that decision in Notes (e.g. "refund pffr — excluded: captured in v0.18.0 R-AUDIT §Area X"). Populate Capability + Reference (pkg@ver) fully in this task. Open the file with a header naming tidyfun + refund pinned versions + survey date (2026-09) + the audit-only/test-coverage-N-A note + a one-line statement of the v0.18.0 refund-coverage boundary. Exclude plotting/visualization and data/IO (out-of-scope).
  </action>
  <verify>
    <automated>test -f .planning/research/survey-tidyfun.md && grep -Eq 'tidyfun@|tf@|refund@' .planning/research/survey-tidyfun.md && grep -q 'Reference (pkg@ver)' .planning/research/survey-tidyfun.md && grep -Eq 'v0\.18\.0|R-AUDIT|R-BACKLOG' .planning/research/survey-tidyfun.md</automated>
    <fails_when>file absent, no `pkg@version` pins for tidyfun/refund, no standardized column header, or no statement of the v0.18.0 refund-coverage boundary (non-zero exit).</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/research/survey-tidyfun.md` exists with a header naming tidyfun + refund pinned versions + survey date + audit-only/test-coverage-N-A note + the v0.18.0 refund-boundary statement.
    - A single capability inventory table uses the exact six standardized column headers.
    - refund rows are restricted to methods not captured in v0.18.0, with at least one Notes entry documenting a refund exclusion where applicable.
    - No plotting/visualization or data/IO rows (out-of-scope respected).
  </acceptance_criteria>
  <done>The tidyfun workflow slice + refund-not-in-v0.18.0 surface is enumerated capability-first with version pins and the v0.18.0 boundary documented.</done>
</task>

<task type="auto">
  <name>Task 2: Map fdars present/partial/absent with per-row "searched fdars for:" evidence</name>
  <files>.planning/research/survey-tidyfun.md</files>
  <read_first>
    - .planning/PROJECT.md `### Validated` section (shipped-capability baseline)
    - fdars-core/src/ module tree (grep target: matrix.rs, fdata.rs, multi_fdata.rs, irreg_fdata/, basis/, smooth_basis.rs, fof_regression.rs, function_on_scalar.rs, famm.rs, scalar_on_function/, prelude.rs)
  </read_first>
  <action>
    For each capability row from Task 1, determine the fdars status (present / partial / absent) by capability, NOT by API name — noting that tidyfun's value is largely a REPRESENTATION/ergonomics layer (tf-vectors, tidy verbs), so map it against fdars' data-representation surface (FdMatrix, FdCurveSet, IrregFdata, MultiFunData, prelude ergonomics). Grep `fdars-core/src/` for evidence (e.g. multi-domain container → grep multi_fdata.rs; irregular representation → grep irreg_fdata/; refund FoFR/pffr-adjacent methods → grep fof_regression.rs function_on_scalar.rs famm.rs). Cross-check against PROJECT.md Validated. Fill the `fdars status` column. For EVERY absent/partial row, fill `Searched-for` with "searched fdars for: <grep terms> — <capability reasoning>". Mark partial rows and describe the missing slice in Notes. Leave `Net-new?` for Task 3. Modify ONLY `.planning/research/survey-tidyfun.md`.
  </action>
  <verify>
    <automated>grep -Eq 'present|partial|absent' .planning/research/survey-tidyfun.md && grep -q 'searched fdars for:' .planning/research/survey-tidyfun.md && git status --porcelain fdars-core/src/ | grep -q . && echo FENCE_VIOLATION || echo FENCE_OK</automated>
    <fails_when>no present/partial/absent verdicts, no "searched fdars for:" evidence, or the fence check prints FENCE_VIOLATION.</fails_when>
  </verify>
  <acceptance_criteria>
    - Every inventory row has a present/partial/absent verdict.
    - Every absent/partial row has a "searched fdars for: <terms> — <reasoning>" note.
    - tidyfun representation rows are mapped against fdars' data-representation surface (FdMatrix/IrregFdata/MultiFunData/prelude).
    - `git status --porcelain fdars-core/src/` is empty.
  </acceptance_criteria>
  <done>fdars parity is mapped by capability with grep-evidenced notes on every absent/partial row, no source file touched.</done>
</task>

<task type="auto">
  <name>Task 3: De-dup against prior backlogs, emit the Net-New Gap List + reverse-parity note</name>
  <files>.planning/research/survey-tidyfun.md</files>
  <read_first>
    - .planning/research/BACKLOG.md (v0.14.0 de-dup baseline)
    - .planning/research/R-BACKLOG.md (v0.18.0 de-dup baseline — especially important here, since refund overlap is the main de-dup risk)
    - .planning/phases/52-ecosystem-surveys/52-CONTEXT.md (hard de-dup rule + reverse-parity requirement)
  </read_first>
  <action>
    For each absent/partial-with-missing-slice row, check the candidate gap by CAPABILITY against both BACKLOG.md and R-BACKLOG.md — applying extra rigor to refund rows, since v0.18.0 already ranked much of refund. Fill `Net-new?`: `yes` only if verified absent from shipped fdars AND both prior backlogs; else `no — matches <ID/title>`. Append a `## Net-New Gap List` table (same six columns) filtered to `Net-new? = yes` rows only. Append a short `## Reverse-Parity Note` recording where fdars leads the tidyfun/refund slice (re-vet specifically — fdars' explainability, streaming depth, conformal, SPM are candidate moats; tidyfun's strength is representation ergonomics, which may be a reverse gap FOR fdars rather than a lead). Keep one gap per distinct capability/method-family. Modify ONLY `.planning/research/survey-tidyfun.md`.
  </action>
  <verify>
    <automated>grep -q 'Net-New Gap List' .planning/research/survey-tidyfun.md && grep -Eq 'Reverse-Parity|Reverse parity|reverse-parity' .planning/research/survey-tidyfun.md && grep -Eq 'R-BACKLOG|BACKLOG' .planning/research/survey-tidyfun.md</automated>
    <fails_when>the "Net-New Gap List" heading is missing, the reverse-parity note is missing, or no de-dup evidence against BACKLOG/R-BACKLOG (non-zero exit).</fails_when>
  </verify>
  <acceptance_criteria>
    - Every absent/partial candidate row has a `Net-new?` value (`yes` or `no — matches <prior item>`).
    - A `## Net-New Gap List` table (six columns) contains only net-new-yes rows.
    - A `## Reverse-Parity Note` records where fdars leads the tidyfun/refund slice.
    - De-dup reasoning references BACKLOG.md and/or R-BACKLOG.md (refund rows de-dup'd against v0.18.0).
    - `git status --porcelain fdars-core/src/` is empty.
  </acceptance_criteria>
  <done>The tidyfun/refund survey emits a de-duplicated Net-New Gap List + reverse-parity note, ready for Phase 53 merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web sources → survey file | Version pins and capability claims come from external docs/registries; risk is inaccuracy, not code execution (audit-only, no installs). |
| v0.18.0 coverage boundary | refund rows must respect what R-AUDIT/R-BACKLOG already captured; risk is double-counting a v0.18.0 gap as net-new. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52.03-01 | Information Disclosure | version pins / capability claims | low | mitigate | Pin `pkg@version` from CRAN / package docs and record the survey date. |
| T-52.03-02 | Repudiation | refund double-counting vs v0.18.0 | medium | mitigate | Task 1 reads R-AUDIT/R-BACKLOG to establish the boundary and records refund exclusions in Notes; Task 3 de-dups refund rows against R-BACKLOG with extra rigor. |
| T-52.03-03 | Tampering | fdars-core/src/ (audit fence) | high | mitigate | Audit-only fence: verified `git status --porcelain fdars-core/src/` empty; no installs, so package-legitimacy gate N/A. |
</threat_model>

<verification>
- `test -f .planning/research/survey-tidyfun.md` — deliverable exists.
- `grep -q 'Net-New Gap List' .planning/research/survey-tidyfun.md` — net-new gap list present.
- `grep -q 'searched fdars for:' .planning/research/survey-tidyfun.md` — per-row evidence present.
- `grep -Eq 'v0\.18\.0|R-AUDIT|R-BACKLOG' .planning/research/survey-tidyfun.md` — v0.18.0 refund boundary respected.
- `git status --porcelain fdars-core/src/` empty — audit-only fence held.
- Test coverage N/A: no `cargo test` expected; this milestone adds zero code.
</verification>

<success_criteria>
- `.planning/research/survey-tidyfun.md` exists with a version-pinned capability-first inventory of the tidyfun representation/workflow slice + refund-not-in-v0.18.0 methods.
- Every row has an fdars verdict; every absent/partial row has a "searched fdars for:" note.
- refund rows respect the v0.18.0 coverage boundary.
- The file ends with a Net-New Gap List (net-new-only, six columns) + a reverse-parity note.
- De-dup against BACKLOG.md and R-BACKLOG.md is evidenced.
- Zero `fdars-core/src/` edits.
</success_criteria>

<output>
Create `.planning/phases/52-ecosystem-surveys/52-03-SUMMARY.md` when done.
</output>
