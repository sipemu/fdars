---
phase: 52-ecosystem-surveys
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/survey-pyx.md
autonomous: true
requirements: [PYX-01]
estimate:
  tokens: 70000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The survey file .planning/research/survey-pyx.md exists (PYX-01)."
    - "It contains a capability-first inventory of Python-beyond-scikit-fda functional components (FDApy / tslearn / sktime + other Python FDA/ML libs) pinned pkg@version."
    - "scikit-fda is explicitly excluded (covered by v0.14.0), with a stated exclusion note."
    - "Every row carries an fdars status column valued present, partial, or absent."
    - "Every absent/partial row carries an explicit 'searched fdars for:' note recording the grep terms used against fdars-core/src/ and the capability reasoning."
    - "The file ends with a 'Net-New Gap List' table using the six standardized columns, filtered to rows verified absent from shipped fdars AND absent from both BACKLOG.md and R-BACKLOG.md."
    - "The file contains a reverse-parity note recording where fdars leads the Python-beyond-scikit-fda ecosystem."
  artifacts:
    - .planning/research/survey-pyx.md
  key_links:
    - "Standardized gap-table columns (Capability | Reference (pkg@ver) | fdars status | Searched-for | Net-new? | Notes) — the merge contract Phase 53 consumes."
    - "scikit-fda exclusion boundary: BACKLOG.md (v0.14.0) already covers scikit-fda; PYX-01 surveys Python FDA/ML libs BEYOND it."
  prohibitions:
    - "No fdars-core/src/ file is modified by this plan (audit-only fence). `git status --porcelain fdars-core/src/` attributable to this phase is empty."
  test_coverage_na: "This is an audit/documentation phase that writes zero code and adds zero tests. Nyquist / Dimension 8 (test coverage) is N/A; verification is document-content grep, not cargo test."
---

<objective>
Survey the Python-beyond-scikit-fda ecosystem — FDApy / tslearn / sktime functional components plus other Python FDA/ML libraries — capability-first, map fdars present/partial/absent against it, de-duplicate every candidate gap against shipped fdars capabilities plus both prior backlogs, and emit a net-new gap list. scikit-fda is EXCLUDED (already covered by v0.14.0's `BACKLOG.md`). Implements PYX-01.

Purpose: Produce the Python-beyond-scikit-fda slice of the raw material Phase 53 consolidates. The value gate is "genuinely net-new" — every emitted gap must be absent from shipped fdars AND from `BACKLOG.md` (v0.14.0) AND from `R-BACKLOG.md` (v0.18.0).
Output: `.planning/research/survey-pyx.md`.

Audit-only fence (MANDATORY): this plan modifies ZERO `fdars-core/src/` files. The only file it creates is its own `.planning/research/survey-pyx.md` deliverable. No git tag, no crate change.

Test coverage N/A: this milestone writes zero code and adds zero tests. Nyquist / Dimension 8 does not apply — verification is document-content grep, not `cargo test`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-ecosystem-surveys/52-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<artifacts_this_phase_produces>
- `.planning/research/survey-pyx.md` — the Python-beyond-scikit-fda capability survey (this plan).
- (siblings, other plans in this wave: `survey-matlab.md`, `survey-julia.md`, `survey-tidyfun.md`)
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Enumerate the Python-beyond-scikit-fda capability surface (FDApy / tslearn / sktime + others), version-pinned</name>
  <files>.planning/research/survey-pyx.md</files>
  <read_first>
    - .planning/phases/52-ecosystem-surveys/52-CONTEXT.md (PYX scope: FDApy / tslearn / sktime functional components + other Python FDA/ML libs; scikit-fda EXCLUDED; capability-first breadth; version-pinning rule; standardized columns)
    - .planning/research/AUDIT-REPORT.md / .planning/research/BACKLOG.md (the scikit-fda v0.14.0 coverage — used to enforce the scikit-fda exclusion + de-dup; do NOT overwrite)
    - .planning/PROJECT.md `### Validated` section (fdars shipped-capability baseline)
  </read_first>
  <action>
    Web-research (WebSearch/WebFetch) the Python FDA/ML libraries BEYOND scikit-fda and pin the latest stable release of each as of 2026-09 as `pkg@version`: FDApy (univariate/multivariate functional data, FCP-TPA, FPCA), tslearn (time-series ML with FDA-adjacent capability: DTW/soft-DTW, shapelets, time-series kmeans/kshape, kernels), sktime (functional/time-series transformers, forecasting, classification adjacent to FDA), plus other actively-maintained Python FDA/ML libs at Claude's discretion (e.g. functional-data or curve-modeling packages). EXCLUDE scikit-fda entirely (covered by v0.14.0). Enumerate every MAJOR capability CATEGORY (not every function; collapse fit/predict/transform) into a single capability-first table in `.planning/research/survey-pyx.md` using the exact six standardized columns: `Capability | Reference (pkg@ver) | fdars status (present/partial/absent) | Searched-for | Net-new? (vs BACKLOG/R-BACKLOG) | Notes`. Populate Capability + Reference (pkg@ver) fully in this task. Open the file with a header naming the surveyed packages + pinned versions + survey date (2026-09) + the audit-only/test-coverage-N-A note + an explicit "scikit-fda excluded (covered by v0.14.0)" statement. Group by area mirroring the AUDIT-REPORT.md ordering. Exclude plotting/visualization and data/IO (out-of-scope).
  </action>
  <verify>
    <automated>test -f .planning/research/survey-pyx.md && grep -Eq 'FDApy@|tslearn@|sktime@|@[0-9]' .planning/research/survey-pyx.md && grep -q 'Reference (pkg@ver)' .planning/research/survey-pyx.md && grep -Eiq 'scikit-fda excluded|excludes scikit-fda|scikit-fda.*v0\.14\.0' .planning/research/survey-pyx.md</automated>
    <fails_when>file absent, no `pkg@version` pins, no standardized column header, or no scikit-fda exclusion statement (non-zero exit).</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/research/survey-pyx.md` exists with a header naming surveyed packages + pinned versions + survey date + the audit-only/test-coverage-N-A note + a "scikit-fda excluded (v0.14.0)" statement.
    - A single capability inventory table uses the exact six standardized column headers.
    - FDApy, tslearn, and sktime are each represented with a `pkg@version` pin.
    - No scikit-fda rows; no plotting/visualization or data/IO rows (out-of-scope respected).
  </acceptance_criteria>
  <done>The Python-beyond-scikit-fda capability surface is enumerated capability-first with version pins; scikit-fda is excluded.</done>
</task>

<task type="auto">
  <name>Task 2: Map fdars present/partial/absent with per-row "searched fdars for:" evidence</name>
  <files>.planning/research/survey-pyx.md</files>
  <read_first>
    - .planning/PROJECT.md `### Validated` section (shipped-capability baseline)
    - fdars-core/src/ module tree (grep target: regression.rs, fpca_variants.rs, multi_fdata.rs, alignment/ (DTW-adjacent elastic), classification/, clustering.rs, clustering_advanced.rs, distance.rs, metric/, fts/, seasonal/, streaming_depth/)
  </read_first>
  <action>
    For each capability row from Task 1, determine the fdars status (present / partial / absent) by capability, NOT by API name — being careful with tslearn/sktime time-series capabilities that are FDA-adjacent but not functional-data (map only the functional-relevant capability, note the rest as out-of-scope in Notes). Grep `fdars-core/src/` for evidence (e.g. multivariate functional data / FCP-TPA → grep multi_fdata.rs fpca_variants.rs; DTW/elastic alignment → grep alignment/ elastic.rs; time-series kmeans/kshape → grep clustering.rs clustering_advanced.rs distance.rs metric/; forecasting → grep fts/forecast.rs). Cross-check against PROJECT.md Validated. Fill the `fdars status` column. For EVERY absent/partial row, fill `Searched-for` with "searched fdars for: <grep terms> — <capability reasoning>". Mark partial rows and describe the missing slice in Notes. Leave `Net-new?` for Task 3. Modify ONLY `.planning/research/survey-pyx.md`.
  </action>
  <verify>
    <automated>grep -Eq 'present|partial|absent' .planning/research/survey-pyx.md && grep -q 'searched fdars for:' .planning/research/survey-pyx.md && git status --porcelain fdars-core/src/ | grep -q . && echo FENCE_VIOLATION || echo FENCE_OK</automated>
    <fails_when>no present/partial/absent verdicts, no "searched fdars for:" evidence, or the fence check prints FENCE_VIOLATION.</fails_when>
  </verify>
  <acceptance_criteria>
    - Every inventory row has a present/partial/absent verdict.
    - Every absent/partial row has a "searched fdars for: <terms> — <reasoning>" note.
    - FDA-adjacent-but-not-functional time-series capabilities are noted as out-of-scope in Notes rather than counted as gaps.
    - `git status --porcelain fdars-core/src/` is empty.
  </acceptance_criteria>
  <done>fdars parity is mapped by capability with grep-evidenced notes on every absent/partial row, no source file touched.</done>
</task>

<task type="auto">
  <name>Task 3: De-dup against prior backlogs, emit the Net-New Gap List + reverse-parity note</name>
  <files>.planning/research/survey-pyx.md</files>
  <read_first>
    - .planning/research/BACKLOG.md (v0.14.0 scikit-fda de-dup baseline — critical here since PYX overlaps scikit-fda's neighborhood)
    - .planning/research/R-BACKLOG.md (v0.18.0 de-dup baseline)
    - .planning/phases/52-ecosystem-surveys/52-CONTEXT.md (hard de-dup rule + reverse-parity requirement)
  </read_first>
  <action>
    For each absent/partial-with-missing-slice row, check the candidate gap by CAPABILITY against both BACKLOG.md and R-BACKLOG.md. Fill `Net-new?`: `yes` only if verified absent from shipped fdars AND both prior backlogs; else `no — matches <ID/title>`. Append a `## Net-New Gap List` table (same six columns) filtered to `Net-new? = yes` rows only. Append a short `## Reverse-Parity Note` recording where fdars leads the Python-beyond-scikit-fda ecosystem (re-vet specifically — explainability, streaming depth, SPM, conformal, functional inference are candidate moats vs FDApy/tslearn/sktime). Keep one gap per distinct capability/method-family. Modify ONLY `.planning/research/survey-pyx.md`.
  </action>
  <verify>
    <automated>grep -q 'Net-New Gap List' .planning/research/survey-pyx.md && grep -Eq 'Reverse-Parity|Reverse parity|reverse-parity' .planning/research/survey-pyx.md && grep -Eq 'R-BACKLOG|BACKLOG' .planning/research/survey-pyx.md</automated>
    <fails_when>the "Net-New Gap List" heading is missing, the reverse-parity note is missing, or no de-dup evidence against BACKLOG/R-BACKLOG (non-zero exit).</fails_when>
  </verify>
  <acceptance_criteria>
    - Every absent/partial candidate row has a `Net-new?` value (`yes` or `no — matches <prior item>`).
    - A `## Net-New Gap List` table (six columns) contains only net-new-yes rows.
    - A `## Reverse-Parity Note` records where fdars leads the Python-beyond-scikit-fda ecosystem.
    - De-dup reasoning references BACKLOG.md and/or R-BACKLOG.md.
    - `git status --porcelain fdars-core/src/` is empty.
  </acceptance_criteria>
  <done>The Python-beyond-scikit-fda survey emits a de-duplicated Net-New Gap List + reverse-parity note, ready for Phase 53 merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web sources → survey file | Version pins and capability claims come from external docs/registries; risk is inaccuracy, not code execution (audit-only, no installs). |
| scikit-fda exclusion boundary | PYX rows must exclude scikit-fda (covered by v0.14.0); risk is re-listing a v0.14.0 gap as net-new. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52.04-01 | Information Disclosure | version pins / capability claims | low | mitigate | Pin `pkg@version` from PyPI / package docs and record the survey date. |
| T-52.04-02 | Repudiation | scikit-fda double-counting vs v0.14.0 | medium | mitigate | Task 1 excludes scikit-fda explicitly; Task 3 de-dups every candidate against BACKLOG.md (v0.14.0). |
| T-52.04-03 | Tampering | fdars-core/src/ (audit fence) | high | mitigate | Audit-only fence: verified `git status --porcelain fdars-core/src/` empty; no installs, so package-legitimacy gate N/A. |
</threat_model>

<verification>
- `test -f .planning/research/survey-pyx.md` — deliverable exists.
- `grep -q 'Net-New Gap List' .planning/research/survey-pyx.md` — net-new gap list present.
- `grep -q 'searched fdars for:' .planning/research/survey-pyx.md` — per-row evidence present.
- `grep -Eiq 'scikit-fda excluded|excludes scikit-fda' .planning/research/survey-pyx.md` — scikit-fda exclusion respected.
- `git status --porcelain fdars-core/src/` empty — audit-only fence held.
- Test coverage N/A: no `cargo test` expected; this milestone adds zero code.
</verification>

<success_criteria>
- `.planning/research/survey-pyx.md` exists with a version-pinned capability-first inventory of FDApy / tslearn / sktime + other Python FDA/ML libs, scikit-fda excluded.
- Every row has an fdars verdict; every absent/partial row has a "searched fdars for:" note.
- The file ends with a Net-New Gap List (net-new-only, six columns) + a reverse-parity note.
- De-dup against BACKLOG.md and R-BACKLOG.md is evidenced.
- Zero `fdars-core/src/` edits.
</success_criteria>

<output>
Create `.planning/phases/52-ecosystem-surveys/52-04-SUMMARY.md` when done.
</output>
