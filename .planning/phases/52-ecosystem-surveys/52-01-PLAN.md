---
phase: 52-ecosystem-surveys
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/survey-matlab.md
autonomous: true
requirements: [MAT-01]
estimate:
  tokens: 70000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The survey file .planning/research/survey-matlab.md exists (MAT-01)."
    - "It contains a capability-first inventory table with every reference row pinned pkg@version (Ramsay fda MATLAB toolbox + PACE MATLAB)."
    - "Every row carries an fdars status column valued present, partial, or absent."
    - "Every absent/partial row carries an explicit 'searched fdars for:' note recording the grep terms used against fdars-core/src/ and the capability reasoning."
    - "The file ends with a 'Net-New Gap List' table using the six standardized columns, filtered to rows verified absent from shipped fdars AND absent from both BACKLOG.md and R-BACKLOG.md."
    - "The file contains a reverse-parity note recording where fdars leads the MATLAB FDA ecosystem."
    - "De-dup evidence against BACKLOG.md and R-BACKLOG.md is present in the Net-new? column reasoning."
  artifacts:
    - .planning/research/survey-matlab.md
  key_links:
    - "Standardized gap-table columns (Capability | Reference (pkg@ver) | fdars status | Searched-for | Net-new? | Notes) — the merge contract Phase 53 (RPT-01/02/03) consumes."
  prohibitions:
    - "No fdars-core/src/ file is modified by this plan (audit-only fence). `git status --porcelain fdars-core/src/` attributable to this phase is empty."
  test_coverage_na: "This is an audit/documentation phase that writes zero code and adds zero tests. Nyquist / Dimension 8 (test coverage) is N/A; verification is document-content grep, not cargo test."
---

<objective>
Survey the MATLAB FDA ecosystem — the Ramsay `fda` MATLAB toolbox AND PACE (MATLAB) — capability-first, map fdars present/partial/absent against it, de-duplicate every candidate gap against shipped fdars capabilities plus both prior backlogs, and emit a net-new gap list. Implements MAT-01.

Purpose: Produce the MATLAB slice of the raw material Phase 53 consolidates into `GAP-AUDIT-REPORT.md` / `GAP-BACKLOG.md`. The value gate is "genuinely net-new" — every emitted gap must be absent from shipped fdars AND from `BACKLOG.md` (v0.14.0) AND from `R-BACKLOG.md` (v0.18.0).
Output: `.planning/research/survey-matlab.md`.

Audit-only fence (MANDATORY): this plan modifies ZERO `fdars-core/src/` files. The only file it creates is its own `.planning/research/survey-matlab.md` deliverable. No git tag, no crate change.

Test coverage N/A: this milestone writes zero code and adds zero tests. Nyquist / Dimension 8 does not apply — verification is document-content grep, not `cargo test`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-ecosystem-surveys/52-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<artifacts_this_phase_produces>
- `.planning/research/survey-matlab.md` — the MATLAB FDA capability survey (this plan).
- (siblings, other plans in this wave: `survey-julia.md`, `survey-tidyfun.md`, `survey-pyx.md`)
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Enumerate the MATLAB FDA capability surface (Ramsay fda toolbox + PACE), version-pinned</name>
  <files>.planning/research/survey-matlab.md</files>
  <read_first>
    - .planning/phases/52-ecosystem-surveys/52-CONTEXT.md (survey depth, capability-first breadth, version-pinning rule, standardized columns — this is the design contract)
    - .planning/research/AUDIT-REPORT.md and .planning/research/R-AUDIT-REPORT.md (table shape + capability-first section ordering to mirror; do NOT overwrite these)
    - .planning/PROJECT.md `### Validated` section (the authoritative fdars shipped-capability baseline)
  </read_first>
  <action>
    Web-research (WebSearch/WebFetch) the two MATLAB reference implementations and pin the latest stable release of each as of 2026-09 as `pkg@version`: (a) the Ramsay `fda` MATLAB toolbox (the canonical Functional Data Analysis reference implementation — smoothing/basis systems, registration, functional regression, PCA/CCA, PDA, etc.) and (b) PACE (MATLAB, Principal Analysis by Conditional Expectation — sparse/irregular FPCA, FVE, functional regression, derivatives). Enumerate every MAJOR capability CATEGORY (not every function; collapse fit/predict/transform variants into one capability row) into a single capability-first inventory table in `.planning/research/survey-matlab.md`. Use the six standardized columns exactly: `Capability | Reference (pkg@ver) | fdars status (present/partial/absent) | Searched-for | Net-new? (vs BACKLOG/R-BACKLOG) | Notes`. In this task, populate Capability + Reference (pkg@ver) columns fully; the remaining columns are filled by Tasks 2 and 3. Open the file with a short header naming the two references, their pinned versions, the survey date (2026-09), and a one-line statement that this is an audit-only document (zero fdars-core/src/ edits) and that test-coverage assertions are N/A for this milestone. Group capabilities by area (e.g. representation/smoothing, registration, functional regression, FPCA/dimension reduction, sparse/irregular, inference, misc) mirroring the section ordering in R-AUDIT-REPORT.md. Do NOT include plotting/visualization or data/IO capabilities (out-of-scope per REQUIREMENTS.md).
  </action>
  <verify>
    <automated>test -f .planning/research/survey-matlab.md && grep -Eq 'fda@|PACE@|pace@' .planning/research/survey-matlab.md && grep -q 'Reference (pkg@ver)' .planning/research/survey-matlab.md</automated>
    <fails_when>file absent, no `pkg@version` pin for the fda toolbox or PACE, or the standardized column header row is missing (non-zero exit).</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/research/survey-matlab.md` exists with a header naming both references + pinned versions + survey date + the audit-only/test-coverage-N-A note.
    - A single capability inventory table uses the exact six standardized column headers.
    - Both the Ramsay `fda` MATLAB toolbox and PACE (MATLAB) are represented, each with a `pkg@version` pin.
    - No plotting/visualization or data/IO capability rows (out-of-scope respected).
  </acceptance_criteria>
  <done>The MATLAB capability surface is enumerated capability-first with version pins in the standardized table; downstream tasks can fill parity + de-dup columns.</done>
</task>

<task type="auto">
  <name>Task 2: Map fdars present/partial/absent with per-row "searched fdars for:" evidence</name>
  <files>.planning/research/survey-matlab.md</files>
  <read_first>
    - .planning/PROJECT.md `### Validated` section (40+ shipped-capability entries — the present/absent baseline)
    - fdars-core/src/ module tree (grep target for capability evidence: alignment/, classification/, depth/, seasonal/, spm/, explain/, regression.rs, fpca_variants.rs, pace_fpca.rs, irreg_fdata/, inference/, basis/, smooth_basis.rs, fem_smoothing.rs, etc.)
  </read_first>
  <action>
    For each capability row from Task 1, determine the fdars status (present / partial / absent) by capability, NOT by API name. For each row, grep `fdars-core/src/` for capability evidence (e.g. registration → grep alignment/ landmark.rs shift.rs; PACE sparse FPCA → grep pace_fpca.rs irreg_fdata/; PDA → grep pda.rs; monotone/positive smoothing → grep smooth_basis.rs fem_smoothing.rs) and cross-check against the PROJECT.md Validated list. Fill the `fdars status` column. For EVERY absent or partial row, fill the `Searched-for` column with an explicit note in the form "searched fdars for: <grep terms> — <capability reasoning>" recording the actual grep terms used and why the match (or partial-match) holds. Where fdars partially covers a capability, mark it `partial` and describe the specific MISSING slice in the Notes column (that missing slice is the candidate gap Task 3 evaluates). Do NOT yet decide net-new status — leave the `Net-new?` column for Task 3. Modify ONLY `.planning/research/survey-matlab.md`; touch no `fdars-core/src/` file.
  </action>
  <verify>
    <automated>grep -Eq 'present|partial|absent' .planning/research/survey-matlab.md && grep -q 'searched fdars for:' .planning/research/survey-matlab.md && git status --porcelain fdars-core/src/ | grep -q . && echo FENCE_VIOLATION || echo FENCE_OK</automated>
    <fails_when>no present/partial/absent verdicts present, no "searched fdars for:" evidence note present, or the fence check prints FENCE_VIOLATION (an fdars-core/src/ file was modified).</fails_when>
  </verify>
  <acceptance_criteria>
    - Every inventory row has a present/partial/absent verdict in the `fdars status` column.
    - Every absent/partial row has a "searched fdars for: <terms> — <reasoning>" note in the `Searched-for` column.
    - Partial rows describe the specific missing slice in Notes.
    - `git status --porcelain fdars-core/src/` is empty (audit-only fence holds).
  </acceptance_criteria>
  <done>fdars parity is mapped by capability with grep-evidenced "searched-for" notes on every absent/partial row, and no source file was touched.</done>
</task>

<task type="auto">
  <name>Task 3: De-dup against prior backlogs, emit the Net-New Gap List + reverse-parity note</name>
  <files>.planning/research/survey-matlab.md</files>
  <read_first>
    - .planning/research/BACKLOG.md (v0.14.0 scikit-fda backlog — de-dup baseline; read the Ranked Backlog table + item titles)
    - .planning/research/R-BACKLOG.md (v0.18.0 R-ecosystem backlog — de-dup baseline; read the ranked items)
    - .planning/phases/52-ecosystem-surveys/52-CONTEXT.md (hard de-dup rule + reverse-parity requirement)
  </read_first>
  <action>
    For each absent or partial-with-missing-slice row, check the candidate gap by CAPABILITY (not API name) against both BACKLOG.md (v0.14.0) and R-BACKLOG.md (v0.18.0). Fill the `Net-new?` column: `yes` only if the capability is verified absent from shipped fdars AND absent from BOTH prior backlogs; otherwise `no — matches <BACKLOG/R-BACKLOG ID or title>` recording which prior-backlog item covers it. Then append a `## Net-New Gap List` section: a table using the same six standardized columns, filtered to ONLY the `Net-new? = yes` rows, so Phase 53 can merge it mechanically. Finally append a short `## Reverse-Parity Note` recording capabilities where fdars LEADS the MATLAB ecosystem (e.g. model explainability for functional models, streaming depth, conformal prediction — cross-check the reverse-parity precedent in R-AUDIT-REPORT.md but re-vet against MATLAB specifically, do not copy uncritically). Keep one gap per distinct capability/method-family (grouped, not per-function). Modify ONLY `.planning/research/survey-matlab.md`.
  </action>
  <verify>
    <automated>grep -q 'Net-New Gap List' .planning/research/survey-matlab.md && grep -Eq 'Reverse-Parity|Reverse parity|reverse-parity' .planning/research/survey-matlab.md && grep -Eq 'R-BACKLOG|BACKLOG' .planning/research/survey-matlab.md</automated>
    <fails_when>the "Net-New Gap List" section heading is missing, the reverse-parity note is missing, or there is no evidence of de-dup against BACKLOG/R-BACKLOG (non-zero exit).</fails_when>
  </verify>
  <acceptance_criteria>
    - Every absent/partial candidate row has a `Net-new?` value that is either `yes` or `no — matches <prior item>`.
    - A `## Net-New Gap List` table (six standardized columns) contains only net-new-yes rows.
    - A `## Reverse-Parity Note` records where fdars leads the MATLAB ecosystem.
    - De-dup reasoning references BACKLOG.md and/or R-BACKLOG.md.
    - `git status --porcelain fdars-core/src/` is empty.
  </acceptance_criteria>
  <done>The MATLAB survey emits a de-duplicated Net-New Gap List (net-new-only, standardized columns) plus a reverse-parity note, ready for Phase 53 merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web sources → survey file | Version pins and capability claims come from external docs/registries; risk is inaccuracy, not code execution (audit-only, no installs). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52.01-01 | Information Disclosure | version pins / capability claims | low | mitigate | Pin `pkg@version` from primary sources (official docs / package pages) and record the survey date so claims are auditable in Phase 53. |
| T-52.01-02 | Tampering | fdars-core/src/ (audit fence) | high | mitigate | Audit-only fence: Task verifies `git status --porcelain fdars-core/src/` is empty; no package installs occur, so the package-legitimacy gate (T-...-SC) is N/A this phase. |
</threat_model>

<verification>
- `test -f .planning/research/survey-matlab.md` — deliverable exists.
- `grep -q 'Net-New Gap List' .planning/research/survey-matlab.md` — net-new gap list present.
- `grep -q 'searched fdars for:' .planning/research/survey-matlab.md` — per-row evidence present.
- `grep -Eq 'fda@|PACE@|pace@' .planning/research/survey-matlab.md` — version pins present for both references.
- `git status --porcelain fdars-core/src/` empty — audit-only fence held.
- Test coverage N/A: no `cargo test` expected; this milestone adds zero code.
</verification>

<success_criteria>
- `.planning/research/survey-matlab.md` exists with a version-pinned capability-first inventory covering both the Ramsay `fda` MATLAB toolbox and PACE (MATLAB).
- Every row has an fdars present/partial/absent verdict; every absent/partial row has a "searched fdars for:" note.
- The file ends with a Net-New Gap List (net-new-only, standardized six columns) + a reverse-parity note.
- De-dup against BACKLOG.md and R-BACKLOG.md is evidenced.
- Zero `fdars-core/src/` edits.
</success_criteria>

<output>
Create `.planning/phases/52-ecosystem-surveys/52-01-SUMMARY.md` when done.
</output>
