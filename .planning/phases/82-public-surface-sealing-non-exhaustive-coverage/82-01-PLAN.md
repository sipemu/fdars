---
phase: 82-public-surface-sealing-non-exhaustive-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/helpers.rs
  - fdars-core/src/smoothing.rs
autonomous: true
requirements: [API-02]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "sort_nan_safe is pub(crate), not pub — invisible from outside the crate (API-02, AUD-07)"
    - "solve_gaussian_pub is pub(crate), not pub — invisible from outside the crate (API-02, AUD-08)"
    - "All in-crate callers of both sealed functions still compile (pub(crate) keeps them visible)"
    - "No numeric or behavioral change — visibility-only edit"
  artifacts:
    - fdars-core/src/helpers.rs
    - fdars-core/src/smoothing.rs
  key_links:
    - "concurrent_regression.rs and scalar_on_function/cv.rs call solve_gaussian_pub — both must still resolve"
---

<objective>
Seal the two accidentally-`pub` internal helpers identified and approved in the Phase 81 audit inventory (API-02): `sort_nan_safe` and `solve_gaussian_pub`. Change `pub` → `pub(crate)` so neither leaks into the 1.0 public surface, while keeping every in-crate caller compiling.

Purpose: Shrink what a 1.0 must commit to. These helpers were never intended to be public (AUD-07, AUD-08 in `81-AUDIT-INVENTORY.md` ## Approval); neither is re-exported and neither has external/test/example/doctest uses of the public path.
Output: Two visibility edits, whole-crate gates green, zero behavior change.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-public-surface-sealing-non-exhaustive-coverage/82-CONTEXT.md

# Build/CI hazards (MEMORY): pre-commit hook runs the full ~2861-test suite and
# times out at 30s. Run `cargo fmt` then `git commit --no-verify`, running gates
# out-of-band. /home disk pressure — if a spurious "No space left" / link failure
# appears, run `rm -rf target/debug/{incremental,examples}` to free space. Run
# cargo from workspace root /home/simonm/projects/rust/fdars.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seal sort_nan_safe (AUD-07) — pub to pub(crate)</name>
  <files>fdars-core/src/helpers.rs</files>
  <read_first>
    - fdars-core/src/helpers.rs:9-12 — the declaration `pub fn sort_nan_safe(slice: &mut [f64])`. It sits directly under the `DEFAULT_CONVERGENCE_TOL` const at line 7. Confirmed NOT re-exported in lib.rs or prelude.rs.
  </read_first>
  <action>
    Change the declaration at helpers.rs line 10 from `pub fn sort_nan_safe` to `pub(crate) fn sort_nan_safe` per AUD-07. Leave the doc comment, body, and signature untouched. Do NOT rename the function. This helper is internal-only (callers in outliers, seasonal, classification, scalar_on_function, tolerance, spm, detrend, conformal, concurrent_regression, cv, plus in-crate tests) — `pub(crate)` keeps every one of them visible, so no caller edits are required.
  </action>
  <acceptance_criteria>
    - helpers.rs line 10 declares the function with `pub(crate)` visibility.
    - No bare `pub` (non-crate-scoped) declaration of sort_nan_safe remains anywhere in fdars-core/src.
    - No caller file was edited (pub(crate) suffices).
  </acceptance_criteria>
  <verify>
    <automated>! grep -rnE '\bpub fn sort_nan_safe\b' fdars-core/src/ && grep -qE '\bpub\(crate\) fn sort_nan_safe\b' fdars-core/src/helpers.rs && cargo build -p fdars-core --features linalg,parallel 2>&1 | { ! grep -qE 'error\['; }</automated>
    <fails_when>`pub fn sort_nan_safe` still appears (seal not applied), OR the `pub(crate) fn sort_nan_safe` form is absent from helpers.rs, OR the build emits an `error[` line (a caller lost visibility).</fails_when>
  </verify>
  <done>sort_nan_safe is crate-scoped, no bare public declaration remains, and fdars-core builds under linalg,parallel with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Seal solve_gaussian_pub (AUD-08) — pub to pub(crate), then whole-crate gates</name>
  <files>fdars-core/src/smoothing.rs</files>
  <read_first>
    - fdars-core/src/smoothing.rs:331-338 — the declaration `pub fn solve_gaussian_pub(a: &mut [f64], b: &mut [f64], p: usize) -> Vec<f64>` which just delegates to the private `solve_gaussian`. Confirmed NOT re-exported in lib.rs or prelude.rs.
    - fdars-core/src/concurrent_regression.rs:226 — caller `smoothing::solve_gaussian_pub(&mut xtx, &mut xty, q)`.
    - fdars-core/src/scalar_on_function/cv.rs:36 — caller `crate::smoothing::solve_gaussian_pub(&mut xtx, &mut xty_vec, k)`.
  </read_first>
  <action>
    Change the declaration at smoothing.rs line 336 from `pub fn solve_gaussian_pub` to `pub(crate) fn solve_gaussian_pub` per AUD-08. Keep the doc comment and body; the two in-crate callers (concurrent_regression.rs:226, scalar_on_function/cv.rs:36) stay valid under pub(crate). Do NOT rename the `_pub` suffix in this plan — CONTEXT flags the rename as optional/low-risk only, and the private `solve_gaussian` at smoothing.rs already owns that name, so a rename would require merging the wrapper into the private fn. Skip it to keep this edit purely a visibility change (rename is deferrable, sealing is the approved requirement). After the edit, run the full whole-crate gate suite from the workspace root to prove no behavior or compile regression across all features/targets/examples.
  </action>
  <acceptance_criteria>
    - smoothing.rs line 336 declares the function with `pub(crate)` visibility, signature unchanged.
    - No bare `pub` (non-crate-scoped) declaration of solve_gaussian_pub remains anywhere in fdars-core/src.
    - Both callers (concurrent_regression.rs, scalar_on_function/cv.rs) compile unchanged.
    - All whole-crate gates green.
  </acceptance_criteria>
  <verify>
    <automated>! grep -rnE '\bpub fn solve_gaussian_pub\b' fdars-core/src/ && grep -qE '\bpub\(crate\) fn solve_gaussian_pub\b' fdars-core/src/smoothing.rs && cargo fmt --check && cargo clippy --all-targets --features linalg,parallel -- -D warnings && cargo test && cargo build --features serde && cargo build --examples</automated>
    <fails_when>`pub fn solve_gaussian_pub` still appears, OR the `pub(crate) fn solve_gaussian_pub` form is absent, OR `cargo fmt --check` exits non-zero, OR clippy emits any warning/error line, OR `cargo test` prints "test result: FAILED" or exits non-zero, OR `cargo build --features serde` exits non-zero, OR `cargo build --examples` exits non-zero (any of the 28 examples fails to compile).</fails_when>
  </verify>
  <done>solve_gaussian_pub is crate-scoped, both callers compile, and all five whole-crate gates (fmt, clippy --all-targets, test, serde build, examples build) are green.</done>
</task>

</tasks>

<verification>
- `sort_nan_safe` and `solve_gaussian_pub` are both `pub(crate)`; no bare `pub fn` form of either remains in fdars-core/src.
- Whole-crate gates green: `cargo fmt --check`, `cargo clippy --all-targets --features linalg,parallel -- -D warnings`, `cargo test`, `cargo build --features serde`, `cargo build --examples`.
- No numeric/behavioral change — diff is exactly two `pub` → `pub(crate)` tokens.
</verification>

<success_criteria>
- API-02 approved Scope-B entries (AUD-07, AUD-08) applied: both helpers sealed to `pub(crate)`.
- All in-crate callers still resolve; zero caller edits needed.
- All whole-crate gates green; 28 examples compile.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)
- Modified `fdars-core/src/helpers.rs` — `sort_nan_safe` sealed to `pub(crate)`.
- Modified `fdars-core/src/smoothing.rs` — `solve_gaussian_pub` sealed to `pub(crate)`.
</artifacts>

<output>
Create `.planning/phases/82-public-surface-sealing-non-exhaustive-coverage/82-01-SUMMARY.md` when done.
</output>
