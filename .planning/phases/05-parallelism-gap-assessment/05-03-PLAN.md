---
phase: 05-parallelism-gap-assessment
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - .planning/research/AUDIT-REPORT.md
autonomous: true
requirements: [PERF-05]
estimate:
  tokens: 55000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The ## Phase 5 section lists all 5 D-07 SEQUENTIAL candidates (classification/cv.rs:76, elastic_fpca.rs:701/720/764, regression.rs:167) as safe-to-parallelize, each with (a) independence/no-shared-mutable-state argument, (b) an RNG-seeding note (or explicit no-RNG-in-loop note), and (c) a projected speedup extrapolated from the measured SC1 scaling (SC2, D-06/D-07)"
    - "The report states no fdars-core/src edits were made for SC2 — the evidence standard is a static safety argument + projected speedup (D-06) — backstop"
    - "The report records the rayon-off unaccelerated cost via Phase-1 karcher combo (~10×: `\"\"`≈1555ms vs `parallel`≈162ms) with artifact links (SC3, D-08a)"
    - "The report records the banding opt-in unaccelerated cost via Phase-3 karcher unbanded-vs-banded (~7× nominal, measured ~4-6×) with the LOW-CONFIDENCE caveat on Phase-3 karcher variance (SC3, D-08b)"
    - "SC4 backlog entries exist with Function / Current-cost / Root-cause fields (+ candidate direction) for the parallelization opportunities, following the Phase-3 D-07 backlog format"
  artifacts:
    - ".planning/research/AUDIT-REPORT.md (SC2 safe-to-parallelize list + SC3 unaccelerated-cost record + SC4 backlog subsections in the ## Phase 5 section)"
  key_links:
    - "SC1 measured karcher/streaming scaling → projected speedup for each SC2 sequential-loop analogue"
    - "Phase-1 p1_karcher_none/parallel artifacts → SC3 rayon-off ~10×; Phase-3 karcher unbanded/banded → SC3 banding ~7×"
  prohibitions:
    - "No edits to fdars-core/src/** — a src edit is a scope violation (SC2 is static argument + projection only, per D-06)"
---

<objective>
**As the** audit consumer, **I want** the safe-to-parallelize sequential loops enumerated with evidence, the cost of the default unaccelerated path recorded, and GSD-ready backlog entries drafted, **so that** a future implementation milestone can pick the highest-leverage parallelization work without re-deriving the candidates or re-measuring the baselines.

Final analysis slice — no new benchmarks. Reuses the SC1 scaling measured in Plans 01/02 (for SC2 projected speedups) and cites existing Phase-1/Phase-3 artifacts (for SC3 unaccelerated-path costs), then drafts the SC4 backlog. Honors the audit-only fence: SC2 is a static safety argument + projected speedup with NO `fdars-core/src` edits (D-06).

Purpose: Complete SC2 (safe-to-parallelize list), SC3 (opt-in/unaccelerated-cost record), and SC4 (backlog).
Output: The SC2/SC3/SC4 subsections appended to the `## Phase 5` report section, completing PERF-05.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-parallelism-gap-assessment/05-CONTEXT.md
@.planning/phases/03-elastic-alignment-hot-path/03-CONTEXT.md
@fdars-core/src/parallel.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the SC2 safe-to-parallelize list (5 D-07 candidates, static safety + projected speedup, no src edits)</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - .planning/research/AUDIT-REPORT.md (read §"Parallelism Gap List" ~line 298 — the authoritative Phase-2 SEQUENTIAL entries with file:line anchors, independence notes, and RNG notes already recorded there; read the `## Phase 5` section's completed Thread-Scaling Table for the measured karcher/streaming scaling used as the projection basis)
    - .planning/phases/05-parallelism-gap-assessment/05-CONTEXT.md (D-06 evidence standard = static safety argument + projected speedup, NO src edits; D-07 the EXACT fixed candidate set — do NOT re-derive)
    - fdars-core/src/parallel.rs (the `iter_maybe_parallel!` family macros each SC2 candidate would use — for the "which macro applies" note; read-only, do not edit)
  </read_first>
  <action>
    Add a `### SC2: Sequential Loops Safe to Parallelize` subsection to the existing `## Phase 5` section. Cite the Phase-2 §"Parallelism Gap List" as the source (D-07) — do NOT re-grep or re-derive the candidate set. Produce a table with exactly these five candidates, one row each:
    - `classification/cv.rs:76` (`fclassif_cv` fold loop)
    - `elastic_fpca.rs:701` (`shooting_vectors_from_psis`)
    - `elastic_fpca.rs:720` (`build_augmented_srsfs`)
    - `elastic_fpca.rs:764` (`svd_scores_and_eigenvalues`)
    - `regression.rs:167` (`center_columns` inside `fdata_to_pc_1d`, distinct from the already-parallel `fdata.rs:center_1d`)

    Columns: `Loop (file:line) | Independence argument | RNG-seeding note | Applicable macro | Projected speedup`.
    - Independence argument: state absence of shared mutable state across iterations (e.g. CV folds are disjoint train/test splits; elastic-FPCA inner N-loops write per-curve rows; center_columns columns are independent).
    - RNG-seeding note: for cv.rs:76 state explicitly that the fold-assignment RNG runs ONCE before the loop (deterministic fold map) so the loop body has NO RNG — no per-thread seeding needed; for any candidate whose parallel form WOULD introduce per-iteration RNG, cite the `StdRng::seed_from_u64(seed + k)` pattern; state "no RNG in loop body" where true (the elastic-FPCA and center_columns loops).
    - Applicable macro: name which `parallel.rs` macro fits (`iter_maybe_parallel!(0..n)` for the N-loops; note center_columns can wrap outer-M or inner-N).
    - Projected speedup: extrapolate from the SC1 measured scaling of the already-parallel analogue — project the N-loop candidates from karcher_mean's `iter_maybe_parallel!` thread-scaling in the SC1 table (state the projected speedup at machine-default threads and note it is bounded by the payback-N: loops that run at small N will not benefit). This is a PROJECTION, not a measurement — label it so.

    State explicitly at the top of the subsection: per D-06 this is a static safety argument + projected speedup with NO `fdars-core/src` edits — actually wrapping the loops and measuring real speedup is deferred to a future implementation milestone. Do NOT edit any file under fdars-core/src/.
  </action>
  <verify>
    <automated>grep -q "SC2" .planning/research/AUDIT-REPORT.md && for anchor in "classification/cv.rs:76" "elastic_fpca.rs:701" "elastic_fpca.rs:720" "elastic_fpca.rs:764" "regression.rs:167"; do grep -qF "$anchor" .planning/research/AUDIT-REPORT.md || exit 1; done && grep -qi "seed_from_u64" .planning/research/AUDIT-REPORT.md && grep -qi "projected" .planning/research/AUDIT-REPORT.md && git diff --name-only | grep -qv "^fdars-core/src/"</automated>
  </verify>
  <acceptance_criteria>
    - A `### SC2` subsection lists all five D-07 anchors: classification/cv.rs:76, elastic_fpca.rs:701, elastic_fpca.rs:720, elastic_fpca.rs:764, regression.rs:167
    - Each row has an independence argument, an RNG note (cv.rs:76 explicitly states the fold RNG runs once before the loop / no RNG in loop body; the `seed_from_u64(seed + k)` pattern is referenced), an applicable macro, and a projected speedup extrapolated from the SC1 measured scaling (labeled as a projection)
    - The subsection states the D-06 evidence standard (static argument + projection, no src edits) and cites the Phase-2 Parallelism Gap List as the candidate source
    - No `fdars-core/src/` file modified
  </acceptance_criteria>
  <done>SC2 is complete: all five sequential candidates documented with independence + RNG note + macro + projected speedup, no src edits.</done>
</task>

<task type="auto">
  <name>Task 2: Write the SC3 unaccelerated-path cost record (rayon-off ~10× + banding opt-in ~7×, cited not re-measured)</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - .planning/research/AUDIT-REPORT.md (read the Phase-1 karcher feature-combo table ~line 220: `""`≈1555ms vs `parallel`≈162ms → ~10×, artifacts p1_karcher_none/parallel; read the Phase-3 §"Banded-vs-Unbanded Analysis" ~line 394 karcher unbanded-vs-banded ~7× nominal / ~4-6× measured and the LOW-CONFIDENCE karcher variance note ~line 378; read the Phase-3 §D-05 Anti-Pattern 2 note that karcher_mean() defaults band_frac=0.0)
    - .planning/phases/05-parallelism-gap-assessment/05-CONTEXT.md (D-08: report BOTH dimensions, cite existing artifacts, note LOW-CONFIDENCE caveat on Phase-3 karcher variance; do NOT re-measure)
    - .planning/phases/03-elastic-alignment-hot-path/03-CONTEXT.md (D-05 banding-opt-in root cause)
  </read_first>
  <action>
    Add a `### SC3: Cost of the Default Unaccelerated Path` subsection to the `## Phase 5` section, reporting BOTH opt-in dimensions by CITATION (D-08) — do NOT run any new benchmark:
    (a) rayon-off cost: cite Phase-1's karcher feature-combo data — `""` (no parallel) ≈ 1555 ms vs `parallel` ≈ 162 ms → ~10× penalty for the rayon-off default when a user builds with `--no-default-features`. Link artifacts `bench/p1_karcher_none_run1.txt` and `bench/p1_karcher_parallel_run1.txt`. Note this is the `--no-default-features` cost, distinct from the SC1 payback-N RAYON_NUM_THREADS=1 baseline (which holds the parallel feature on and only varies thread count).
    (b) banding opt-in cost: cite Phase-3's karcher unbanded-vs-banded — `karcher_mean()` defaults `band_frac=0.0` (Anti-Pattern 2, karcher.rs:300) → full O(m²) DP; the banded path (~7× nominal at band_frac=0.1, measured ~4-6× at representative cells) is opt-in via `karcher_mean_banded()`. Link Phase-3 karcher artifacts. State the LOW-CONFIDENCE caveat: the raw Phase-3 karcher cells showed 34-204% two-run variance (OS scheduler jitter), so the ~7× is directional; the stable elastic_cross cells (0-4% variance) corroborate the ~4.5-5.7× banding reduction.
    Frame both as "the cost every user of the default API path pays". Do NOT edit any file under fdars-core/src/.
  </action>
  <verify>
    <automated>grep -q "SC3" .planning/research/AUDIT-REPORT.md && grep -q "p1_karcher_none" .planning/research/AUDIT-REPORT.md && grep -qi "band_frac" .planning/research/AUDIT-REPORT.md && grep -Eiq "low.?confidence" .planning/research/AUDIT-REPORT.md && grep -Eq "1555|10.?×|10x" .planning/research/AUDIT-REPORT.md && git diff --name-only | grep -qv "^fdars-core/src/"</automated>
  </verify>
  <acceptance_criteria>
    - A `### SC3` subsection records the rayon-off ~10× cost citing p1_karcher_none/p1_karcher_parallel artifacts (1555ms vs 162ms)
    - It records the banding opt-in ~7× (measured ~4-6×) cost citing Phase-3 karcher unbanded-vs-banded, naming the band_frac=0.0 default (Anti-Pattern 2)
    - It carries the LOW-CONFIDENCE caveat on the raw Phase-3 karcher variance, corroborated by the stable elastic_cross cells
    - It distinguishes the `--no-default-features` rayon-off cost from the SC1 RAYON_NUM_THREADS=1 payback baseline
    - No `fdars-core/src/` file modified
  </acceptance_criteria>
  <done>SC3 is complete: both opt-in unaccelerated-path costs recorded by citation with the LOW-CONFIDENCE caveat, no re-measurement, no src edits.</done>
</task>

<task type="auto">
  <name>Task 3: Draft the SC4 GSD-ready parallelization backlog (Function/Current-cost/Root-cause fields)</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - .planning/research/AUDIT-REPORT.md (read the Phase-3 "#### Draft Backlog" entries ~line 460 for the exact field format: Function | Current cost (measured) | Root cause | Candidate fix | Observed/projected reduction; read the completed ## Phase 5 SC1/SC2/SC3 subsections this task summarizes into backlog entries)
    - .planning/phases/03-elastic-alignment-hot-path/03-CONTEXT.md (D-07 backlog-entry field format reused for SC4)
    - .planning/phases/05-parallelism-gap-assessment/05-CONTEXT.md (SC4 = parallelization-opportunity backlog entries; the two opt-in-cost items from SC3 are also backlog-worthy)
  </read_first>
  <action>
    Add a `### SC4: Parallelization Backlog (draft, GSD-ready)` subsection to the `## Phase 5` section, reusing the Phase-3 D-07 backlog field format (a field table per entry: Function | Current cost | Root cause | Candidate direction | Projected/observed reduction | Evidence link). Draft at least these entries, phrased as candidate requirements/phases for Phase-9 ranking:
    1. Parallelize the classification CV fold loop (`classification/cv.rs:76`) — Function/Current-cost (cite CV sentinel baseline)/Root-cause (SEQUENTIAL, folds independent, fold RNG outside loop)/Candidate-direction (wrap in `iter_maybe_parallel!(0..nfold)`)/Projected reduction (from SC1 karcher scaling projection).
    2. Parallelize the three elastic-FPCA inner N-loops (`elastic_fpca.rs:701/720/764`) — one grouped entry, same fields, projected from SC1.
    3. Parallelize `center_columns` (`regression.rs:167`) inside `fdata_to_pc_1d` — same fields; note it is distinct from the already-parallel `center_1d`.
    4. (From SC3) Change elastic-alignment API defaults to a banded path / expose band_frac — cross-reference the Phase-3 backlog entry so it is not duplicated but is linked as a parallelism-adjacent opt-in-default cost.
    Each entry MUST carry Function, Current-cost (or projected), and Root-cause fields at minimum (SC4 requirement). Evidence links point at the SC1 table rows and the cited Phase-1/Phase-3 artifacts. Do NOT edit any file under fdars-core/src/.
  </action>
  <verify>
    <automated>grep -q "SC4" .planning/research/AUDIT-REPORT.md && grep -Eiq "Function|Root cause|Root-cause" .planning/research/AUDIT-REPORT.md && grep -qF "classification/cv.rs:76" .planning/research/AUDIT-REPORT.md && grep -qF "regression.rs:167" .planning/research/AUDIT-REPORT.md && grep -c "^## Phase 5: Parallelism Gap Assessment" .planning/research/AUDIT-REPORT.md | grep -qx 1 && git diff --name-only | grep -qv "^fdars-core/src/"</automated>
  </verify>
  <acceptance_criteria>
    - A `### SC4` subsection contains backlog entries with Function / Current-cost / Root-cause fields (+ candidate direction + evidence link) for: CV fold loop, the three elastic-FPCA N-loops, and center_columns
    - The elastic-alignment banded-default item is cross-referenced (not duplicated) from the Phase-3 backlog
    - Each entry is phrased as a candidate requirement/phase ready for Phase-9 ranking
    - Exactly one `## Phase 5: Parallelism Gap Assessment` heading remains (all SC subsections nested under it)
    - No `fdars-core/src/` file modified
  </acceptance_criteria>
  <done>SC4 is complete: GSD-ready parallelization backlog entries drafted with Function/Current-cost/Root-cause fields; PERF-05 fully addressed across the ## Phase 5 section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none of security relevance) | This plan only reads prior benchmark artifacts and writes markdown into `.planning/research/AUDIT-REPORT.md`. No benchmarks run, no network I/O, no untrusted input, no auth, no DB, no user-facing surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering | report edit scope | low | mitigate | Verify gates assert `git diff --name-only` excludes `fdars-core/src/**`; SC2 is explicitly static-argument-only (D-06), so no src prototyping occurs. |

No package installs, no network egress, no code execution beyond markdown edits. No high/critical threats to block on.
</threat_model>

<verification>
- Single `## Phase 5` section now carries SC2 (safe-to-parallelize list), SC3 (unaccelerated-cost record), and SC4 (backlog) subsections.
- All five D-07 anchors present in SC2 with independence + RNG + projected-speedup fields.
- SC3 cites p1_karcher and Phase-3 karcher artifacts with the LOW-CONFIDENCE caveat.
- SC4 backlog entries carry Function/Current-cost/Root-cause fields.
- No `fdars-core/src/` file modified.
</verification>

<success_criteria>
PERF-05 is complete: the `## Phase 5` section delivers all four success criteria — SC1 thread-scaling table + payback-N (Plans 01/02), SC2 safe-to-parallelize list with RNG note, SC3 opt-in/unaccelerated-cost record, and SC4 GSD-ready parallelization backlog — all as audit analysis with zero edits to `fdars-core/src/`.
</success_criteria>

<output>
Create `.planning/phases/05-parallelism-gap-assessment/05-03-SUMMARY.md` when done.

## Artifacts this plan produces
- `### SC2: Sequential Loops Safe to Parallelize` subsection (5 D-07 candidates, static safety + projected speedup) in the `## Phase 5` section of AUDIT-REPORT.md.
- `### SC3: Cost of the Default Unaccelerated Path` subsection (rayon-off ~10× + banding ~7×, cited) in the same section.
- `### SC4: Parallelization Backlog (draft, GSD-ready)` subsection (Function/Current-cost/Root-cause entries) in the same section.
</output>
