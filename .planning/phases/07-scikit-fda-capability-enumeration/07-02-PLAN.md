---
phase: 07-scikit-fda-capability-enumeration
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - .planning/research/AUDIT-REPORT.md
autonomous: true
requirements: [GAP-01]
user_setup: []

estimate:
  tokens: 65000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The Phase-7 section enumerates ALL SIX SC1 report areas as `### Area:` subsections: representation (from Plan 01), preprocessing, exploratory, ML, inference, misc (SC1)."
    - "Every enumerated capability row reuses the Plan-01 schema verbatim (D-03 two-level structure, fit/predict/transform collapsed to one row, D-04 Relevance value) (SC3, D-04)."
    - "The `exploratory` area routes scikit-fda Visualization/plotting items to Out-of-Scope (plotting), and misc/infrastructure routes dataset-loaders/DataFrame round-trips to Out-of-Scope (IO), and FDAFeatureUnion/PerClassTransformer/sklearn-Pipeline to Out-of-Scope (D-04 borderline rulings)."
    - "A `### Design-Goal Filter` subsection (one page, within the Phase-7 section, NOT a separate file) gives explicit rulings on the D-04 borderline items and reports in-scope vs out-of-scope capability counts SEPARATELY (SC4)."
    - "The section is internally consistent: the counts in the design-goal filter match the Relevance tags in the six area tables."
  artifacts:
    - ".planning/research/AUDIT-REPORT.md (five additional `### Area:` subsections + `### Design-Goal Filter` subsection with separated in/out-of-scope counts)"
  key_links:
    - "The six area tables + the four-value Relevance taxonomy are the exact inputs Phase 8 consumes for GAP-02 (parity axis) and GAP-03 (categorization); the separated counts prevent plotting/IO inflating Phase 8's actionable gap count (Pitfall 14)."
  prohibitions:
    - "MUST NOT edit any file under fdars-core/src/ (audit-only milestone)."
    - "MUST NOT contain any fdars-side parity note (no `fdars has`, `fdars partial`, `fdars equivalent`, present/absent) anywhere in the Phase-7 output — that is Phase 8."
    - "MUST NOT create a separate design-goal-filter file — the filter is a subsection of the AUDIT-REPORT.md Phase 7 section (D-05)."
    - "MUST NOT include gap categorization (table-stakes/differentiator) or fdars-strengths documentation — those are Phase 8 (GAP-03/04)."
---

<objective>
Fill the remaining five report areas into the Plan-01 schema, then write the one-page design-goal filter and run the final scope-fence/consistency pass.

Deliver, appended to the `## Phase 7 — scikit-fda Capability Enumeration` section of `.planning/research/AUDIT-REPORT.md`:
1. Five `### Area:` subsections — preprocessing, exploratory, ML, inference, misc — each populated in the SAME schema table shape Plan 01 proved, with fdars notes stripped (D-02) and D-04 Relevance applied per row.
2. A `### Design-Goal Filter` one-page subsection: explicit rulings on the D-04 borderline items and separated in-scope vs out-of-scope capability counts (SC4).
3. A final consistency + scope-fence pass verifying the counts match the tables and no prohibited content leaked.

Purpose: complete the scikit-fda-side inventory that Phase 8's parity matrix and categorization consume.
Output: the completed Phase-7 section (six areas + design-goal filter) in AUDIT-REPORT.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<artifacts_produced>
New markdown artifacts (no code symbols created this phase):
- Five `### Area:` subsections (preprocessing, exploratory, ML, inference, misc) appended to the `## Phase 7` section of `.planning/research/AUDIT-REPORT.md`, each a capability table in the Plan-01 schema shape.
- A `### Design-Goal Filter` subsection within the same Phase-7 section, with explicit borderline rulings and separated in-scope / out-of-scope capability counts.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate preprocessing, exploratory, and ML areas</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - .planning/research/AUDIT-REPORT.md (the Plan-01 `### Capability-Row Schema` subsection — reuse its EXACT columns and collapse rule; the `### Area: Representation` table as the format template)
    - .planning/research/FEATURES.md §"Area 2: Smoothing", §"Area 3: Registration/Alignment", §"Area 4: Dimensionality Reduction & Feature Construction" (→ preprocessing); §"Area 5: Depth & Outlier", §"Area 6: Summary Statistics & Visualization" (→ exploratory); §"Area 7: Classification", §"Area 8: Regression", §"Area 9: Clustering" (→ ML) — use the `### scikit-fda Public API` tables; STRIP the `### fdars Current Status` and `### Categorization` subsections
    - .planning/research/PITFALLS.md §"Pitfall 9" (collapse fit/predict/transform to one row) and §"Pitfall 14" (Visualization items → Out-of-Scope (plotting))
    - .planning/phases/07-scikit-fda-capability-enumeration/07-CONTEXT.md (D-02 mapping, D-03 grouping, D-04 taxonomy)
  </read_first>
  <action>
    Append three `### Area:` subsections to the Phase-7 section, each populating the Plan-01 schema table (exact same columns):
    - `### Area: Preprocessing` — task groupings smoothing (KernelSmoother + NW/LocalLinear/kNN hat-matrix strategies collapsed sensibly, BasisSmoother, SmoothingParameterSearch, LOO/GCV scorers, AIC/FPE/Shibata/Rice selectors, MissingValuesInterpolation), registration/alignment (LeastSquaresShiftRegistration, FisherRaoElasticRegistration, landmark_* utilities, invert/normalize_warping, AmplitudePhaseDecomposition + LS/Sobolev/PairwiseCorrelation validators), and dimensionality-reduction/feature-construction (FPCA, FPLS, DiffusionMap, MaximaHunting/RecursiveMaximaHunting/RKHS/mRMR variable selection, LocalAverages/OccupationMeasure/NumberCrossings transformers + their functional-feature functions). Rule FDAFeatureUnion/PerClassTransformer → Out-of-Scope (sklearn-pipeline plumbing, per D-04).
    - `### Area: Exploratory` — depth/outlier (Integrated/Band/ModifiedBand/DistanceBased/Outlyingness/Projection/Simplicial depth, StahelDonoho, Boxplot/MSPlot outlier detectors, directional_outlyingness_stats) and summary statistics (mean/gmean/trim_mean/depth_based_median/geometric_median/fisher_rao_karcher_mean/cov/var/std). Route ALL Visualization classes (GraphPlot, ScatterPlot, ParametricPlot, Boxplot, SurfaceBoxplot, Outliergram, MagnitudeShapePlot, ClusterPlot, ClusterMembership*, FPCAPlot) → Out-of-Scope (plotting) per D-04/Pitfall 14. Note: an outlier DETECTOR (e.g. MSPlotOutlierDetector as an algorithm producing outlier labels) is In-Scope Algorithm even though its PLOT counterpart (MagnitudeShapePlot) is Out-of-Scope (plotting) — rule them as distinct rows.
    - `### Area: ML` — classification (KNeighbors/RadiusNeighbors/NearestCentroid/DTM/MaximumDepth/DD/DDG classifiers, LogisticRegression, QDA), regression (LinearRegression, HistoricalLinearRegression, KNeighbors/RadiusNeighbors regressors, KernelRegression, FPCARegression, FPLSRegression), clustering (KMeans, FuzzyCMeans, NearestNeighbors, AgglomerativeClustering). Collapse each estimator's fit/predict to one row.
    Strip ALL fdars-side notes and categorization. Reuse the exact schema columns; do not invent a new table shape.
  </action>
  <verify>
    <automated>grep -Fq "### Area: Preprocessing" .planning/research/AUDIT-REPORT.md && grep -Fq "### Area: Exploratory" .planning/research/AUDIT-REPORT.md && grep -Fq "### Area: ML" .planning/research/AUDIT-REPORT.md && grep -Fq "KernelSmoother" .planning/research/AUDIT-REPORT.md && grep -Fq "KMeans" .planning/research/AUDIT-REPORT.md && grep -Fq "Out-of-Scope (plotting)" .planning/research/AUDIT-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - Subsections `### Area: Preprocessing`, `### Area: Exploratory`, `### Area: ML` all exist under the Phase-7 section, each with a table using the Plan-01 schema columns.
    - Smoothing (`KernelSmoother`), clustering (`KMeans`), and registration (`FisherRaoElasticRegistration`) capabilities are present as rows.
    - At least one Visualization item is tagged `Out-of-Scope (plotting)` in the exploratory area.
    - FDAFeatureUnion / PerClassTransformer are tagged Out-of-Scope.
    - No `fdars (has|partial|equivalent)` and no `### fdars Current Status` heading in the three new subsections.
  </acceptance_criteria>
  <reversibility rating="costly">Row granularity is the Phase-8 parity schema (D-03); re-slicing later re-maps the matrix.</reversibility>
  <done>Preprocessing, exploratory, and ML areas are enumerated in the Plan-01 schema with borderline plotting/pipeline items ruled Out-of-Scope and all fdars notes stripped.</done>
</task>

<task type="auto">
  <name>Task 2: Enumerate inference + misc areas, then write the Design-Goal Filter subsection</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - .planning/research/AUDIT-REPORT.md (the Plan-01 schema columns + the five area tables written so far — the design-goal filter must count against these)
    - .planning/research/FEATURES.md §"Area 10: Inference" (→ inference); §"Area 11: Metrics & Norms", §"Area 12: Infrastructure" (→ misc) — use the `### scikit-fda Public API` tables; STRIP fdars notes
    - .planning/research/PITFALLS.md §"Pitfall 14" (the four-value Relevance taxonomy, separate in/out-of-scope counts) and §"Pitfall 9"
    - .planning/phases/07-scikit-fda-capability-enumeration/07-CONTEXT.md §"Design-Goal Filter" (D-04 — the exact borderline rulings the one-pager must give)
    - .planning/PROJECT.md §"Out of Scope" (plotting/IO/type-system-refactor rulings)
  </read_first>
  <action>
    First, append the remaining two `### Area:` subsections in the Plan-01 schema shape:
    - `### Area: Inference` — oneway_anova, v_sample_stat, v_asymptotic_stat, hotelling_t2, hotelling_test_ind (collapse the ANOVA statistic helpers under their test where sensible; one row per distinct test/statistic capability).
    - `### Area: Misc` — metrics/norms (LpNorm/LpDistance, MahalanobisDistance, NormInducedMetric, PairwiseMetric, TransformationMetric, lp_norm/lp_distance, angular_distance, fisher_rao_distance + amplitude/phase, inner_product/inner_product_matrix, cosine_similarity/_matrix) and infrastructure (named covariance kernels Brownian/Exponential/Gaussian/Matern/Linear/Polynomial/WhiteNoise; operators Identity/LinearDifferentialOperator/SRSF; L2Regularization; data-generation make_* helpers; scoring metrics r2_score etc.). Route dataset `fetch_*` loaders and DataFrame/pandas round-trips → Out-of-Scope (IO) per D-04. Scoring metrics and operators are In-Scope Algorithm; the sklearn-scoring-API shape may be tagged In-Scope API-Ergonomics where relevant.
    Then write a `### Design-Goal Filter` subsection (one page, WITHIN the Phase-7 section — NOT a separate file, per D-05). It MUST: (a) give explicit, itemized rulings on the D-04 borderline items — Visualization → Out-of-Scope (plotting); DataFrame/pandas + dataset loaders → Out-of-Scope (IO); FDAFeatureUnion/PerClassTransformer/sklearn-Pipeline → Out-of-Scope; representation type-system (FDataGrid/FDataBasis/FDataIrregular hierarchy) → Out-of-Scope (type-system refactor) BUT algorithmic capabilities riding on it (e.g. irregular-data covariance) → In-Scope Algorithm; (b) report the capability counts SEPARATELY — total In-Scope (Algorithm + API-Ergonomics) vs total Out-of-Scope (plotting + IO), so Phase 8's actionable gap count is not inflated; (c) present the four-value taxonomy legend once.
    Do NOT categorize gaps table-stakes/differentiator (Phase 8). Do NOT write fdars-side notes. Do NOT create a separate file.
  </action>
  <verify>
    <automated>grep -Fq "### Area: Inference" .planning/research/AUDIT-REPORT.md && grep -Fq "### Area: Misc" .planning/research/AUDIT-REPORT.md && grep -Fq "### Design-Goal Filter" .planning/research/AUDIT-REPORT.md && grep -Fq "hotelling_t2" .planning/research/AUDIT-REPORT.md && grep -Fq "Out-of-Scope (IO)" .planning/research/AUDIT-REPORT.md && grep -Eiq "in-scope.*count|count.*in-scope" .planning/research/AUDIT-REPORT.md && test ! -e .planning/research/design-goal-filter.md</automated>
  </verify>
  <acceptance_criteria>
    - `### Area: Inference` and `### Area: Misc` subsections exist in the Plan-01 schema shape; `hotelling_t2` appears as a row.
    - `### Design-Goal Filter` subsection exists WITHIN the Phase-7 section; NO separate `.planning/research/design-goal-filter.md` file exists.
    - The filter gives an explicit ruling for each borderline class: Visualization → Out-of-Scope (plotting); dataset loaders / DataFrame → Out-of-Scope (IO); FDAFeatureUnion/PerClassTransformer/sklearn-Pipeline → Out-of-Scope; type-system → Out-of-Scope but riding algorithmic capabilities In-Scope Algorithm.
    - The filter reports a total In-Scope count and a total Out-of-Scope count as SEPARATE numbers (grep for an in-scope count phrase succeeds).
  </acceptance_criteria>
  <reversibility rating="reversible">The Relevance taxonomy is a reporting classification; re-classifying an item is a local edit (D-04).</reversibility>
  <done>Inference and misc areas are enumerated, and the one-page design-goal filter gives explicit borderline rulings with separated in-scope vs out-of-scope counts — all within the single Phase-7 section.</done>
</task>

<task type="auto">
  <name>Task 3: Final consistency + scope-fence pass</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - .planning/research/AUDIT-REPORT.md (the complete Phase-7 section — read header→EOF for the consistency pass)
    - .planning/phases/07-scikit-fda-capability-enumeration/07-CONTEXT.md §"Not in scope" and §"Deferred Ideas" (the scope fences to enforce)
  </read_first>
  <action>
    Run a final review of the complete `## Phase 7 — scikit-fda Capability Enumeration` section and correct any inconsistency:
    - Confirm all SIX areas are present as `### Area:` subsections (representation, preprocessing, exploratory, ML, inference, misc) and every table uses the identical Plan-01 schema columns.
    - Reconcile the Design-Goal Filter counts against the Relevance tags actually present in the six area tables: the reported In-Scope total and Out-of-Scope total must match the row tags. Fix whichever is wrong (recount, or correct a mis-tagged row).
    - Enforce the scope fences: remove any leaked fdars-side note (`fdars has/partial/equivalent`, present/absent), any gap categorization (table-stakes/differentiator), any fdars-strengths text, and confirm no separate filter file was created. Confirm zero edits under `fdars-core/src/`.
    - Confirm the methodology's version pin (0.10.1 + D-01 path + baseline=latest note) is intact after the appends.
    Make only corrective edits scoped to the Phase-7 section; do not restructure earlier phases' sections.
  </action>
  <verify>
    <automated>test $(grep -c "^### Area: " .planning/research/AUDIT-REPORT.md) -ge 6 && ! awk '/^## Phase 7 — scikit-fda Capability Enumeration/{p=1} p' .planning/research/AUDIT-REPORT.md | grep -vE '^\s*#' | grep -Eqi 'fdars (has|partial|equivalent)' && ! awk '/^## Phase 7 — scikit-fda Capability Enumeration/{p=1} p' .planning/research/AUDIT-REPORT.md | grep -vE '^\s*#' | grep -Eqi 'table[- ]stakes|differentiator' && test ! -e .planning/research/design-goal-filter.md && test -z "$(git status --porcelain fdars-core/src)"</automated>
  </verify>
  <acceptance_criteria>
    - At least six `### Area:` subsections exist in AUDIT-REPORT.md.
    - Region-scoped negative gate (Phase-7 header → EOF, comments excluded): no `fdars (has|partial|equivalent)` and no `table-stakes|differentiator` gap categorization.
    - `.planning/research/design-goal-filter.md` does NOT exist (filter is a subsection).
    - `git status --porcelain fdars-core/src` is empty (final command prints `SRC_CLEAN`).
    - Design-Goal Filter in-scope/out-of-scope totals match the Relevance tags across the six area tables (reviewer-confirmed; note the reconciled counts in the SUMMARY).
  </acceptance_criteria>
  <done>The Phase-7 section is internally consistent, all scope fences hold, and the enumeration is ready for Phase 8 consumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan appends markdown to `.planning/research/AUDIT-REPORT.md` only. No runtime, no network, no user input, no shipped code. The D-01 venv install (if attempted) happened in Plan 01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02 | Tampering | `.planning/research/AUDIT-REPORT.md` append | low | accept | Docs-only edit to a planning artifact; no ASVS-L1-relevant surface. No shipped code, no user input, no network exposure. |

No L1-relevant threats exist for this docs-only plan. Recorded honestly for completeness, not as a mitigated risk.
</threat_model>

<verification>
- Six `### Area:` subsections present (representation + five added here).
- `### Design-Goal Filter` subsection present within the Phase-7 section; no separate file.
- All four D-04 Relevance values used across the tables; separated in-scope vs out-of-scope counts reported.
- Region-scoped negative gate (Phase-7 header → EOF) clean of fdars-side notes and gap categorization.
- `git status --porcelain fdars-core/src` empty.
</verification>

<success_criteria>
The Phase-7 section enumerates all six SC1 areas capability-oriented (SC1, SC3), pins version 0.10.1 (SC2, from Plan 01), and carries a one-page design-goal filter with separated in/out-of-scope counts and explicit borderline rulings (SC4) — with zero fdars-side notes, no gap categorization, and no `fdars-core/src` edits.
</success_criteria>

<output>
Create `.planning/phases/07-scikit-fda-capability-enumeration/07-02-SUMMARY.md` when done.
</output>