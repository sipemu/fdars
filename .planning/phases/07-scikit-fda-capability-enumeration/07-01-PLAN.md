---
phase: 07-scikit-fda-capability-enumeration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/AUDIT-REPORT.md
  - .planning/research/skfda-verify/  # throwaway D-01 evidence (venv log + version/dir capture); not the deliverable
autonomous: true
requirements: [GAP-01]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "AUDIT-REPORT.md contains a top-level section header `## Phase 7 — scikit-fda Capability Enumeration` (SC1, D-05)."
    - "That section records the pinned scikit-fda version `0.10.1` and which D-01 verification path was used (venv `__version__` capture OR PyPI/readthedocs docs fallback) and why (SC2, D-01)."
    - "The methodology explicitly notes that 0.10.1 is BOTH the agreed baseline AND the current latest PyPI release (D-01a)."
    - "The section defines the capability-row schema: a two-level structure (six report areas → task groupings → one row per distinct method/algorithm) with fit/predict/transform/inverse_transform of one estimator collapsed to a single row (SC3, D-03)."
    - "The section carries a Relevance column using exactly the four D-04 taxonomy values (In-Scope Algorithm / In-Scope API-Ergonomics / Out-of-Scope (plotting) / Out-of-Scope (IO)) (SC4, D-04)."
    - "The `representation` area is enumerated end-to-end in the final table shape as the tracer, proving the schema before the other five areas are filled."
  artifacts:
    - ".planning/research/AUDIT-REPORT.md (appended `## Phase 7 — scikit-fda Capability Enumeration` section with methodology + schema + representation area)"
  key_links:
    - "The representation-area rows are keyed off FEATURES.md Area 1, with fdars-side notes stripped (D-02) — Phase 8's parity matrix consumes these method rows (D-03 granularity)."
  prohibitions:
    - "MUST NOT edit any file under fdars-core/src/ (audit-only milestone)."
    - "MUST NOT contain any fdars-side parity note (no `fdars has`, `fdars partial`, `fdars equivalent`, present/absent) anywhere in the Phase-7 output — that is Phase 8."
    - "MUST NOT create a separate design-goal-filter file — the filter is a subsection of the AUDIT-REPORT.md Phase 7 section (D-05)."
    - "MUST NOT include gap categorization (table-stakes/differentiator) or fdars-strengths documentation — those are Phase 8 (GAP-03/04)."
---

<objective>
Prove the Phase-7 enumeration schema end-to-end on ONE report area (representation), so the remaining five areas in Plan 02 slot into a validated table shape.

Deliver, appended as `## Phase 7 — scikit-fda Capability Enumeration` to `.planning/research/AUDIT-REPORT.md`:
1. A methodology subsection that pins scikit-fda `0.10.1`, records the D-01 verification path actually used (runtime venv `__version__` OR docs fallback) and why, and states the D-01a baseline=latest coincidence.
2. The capability-row schema (D-03 two-level structure, fit/predict/transform collapse rule) and the D-04 four-value Relevance taxonomy, both stated once as the schema the whole section obeys.
3. The `representation` area (from FEATURES.md Area 1, fdars notes stripped) fully enumerated in that final table shape — the tracer slice.

Purpose: catch a schema dead-end after one committed area, not after all six.
Output: the Phase-7 section header, methodology, schema definition, and the representation table in AUDIT-REPORT.md; throwaway D-01 evidence under `.planning/research/skfda-verify/`.
</objective>

<phase_goal>
**As a** Phase-8 parity-matrix author, **I want to** consume a versioned, capability-oriented scikit-fda enumeration keyed by method (not API name), **so that** I can mark fdars parity per method without re-deriving scikit-fda's surface.

(Adapted to a documentation deliverable per orchestrator MVP note: the vertical slice is one fully-worked report area carried end-to-end — methodology + version pin + representation enumerated in the final table shape + the design-goal filter demonstrated on it — proving the schema before Plan 02 fills the remaining five areas.)
</phase_goal>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<artifacts_produced>
New markdown artifacts (no code symbols created this phase):
- `## Phase 7 — scikit-fda Capability Enumeration` section appended to `.planning/research/AUDIT-REPORT.md`, containing (this plan): the methodology subsection (version pin + D-01 verification path), the schema definition (D-03 rows + D-04 taxonomy), and the fully-enumerated `representation` area table.
- Throwaway D-01 evidence captured under `.planning/research/skfda-verify/` (venv log and/or `__version__`+`dir()` capture, OR a note recording the docs-fallback path). This is evidence, NOT the deliverable — the deliverable is the report section.
</artifacts_produced>

<tasks>

<task type="tracer">
  <name>Task 1: D-01 version verification (hybrid with graceful fallback)</name>
  <files>.planning/research/skfda-verify/verify.log, .planning/research/skfda-verify/version.txt</files>
  <read_first>
    - .planning/phases/07-scikit-fda-capability-enumeration/07-CONTEXT.md §"Version Pinning & Verification" (D-01, D-01a) and §"Claude's Discretion" (venv mechanics)
    - .planning/PROJECT.md §"Key Decisions" (scikit-fda 0.10.1 sole baseline)
  </read_first>
  <action>
    Establish the pinned scikit-fda version as this phase's runtime verification step. Attempt the runtime path first, fall back gracefully.
    PRIMARY (attempt): create a throwaway venv at `.planning/research/skfda-verify/venv` with `python3 -m venv`, activate/upgrade pip, and install the pinned package: `pip install "scikit-fda==0.10.1"`. Then capture `python -c "import skfda; print(skfda.__version__)"` and spot-check a few module listings (e.g. `import skfda.preprocessing.smoothing as s; print(sorted(dir(s)))` and the depth/ml modules) into version.txt. Tee all stdout/stderr into verify.log.
    FALLBACK (only if install fails on Python 3.14 / heavy-dependency compat — do NOT let install failure block the phase): record the failure reason in verify.log, then confirm the pin from PyPI release metadata for project `scikit-fda` (confirm 0.10.1 is the latest release) and the readthedocs 0.10.1 API reference; write the docs-derived pin and the URLs into version.txt.
    In BOTH branches, version.txt MUST state (a) the pinned version string `0.10.1`, (b) which path was used (RUNTIME or DOCS-FALLBACK) and the one-line reason, and (c) the D-01a coincidence that 0.10.1 is both the agreed baseline and the current latest PyPI release. These files are throwaway evidence, not the deliverable.
    Do NOT edit anything under fdars-core/src/. Do NOT install scikit-fda anywhere outside the throwaway venv directory.
  </action>
  <verify>
    <automated>test -f .planning/research/skfda-verify/version.txt && grep -q "0.10.1" .planning/research/skfda-verify/version.txt && grep -Eqi "RUNTIME|DOCS-FALLBACK" .planning/research/skfda-verify/version.txt</automated>
  </verify>
  <acceptance_criteria>
    - version.txt exists and contains the literal string `0.10.1`.
    - version.txt names the path used with a token matching `RUNTIME` or `DOCS-FALLBACK` and gives a one-line reason.
    - version.txt records the baseline=latest coincidence (D-01a).
    - No file under fdars-core/src/ was modified (git status shows no such change).
  </acceptance_criteria>
  <precondition>Python 3.14.5 and `python3 -m venv` are available locally, and PyPI network access is reachable (confirmed at planning time). If the network is unreachable, the DOCS-FALLBACK branch still requires reading the readthedocs 0.10.1 reference.</precondition>
  <reversibility rating="reversible">Verification-method choice; no shipped-code impact (D-01).</reversibility>
  <done>The pinned version 0.10.1 is verified and the exact D-01 path (runtime vs docs-fallback) is recorded as evidence for the methodology.</done>
</task>

<task type="tracer">
  <name>Task 2: Section header + methodology + schema definition</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - .planning/research/AUDIT-REPORT.md (READ the tail — mirror the existing `## Phase N: Title` header style, the `### Methodology` subsection convention, and the evidence-linking/table conventions used by Phases 5–6)
    - .planning/research/skfda-verify/version.txt (the D-01 verification output from Task 1)
    - .planning/research/PITFALLS.md §"Pitfall 9" (capability-not-name grouping → governs the D-03 collapse rule) and §"Pitfall 14" (the four-value Relevance taxonomy → governs D-04)
    - .planning/phases/07-scikit-fda-capability-enumeration/07-CONTEXT.md (D-03 grouping, D-04 taxonomy, D-05 single-section convention)
  </read_first>
  <action>
    Append a new top-level section `## Phase 7 — scikit-fda Capability Enumeration` to the END of AUDIT-REPORT.md, mirroring the header/methodology/evidence-link style of the existing Phase 5–6 sections.
    Write a `### Methodology` subsection that: states the pinned version 0.10.1; records which D-01 path was used (runtime venv __version__ capture or docs fallback) and why, citing .planning/research/skfda-verify/version.txt (and the readthedocs/PyPI URLs if the fallback path was taken); notes the D-01a coincidence that 0.10.1 is both the agreed sole baseline and the current latest PyPI release (preempting "is this stale?"); and states that this section promotes/refactors FEATURES.md per D-02 (scikit-fda side only, fdars parity notes stripped, reorganized under the six SC1 report areas).
    Then write a `### Capability-Row Schema` subsection defining, ONCE for the whole section: (a) the two-level D-03 structure — six report areas (representation, preprocessing, exploratory, ML, inference, misc) → task groupings → one row per distinct method/algorithm; (b) the collapse rule — a single estimator's fit/predict/transform/inverse_transform is ONE capability row (Pitfall 9), noted in a collapsed-note column; (c) the D-04 Relevance taxonomy — the four values In-Scope Algorithm / In-Scope API-Ergonomics / Out-of-Scope (plotting) / Out-of-Scope (IO); (d) the chosen table columns (Claude's Discretion — e.g. Task | Method | Collapsed calls | Relevance | Confidence | Source), which the representation table (Task 3) and Plan 02's five areas MUST reuse verbatim.
    Do NOT write any fdars-side note. Do NOT create a separate filter file. Do NOT categorize gaps (table-stakes/differentiator).
  </action>
  <verify>
    <automated>grep -Fq "## Phase 7 — scikit-fda Capability Enumeration" .planning/research/AUDIT-REPORT.md && grep -Fq "0.10.1" .planning/research/AUDIT-REPORT.md && grep -Fq "In-Scope Algorithm" .planning/research/AUDIT-REPORT.md && grep -Fq "Out-of-Scope (plotting)" .planning/research/AUDIT-REPORT.md && grep -Fq "Out-of-Scope (IO)" .planning/research/AUDIT-REPORT.md && grep -Fq "In-Scope API-Ergonomics" .planning/research/AUDIT-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - AUDIT-REPORT.md contains the exact header `## Phase 7 — scikit-fda Capability Enumeration`.
    - The methodology text contains `0.10.1` and a phrase recording both baseline-and-latest (grep for `latest` near the version pin).
    - All four D-04 taxonomy values appear verbatim: `In-Scope Algorithm`, `In-Scope API-Ergonomics`, `Out-of-Scope (plotting)`, `Out-of-Scope (IO)`.
    - The schema text states the fit/predict/transform collapse rule (grep for `fit` and `collapse` / `single row`).
    - No occurrence of `fdars has`, `fdars partial`, or `fdars equivalent` in the new section (region-scoped: from the Phase 7 header to EOF).
  </acceptance_criteria>
  <done>The Phase-7 section, methodology (version pin + D-01 path), and the reusable D-03/D-04 schema are written and consistent with the AUDIT-REPORT conventions.</done>
</task>

<task type="tracer">
  <name>Task 3: Enumerate the `representation` area in the final table shape (tracer proof)</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - .planning/research/FEATURES.md §"Area 1: Representation" (the source enumeration to promote — its `### scikit-fda Public API` table is the row source; its `### fdars Current Status` and `### Categorization` subsections are Phase-8 material and MUST be stripped)
    - .planning/research/AUDIT-REPORT.md (the schema subsection just written in Task 2 — reuse its exact columns)
    - .planning/research/PITFALLS.md §"Pitfall 14" (borderline rulings — representation type-system → Out-of-Scope refactor, but algorithmic capabilities riding on it e.g. irregular-data covariance → In-Scope Algorithm)
    - .planning/phases/07-scikit-fda-capability-enumeration/07-CONTEXT.md (D-04 borderline ruling for the representation type-system)
  </read_first>
  <action>
    Under the Phase-7 section, add a `### Area: Representation` subsection and populate the schema table (exact columns from Task 2) with ONE row per distinct representation method/capability from FEATURES.md Area 1: the data-representation types (FDataGrid, FDataBasis, FDataIrregular), the basis systems (BSpline, Fourier, Monomial, Constant, Custom, Tensor, FiniteElement, VectorValued), the interpolation/extrapolation capabilities (SplineInterpolation, the four extrapolation policies), the irregular→basis converters (MinimizeMixedEffects, EMMixedEffects), and grid-to-basis conversion.
    Apply the D-04 Relevance ruling per row: the representation TYPE-SYSTEM (FDataGrid/FDataBasis/FDataIrregular as a first-class object hierarchy) is Out-of-Scope (type-system refactor per PROJECT.md) — mark those type rows accordingly; but specific ALGORITHMIC capabilities riding on it (e.g. FDataIrregular covariance estimation, grid-to-basis conversion math, spline interpolation) are In-Scope Algorithm. Collapse each estimator's fit/predict/transform into one row in the collapsed-calls column. Cite the FEATURES.md/readthedocs source per the Source column.
    STRIP every fdars-side note during promotion — no "fdars has X / partial / equivalent", no "### fdars Current Status", no categorization. This area is the tracer: it must be a complete, publishable table proving the schema works before Plan 02 fills the other five areas.
  </action>
  <verify>
    <automated>grep -Fq "### Area: Representation" .planning/research/AUDIT-REPORT.md && grep -Fq "FDataGrid" .planning/research/AUDIT-REPORT.md && grep -Fq "BSpline" .planning/research/AUDIT-REPORT.md && ! awk '/^## Phase 7 — scikit-fda Capability Enumeration/{p=1} p' .planning/research/AUDIT-REPORT.md | grep -vE '^\s*#' | grep -Eqi 'fdars (has|partial|equivalent)'</automated>
  </verify>
  <acceptance_criteria>
    - A `### Area: Representation` subsection exists under the Phase-7 section with a table using the exact columns defined in Task 2.
    - The table has one row per distinct representation capability (FDataGrid, FDataBasis, FDataIrregular, BSpline/Fourier/Monomial/Constant/Custom/Tensor/FiniteElement/VectorValued bases, SplineInterpolation, extrapolation policies, mixed-effects converters, grid-to-basis).
    - Each row carries a Relevance value drawn from the four D-04 taxonomy values; the type-system rows are marked Out-of-Scope while riding algorithmic capabilities (e.g. FDataIrregular covariance) are In-Scope Algorithm.
    - Region-scoped negative gate passes: from the `## Phase 7` header to EOF, NO line matches `fdars (has|partial|equivalent)` (case-insensitive, comment lines excluded).
    - No `### fdars Current Status` or gap-categorization heading appears in the Phase-7 section.
  </acceptance_criteria>
  <reversibility rating="costly">The row granularity is the schema Phase 8's parity matrix keys off (D-03); changing it later forces re-mapping the matrix.</reversibility>
  <done>The representation area is fully enumerated in the final schema shape with fdars notes stripped and D-04 Relevance applied — the schema is proven end-to-end for Plan 02 to reuse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev shell → PyPI | The only external action is a throwaway local `pip install "scikit-fda==0.10.1"` into a disposable venv, for D-01 `__version__` capture. No network-exposed surface, no user input, no shipped code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | throwaway `pip install scikit-fda==0.10.1` (D-01 venv) | low | accept | Informational only: pinned, well-known PyPI package installed into a disposable local venv that is not shipped and touches no `fdars-core/src`. No ASVS-L1-relevant threat to the audit deliverable (a markdown report section). Install is optional — the DOCS-FALLBACK path avoids it entirely. |

No L1-relevant threats exist for this docs-only phase: no runtime is shipped, no network-exposed surface is created, no user input is processed, and the deliverable is a `.planning/` markdown section. The pinned-package install is recorded as informational supply-chain context, not a mitigated threat.
</threat_model>

<verification>
- `## Phase 7 — scikit-fda Capability Enumeration` header present in AUDIT-REPORT.md.
- Version `0.10.1` pinned in methodology with the D-01 path (RUNTIME | DOCS-FALLBACK) and baseline=latest note recorded.
- All four D-04 Relevance values present verbatim; D-03 collapse rule stated.
- Representation area table populated in the final schema shape.
- Region-scoped negative gate (Phase-7 header → EOF) shows no fdars-side parity note.
- `git status` shows zero changes under `fdars-core/src/`.
</verification>

<success_criteria>
The Phase-7 section exists with a verified version pin (SC2), a capability-oriented schema (SC3 groundwork), the four-value Relevance taxonomy (SC4 groundwork), and one fully-worked area (representation) proving the schema — with all fdars-side notes stripped and zero `fdars-core/src` edits.
</success_criteria>

<output>
Create `.planning/phases/07-scikit-fda-capability-enumeration/07-01-SUMMARY.md` when done.
</output>