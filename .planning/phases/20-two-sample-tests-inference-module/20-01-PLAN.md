---
phase: 20-two-sample-tests-inference-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/inference/mod.rs
  - fdars-core/src/inference/permutation.rs
  - fdars-core/src/inference/hotelling.rs
  - fdars-core/src/inference/scb.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [INF-01]
estimate:
  tokens: 78000
  raw_tokens: 52000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "fdars_core::t_perm_test, f_perm_test, two_sample_mean_test, mean_scb, scb_two_sample_test, and TestResult are reachable at the crate root without a submodule path (INF-01)"
    - "t_perm_test and f_perm_test return p ≈ 0 for clearly-separated samples and a large (near-uniform) p under the null; both are deterministic under a fixed seed"
    - "two_sample_mean_test (Hotelling T² on a shared FPC basis) rejects when group means differ and fails to reject when they coincide"
    - "mean_scb returns a band containing the true mean at ~requested coverage; scb_two_sample_test flags a real mean difference"
    - "every new public fn returns Result<_, FdarError>, validates inputs at entry, and adds NO change to any existing public signature (fanova / hotelling_t2 / scb_mean_degras signatures unchanged)"
  artifacts:
    - fdars-core/src/inference/mod.rs
    - fdars-core/src/inference/permutation.rs
    - fdars-core/src/inference/hotelling.rs
    - fdars-core/src/inference/scb.rs
  key_links:
    - "lib.rs `pub mod inference;` + crate-root `pub use inference::{...}` re-export block"
    - "permutation.rs reuses function_on_scalar integrated-F logic (k=2) without altering fanova's public signature"
    - "hotelling.rs projects both samples through regression::fdata_to_pc_1d onto a shared FPC basis, then spm::stats::hotelling_t2"
    - "scb.rs surfaces tolerance::degras::scb_mean_degras for mean_scb and the difference-of-means SCB test"
---

<objective>
Create fdars' first standalone functional-inference surface: a new `fdars-core/src/inference/` module exposing two-sample functional hypothesis tests, built by lifting/reusing existing permutation, Hotelling-T², and bootstrap-band machinery. Closes INF-01.

Purpose: Close R-parity Area 5 (Inference), currently 0/22 present, with the table-stakes two-sample tests (`fda::tperm.fd`/`Fperm.fd`, `fda.usc` mean/cov equality, `SCBmeanfd`).
Output: `inference/` module (mod.rs + permutation.rs + hotelling.rs + scb.rs), a shared `TestResult` struct, crate-root re-exports in lib.rs, all with inline `#[cfg(test)]` tests. Additive/non-breaking — zero changes to existing public signatures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

Build/test env: prefix cargo invocations with `TMPDIR=/home/simonm/.cache/fdars-bench-tmp` (doctest/linking uses /tmp which can exhaust — see project MEMORY). CODE commits must pass the gate (do NOT use --no-verify for code commits).
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-two-sample-tests-inference-module/20-CONTEXT.md

# Reuse anchors (verified file:line in CONTEXT) — read the referenced regions, do NOT re-derive the math:
@fdars-core/src/function_on_scalar.rs
@fdars-core/src/spm/stats.rs
@fdars-core/src/tolerance/degras.rs
@fdars-core/src/regression.rs
@fdars-core/src/fdata.rs
@fdars-core/src/helpers.rs
@fdars-core/src/lib.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Stand up inference/ module + TestResult + t_perm_test end-to-end</name>
  <files>fdars-core/src/inference/mod.rs, fdars-core/src/inference/permutation.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - function_on_scalar.rs — `fanova` (fn at line 771) and its private helpers `compute_group_means` (677), `pointwise_f_statistic` (710), `global_f_statistic` (748): the LCG-based permutation loop and integrated-statistic pattern to mirror.
    - fdata.rs — `mean_1d` (167) for per-sample mean curves.
    - helpers.rs — `simpsons_weights` (57) for the integration weights of the L2 statistic.
    - lib.rs — module declaration block (lines 74-125, alphabetical) and re-export blocks (e.g. tolerance block at 213) for the crate-root `pub use` convention.
  </read_first>
  <behavior>
    - TestResult { statistic: f64, p_value: f64, n_perm: usize } derives Debug, Clone, PartialEq and is serde-gated via `#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]`.
    - t_perm_test on two clearly-separated samples (e.g. data_b = data_a shifted by a large constant) returns p_value very small (assert < 0.05, ideally ≈ 1/(n_perm+1)).
    - t_perm_test on two samples drawn from the same generator (exchangeable under the null) returns a large p_value (assert > 0.1).
    - t_perm_test called twice with the same fixed seed returns bit-identical statistic and p_value (determinism).
    - Invalid input (mismatched column counts between data_a and data_b, or zero columns) returns Err(FdarError::InvalidDimension); n_perm=0 is clamped to >=1 (mirror fanova) OR rejected via InvalidParameter — pick one and assert it.
  </behavior>
  <action>
    Create `fdars-core/src/inference/mod.rs` as the module root. Define `TestResult` here (fields per behavior; add `reject: bool` at Claude's discretion only if it stays additive). Declare `mod permutation;` and re-export `pub use permutation::{t_perm_test};` (f_perm_test added in Task 2). Add module-level `//!` doc naming the R baselines (tperm.fd/Fperm.fd, fda.usc, SCBmeanfd).

    In `fdars-core/src/inference/permutation.rs` implement `t_perm_test(data_a: &FdMatrix, data_b: &FdMatrix, argvals: &[f64], n_perm: usize, seed: u64) -> Result<TestResult, FdarError>`. Statistic: integrated L2 distance between the two sample-mean curves — compute mean_a and mean_b via `fdata::mean_1d`, form the pointwise squared difference, integrate with `helpers::simpsons_weights(argvals)` (Claude's discretion: integrated |t(t)| is an acceptable alternative; L2-of-difference is the recommended form). Build the permutation null by pooling all n_a + n_b rows into one matrix, relabeling group membership via a Fisher-Yates shuffle over pooled row indices, recomputing the statistic, and counting `perm_stat >= observed`. Use `rand::rngs::StdRng::seed_from_u64(seed)` for the RNG (fdars convention) — NOT the LCG in fanova (that is fanova's internal detail; the inference surface takes an explicit seed per the locked decision). p_value = (n_ge + 1) / (n_perm + 1). Default n_perm at the call sites is 999 (document it; the param is required).

    Validate at entry: data_a and data_b must have equal, non-zero column counts (else InvalidDimension), argvals.len() must equal that column count (InvalidDimension), each sample needs >= 2 rows (InvalidDimension), n_perm handling per behavior. Return `Result<TestResult, FdarError>`. Do NOT put `#[must_use]` on this Result-returning fn (clippy::double_must_use fires under -D warnings). Add an inline `#[cfg(test)] mod tests` covering the four behaviors above, using `crate::test_helpers::uniform_grid` for argvals and a small deterministic generator for the two samples.

    Wire the module into `fdars-core/src/lib.rs`: add `pub mod inference;` in the alphabetical module block (between `gmm`/`helpers` region — place it where it sorts), and add a crate-root re-export block `pub use inference::{t_perm_test, TestResult};` (extended in later tasks). Do NOT touch any existing `pub use` line or the `fanova` re-export.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel inference::permutation 2>&1 | tail -20</automated>
  </verify>
  <done>The `inference/` module exists, `fdars_core::t_perm_test` and `fdars_core::TestResult` resolve at the crate root, and the inline t_perm_test tests (separated → p≈0, null → large p, determinism, invalid-input Err) pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: f_perm_test (k=2 integrated-F permutation) + two_sample_mean_test (Hotelling T² on shared FPC basis)</name>
  <files>fdars-core/src/inference/permutation.rs, fdars-core/src/inference/hotelling.rs, fdars-core/src/inference/mod.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - function_on_scalar.rs — `pointwise_f_statistic` (710) + `global_f_statistic` (748): the integrated-F statistic to assemble a 2-group problem around. Keep `fanova` (771) public signature unchanged.
    - spm/stats.rs — `hotelling_t2` (86): takes (scores: &FdMatrix, eigenvalues: &[f64]) -> Result<Vec<f64>>; requires ncomp columns matching eigenvalues length and positive eigenvalues.
    - regression.rs — `fdata_to_pc_1d` (287): `(data, ncomp, argvals) -> Result<FpcaResult>`; FpcaResult has `singular_values`, `rotation`, `scores`, `mean` (25-33). FpcaResult exposes `project` for reusing a fitted basis on new data.
    - spm/mfpca.rs (353-358): eigenvalue convention `eigenvalue = singular_value^2 / (n - 1)` — use this to derive eigenvalues from singular_values for hotelling_t2.
  </read_first>
  <behavior>
    - f_perm_test(data_a, data_b, n_perm, seed) on separated samples returns p ≈ 0; on null (same generator) returns large p; deterministic under fixed seed. Agrees in reject/accept decision with fanova run on the same data stacked as a 2-group problem.
    - two_sample_mean_test on two samples with clearly different mean curves rejects (returns a TestResult whose p_value is small); on two samples with coinciding means fails to reject (large p_value).
    - two_sample_mean_test validates: equal non-zero column counts, ncomp >= 1 and ncomp <= min available components, each sample >= 2 rows; returns InvalidDimension / InvalidParameter on violation.
  </behavior>
  <action>
    In `permutation.rs`, add `f_perm_test(data_a: &FdMatrix, data_b: &FdMatrix, argvals: &[f64], n_perm: usize, seed: u64) -> Result<TestResult, FdarError>`. Assemble the k=2 problem: stack data_a over data_b into one pooled FdMatrix, build integer group labels (0 for the first n_a rows, 1 for the next n_b), and compute the observed integrated-F statistic using the same integrated-F pattern as `function_on_scalar::pointwise_f_statistic` + `global_f_statistic` (k=2 case). To avoid duplicating that math, factor the reusable core out of `fanova` into a shared helper (e.g. a private `pub(crate) fn integrated_f_statistic(data, groups, labels) -> f64` in function_on_scalar.rs) and call it from BOTH `fanova` and `f_perm_test` — `fanova`'s public signature and behavior MUST stay identical (refactor only its internals; its existing tests must still pass). Permutation null: relabel via Fisher-Yates over pooled indices seeded by `StdRng::seed_from_u64(seed)`, count `perm_stat >= observed`, p = (n_ge+1)/(n_perm+1). Same validation + no-`#[must_use]` rules as Task 1.

    Create `fdars-core/src/inference/hotelling.rs` with `two_sample_mean_test(data_a: &FdMatrix, data_b: &FdMatrix, argvals: &[f64], ncomp: usize) -> Result<TestResult, FdarError>`. Project onto a shared FPC basis: pool the two samples, fit `regression::fdata_to_pc_1d(&pooled, ncomp, argvals)` to get a common basis (mean + rotation + eigenvalues), then obtain per-sample scores on that shared basis (use FpcaResult::project on each sample so both are expressed in the same coordinates). Derive eigenvalues from `FpcaResult.singular_values` via `sv*sv/(n_pooled - 1)` (the mfpca.rs convention). Form the Hotelling T² statistic on the difference of the two group score-means against the shared eigenvalues by calling `spm::stats::hotelling_t2` on a 1-row score matrix built from (mean_scores_a - mean_scores_b) scaled by the effective sample size sqrt(n_a*n_b/(n_a+n_b)) (document the scaling). Convert T² to a p_value via the chi-square(ncomp) upper tail (statrs `ChiSquared`), since T² on FPC scores is asymptotically chi-square(ncomp) — see spm usage of hotelling_t2 for the reference relationship. Return TestResult{ statistic: t2, p_value, n_perm: 0 } (n_perm=0 marks the non-permutation path). Validate inputs at entry per behavior.

    Extend `inference/mod.rs`: `pub use permutation::{t_perm_test, f_perm_test};` and `mod hotelling; pub use hotelling::two_sample_mean_test;`. Extend the lib.rs crate-root block to `pub use inference::{t_perm_test, f_perm_test, two_sample_mean_test, TestResult};`. Add inline `#[cfg(test)] mod tests` in each file covering the behaviors (f_perm separated/null/determinism + agreement-with-fanova decision; two_sample_mean_test differ→reject / coincide→fail-to-reject / invalid-input Err).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel inference:: function_on_scalar::tests::test_fanova 2>&1 | tail -25</automated>
  </verify>
  <done>f_perm_test and two_sample_mean_test resolve at the crate root and pass their statistical-sanity tests; the shared integrated-F helper is factored out and `fanova`'s existing tests still pass unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: mean_scb + scb_two_sample_test (Degras SCB reuse)</name>
  <files>fdars-core/src/inference/scb.rs, fdars-core/src/inference/mod.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - tolerance/degras.rs — `scb_mean_degras(data, argvals, bandwidth, nb, confidence, multiplier) -> Result<ToleranceBand>` (82): the band math to reuse; do NOT reimplement it.
    - tolerance/types.rs — `ToleranceBand { lower, upper, center }` (4-10) and `MultiplierDistribution` enum (38).
    - lib.rs (213) — tolerance re-export block: `scb_mean_degras`, `ToleranceBand`, `MultiplierDistribution` are already crate-root re-exported; import them via `crate::tolerance::...`.
  </read_first>
  <behavior>
    - mean_scb returns a ToleranceBand whose [lower, upper] contains a known true mean at approximately the requested coverage (test: generate curves around a known mean function, assert the true mean lies within [lower,upper] at every grid point for confidence=0.95).
    - scb_two_sample_test on two samples with a genuine mean difference returns a TestResult flagging rejection (statistic/p_value indicating the difference band excludes zero); on two samples with coinciding means it fails to reject.
    - Both validate inputs (>=3 rows for Degras, matching argvals length, bandwidth>0, nb>=1, confidence in (0,1)) and forward the relevant FdarError from scb_mean_degras.
  </behavior>
  <action>
    Create `fdars-core/src/inference/scb.rs`. Implement `mean_scb(data: &FdMatrix, argvals: &[f64], bandwidth: f64, nb: usize, confidence: f64, multiplier: MultiplierDistribution) -> Result<ToleranceBand, FdarError>` as a thin inference-facing wrapper that delegates to `crate::tolerance::scb_mean_degras` (surface it here per the locked decision; do NOT reimplement band math). Keep the signature aligned with the Degras fn so the reuse is transparent; add validation-forwarding only (the delegate already validates).

    Implement `scb_two_sample_test(data_a: &FdMatrix, data_b: &FdMatrix, argvals: &[f64], bandwidth: f64, nb: usize, confidence: f64, multiplier: MultiplierDistribution) -> Result<TestResult, FdarError>` building the SCB around the DIFFERENCE of the two mean functions: form the paired/pooled difference representation and reuse the same Degras multiplier-bootstrap machinery to produce a simultaneous band for the mean difference (construct a difference data matrix so `scb_mean_degras` produces the band around the difference, per CONTEXT: "band around the difference of means"). Reject the null of equal means when the simultaneous band for the difference excludes zero at any grid point. Return TestResult{ statistic, p_value, n_perm: 0 } where `statistic` is the max standardized excursion of the difference band from zero and `p_value` reflects the SCB decision (Claude's discretion on the exact p mapping — at minimum encode reject/accept faithfully; document the mapping). Validate inputs at entry (equal non-zero column counts, argvals length, bandwidth>0, nb>=1, confidence in (0,1)).

    Extend `inference/mod.rs`: `mod scb; pub use scb::{mean_scb, scb_two_sample_test};`. Extend the lib.rs crate-root block to `pub use inference::{t_perm_test, f_perm_test, two_sample_mean_test, mean_scb, scb_two_sample_test, TestResult};`. Do NOT add a second `pub use crate::tolerance::MultiplierDistribution` — it is already re-exported; reference the existing path in signatures. No `#[must_use]` on the Result-returning fns. Add inline `#[cfg(test)] mod tests` covering the coverage and difference-detection behaviors.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel inference::scb 2>&1 | tail -20</automated>
  </verify>
  <done>mean_scb and scb_two_sample_test resolve at the crate root; the SCB covers the true mean at ~requested coverage and the two-sample test flags a real mean difference — both via inline tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → inference API | Untrusted `FdMatrix`/`argvals`/params cross into new public fns; all must be validated at entry before any indexing or SVD/bootstrap work. |

No new external dependencies are introduced (statrs/rand/rayon already in the dependency tree). No package-manager installs → no package-legitimacy gate required.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Denial of Service | permutation.rs / scb.rs | low | mitigate | Validate row/column counts and `n_perm`/`nb` at entry; reject zero-column/degenerate input with FdarError rather than panicking (no unchecked slice indexing in loops). |
| T-20-02 | Tampering | function_on_scalar.rs (fanova refactor) | medium | mitigate | Factoring the integrated-F core out of `fanova` must not change its public signature or numerical output — gated by `fanova`'s existing inline tests staying green in Task 2's verify. |
| T-20-03 | Information Disclosure | RNG seeding | low | accept | Deterministic `StdRng::seed_from_u64(seed)` is intentional (reproducibility), not a security RNG; no secret material involved. |
</threat_model>

<verification>
Full-suite gate (run once after Task 3, from repo root):

```
TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel
TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings
```

Both MUST be green/clean. The clippy invocation lints test/bench code too (CI parity — see project MEMORY: a plain `-p ... -D warnings` misses test-code warnings). Additionally confirm crate-root reachability compiles: `t_perm_test`, `f_perm_test`, `two_sample_mean_test`, `mean_scb`, `scb_two_sample_test`, `TestResult` all resolve as `fdars_core::<name>` (exercised by the inline tests using `super::*` and by the re-export block).
</verification>

<success_criteria>
- ROADMAP Phase 20 success criteria 1-5 all satisfied: module exists + crate-root re-exports (1); t/F-perm separated→p≈0, null→large p (2); Hotelling mean test rejects on differing means, not on coinciding (3); mean_scb covers true mean + SCB two-sample flags difference (4); all fns Result-returning, input-validated, zero existing-signature changes (5).
- `fanova`, `hotelling_t2`, `scb_mean_degras` public signatures unchanged (additive/non-breaking).
- Full test suite green under `linalg,parallel`; `cargo clippy --all-targets --features linalg,parallel -- -D warnings` clean.
</success_criteria>

<output>
Create `.planning/phases/20-two-sample-tests-inference-module/20-01-SUMMARY.md` when done.
</output>
