---
phase: 09-consolidated-report-prioritized-backlog
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - .planning/research/BACKLOG.md
  - .planning/research/AUDIT-REPORT.md
autonomous: true
requirements: [RPT-01, RPT-02, RPT-03]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A consolidated audit report combines the performance findings and the gap findings, with reproducible evidence attached to each finding (benchmark numbers, allocation counts, or doc/source references under .planning/research/bench/)"
    - "A prioritized backlog phrases each item as a candidate requirement/phase, ranked by user value not ease (heuristic value / sqrt(effort)), ready to promote via /gsd-new-milestone"
    - "Every backlog item passes a completeness checklist: location/area, current cost or gap, root cause, proposed direction, severity (P1/P2/P3), effort estimate (S/M/L), and an evidence link"
  artifacts:
    - "BACKLOG.md item blocks for all performance backlog items (elastic banding, FPCA clone/truncated-SVD, parallelism P5-1..P5-3, accuracy-validation ACC-01)"
    - "AUDIT-REPORT.md Consolidated Findings Performance Findings subsection with remaining performance findings + evidence links"
  key_links:
    - "Each performance backlog item's evidence-link field resolves to a real p1_/p3_/p4_/p5_ file under .planning/research/bench/ or a doc/source reference"
    - "Each performance backlog item carries a P1/P2/P3 severity and S/M/L effort so Plan 03 can compute its value/sqrt(effort) rank"
---

<objective>
Fill the performance half of the backlog and the report's Consolidated Findings performance subsection, reusing the tracer's ranking + 7-field-checklist discipline established in Plan 01.

This is the FINAL phase of an audit-only milestone. **Deliverables are Markdown documents only — no `fdars-core` source, benchmark, or test code is edited.**

Purpose: The audit produced four performance draft-backlog slices (Phase 3 elastic alignment, Phase 4 FPCA/SVD, Phase 5 parallelism, Phase 6 faer — done in Plan 01) plus the ACC-01 accuracy-validation candidate. This plan promotes each into a full BACKLOG.md item (7 checklist fields + severity + effort) with a real evidence link, and writes the matching consolidated performance findings in the report.

Output: Performance backlog item blocks appended to `BACKLOG.md` with ranked-table rows; the remaining performance findings written into `AUDIT-REPORT.md` Consolidated Findings → Performance Findings.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# The running report (draft backlog slices to promote) and the scaffold Plan 01 created:
@.planning/research/AUDIT-REPORT.md
@.planning/research/BACKLOG.md
</context>

<artifacts_this_phase_produces>
- `BACKLOG.md` Backlog Items — new item blocks for the performance track: elastic-alignment banding defaults (Phase-3 Draft Backlog entries 1 & 2), FPCA clone-elimination + truncated-SVD (Phase-4 Draft Backlog entries 1 & 2), the three parallelism items P5-1/P5-2/P5-3 (Phase-5 SC4), and the cross-cutting numerical-accuracy-validation item ACC-01 (Phase-8 D-02a). Each carries all 7 checklist fields.
- `BACKLOG.md` Ranked Backlog — one appended row per performance item (Severity, Value, Effort, computed Score). The final global sort is Plan 03's job; this plan appends rows and computes each row's score.
- `AUDIT-REPORT.md` Consolidated Findings → Performance Findings — the remaining performance findings (elastic alignment is the top bottleneck; banding opt-in default cost; parallelism gaps; faer SVD win) each with a reproducible evidence link.

**Note on P5-4 / faer (P6-1):** P5-4 is a cross-reference to the elastic-banding item (do NOT create a duplicate item — cite the elastic-banding item ID). P6-1 already exists from Plan 01 — do NOT recreate it; this plan may add its consolidated finding text if not already present.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Promote all performance draft-backlog slices into full BACKLOG.md items with 7 fields + severity + effort + ranked rows</name>
  <files>.planning/research/BACKLOG.md</files>
  <read_first>
    - .planning/research/BACKLOG.md (Plan 01 scaffold — reuse the exact Ranking Methodology scale, the Ranked Backlog column order, and the Backlog Items block format; the P6-1 row already exists)
    - .planning/research/AUDIT-REPORT.md lines 453-473 (Phase-3 Draft Backlog: elastic banding entries 1 and 2 — Function/Current cost/Root cause/Candidate fix/Observed reduction, with p3 artifact links)
    - .planning/research/AUDIT-REPORT.md lines 587-609 (Phase-4 Draft Backlog: clone-elimination entry 1 + truncated-SVD entry 2, each with a Severity+Effort line already drafted — Low/Medium and Medium/High)
    - .planning/research/AUDIT-REPORT.md lines 734-773 (Phase-5 SC4: P5-1 CV-fold, P5-2 elastic-FPCA inner loops, P5-3 center_columns, P5-4 cross-reference — each with Function/Current cost/Root cause/Candidate direction/Projected reduction/Evidence link)
    - .planning/research/AUDIT-REPORT.md lines 2356-2376 (ACC-01 numerical-accuracy validation item — Area/Current gap/Root cause + the four fragile areas + recommended approach)
    - the referenced bench artifacts to confirm numbers are real: .planning/research/bench/p3_elastic_cross_linalg,parallel_run1.txt, .planning/research/bench/p4_fpca_linalg,parallel_run1.txt, .planning/research/bench/p4_dhat_fpca_n500_m200.txt, .planning/research/bench/p1_cv_linalg,parallel_run1.txt
  </read_first>
  <action>
    Append to `BACKLOG.md`. For EACH performance backlog item below, add a Backlog Items block carrying all 7 checklist fields (**Location/area**, **Current cost or gap**, **Root cause**, **Proposed direction**, **Severity (P1/P2/P3)**, **Effort estimate (S/M/L)**, **Evidence link**) AND append one row to the Ranked Backlog table (Rank left blank for now — Plan 03 sorts — but ID, Severity, Value, Effort, and the COMPUTED Score MUST be filled using the Plan-01 value scale and S=1/M=3/L=9 map).

    Items to create (keep IDs stable and unique):
    - PERF-ELASTIC-BAND (Phase-3 entries 1 and 2): default elastic alignment (karcher_mean + self/cross distance matrices) to a banded path / expose band_frac. Current-cost field cites the measured unbanded times and the 4-6x banded reduction. Evidence link resolves to bench/p3_elastic_cross_linalg,parallel_run1.txt (and may add p3_karcher / p3_elastic_self). This is a default-path cost affecting every caller (high user value); assign severity per the scale and justify.
    - PERF-FPCA-CLONE (Phase-4 entry 1): eliminate redundant centered.clone() + evaluate zero-copy to_dmatrix bridge. Current-cost field cites the dhat allocation figures (three O(n*m) allocations, ~0.17% copy-share). Evidence link resolves to bench/p4_dhat_fpca_n500_m200.txt. Low severity per the drafted line.
    - PERF-FPCA-TRUNCSVD (Phase-4 entry 2): truncated/thin SVD computing only ncomp components. Current-cost field cites SVD ~99.8% share + M-scaling. Evidence link resolves to bench/p4_fpca_linalg,parallel_run1.txt. Medium severity, High effort per the drafted line.
    - PERF-PAR-CV (P5-1): parallelize classification CV fold loop. Evidence link resolves to bench/p1_cv_linalg,parallel_run1.txt (plus a pointer to the SC1 thread-scaling table). Projected ~4-5x.
    - PERF-PAR-ELFPCA (P5-2): parallelize the three elastic-FPCA inner N-loops. Projected only (no dedicated measurement) — mark the current-cost field explicitly as projected, and use the SC2/SC4 report subsection as the evidence pointer.
    - PERF-PAR-CENTER (P5-3): parallelize center_columns — low priority, light body. Evidence pointer to bench/p1_fpca_linalg,parallel_run1.txt + the SC2 row.
    - ACC-VALIDATE (ACC-01): comparative fdars-vs-scikit-fda numerical-accuracy validation across the four fragile areas (B-spline round-trip, elastic level-encoding, Lomb-Scargle NaN, GMM over-split). Evidence link resolves to a doc/source reference (the AUDIT-REPORT D-02a subsection and/or the skfda venv path) — a doc reference is a valid evidence link per RPT-01.

    For P5-4 (the parallelism-to-banding cross-reference) do NOT create a new item — inside the PERF-ELASTIC-BAND block add a one-line note that it is also the P5-4 cross-referenced default-path cost.

    Every current-cost/gap field MUST cite a real number or a real source pointer read from the artifacts above — no invented figures. Assign each item's Value (1-5) and Effort (S/M/L) using the Plan-01 scale and compute Score = value / sqrt(effort); write the computed number in the Ranked-Backlog row.

    Do NOT edit any file under fdars-core/. This is a docs deliverable.
  </action>
  <verify>
    <automated>grep -q 'PERF-ELASTIC-BAND' .planning/research/BACKLOG.md && grep -q 'PERF-FPCA-CLONE' .planning/research/BACKLOG.md && grep -q 'PERF-FPCA-TRUNCSVD' .planning/research/BACKLOG.md && grep -q 'PERF-PAR-CV' .planning/research/BACKLOG.md && grep -q 'ACC-VALIDATE' .planning/research/BACKLOG.md && grep -q 'bench/p3_elastic_cross_linalg,parallel_run1.txt' .planning/research/BACKLOG.md && grep -q 'bench/p4_dhat_fpca_n500_m200.txt' .planning/research/BACKLOG.md && [ $(grep -c 'Proposed direction' .planning/research/BACKLOG.md) -ge 8 ]</automated>
  </verify>
  <acceptance_criteria>
    - BACKLOG.md contains item blocks for at least PERF-ELASTIC-BAND, PERF-FPCA-CLONE, PERF-FPCA-TRUNCSVD, PERF-PAR-CV, PERF-PAR-ELFPCA, PERF-PAR-CENTER, ACC-VALIDATE (plus the pre-existing P6-1)
    - Every one of those item blocks contains all 7 checklist fields (location/area, current cost or gap, root cause, proposed direction, severity, effort, evidence link)
    - Each performance item's evidence-link field resolves to an existing file under `.planning/research/bench/` OR a real doc/source reference (ACC-VALIDATE may use a doc reference)
    - Each current-cost/gap field cites a real number or real source pointer from the linked artifact, not an invented figure
    - Each item has one appended Ranked-Backlog row with a COMPUTED numeric Score (value / sqrt(effort))
    - No P5-4 duplicate item is created (it is a one-line cross-reference inside PERF-ELASTIC-BAND)
    - No file under `fdars-core/` is modified
  </acceptance_criteria>
  <done>All performance draft-backlog slices are promoted into full 7-field BACKLOG.md items with computed-score ranked rows and real evidence links.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write the remaining performance findings into the report Consolidated Findings section</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - .planning/research/AUDIT-REPORT.md Consolidated Findings / Performance Findings subsection (created by Plan 01 — append here, keep the one existing FPCA-SVD-dominance finding)
    - .planning/research/AUDIT-REPORT.md lines 337-473 (Phase 3 elastic-alignment results table + banded analysis + n500_m200 infeasibility note — the top-bottleneck evidence)
    - .planning/research/AUDIT-REPORT.md lines 612-773 (Phase 5 thread-scaling table + payback-N + SC2/SC3/SC4 — parallelism findings)
    - .planning/research/AUDIT-REPORT.md lines 803-844 (Phase 6 faer vs nalgebra comparison table — the SVD-swap win)
    - bench artifacts to confirm the cited numbers: .planning/research/bench/p3_elastic_cross_linalg,parallel_run1.txt, .planning/research/bench/p5_karcher_linalg,parallel_run1.txt, .planning/research/bench/p6_svd_faer_seq_linalg_run1.txt
  </read_first>
  <action>
    Append to the `## Consolidated Findings` → `### Performance Findings` subsection in `AUDIT-REPORT.md` the remaining consolidated performance findings, each stated as a one-line finding followed by a reproducible evidence link (Markdown link to a bench artifact) and the promotion-candidate backlog ID it feeds:
    - Elastic alignment is fdars' top performance bottleneck (O(N^2 * M^2) unbanded; N=500,M=200 distance matrices INFEASIBLE ~384-700 s/iter) → links bench/p3_elastic_cross_linalg,parallel_run1.txt → feeds PERF-ELASTIC-BAND.
    - The banded fast path exists but is opt-in (band_frac=0.0 default), imposing a measured 4-6x default-path penalty → cross-links the Phase-3 banded analysis → feeds PERF-ELASTIC-BAND / P5-4.
    - Parallelism: heavy loops (karcher) pay back at any N (~4.7x at 8 threads); light loops (streaming depth_batch) only pay back above N~50; three sequential loops (CV folds, elastic-FPCA inner, center_columns) are safe-to-parallelize gaps → links bench/p5_karcher_linalg,parallel_run1.txt → feeds PERF-PAR-*.
    - faer thin_svd is 1.8-4.1x faster than nalgebra SVD at real FPCA sizes (1.8x at the primary N=500,M=200 cell) and eliminates the to_dmatrix copy → links bench/p6_svd_faer_seq_linalg_run1.txt → feeds P6-1.

    Every finding MUST carry a real number and a resolving evidence link — no invented figures. Do not delete the existing FPCA-SVD-dominance finding.

    Do NOT edit any file under fdars-core/. This is a docs deliverable.
  </action>
  <verify>
    <automated>grep -q 'bench/p3_elastic_cross_linalg,parallel_run1.txt' .planning/research/AUDIT-REPORT.md && grep -q 'bench/p6_svd_faer_seq_linalg_run1.txt' .planning/research/AUDIT-REPORT.md && grep -Eiq 'top .*bottleneck|top bottleneck|primary bottleneck' .planning/research/AUDIT-REPORT.md && grep -q '### Performance Findings' .planning/research/AUDIT-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - The Performance Findings subsection contains at least four distinct findings (elastic-bottleneck, banding-opt-in cost, parallelism gaps, faer SVD win) beyond the Plan-01 FPCA-SVD-dominance finding
    - Each added finding links to an existing file under `.planning/research/bench/` and cites a real number from it
    - Each added finding names the backlog item ID it feeds (traceability from report finding to BACKLOG.md item)
    - The Plan-01 FPCA-SVD-dominance finding is still present
    - No file under `fdars-core/` is modified
  </acceptance_criteria>
  <done>The report's Performance Findings subsection carries every performance finding with a real evidence link and a pointer to its backlog item.</done>
</task>

</tasks>

<threat_model>
Docs-only audit deliverable — no code, no new attack surface; no applicable threats.
</threat_model>

<verification>
- BACKLOG.md has full 7-field item blocks for every performance item + ACC-VALIDATE, each with a resolving evidence link and a computed-score ranked row.
- AUDIT-REPORT.md Performance Findings subsection carries every performance finding with a real bench evidence link and a backlog-ID pointer.
- Every evidence link written in this plan resolves to a real file under `.planning/research/bench/` (ACC-VALIDATE may use a doc reference).
- No `fdars-core/` file touched.
</verification>

<success_criteria>
The performance half of the backlog and the report's performance findings are complete, evidence-linked, and ranked (per-row scores computed), ready for Plan 03's gap items and final global sort.
</success_criteria>

<output>
Create `.planning/phases/09-consolidated-report-prioritized-backlog/09-02-SUMMARY.md` when done.
</output>
