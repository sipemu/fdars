---
phase: 09-consolidated-report-prioritized-backlog
plan: 03
type: execute
wave: 3
depends_on: [09-01, 09-02]
files_modified:
  - .planning/research/BACKLOG.md
  - .planning/research/AUDIT-REPORT.md
autonomous: true
requirements: [RPT-01, RPT-02, RPT-03]
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A consolidated audit report combines the performance findings and the gap findings, with reproducible evidence attached to each finding (benchmark numbers, allocation counts, or doc/source references under .planning/research/bench/)"
    - "A prioritized backlog phrases each item as a candidate requirement/phase, ranked by user value not ease (heuristic value / sqrt(effort)), ready to promote via /gsd-new-milestone"
    - "Every backlog item passes a completeness checklist: location/area, current cost or gap, root cause, proposed direction, severity (P1/P2/P3), effort estimate (S/M/L), and an evidence link"
    - "At least one P1 item exists, no top-10 item is a cosmetic convenience-only entry, and the report's methodology section documents build-mode/feature-flag discipline and infra-vs-code failure triage"
  artifacts:
    - "BACKLOG.md item blocks for all in-scope gap backlog items (PREP, REPR, EXPL, ML, INF, MISC)"
    - "BACKLOG.md fully sorted Ranked Backlog master table (descending value/sqrt(effort)) with computed scores"
    - "AUDIT-REPORT.md Consolidated Findings gap-findings summary + fdars strengths summary"
    - "AUDIT-REPORT.md status header flipped to Complete"
  key_links:
    - "The Ranked Backlog table is sorted strictly by descending score and every row's score = value / sqrt(effort)"
    - "At least one item is marked P1 and no top-10 item is a cosmetic convenience-only entry"
    - "Each gap backlog item's evidence-link field resolves to an AUDIT-REPORT.md parity-table section or a phase SUMMARY"
---

<objective>
Fill the gap half of the backlog, write the gap-findings + strengths summary in the report, then perform the FINAL global ranking and the three phase-level completeness assertions that close the audit milestone.

This is the FINAL phase of an audit-only milestone. **Deliverables are Markdown documents only — no `fdars-core` source, benchmark, or test code is edited.**

Purpose: Phase 8 drafted ~20 unranked gap-backlog entries (PREP/REPR/EXPL/ML/INF/MISC). This plan promotes each into a full 7-field BACKLOG.md item, writes the consolidated gap-findings + fdars-strengths summary into the report, then sorts the complete Ranked Backlog by descending value/sqrt(effort) and asserts: at least one P1 exists, no top-10 item is cosmetic-only, and the ordering is strictly descending. It also flips the report status to Complete — closing the milestone.

Output: All gap backlog item blocks + ranked rows in `BACKLOG.md`; the final sorted master table; the gap-findings + strengths summary in the report; report status = Complete.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# The running report (drafted gap backlog + strengths sweep + gap counts) and the backlog built by Plans 01-02:
@.planning/research/AUDIT-REPORT.md
@.planning/research/BACKLOG.md
</context>

<artifacts_this_phase_produces>
- `BACKLOG.md` Backlog Items — full 7-field item blocks for every drafted in-scope gap entry: PREP-01..PREP-09, REPR-01..REPR-04, EXPL-01..EXPL-03, ML-01..ML-02, INF-01..INF-02, MISC-01..MISC-04 (the Phase-8 draft entries; ACC-01 was already promoted as ACC-VALIDATE in Plan 02 — do NOT duplicate it).
- `BACKLOG.md` Ranked Backlog — the FINAL master table, fully sorted by descending Score, Rank column filled 1..N, every row with a computed score.
- `AUDIT-REPORT.md` Consolidated Findings → Gap Findings — a summary of the actionable in-scope gap picture (82 in-scope gaps: 36 table-stakes / 46 differentiator; 32 out-of-scope excluded) with a pointer to the Phase-8 parity tables as evidence.
- `AUDIT-REPORT.md` Consolidated Findings → fdars Strengths — a summary of the 30 fdars-exclusive/advantaged capabilities (4 SC3 headliners + the rest) so the audit reflects strengths, not only gaps.
- `AUDIT-REPORT.md` header status flipped to "Complete".
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Promote every drafted gap entry into a full 7-field BACKLOG.md item with a ranked row</name>
  <files>.planning/research/BACKLOG.md</files>
  <read_first>
    - .planning/research/BACKLOG.md (Plans 01-02 output — reuse the exact Ranking Methodology scale, the Ranked Backlog columns, and the Backlog Items block format)
    - .planning/research/AUDIT-REPORT.md lines 2140-2352 (the full Drafted Gap Backlog: PREP-01..09, REPR-01..04, EXPL-01..03, ML-01..02, INF-01..02, MISC-01..04 — each with Area / Current gap / Root cause and a table-stakes-vs-differentiator category)
    - .planning/research/AUDIT-REPORT.md lines 2043-2069 (the actionable in-scope gap counts + category split — informs value assignment: table-stakes gaps are higher user value than differentiators)
  </read_first>
  <action>
    Append to `BACKLOG.md`. For EACH drafted gap entry (PREP-01..09, REPR-01..04, EXPL-01..03, ML-01..02, INF-01..02, MISC-01..04) add a Backlog Items block carrying all 7 checklist fields:
    - **Location/area** — the fdars module/file named in the draft root-cause (e.g. smoothing.rs, basis/, depth/) plus the scikit-fda area.
    - **Current cost or gap** — the draft's Current-gap text (which scikit-fda capability is absent/partial).
    - **Root cause** — the draft's root-cause text.
    - **Proposed direction** — phrase as a GSD-ready candidate requirement/phase (e.g. "Add AIC/FPE/Shibata/Rice bandwidth-selection criteria to smoothing.rs").
    - **Severity (P1/P2/P3)** — map from category: table-stakes gaps that block common workflows to P1 or P2; differentiators to P2 or P3. Justify each in one clause.
    - **Effort estimate (S/M/L)** — infer from the root cause (thin wrapper over existing code = S; new criterion formulas / API layer = M; new algorithm / mesh structure / mixed-effects solver = L). The drafts often state the algorithmic weight explicitly.
    - **Evidence link** — a Markdown link to the specific parity-table section in AUDIT-REPORT.md (e.g. the Preprocessing parity subsection) or the Phase-8 SUMMARY. A doc/source reference is a valid evidence link per RPT-01 (gap findings have no benchmark artifact).

    Append one Ranked-Backlog row per item with ID, Severity, Value (1-5 per the Plan-01 scale — table-stakes higher than differentiator), Effort (S/M/L), and the COMPUTED Score = value / sqrt(effort).

    Do NOT re-add ACC-VALIDATE (already promoted in Plan 02). Every field MUST come from the drafted entry or the parity tables — no invented gaps. Do NOT edit any file under fdars-core/. This is a docs deliverable.
  </action>
  <verify>
    <automated>grep -q 'PREP-01' .planning/research/BACKLOG.md && grep -q 'PREP-09' .planning/research/BACKLOG.md && grep -q 'REPR-04' .planning/research/BACKLOG.md && grep -q 'EXPL-03' .planning/research/BACKLOG.md && grep -q 'ML-02' .planning/research/BACKLOG.md && grep -q 'INF-02' .planning/research/BACKLOG.md && grep -q 'MISC-04' .planning/research/BACKLOG.md</automated>
  </verify>
  <acceptance_criteria>
    - BACKLOG.md contains item blocks for all drafted gap IDs: PREP-01..09, REPR-01..04, EXPL-01..03, ML-01..02, INF-01..02, MISC-01..04
    - Every gap item block contains all 7 checklist fields (location/area, current cost or gap, root cause, proposed direction, severity, effort, evidence link)
    - Each gap item's evidence-link field resolves to a real AUDIT-REPORT.md parity section or a phase SUMMARY
    - Each gap item's severity is derived from its table-stakes/differentiator category (table-stakes weighted higher)
    - Each gap item has one appended Ranked-Backlog row with a COMPUTED numeric Score
    - ACC-VALIDATE is NOT duplicated (it was created in Plan 02)
    - No file under `fdars-core/` is modified
  </acceptance_criteria>
  <done>Every drafted gap entry is a full 7-field BACKLOG.md item with a computed-score ranked row and a resolving evidence link.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Sort the master Ranked Backlog and assert the three phase-level completeness gates</name>
  <files>.planning/research/BACKLOG.md</files>
  <read_first>
    - .planning/research/BACKLOG.md (all rows from Plans 01-03 — the Ranked Backlog table now holds every performance + gap + ACC-VALIDATE row with computed scores; the Completeness Gate section from Plan 01)
  </read_first>
  <action>
    In `BACKLOG.md`, rewrite the `## Ranked Backlog` table as the FINAL master table: sort ALL rows strictly by descending Score (value / sqrt(effort)); fill the Rank column 1..N in that order. Every row must show ID, Title, Severity, Value, Effort, and the computed Score.

    Then satisfy and record the three phase-level completeness assertions in the `## Completeness Gate` section (deferred from Plan 01):
    - **At least one P1 item exists.** If no item currently carries P1, promote the clear highest-value default-path/table-stakes blocker (the elastic-banding default-path cost PERF-ELASTIC-BAND is the canonical P1 candidate — it penalizes every default caller and blocks n500+ workloads) and document the promotion rationale. Record which item(s) are P1.
    - **No top-10 ranked item is a cosmetic convenience-only entry.** Inspect ranks 1-10; for each, confirm it is a real capability/perf gap, not a cosmetic convenience. If a cosmetic-only entry landed in the top 10 (e.g. a pure API-ergonomics wrapper with no capability/perf value), re-check its value assignment and demote it. Record the top-10 review verdict (one line per top-10 item confirming non-cosmetic).
    - **Ordering is strictly descending by score.** Confirm each row's score is <= the row above it.

    Do NOT edit any file under fdars-core/. This is a docs deliverable.
  </action>
  <verify>
    <automated>grep -Eiq 'at least one P1' .planning/research/BACKLOG.md && grep -q 'P1' .planning/research/BACKLOG.md && grep -Eiq 'top.?10' .planning/research/BACKLOG.md && grep -Eiq 'descending' .planning/research/BACKLOG.md</automated>
  </verify>
  <acceptance_criteria>
    - The `## Ranked Backlog` table Rank column is filled 1..N and rows are ordered by non-increasing Score
    - At least one backlog item is marked P1, and the Completeness Gate records which item(s) and the promotion rationale if one was promoted
    - The Completeness Gate records a per-item top-10 non-cosmetic review verdict
    - The Completeness Gate records that the ordering is strictly descending by score
    - No file under `fdars-core/` is modified
  </acceptance_criteria>
  <done>The master Ranked Backlog is sorted 1..N by descending score; a P1 item exists; the top-10 is confirmed non-cosmetic; the strictly-descending order is recorded.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write the gap-findings + fdars-strengths summary and flip the report to Complete</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - .planning/research/AUDIT-REPORT.md Consolidated Findings section (Plans 01-02 output — append Gap Findings and fdars Strengths subsections here)
    - .planning/research/AUDIT-REPORT.md lines 2002-2069 (gap counts: 82 in-scope / 32 out-of-scope, 36 table-stakes / 46 differentiator by area)
    - .planning/research/AUDIT-REPORT.md lines 2073-2124 (reverse-parity strengths sweep: 30 fdars-exclusive/advantaged capabilities, 22 none / 8 partial-advantage, 4 SC3 headliners)
    - .planning/research/AUDIT-REPORT.md lines 1-8 (header status line — flip to Complete)
  </read_first>
  <action>
    Append to `## Consolidated Findings` in `AUDIT-REPORT.md`:
    - A `### Gap Findings` subsection summarizing the actionable in-scope gap picture: 82 in-scope gaps (36 table-stakes + 46 differentiator), with the 32 out-of-scope (plotting/IO/type-system) explicitly excluded from the actionable count (Pitfall 14). Attach the Phase-8 gap-counts parity subsection as the evidence pointer, and cross-reference the gap backlog items (PREP/REPR/EXPL/ML/INF/MISC) as the promotion candidates.
    - A `### fdars Strengths` subsection summarizing the 30 fdars-exclusive/advantaged capabilities (the 4 SC3 headliners — model explainability, SPM/control charts, seasonal decomposition, streaming depth — plus the D-04 list), so the audit reflects strengths, not only gaps (GAP-04 carried into the consolidated report). Attach the reverse-parity strengths-sweep section as the evidence pointer.

    Then flip the report header status line to "Complete" (e.g. "Status: Complete — audit milestone v0.14.0"). Do NOT delete any existing section.

    Do NOT edit any file under fdars-core/. This is a docs deliverable.
  </action>
  <verify>
    <automated>grep -q '### Gap Findings' .planning/research/AUDIT-REPORT.md && grep -q '### fdars Strengths' .planning/research/AUDIT-REPORT.md && grep -Eiq 'Status:.*Complete' .planning/research/AUDIT-REPORT.md && grep -q '## Phase 8' .planning/research/AUDIT-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - AUDIT-REPORT.md Consolidated Findings contains a `### Gap Findings` subsection summarizing 82 in-scope gaps (36 table-stakes / 46 differentiator) with the 32 out-of-scope explicitly excluded, and a pointer to the Phase-8 parity/gap-count section as evidence
    - AUDIT-REPORT.md Consolidated Findings contains a `### fdars Strengths` subsection summarizing the 30 fdars-exclusive/advantaged capabilities including the 4 SC3 headliners, with a pointer to the reverse-parity strengths sweep as evidence
    - The report header status line reads "Complete"
    - All prior `## Phase N` sections still exist (spot-check `## Phase 8`)
    - No file under `fdars-core/` is modified
  </acceptance_criteria>
  <done>The report carries a gap-findings summary and an fdars-strengths summary (both evidence-linked), and its status header reads Complete — the audit milestone deliverable is closed.</done>
</task>

</tasks>

<threat_model>
Docs-only audit deliverable — no code, no new attack surface; no applicable threats.
</threat_model>

<verification>
- BACKLOG.md holds full 7-field item blocks for every in-scope gap entry, each with a resolving evidence link and a ranked row.
- The Ranked Backlog master table is sorted 1..N by descending value/sqrt(effort); at least one P1 exists; the top-10 is confirmed non-cosmetic; strictly-descending order recorded.
- AUDIT-REPORT.md carries the gap-findings + fdars-strengths summaries (evidence-linked) and status = Complete, with all prior phase sections intact.
- No `fdars-core/` file touched.
</verification>

<success_criteria>
The consolidated audit report is complete (methodology + performance findings + gap findings + strengths, all evidence-linked) and the standalone BACKLOG.md carries a fully-ranked, 7-field-checklist-complete, promotion-ready backlog with at least one P1 and a non-cosmetic top-10 — satisfying RPT-01/02/03 and all four ROADMAP success criteria.
</success_criteria>

<output>
Create `.planning/phases/09-consolidated-report-prioritized-backlog/09-03-SUMMARY.md` when done.
</output>