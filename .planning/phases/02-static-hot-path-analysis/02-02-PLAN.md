---
phase: 02-static-hot-path-analysis
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - .planning/research/AUDIT-REPORT.md
autonomous: true
requirements: [PERF-01]
estimate:
  tokens: 70000
  raw_tokens: 38000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "The `### Complexity Table` contains rows for ALL 6 modules (elastic alignment [from Plan 01], FPCA/SVD, depth & distance, CV loops, streaming depth, smoothing), each with Big-O in N and M and a feature-gate tag — `grep -c \"O(n\" .planning/research/AUDIT-REPORT.md` ≥ 6"
    - "The `### Allocation Hotspot List` enumerates all 8 production `to_dmatrix()` SVD sites (adding regression.rs:298 and spm/mfpca.rs:336 to the 6 elastic ones), the 14 `DMatrix::from_column_slice` basis sites as a DISTINCT category, and the redundant `weighted.clone()` at regression.rs:291 — `grep -c \"to_dmatrix\" .planning/research/AUDIT-REPORT.md` ≥ 8; `matrix.rs:682` absent"
    - "The `### Parallelism Gap List` flags cv.rs:76 (fold loop) as a SEQUENTIAL gap and labels streaming fraiman_muniz.rs:82, mbd.rs:76, smoothing.rs:110 as ALREADY PARALLEL — no already-parallel loop is mislabeled a gap (no false positives feeding Phase 5)"
    - "Every finding across all 3 lists carries exactly one feature-gate annotation ([parallel-gated] / [sequential] / [linalg-gated] / [always])"
    - { statement: "The static fraiman_muniz_1d depth module parallelism status is determined by grepping depth/mod.rs (research Open Question 1) BEFORE writing the depth row/label — labeled ALREADY PARALLEL or SEQUENTIAL gap per the grep result, never guessed", verification: backstop }
    - { statement: "Every complexity-table row cites ≥1 real file:line that still exists in fdars-core/src/, and no loop labeled SEQUENTIAL is wrapped in a parallelism macro at its cited line", verification: backstop }
  artifacts:
    - .planning/research/AUDIT-REPORT.md
  key_links:
    - "Rows append into the three sub-section tables created by Plan 01; the report's Phase 2 section is the sole input to Phases 3–6 hot-path benchmarking and Phase 9 backlog ranking"
---

<objective>
**As a** future-milestone planner, **I want to** the Phase 2 static hot-path map completed across all six hot-path modules, **so that** every downstream benchmark phase (3–6) starts from a source-anchored priority map of complexity, allocation, and parallelism candidates.

This is the **expansion slice**. Plan 01 proved the three-list format end-to-end on elastic alignment. This plan fills in the remaining 5 modules (FPCA/SVD, depth & distance, CV loops, streaming depth, smoothing) across the same three tables, adds the two non-elastic `to_dmatrix()` SVD sites, the 14 `DMatrix::from_column_slice` basis sites as a distinct category, and the redundant `weighted.clone()`, then closes both research Open Questions with explicit source greps.

Purpose: Complete PERF-01 — all 6 modules present in all 3 lists with feature-gate annotations and no false-positive parallelism gaps.
Output: The completed Phase 2 section of AUDIT-REPORT.md (all SC1–SC4 greps pass).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-static-hot-path-analysis/02-RESEARCH.md
@.planning/phases/02-static-hot-path-analysis/02-VALIDATION.md
@.planning/research/AUDIT-REPORT.md
@.planning/phases/02-static-hot-path-analysis/02-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Analysis-only. No `fdars-core` source symbols created. The only artifact is additional markdown rows appended into the three Phase 2 sub-sections of `.planning/research/AUDIT-REPORT.md` (created by Plan 01):
- `### Complexity Table` — adds 5 rows (FPCA/SVD, depth & distance, CV loops, streaming depth, smoothing).
- `### Allocation Hotspot List` — adds regression.rs:298 + spm/mfpca.rs:336 SVD sites, the 14 `from_column_slice` basis sites (distinct category), and the regression.rs:291 redundant clone.
- `### Parallelism Gap List` — adds cv.rs:76 (gap), streaming fraiman_muniz.rs:82/mbd.rs:76 (already parallel), smoothing.rs:110 (already parallel), depth static FM (per Open-Question grep), plus the FPCA sequential `center_columns` note.
No new files (D-05: single growing report).
</artifacts_this_phase_produces>

<schema_note>
N/A — no schema surface (numeric Rust library, no ORM/DB).
</schema_note>

<tasks>

<task type="auto">
  <name>Task 1: Complete the Complexity Table (5 remaining module rows) + resolve the depth Open Question</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - fdars-core/src/regression.rs (READ lines ~165–322: center_columns sequential double loop at 167–181, weighted.clone() at 291, SVD::new(weighted.to_dmatrix(),…) at 298 — for the FPCA/SVD row Big-O and feature gate)
    - fdars-core/src/depth/mod.rs (GREP + READ: run `grep -n "iter_maybe_parallel\|maybe_par_chunks\|fn fraiman_muniz_1d\|for .*0\.\." fdars-core/src/depth/mod.rs` to resolve RESEARCH Open Question 1 — whether the STATIC fraiman_muniz_1d uses a parallelism macro or a plain sequential loop — BEFORE writing the depth row's feature-gate tag)
    - fdars-core/src/metric/lp.rs (READ ~57, ~105: lp_cross_1d / lp_self_1d both use iter_maybe_parallel! — distance-matrix O(n²·m) anchor)
    - fdars-core/src/classification/cv.rs (READ ~45–120: for fold in 0..nfold at 76, per-fold cost = fdata_to_pc_1d + classifier fit/predict)
    - fdars-core/src/streaming_depth/fraiman_muniz.rs (READ ~77–90: iter_maybe_parallel!(0..nobj) at 82)
    - fdars-core/src/smoothing.rs (READ ~72, ~100–130: slice_maybe_parallel!(x_new) at 110, sequential inner for i in 0..n)
    - .planning/phases/02-static-hot-path-analysis/02-RESEARCH.md §4 Modules 2–6 + §Open Questions (anchors + the two questions to resolve)
    - .planning/research/AUDIT-REPORT.md (the Complexity Table header + elastic row from Plan 01 — match column order)
  </read_first>
  <action>
    Using the Edit tool, append 5 data rows to the `### Complexity Table` (one per remaining module), each stating N complexity and M complexity separately and citing ≥1 real file:line:
    - **FPCA/SVD**: `fdata_to_pc_1d` (`regression.rs:212`); centering O(n·m) sequential (`regression.rs:167`), SVD O(min(n,m)·n·m) (`regression.rs:298`); Feature gate = `[sequential]` (center_columns + nalgebra SVD both sequential regardless of `parallel`). Note the sequential `center_columns` (NOT the parallel `fdata.rs:center_1d` — RESEARCH Pitfall 1).
    - **Depth & distance**: `fraiman_muniz_1d` (`depth/mod.rs:<line from your grep>`); FM depth O(n_obj·n_ref·m), distance matrix O(n²·m) (`metric/lp.rs:57/105`); Feature gate = derived from the Open-Question-1 grep result (`[parallel-gated]` if macro-wrapped, `[sequential]` if a plain loop). State which you found.
    - **CV loops**: `fclassif_cv` (`classification/cv.rs:45`); O(nfold · per-fold FPCA+fit+predict), fold loop sequential (`cv.rs:76`); Feature gate = `[sequential]` (fold loop) — note inner FPCA is also sequential.
    - **Streaming depth**: `StreamingFraimanMuniz::depth_batch` (`streaming_depth/fraiman_muniz.rs:77`); O(n_obj·n_ref·log n_ref) query + O(n_ref·m·log n_ref) build; Feature gate = `[parallel-gated]` (`iter_maybe_parallel!` at :82).
    - **Smoothing**: `nadaraya_watson` (`smoothing.rs:72`); O(n_pred·n_train) kernel evals; outer loop `[parallel-gated]` (`slice_maybe_parallel!` at :110), inner `for i in 0..n` sequential.

    Point to RESEARCH §4 for the derivations; confirm loop nesting from the source reads. Author prose fresh; no fenced code in the table.
  </action>
  <verify>
    <automated>test $(grep -c "O(n" .planning/research/AUDIT-REPORT.md) -ge 6 && grep -q "regression.rs:298" .planning/research/AUDIT-REPORT.md && grep -q "cv.rs:76" .planning/research/AUDIT-REPORT.md && grep -q "smoothing.rs:110" .planning/research/AUDIT-REPORT.md && grep -q "streaming_depth/fraiman_muniz.rs:82" .planning/research/AUDIT-REPORT.md</automated>
    <human-check>Confirm the depth-module row's feature-gate tag matches the actual `grep -n iter_maybe_parallel fdars-core/src/depth/mod.rs` result for the STATIC fraiman_muniz_1d path (Open Question 1), and each row cites a line that still exists.</human-check>
  </verify>
  <done>Complexity Table has all 6 module rows; `grep -c "O(n"` ≥ 6; each new row cites a real file:line; the depth row's gate tag is justified by an explicit grep of depth/mod.rs; FPCA row cites the sequential center_columns not the parallel center_1d.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the Allocation Hotspot List (SVD sites + from_column_slice category + redundant clone)</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - fdars-core/src/regression.rs (READ ~291, ~298: the `weighted.clone()` at 291 and `SVD::new(weighted.to_dmatrix(),…)` at 298 — RESEARCH §2A/§2C)
    - fdars-core/src/spm/mfpca.rs (READ ~325–356: `SVD::new(stacked.to_dmatrix(),…)` at 336)
    - .planning/phases/02-static-hot-path-analysis/02-RESEARCH.md §2A (the 8 production to_dmatrix() rows — 2 not yet added), §2B (the 14 DMatrix::from_column_slice basis sites, with exact file:line list), §2C (regression.rs:291 redundant clone), and Pitfall 2 (exclude matrix.rs:682)
    - .planning/research/AUDIT-REPORT.md (the Allocation Hotspot List + elastic entries from Plan 01)
  </read_first>
  <action>
    Using the Edit tool, complete the `### Allocation Hotspot List`:

    (a) Add the 2 remaining production `to_dmatrix()` SVD sites (Category = `to_dmatrix() SVD copy`, Feature gate = `[always]`, Phase target = Phase 4 dhat): `regression.rs:298` (core FPCA `fdata_to_pc_1d`, always executed) and `spm/mfpca.rs:336` (multivariate FPCA `stacked`). Together with Plan 01's 6 elastic sites this totals **8 production SVD sites**. Add a one-line summary asserting "8 production `to_dmatrix()` SVD sites (ROADMAP's '8' confirmed; the 9th at `matrix.rs:682` is `#[cfg(test)]`, excluded)".

    (b) Add a DISTINCT sub-category "DMatrix::from_column_slice — basis construction (NOT SVD copies)" listing all 14 sites from RESEARCH §2B: `smooth_basis.rs:198,199,695,696`, `seasonal/ssa.rs:178`, `basis/auto_select.rs:95,128`, `basis/fourier_fit.rs:68`, `basis/projection.rs:113`, `elastic_regression/regression.rs:274,278`, `elastic_regression/scalar_on_shape.rs:117,119`, `basis/pspline.rs:87`. Category = `from_column_slice basis`, Phase target = Phase 4 (secondary). Tag each with its feature gate (`[always]` unless it is inside a `linalg`-gated path — check and mark `[linalg-gated]` if so). Note explicitly these are least-squares/basis constructions, a different optimization path from the SVD copies (RESEARCH Pitfall 5).

    (c) Add the redundant clone entry: `regression.rs:291` — `weighted = centered.clone()`, Category = `redundant clone`, size n×m, Feature gate = `[always]`, note it is a double-size allocation (centered + weighted both n×m) and a zero-copy candidate.

    Do NOT list `matrix.rs:682`. Point to RESEARCH §2 for anchors; author prose fresh; no fenced code in tables.
  </action>
  <verify>
    <automated>test $(grep -c "to_dmatrix" .planning/research/AUDIT-REPORT.md) -ge 8 && test $(grep -c "from_column_slice" .planning/research/AUDIT-REPORT.md) -ge 1 && grep -q "regression.rs:298" .planning/research/AUDIT-REPORT.md && grep -q "spm/mfpca.rs:336" .planning/research/AUDIT-REPORT.md && grep -q "regression.rs:291" .planning/research/AUDIT-REPORT.md && grep -q "smooth_basis.rs:198" .planning/research/AUDIT-REPORT.md && ! grep -q "matrix.rs:682" .planning/research/AUDIT-REPORT.md</automated>
  </verify>
  <done>All 8 production `to_dmatrix()` SVD sites listed (`grep -c to_dmatrix` ≥ 8); the 14 `from_column_slice` basis sites listed as a distinct category; the regression.rs:291 redundant clone listed; `matrix.rs:682` absent; each entry carries a feature-gate tag.</done>
</task>

<task type="auto">
  <name>Task 3: Complete the Parallelism Gap List + final SC1–SC4 grep pass</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - fdars-core/src/classification/cv.rs (READ ~76: confirm `for fold in 0..nfold` is a plain loop with NO parallelism macro — the gap)
    - fdars-core/src/streaming_depth/mbd.rs (READ ~76: confirm `StreamingMBD::depth_batch` uses `iter_maybe_parallel!` — already parallel)
    - fdars-core/src/streaming_depth/fraiman_muniz.rs (READ ~82: already confirmed parallel — already-parallel entry)
    - fdars-core/src/smoothing.rs (READ ~110: `slice_maybe_parallel!(x_new)` outer already parallel, inner sequential)
    - fdars-core/src/regression.rs (READ ~167: center_columns plain sequential double loop — the FPCA centering gap; contrast with the parallel fdata.rs:center_1d per RESEARCH Pitfall 1)
    - .planning/phases/02-static-hot-path-analysis/02-RESEARCH.md §3 (all sub-parts) + §5 (SC3/SC4 checks) + 02-VALIDATION.md Manual-Only Verifications (the no-false-positive cross-check)
    - .planning/research/AUDIT-REPORT.md (Parallelism Gap List + elastic entries from Plan 01)
  </read_first>
  <action>
    Using the Edit tool, complete the `### Parallelism Gap List` with the non-elastic modules:
    - **cv.rs:76** — Status = SEQUENTIAL, macro = none, tag = `[sequential]`, Gap = Yes. Note each fold is independent → safe `iter_maybe_parallel!` candidate (Phase 5); note the thread-safe RNG pattern is not needed here (no RNG in fold loop).
    - **regression.rs:167** (`center_columns`) — Status = SEQUENTIAL, macro = none, tag = `[sequential]`, Gap = Yes (FPCA centering). Explicitly note this is the sequential `center_columns` called by `fdata_to_pc_1d`, NOT the parallel `fdata.rs:center_1d` (RESEARCH Pitfall 1).
    - **streaming_depth/fraiman_muniz.rs:82** — Status = ALREADY PARALLEL, macro = `iter_maybe_parallel!`, tag = `[parallel-gated]`, Gap = No.
    - **streaming_depth/mbd.rs:76** — Status = ALREADY PARALLEL, macro = `iter_maybe_parallel!`, tag = `[parallel-gated]`, Gap = No.
    - **smoothing.rs:110** — Status = ALREADY PARALLEL (outer over prediction points via `slice_maybe_parallel!`), inner `for i in 0..n` sequential, tag = `[parallel-gated]`, Gap = No (outer). Note the inner training-point loop is O(n_train) sequential but low-value.
    - **depth/mod.rs static FM** — Status derived from Task 1's Open-Question-1 grep (ALREADY PARALLEL if macro-wrapped, else SEQUENTIAL gap); tag accordingly. Keep it consistent with the depth complexity-table row.

    Before finalizing, for EACH entry labeled SEQUENTIAL/gap, run `grep -n "iter_maybe_parallel\|slice_maybe_parallel\|maybe_par_chunks" fdars-core/src/<file>` near the cited line and confirm NO macro wraps it (02-VALIDATION.md Manual-Only Verification — no false positives feeding Phase 5).

    Then run the final SC1–SC4 grep pass from 02-VALIDATION.md and record the counts in a short "SC verification" note at the end of the Parallelism Gap List (SC1 `grep -c "O(n"` ≥ 6, SC2 `grep -c "to_dmatrix"` ≥ 8, SC3/SC4 gate-tag coverage). Point to RESEARCH §3; author prose fresh; no fenced code in tables.
  </action>
  <verify>
    <automated>grep -q "cv.rs:76" .planning/research/AUDIT-REPORT.md && grep -q "streaming_depth/mbd.rs:76" .planning/research/AUDIT-REPORT.md && grep -q "regression.rs:167" .planning/research/AUDIT-REPORT.md && test $(grep -Eci "already parallel" .planning/research/AUDIT-REPORT.md) -ge 4 && test $(grep -Eoc "\[parallel-gated\]|\[sequential\]|\[linalg-gated\]|\[always\]" .planning/research/AUDIT-REPORT.md) -ge 10 && test $(grep -c "O(n" .planning/research/AUDIT-REPORT.md) -ge 6 && test $(grep -c "to_dmatrix" .planning/research/AUDIT-REPORT.md) -ge 8</automated>
    <human-check>Confirm no entry labeled SEQUENTIAL is wrapped in a parallelism macro at its cited line, and that the depth-module status is consistent between the complexity table and the gap list (both derived from the same Open-Question-1 grep).</human-check>
  </verify>
  <done>Parallelism Gap List is complete: cv.rs:76 and regression.rs:167 flagged SEQUENTIAL gaps; streaming fraiman_muniz.rs:82, mbd.rs:76, smoothing.rs:110 labeled ALREADY PARALLEL; depth static-FM status consistent with the complexity row; every finding carries a gate tag; and the final SC1–SC4 grep counts pass (O(n ≥6, to_dmatrix ≥8, ≥4 already-parallel labels, ≥10 gate tags).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. Read-only static analysis of local `fdars-core/src/` files writing markdown to a local file. No network, no user input, no secrets, no auth, no `fdars-core` code execution, no package installs.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | — | Read-only local source analysis → local markdown | n/a | accept | No applicable threats (read-only local analysis; no attack surface). ASVS L1 categories V2–V6 all N/A per 02-RESEARCH.md Security Domain. |
</threat_model>

<verification>
Final phase-level SC greps against `.planning/research/AUDIT-REPORT.md` (from 02-VALIDATION.md):
- SC1: `grep -c "O(n" .planning/research/AUDIT-REPORT.md` ≥ 6 (all 6 module complexity rows).
- SC2: `grep -c "to_dmatrix" .planning/research/AUDIT-REPORT.md` ≥ 8 (all production SVD sites); `from_column_slice` present; `matrix.rs:682` absent.
- SC3: `grep -Eic "sequential|gap candidate|already parallel" .planning/research/AUDIT-REPORT.md` ≥ expected; cv.rs:76 flagged, streaming/smoothing labeled already-parallel.
- SC4: `grep -Eoc "\[parallel-gated\]|\[sequential\]|\[linalg-gated\]|\[always\]"` ≥ 10; no macro-wrapped loop mislabeled sequential.
- Provenance backstop: each cited `file:line` still exists via `grep -n` in `fdars-core/src/`.
</verification>

<success_criteria>
- All 6 modules present in the Complexity Table with N/M Big-O and gate tags.
- All 8 production `to_dmatrix()` SVD sites + 14 `from_column_slice` basis sites (distinct category) + regression.rs:291 clone listed; matrix.rs:682 excluded.
- cv.rs:76 & regression.rs:167 flagged SEQUENTIAL gaps; streaming + smoothing labeled ALREADY PARALLEL (no false positives).
- Both research Open Questions resolved by explicit source greps.
- All SC1–SC4 greps pass.
</success_criteria>

<output>
Create `.planning/phases/02-static-hot-path-analysis/02-02-SUMMARY.md` when done.
</output>