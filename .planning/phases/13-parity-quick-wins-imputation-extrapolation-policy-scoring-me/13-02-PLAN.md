---
phase: 13-parity-quick-wins-imputation-extrapolation-policy-scoring-me
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - fdars-core/src/scoring.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [FEAT-05]
estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "A caller can score functional predictions with functional_mae and functional_mse over FdMatrix, integrated over argvals, each returning Result<f64, FdarError> (FEAT-05)"
    - "A caller can score with functional_mape, functional_msle, functional_explained_variance, each returning Result<f64, FdarError> (FEAT-05)"
    - "functional_mape returns Err(InvalidParameter) when y_true contains near-zero values; functional_msle returns Err(InvalidParameter) when any input value < -1 (FEAT-05)"
    - "Shape mismatches (y_pred vs y_true, argvals vs ncols) return Err(InvalidDimension) (FEAT-05)"
    - "Each metric matches a hand-computed reference value in an inline test (FEAT-05)"
  artifacts:
    - "fdars-core/src/scoring.rs — new module with five functional_* metric fns + inline tests"
    - "fdars-core/src/lib.rs — pub mod scoring; + pub use scoring::{...} re-export block"
  key_links:
    - "All five metrics call helpers::simpsons_weights(argvals) once and apply weights across per-curve error, averaging over curves"
    - "scoring module declared and re-exported at crate root in lib.rs"
---

<objective>
Add FEAT-05: a new independent `fdars-core/src/scoring.rs` module with five functional scoring metrics — `functional_mae`, `functional_mse`, `functional_mape`, `functional_msle`, `functional_explained_variance` — each integrating a pointwise error over `argvals` via Simpson's rule and returning `Result<f64, FdarError>` with dimension/domain validation.

Purpose: Callers can score functional predictions against observations (scikit-fda MISC-04 parity gap).
Output: `scoring.rs` module, `pub mod scoring;` + crate-root re-exports, inline tests against hand-computed references.

This plan is placed in wave 2 (depends_on 13-01) SOLELY because both plans edit `lib.rs` and same-wave plans must not share a `files_modified` file. `scoring.rs` itself is fully independent of the helpers.rs work — no code dependency exists.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

## Executor guardrails (MANDATORY — bake into every command)

1. **TMPDIR prefix on ALL cargo + git commands.** System `/tmp` is ~1.9G and doctest linking fills it, producing a bogus "No space left on device" that fails the pre-commit hook. Prefix EVERY cargo and git command with `TMPDIR=/home/simonm/projects/rust/fdars/.gsd-build-tmp` (the dir already exists).
2. **Do NOT use `gsd-tools query commit` for code commits** — it does not inherit TMPDIR and the hook fails. Use plain `TMPDIR=/home/simonm/projects/rust/fdars/.gsd-build-tmp git commit -m "..."`.
3. **CI runs `clippy --all-targets --all-features -D warnings`** (stricter than local hook). New code INCLUDING `#[cfg(test)]` must be clean: `.to_vec()` not `iter().copied().collect()`; `.and_then` not `.map().flatten()`; no identity ops; no needless `map(|x| *x)`. Verify: `TMPDIR=/home/simonm/projects/rust/fdars/.gsd-build-tmp cargo clippy -p fdars-core --features linalg --all-targets -- -D warnings 2>&1 | tail -20`.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-parity-quick-wins-imputation-extrapolation-policy-scoring-me/13-CONTEXT.md
@.planning/phases/13-parity-quick-wins-imputation-extrapolation-policy-scoring-me/13-RESEARCH.md

# Source files this plan reads / creates (all VERIFIED in RESEARCH.md)
@fdars-core/src/utility.rs
@fdars-core/src/helpers.rs
@fdars-core/src/error.rs
@fdars-core/src/matrix.rs
@fdars-core/src/lib.rs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Create scoring.rs with functional_mae + functional_mse, wire into lib.rs (end-to-end)</name>
  <files>fdars-core/src/scoring.rs, fdars-core/src/lib.rs</files>
  <read_first>
    RESEARCH.md "Scoring Metrics Detail (FEAT-05)": Recommended Module Structure, Shape contract, "Integration pattern (identical for all five)", "Functional MAE Skeleton" (VERIFIED), and "lib.rs Wiring". utility.rs:15-26 integrate_simpson (integration reference). helpers.rs simpsons_weights (57-86). This is a new file — Wave 0 gap per RESEARCH ("scoring.rs does not exist, must be created in task 1").
  </read_first>
  <behavior>
    Tracer: create the module, declare it in lib.rs, re-export, and prove two metrics end-to-end before adding the rest.
    - functional_mae on a constant-error case (y_pred = y_true + c everywhere) integrates |c| over argvals and averages over curves; assert against a hand-computed value over a known uniform grid.
    - functional_mse on the same case yields c^2 integrated / averaged; assert hand-computed.
    - Shape guard: y_pred.shape() != y_true.shape() -> Err(InvalidDimension { parameter: "y_pred", .. }).
    - Shape guard: argvals.len() != y_true.ncols() -> Err(InvalidDimension { parameter: "argvals", .. }).
    - Degenerate guard: n == 0 || m < 2 -> Err(InvalidDimension).
  </behavior>
  <action>
    1. Create `fdars-core/src/scoring.rs` with module doc `//! Functional scoring metrics ...`. Imports: `use crate::helpers::simpsons_weights; use crate::FdarError; use crate::matrix::FdMatrix;`.
    2. Implement `functional_mae` exactly per the VERIFIED skeleton (dimension guards first, then `simpsons_weights(argvals)`, single pass summing `(y_true[(i,j)] - y_pred[(i,j)]).abs() * weights[j]`, return `Ok(total / n as f64)`).
    3. Implement `functional_mse` identically but with squared error `(diff * diff)`.
    4. Factor the shared shape/degenerate validation into a small private helper `fn validate_shapes(y_true, y_pred, argvals) -> Result<(usize, usize), FdarError>` returning `(n, m)` so the remaining three metrics reuse it (keeps later tasks small and avoids duplicated guards). Keep it clippy-clean.
    5. In lib.rs: add `pub mod scoring;` adjacent to the other shared-utility module decls (near `pub mod helpers;` / `pub mod utility;`), and add a NEW re-export block `pub use scoring::{ functional_explained_variance, functional_mae, functional_mape, functional_mse, functional_msle };` (all five names now so the block is stable across tasks; the not-yet-implemented ones are added in task 2 within the same file — implement all five before final commit OR stage the re-export list additions per task; simplest: add only mae+mse names here, extend in task 2).
    6. Inline `#[cfg(test)] mod tests` (use `crate::test_helpers::uniform_grid`): `test_functional_mae`, `test_functional_mse`, plus a shape-mismatch error test. Hand-compute expected integrals.
    Commit message: `feat(13-02): add scoring.rs with functional_mae + functional_mse (FEAT-05)`
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/projects/rust/fdars/.gsd-build-tmp cargo test -p fdars-core --features linalg test_functional_m 2>&1 | tail -8</automated>
  </verify>
  <done>scoring.rs exists and compiles; functional_mae/mse pass hand-computed tests; shape-mismatch test returns Err(InvalidDimension); module declared + names re-exported in lib.rs; clippy `--all-targets -D warnings` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add functional_mape, functional_msle, functional_explained_variance</name>
  <files>fdars-core/src/scoring.rs, fdars-core/src/lib.rs</files>
  <read_first>
    RESEARCH.md function docs for mape/msle/explained_variance; Pitfall 1 (MAPE zero denominator via NUMERICAL_EPS), Pitfall 2 (MSLE domain y > -1); Assumption A3 (explained_variance aggregates per-curve then averages). helpers.rs NUMERICAL_EPS constant.
  </read_first>
  <behavior>
    - functional_mape integrates |y_true - y_pred| / |y_true| over argvals, averaged over curves; returns Err(InvalidParameter { parameter: "y_true", .. }) if any |y_true[(i,j)]| < NUMERICAL_EPS (near-zero denominator).
    - functional_msle integrates (ln(1+y_true) - ln(1+y_pred))^2; returns Err(InvalidParameter) if any y_true or y_pred value <= -1 + NUMERICAL_EPS (log domain).
    - functional_explained_variance computes, per curve, 1 - SS_res/SS_tot where SS_res = integrated squared (residual - mean_residual) and SS_tot = integrated squared (y_true - mean_true), then averages over curves; returns ~1.0 for perfect prediction (y_pred == y_true) and handles SS_tot near-zero without panicking.
    - All three reuse the shared `validate_shapes` helper from task 1 for dimension guards.
  </behavior>
  <action>
    Add three fns to scoring.rs (reuse `validate_shapes`, `simpsons_weights`):
    1. `functional_mape`: after shape validation, pre-scan all `y_true[(i,j)]`; if any `.abs() < NUMERICAL_EPS` return InvalidParameter (parameter "y_true", message describing undefined-at-zero). Then integrate the percentage error per skeleton, average over curves.
    2. `functional_msle`: after shape validation, pre-scan y_true and y_pred; if any value `<= -1.0 + NUMERICAL_EPS` return InvalidParameter (log domain). Then integrate `(f64::ln_1p(yt) - f64::ln_1p(yp)).powi(2)`, average.
    3. `functional_explained_variance`: per curve compute integrated mean of residual and of y_true, SS_res and SS_tot via Simpson weights, `1 - SS_res/SS_tot` guarding SS_tot near-zero (return 1.0 when both SS_res and SS_tot are ~0, i.e. perfect trivial fit; document the near-zero convention). Average over curves.
    4. Extend the lib.rs `pub use scoring::{...}` block to include all five names (if task 1 only listed mae+mse, add the remaining three now).
    5. Inline tests: `test_functional_mape_zero` (Err on zero y_true), `test_functional_msle_domain` (Err on y < -1), `test_explained_variance_perfect` (== 1.0, use approx tolerance), plus a positive mape and msle hand-computed check. Keep clippy-clean (use `.powi(2)`, `f64::ln_1p`, avoid needless closures).
    Commit message: `feat(13-02): add functional_mape/msle/explained_variance (FEAT-05)`
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/projects/rust/fdars/.gsd-build-tmp cargo test -p fdars-core --features linalg test_functional_mape test_functional_msle test_explained_variance 2>&1 | tail -10</automated>
  </verify>
  <done>All five metrics implemented; mape zero-denominator and msle domain-violation return Err(InvalidParameter); explained_variance == 1.0 for perfect prediction; all five re-exported at crate root; clippy `--all-targets -D warnings` clean.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite + clippy gate for scoring module, then commit</name>
  <files>fdars-core/src/scoring.rs, fdars-core/src/lib.rs</files>
  <action>
    Final integration gate for FEAT-05. Run the full crate test suite and the strict CI-equivalent clippy across all targets. Confirm the five scoring functions are reachable from the crate root (they are re-exported). If anything fails, fix and add a follow-up commit `fix(13-02): resolve clippy/test findings in scoring module`. No new behavior — proves the module integrates cleanly and matches CI gating. Confirm lib.rs changes are additive only (new `pub mod scoring;` + one new re-export block; no edits to existing re-exports).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/projects/rust/fdars/.gsd-build-tmp cargo test -p fdars-core --features linalg 2>&1 | tail -5 && TMPDIR=/home/simonm/projects/rust/fdars/.gsd-build-tmp cargo clippy -p fdars-core --features linalg --all-targets -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <done>Full `cargo test -p fdars-core --features linalg` green; `cargo clippy --all-targets -D warnings` clean; all five scoring fns reachable from crate root; lib.rs changes additive-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → scoring fn | Untrusted `y_true`, `y_pred` FdMatrices and `argvals` cross in; may contain zeros (MAPE), out-of-domain values (MSLE), or mismatched shapes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05 | Tampering | functional_mape denominator | medium | mitigate | Pre-scan y_true for |v| < NUMERICAL_EPS; return InvalidParameter before integrating (prevents Inf/NaN leakage — V5) |
| T-13-06 | Tampering | functional_msle log domain | medium | mitigate | Pre-validate all values > -1 + NUMERICAL_EPS; return InvalidParameter (prevents NaN from ln_1p of ≤ -1) |
| T-13-07 | Information Disclosure | NaN/Inf propagation in metrics | low | mitigate | Explicit domain guards produce Err instead of silently returning misleading NaN scores |
| T-13-08 | Tampering | shape mismatch | medium | mitigate | validate_shapes entry guard -> InvalidDimension; no silent truncation (V5) |
| T-13-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new dependencies added this phase (RESEARCH: "no external crates are added") |
</threat_model>

<verification>
- `TMPDIR=/home/simonm/projects/rust/fdars/.gsd-build-tmp cargo test -p fdars-core --features linalg` passes.
- `TMPDIR=/home/simonm/projects/rust/fdars/.gsd-build-tmp cargo clippy -p fdars-core --features linalg --all-targets -- -D warnings` clean.
- All five `functional_*` names resolve from `fdars_core::` (crate root re-export).
</verification>

<success_criteria>
- FEAT-05: `functional_mae`, `functional_mse`, `functional_mape`, `functional_msle`, `functional_explained_variance` each return `Result<f64, FdarError>` with dimension validation.
- MAPE errors on near-zero y_true; MSLE errors on values ≤ -1; explained_variance == 1.0 for perfect prediction.
- Each metric matches a hand-computed reference in an inline test.
- New `scoring.rs` module declared and re-exported at crate root; fully additive.
</success_criteria>

<output>
Create `.planning/phases/13-parity-quick-wins-imputation-extrapolation-policy-scoring-me/13-02-SUMMARY.md` when done.
</output>
