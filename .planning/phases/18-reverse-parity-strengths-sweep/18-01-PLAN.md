---
phase: 18-reverse-parity-strengths-sweep
plan: "18-01"
type: execute
wave: 1
requirements: [GAP-03]
audit_only: true
deliverable: .planning/research/R-AUDIT-REPORT.md
files_modified:
  - .planning/research/R-AUDIT-REPORT.md
status: ready
---

# Phase 18 · Plan 01 — Reverse-Parity Strengths Sweep

**Goal:** Append `§Reverse-Parity-Strengths` to `.planning/research/R-AUDIT-REPORT.md` — a module-walk catalogue of fdars capabilities unique vs R or ahead of their closest R analog. Audit-only — zero `fdars-core/src/` edits.

## Task 1 — Exhaustive module walk + strengths catalogue (GAP-03)
Walk EVERY module in `fdars-core/src/` (documented per-module coverage table proving exhaustiveness). Produce two catalogues:
1. **fdars-unique (no R equivalent)** — each row: capability · fdars module/function · closest R analog "none found" + the R search that established it. Verify against the Phase-16 R inventory + Phase-17 parity matrix (R is broader than scikit-fda — re-vet, don't copy the v0.14.0 scikit-fda strengths list).
2. **fdars-ahead (leads closest R analog)** — each row: capability · fdars module · named R analog · nature of the lead. Be fair: where R leads (fdasrvf elastic breadth, fdapace sparse FPCA) it belongs in Phase 17, not here.

### Success checks (ROADMAP SC1–SC3)
- [ ] Catalogues fdars capabilities with no R equivalent (each with closest-R "none found" note).
- [ ] Lists where fdars leads its closest R analog (R analog named + lead stated).
- [ ] Derived from a full module-map walk (per-module coverage documented → demonstrably exhaustive).

**Constraints:** append to existing `R-AUDIT-REPORT.md`; do not overwrite Phases 16/17; do not touch archived scikit-fda `AUDIT-REPORT.md`. Zero `fdars-core/src/` edits.

**Commit:** `docs(18): reverse-parity fdars strengths sweep (GAP-03)`
