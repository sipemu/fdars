---
phase: 81-api-audit-deprecated-form-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/81-api-audit-deprecated-form-removal/81-AUDIT-INVENTORY.md
autonomous: false
requirements: [AUDIT-01]
estimate:
  tokens: 78000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A ranked breaking-change inventory exists as a durable markdown artifact covering all FOUR audit scopes (per AUDIT-01)"
    - "Every inventory entry lists location, proposed change, blast radius (internal callers / examples / doctests), and a value/risk rating"
    - "Naming recommendations are BALANCED — high-value renames recommended, low-value flagged optional/deferrable"
    - "Pub-sealing recommendations are AGGRESSIVE and non_exhaustive coverage is FULL"
    - "The user has approved the inventory (possibly at a reduced scope) via a blocking human-verification checkpoint"
  artifacts:
    - ".planning/phases/81-api-audit-deprecated-form-removal/81-AUDIT-INVENTORY.md"
  key_links:
    - "Each inventory entry has a stable ID (AUD-NN) so Phases 82/83 can reference approved entries by ID"
    - "Scope tags map entries to downstream requirements: pub->API-02, non_exhaustive->API-03, naming->API-04, deprecated-form->API-01"
---

<objective>
Produce a ranked, user-approved public-API breaking-change inventory for `fdars-core` covering all four audit scopes: (1) deprecated-form removal, (2) accidental `pub` exposure, (3) `#[non_exhaustive]` gaps, and (4) naming inconsistencies. Write it as a durable markdown artifact whose entries carry stable IDs, so Phases 82 (API-02/03) and 83 (API-04) draw their concrete change sets from the approved list. This satisfies AUDIT-01.

Purpose: This is the audit gate for the whole v0.41.0 breaking milestone. Nothing breaking in Phases 82/83 executes until the user approves this inventory. The inventory is analysis and recommendation only — it changes NO `fdars-core/src/` code.

Output: `.planning/phases/81-api-audit-deprecated-form-removal/81-AUDIT-INVENTORY.md` (approved).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/81-api-audit-deprecated-form-removal/81-CONTEXT.md
</context>

<artifacts_this_phase_produces>
This plan (81-01) creates ONE new durable artifact (no source-code changes):

| Artifact | Kind | Purpose |
|----------|------|---------|
| `.planning/phases/81-api-audit-deprecated-form-removal/81-AUDIT-INVENTORY.md` | markdown | Ranked breaking-change inventory, source of truth for Phases 82/83 change sets |

Symbols introduced: none (analysis artifact only). Entry IDs `AUD-01`, `AUD-02`, ... are defined for cross-phase reference.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Scan all four audit scopes into a raw evidence dump</name>
  <files>.planning/phases/81-api-audit-deprecated-form-removal/81-AUDIT-INVENTORY.md</files>
  <read_first>
    - fdars-core/src/lib.rs (crate-root `pub use` re-export blocks — the public surface)
    - fdars-core/src/prelude.rs (prelude re-exports)
    - fdars-core/src/depth/mod.rs (barrel re-export pattern — explicit `pub use`, no wildcards)
    - .planning/phases/81-api-audit-deprecated-form-removal/81-CONTEXT.md (the audit-aggressiveness decisions)
  </read_first>
  <action>
    This is the tracer slice: prove the audit method works end-to-end on ONE representative entry per scope before scaling to the whole surface. Run scope-scoped scans across `fdars-core/src/` and capture raw hits (do NOT yet rank or rate). For each of the FOUR scopes, collect at least one concrete real entry with its file and line so the method is proven:
    - Scope A (deprecated-form removal, feeds API-01): scan for `#[deprecated` attributes. The 6 forms (`mean_2d`, `fanova`, `random_tukey_2d`, `random_projection_2d`, `fraiman_muniz_2d`, `modal_2d`) are already known; record each with its definition site and its `Dim`/`_seeded` replacement.
    - Scope B (accidental `pub`, feeds API-02): scan for `pub fn` / `pub struct` / `pub enum` / `pub use` that leak internal helpers not deliberately in the public API. Cross-check crate-root and prelude re-export lists against per-module `pub use` barrels. AGGRESSIVE posture per CONTEXT: a candidate for sealing is anything not deliberately public.
    - Scope C (`#[non_exhaustive]` gaps, feeds API-03): enumerate every public enum and public result struct; flag those lacking `#[non_exhaustive]` that could plausibly grow variants or fields. FULL coverage posture.
    - Scope D (naming, feeds API-04): scan `_1d` / `_2d` / `_nd` suffix sprawl and inconsistent config/result naming. BALANCED posture — note which are high-value dispatcher collapses vs low-value or marginal.
    Write the raw dump into `81-AUDIT-INVENTORY.md` under a `## Raw Evidence (unranked)` heading, one subsection per scope. Ground every hit with a real path and line. Do NOT modify any `fdars-core/src/` file — this is analysis only.
  </action>
  <verify>
    <automated>test -f .planning/phases/81-api-audit-deprecated-form-removal/81-AUDIT-INVENTORY.md && grep -qi 'Raw Evidence' .planning/phases/81-api-audit-deprecated-form-removal/81-AUDIT-INVENTORY.md && grep -qi 'Scope A' .planning/phases/81-api-audit-deprecated-form-removal/81-AUDIT-INVENTORY.md && grep -qi 'Scope B' .planning/phases/81-api-audit-deprecated-form-removal/81-AUDIT-INVENTORY.md && grep -qi 'Scope C' .planning/phases/81-api-audit-deprecated-form-removal/81-AUDIT-INVENTORY.md && grep -qi 'Scope D' .planning/phases/81-api-audit-deprecated-form-removal/81-AUDIT-INVENTORY.md && git diff --quiet -- fdars-core/src/</automated>
    <fails_when>The inventory file is missing, any of the four scope subsections is absent, the Raw Evidence heading is missing, OR any file under fdars-core/src/ was modified (this task must not touch source).</fails_when>
  </verify>
  <acceptance_criteria>
    - `81-AUDIT-INVENTORY.md` exists with a `## Raw Evidence (unranked)` section containing all four scope subsections (A/B/C/D).
    - Each scope has at least one real, path:line-grounded entry.
    - Zero changes to any `fdars-core/src/` file.
  </acceptance_criteria>
  <done>The four-scope raw evidence dump is written to the inventory artifact, every hit grounded to a real source location, and no source code was modified.</done>
</task>

<task type="auto">
  <name>Task 2: Rank, rate, and ID every entry into the final inventory</name>
  <files>.planning/phases/81-api-audit-deprecated-form-removal/81-AUDIT-INVENTORY.md</files>
  <read_first>
    - .planning/phases/81-api-audit-deprecated-form-removal/81-AUDIT-INVENTORY.md (the raw evidence from Task 1)
    - .planning/phases/81-api-audit-deprecated-form-removal/81-CONTEXT.md (aggressiveness decisions: naming Balanced, pub Aggressive, non_exhaustive Full)
    - fdars-core/examples (all 28 example dirs — blast-radius: which examples call each candidate)
  </read_first>
  <action>
    Promote the raw evidence into a ranked, IDed inventory. Replace / augment the raw section with a `## Ranked Inventory` section: one table row (or subsection) per candidate change, each carrying:
    - A stable ID `AUD-NN` (sequential; do NOT renumber later — Phases 82/83 cite these).
    - Scope tag mapping to the downstream requirement (deprecated-form -> API-01, pub -> API-02, non_exhaustive -> API-03, naming -> API-04).
    - Location (`path:line` or symbol).
    - Proposed change (concrete: e.g. `pub` -> `pub(crate)`, add `#[non_exhaustive]`, rename `foo_1d`/`foo_2d` -> `foo(..., Dim)`, remove `mean_2d`).
    - Blast radius: count and list internal callers, examples, and doctests affected (grep the crate + `fdars-core/examples/` + doctest `///` blocks per candidate).
    - Value/risk rating (e.g. High-value/Low-risk ... Low-value/High-risk).
    - Recommendation flag: for NAMING entries apply BALANCED policy — mark high-value renames `RECOMMENDED` and low-value/marginal ones `OPTIONAL (deferrable to STAB-03 checklist)`. For PUB apply AGGRESSIVE (recommend sealing unless deliberately public). For NON_EXHAUSTIVE apply FULL (recommend on all plausibly-growing types).
    Rank entries within each scope by value/risk. Add a short `## How Phases 82/83 Consume This` note explaining the ID-reference contract. Keep the deprecated-form entries (Scope A) present but note they are already fully specified and executed by plan 81-02 (they do not need approval to proceed). Analysis only — no `fdars-core/src/` edits.
  </action>
  <verify>
    <automated>grep -qi 'Ranked Inventory' .planning/phases/81-api-audit-deprecated-form-removal/81-AUDIT-INVENTORY.md && grep -qE 'AUD-0*1' .planning/phases/81-api-audit-deprecated-form-removal/81-AUDIT-INVENTORY.md && grep -qiE 'API-0[1234]' .planning/phases/81-api-audit-deprecated-form-removal/81-AUDIT-INVENTORY.md && grep -qiE 'blast radius|blast-radius' .planning/phases/81-api-audit-deprecated-form-removal/81-AUDIT-INVENTORY.md && grep -qiE 'value|risk' .planning/phases/81-api-audit-deprecated-form-removal/81-AUDIT-INVENTORY.md && git diff --quiet -- fdars-core/src/</automated>
    <fails_when>The Ranked Inventory section is missing, no `AUD-NN` IDs are present, downstream requirement tags (API-01..04) are absent, blast-radius or value/risk fields are missing, OR any fdars-core/src/ file was modified.</fails_when>
  </verify>
  <acceptance_criteria>
    - Every candidate has a unique `AUD-NN` ID, a scope->requirement tag, a location, a concrete proposed change, a blast-radius listing, and a value/risk rating.
    - Naming entries carry RECOMMENDED vs OPTIONAL flags (Balanced); pub entries reflect Aggressive; non_exhaustive reflects Full.
    - A "How Phases 82/83 Consume This" contract note is present.
    - Zero changes to any `fdars-core/src/` file.
  </acceptance_criteria>
  <done>The inventory is a ranked, IDed, rated table across all four scopes with blast radius and value/risk on every entry, ready for user approval, with no source modified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: User approves the audit inventory (reduced scope allowed)</name>
  <files>.planning/phases/81-api-audit-deprecated-form-removal/81-AUDIT-INVENTORY.md</files>
  <action>
    Present the ranked inventory to the user for approval. This is a genuine decision gate: Phases 82 (API-02/03) and 83 (API-04) draw their concrete change sets ONLY from the entries the user approves here. The user MAY approve a REDUCED scope — especially for naming (per CONTEXT: low-value naming renames the user declines fold into the STAB-03 1.0 gap checklist in Phase 84, NOT forced into Phase 83).

    Show the user:
    - The full ranked inventory grouped by scope, with each `AUD-NN` entry's proposed change, blast radius, and value/risk.
    - The naming entries' RECOMMENDED vs OPTIONAL split, so they can right-size Phase 83.

    Ask the user to respond with one of:
    - "approve all" — every entry becomes part of the downstream change sets.
    - "approve except AUD-NN, AUD-NN, ..." — named entries are excluded (excluded naming entries route to STAB-03).
    - a reduced-scope instruction in their own words.

    After the user responds, record the outcome in the inventory under a `## Approval` section: date, which `AUD-NN` entries are APPROVED, which are DEFERRED (and where they route, e.g. STAB-03), and any user notes. Do NOT proceed to modify source; this plan's output is the approved inventory only.
  </action>
  <what-built>
    A ranked, four-scope, ID-addressable breaking-change inventory (`81-AUDIT-INVENTORY.md`) covering deprecated-form removal (Scope A), accidental `pub` exposure (Scope B), `#[non_exhaustive]` gaps (Scope C), and naming inconsistencies (Scope D). Each `AUD-NN` entry has a location, proposed change, blast radius, and value/risk rating. Naming entries are split RECOMMENDED vs OPTIONAL (Balanced); pub is Aggressive; non_exhaustive is Full.
  </what-built>
  <how-to-verify>
    Open `.planning/phases/81-api-audit-deprecated-form-removal/81-AUDIT-INVENTORY.md`. Read the `## Ranked Inventory` section grouped by scope. Confirm each entry's proposed change, blast radius, and value/risk make sense. Decide whether to approve all entries or exclude specific `AUD-NN` entries (excluded naming entries route to the STAB-03 1.0 gap checklist in Phase 84).
  </how-to-verify>
  <verify>
    <human-check>The user has explicitly stated their approval decision (approve all / approve-except / reduced scope), and the `## Approval` section records the approved and deferred entry IDs plus the date.</human-check>
  </verify>
  <resume-signal>
    User replies with their approval decision: "approve all", "approve except AUD-NN, ...", or a reduced-scope instruction. On reply, record the outcome in the inventory's `## Approval` section and mark the checkpoint resolved.
  </resume-signal>
  <acceptance_criteria>
    - The user has given an explicit approval decision.
    - The inventory has an `## Approval` section listing APPROVED and DEFERRED `AUD-NN` entries, with deferred naming entries routed to STAB-03.
  </acceptance_criteria>
  <done>The user has approved the inventory (at full or reduced scope) and the approval outcome is recorded in `81-AUDIT-INVENTORY.md`.</done>
</task>

</tasks>

<threat_model>
N/A — internal library API-shape audit. This plan produces a markdown analysis artifact and touches no source code, no external input, no network, no auth, and no data boundary. No STRIDE threats apply.
</threat_model>

<verification>
- `81-AUDIT-INVENTORY.md` exists and covers all four scopes (deprecated-form, pub, non_exhaustive, naming).
- Every entry has: stable `AUD-NN` ID, scope->requirement tag, location, proposed change, blast radius, value/risk rating.
- Naming entries are flagged RECOMMENDED vs OPTIONAL (Balanced); pub is Aggressive; non_exhaustive is Full.
- `git diff --quiet -- fdars-core/src/` — no source code changed by this plan.
- The `## Approval` section records the user's decision and approved/deferred IDs.
</verification>

<success_criteria>
- AUDIT-01 satisfied: a ranked, four-scope, user-approved breaking-change inventory exists as a durable artifact with per-entry location, proposed change, blast radius, and value/risk.
- The inventory is ID-addressable so Phases 82/83 can reference approved entries.
- No `fdars-core/src/` code was modified in this plan.
</success_criteria>

<output>
Create `.planning/phases/81-api-audit-deprecated-form-removal/81-01-SUMMARY.md` when done.
</output>
