---
phase: 81-api-audit-deprecated-form-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/fdata.rs
  - fdars-core/src/function_on_scalar.rs
  - fdars-core/src/depth/fraiman_muniz.rs
  - fdars-core/src/depth/modal.rs
  - fdars-core/src/depth/random_tukey.rs
  - fdars-core/src/depth/random_projection.rs
  - fdars-core/src/depth/mod.rs
  - fdars-core/src/depth/tests.rs
  - fdars-core/src/lib.rs
  - fdars-core/src/prelude.rs
  - fdars-core/src/inference/anova.rs
  - fdars-core/src/inference/permutation.rs
  - fdars-core/tests/equivalence_phase50.rs
  - fdars-core/tests/validate_against_r.rs
  - fdars-core/examples/21_function_on_scalar/main.rs
autonomous: true
requirements: [API-01]
estimate:
  tokens: 92000
  raw_tokens: 61000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "The 6 deprecated forms (`mean_2d`, `fanova`, `random_tukey_2d`, `random_projection_2d`, `fraiman_muniz_2d`, `modal_2d`) no longer exist in fdars-core (hard-removed, no shim, per API-01)"
    - "Their crate-root (src/lib.rs) and prelude (src/prelude.rs) and depth barrel (src/depth/mod.rs) re-exports are removed"
    - "All internal callers, unit tests, doctests, and example 21 use the `Dim`/`_seeded` replacements or are removed if they only tested the deprecated form"
    - "`fanova` callers that pinned bit-identical behavior now call `fanova_seeded(data, groups, n_perm, 42)` preserving the legacy seed-42 output"
    - "The crate and all 28 examples compile; the whole-crate test suite passes; the --features serde build stays green"
  artifacts:
    - "fdars-core/src/fdata.rs (mean_2d removed)"
    - "fdars-core/src/function_on_scalar.rs (fanova removed)"
    - "fdars-core/src/depth/{fraiman_muniz,modal,random_tukey,random_projection}.rs (the four _2d forms removed)"
  key_links:
    - "Replacement mapping: mean_2d->mean(_,Dim::Two); fanova(d,g,n)->fanova_seeded(d,g,n,42); {fraiman_muniz,modal,random_tukey,random_projection}_2d(a,b,p)->same(a,b,p,Dim::Two)"
    - "Removing a #[deprecated] item also requires removing its #[allow(deprecated)] guards on re-export blocks and test fns, else clippy -D warnings fails on the now-useless allow"
---

<objective>
Hard-remove the 6 already-deprecated forms and migrate every consumer to the `Dim`/`_seeded` replacements, so the crate and all 28 examples compile and the whole-crate test suite passes. This satisfies API-01. API-01 is fully specified and does NOT depend on the 81-01 audit approval, so it proceeds independently in the same wave.

Purpose: Land the mechanical, low-risk part of the breaking milestone first. These forms are pure delegation shims; their removal is behavior-preserving because every call migrates to the function the shim already forwards to (`fanova` -> `fanova_seeded(..., 42)` preserves the legacy seed-42 output bit-for-bit).

Output: 6 forms removed, all re-exports removed, all callers/tests/doctests/example-21 migrated or removed, whole-crate gates green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/81-api-audit-deprecated-form-removal/81-CONTEXT.md
</context>

<artifacts_this_phase_produces>
This plan (81-02) removes symbols and migrates consumers. No NEW files.

| Removed symbol | Defined at | Replacement (behavior-preserving) |
|----------------|-----------|-----------------------------------|
| `fdata::mean_2d` | src/fdata.rs:206 | `fdata::mean(data, Dim::Two)` |
| `function_on_scalar::fanova` | src/function_on_scalar.rs:917 | `fanova_seeded(data, groups, n_perm, 42)` |
| `depth::fraiman_muniz_2d` | src/depth/fraiman_muniz.rs:66 | `fraiman_muniz(obj, ori, scale, Dim::Two)` |
| `depth::modal_2d` | src/depth/modal.rs:67 | `modal(obj, ori, h, Dim::Two)` |
| `depth::random_tukey_2d` | src/depth/random_tukey.rs:61 | `random_tukey(obj, ori, nproj, Dim::Two)` |
| `depth::random_projection_2d` | src/depth/random_projection.rs:87 | `random_projection(obj, ori, nproj, Dim::Two)` |

Note: `random_projection`'s replacement signature spans src/depth/random_projection.rs:70 — confirm its exact parameter order when migrating (it is multi-line).
</artifacts_this_phase_produces>

<build_ci_hazards>
Per project MEMORY.md, on this large crate:
- The pre-commit hook runs the full test suite and can time out. Run `cargo fmt` first, then commit with `git commit --no-verify`, running the gates out-of-band.
- `/tmp` and `target/` disk pressure cause spurious "No space left" / link failures on full builds. If a build/test dies at a LINK step, run `rm -rf target/debug/{incremental,examples}` to free space and retry.
- Clippy MUST be run with `--all-targets --features linalg,parallel -- -D warnings` (CI lints test/bench code; a plain `-p` lint gives a false green).
- Keep the `--features serde` build green (do not regress it).
</build_ci_hazards>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Remove the 6 form definitions and all their re-exports + dead allow(deprecated) guards</name>
  <files>fdars-core/src/fdata.rs, fdars-core/src/function_on_scalar.rs, fdars-core/src/depth/fraiman_muniz.rs, fdars-core/src/depth/modal.rs, fdars-core/src/depth/random_tukey.rs, fdars-core/src/depth/random_projection.rs, fdars-core/src/depth/mod.rs, fdars-core/src/lib.rs, fdars-core/src/prelude.rs</files>
  <read_first>
    - fdars-core/src/fdata.rs (mean_2d def at ~206; #[deprecated] block above it)
    - fdars-core/src/function_on_scalar.rs (fanova def at ~917; #[deprecated] block above it)
    - fdars-core/src/depth/fraiman_muniz.rs, modal.rs, random_tukey.rs, random_projection.rs (the four `_2d` defs and their #[deprecated] blocks)
    - fdars-core/src/depth/mod.rs (barrel `pub use` lines 37/41/43-45/47-48 and their #[allow(deprecated)] at 36/41/43/47)
    - fdars-core/src/lib.rs (re-export blocks: fanova at ~393-398 with #[allow(deprecated)] at 395; depth block at ~650-661 with #[allow(deprecated)] at 651; fdata block at ~680-685 with #[allow(deprecated)] at 681)
    - fdars-core/src/prelude.rs (depth block ~40-47 with #[allow(deprecated)] at 42)
  </read_first>
  <action>
    This is the tracer slice — it wires the removal through every layer (definition -> module barrel -> crate root -> prelude) so a compile proves the removal is architecturally complete before test/example migration. For EACH of the 6 forms:
    1. Delete the `pub fn <form>(...)` definition and its `#[deprecated(...)]` attribute block (and any `#[must_use]` that decorated only that fn). Leave the corresponding `Dim`/`_seeded` replacement fn untouched.
    2. Remove the form's name from every `pub use` list: the depth barrel `src/depth/mod.rs`, the crate root `src/lib.rs`, and `src/prelude.rs`.
    3. Because the ONLY reason for the `#[allow(deprecated)]` attributes on the re-export blocks (lib.rs 395/651/681, prelude.rs 42, depth/mod.rs 36/41/43/47) was the now-removed deprecated re-exports, remove each `#[allow(deprecated)]` that no longer guards any deprecated item — a redundant `#[allow]` is itself a clippy warning under `-D warnings`. If a block still re-exports OTHER genuinely-deprecated items, keep the allow; otherwise delete it. Also update the stale explanatory comments above those blocks that reference the removed forms.
    Do NOT touch tests, doctests, or examples in this task — only definitions and re-exports. The crate lib will NOT fully compile yet (internal callers/tests still reference the forms); that is expected and handled in Tasks 2-3. Run a targeted check that the DEFINITIONS and RE-EXPORTS are gone.
  </action>
  <verify>
    <automated>! grep -rnE 'pub fn (mean_2d|fanova|random_tukey_2d|random_projection_2d|fraiman_muniz_2d|modal_2d)\b' fdars-core/src/ && ! grep -rnE '\b(mean_2d|fanova|random_tukey_2d|random_projection_2d|fraiman_muniz_2d|modal_2d)\b' fdars-core/src/lib.rs fdars-core/src/prelude.rs fdars-core/src/depth/mod.rs</automated>
    <fails_when>Any of the 6 `pub fn` definitions still exists in fdars-core/src/, OR any of the 6 form names still appears in lib.rs, prelude.rs, or depth/mod.rs (definition or re-export not fully removed).</fails_when>
  </verify>
  <acceptance_criteria>
    - No `pub fn` for any of the 6 forms remains under `fdars-core/src/`.
    - None of the 6 form names appears in `src/lib.rs`, `src/prelude.rs`, or `src/depth/mod.rs`.
    - No `#[allow(deprecated)]` remains that no longer guards a deprecated item on the touched re-export blocks.
    - The replacement functions (`mean`, `fanova_seeded`, `fraiman_muniz`, `modal`, `random_tukey`, `random_projection`) are untouched.
  </acceptance_criteria>
  <done>The 6 form definitions and every re-export of them are removed, and the now-dead `#[allow(deprecated)]` guards on the touched blocks are cleaned up; only internal callers/tests/examples still reference the removed forms (handled next).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate internal production callers and doctests to replacements</name>
  <files>fdars-core/src/inference/anova.rs, fdars-core/src/inference/permutation.rs, fdars-core/src/function_on_scalar.rs</files>
  <read_first>
    - fdars-core/src/inference/anova.rs (fanova callers at ~244, ~266; `#[allow(deprecated)]` on those test fns at ~188/230/253; doctest `///` refs at ~58/66)
    - fdars-core/src/inference/permutation.rs (fanova caller at ~402; `#[allow(deprecated)]` at ~381; doctest ref at ~198)
    - fdars-core/src/function_on_scalar.rs (in-module `fanova` test callers at ~1084/1118/1151/1163/1166 with `#[allow(deprecated)]` at ~1066/1100/1130/1158; doctest `///` ref at ~895)
  </read_first>
  <action>
    Migrate every remaining `fanova(data, groups, n_perm)` call to `fanova_seeded(data, groups, n_perm, 42)`. The literal `42` is mandatory: `fanova` delegated to `fanova_seeded(..., 42)`, so seed 42 preserves the exact legacy LCG output the surrounding tests pin (RESEARCH pitfall #3 — the hand-rolled LCG). These callers are in `inference/anova.rs`, `inference/permutation.rs`, and the in-module tests of `function_on_scalar.rs`. Their comments say "do NOT migrate to `fanova_seeded`" meaning do not change the SEED/behavior — migrating the CALL to `fanova_seeded(..., 42)` satisfies that intent exactly; update those comments to reflect the shim no longer exists (behavior still pinned via explicit seed 42).
    For each migrated call site, remove the now-unnecessary `#[allow(deprecated)]` on the enclosing test fn (the callee `fanova_seeded` is not deprecated) — a stale `#[allow]` warns under `-D warnings`.
    Update doctest / rustdoc `///` references that point at `fanova` (e.g. `[`fanova`]` intra-doc links and prose in function_on_scalar.rs:895, inference/anova.rs:58/66, inference/permutation.rs:198) to reference `fanova_seeded` instead, so intra-doc links do not break the doc build. Do NOT alter numeric expectations — behavior is preserved.
  </action>
  <verify>
    <automated>! grep -rnE '\bfanova\s*\(' fdars-core/src/inference/ fdars-core/src/function_on_scalar.rs | grep -v 'fanova_seeded' && ! grep -rn 'allow(deprecated)' fdars-core/src/inference/ && cargo build -p fdars-core --features linalg,parallel 2>&1 | grep -vE '^\s*$' | { ! grep -qE 'error\['; }</automated>
    <fails_when>Any bare `fanova(` call (not `fanova_seeded(`) remains in inference/ or function_on_scalar.rs, OR a stale `#[allow(deprecated)]` remains in inference/, OR `cargo build -p fdars-core --features linalg,parallel` emits an `error[` line.</fails_when>
  </verify>
  <acceptance_criteria>
    - Every remaining `fanova(...)` call is now `fanova_seeded(..., 42)`.
    - No stale `#[allow(deprecated)]` remains on migrated production/test fns in inference/ or function_on_scalar.rs.
    - Intra-doc `[`fanova`]` links now target `fanova_seeded`.
    - `cargo build -p fdars-core --features linalg,parallel` compiles (library) with no `error[` (unit tests still migrated in Task 3 may reference removed depth forms — those live in depth/tests.rs, handled next).
  </acceptance_criteria>
  <done>All production callers and doctests use `fanova_seeded(..., 42)`; numeric behavior is unchanged and the library crate compiles.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migrate/remove tests + example 21, then pass whole-crate gates</name>
  <files>fdars-core/src/depth/tests.rs, fdars-core/src/fdata.rs, fdars-core/tests/equivalence_phase50.rs, fdars-core/tests/validate_against_r.rs, fdars-core/examples/21_function_on_scalar/main.rs</files>
  <read_first>
    - fdars-core/src/depth/tests.rs (deprecated-form delegation tests ~318-365: test_fraiman_muniz_2d_delegates, test_modal_2d_delegates, test_random_projection_2d_returns_valid — each with `#[allow(deprecated)]`)
    - fdars-core/src/fdata.rs (test_mean_2d_delegates at ~1097-1105 with `#[allow(deprecated)]`)
    - fdars-core/tests/equivalence_phase50.rs (fanova_shim_seed42_bit_identical ~68-78; dispatch_random_projection_2d_is_valid ~249; dispatch_random_tukey_2d_is_valid ~260; modal/fraiman_muniz/mean `_2d` equivalence pairs ~214/225/239)
    - fdars-core/tests/validate_against_r.rs (test_2d_delegates_to_1d_fm ~2278; test_2d_mean_valid ~2510)
    - fdars-core/examples/21_function_on_scalar/main.rs (only a `//!` doc comment mentions `fanova` at line 6)
  </read_first>
  <action>
    Handle the tests that reference the removed forms. Two patterns:
    - Tests whose ENTIRE purpose was to prove the deprecated shim delegates to / matches its replacement have no reason to exist once the shim is gone -> REMOVE the whole test fn (and its `#[allow(deprecated)]`): `test_fraiman_muniz_2d_delegates`, `test_modal_2d_delegates`, `test_random_projection_2d_returns_valid` (depth/tests.rs); `test_mean_2d_delegates` (fdata.rs); `fanova_shim_seed42_bit_identical`, `dispatch_random_projection_2d_is_valid`, `dispatch_random_tukey_2d_is_valid`, and the `modal_2d`/`fraiman_muniz_2d`/`mean_2d` equivalence pairs (equivalence_phase50.rs); `test_2d_delegates_to_1d_fm`, `test_2d_mean_valid` (validate_against_r.rs). The replacement-side coverage (`fanova_seeded_seed42_bit_identical`, `modal(_,Dim::Two)`, `fraiman_muniz(_,Dim::Two)`, `mean(_,Dim::Two)`, etc.) already remains and is sufficient. Delete the accompanying module-doc lines / comments that only describe the removed shim, and drop any now-unused `use` imports the removed tests introduced (e.g. `use fdars_core::depth::modal_2d;`).
    - example 21: the only reference is the `//!` doc line "Functional ANOVA (`fanova`) for group comparisons" — update the prose to `fanova_seeded` (the example body itself does not call the removed form; confirm with a grep and migrate any call to `fanova_seeded(..., 42)` if one exists).
    Then run the whole-crate gates (see build/CI hazards). Commit with `cargo fmt` first, then `git commit --no-verify`, running gates out-of-band. If a build dies at a LINK step, `rm -rf target/debug/{incremental,examples}` and retry.
  </action>
  <verify>
    <automated>! grep -rnE '\b(mean_2d|random_tukey_2d|random_projection_2d|fraiman_muniz_2d|modal_2d)\b' fdars-core/src/ fdars-core/tests/ fdars-core/examples/ && ! grep -rnE '\bfanova\s*\(' fdars-core/tests/ fdars-core/examples/ | grep -v 'fanova_seeded' && cargo fmt --check && cargo clippy --all-targets --features linalg,parallel -- -D warnings && cargo test && cargo build --features serde && cargo build --examples</automated>
    <fails_when>Any of the 6 removed form names still appears anywhere under src/, tests/, or examples/; OR a bare `fanova(` call remains in tests/ or examples/; OR `cargo fmt --check` reports drift; OR clippy emits any warning/error under `-D warnings`; OR `cargo test` reports "test result: FAILED"; OR `cargo build --features serde` fails; OR `cargo build --examples` emits `error[` (any of the 28 examples fails to compile).</fails_when>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of the 6 removed form names anywhere under `fdars-core/src/`, `fdars-core/tests/`, `fdars-core/examples/`.
    - No bare `fanova(` call remains in tests or examples.
    - `cargo fmt --check`, `cargo clippy --all-targets --features linalg,parallel -- -D warnings`, `cargo test`, `cargo build --features serde`, and `cargo build --examples` all pass.
  </acceptance_criteria>
  <done>All shim-only tests are removed, remaining tests/doctests/example-21 use the replacements, and the whole-crate gates (fmt, clippy all-targets, test, serde build, all 28 examples compile) are green.</done>
</task>

</tasks>

<threat_model>
N/A — internal library API-shape change (removing delegation shims and re-exports). No untrusted input, network, auth, or data boundary; numeric behavior is preserved. No STRIDE threats apply. This phase adds no package-manager installs (no new crate dependency), so the package-legitimacy gate does not fire.
</threat_model>

<verification>
- None of the 6 forms exists as a definition or re-export anywhere in the crate.
- Every migrated `fanova` caller uses `fanova_seeded(..., 42)` (behavior preserved).
- Shim-only delegation/equivalence tests are removed; replacement-side coverage remains.
- Whole-crate gates green: `cargo fmt --check`; `cargo clippy --all-targets --features linalg,parallel -- -D warnings`; `cargo test`; `cargo build --features serde`; `cargo build --examples` (all 28 compile).
</verification>

<success_criteria>
- API-01 satisfied: the 6 deprecated forms and their crate-root/prelude/barrel re-exports are hard-removed; all internal callers, unit tests, doctests, and example 21 use the `Dim`/`_seeded` replacements (or are removed if they only tested the shim).
- The crate and all 28 examples compile; the whole-crate test suite passes; the `--features serde` build stays green.
- No numeric/behavioral change (seed-42 legacy output preserved via explicit `fanova_seeded(..., 42)`).
</success_criteria>

<output>
Create `.planning/phases/81-api-audit-deprecated-form-removal/81-02-SUMMARY.md` when done.
</output>
