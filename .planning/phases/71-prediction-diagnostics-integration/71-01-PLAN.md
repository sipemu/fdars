---
phase: 71-prediction-diagnostics-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/wavelet/regression.rs
  - fdars-core/src/wavelet/mod.rs
  - fdars-core/src/lib.rs
  - fdars-core/src/prelude.rs
autonomous: true
requirements: [WAV-05, WAV-06]

estimate:
  tokens: 55000
  raw_tokens: 40000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "WcrResult::predict and WnetResult::predict re-pass the training curves and reproduce the stored training fitted_values within 1e-8 abs (SC1, WAV-05)"
    - "predict on fresh curves returns finite, NaN-free values; a training-grid-length mismatch returns FdarError, never panics (SC1, WAV-05)"
    - "beta_t()/coefficient_function() and fitted_values() accessors on both result structs return the stored beta_t and fitted_values slices (SC2, WAV-05)"
    - "The full wavelet surface is reachable via BOTH fdars_core::prelude::* AND crate-root paths (fdars_core::wcr, fdars_core::WaveletFamily, ...) (SC3, WAV-06)"
    - "A running end-to-end module doctest (fit -> predict -> read beta_t/fitted via prelude) passes under cargo test --doc (SC3, WAV-06)"
    - "Whole-crate cargo test + clippy --all-targets + fmt --check are green; the 28 examples and R/WASM bindings still build (SC4, WAV-06)"
  artifacts:
    - fdars-core/src/wavelet/regression.rs
    - fdars-core/src/wavelet/mod.rs
    - fdars-core/src/lib.rs
    - fdars-core/src/prelude.rs
  key_links:
    - "predict reuses the pub(crate) curves_to_coeff_design seam with the STORED family/mode/level (Some(level)) so the new-curve design matches the fit-time design exactly"
    - "crate-root pub use wavelet::{...} + pub use wavelet::regression::{...} and the mirrored prelude group expose the surface without pulling in the serde feature"
---

<objective>
Make the Phase-70 wavelet-domain regressors usable end-to-end and expose the full public surface.

Add out-of-sample `predict` (inherent methods) plus coefficient-function / fitted-value accessors to `WcrResult` and `WnetResult`; re-export the entire wavelet surface (DWT + `wcr` + `wnet` + config/result types) at BOTH the crate root and the prelude; and add one running end-to-end module doctest.

Purpose: WAV-05 (prediction + accessors) and WAV-06 (integration + doctest) — the final phase of the v0.37.0 WAV milestone. After this the milestone is code-complete.

Output: additive, non-breaking edits to `fdars-core/src/wavelet/regression.rs`, `fdars-core/src/wavelet/mod.rs`, `fdars-core/src/lib.rs`, `fdars-core/src/prelude.rs`. No new fields are required on the result structs — both already carry `intercept`, `coeff_weights`, `family`, `mode`, `level` — and `predict` reuses the existing `pub(crate) curves_to_coeff_design` seam. No `Cargo.toml` change, no existing public signature changed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The surface this phase extends (READ the struct/field/seam definitions before editing):
@fdars-core/src/wavelet/regression.rs
@fdars-core/src/wavelet/mod.rs
@fdars-core/src/lib.rs
@fdars-core/src/prelude.rs
</context>

<artifacts_this_phase_produces>
Every NEW public symbol / edit this phase introduces (all additive):

**On `WcrResult` (impl block in `fdars-core/src/wavelet/regression.rs`):**
- `pub fn predict(&self, new: &FdMatrix) -> Result<Vec<f64>, FdarError>` — inherent method.
- `pub fn beta_t(&self) -> &[f64]` — accessor over the existing `beta_t` field.
- `pub fn coefficient_function(&self) -> &[f64]` — alias of `beta_t()` (crate-convention name).
- `pub fn fitted_values(&self) -> &[f64]` — accessor over the existing `fitted_values` field.

**On `WnetResult` (impl block in `fdars-core/src/wavelet/regression.rs`):**
- `pub fn predict(&self, new: &FdMatrix) -> Result<Vec<f64>, FdarError>` — inherent method.
- `pub fn beta_t(&self) -> &[f64]`.
- `pub fn coefficient_function(&self) -> &[f64]`.
- `pub fn fitted_values(&self) -> &[f64]`.

**NO new struct fields.** Both structs already carry `intercept`, `coeff_weights`, `family`, `mode`, `level: usize` (effective depth) — everything `predict` needs. Do NOT add fields.

**Crate-root re-exports (`fdars-core/src/lib.rs`, new `pub use` block near the other module re-exports, e.g. after the PEER block ~line 602):**
- `pub use wavelet::{decompose, decompose_matrix, max_level, reconstruct, BoundaryMode, WaveletCoeffs, WaveletFamily};`
- `pub use wavelet::regression::{wcr, wnet, WcrConfig, WcrMethod, WcrResult, WnetConfig, WnetResult};`

**Prelude re-exports (`fdars-core/src/prelude.rs`, new `// Wavelet-domain regression (v0.37.0)` group after the PEER group ~line 111):**
- `pub use crate::wavelet::{decompose, decompose_matrix, max_level, reconstruct, BoundaryMode, WaveletCoeffs, WaveletFamily};`
- `pub use crate::wavelet::regression::{wcr, wnet, WcrConfig, WcrMethod, WcrResult, WnetConfig, WnetResult};`

**Doctest:** one `//!`-level end-to-end module doctest at the top of `fdars-core/src/wavelet/regression.rs` (most discoverable — it is the regressor module), using `use fdars_core::prelude::*;`, that fits `wcr`, calls `predict`, and reads `beta_t()`/`fitted_values()`.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end tracer — WcrResult::predict self-consistent on training curves, one path only</name>
  <files>fdars-core/src/wavelet/regression.rs</files>
  <read_first>
    - fdars-core/src/wavelet/regression.rs lines 80-109 (WcrResult fields: it ALREADY carries intercept, coeff_weights, family, mode, level — confirm these are the predict inputs; no new field needed).
    - fdars-core/src/wavelet/regression.rs lines 156-218 (`pub(crate) fn curves_to_coeff_design(data, family, mode, level) -> (FdMatrix, CoeffLayout)` — the exact seam predict reuses).
    - fdars-core/src/wavelet/regression.rs lines 1117-1129 (`compute_fitted_affine(design, coeffs, intercept)` — the intercept + X·beta dot predict mirrors).
    - fdars-core/src/wavelet/regression.rs lines 461-558 (`wcr` fit — note it stores `level: layout.levels()`; predict must pass `Some(self.level)` so the new-curve decomposition reproduces the fit-time layout exactly).
    - fdars-core/src/wavelet/mod.rs lines 520-551 (`decompose_matrix` rejects zero rows/cols and surfaces DWT family/level errors — those propagate through curves_to_coeff_design).
  </read_first>
  <action>
    Add an `impl WcrResult` block with a single inherent method `predict(&self, new: &FdMatrix) -> Result<Vec<f64>, FdarError>`.

    Implementation, reusing the existing seams (do NOT reconstruct predict inputs from beta_t — use the stored coefficient-space coeff_weights per D-locked decision):
    1. Validate the new-curve grid matches the training grid. The training grid length is recoverable as `self.beta_t.len()` (beta_t has length m = curve length). If `new.ncols() != self.beta_t.len()`, return `FdarError::InvalidDimension { parameter: "new", expected: <training m>, actual: <new ncols> }`. This is the never-panic grid-length gate.
    2. Re-transform the new curves with the STORED fitted DWT config: call `curves_to_coeff_design(new, self.family.clone(), self.mode, Some(self.level))` to get the new-curve design (ignore the returned layout — predict only needs the design). Surface any DWT error unchanged via `?`.
    3. Guard the coefficient-space width: the returned design has `ncols == self.coeff_weights.len()` because the same family/mode/level over the same grid length produces the same P. If they disagree, return `FdarError::InvalidDimension { parameter: "new", ... }` rather than indexing out of bounds (defensive; should not happen for a matching grid).
    4. Compute `y_hat_i = self.intercept + Σ_j design[(i,j)] * self.coeff_weights[j]` for each row i (the same affine coefficient-space dot `compute_fitted_affine` uses). Return the length-`new.nrows()` `Vec<f64>`.

    Add `#[must_use]` to `predict` (crate convention for non-trivial getters/producers).

    Add exactly TWO tracer tests in the existing `#[cfg(test)] mod tests` block:
    - `wcr_predict_reproduces_training_fitted`: build a spanning full-rank design (reuse the module's `spanning_design`/`pseudo_random` helpers), fit `wcr`, call `fit.predict(&data)` on the SAME training curves, and assert each returned value equals the corresponding `fit.fitted_values[i]` within 1e-8 absolute. Do NOT loosen this tolerance — a mismatch means the re-transform used the wrong config, which is a real bug (project memory: recovery tolerance is never loosened).
    - `wcr_predict_on_new_curves_is_finite_and_rejects_grid_mismatch`: predict on a fresh spanning design of the SAME m (assert all finite), and predict on a design with a DIFFERENT ncols (assert `Err(FdarError::InvalidDimension { .. })`).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && cargo test -p fdars-core --features linalg,parallel wavelet::regression::tests::wcr_predict 2>&1 | tail -20</automated>
    <fails_when>non-zero exit, "test result: FAILED", or fewer than 2 wcr_predict tests run (compile error or missing test)</fails_when>
  </verify>
  <acceptance_criteria>
    - `WcrResult::predict` exists as an inherent `#[must_use]` method returning `Result<Vec<f64>, FdarError>`.
    - Re-passing training curves reproduces `fit.fitted_values` to within 1e-8 absolute (self-consistency).
    - Prediction on fresh same-m curves is finite/NaN-free; a grid-length mismatch returns `FdarError::InvalidDimension` (no panic).
    - `predict` reuses `curves_to_coeff_design(new, self.family.clone(), self.mode, Some(self.level))` — the stored config — and the stored `coeff_weights` + `intercept`; it does NOT reconstruct inputs from `beta_t`.
    - No new fields were added to `WcrResult`; no existing field/signature changed.
  </acceptance_criteria>
  <done>The wcr predict happy path works end-to-end, is self-consistent to 1e-8 on the training curves, and is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Expand — WnetResult::predict + accessors on both result structs</name>
  <files>fdars-core/src/wavelet/regression.rs</files>
  <read_first>
    - fdars-core/src/wavelet/regression.rs lines 632-666 (WnetResult fields: it ALREADY carries intercept, coeff_weights, family, mode, level — identical predict inputs to WcrResult).
    - fdars-core/src/wavelet/regression.rs Task-1 `impl WcrResult { fn predict }` (mirror it exactly for wnet; the math is identical — affine coeff-space dot).
    - fdars-core/src/wavelet/mod.rs lines 371-409 (`WaveletCoeffs` accessor convention: `#[must_use]` getters returning `&[f64]` — mirror this style).
    - fdars-core/src/scalar_on_function/fregre_lm.rs (FregreLmResult accessor conventions — for naming `coefficient_function` vs `beta_t`).
  </read_first>
  <action>
    1. Add `WnetResult::predict(&self, new: &FdMatrix) -> Result<Vec<f64>, FdarError>` — a verbatim structural mirror of `WcrResult::predict` from Task 1 (same grid-length gate against `self.beta_t.len()`, same `curves_to_coeff_design(new, self.family.clone(), self.mode, Some(self.level))`, same width guard, same `intercept + Σ design·coeff_weights` dot). `#[must_use]`.

    2. Add accessor methods to BOTH `impl WcrResult` and `impl WnetResult`:
       - `beta_t(&self) -> &[f64]` returning `&self.beta_t`.
       - `coefficient_function(&self) -> &[f64]` returning `&self.beta_t` (crate-convention alias; provide both since they are cheap, per Claude's-discretion in CONTEXT.md).
       - `fitted_values(&self) -> &[f64]` returning `&self.fitted_values`.
       Mark each `#[must_use]`, matching the `WaveletCoeffs::approx`/`FregreLmResult` getter style. These read the EXISTING public fields — do not rename or remove the fields (they stay public for back-compat within the milestone).

    3. Add tests in the `#[cfg(test)] mod tests` block:
       - `wnet_predict_reproduces_training_fitted`: fit `wnet` on a sparse-signal spanning design (reuse `sparse_wnet_problem`), call `fit.predict(&data)` on the training curves, assert each equals `fit.fitted_values[i]` within 1e-8 absolute. Tolerance NOT loosened.
       - `wnet_predict_on_new_curves_is_finite_and_rejects_grid_mismatch`: fresh same-m design predicts finite; different-ncols design returns `Err(FdarError::InvalidDimension { .. })`.
       - `accessors_return_stored_slices`: for one `wcr` fit and one `wnet` fit, assert `beta_t()` and `coefficient_function()` both equal the stored `beta_t` field slice and have length m, and `fitted_values()` equals the stored `fitted_values` field slice and has length n.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && cargo test -p fdars-core --features linalg,parallel wavelet::regression 2>&1 | tail -25</automated>
    <fails_when>non-zero exit, "test result: FAILED", or the three new tests (wnet_predict_reproduces_training_fitted, wnet_predict_on_new_curves_is_finite_and_rejects_grid_mismatch, accessors_return_stored_slices) do not all run</fails_when>
  </verify>
  <acceptance_criteria>
    - `WnetResult::predict` exists, mirrors `WcrResult::predict`, and reproduces training fitted values to 1e-8 absolute; fresh-curve prediction is finite; grid mismatch returns `FdarError::InvalidDimension`.
    - `beta_t()`, `coefficient_function()`, and `fitted_values()` exist as `#[must_use]` `&[f64]` accessors on BOTH `WcrResult` and `WnetResult` and return the stored field slices.
    - The full pre-existing wavelet::regression test module still passes (no regression in the wcr/wnet fit tests).
    - No struct fields renamed/removed; no existing signature changed.
  </acceptance_criteria>
  <done>Both structs predict self-consistently and expose beta_t()/coefficient_function()/fitted_values(); the wavelet::regression test module is green.</done>
</task>

<task type="auto">
  <name>Task 3: Expand — crate-root + prelude re-exports of the full wavelet surface</name>
  <files>fdars-core/src/lib.rs, fdars-core/src/prelude.rs</files>
  <read_first>
    - fdars-core/src/lib.rs lines 599-602 (the PEER `pub use peer::{...}` block — add the new wavelet re-export block in the same crate-root re-export region, following this comment+`pub use` style).
    - fdars-core/src/lib.rs line 123 (`pub mod wavelet;` — the module is already declared; only re-exports are added).
    - fdars-core/src/prelude.rs lines 107-111 (the PEER prelude group — add the wavelet group directly after it, matching the `// <section comment>` + `pub use crate::...` grouping style).
    - fdars-core/src/wavelet/mod.rs lines 42-55, 79-92, 303-336, 347-476, 520-551 (the exact public names to re-export: WaveletFamily, BoundaryMode, WaveletCoeffs, max_level, decompose, reconstruct, decompose_matrix).
    - fdars-core/src/wavelet/regression.rs lines 33-109, 439-461, 577-666, 975-1003 (the exact public names: WcrMethod, WcrConfig, WcrResult, wcr, WnetConfig, WnetResult, wnet).
  </read_first>
  <action>
    1. In `fdars-core/src/lib.rs`, add a new crate-root re-export block in the module re-export region (e.g. immediately after the PEER `pub use peer::{...}` block near line 602). Use a section comment `// Re-export wavelet DWT primitive + wavelet-domain regressors (v0.37.0 WAV, full surface)` then:
       - `pub use wavelet::{decompose, decompose_matrix, max_level, reconstruct, BoundaryMode, WaveletCoeffs, WaveletFamily};`
       - `pub use wavelet::regression::{wcr, wnet, WcrConfig, WcrMethod, WcrResult, WnetConfig, WnetResult};`
       Keep names alphabetized within each brace to match the crate's re-export style. These re-exports must NOT be feature-gated and must NOT pull in the `serde` feature (the wavelet types are serde-clean; the pre-existing `--features serde` break in shapelet/classifier.rs is unrelated and must not be entangled).

    2. In `fdars-core/src/prelude.rs`, add a new group after the PEER group (line 111) with a section comment `// Wavelet-domain regression (v0.37.0)` then the mirrored:
       - `pub use crate::wavelet::{decompose, decompose_matrix, max_level, reconstruct, BoundaryMode, WaveletCoeffs, WaveletFamily};`
       - `pub use crate::wavelet::regression::{wcr, wnet, WcrConfig, WcrMethod, WcrResult, WnetConfig, WnetResult};`

    Do not add any other re-exports. Do not touch the `pub(crate) CoeffLayout` or the `pub(crate)` seams — they stay crate-private (re-exporting them would fail to compile). This is purely additive; no existing `pub use` line is modified.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && cargo build -p fdars-core --features linalg,parallel 2>&1 | tail -15 && cargo build -p fdars-core --features linalg,parallel --tests 2>&1 | tail -5</automated>
    <fails_when>non-zero exit, "error[" in output, or "cannot find" / "unresolved import" (a re-exported name is misspelled or not public)</fails_when>
  </verify>
  <acceptance_criteria>
    - `fdars-core/src/lib.rs` has a crate-root `pub use wavelet::{...}` + `pub use wavelet::regression::{...}` block exposing all of: WaveletFamily, BoundaryMode, WaveletCoeffs, decompose, reconstruct, decompose_matrix, max_level, WcrMethod, WcrConfig, WcrResult, WnetConfig, WnetResult, wcr, wnet.
    - `fdars-core/src/prelude.rs` has the mirrored `// Wavelet-domain regression (v0.37.0)` group with the same symbols.
    - The re-exports are not feature-gated and compile under `--features linalg,parallel` (no serde entanglement).
    - No existing `pub use` line was modified; the crate builds (lib + tests).
  </acceptance_criteria>
  <done>The full wavelet surface is reachable via both `fdars_core::{...}` crate-root paths and `fdars_core::prelude::*`; the crate builds clean.</done>
</task>

<task type="auto">
  <name>Task 4: End-to-end module doctest + whole-crate gate (SC3 + SC4)</name>
  <files>fdars-core/src/wavelet/regression.rs</files>
  <read_first>
    - fdars-core/src/wavelet/regression.rs lines 1-27 (the existing `//!` module doc — the doctest is appended to this module-level doc, the most discoverable location per CONTEXT.md).
    - fdars-core/src/prelude.rs lines 4-6 (existing crate doctests use `use fdars_core::prelude::*;` — mirror this to also prove the prelude surface).
    - fdars-core/src/wavelet/regression.rs lines 461-558 (`wcr` signature + WcrConfig fields — to write a correct, compiling fit call in the doctest).
    - fdars-core/src/matrix.rs (`FdMatrix::from_column_major(flat, nrows, ncols)` constructor — to build the doctest's synthetic data in a way that actually compiles).
  </read_first>
  <action>
    1. Append one self-contained end-to-end module doctest to the `//!` module doc block at the top of `fdars-core/src/wavelet/regression.rs`. The doctest MUST:
       - open with `use fdars_core::prelude::*;` (proving the prelude surface reaches wcr/WcrConfig/WcrResult/FdMatrix — do NOT add explicit `use fdars_core::wcr;` crate-root imports inside the same doctest; the crate-root reachability is covered by the build in Task 3 and can be asserted in a separate small unit test if desired, but keep the doctest prelude-only so it reads cleanly).
       - build a small synthetic predictor matrix via `FdMatrix::from_column_major(...)` (n >= 3 rows so wcr's n>=3 gate passes; m a modest length like 32 that admits a db4 decomposition) and a matching `y` vector.
       - fit with `let fit = wcr(&data, &y, &WcrConfig::default())?;` (use `?` inside a doctest `fn main() -> Result<(), fdars_core::FdarError>` wrapper, or `.unwrap()` — pick whichever compiles cleanly and reads well; prefer the `?`/`Ok(())` form).
       - call `let preds = fit.predict(&data)?;` and read `let beta = fit.beta_t();` and `let fitted = fit.fitted_values();`, then `assert_eq!(preds.len(), fitted.len());` and `assert_eq!(beta.len(), 32);` (or whatever m you chose) so the doctest actually verifies behavior, not just compiles.
       - IMPORTANT: the doctest must NOT require the `serde` feature. Use only prelude symbols. Keep it deterministic (no RNG that could make an assert flaky) — use fixed literal data or a simple arithmetic fill.

    2. Confirm the doctest runs (not `ignore`/`no_run`): it must execute under `cargo test --doc`.

    3. Run the FULL whole-crate gate (SC4). This is the success-criterion gate and is SLOW (hundreds of lib + doc tests) — budget for it here as the dedicated final task, not per-commit:
       - `cargo test -p fdars-core --features linalg,parallel` (lib + doctests, whole crate).
       - `cargo clippy --all-targets --features linalg,parallel -- -D warnings` (CI lints test/bench code — must be `--all-targets`).
       - `cargo fmt` then `cargo fmt --check`.
       All three must be green, proving the 28 examples + R/WASM bindings still build and nothing regressed.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && cargo test -p fdars-core --features linalg,parallel --doc wavelet 2>&1 | tail -20 && cargo test -p fdars-core --features linalg,parallel 2>&1 | tail -15 && cargo clippy --all-targets --features linalg,parallel -- -D warnings 2>&1 | tail -5 && cargo fmt --check 2>&1 | tail -3</automated>
    <fails_when>the wavelet module doctest does not run (0 doc tests matched, or "ignored"), any "test result: FAILED", non-zero exit on the whole-crate test, any clippy warning under --all-targets, or a non-empty `cargo fmt --check` diff</fails_when>
  </verify>
  <acceptance_criteria>
    - A running end-to-end module doctest lives in the `//!` doc of `fdars-core/src/wavelet/regression.rs`, uses `use fdars_core::prelude::*;`, fits `wcr`, calls `predict`, reads `beta_t()`/`fitted_values()`, and executes (not `ignore`/`no_run`) under `cargo test --doc`.
    - Whole-crate `cargo test -p fdars-core --features linalg,parallel` (lib + doc) is green.
    - `cargo clippy --all-targets --features linalg,parallel -- -D warnings` is clean.
    - `cargo fmt --check` shows no diff.
    - The doctest does not require the `serde` feature (prelude symbols only).
  </acceptance_criteria>
  <done>The end-to-end module doctest passes under `cargo test --doc`, and the whole-crate test + clippy --all-targets + fmt gates are all green (SC3 + SC4 proven; 28 examples and R/WASM bindings still build).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → `predict` | Untrusted `new: &FdMatrix` crosses in: arbitrary grid length and row count. Must be validated (grid-length gate), never panic. |
| caller → re-exported surface | New crate-root/prelude names become part of the public API; a wrong/over-broad re-export could widen the surface or entangle the serde feature. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-01 | Denial of Service | `WcrResult::predict` / `WnetResult::predict` | medium | mitigate | Grid-length gate (`new.ncols() != self.beta_t.len()` → `FdarError::InvalidDimension`) + coeff-width guard before indexing; return `Result`, never panic/OOB (Tasks 1–2, tested by the grid-mismatch tests). |
| T-71-02 | Tampering | crate-root + prelude re-exports | low | mitigate | Re-export only the intended additive names, alphabetized, not feature-gated; do NOT re-export the `pub(crate)` seams/`CoeffLayout`; keep serde-clean so the pre-existing `--features serde` shapelet break is not pulled into a required path (Task 3, verified by the build gate). |
| T-71-03 | Information Disclosure | accessor methods returning `&[f64]` | low | accept | Accessors expose already-public fields (`beta_t`, `fitted_values`) as read-only slices; no new data exposed. Rationale: additive read-only getters over fields that are already `pub`. |
| T-71-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager installs in this phase — no `Cargo.toml` change, no new dependency (locked decision). Nothing to audit. |
</threat_model>

<verification>
## Overall Phase Checks

Run in order; scoped checks during iteration, whole-crate gate last.

- Scoped (iteration): `cargo test -p fdars-core --features linalg,parallel wavelet 2>&1 | tail -30`
- Doctest (SC3): `cargo test -p fdars-core --features linalg,parallel --doc wavelet 2>&1 | tail -20` — the wavelet module doctest must actually run.
- WHOLE-CRATE (SC4 — final, SLOW, hundreds of tests): `cargo test -p fdars-core --features linalg,parallel 2>&1 | tail -15`
- Clippy: `cargo clippy --all-targets --features linalg,parallel -- -D warnings` (CI lints test/bench code — `--all-targets` is mandatory).
- Format: `cargo fmt` per commit, then `cargo fmt --check`.

## EXECUTOR NOTES (project memory — read before committing)

- The pre-commit hook runs the full suite and TIMES OUT (~30s). Run all gates OUT-OF-BAND (above), then commit with `git commit --no-verify`. Run `cargo fmt` per commit so `--no-verify` does not leave fmt drift for CI.
- `/home` is ~94% full. If a build fails on disk space (e.g. "No space left", or an example LINK failure "linking with cc failed"), run `rm -rf target/debug/{incremental,examples}` to free space and retry — this is NOT a code bug.
- If executor subagents stall on long cargo builds, execute inline and `commit --no-verify` after the out-of-band gates pass.
- Pre-existing (unrelated) `--features serde` break in `shapelet/classifier.rs` (Phase 60): do NOT run the serde build as a gate for this phase and do NOT let the new re-exports depend on serde. The default/linalg/parallel builds are the gates.
</verification>

<success_criteria>
- SC1 (WAV-05): `WcrResult::predict` and `WnetResult::predict` reproduce the stored training `fitted_values` within 1e-8 absolute when re-passed the training curves; prediction on fresh curves is finite; a training-grid-length mismatch returns `FdarError`, never panics.
- SC2 (WAV-05): `beta_t()`/`coefficient_function()` and `fitted_values()` on both result structs return the stored slices with the expected lengths (m and n).
- SC3 (WAV-06): the full surface (decompose/reconstruct/decompose_matrix/max_level/WaveletFamily/BoundaryMode/WaveletCoeffs + wcr/wnet + Wcr*/Wnet* config/result types) is reachable via BOTH `fdars_core::prelude::*` AND crate-root paths; a running end-to-end module doctest passes under `cargo test --doc`.
- SC4 (WAV-06): additive/non-breaking — whole-crate `cargo test` + `cargo clippy --all-targets --features linalg,parallel -- -D warnings` + `cargo fmt --check` all green; the 28 examples and R/WASM bindings still build; no existing public signature changed; no `Cargo.toml` change.
</success_criteria>

<output>
Create `.planning/phases/71-prediction-diagnostics-integration/71-01-SUMMARY.md` when done.
</output>
