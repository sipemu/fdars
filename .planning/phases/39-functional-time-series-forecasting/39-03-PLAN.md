---
phase: 39-functional-time-series-forecasting
plan: 03
type: execute
wave: 3
depends_on: [39-01]
files_modified:
  - fdars-core/src/fts/forecast.rs
  - fdars-core/src/fts/mod.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [FTS-01-03]
estimate:
  tokens: 55000
  raw_tokens: 27000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "A user can call fplsr(data, ncomp, argvals) to obtain a PLS-score-based 1-step-ahead forecast of the next curve as an alternative to FPC-score regression (FTS-01-03)."
    - "fplsr uses a lag-1 design (predictor = current curve, response = next curve) reusing existing PLS machinery, not a new PLS subsystem."
    - "fplsr produces a valid finite forecast and in-sample fitted curves; on AR-structured data it is a usable alternative point forecast."
    - "Invalid inputs (empty/too-short series, ncomp out of range, argvals mismatch, degenerate columns) return FdarError, never panic."
  artifacts:
    - "fplsr public entry point"
    - "FplsrResult public result struct (forecast 1 x m, fitted (n-1) x m, ncomp)"
    - "FplsrResult wiring in fts/mod.rs + crate-root re-export in src/lib.rs"
  key_links:
    - "fplsr delegates per-evaluation-point PLS to regression::fdata_to_pls_1d over the lag-1 design (X_cur rows 0..n-2, X_next rows 1..n-1) — no custom NIPALS."
    - "fplsr forecast projects the last curve via each per-point PlsResult and applies its regression coefficients to assemble the 1 x m forecast curve."
---

<objective>
Add the functional PLS forecasting variant `fplsr` (FTS-01-03) — the final expansion task, independent of Plan 02, building only on the module scaffolding from Plan 01. It uses a lag-1 PLS design (regress the next curve on the current curve) via per-evaluation-point scalar PLS reusing `fdata_to_pls_1d`, and declares the `FplsrResult` struct + wiring.

Purpose: fplsr shares the file with Plan 01/02 but is functionally independent of the AR forecasting path, so it forms its own wave to avoid same-file conflict while completing the fifth entry point.
Output: `fplsr`, `FplsrResult` struct + wiring, inline tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-functional-time-series-forecasting/39-CONTEXT.md
@.planning/phases/39-functional-time-series-forecasting/39-RESEARCH.md
@.planning/phases/39-functional-time-series-forecasting/39-PATTERNS.md
@.planning/phases/39-functional-time-series-forecasting/39-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan adds: `fplsr` (public entry point), `FplsrResult` (public result struct: forecast 1 x m, fitted (n-1) x m, ncomp). Wiring: add `FplsrResult` declaration to `fts/mod.rs`, add `fplsr` + `FplsrResult` to the `pub use forecast::{...}` block in `fts/mod.rs` and the crate-root `pub use fts::{...}` block in `src/lib.rs` (extend the blocks created in Plan 01). See the full phase artifact list in 39-01-PLAN.md.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Declare FplsrResult + wire exports (FTS-01-03 scaffolding)</name>
  <files>fdars-core/src/fts/mod.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/fts/mod.rs (the FtsmResult/FtsmForecastResult declarations added in Plan 01 — mirror the exact derive chain; the `pub use forecast::{...}` block to extend)
    - fdars-core/src/lib.rs (the `pub use fts::{...}` block extended in Plan 01 — add fplsr, FplsrResult keeping the existing alphabetical grouping)
    - .planning/phases/39-functional-time-series-forecasting/39-PATTERNS.md (lines 409-425: FplsrResult struct definition + fields; lines 443-448: lib.rs export block)
  </read_first>
  <precondition>Build/doctest linking requires TMPDIR=/home/simonm/.cache/fdars-bench-tmp; export it before any cargo build/test/doctest invocation.</precondition>
  <action>
    Declare `pub struct FplsrResult` in `fdars-core/src/fts/mod.rs` next to the other FTS result structs, using the identical four-attribute chain: `#[derive(Debug, Clone, PartialEq)]` + `#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]` + `#[non_exhaustive]`, with per-field doc comments. Fields: forecast (FdMatrix, 1 x m predicted next curve), fitted (FdMatrix, (n-1) x m in-sample lag-1 fitted curves), ncomp (usize, PLS components used). Extend the `pub use forecast::{...}` line in mod.rs to add `fplsr` and `FplsrResult`. Extend the crate-root `pub use fts::{...}` block in `src/lib.rs` to add `fplsr` and `FplsrResult`, keeping the existing grouping (functions then result structs). Keep the crate compiling at this commit — either add a `pub fn fplsr` stub that returns a valid FplsrResult path or land the export together with Task 2 in one commit; the executor keeps each commit green.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --features linalg,parallel 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `fdars-core/src/fts/mod.rs` contains `pub struct FplsrResult` with the four-attribute derive chain and fields forecast, fitted, ncomp.
    - `fdars-core/src/fts/mod.rs` `pub use forecast::{...}` references `fplsr` and `FplsrResult`.
    - `fdars-core/src/lib.rs` `pub use fts::{...}` references `fplsr` and `FplsrResult`.
    - `cargo build -p fdars-core --features linalg,parallel` (TMPDIR set) exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">New additive struct + export lines; no existing public signature changed.</reversibility>
  <done>FplsrResult declared and exported from mod.rs + lib.rs; crate builds green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fplsr — lag-1 per-point PLS functional forecasting (FTS-01-03)</name>
  <files>fdars-core/src/fts/forecast.rs</files>
  <read_first>
    - fdars-core/src/regression.rs (lines 406-417: PlsResult fields + project; line 614 area: fdata_to_pls_1d signature + ncomp clamping)
    - fdars-core/src/scalar_on_function/pls.rs (lines 45-183: fregre_pls validation + PLS call pattern to mirror per evaluation point)
    - .planning/phases/39-functional-time-series-forecasting/39-RESEARCH.md (lines 350-374: fplsr algorithm Option A per-point PLS; lines 588-594: Pitfall 2 overfitting guard ncomp < n-1)
    - .planning/phases/39-functional-time-series-forecasting/39-PATTERNS.md (lines 284-296: fdata_to_pls_1d call pattern + lag-1 design + ncomp clamp)
    - fdars-core/src/fts/forecast.rs (validate_fts_input + error patterns from Plan 01 — reuse)
  </read_first>
  <action>
    Implement `pub fn fplsr(data: &FdMatrix, ncomp: usize, argvals: &[f64]) -> Result<FplsrResult, FdarError>` with `#[must_use = "expensive computation whose result should not be discarded"]`. Validation: validate_fts_input(data, argvals)?; `ncomp == 0` -> InvalidParameter; `n < 3` -> InvalidParameter (need >= 2 lag-1 rows plus a forecast origin); clamp `ncomp = ncomp.min(n - 1).min(m)` (lag-1 design has n-1 rows — mirrors pls.rs clamping) and guard against overfitting per Pitfall 2. Build the lag-1 design: X_cur = rows 0..n-2 of data (shape (n-1) x m), X_next = rows 1..n-1 (shape (n-1) x m). For each evaluation point j in 0..m: extract the scalar response y_j = column j of X_next (length n-1) and call `fdata_to_pls_1d(X_cur, &y_j, ncomp, argvals)?` to obtain the per-point PlsResult; use it to (a) produce in-sample fitted values for column j over the (n-1) training rows and (b) project the last observed curve (row n-1 of data, as a 1 x m FdMatrix) via PlsResult::project and apply the per-point regression coefficients to get the scalar forecast at point j. Assemble the m per-point forecasts into a 1 x m forecast curve and the per-point in-sample fits into the (n-1) x m fitted matrix. Return FplsrResult { forecast, fitted, ncomp }. Add rustdoc documenting the divergence from ftsa (per-evaluation-point scalar PLS, not a unified NIPALS/SIMPLS functional operator). This path is deterministic — no rand imports.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel fts::forecast::tests::fplsr_ 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `forecast.rs` contains `pub fn fplsr` returning `Result<FplsrResult, FdarError>`.
    - fplsr_ test: on AR-structured synthetic curves (phi≈0.7, n>=40, ncomp=2), fplsr returns a forecast of shape 1 x m with all-finite entries and fitted of shape (n-1) x m.
    - Usability test: fplsr forecast MSE against the true next curve is finite and no worse than the naive last-curve baseline on AR-structured data (documented as an alternative point forecast).
    - Error-path tests: empty data -> InvalidDimension; ncomp == 0 -> InvalidParameter; n < 3 -> InvalidParameter; argvals mismatch -> InvalidDimension.
    - Determinism test: two identical fplsr calls return equal FplsrResult.
    - `cargo test ... fts::forecast::tests::fplsr_` (TMPDIR set) exits 0; whole fts::forecast suite green.
  </acceptance_criteria>
  <behavior>
    - Test fplsr_produces_finite_forecast: 1 x m all-finite forecast, (n-1) x m fitted on AR(0.7) data.
    - Test fplsr_no_worse_than_naive: forecast MSE <= naive last-curve MSE on AR-structured data.
    - Test fplsr_deterministic: two identical calls equal.
    - Test fplsr_rejects_bad_input: empty/ncomp==0/n<3/argvals-mismatch each return the documented FdarError variant.
  </behavior>
  <reversibility rating="reversible">New additive public fn delegating to shipped PLS machinery; no existing signature touched.</reversibility>
  <done>fplsr produces a finite lag-1 PLS forecast + fitted curves, is deterministic, no worse than naive baseline, rejects invalid inputs; fplsr_ tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → fplsr | Untrusted numeric input (FdMatrix, ncomp, argvals) crosses into the library. No network, filesystem, deserialization of untrusted data, or process spawning. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-08 | Tampering | fplsr entry point | low | mitigate | validate_fts_input + ncomp >= 1 + n >= 3 + argvals length checks at entry; return FdarError, never panic. |
| T-39-09 | Tampering | fplsr overfitting / rank deficiency (ncomp near n-1) | low | mitigate | Clamp ncomp = ncomp.min(n-1).min(m); fdata_to_pls_1d re-clamps per-call; Pitfall-2 guard. Degenerate columns surface as ComputationFailed from the delegated PLS. |
| T-39-10 | Denial of Service | per-point PLS loop (m fits) | low | accept | Bounded by m (caller-supplied grid size); O(m·n·ncomp) — no unbounded growth. Caller controls m. |

No high-severity threats: pure in-process numeric library, no external attack surface. Nothing to block on per ASVS L1 (block-on: high).
</threat_model>

<verification>
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel fts::forecast` green (all Plan 01/02/03 tests).
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings` clean.
- No changes to existing public signatures; no new crate dependency.
</verification>

<success_criteria>
- fplsr produces a finite lag-1 PLS forecast + in-sample fitted curves, no worse than the naive baseline (FTS-01-03).
- All invalid inputs return FdarError; fplsr is deterministic.
- FplsrResult exported from crate root; module lints clean with --all-targets under linalg,parallel.
</success_criteria>

<output>
Create `.planning/phases/39-functional-time-series-forecasting/39-03-SUMMARY.md` when done.
</output>
