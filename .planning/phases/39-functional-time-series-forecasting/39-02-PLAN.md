---
phase: 39-functional-time-series-forecasting
plan: 02
type: execute
wave: 2
depends_on: [39-01]
files_modified:
  - fdars-core/src/fts/forecast.rs
autonomous: true
requirements: [FTS-01-04, FTS-01-05]
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "A user can call ftsm_forecast_multistep(fit, h, argvals) to obtain per-horizon forecast curves for h > 1 via iterative AR plug-in, and the h=1 curve is bit-identical to ftsm_forecast (FTS-01-05)."
    - "A user can call ftsm_update(fit, new_curve, argvals) to update an existing fit as new observation(s) arrive by projecting onto frozen FPC loadings, appending scores, and re-fitting per-score AR models WITHOUT refitting FPCA (FTS-01-04)."
    - "The dynamic-update forecast agrees with a full ftsm refit on the same extended data within a documented relative-L2 tolerance."
    - "Invalid inputs (h < 1, new_curve wrong shape, argvals mismatch) return FdarError, never panic."
  artifacts:
    - "ftsm_forecast_multistep public entry point (h x m forecast)"
    - "ftsm_update public entry point (returns updated FtsmResult with frozen mean/rotation)"
  key_links:
    - "ftsm_forecast is refactored to delegate to ftsm_forecast_multistep(fit, 1, argvals) so h=1 is guaranteed bit-identical (single arithmetic path)."
    - "ftsm_update projects via the frozen fit.rotation/fit.mean/fit.weights (FpcaResult::project arithmetic) and re-fits ArModel per component — never calls fdata_to_pc_1d."
---

<objective>
Expand the proven tracer to the two derived forecasting paths that build on FtsmResult: iterative multi-step forecasting (`ftsm_forecast_multistep`, FTS-01-05) and dynamic forecast update (`ftsm_update`, FTS-01-04). Refactor `ftsm_forecast` to delegate its h=1 case to the multi-step function so the h=1 consistency invariant holds by construction.

Purpose: Multi-step and update reuse the AR machinery + reconstruction proven in Plan 01; keeping them in a separate wave avoids the same-file conflict with Plan 01 and keeps each plan under budget.
Output: `ftsm_forecast_multistep`, `ftsm_update`, refactored `ftsm_forecast`, inline tests (T-04, T-05).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-functional-time-series-forecasting/39-CONTEXT.md
@.planning/phases/39-functional-time-series-forecasting/39-RESEARCH.md
@.planning/phases/39-functional-time-series-forecasting/39-PATTERNS.md
@.planning/phases/39-functional-time-series-forecasting/39-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan adds: `ftsm_forecast_multistep` (public), `ftsm_update` (public). It refactors `ftsm_forecast` (Plan 01) to delegate to `ftsm_forecast_multistep`. Both are exported via the `pub use forecast::{...}` block in `fts/mod.rs` and the crate-root re-export in `src/lib.rs` (extend the blocks Plan 01 created). See the full phase artifact list in 39-01-PLAN.md.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ftsm_forecast_multistep + refactor ftsm_forecast to delegate (FTS-01-05)</name>
  <files>fdars-core/src/fts/forecast.rs</files>
  <read_first>
    - fdars-core/src/fts/forecast.rs (the ArModel::forecast iterative plug-in + ftsm_forecast reconstruction written in Plan 01 — reuse, do not duplicate arithmetic)
    - .planning/phases/39-functional-time-series-forecasting/39-RESEARCH.md (lines 303-347: iterative plug-in recursion; lines 410-427: multi-step spec; lines 664-674: T-04 h=1 consistency test; lines 608-614: Pitfall 4 — implement ftsm_forecast as ftsm_forecast_multistep(fit,1,..) for zero duplication)
    - .planning/phases/39-functional-time-series-forecasting/39-PATTERNS.md (lines 217-227: must_use forecast string; lines 513-517: multistep-as-primary note)
  </read_first>
  <precondition>Build/doctest linking requires TMPDIR=/home/simonm/.cache/fdars-bench-tmp; export it before any cargo build/test/doctest invocation.</precondition>
  <action>
    Implement `pub fn ftsm_forecast_multistep(fit: &FtsmResult, h: usize, argvals: &[f64]) -> Result<FtsmForecastResult, FdarError>` with `#[must_use = "returns forecast result; result should be examined"]`. Validation: `h == 0` -> InvalidParameter { parameter: "h", .. }; `argvals.len() != fit.mean.len()` -> InvalidDimension. For each component k, reconstruct the ArModel from fit.ar_models[k] + fit.scores.column(k) history, then call ArModel::forecast(h) to get h per-horizon score forecasts (iterative plug-in: horizons 1..=h feed already-computed forecasts back into the AR window). Build a h x ncomp forecast-score matrix, then reconstruct each horizon-step curve as forecast[step][j] = fit.mean[j] + Σ_k forecast_scores[step][k] * fit.rotation[(j,k)]. Return FtsmForecastResult { forecast: h x m FdMatrix, h }. Refactor `ftsm_forecast` (from Plan 01) to be a thin wrapper: `ftsm_forecast(fit, h, argvals) = ftsm_forecast_multistep(fit, h, argvals)` — eliminating duplicated arithmetic and guaranteeing the h=1 rows are bit-identical (Pitfall 4). Preserve ftsm_forecast's public signature and must_use exactly.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel fts::forecast::tests::multistep_ 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `forecast.rs` contains `pub fn ftsm_forecast_multistep` returning `Result<FtsmForecastResult, FdarError>` and `ftsm_forecast` now delegates to it.
    - T-04 test (multistep_ prefix): for all j in 0..m, `(ftsm_forecast(&fit,1,&argvals)?.forecast[(0,j)] - ftsm_forecast_multistep(&fit,1,&argvals)?.forecast[(0,j)]).abs() < 1e-12`.
    - Multi-step shape test: `ftsm_forecast_multistep(&fit, 5, &argvals)?.forecast.shape() == (5, m)` and `.h == 5`.
    - Error-path: h=0 returns InvalidParameter.
    - `cargo test ... fts::forecast::tests::multistep_` (TMPDIR set) exits 0; ftsm_/forecast_ tests from Plan 01 still green.
  </acceptance_criteria>
  <behavior>
    - Test multistep_h1_equals_single_step: h=1 multi-step curve bit-identical to ftsm_forecast (< 1e-12 per point).
    - Test multistep_returns_h_rows: h=5 yields a 5 x m forecast matrix.
    - Test multistep_rejects_h_zero: h=0 returns InvalidParameter.
  </behavior>
  <reversibility rating="reversible">New additive public fn + internal refactor of a Plan-01 fn to delegate; public signature unchanged.</reversibility>
  <done>ftsm_forecast_multistep returns per-horizon curves; h=1 matches single-step to < 1e-12; ftsm_forecast delegates; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ftsm_update — dynamic update without FPCA refit (FTS-01-04)</name>
  <files>fdars-core/src/fts/forecast.rs</files>
  <read_first>
    - fdars-core/src/regression.rs (lines 81-103: FpcaResult::project arithmetic — center by mean, multiply by rotation with weights — to replicate against frozen fit fields)
    - .planning/phases/39-functional-time-series-forecasting/39-RESEARCH.md (lines 378-406: dynamic-update algorithm + frozen-loadings invariant; lines 676-691: T-05 update-vs-refit test + tolerance; lines 598-606: Pitfall 3 mean-drift documentation)
    - .planning/phases/39-functional-time-series-forecasting/39-PATTERNS.md (lines 274-278: project call pattern; lines 553-558: anti-pattern — never refit FPCA in ftsm_update)
    - fdars-core/src/fts/forecast.rs (ftsm fit, ArModel::fit, reconstruction from Plan 01 — reuse)
  </read_first>
  <action>
    Implement `pub fn ftsm_update(fit: &FtsmResult, new_curve: &FdMatrix, argvals: &[f64]) -> Result<FtsmResult, FdarError>` with `#[must_use = "returns forecast result; result should be examined"]`. Validation: new_curve must be 1 x m (or k x m for k new obs) with m == fit.mean.len() -> else InvalidDimension; argvals.len() == m -> else InvalidDimension. Project each new-curve row onto the FROZEN loadings using the FpcaResult::project arithmetic against fit.rotation, fit.mean, fit.weights: new_score[k] = Σ_j (new_curve[(r,j)] - fit.mean[j]) * fit.rotation[(j,k)] * fit.weights[j], producing k_new x ncomp new scores. Do NOT call fdata_to_pc_1d — mean and rotation stay frozen (anti-pattern to refit). Build the extended scores matrix (fit.scores rows followed by the new-score rows), re-fit ArModel::fit per component on each extended score column (same AIC selection), and rebuild ar_models (Vec<ArModelResult>). Rebuild fitted for the extended series via the reconstruction arithmetic (mean + Σ_k score·rotation). Return an updated FtsmResult { mean: fit.mean (frozen), rotation: fit.rotation (frozen), weights: fit.weights (frozen), scores: extended_scores, fitted: extended_fitted, ncomp: fit.ncomp, ar_models: rebuilt }. Add rustdoc noting the frozen-mean divergence from a full refit (Pitfall 3): "The dynamic update freezes the mean curve from the initial ftsm fit; for long update sequences, periodic full refit via ftsm is recommended."
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel fts::forecast::tests::update_ 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `forecast.rs` contains `pub fn ftsm_update` returning `Result<FtsmResult, FdarError>`.
    - T-05 test (update_ prefix): fit ftsm on data[0..n-1]; updated = ftsm_update(fit, last_row); full_refit = ftsm(data[0..n]); relative-L2 between updated 1-step forecast and refit 1-step forecast < 0.01 (1% tolerance).
    - Frozen-loadings assertion: `updated.mean == fit.mean` and `updated.rotation == fit.rotation` (bitwise equal — proving no FPCA refit).
    - `updated.scores` has one more row than `fit.scores`.
    - Error-path: new_curve with wrong m returns InvalidDimension.
    - `cargo test ... fts::forecast::tests::update_` (TMPDIR set) exits 0; full `fts::forecast` suite green.
  </acceptance_criteria>
  <behavior>
    - Test update_agrees_with_refit: 1-step forecast from update within 1% relative-L2 of full-refit forecast.
    - Test update_freezes_loadings: updated.mean and updated.rotation exactly equal the input fit's (no FPCA refit).
    - Test update_extends_scores: updated.scores row count == fit.scores rows + 1.
    - Test update_rejects_bad_shape: new_curve with mismatched columns returns InvalidDimension.
  </behavior>
  <reversibility rating="reversible">New additive public fn; reuses frozen state; no existing signature touched.</reversibility>
  <done>ftsm_update projects onto frozen loadings, appends scores, re-fits AR without FPCA refit, agrees with full refit within 1%, and rejects bad shapes; update_ tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → ftsm_forecast_multistep / ftsm_update | Untrusted numeric input (FtsmResult, new_curve, h, argvals) crosses into the library. No network, filesystem, deserialization of untrusted data, or process spawning. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-05 | Tampering | ftsm_forecast_multistep / ftsm_update entry points | low | mitigate | Validate h >= 1, new_curve shape (k x m, m == fit.mean.len()), argvals length at entry; return FdarError, never panic. |
| T-39-06 | Tampering | ftsm_update projection (weight/dimension mismatch) | low | mitigate | Reuse frozen fit.rotation/weights/mean with dimension checks before projection; no reallocation of loadings. |
| T-39-07 | Denial of Service | multi-step iterative loop | low | accept | Loop bounded by caller-supplied h; O(h·ncomp·p) per forecast — no unbounded growth; h validated >= 1. Caller controls h. |

No high-severity threats: pure in-process numeric library, no external attack surface. Nothing to block on per ASVS L1 (block-on: high).
</threat_model>

<verification>
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel fts::forecast` green (all Plan 01 + 02 tests).
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings` clean.
- No changes to existing public signatures; no new crate dependency.
</verification>

<success_criteria>
- ftsm_forecast_multistep returns per-horizon curves; h=1 bit-identical to ftsm_forecast (FTS-01-05).
- ftsm_update updates without FPCA refit and agrees with full refit within 1% (FTS-01-04).
- All invalid inputs return FdarError.
- Module lints clean with --all-targets under linalg,parallel.
</success_criteria>

<output>
Create `.planning/phases/39-functional-time-series-forecasting/39-02-SUMMARY.md` when done.
</output>
