---
phase: 79-serde-feature-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/classification/fit.rs
  - fdars-core/src/classification/mod.rs
  - fdars-core/src/tolerance/types.rs
  - fdars-core/src/elastic_fpca.rs
  - fdars-core/tests/serde_feature_roundtrip.rs
autonomous: true
requirements:
  - BUILD-01
estimate:
  tokens: 55000
  raw_tokens: 34000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "`cargo build -p fdars-core --features serde` exits 0 (was 12 trait-bound errors, broken since Phase 60) — BUILD-01"
    - "`cargo build -p fdars-core --features linalg,parallel,serde` exits 0 (the CI feature set) — BUILD-01"
    - "A ClassifFit constructed value survives serde_json serialize→deserialize and compares PartialEq-equal — BUILD-01"
    - "cargo fmt --check and clippy --all-targets (serde feature set, CI -A allows) are clean — BUILD-01"
  artifacts:
    - "fdars-core/src/classification/fit.rs (serde cfg_attr on ClassifFit + ClassifMethod)"
    - "fdars-core/src/classification/mod.rs (serde cfg_attr on ClassifResult)"
    - "fdars-core/src/tolerance/types.rs (serde cfg_attr on NonConformityScore)"
    - "fdars-core/src/elastic_fpca.rs (serde cfg_attr on JointFpcaResult)"
    - "fdars-core/tests/serde_feature_roundtrip.rs (round-trip regression test, serde-gated)"
  key_links:
    - "ClassifFit embeds ClassifResult + ClassifMethod — deriving serde on ClassifFit is a no-op unless BOTH embedded types also derive it (the cascade)"
    - "The existing CI step `cargo test --features linalg,parallel,serde` (rust-ci.yml:55) is the durable re-breakage guard; it compiles the new test on every push"
---

<objective>
Restore `cargo build --features serde` (broken since Phase 60) by adding the crate's conditional serde derives to the three public result types that lack them — `ClassifFit`, `NonConformityScore`, `JointFpcaResult` — plus the crate-owned types `ClassifFit` embeds (`ClassifResult`, `ClassifMethod`), and land a serde round-trip regression test so a future missing-derive gap fails a test, not just a build.

Purpose: A `--features serde` build/test is exercised by CI (rust-ci.yml:55/77) but has been RED since Phase 60, silently blocking any serde-dependent consumer (persistence, R/WASM binding workflows). This is a mechanical, additive, behavior-preserving fix — no new dependency, no API change.

Output: Both serde build configs compile, a round-trip test asserts ClassifFit survives serialize→deserialize, and the whole-crate gates stay green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-serde-feature-repair/79-CONTEXT.md

# The three named types and their embedded crate types (read before editing)
@fdars-core/src/classification/fit.rs
@fdars-core/src/classification/mod.rs
@fdars-core/src/tolerance/types.rs
@fdars-core/src/elastic_fpca.rs
</context>

## Artifacts this phase produces

- **New test file:** `fdars-core/tests/serde_feature_roundtrip.rs` containing a `#[cfg(feature = "serde")]`-gated integration test `classiffit_serde_roundtrip` that constructs a small `ClassifFit`, `serde_json::to_string`s it, `serde_json::from_str`s it back, and `assert_eq!`s the round trip via `PartialEq`.
- **New derives (additive only):** `#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]` added alongside the existing `#[derive(...)]` on `ClassifFit`, `ClassifMethod` (fit.rs), `ClassifResult` (classification/mod.rs), `NonConformityScore` (tolerance/types.rs), and `JointFpcaResult` (elastic_fpca.rs). No fields removed, no `#[serde(skip)]` expected (all embedded field types are crate-owned-with-serde or std types).
- No behavior change, no new dependency, no API break.

<reproduction_note>
Confirmed by a live `cargo build -p fdars-core --features serde` at plan time: exactly 12 trait-bound errors, from `shapelet/classifier.rs` (needs `ClassifFit: Serialize/Deserialize`), `tolerance/conformal_anomaly.rs` (needs `NonConformityScore`), and `jfpca_model.rs` (needs `JointFpcaResult`). `ClassifFit` embeds `ClassifResult` (classification/mod.rs) and `ClassifMethod` (fit.rs) — both currently only `#[derive(Debug, Clone, PartialEq)]`, so they MUST gain the derive too or the `ClassifFit` derive fails to compile. `FdMatrix` (matrix.rs:39) already has the serde cfg_attr, so `fpca_rotation`/`fpca_scores`/`probabilities` need no further work. Do not assume this list is exhaustive — Task 3's gate is a clean build of BOTH configs, and any new missing-derive error it surfaces on a crate-owned type gets the same one-line cfg_attr treatment.
</reproduction_note>

<tasks>

<task type="tracer">
  <name>Task 1: Land serde on ClassifFit + its embedded types and prove the round-trip end-to-end</name>
  <files>fdars-core/src/classification/fit.rs, fdars-core/src/classification/mod.rs, fdars-core/tests/serde_feature_roundtrip.rs</files>
  <read_first>fdars-core/src/classification/fit.rs (ClassifFit at line 49, ClassifMethod at line 21), fdars-core/src/classification/mod.rs (ClassifResult at line 35), fdars-core/src/matrix.rs (confirm FdMatrix already has the serde cfg_attr at line 39)</read_first>
  <action>This is the thin end-to-end slice: pick the headline type `ClassifFit` and wire serde through EVERY layer it touches, then prove it with a real round-trip. Add the line `#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]` immediately below the existing `#[derive(Debug, Clone, PartialEq)]` (and above `#[non_exhaustive]`) on THREE types: `ClassifFit` and `ClassifMethod` in fit.rs, and `ClassifResult` in classification/mod.rs. Use the exact crate-established form verbatim per D-decision "Derive convention" (as in pda.rs:64, multi_fdata.rs:41, matrix.rs:39). Then create `fdars-core/tests/serde_feature_roundtrip.rs` as a `#[cfg(feature = "serde")]`-gated integration test named `classiffit_serde_roundtrip`: fit a small `ClassifFit` via `fdars_core::classification::fclassif_lda_fit` on a tiny 2-class dataset (or construct one from a minimal example already in the classification tests), `serde_json::to_string(&fit)`, `serde_json::from_str::<ClassifFit>(&json)`, and `assert_eq!(fit, roundtrip)`. Do NOT touch NonConformityScore or JointFpcaResult in this task — they are the horizontal expansion in Task 2. Do NOT write the literal derive attribute or feature-gate string inside any prose comment in the test file.</action>
  <verify>
    <automated>cargo test -p fdars-core --features linalg,parallel,serde --test serde_feature_roundtrip classiffit_serde_roundtrip</automated>
    <fails_when>non-zero exit, or "could not compile", or "the trait bound", or "test result: FAILED", or the named test reports "0 passed"</fails_when>
  </verify>
  <done>ClassifFit, ClassifMethod, and ClassifResult carry the conditional serde derive; the round-trip test compiles under the serde feature and passes (a constructed ClassifFit serializes to JSON and deserializes back PartialEq-equal). The single ClassifFit serde path works end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Expand serde to NonConformityScore and JointFpcaResult, then chase the cascade to a clean serde-only build</name>
  <files>fdars-core/src/tolerance/types.rs, fdars-core/src/elastic_fpca.rs</files>
  <read_first>fdars-core/src/tolerance/types.rs (NonConformityScore at line 28), fdars-core/src/elastic_fpca.rs (JointFpcaResult at line 73)</read_first>
  <action>Add the same `#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]` line below the existing `#[derive(...)]` (and above `#[non_exhaustive]`) on `NonConformityScore` (a fieldless C-like enum — no embedded types, no cascade) and on `JointFpcaResult` (embeds only `FdMatrix` — already serde — plus `Vec<f64>` and `f64`, so no further cascade expected). Then run `cargo build -p fdars-core --features serde` (the exact minimal broken config, no linalg/parallel) and read the output: if it surfaces any NEW missing-derive error on a crate-owned type not yet covered, add the same one-line cfg_attr to that type and re-run. Iterate until the build is clean. Per D-decision, only if a field's foreign type genuinely cannot derive serde do you fall back to `#[serde(skip)]` / a documented shim (not expected here) — first prefer adding the derive to the crate-owned embedded type, and record any skip in the SUMMARY.</action>
  <verify>
    <automated>cargo build -p fdars-core --features serde</automated>
    <fails_when>non-zero exit, or "could not compile", or "the trait bound", or "is not implemented for" in the output</fails_when>
  </verify>
  <done>The minimal `--features serde` build (no linalg/parallel) compiles cleanly — zero trait-bound errors. All three named types plus any cascade-surfaced crate types carry the conditional serde derive.</done>
</task>

<task type="auto">
  <name>Task 3: Green both build configs and the whole-crate gates (fmt, clippy, tests)</name>
  <files>.github/workflows/rust-ci.yml (read-only reference; no edit required unless adding the optional serde-only build step)</files>
  <read_first>.github/workflows/rust-ci.yml (line 55 serde test step, line 77 clippy allows), .claude/CLAUDE.md (build/commit conventions)</read_first>
  <action>Run the full gate set exactly as CI does and confirm all green: (1) the CI feature-set build `cargo build -p fdars-core --features linalg,parallel,serde`; (2) `cargo fmt` then `cargo fmt --check`; (3) clippy with the CI allows `cargo clippy --all-targets --features linalg,parallel,serde -- -D warnings -A clippy::too_many_arguments -A clippy::useless_vec -A clippy::type_complexity -A clippy::manual_memcpy -A clippy::wildcard_in_or_patterns`; (4) the serde test matrix `cargo test -p fdars-core --features linalg,parallel,serde`. Fix any fmt drift or clippy warning introduced by the edits (none expected — additive attributes only). Confirm in the SUMMARY that the existing CI step `cargo test --features linalg,parallel,serde` (rust-ci.yml:55) is the durable re-breakage guard (it was RED on serde since Phase 60 and turns green now), per D-decision "Re-breakage guard"; adding an explicit serde-only build step to CI is optional (Claude's discretion) — if skipped, state so. Commit with `git commit --no-verify` (30s hook timeout on the full suite) and `cargo fmt` applied per the project conventions.</action>
  <verify>
    <automated>cargo build -p fdars-core --features linalg,parallel,serde && cargo fmt --check && cargo clippy --all-targets --features linalg,parallel,serde -- -D warnings -A clippy::too_many_arguments -A clippy::useless_vec -A clippy::type_complexity -A clippy::manual_memcpy -A clippy::wildcard_in_or_patterns && cargo test -p fdars-core --features linalg,parallel,serde --test serde_feature_roundtrip</automated>
    <fails_when>non-zero exit from any stage, or "could not compile", or "Diff in" (fmt drift), or a clippy "warning:"/"error:" line, or "test result: FAILED"</fails_when>
  </verify>
  <done>Both build configs (`--features serde` and `--features linalg,parallel,serde`) compile; fmt is clean; clippy is clean under the CI feature set and allows; the serde round-trip test passes. BUILD-01 gate satisfied end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This phase adds compile-time derive attributes only. No new trust boundary is introduced — no I/O, no network, no untrusted-input parsing path is created beyond serde codepaths that the crate already exposes for all other result types. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-79-01 | Tampering | serde deserialization of `ClassifFit`/`JointFpcaResult`/`NonConformityScore` from untrusted JSON | low | accept | These types join the crate's existing serde surface (FdMatrix, FpcaResult, ~most result structs). Deserialization uses derived impls with `#[non_exhaustive]` enums; no custom unsafe deserializer. Consumers deserializing untrusted input carry the same risk as every other serde-enabled type; out of scope for an additive build fix. |
| T-79-02 | Denial of Service | large/deeply-nested JSON into derived Deserialize | low | accept | serde_json enforces its own recursion/size limits; the added types hold flat Vec/FdMatrix fields with no recursive nesting. No amplification introduced. |
| T-79-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this phase — `serde` and `serde_json` are already existing optional deps (Cargo.toml, behind the `serde` feature). No new dependency added. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
Phase-level checks (all must be green):

1. `cargo build -p fdars-core --features serde` — exits 0 (the minimal config that was broken since Phase 60).
2. `cargo build -p fdars-core --features linalg,parallel,serde` — exits 0 (the CI feature set).
3. `cargo test -p fdars-core --features linalg,parallel,serde --test serde_feature_roundtrip` — the round-trip test passes.
4. `cargo fmt --check` — clean.
5. `cargo clippy --all-targets --features linalg,parallel,serde -- -D warnings` + the 5 CI `-A` allows — clean.
6. `cargo test -p fdars-core --features linalg,parallel,serde` — full serde matrix green (the durable CI re-breakage guard).
</verification>

<success_criteria>
- Both `--features serde` and `--features linalg,parallel,serde` builds compile cleanly (BUILD-01).
- `ClassifFit`, `ClassifMethod`, `ClassifResult`, `NonConformityScore`, `JointFpcaResult` (plus any cascade-surfaced crate types) carry `#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]`.
- A serde-gated round-trip test asserts a constructed `ClassifFit` survives serialize→deserialize PartialEq-equal.
- fmt, clippy (CI feature set + allows), and the full serde test matrix are all green.
- Fix is additive/non-breaking: no fields removed, no API change, no new dependency; R + WASM bindings and 28 examples unaffected.
</success_criteria>

<output>
Create `.planning/phases/79-serde-feature-repair/79-01-SUMMARY.md` when done. Include: the final list of types that gained the derive (named types + any cascade), whether any `#[serde(skip)]` shim was needed (expected: none), the round-trip test name, and a one-line confirmation that rust-ci.yml:55 `cargo test --features linalg,parallel,serde` is the durable re-breakage guard (RED since Phase 60, now green) — plus whether an explicit serde-only CI build step was added or intentionally skipped.
</output>