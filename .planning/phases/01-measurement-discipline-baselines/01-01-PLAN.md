---
phase: 01-measurement-discipline-baselines
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/benches/audit_hotpaths.rs
  - fdars-core/Cargo.toml
  - .planning/research/bench/
  - .planning/research/AUDIT-REPORT.md
autonomous: true
requirements: [PERF-02]
user_setup: []

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The audit measurement apparatus is proven end-to-end on ONE sentinel: it compiles under all 4 feature combos, runs in release, and produces a saved raw artifact (D-04 tracer)."
    - "A criterion bench binary for `audit_hotpaths` executes from a `target/release/deps/` path (Pitfall 1 / SC3)."
    - "The 4-combo feature-flag matrix (`\"\"`, `parallel`, `linalg`, `linalg,parallel`) is validated on a sentinel that genuinely differs across combos (D-04, Pitfall 18)."
    - "Raw criterion stdout is saved under `.planning/research/bench/` using the `p1_<target>_<features>_run<N>.txt` naming convention (D-06)."
    - "The audit bench wraps both inputs AND outputs in `criterion::black_box` and registers via a `[[bench]]` entry with `harness = false` matching the existing convention (D-02, Pitfall 3)."
  artifacts:
    - "fdars-core/benches/audit_hotpaths.rs (seeded column-major generator + FPCA/SVD sentinel + 4-combo sentinel)"
    - "fdars-core/Cargo.toml [[bench]] audit_hotpaths entry (harness = false)"
    - ".planning/research/bench/ directory"
    - ".planning/research/bench/p1_fpca_linalg,parallel_run1.txt"
    - ".planning/research/bench/p1_karcher_none_run1.txt (or p1_fpca_* if A5 disproven)"
    - ".planning/research/AUDIT-REPORT.md (seeded, records the 4-combo sentinel choice + A5 finding)"
  key_links:
    - "audit_hotpaths.rs → fdars_core::regression::fdata_to_pc_1d (FPCA module baseline sentinel, D-03)"
    - "audit_hotpaths.rs → fdars_core::alignment::karcher_mean (4-combo feature-matrix sentinel, D-04, exercises iter_maybe_parallel!)"
    - "cargo bench --features flags → same source compiles 4 different binaries (Pitfall 18)"
---

## User story (MVP framing)

> **As an** fdars performance auditor, **I want to** prove the entire benchmark measurement apparatus (release build + feature-flag matrix + black_box + release-path confirmation + raw-artifact capture) works end-to-end on a single sentinel, **so that** every downstream per-module baseline and every later-phase sweep inherits a validated, reproducible harness instead of discovering apparatus bugs after ten curves are already measured.

*(ROADMAP Goal line for this phase is stated as an outcome, not in `As a / I want to / so that` form; the story above is derived from the auditor perspective without adding scope. If a strict user-story goal line is wanted, run `/gsd mvp-phase 01`.)*

<objective>
Lead the phase with a single production-quality TRACER slice that wires the WHOLE measurement apparatus through ONE sentinel and verifies it before any expansion. Author `fdars-core/benches/audit_hotpaths.rs` (`harness = false`) containing the seeded column-major generator, the FPCA/SVD module baseline sentinel (`fdata_to_pc_1d`, D-03), and the 4-combo feature-matrix sentinel (D-04); register the `[[bench]]` entry; create `.planning/research/bench/`; seed `.planning/research/AUDIT-REPORT.md`. Prove: compiles under all 4 feature combos, runs in release with `/release/` path confirmed, black_box wraps inputs and outputs, raw stdout saved under `bench/`.

Purpose: Validate the apparatus (Pitfall 18) on the agent's best early-context tokens so an architectural dead-end in the harness surfaces after one commit, not ten.
Output: `audit_hotpaths.rs` (2 sentinels), the Cargo `[[bench]]` entry, the `bench/` dir, and a seeded `AUDIT-REPORT.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-measurement-discipline-baselines/01-CONTEXT.md
@.planning/phases/01-measurement-discipline-baselines/01-RESEARCH.md
@.planning/phases/01-measurement-discipline-baselines/01-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 01 subset)

| Artifact | Path | Notes |
|----------|------|-------|
| Audit bench file | `fdars-core/benches/audit_hotpaths.rs` | new; created here, expanded in Plan 02 |
| Cargo bench entry | `fdars-core/Cargo.toml` → `[[bench]] name = "audit_hotpaths"` | `harness = false` |
| Seeded generator fn | `generate_curves(n, m) -> (FdMatrix, Vec<f64>)` inside the bench | column-major, deterministic-trig |
| FPCA module sentinel | `bench_fpca_sentinel` → `fdata_to_pc_1d` (D-03 module baseline) | one cell N=500,M=200 |
| 4-combo sentinel | `bench_matrix_sentinel` → `karcher_mean` (D-04) | exercises `iter_maybe_parallel!` |
| Raw artifact dir | `.planning/research/bench/` | new directory |
| Report seed | `.planning/research/AUDIT-REPORT.md` | records 4-combo sentinel choice + A5 finding |
| Raw run artifacts | `.planning/research/bench/p1_fpca_linalg,parallel_run1.txt`, `p1_karcher_<features>_run1.txt` (4 files) | tracer proof |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | No trust boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | (n/a) | audit_hotpaths.rs bench | low | accept | No attack surface: analysis-only audit phase; new code is a dev-dependency criterion bench over locally-generated synthetic in-memory data. No network, no untrusted input, no persistence beyond local text files, no change to shipped `fdars-core` public API. `StdRng` (if used) is for reproducibility, not security. |

No high-severity threats. API-coverage / assumption-delta / schema-push detectors do NOT fire (no external API/SDK, no singular→plural seam, no ORM/schema).
</threat_model>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end audit apparatus on the FPCA + 4-combo sentinels — one path only</name>
  <files>fdars-core/benches/audit_hotpaths.rs, fdars-core/Cargo.toml, .planning/research/bench/, .planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - fdars-core/benches/regression_benchmarks.rs (lines 1-11 imports; 18-44 generator; 58-74 bench_fpca group; 142-148 criterion_group/main) — PRIMARY analog, copy scaffold verbatim
    - fdars-core/benches/alignment_benchmarks.rs (lines 17-31 generator; 84-110 group; 149-155 macros) — column-major layout `data[i + j*n]`
    - fdars-core/Cargo.toml (lines 54-88 existing `[[bench]]` block) — the three-line entry pattern to mirror
    - .planning/phases/01-measurement-discipline-baselines/01-PATTERNS.md (§Imports pattern; §Seeded generator; §Criterion group scaffold; §black_box; §criterion_group! macros) — the exact code templates
    - .planning/phases/01-measurement-discipline-baselines/01-RESEARCH.md (§Pattern 1-4; §Section 6 Module 2 FPCA; §Section 7 generator; §Open Question 1 = A5)
    - .planning/research/PITFALLS.md (Pitfall 1 release-mode; Pitfall 3 black_box; Pitfall 18 feature matrix)
  </read_first>
  <action>
    Create `fdars-core/benches/audit_hotpaths.rs` with `harness = false`. Include a module-level `//!` doc comment (per PATTERNS.md §Module-level doc comment). Add the seeded column-major generator `generate_curves(n, m) -> (FdMatrix, Vec<f64>)` using the deterministic-trig form from PATTERNS.md §Seeded generator (layout `data[i + j*n]`, built via `fdars_core::matrix::FdMatrix::from_column_major(data, n, m).unwrap()` OUTSIDE any `b.iter()` closure — Pitfall 3). Import only `std::f64::consts::PI` for the generator (no `rand` needed).

    Add TWO sentinel bench groups:
    (a) `bench_fpca_sentinel` — the FPCA/SVD MODULE baseline (D-03): call `fdars_core::regression::fdata_to_pc_1d(black_box(&data), black_box(5usize), black_box(&argvals))` at cell N=500,M=200. Wrap all inputs in `black_box`; the `Result<FpcaResult,_>` return is dropped by Criterion (sufficient for a large struct per RESEARCH §Pattern 2). Tune per PATTERNS.md §sample_size table: `group.sample_size(20)`, `measurement_time` 20s, `warm_up_time` 5s. Always call `group.finish()`.
    (b) `bench_matrix_sentinel` — the 4-COMBO FEATURE-MATRIX sentinel (D-04). RESEARCH Open Question A5 asked whether `fdata_to_pc_1d` centering is parallel-gated. It is NOT: `center_columns` in `src/regression.rs` lines 167-181 uses plain sequential `for` loops and nalgebra SVD is always sequential, so `fdata_to_pc_1d` measures near-identical code between the `parallel` and non-`parallel` combos. Per the tracer substitution rule, use `fdars_core::alignment::karcher_mean(black_box(&data), black_box(&argvals), black_box(10usize), black_box(1e-3), black_box(0.0))` as the 4-combo sentinel — it genuinely differs across combos because its inner N-loop uses `iter_maybe_parallel!` (`src/alignment/karcher.rs:185`). Cell N=100,M=50 to keep the 4 combo runs fast; `group.sample_size(20)`, `measurement_time` 20s, `warm_up_time` 5s; wrap the `KarcherMeanResult` output in `black_box(...)` too. Record the A5 substitution decision in AUDIT-REPORT.md (see below). Both sentinels must call ONLY APIs present in the leanest (`""`) build so all 4 combos compile (both `fdata_to_pc_1d` and `karcher_mean` are non-`linalg`-gated — RESEARCH §Section 2 confirms).

    Register both in `criterion_group!(benches, bench_fpca_sentinel, bench_matrix_sentinel);` + `criterion_main!(benches);`.

    Append to `fdars-core/Cargo.toml` immediately after the last existing `[[bench]]` block (after line 88): a `[[bench]]` entry with `name = "audit_hotpaths"` and `harness = false` (mirror the existing three-line pattern exactly; `name` must equal the file stem).

    Create the directory `.planning/research/bench/` (add a `.gitkeep` if empty).

    Seed `.planning/research/AUDIT-REPORT.md` with a title, a short "Phase 1 — Measurement Discipline & Baselines" intro, and a "4-combo sentinel selection" note stating: FPCA (`fdata_to_pc_1d`) is the module baseline sentinel (D-03) but `karcher_mean` was substituted as the 4-combo feature-matrix sentinel (D-04) because `center_columns` (regression.rs:167-181) is sequential and nalgebra SVD is always sequential, so FPCA does not distinguish the parallel combos; `karcher_mean` uses `iter_maybe_parallel!` and does. (Full methodology + workload-matrix sections are written in Plan 02.)

    Prove the apparatus end-to-end (this is the tracer verification, not decoration):
    1. Compile under all 4 combos with `cargo bench -p fdars-core --bench audit_hotpaths <flags> -- --test` where flags are: `--no-default-features` | `--no-default-features --features parallel` | `--no-default-features --features linalg` | `--features linalg,parallel` (exact invocations in RESEARCH §Section 2). `-- --test` smoke-runs each bench once without measuring.
    2. Run the FPCA sentinel in release at `linalg,parallel` and confirm the executed binary is under `target/release/deps/` (Pitfall 1). Save stdout to `.planning/research/bench/p1_fpca_linalg,parallel_run1.txt` via `2>&1 | tee`. Record the `Running target/release/deps/audit_hotpaths-<hash>` line as the first fact.
    3. Run the `karcher_mean` 4-combo sentinel once per combo, saving `p1_karcher_none_run1.txt`, `p1_karcher_parallel_run1.txt`, `p1_karcher_linalg_run1.txt`, `p1_karcher_linalg,parallel_run1.txt`.

    INFRA NOTE (Pitfall 8 / known env): `/tmp` tmpfs is ~94% full on this machine; bench/doctest linking may fail with `LLVM ERROR: IO failure on output stream: No space left on device` or a SIGBUS. That is an INFRASTRUCTURE failure, not a bench defect. Mitigation: set `TMPDIR` to a location on the larger `/home` disk (e.g. `export TMPDIR=/home/simonm/.cache/fdars-bench-tmp` after `mkdir -p`) or free `/tmp`, then re-run. Do NOT treat a link failure as a code/perf defect.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && for F in "--no-default-features" "--no-default-features --features parallel" "--no-default-features --features linalg" "--features linalg,parallel"; do echo "=== combo: $F ==="; cargo bench -p fdars-core --bench audit_hotpaths $F -- --test 2>&1 | tail -3 || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `fdars-core/benches/audit_hotpaths.rs` exists and contains `fn generate_curves`, `fdata_to_pc_1d`, `karcher_mean`, and `criterion_main!`.
    - `fdars-core/Cargo.toml` contains a `[[bench]]` block with `name = "audit_hotpaths"` and `harness = false`.
    - `cargo bench -p fdars-core --bench audit_hotpaths --no-default-features -- --test` compiles and smoke-runs (leanest combo — proves no `linalg`-gated call).
    - `cargo bench -p fdars-core --bench audit_hotpaths --features linalg,parallel -- --test` compiles and smoke-runs.
    - A real (non-`--test`) `linalg,parallel` run prints a `Running target/release/deps/audit_hotpaths-<hash>` line (`/release/` confirmed, NOT `/debug/`).
    - File `.planning/research/bench/p1_fpca_linalg,parallel_run1.txt` exists, is non-empty, and contains a criterion `time:` line.
    - Files `p1_karcher_none_run1.txt`, `p1_karcher_parallel_run1.txt`, `p1_karcher_linalg_run1.txt`, `p1_karcher_linalg,parallel_run1.txt` exist under `.planning/research/bench/`.
    - `.planning/research/AUDIT-REPORT.md` exists and contains the "4-combo sentinel selection" note recording the `karcher_mean` substitution and the A5 (regression.rs:167-181 sequential) reason.
    - A link failure mentioning "No space left on device" or SIGBUS is recorded as INFRA (retry after `TMPDIR` change), not as a task failure.
  </acceptance_criteria>
  <reversibility rating="reversible">New dev-only bench file + additive Cargo entry; no shipped-library impact (D-01).</reversibility>
  <done>audit_hotpaths.rs + [[bench]] entry exist; bench compiles+smoke-runs under all 4 feature combos; the FPCA sentinel ran in release with `/release/` confirmed; the karcher 4-combo sentinel produced 4 raw artifacts; AUDIT-REPORT.md is seeded with the sentinel-selection note. The whole apparatus is proven on one path and committed.</done>
</task>

</tasks>

<verification>
- All 4 feature combos compile+smoke-run: `for F in ...; do cargo bench -p fdars-core --bench audit_hotpaths $F -- --test; done` (command above).
- Release confirmed: a real run's stdout contains `target/release/deps/audit_hotpaths-`.
- Artifacts present: `ls .planning/research/bench/p1_fpca_linalg,parallel_run1.txt .planning/research/bench/p1_karcher_*_run1.txt` lists 5 files.
- Report seeded: `grep -c "4-combo sentinel" .planning/research/AUDIT-REPORT.md` ≥ 1.
</verification>

<success_criteria>
The measurement apparatus is proven end-to-end on ONE sentinel path: `audit_hotpaths.rs` compiles under all 4 feature combos, the FPCA sentinel runs in release with `/release/` confirmed, the 4-combo `karcher_mean` sentinel produced raw artifacts under `bench/`, and AUDIT-REPORT.md records the sentinel-selection decision. Plan 02 expands to the remaining 5 module sentinels + methodology + workload matrix.
</success_criteria>

<output>
Create `.planning/phases/01-measurement-discipline-baselines/01-01-SUMMARY.md` when done.
</output>
