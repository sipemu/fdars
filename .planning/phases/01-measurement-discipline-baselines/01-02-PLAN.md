---
phase: 01-measurement-discipline-baselines
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - fdars-core/benches/audit_hotpaths.rs
  - .planning/research/bench/
  - .planning/research/AUDIT-REPORT.md
autonomous: true
requirements: [PERF-02]
user_setup: []

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A baseline criterion run for at least one sentinel target PER hot-path module (all 6 modules) is recorded at release + linalg,parallel, /release/ confirmed, saved under .planning/research/bench/ (SC3, D-03)."
    - "Each module baseline has TWO independent invocations so the ±5% two-run variance check can be applied (>10% = LOW CONFIDENCE) (D-03, SC2)."
    - "AUDIT-REPORT.md contains a methodology section documenting --release discipline, the feature-flag matrix, black_box, rustc version capture, ±5% variance, and the infra-vs-code triage rule (SC2, SC4)."
    - "AUDIT-REPORT.md contains a workload-matrix section: per-module N×M cells with realistic sizes and a documented cap justification for expensive modules (elastic N≤500,M≤200; CV N≤500,M≤200) (SC1, PERF-02, D-07)."
  artifacts:
    - "fdars-core/benches/audit_hotpaths.rs expanded with 4 more sentinels (elastic, depth, CV, streaming, smoothing) — total 6 module sentinels"
    - ".planning/research/bench/p1_<module>_linalg,parallel_run1.txt and _run2.txt for all 6 modules (>= 12 files)"
    - ".planning/research/AUDIT-REPORT.md with §Methodology and §Workload Matrix sections"
  key_links:
    - "audit_hotpaths.rs → elastic_self_distance_matrix, fraiman_muniz_1d, fclassif_cv, StreamingFraimanMuniz::depth_batch, nadaraya_watson (5 module sentinels)"
    - "methodology §infra-vs-code triage → PITFALLS.md Pitfall 8 (governs all later benchmark phases)"
    - "workload-matrix cell sizes → CONCERNS.md complexity notes (elastic O(n²·m²)≈60s justifies the N≤500,M≤200 cap)"
---

<objective>
Expand from the proven tracer to the full Phase 1 baseline: add the remaining 5 hot-path module sentinels to `audit_hotpaths.rs`, record a release + `linalg,parallel` baseline (2 independent runs) for all 6 modules under `.planning/research/bench/`, and write the methodology + workload-matrix sections into `.planning/research/AUDIT-REPORT.md`.

Purpose: Satisfy the 4 ROADMAP Success Criteria — one baseline target per module (SC3), a documented methodology (SC2), a justified workload matrix (SC1), and the infra-vs-code triage rule (SC4) — keeping Phase 1 lean (one sentinel per module; deep sweeps are Phases 3-6).
Output: expanded `audit_hotpaths.rs` (6 sentinels), ≥12 raw baseline artifacts, and the seeded report's §Methodology + §Workload Matrix sections.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-measurement-discipline-baselines/01-CONTEXT.md
@.planning/phases/01-measurement-discipline-baselines/01-RESEARCH.md
@.planning/phases/01-measurement-discipline-baselines/01-PATTERNS.md
@.planning/phases/01-measurement-discipline-baselines/01-01-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 02 subset)

| Artifact | Path | Notes |
|----------|------|-------|
| Elastic sentinel | `bench_elastic_sentinel` → `elastic_self_distance_matrix` | cell N=100,M=50 (capped) |
| Depth sentinel | `bench_depth_sentinel` → `fraiman_muniz_1d` | cell N=500,M=200 |
| CV sentinel | `bench_cv_sentinel` → `fclassif_cv` (`method="lda"`) | cell N=100,M=50 (capped) |
| Streaming sentinel | `bench_streaming_sentinel` → `StreamingFraimanMuniz::depth_batch` | cell N=500,M=200 |
| Smoothing sentinel | `bench_smooth_sentinel` → `nadaraya_watson` | cell N=500,M=200 |
| Class-label helper | `make_class_labels(n) -> Vec<usize>` in the bench | for CV sentinel |
| Smoothing data helper | `generate_smoothing_data(n, m)` in the bench | plain `Vec<f64>` (not FdMatrix) |
| Baseline artifacts | `.planning/research/bench/p1_<module>_linalg,parallel_run{1,2}.txt` (6 modules × 2) | ±5% variance inputs |
| Report §Methodology | `.planning/research/AUDIT-REPORT.md` | SC2 + SC4 |
| Report §Workload Matrix | `.planning/research/AUDIT-REPORT.md` | SC1 + D-07 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | No trust boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | (n/a) | audit_hotpaths.rs + report | low | accept | No attack surface: analysis-only audit phase; new code is a dev-dependency criterion bench over locally-generated synthetic in-memory data; report is a local markdown file. No network, no untrusted input, no persistence beyond local text files, no change to shipped `fdars-core` public API. |

No high-severity threats. API-coverage / assumption-delta / schema-push detectors do NOT fire.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add the remaining 5 module sentinels to audit_hotpaths.rs</name>
  <files>fdars-core/benches/audit_hotpaths.rs</files>
  <read_first>
    - fdars-core/benches/audit_hotpaths.rs (the file from Plan 01 — the file being modified)
    - fdars-core/benches/alignment_benchmarks.rs (lines 84-110 `bench_self_distance_matrix` call site, line 93) — elastic sentinel analog
    - fdars-core/benches/depth_benchmarks.rs (line 40 `fraiman_muniz_1d`; lines 58-87 streaming construct-then-query) — depth + streaming analogs
    - fdars-core/benches/smoothing_benchmarks.rs (lines 17-29 data gen; lines 41-51 `nadaraya_watson` call, `.unwrap()` inside iter) — smoothing analog
    - .planning/phases/01-measurement-discipline-baselines/01-PATTERNS.md (§Sentinel Function to Public Path Map; §black_box on both inputs and outputs; §Streaming depth sentinel pattern; §make_class_labels; §generate_smoothing_data)
    - .planning/phases/01-measurement-discipline-baselines/01-RESEARCH.md (§Section 6 Modules 1,3,4,5,6 — signatures + public paths; §Pattern 3 sample_size table)
  </read_first>
  <action>
    Extend `fdars-core/benches/audit_hotpaths.rs` with 4 additional group functions (the FPCA and karcher sentinels already exist from Plan 01). Add helper fns `make_class_labels(n) -> Vec<usize>` and `generate_smoothing_data(n, m) -> (Vec<f64>, Vec<f64>, Vec<f64>)` per PATTERNS.md §generators. Add these imports at the top: `use fdars_core::alignment::elastic_self_distance_matrix;`, `use fdars_core::classification::fclassif_cv;`, `use fdars_core::depth::fraiman_muniz_1d;`, `use fdars_core::smoothing::nadaraya_watson;`, `use fdars_core::streaming_depth::{SortedReferenceState, StreamingFraimanMuniz};`.

    Add the 5 remaining sentinel groups (Phase 1 baseline cell per RESEARCH §Section 8 table; build inputs OUTSIDE `b.iter()`; always `group.finish()`; per-group `sample_size`/`measurement_time` per PATTERNS.md §sample_size table):
    - `bench_elastic_sentinel` → `elastic_self_distance_matrix(black_box(&data), black_box(&argvals), black_box(0.0))` at N=100,M=50 (CAPPED per D-07 — O(n²·m²)). sample_size 20, measurement_time 20s, warm_up 5s. Wrap the `Result<Vec<f64>,_>` output in `black_box(...)`.
    - `bench_depth_sentinel` → `black_box(fraiman_muniz_1d(black_box(&data), black_box(&data), true))` at N=500,M=200 (returns `Vec<f64>` — wrap output). sample_size 30, measurement_time 15s, warm_up 3s. Confirm the exact arg types against depth_benchmarks.rs:40 (slice vs FdMatrix) before wiring.
    - `bench_cv_sentinel` → `fclassif_cv(black_box(&data), black_box(&argvals), black_box(&y), black_box(None), black_box("lda"), black_box(5usize), black_box(5usize), black_box(42u64))` where `y = make_class_labels(n)`, at N=100,M=50 (CAPPED per D-07). sample_size 15, measurement_time 20s, warm_up 5s. Verify the exact signature/arg order against `src/classification/cv.rs` (RESEARCH §Section 6 Module 4 gives it) — there is no existing bench analog for this call, so read the source signature before wiring.
    - `bench_streaming_sentinel` → inside `b.iter`: `let state = SortedReferenceState::from_reference(black_box(&data)); let fm = StreamingFraimanMuniz::new(state, true); fm.depth_batch(black_box(&data))` at N=500,M=200 (construct+query as one unit, per PATTERNS.md §Streaming). sample_size 30, measurement_time 15s, warm_up 3s.
    - `bench_smooth_sentinel` → `nadaraya_watson(black_box(&x), black_box(&y), black_box(&x_new), black_box(bandwidth), black_box("gaussian")).unwrap()` with `(x, y, x_new) = generate_smoothing_data(500, 200)` — smoothing N is #observations, M is prediction-grid size (RESEARCH §Section 6 Module 6). sample_size 30, measurement_time 10s, warm_up 3s.

    Register all six in `criterion_group!(benches, bench_fpca_sentinel, bench_matrix_sentinel, bench_elastic_sentinel, bench_depth_sentinel, bench_cv_sentinel, bench_streaming_sentinel, bench_smooth_sentinel);`. Every sentinel must call ONLY non-`linalg`-gated APIs so the `""` combo still compiles (all 6 are confirmed non-gated in RESEARCH §Section 6 Sentinel Summary Table).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && cargo bench -p fdars-core --bench audit_hotpaths --no-default-features -- --test 2>&1 | tail -5 && cargo bench -p fdars-core --bench audit_hotpaths --features linalg,parallel -- --test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `audit_hotpaths.rs` contains all six functions: `bench_fpca_sentinel`, `bench_matrix_sentinel`, `bench_elastic_sentinel`, `bench_depth_sentinel`, `bench_cv_sentinel`, `bench_streaming_sentinel`, `bench_smooth_sentinel`, all registered in `criterion_group!`.
    - `cargo bench -p fdars-core --bench audit_hotpaths --no-default-features -- --test` compiles and smoke-runs (leanest combo — proves no `linalg`-gated call slipped in).
    - `cargo bench -p fdars-core --bench audit_hotpaths --features linalg,parallel -- --test` compiles and smoke-runs all six groups without panic.
    - Each new group builds its inputs OUTSIDE `b.iter()` and calls `group.finish()`.
    - Elastic and CV sentinels use the capped cells (N=100,M=50) per D-07.
  </acceptance_criteria>
  <done>All six module sentinels compile and smoke-run under both the leanest (`""`) and full (`linalg,parallel`) combos; each uses the Phase 1 baseline cell and the tuned per-group sample_size; capped modules honor D-07.</done>
</task>

<task type="auto">
  <name>Task 2: Record release baseline (2 runs) for all 6 modules under bench/</name>
  <files>.planning/research/bench/</files>
  <read_first>
    - fdars-core/benches/audit_hotpaths.rs (the six sentinels from Task 1)
    - .planning/phases/01-measurement-discipline-baselines/01-RESEARCH.md (§Section 1 tee/artifact capture; §Section 3 release confirmation check; §Section 4 rustc capture)
    - .planning/research/PITFALLS.md (Pitfall 1 release; Pitfall 7 ±5%/2-run; Pitfall 8 infra triage; Pitfall 17 artifact-per-finding)
    - .planning/phases/01-measurement-discipline-baselines/01-01-SUMMARY.md (the p1_fpca run already recorded in Plan 01 — reuse as run1 or re-run cleanly)
  </read_first>
  <action>
    For EACH of the 6 modules, run the sentinel TWICE independently at release + `linalg,parallel` and capture stdout with `tee` to `.planning/research/bench/p1_<module>_linalg,parallel_run1.txt` and `..._run2.txt`, where `<module>` ∈ {fpca, elastic, depth, cv, streaming, smooth}. Command shape per RESEARCH §Section 1: `cargo bench -p fdars-core --bench audit_hotpaths --features linalg,parallel -- <group_filter> 2>&1 | tee .planning/research/bench/p1_<module>_linalg,parallel_run<N>.txt` (use the criterion group-name filter, e.g. `audit_fpca`, to run one sentinel at a time and keep runs tractable). The FPCA run1 from Plan 01 may be reused; ensure a matching run2 exists.

    As the FIRST recorded fact in each artifact, capture the environment + release confirmation (RESEARCH §Section 3-4): `rustc --version`, `cargo --version`, and the `Running target/release/deps/audit_hotpaths-<hash>` line proving `/release/` (NOT `/debug/`). If any run's binary path shows `target/debug/`, STOP — the numbers are invalid (Pitfall 1).

    Apply the ±5% two-run variance rule (Pitfall 7): compare the two median times per module; if they differ by >10%, tag that module's baseline LOW CONFIDENCE in the artifact and in the report (Plan 02 Task 3).

    INFRA NOTE (Pitfall 8 / known env, MANDATORY): `/tmp` tmpfs is ~94% full; a bench/doctest link step may fail with `LLVM ERROR: IO failure on output stream: No space left on device` or SIGBUS. Classify that as an INFRASTRUCTURE failure per the triage rule (a signal/link failure with NO named `FAILED test_name` = infra, does NOT count as a fdars defect). Mitigate by `mkdir -p /home/simonm/.cache/fdars-bench-tmp && export TMPDIR=/home/simonm/.cache/fdars-bench-tmp` before re-running; do not record it as a benchmark result.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && COUNT=$(ls .planning/research/bench/p1_{fpca,elastic,depth,cv,streaming,smooth}_linalg,parallel_run{1,2}.txt 2>/dev/null | wc -l) && echo "artifacts: $COUNT" && test "$COUNT" -ge 12 && grep -l "release/deps/audit_hotpaths" .planning/research/bench/p1_*_linalg,parallel_run1.txt | head</automated>
  </verify>
  <acceptance_criteria>
    - At least 12 baseline artifacts exist: `.planning/research/bench/p1_<module>_linalg,parallel_run{1,2}.txt` for all 6 modules ∈ {fpca, elastic, depth, cv, streaming, smooth}, all non-empty.
    - Each `_run1.txt` contains a `Running target/release/deps/audit_hotpaths-<hash>` line (SC3 `/release/` confirmed) and a `rustc --version` line (SC2 toolchain capture).
    - Each artifact contains at least one criterion `time:` measurement line.
    - Any module whose two runs differ by >10% is tagged LOW CONFIDENCE in its artifact (Pitfall 7).
    - Any link failure ("No space left on device" / SIGBUS with no named `FAILED test_name`) is classified INFRA and re-run after `TMPDIR` change — NOT recorded as a benchmark result or a task failure.
  </acceptance_criteria>
  <done>All 6 modules have two release + linalg,parallel baseline artifacts under bench/ with /release/ and rustc version confirmed as the first recorded facts; variance tagging applied; SC3 satisfied (≥1 target per module recorded).</done>
</task>

<task type="auto">
  <name>Task 3: Write §Methodology + §Workload Matrix into AUDIT-REPORT.md</name>
  <files>.planning/research/AUDIT-REPORT.md</files>
  <read_first>
    - .planning/research/AUDIT-REPORT.md (the file seeded in Plan 01 — the file being modified)
    - .planning/phases/01-measurement-discipline-baselines/01-RESEARCH.md (§Section 2 feature-flag table; §Section 3 release check; §Section 5 infra triage text — copy the methodology paragraph verbatim; §Section 8 workload-matrix cell table + Phase 1 baseline cells)
    - .planning/research/PITFALLS.md (Pitfall 1,3,7,8,17,18 — the discipline the methodology codifies)
    - .planning/codebase/CONCERNS.md (§Performance Bottlenecks, §Scaling Limits — the elastic O(n²·m²)≈60s and SVD O(m³) cost notes that justify the per-module caps)
    - .planning/phases/01-measurement-discipline-baselines/01-CONTEXT.md (D-05 single-report, D-06 naming, D-07 sizing/caps)
  </read_first>
  <action>
    Append two sections to `.planning/research/AUDIT-REPORT.md` (the single growing report, D-05 — do NOT create per-phase fragment files):

    §Methodology — document ALL of (SC2 + SC4): (1) the mandatory `--release` build check and how to confirm it (the `target/release/deps/` binary-path line; use `cargo bench`, never `cargo test --bench` — Pitfall 1); (2) the feature-flag matrix table verbatim from RESEARCH §Section 2 (the 4 combos, which need `--no-default-features`, what each tests — Pitfall 18); (3) the `black_box`-on-inputs-and-outputs requirement (Pitfall 3); (4) rustc/toolchain version capture (`rustc --version` recorded as the first fact in every artifact; the linalg 1.84 floor; current 1.97.0); (5) the ±5% two-run variance threshold with >10% = LOW CONFIDENCE (Pitfall 7); (6) the criterion sample_size/measurement_time tuning approach for large cells (per-group, not global); (7) the artifact naming convention `p1_<target>_<features>_run<N>.txt` under `.planning/research/bench/` with every finding linking to its artifact (D-06, Pitfall 17); (8) the INFRA-VS-CODE TRIAGE RULE — copy the methodology paragraph from RESEARCH §Section 5 verbatim: a `cargo bench`/`cargo test --doc` invocation exiting via signal (SIGBUS) or link failure ("No space left on device") WITHOUT a named `FAILED test_name` line is an INFRASTRUCTURE failure and does NOT count as a fdars defect; `FAILED test_name` lines are code failures and count (Pitfall 8). Note the observed env cause: `/tmp` tmpfs ~94% full → set `TMPDIR` on `/home`.

    §Workload Matrix — write the per-module N×M cell table from RESEARCH §Section 8 (SC1 + PERF-02 + D-07): all 6 module rows (Elastic, FPCA/SVD, Depth, CV, Streaming, Smoothing), each with its N cells, M cells, cap/rationale, and full-grid Yes/No. The cap justifications MUST cite the complexity source: elastic capped N≤500,M≤200 because O(n²·m²), n=1000×m=500≈60s per CONCERNS.md; CV capped N≤500,M≤200 because each fold runs FPCA O(m³)+fit+predict × K=5. Include the "Phase 1 baseline cell per module" table (the one cell actually benchmarked here per D-03) and link each module's baseline row to its `bench/` artifact (D-06). State explicitly that candidate sizes are N∈{100,500,1000}×M∈{50,200,500} and that this matrix is the shared size contract Phases 3-6 benchmark against.

    Do NOT re-derive or move the "4-combo sentinel selection" note Plan 01 already wrote; leave it. Do NOT write any other phase's section (Phases 2-9 append their own later).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && grep -qi "infrastructure failure" .planning/research/AUDIT-REPORT.md && grep -qi "release" .planning/research/AUDIT-REPORT.md && grep -qi "black_box" .planning/research/AUDIT-REPORT.md && grep -qi "workload matrix" .planning/research/AUDIT-REPORT.md && for M in Elastic FPCA Depth CV Streaming Smoothing; do grep -qi "$M" .planning/research/AUDIT-REPORT.md || { echo "MISSING module row: $M"; exit 1; }; done && echo "methodology+matrix OK"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/research/AUDIT-REPORT.md` has a §Methodology section containing: `--release` discipline, the 4-combo feature-flag matrix table, `black_box` requirement, `rustc --version` capture + 1.84 linalg floor, ±5% / >10%-LOW-CONFIDENCE variance rule, the `p1_<target>_<features>_run<N>.txt` naming convention, and the infra-vs-code triage paragraph containing the literal phrase "infrastructure failure".
    - The §Workload Matrix section has all 6 module rows (Elastic, FPCA, Depth, CV, Streaming, Smoothing), each with N cells, M cells, and cap/rationale; elastic and CV caps cite their O() complexity source from CONCERNS.md.
    - A "Phase 1 baseline cell" listing states the one cell benchmarked per module (D-03) and links each to its `bench/` artifact (D-06).
    - The pre-existing "4-combo sentinel selection" note from Plan 01 is untouched; no other phase's section was added.
  </acceptance_criteria>
  <reversibility rating="costly">D-05 (single growing report) and D-07 (workload-matrix sizing) are the shared contracts Phases 3-9 append to and benchmark against; changing the report structure or cell sizes later re-threads every downstream phase. Surfaced, not a strict one-way door.</reversibility>
  <done>AUDIT-REPORT.md contains complete §Methodology (all 6 discipline items + infra-vs-code triage rule) and §Workload Matrix (6 module rows with justified caps + Phase 1 baseline cells), satisfying SC1, SC2, and SC4.</done>
</task>

</tasks>

<verification>
- Bench compiles all 6 sentinels under `""` and `linalg,parallel` combos (Task 1 automated).
- ≥12 baseline artifacts with `/release/` + rustc confirmed (Task 2 automated).
- Report has methodology (release, feature matrix, black_box, variance, triage) + workload matrix (6 modules) (Task 3 automated).
- Decision coverage: D-01 (Task 1 file), D-02 (black_box in Task 1), D-03 (Task 2 baselines + Task 3 baseline cells), D-04 (Plan 01 karcher sentinel referenced in report), D-05 (single AUDIT-REPORT.md, Task 3), D-06 (naming convention, Task 2/3), D-07 (workload-matrix caps, Task 3).
</verification>

<success_criteria>
All 4 ROADMAP Success Criteria met: (SC1) justified workload matrix in the report; (SC2) methodology documents --release + feature matrix + black_box + rustc capture + ±5% variance; (SC3) ≥1 baseline target per module recorded (2 runs, /release/ confirmed) under bench/; (SC4) methodology documents the infra-vs-code triage rule. PERF-02 satisfied.
</success_criteria>

<output>
Create `.planning/phases/01-measurement-discipline-baselines/01-02-SUMMARY.md` when done.
</output>