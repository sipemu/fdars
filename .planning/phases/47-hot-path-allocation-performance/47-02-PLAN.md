---
phase: 47-hot-path-allocation-performance
plan: 02
type: execute
wave: 2
depends_on: [47-01]
files_modified:
  - fdars-core/src/fpca_variants.rs
  - fdars-core/src/fts/acf.rs
  - fdars-core/tests/equivalence_phase47.rs
  - fdars-core/tests/alloc_audit_dpca.rs
  - .planning/phases/47-hot-path-allocation-performance/PERF-RESULTS.md
autonomous: true
requirements: [PERF-02]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "OPT-C: ssvd builds its scaled covariance via DMatrix::from_fn (no c_scaled staging Vec); output provably-equivalent (golden, rel <=1e-10) (PERF-02)"
    - "OPT-B: fsvd builds its gram matrix via DMatrix::from_fn (no gram staging Vec); output provably-equivalent (golden, rel <=1e-10) (PERF-02)"
    - "OPT-D: functional_acf builds c0_scaled via DMatrix::from_fn with a precomputed sqrt_w (drops m^2 redundant sqrt calls + the staging Vec); output provably-equivalent (golden, rel <=1e-10) (PERF-02)"
    - "The existing full suite stays green and clippy --all-targets is clean"
    - "No public signature changes on fsvd, ssvd, or functional_acf"
  artifacts:
    - "fdars-core/src/fpca_variants.rs (fsvd + ssvd edits)"
    - "fdars-core/src/fts/acf.rs (functional_acf edit)"
    - "fdars-core/tests/equivalence_phase47.rs (golden_fsvd, golden_ssvd, golden_functional_acf)"
  key_links:
    - "DMatrix::from_fn column-major closure convention -> matrix identical to from_column_slice(&buf) -> golden passes"
    - "sqrt_w precompute in functional_acf -> same c0_scaled values (sqrt(w) commutes) -> golden passes"
---

<objective>
Wave 2 expansion from the OPT-A tracer: land the three mechanical, LOW-risk FdMatrix<->DMatrix copy removals surfaced by PROF-01 — OPT-C (`ssvd`), OPT-B (`fsvd`), OPT-D (`functional_acf`). Each replaces a `Vec` staging buffer + `DMatrix::from_column_slice(...,&buf)` with a direct `DMatrix::from_fn(...)`, eliminating one intermediate allocation per call; OPT-D also precomputes `sqrt_w` to drop m^2 redundant `sqrt()` calls. Each is guarded by a permanent golden-equivalence test authored before (or with) the change.

Purpose: allocation-first, low-risk-first per CONTEXT.md — these three are the smaller allocation wins (allocation ranks #2/#3/#4) but are pure copy-elimination with no algorithmic change.
Output: edits in `fpca_variants.rs` (fsvd + ssvd) and `fts/acf.rs` (functional_acf); three golden tests in `equivalence_phase47.rs`; PERF-RESULTS.md rows.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/47-hot-path-allocation-performance/47-CONTEXT.md
@.planning/phases/47-hot-path-allocation-performance/47-RESEARCH.md
@.planning/phases/47-hot-path-allocation-performance/47-VALIDATION.md
@.planning/phases/46-whole-crate-profiling-measurement/PROF-01-hotpath-targets.md
</context>

<operational_notes>
Same as Plan 01 (bake into every command): `TMPDIR=/home/simonm/.cache/fdars-bench-tmp`; full lint gate `cargo clippy --all-targets --features linalg,parallel -- -D warnings`; `cargo fmt -p fdars-core` then `git commit --no-verify` per commit; dhat tests single-threaded in own binary.
Depends on Plan 01: reuse the `assert_rel_close` + `generate_curves` helpers and the golden-reference-capture convention already in `equivalence_phase47.rs`.
</operational_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OPT-C + OPT-B — DMatrix::from_fn copy removals in ssvd and fsvd (both in fpca_variants.rs)</name>
  <files>fdars-core/src/fpca_variants.rs, fdars-core/tests/equivalence_phase47.rs</files>
  <read_first>
    - fdars-core/src/fpca_variants.rs lines 733-740 (ssvd: `c_scaled` Vec fill at :734-738 column-major `c_scaled[row + col*m]`, then `DMatrix::from_column_slice(m, m, &c_scaled).symmetric_eigen()` at :740; smooth_cov is already an FdMatrix indexable as smooth_cov[(row,col)])
    - fdars-core/src/fpca_variants.rs lines 471-488 (fsvd: `gram` Vec fill at :472-487 with two branches gram_on_right true/false, indices `gram[a + b*q]` / `gram[a + b*p]`; then `DMatrix::from_column_slice(g_dim, g_dim, &gram).symmetric_eigen()` at :488)
    - fdars-core/src/fpca_variants.rs lines 405-411 (fsvd signature) and 670-675 (ssvd signature — ssvd returns FpcaResult)
    - fdars-core/src/regression.rs lines 25-38 (FpcaResult fields: singular_values, rotation, scores, mean, centered, weights) and fpca_variants.rs lines 41-53 (FsvdResult fields: singular_values, left_functions, right_functions, left_scores, right_scores)
    - 47-RESEARCH.md OPT-C + OPT-B "Proposed change" and Pitfall 3 (from_fn |row,col| fills row/col order; the closure must use the SAME column-major index arithmetic as the current fill: for c_scaled use `sqrt_w[row]*smooth_cov[(row,col)]*sqrt_w[col]`; for gram use the existing `(0..p).map(|s| cw[(s,a)]*cw[(s,b)]).sum()` etc.)
  </read_first>
  <behavior>
    - golden_ssvd: ssvd on deterministic n=30, m=12, ncomp=3 produces identical singular_values, rotation, scores after OPT-C.
    - golden_fsvd: fsvd on deterministic (n=20, p=15, q=10, ncomp=3) produces identical singular_values, left_functions, right_functions after OPT-B.
  </behavior>
  <action>
    First author the golden tests (capture reference constants from the CURRENT code, same convention as golden_dpca in Plan 01):
    - `golden_ssvd_n30_m12`: build data via `generate_curves(30, 12)`, argvals uniform on [0,1], call `fdars_core::fpca_variants::ssvd(&data, 3, &argvals, bandwidth)` (pick a fixed finite bandwidth, e.g. 0.1). Assert representative checkpoints of `singular_values[0..3]`, `rotation.as_slice()[0]`, `scores.as_slice()[0]` via `assert_rel_close(_, EXPECTED, 1e-12)` (fall back to 1e-10 only if needed; document).
    - `golden_fsvd_n20_p15_q10`: build two deterministic FdMatrices (reuse generate_curves with different m for x and y), call `fdars_core::fpca_variants::fsvd(&x, &argvals_x, &y, &argvals_y, 3)`. Assert checkpoints of `singular_values`, `left_functions.as_slice()[0]`, `right_functions.as_slice()[0]`.

    Then apply the edits:
    - OPT-C (ssvd, ~line 733-740): replace the `let mut c_scaled = vec![0.0; m*m]; for col {for row {c_scaled[row+col*m] = sqrt_w[row]*smooth_cov[(row,col)]*sqrt_w[col];}} DMatrix::from_column_slice(m,m,&c_scaled).symmetric_eigen()` block with `DMatrix::from_fn(m, m, |row, col| sqrt_w[row] * smooth_cov[(row,col)] * sqrt_w[col]).symmetric_eigen()`. Verify the index convention matches (Pitfall 3).
    - OPT-B (fsvd, ~line 471-488): replace the `let mut gram = vec![0.0; g_dim*g_dim];` + the two-branch fill + `DMatrix::from_column_slice(g_dim, g_dim, &gram).symmetric_eigen()` with a branch on `gram_on_right` producing `DMatrix::from_fn(q, q, |a, b| (0..p).map(|s| cw[(s,a)]*cw[(s,b)]).sum())` (right branch) or `DMatrix::from_fn(p, p, |a, b| (0..q).map(|t| cw[(a,t)]*cw[(b,t)]).sum())` (left branch), then `.symmetric_eigen()` on the result. Preserve exactly the current arithmetic.

    Both edits are algebraically identical — no numeric change. Keep `#[cfg(feature="linalg")]` gating exactly as-is if present around these paths.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel -- golden_ssvd_n30_m12 golden_fsvd_n20_p15_q10 --nocapture 2>&1 | tail -8</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel 2>&1 | tail -5</automated>
    <acceptance_criteria>
      - golden_ssvd_n30_m12 and golden_fsvd_n20_p15_q10 pass (output equivalent within documented tolerance)
      - full existing suite green
      - fsvd + ssvd public signatures unchanged (grep the two fn signatures — byte-identical)
      - no `from_column_slice(..., &c_scaled)` / `&gram` remains in fpca_variants.rs (grep -v '^\s*//' | grep -c returns 0 for those two staging buffers)
    </acceptance_criteria>
  </verify>
  <done>ssvd and fsvd construct their eigen matrices via from_fn with no staging Vec; both golden tests green; suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OPT-D — functional_acf c0_scaled from_fn + sqrt_w precompute (fts/acf.rs)</name>
  <files>fdars-core/src/fts/acf.rs, fdars-core/tests/equivalence_phase47.rs</files>
  <read_first>
    - fdars-core/src/fts/acf.rs lines 254-262 (functional_acf signature: data, argvals, max_lag: Option<usize>, n_sim, ci, seed -> FacfResult) and lines 329-337 (c0_scaled Vec fill with `weights[j2].sqrt()` recomputed per (j1,j2) pair, then `DMatrix::from_column_slice(m, m, &c0_scaled)` at :337; the defensive symmetrize follows)
    - fdars-core/src/fts/mod.rs lines 125-134 (FacfResult fields: lags: Vec<u32>, acf: Vec<f64>, pacf: Vec<f64>, upper_band: Vec<f64>)
    - 47-RESEARCH.md OPT-D "Proposed change" (precompute `sqrt_w: Vec<f64> = weights.iter().map(|w| w.sqrt()).collect()`; `DMatrix::from_fn(m, m, |j1, j2| c0[j1 + j2*m] * sqrt_w[j1] * sqrt_w[j2])`; keep the defensive symmetrize loop; NOTE: long_run_covariance itself is NOT changed — its 6 blocks are load-bearing per-lag Vecs)
    - 47-CONTEXT.md line 40 (tolerance)
  </read_first>
  <behavior>
    - golden_functional_acf: functional_acf on deterministic n=40, m=12 with a fixed seed produces identical acf, pacf, upper_band after OPT-D.
  </behavior>
  <action>
    Author `golden_functional_acf_n40_m12` first (capture reference constants from current code): build `generate_curves(40, 12)`, argvals uniform, call `fdars_core::fts::functional_acf(&data, &argvals, Some(5), <n_sim>, 0.95, 7)` with a small deterministic `n_sim` (e.g. 50 — the seed makes it deterministic; the Monte-Carlo `upper_band` depends on seed+n_sim so hold both fixed). Assert `acf` and `pacf` element-wise via `assert_rel_close(_, EXPECTED, 1e-12)` (these come from the deterministic covariance path and must be bitwise-stable); assert `upper_band[0]` at rel 1e-10 (Monte-Carlo path, but seed-fixed so should also be exact — use the tighter bound and relax only if reordering forces it; document).

    Apply OPT-D at acf.rs ~line 329-337: replace the `let mut c0_scaled = vec![0.0; m*m]; for j1 { let sw1 = weights[j1].sqrt(); for j2 { c0_scaled[j1+j2*m] = c0[j1+j2*m]*sw1*weights[j2].sqrt(); }}` + `DMatrix::from_column_slice(m,m,&c0_scaled)` with: `let sqrt_w: Vec<f64> = weights.iter().map(|w| w.sqrt()).collect();` then `let mut c0_mat = DMatrix::from_fn(m, m, |j1, j2| c0[j1 + j2*m] * sqrt_w[j1] * sqrt_w[j2]);`. Keep the existing defensive symmetrization loop that follows unchanged. This drops the c0_scaled staging Vec AND the m^2 redundant `weights[j2].sqrt()` recomputations (now m sqrt calls instead of ~m^2). Do NOT touch `long_run_covariance` (:676-730) — its allocations are load-bearing.

    Record OPT-D before/after in PERF-RESULTS.md (allocation rank #4: 6 blocks / 100,400 B before; note the staging Vec + m^2-m sqrt calls eliminated).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel -- golden_functional_acf_n40_m12 --nocapture 2>&1 | tail -8</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel 2>&1 | tail -5</automated>
    <acceptance_criteria>
      - golden_functional_acf_n40_m12 passes (acf/pacf/upper_band equivalent within documented tolerance)
      - full existing suite green
      - functional_acf public signature unchanged
      - no `from_column_slice(m, m, &c0_scaled)` remains in acf.rs (staging Vec eliminated)
    </acceptance_criteria>
  </verify>
  <done>functional_acf uses from_fn with precomputed sqrt_w; golden test green; suite green; PERF-RESULTS.md OPT-D row filled.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-2 merge gate — dhat re-measure of ssvd/fsvd/long_run_cov + clippy --all-targets + full suite</name>
  <files>fdars-core/tests/alloc_audit_dpca.rs, .planning/phases/47-hot-path-allocation-performance/PERF-RESULTS.md</files>
  <read_first>
    - fdars-core/tests/alloc_audit_dpca.rs (Plan 01's dhat file — extend it with additional cells here)
    - 47-VALIDATION.md "Per-Task Verification Map" OPT-B/C/D row (`count_fsvd_allocations` etc.) and "Sampling Rate"
    - 47-RESEARCH.md OPT-B/C/D before-numbers (fsvd 275 blocks/600KB; ssvd 22 blocks/314KB; long_run_covariance 6 blocks/100KB @ n200_m50)
  </read_first>
  <action>
    Add feature-gated dhat cells to `tests/alloc_audit_dpca.rs` (it is the phase's alloc-audit file; keep all Phase-47 alloc cells together): `count_fsvd_allocations_n200_m50` and `count_ssvd_allocations_n200_m50`, each mirroring the existing pattern (build data outside profiler, `Profiler::builder().testing().build()`, call the target, print total_blocks/total_bytes/max_bytes). These are BASELINE/informational prints (the wins are small — 1 Vec each — so a hard `< N` gate is brittle); record the measured before/after in PERF-RESULTS.md rather than hard-asserting. Add a `// baseline print, not a regression gate` comment (matching alloc_audit_fpca.rs convention).

    Fill PERF-RESULTS.md OPT-B and OPT-C rows with before (fsvd 275 blocks / ssvd 22 blocks) and after (measured) block counts.

    Run the Wave-2 merge gate: full suite + `cargo clippy --all-targets --features linalg,parallel -- -D warnings`.

    Commit sequence (each: `cargo fmt -p fdars-core` then `git commit --no-verify`): OPT-C+OPT-B edit + golden tests (Task 1), OPT-D edit + golden test (Task 2), dhat cells + ledger (Task 3).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features dhat-heap,linalg -- count_fsvd_allocations_n200_m50 count_ssvd_allocations_n200_m50 --nocapture --test-threads=1 2>&1 | tail -12</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy -p fdars-core --all-targets --features linalg,parallel -- -D warnings 2>&1 | tail -5</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel 2>&1 | tail -5</automated>
    <acceptance_criteria>
      - fsvd/ssvd dhat cells run and print block counts; PERF-RESULTS.md OPT-B/C/D rows filled with before/after
      - clippy --all-targets clean
      - full existing suite green
    </acceptance_criteria>
  </verify>
  <done>OPT-B/C/D landed behavior-preservingly; alloc before/after recorded; clippy + suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure in-process numerical optimization — no external input, I/O, auth, or network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-03 | Tampering | numeric output of fsvd/ssvd/functional_acf after from_fn refactor | high | mitigate | per-target golden equivalence test (rel <=1e-10) captured pre-change; full suite green at every commit |
| T-47-04 | Tampering | from_fn column-major index convention transposes the matrix (Pitfall 3) | high | mitigate | golden tests catch transposition (rel error ~1.0); index convention verified against current fill loop before switching |

No package installs. No T-47-SC.
</threat_model>

<verification>
- Three golden equivalence tests (ssvd, fsvd, functional_acf) pass within documented tolerance.
- Full existing suite green + clippy --all-targets clean.
- fsvd/ssvd/functional_acf public signatures unchanged.
- Staging Vecs (c_scaled, gram, c0_scaled) eliminated; from_fn used instead.
</verification>

<success_criteria>
- OPT-B, OPT-C, OPT-D landed as behavior-preserving copy-eliminations, each proven by a permanent golden test.
- Allocation before/after recorded in PERF-RESULTS.md.
- No public signature changed; suite + clippy green.
</success_criteria>

<output>
Create `.planning/phases/47-hot-path-allocation-performance/47-02-SUMMARY.md` when done. Record the golden tolerances used and the fsvd/ssvd alloc before/after numbers.
</output>
