---
phase: 91-suite-wide-robustness-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/91-suite-wide-robustness-sweep/91-AUDIT.md
autonomous: true
requirements: [ROBUST-01, ROBUST-02]
estimate:
  tokens: 55000
  raw_tokens: 33000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A committed 91-AUDIT.md inventories every fragility class (backend-dependent goldens; RNG/thread-order; env/BLAS/disk) with a per-item disposition (covered-by-guard / deterministic-safe-to-leave / fixed) and rationale + reproduction command (ROBUST-01, ROBUST-02)"
    - "Full suite is green (0 FAILED) under BOTH CI configs: --features linalg,parallel,serde AND --no-default-features --features linalg (ROBUST-02)"
    - "A repeated-run and thread-count sweep (RAYON_NUM_THREADS varied) confirms determinism — bit-identical passed counts, 0 FAILED (ROBUST-02)"
    - "Every fragile test is either fixed (guard/tolerance) or justified safe-to-leave in 91-AUDIT.md; no over-guarding of sound tests (ROBUST-02)"
    - "No new crate dependency added (Cargo.toml [dependencies]/[dev-dependencies] unchanged)"
  artifacts:
    - ".planning/phases/91-suite-wide-robustness-sweep/91-AUDIT.md"
  key_links:
    - "91-AUDIT.md dispositions trace to reproduction commands that re-run green under both CI configs"
    - "The no-linalg full run (0 failed) is the fast leak-detector for any unguarded backend-dependent test"
---

<objective>
Audit the entire fdars-core test suite (integration tests + lib unit tests + doctests) for fragility analogous to the Phase 90 golden flake, across all three fragility classes, and either fix or justify-as-safe every additional fragile test. Produce a committed audit artifact `91-AUDIT.md`.

Purpose: Satisfy ROBUST-01 (audit → findings list) and ROBUST-02 (fix-or-justify each), reusing the Phase 90 diagnosis technique. The orchestrator has already gathered decisive evidence (see 91-evidence.md) showing the suite is green under both CI configs and thread-order-independent, with ZERO additional fragile tests requiring fixes. This plan re-checks that evidence reproducibly and records it as a durable, per-item audit.

Output: `.planning/phases/91-suite-wide-robustness-sweep/91-AUDIT.md` (the ROBUST-01+02 deliverable). Expected: zero code changes. The fix task is CONDITIONAL — it fires only if the sweep surfaces a NEW unguarded fragile test (none expected).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-suite-wide-robustness-sweep/91-CONTEXT.md
@.planning/phases/90-golden-flake-root-cause-deterministic-fix/90-DIAGNOSIS.md

# Build hazards (MEMORY.md): Bash default timeout is 2min — long full-suite gates need explicit 600s timeout and per-gate batching. clippy is `--all-targets --features linalg,parallel -- -D warnings`. Run `cargo fmt` per commit. Pre-commit hook times out → commit `--no-verify` after out-of-band gates. Free `target/debug/{incremental,examples}` before long runs (target/ fills /home; doctests link in a small /tmp tmpfs). Execute inline — fdars executor subagents stall on long cargo.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end config-matrix reproduction — prove both CI configs green under one runnable path</name>
  <files>(no source edits — reproduction + captured evidence only)</files>
  <read_first>
    - .planning/phases/91-suite-wide-robustness-sweep/91-CONTEXT.md (acceptance bar: both CI configs green + repeated/thread-sweep check)
    - .planning/phases/90-golden-flake-root-cause-deterministic-fix/90-DIAGNOSIS.md (the reproduction technique: run under a config that omits linalg → nalgebra path diverges)
    - .github/workflows/rust-ci.yml (the two CI configs of record)
  </read_first>
  <action>
    Free disk first: `rm -rf target/debug/incremental target/debug/examples` (target/ fills /home; doctests link in a small /tmp tmpfs).

    Run the two CI-supported full-suite configs FOREGROUND, each with an explicit 600s tool timeout (do NOT run the combined gate as one background job — it gets killed mid-run):
    1. CI config 1: `cargo test -p fdars-core --features linalg,parallel,serde 2>&1 | tail -8`
    2. CI config 2: `cargo test -p fdars-core --no-default-features --features linalg 2>&1 | tail -8`
    3. Leak-detector (fast surfacing of any OTHER unguarded backend-dependent test): `cargo test -p fdars-core --features parallel 2>&1 | tail -8` (default features, NO linalg — the 3 Phase-90 goldens + unrelated doctests should be `ignored`, NOT `failed`).

    Capture the `test result:` summary line(s) (passed / failed / ignored counts) from each run — these become the evidence rows in Task 2's 91-AUDIT.md. This is the single end-to-end path through every fragility surface: config 1 exercises the linalg/faer backend + serde + parallel; config 2 exercises the no-default no-linalg-with-linalg path; the leak-detector exercises the nalgebra backend to catch unguarded backend-dependent asserts. Do NOT edit any source in this task.
  </action>
  <verify>
    <automated>cargo test -p fdars-core --features linalg,parallel,serde 2>&1 | tail -3</automated>
    <fails_when>non-zero exit, or "test result: FAILED" / "error[" in output, or any nonzero "failed" count in a "test result:" line</fails_when>
    <automated>cargo test -p fdars-core --no-default-features --features linalg 2>&1 | tail -3</automated>
    <fails_when>non-zero exit, or "test result: FAILED" in output, or any nonzero "failed" count in a "test result:" line</fails_when>
    <automated>cargo test -p fdars-core --features parallel 2>&1 | grep -E "test result:" | grep -v "0 failed" | grep -c failed || true</automated>
    <fails_when>output prints a nonzero number (a "test result:" line with a nonzero failed count leaked under the no-linalg config = an unguarded backend-dependent test)</fails_when>
  </verify>
  <acceptance_criteria>
    - `cargo test -p fdars-core --features linalg,parallel,serde` exits 0 with every "test result:" line showing "0 failed".
    - `cargo test -p fdars-core --no-default-features --features linalg` exits 0 with every "test result:" line showing "0 failed".
    - `cargo test -p fdars-core --features parallel` (no linalg) produces zero "test result:" lines with a nonzero failed count (the 3 Phase-90 goldens appear as ignored, not failed).
    - The passed/failed/ignored counts from all three runs are captured verbatim for use in Task 2.
  </acceptance_criteria>
  <done>Both CI configs run green (0 failed) end-to-end, the no-linalg leak-detector surfaces no unguarded backend-dependent failure, and the three summary lines are captured.</done>
</task>

<task type="auto">
  <name>Task 2: Structural enumeration + thread/repeated-run determinism sweep, then write committed 91-AUDIT.md</name>
  <files>.planning/phases/91-suite-wide-robustness-sweep/91-AUDIT.md</files>
  <read_first>
    - fdars-core/tests/equivalence_phase48.rs (Phase-90 guards at lines ~56, ~72)
    - fdars-core/tests/equivalence_phase49.rs (Phase-90 guard at line ~384; PACE sibling stays unguarded)
    - fdars-core/tests/svd_equivalence.rs (existing cfg(feature="linalg") guards)
    - fdars-core/tests/validate_against_r.rs (existing cfg(feature="linalg") guard; ~95 backend-independent assert_eq)
    - .planning/phases/90-golden-flake-root-cause-deterministic-fix/90-DIAGNOSIS.md (class definitions + PACE-sibling-must-stay-unguarded note)
  </read_first>
  <action>
    Perform the structural enumeration across the three fragility classes, then run the determinism sweep, then write `91-AUDIT.md`.

    STRUCTURAL GREP (backend class — enumerate candidates): grep the test tree for backend-dependent call sites and existing guards:
    - `grep -rn "fdata_to_pc\|co_cluster\|pace_fpca\|fpca" fdars-core/tests/` — candidate backend-routed asserts.
    - `grep -rn 'cfg_attr(not(feature = "linalg"), ignore)' fdars-core/tests/` — the Phase-90 ignore-guard form.
    - `grep -rln 'cfg(feature = "linalg")' fdars-core/tests/` — the compile-out guard form.
    - For each candidate file, confirm its disposition: guarded (compiled-out or ignore-guarded) OR backend-independent (pure arithmetic χ²/gamma / R-reference goldens that pass off-linalg per Task 1's leak-detector) OR allocation-only (no value asserts).

    THREAD/REPEATED-RUN SWEEP (RNG/thread-order + env class — disprove nondeterminism): on the serde CI config, run the full suite under varied thread counts and repeated runs, each FOREGROUND with 600s timeout:
    - `RAYON_NUM_THREADS=1 cargo test -p fdars-core --features linalg,parallel,serde 2>&1 | grep "test result:" | tail -3`
    - `RAYON_NUM_THREADS=2 cargo test -p fdars-core --features linalg,parallel,serde 2>&1 | grep "test result:" | tail -3`
    - default thread count (nproc), 3 consecutive runs: `for i in 1 2 3; do cargo test -p fdars-core --features linalg,parallel,serde 2>&1 | grep -q "test result: FAILED" && { echo "RUN $i FAILED"; exit 1; }; done; echo ALL_3_GREEN`
    - Confirm passed counts are bit-identical across thread counts (thread-count independence of the `seed_for_thread(seed,k)` reseed contract) and 0 failed across repeated runs.

    WRITE 91-AUDIT.md with these sections (use the captured evidence from Task 1 + this task; embed each reproduction command verbatim):
    1. `# Phase 91 — Suite-Wide Robustness Audit (ROBUST-01, ROBUST-02)` header + method note (reuses Phase 90 technique).
    2. `## Config matrix (ROBUST-01 evidence)` — a table of the three Task-1 runs with exact passed/failed/ignored counts + the command per row.
    3. `## Thread-count & repeated-run sweep (determinism, ROBUST-02)` — the thread-sweep + 3-consecutive-run results with commands; state the bit-identical passed-count observation.
    4. `## Fragility-class inventory` — one subsection per class (Feature-backend-dependent goldens; RNG/thread-order nondeterminism; Environment/BLAS/disk dependence). For each: enumerate the candidate tests/files found by grep, and for EACH give a disposition (covered-by-guard / deterministic-safe-to-leave / fixed) + a one-line rationale + the reproduction command. Explicitly note: the 3 Phase-90 goldens (covered-by-ignore-guard); svd_equivalence.rs + validate_against_r.rs (covered-by-cfg-guard); phase49 χ²/gamma + validate_against_r R-reference asserts (backend-independent, pass off-linalg → safe-to-leave); PACE sibling `svd_sign_pace_eigenfunctions_single_matrix_bit_identical` (unguarded ON PURPOSE — passes off-linalg, must NOT be guarded per 90-DIAGNOSIS §7); alloc_audit_* (allocation audits, no value asserts).
    5. `## Environment/disk hypothesis` — record that the Phase-90 disk-pressure hypothesis is overturned; deterministic across every run above; no env-pinning/serialization warranted (no proven race).
    6. `## Findings` — the ROBUST-01 findings list: state that ZERO additional fragile tests require fixing, with the reason each class is already covered.
    7. `## Disposition summary` — a one-line-per-class table: class → # candidates → # covered-by-guard → # safe-to-leave → # fixed.

    Do NOT invent fixes for tests that are already robust. Do NOT edit any test source in this task.
  </action>
  <verify>
    <automated>test -s .planning/phases/91-suite-wide-robustness-sweep/91-AUDIT.md && grep -qi "ROBUST-01" .planning/phases/91-suite-wide-robustness-sweep/91-AUDIT.md && grep -qi "ROBUST-02" .planning/phases/91-suite-wide-robustness-sweep/91-AUDIT.md && echo AUDIT_OK</automated>
    <fails_when>91-AUDIT.md is empty/absent, or missing a ROBUST-01 or ROBUST-02 citation (prints nothing instead of AUDIT_OK)</fails_when>
    <automated>grep -ci "disposition\|safe-to-leave\|covered-by-guard\|fixed" .planning/phases/91-suite-wide-robustness-sweep/91-AUDIT.md</automated>
    <fails_when>output is 0 (no per-item disposition language present in the inventory)</fails_when>
    <automated>grep -qi "pace" .planning/phases/91-suite-wide-robustness-sweep/91-AUDIT.md && grep -qi "linalg,parallel,serde" .planning/phases/91-suite-wide-robustness-sweep/91-AUDIT.md && grep -qi "no-default-features" .planning/phases/91-suite-wide-robustness-sweep/91-AUDIT.md && echo COVERAGE_OK</automated>
    <fails_when>the audit omits the PACE-sibling note, or omits either CI-config reproduction command (prints nothing instead of COVERAGE_OK)</fails_when>
    <automated>for i in 1 2 3; do cargo test -p fdars-core --features linalg,parallel,serde 2>&1 | grep -q "test result: FAILED" && { echo "RUN $i FAILED"; exit 1; }; done; echo ALL_3_GREEN</automated>
    <fails_when>any of the 3 consecutive serde-config runs prints "RUN N FAILED" / "test result: FAILED" instead of ALL_3_GREEN (residual intermittency)</fails_when>
  </verify>
  <acceptance_criteria>
    - `91-AUDIT.md` exists, is non-empty, and cites both ROBUST-01 and ROBUST-02.
    - It contains a config-matrix table (3 rows, exact counts + commands), a thread/repeated-run sweep section, a per-class inventory with a disposition + rationale + reproduction command for every candidate, an overturned-disk-hypothesis note, a ROBUST-01 findings list, and a disposition-summary table.
    - It explicitly records the PACE sibling as intentionally-unguarded and both CI-config commands verbatim.
    - 3 consecutive full serde-config runs are green (0 failed) and thread counts 1/2/default show bit-identical passed counts (recorded in the audit).
  </acceptance_criteria>
  <done>91-AUDIT.md is written with the full config matrix, thread/repeated-run sweep, per-class disposition inventory, overturned-disk note, findings list, and disposition-summary table — and the repeated-run determinism check is green.</done>
</task>

<task type="auto">
  <name>Task 3: CONDITIONAL Phase-90-style fix for any NEW unguarded fragile test (no-op if none found)</name>
  <files>fdars-core/tests/*.rs (ONLY if a leak was found; otherwise no edits)</files>
  <read_first>
    - .planning/phases/91-suite-wide-robustness-sweep/91-AUDIT.md (the findings list from Task 2 — the source of truth for whether a fix is needed)
    - fdars-core/tests/equivalence_phase48.rs (the Phase-90 guard idiom to mirror: `#[cfg_attr(not(feature = "linalg"), ignore)]` above `#[test]`, with a doc note)
    - .planning/phases/90-golden-flake-root-cause-deterministic-fix/90-DIAGNOSIS.md §6 (fix-vs-document hierarchy: cfg-guard backend-dependent; tolerance ONLY where FP nondeterminism proven; no new dependency)
  </read_first>
  <action>
    Branch on Task 1's leak-detector + Task 2's findings:

    IF the no-linalg leak-detector (Task 1 command 3) surfaced any "test result:" line with a nonzero failed count that is NOT one of the 3 already-guarded Phase-90 goldens — i.e. a NEW unguarded backend-dependent test — THEN apply the Phase-90 fix idiom to exactly that test: add `#[cfg_attr(not(feature = "linalg"), ignore)]` above its `#[test]` plus a one-line doc note explaining the backend dependence. For a proven FP-nondeterminism assert (only if evidence in the audit proves it — none expected), relax that single `assert_eq!` to a tight relative tolerance and document the evidence. Add NO new crate dependency. After any edit: `cargo fmt`, then re-run both CI configs FOREGROUND (600s) to confirm green, and update 91-AUDIT.md to move that item's disposition to "fixed" with the diff rationale.

    IF NO leak was found (the expected outcome per 91-evidence.md): make NO code change. Record in 91-AUDIT.md's findings section that the conditional fix branch was a no-op (zero code changes), with the reason (all backend-dependent tests already covered by Phase-90 + pre-existing guards; RNG/thread-order robust; env/disk disproven). This is a legitimate completed outcome, not a skipped task.

    Guardrail either way: verify no new dependency was introduced — `git diff -- fdars-core/Cargo.toml Cargo.toml` must show no added `[dependencies]`/`[dev-dependencies]` line.
  </action>
  <verify>
    <automated>git diff --unified=0 -- fdars-core/Cargo.toml Cargo.toml 2>&1 | grep -E '^\+' | grep -vE '^\+\+\+' | grep -Ec '^\+' || true</automated>
    <fails_when>output prints a nonzero number AND those added lines introduce a dependency (any new [dependencies]/[dev-dependencies] entry — no new crate dependency is allowed)</fails_when>
    <automated>cargo test -p fdars-core --features linalg,parallel,serde 2>&1 | tail -3</automated>
    <fails_when>non-zero exit or "test result: FAILED" in output (a conditional fix, if applied, must keep CI config 1 green)</fails_when>
    <automated>cargo test -p fdars-core --no-default-features --features linalg 2>&1 | tail -3</automated>
    <fails_when>non-zero exit or "test result: FAILED" in output (a conditional fix, if applied, must keep CI config 2 green)</fails_when>
    <automated>grep -qi "no-op\|fixed\|conditional" .planning/phases/91-suite-wide-robustness-sweep/91-AUDIT.md && echo BRANCH_RECORDED</automated>
    <fails_when>the audit does not record the conditional-fix branch outcome (no-op or fixed) — prints nothing instead of BRANCH_RECORDED</fails_when>
  </verify>
  <acceptance_criteria>
    - No new crate dependency was added (`git diff` on Cargo.toml files shows no added dependency entry).
    - Both CI configs remain green (0 failed) after this task, whether a fix was applied or not.
    - If a NEW unguarded fragile test was found, it carries the Phase-90 guard idiom (or a proven-FP tolerance) and its 91-AUDIT.md disposition reads "fixed"; if none, 91-AUDIT.md records the branch as a no-op with rationale.
    - Any test file touched is `cargo fmt`-clean.
  </acceptance_criteria>
  <done>The conditional fix branch is resolved: either no leak was found and the audit records a no-op (zero code changes), or the single leaked test is guarded/tolerance-fixed and both CI configs stay green — with no new dependency in either case.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-config → assertion | Feature-flag config (linalg on/off, thread count) crosses into golden `assert_eq!` bit-identity checks; a config-dependent numeric backend can silently flip a golden. |
| RNG seed → parallel reduce | Per-thread `seed_for_thread(seed,k)` reseeding crosses into parallel FP-reduction order; thread-count variation could perturb bit-identity if the contract leaked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-91-01 | Tampering | An unguarded backend-dependent golden `assert_eq!` in fdars-core/tests/ | medium | mitigate | Task 1 no-linalg leak-detector surfaces it; Task 3 applies the Phase-90 cfg-guard. |
| T-91-02 | Denial of Service | Determinism regression under thread-count/repeated runs | low | mitigate | Task 2 thread sweep (RAYON_NUM_THREADS 1/2/default) + 3 consecutive runs confirm bit-identical green. |
| T-91-03 | Tampering | Silent scope creep — a new crate dependency for "serialization" | low | mitigate | Task 3 `git diff` guard on Cargo.toml files asserts no added dependency (CONTEXT.md: no new dependency; Phase 90 found no race). |
| T-91-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase — audit-and-document only; no `cargo add`, no manifest edits expected. |
</threat_model>

<verification>
Phase-level checks (all runnable, batched FOREGROUND with 600s timeout):
1. `cargo test -p fdars-core --features linalg,parallel,serde` → exit 0, all "test result:" lines "0 failed".
2. `cargo test -p fdars-core --no-default-features --features linalg` → exit 0, all "test result:" lines "0 failed".
3. `cargo test -p fdars-core --features parallel` (no linalg) → the 3 Phase-90 goldens ignored, zero unexpected failures.
4. Thread sweep (RAYON_NUM_THREADS 1/2/default on the serde config) → bit-identical passed counts, 0 failed.
5. 3 consecutive serde-config runs → all green (no intermittency).
6. `91-AUDIT.md` exists, cites ROBUST-01/02, has the config matrix + per-class disposition inventory + findings + disposition-summary.
7. `git diff` on Cargo.toml files → no added dependency.
8. `cargo fmt --check` clean if any test file was touched.
</verification>

<success_criteria>
- ROBUST-01: `91-AUDIT.md` enumerates every candidate fragile assertion across all three classes with a disposition (or records that none were found) — committed.
- ROBUST-02: Every fragile test is fixed (guard/tolerance) or justified safe-to-leave in `91-AUDIT.md`; full suite green under BOTH CI configs + thread/repeated-run determinism check green.
- No new crate dependency introduced.
- Expected outcome: zero code changes (conditional fix is a recorded no-op).
</success_criteria>

<artifacts>
## Artifacts this phase produces
- `.planning/phases/91-suite-wide-robustness-sweep/91-AUDIT.md` — the ROBUST-01+02 deliverable: config matrix, thread/repeated-run sweep, per-class disposition inventory, overturned-disk note, findings list, disposition-summary table.
- (CONDITIONAL, only if a leak is found — none expected) a guard/tolerance edit to a single `fdars-core/tests/*.rs` file mirroring the Phase-90 idiom.
</artifacts>

<output>
Create `.planning/phases/91-suite-wide-robustness-sweep/91-01-SUMMARY.md` when done.
</output>
