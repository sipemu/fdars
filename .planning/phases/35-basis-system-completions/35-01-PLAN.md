---
phase: 35-basis-system-completions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/basis/basis_system.rs
  - fdars-core/src/basis/monomial.rs
  - fdars-core/src/basis/mod.rs
  - fdars-core/src/smooth_basis.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [REP-01]
estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "monomial_basis(argvals, nbasis) returns Ok(BasisSystem) whose eval_matrix equals the closed-form powers t^j on hand-computed reference points within tolerance (REP-01 SC1)"
    - "BasisSystem bundles a column-major eval matrix AND a penalty matrix in one Result-returning factory (REP-01 SC1)"
    - "monomial_basis with invalid inputs (n<2 argvals, nbasis<1) returns FdarError rather than panicking (REP-01 SC4)"
    - "New monomial_basis + BasisSystem are crate-root re-exported and callable as fdars_core::monomial_basis / fdars_core::BasisSystem (REP-01 SC1)"
    - "Existing bspline/fourier/constant factories and smooth_basis/pspline penalties keep working unchanged; suite + clippy --all-targets green (REP-01 SC5)"
  artifacts:
    - fdars-core/src/basis/basis_system.rs
    - fdars-core/src/basis/monomial.rs
  key_links:
    - "basis/mod.rs pub use exposes monomial_basis + BasisSystem"
    - "src/lib.rs pub use basis::{...} re-exports monomial_basis + BasisSystem at crate root"
    - "smooth_basis::{differentiate_basis_columns, integrate_symmetric_penalty} promoted to pub(crate) for downstream numeric-Gram reuse"
---

<objective>
Tracer slice for REP-01: prove the additive basis-factory architecture end-to-end with ONE factory. Introduce the `BasisSystem` result struct (col-major eval matrix + penalty matrix + metadata), implement `monomial_basis` with an analytic Gram penalty, and wire both all the way through `basis/mod.rs` and the crate-root `src/lib.rs` re-export. Also promote the two private numeric-Gram helpers in `smooth_basis.rs` to `pub(crate)` so the expansion plans (exponential/polygonal) can reuse them without copy-paste (per D — Penalty Matrices Option B, RESEARCH Open-Q3/A7).

Purpose: The single hardest architectural question this phase must answer is "does a new Result-returning BasisSystem factory register cleanly alongside the untouched bare-Vec factories, and can new basis files reuse the smooth_basis numeric-penalty machinery?" This tracer answers it on the agent's best early-context tokens, before three more factories + two new modules pile on.
Output: BasisSystem, monomial_basis, pub(crate) helper promotion, mod.rs + lib.rs registration, inline tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-basis-system-completions/35-CONTEXT.md
@.planning/phases/35-basis-system-completions/35-RESEARCH.md

# Reuse analogs (verified on disk):
@fdars-core/src/basis/bspline.rs
@fdars-core/src/basis/mod.rs
@fdars-core/src/basis/pspline.rs
@fdars-core/src/smooth_basis.rs
@fdars-core/src/error.rs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: BasisSystem struct + monomial_basis factory (analytic penalty), end-to-end</name>
  <files>fdars-core/src/basis/basis_system.rs, fdars-core/src/basis/monomial.rs</files>
  <read_first>
    - fdars-core/src/basis/bspline.rs — the column-major flat-Vec factory convention: eval index basis[ti + j*n] where n = argvals.len().
    - fdars-core/src/basis/pspline.rs — PsplineFitResult uses derive(Debug, Clone, PartialEq), non_exhaustive, conditional serde derive. Mirror exactly.
    - fdars-core/src/error.rs — FdarError::InvalidDimension { parameter, expected, actual } and FdarError::InvalidParameter { parameter, message } variant shapes.
    - 35-RESEARCH.md "BasisSystem Struct Design" (fields eval_matrix, penalty_matrix, nbasis, n_eval, lfd_order) and "Analytic Gram (Monomial and Power with Integer Exponents)" (falling-factorial Gram entry formula plus the near-zero-power log-case guard and ci/cj near-zero guards).
  </read_first>
  <behavior>
    - monomial_basis(&[0.0, 1.0, 2.0], 3): eval_matrix column-major equals [1,1,1, 0,1,2, 0,1,4] (col0=B0=1, col1=B1=t, col2=B2=t^2); eval_matrix.len() == 3*3.
    - Returned BasisSystem has nbasis==3, n_eval==3, lfd_order==2 (default 2nd-derivative curvature penalty per D — Penalty Matrices).
    - penalty_matrix is nbasis*nbasis, symmetric (P[i+j*k]==P[j+i*k]), PSD diagonal (>= -1e-10). For exponents [0,1,2], lfd_order=2, domain [0,1]: P[0,0]==0, P[1,1]==0, P[2,2]==4.0 (per RESEARCH validation values).
    - Invalid: argvals.len() < 2 -> Err(InvalidDimension); nbasis < 1 -> Err(InvalidParameter). Never panics.
  </behavior>
  <action>
    Create basis_system.rs defining a public BasisSystem struct with public fields eval_matrix: Vec<f64>, penalty_matrix: Vec<f64>, nbasis: usize, n_eval: usize, lfd_order: usize. Derive Debug/Clone/PartialEq, add the non_exhaustive attribute, and the conditional serde derive exactly as PsplineFitResult does. Document the column-major eval layout (element (t_i,j) at index i + j*n_eval) and that penalty_matrix is the Gram of the lfd_order-th derivative.

    Create monomial.rs defining monomial_basis(argvals: &[f64], nbasis: usize) -> Result<BasisSystem, FdarError>. Default exponents are 0..nbasis. Validate n>=2 and nbasis>=1 at entry (FdarError). Build eval_matrix col-major via eval_matrix[ti + j*n] = t.powi(j as i32). Compute the penalty analytically with lfd_order=2 using the falling-factorial Gram entry over domain [argvals[0], argvals[n-1]]: guard ci/cj near-zero -> 0.0, and guard the near-zero-power (integrable log) case per RESEARCH. Keep the Gram-entry helper private to this file. Return BasisSystem.

    Do NOT modify bspline.rs / fourier.rs / constant.rs — they stay bare-Vec and untouched (additive; D — factories LEFT UNTOUCHED).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars &amp;&amp; TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel basis::monomial 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - monomial_basis closed-form eval test passes (eval_matrix == [1,1,1,0,1,2,0,1,4] for t=[0,1,2], nbasis=3).
    - Penalty symmetry test passes and P[2,2]==4.0 (within 1e-9) for exponents [0,1,2], domain [0,1], lfd_order=2.
    - Invalid nbasis=0 returns Err(InvalidParameter); argvals of length 1 returns Err(InvalidDimension); neither panics.
    - BasisSystem derives Debug/Clone/PartialEq and carries the non_exhaustive attribute.
  </acceptance_criteria>
  <done>monomial_basis returns a BasisSystem with correct closed-form eval matrix and analytic penalty; invalid inputs return FdarError; inline tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Promote smooth_basis numeric-Gram helpers to pub(crate)</name>
  <files>fdars-core/src/smooth_basis.rs</files>
  <read_first>
    - fdars-core/src/smooth_basis.rs lines ~550-600 — differentiate_basis_columns(basis, n_quad, nbasis, h, lfd_order) -> Vec<f64> and integrate_symmetric_penalty(deriv_basis, weights, k, n_quad) -> Vec<f64>. These are the numeric derivative-Gram pattern the exponential/polygonal expansion plan (35-02) will reuse.
    - 35-RESEARCH.md "Numeric Gram" section plus Assumption A7 (promote to pub(crate), Option B — no public API change).
  </read_first>
  <action>
    Change the visibility of differentiate_basis_columns and integrate_symmetric_penalty in smooth_basis.rs from private to pub(crate) so 35-02 can import them via crate::smooth_basis (per D — Penalty Matrices Option B; RESEARCH Open-Q3). Change ONLY the leading fn to pub(crate) fn on those two items; do not alter their bodies, signatures, or any other smooth_basis code. This is a crate-internal change with zero public-API impact (existing bspline_penalty_matrix and other smooth_basis public signatures unchanged).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars &amp;&amp; TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --features linalg,parallel 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'pub(crate) fn differentiate_basis_columns' fdars-core/src/smooth_basis.rs returns 1.
    - grep -c 'pub(crate) fn integrate_symmetric_penalty' fdars-core/src/smooth_basis.rs returns 1.
    - cargo build -p fdars-core --features linalg,parallel succeeds (an as-yet-unused pub(crate) helper is acceptable — it is consumed in 35-02).
    - No other line in smooth_basis.rs changed (public signatures untouched).
  </acceptance_criteria>
  <done>Both numeric-Gram helpers are pub(crate); crate builds; no public signature changed.</done>
</task>

<task type="auto">
  <name>Task 3: Register BasisSystem + monomial_basis in basis/mod.rs and crate-root lib.rs</name>
  <files>fdars-core/src/basis/mod.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/basis/mod.rs — the pub mod declarations and the pub use re-export block (each factory listed explicitly; no wildcards).
    - fdars-core/src/lib.rs lines ~483-490 — the pub use basis::{ ... } crate-root re-export block to extend.
  </read_first>
  <action>
    In basis/mod.rs add pub mod basis_system; and pub mod monomial;, then extend the pub use block with pub use basis_system::BasisSystem; and pub use monomial::monomial_basis; consistent with the existing ordering. In lib.rs extend the existing pub use basis::{ ... } block to add monomial_basis and BasisSystem (keep existing entries, maintain the block's ordering convention). Do not remove or reorder existing exports.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars &amp;&amp; TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel basis:: 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - basis/mod.rs contains pub mod basis_system; and pub mod monomial; plus their pub use lines.
    - lib.rs pub use basis::{...} block includes monomial_basis and BasisSystem.
    - cargo test basis:: green; a downstream user could call fdars_core::monomial_basis and fdars_core::BasisSystem.
  </acceptance_criteria>
  <done>monomial_basis + BasisSystem are registered in basis/mod.rs and re-exported at crate root; basis tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library caller -> monomial_basis | Numeric args (argvals, nbasis); no network/file/deserialization surface. Invalid values are rejected via FdarError, never panic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-01 | Denial of Service | monomial_basis input validation | low | mitigate | Reject n<2 argvals / nbasis<1 via FdarError at entry; no unbounded allocation from unchecked sizes; no panic path. |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | No new crate dependency added this phase (reuses existing nalgebra/helpers). No package-install surface. |
</threat_model>

<verification>
- cargo test -p fdars-core --features linalg,parallel basis:: — monomial eval + penalty + validation tests green.
- cargo clippy --all-targets --features linalg,parallel -- -D warnings — clean (run at end of task 3).
- Existing bspline/fourier/constant/pspline signatures unchanged (git diff shows only additive files + two pub(crate) edits + two additive re-export lines).
</verification>

<success_criteria>
BasisSystem + monomial_basis exist, are crate-root re-exported, evaluate to closed-form monomials with an analytic penalty, reject invalid inputs via FdarError, and the smooth_basis numeric-Gram helpers are pub(crate) for downstream reuse — all with existing code untouched and suite + clippy green.
</success_criteria>

<output>
Create `.planning/phases/35-basis-system-completions/35-01-SUMMARY.md` when done
</output>
