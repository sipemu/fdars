---
phase: 35-basis-system-completions
plan: 04
type: execute
wave: 4
depends_on: [35-03]
files_modified:
  - fdars-core/src/pda.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [REP-01]
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "An Lfd linear-differential-operator object can be constructed from coefficient functions and applied to functional data via apply(data, argvals) -> Result<FdMatrix, FdarError> (REP-01 SC3)"
    - "Lfd::apply forms Lx = D^m x + sum_k beta_k(t)*D^k x using helpers::gradient finite differences, broadcasting length-1 (constant) coefficient vectors (REP-01 SC3)"
    - "principal_differential_analysis(data, argvals, order) recovers the coefficients of a known linear ODE (harmonic oscillator x''=-w^2 x -> beta0 ~ w^2, beta1 ~ 0) within tolerance (REP-01 SC3)"
    - "Invalid inputs (mismatched argvals length, singular/underdetermined PDA design with n < order+1) return FdarError rather than panicking (REP-01 SC4)"
    - "Lfd, PdaResult, principal_differential_analysis are crate-root re-exported; existing code unchanged; full suite + clippy --all-targets green (REP-01 SC3/SC5)"
  artifacts:
    - fdars-core/src/pda.rs
  key_links:
    - "src/lib.rs adds pub mod pda; and pub use pda::{Lfd, PdaResult, principal_differential_analysis}"
    - "Lfd::apply and PDA reuse crate::helpers::gradient for iterated finite-difference derivatives"
    - "PDA pointwise normal equations solved via nalgebra SVD pseudoinverse (rank-deficient guard)"
---

<objective>
Expansion: add the `Lfd` linear-differential-operator object and the `principal_differential_analysis` (PDA) estimator in a new `fdars-core/src/pda.rs`, plus the `PdaResult` struct. `Lfd::apply` forms Lx via iterated finite-difference derivatives (`helpers::gradient`); PDA recovers the pointwise coefficient functions of a linear ODE by independent least squares at each grid point (SVD pseudoinverse with a singular-design guard). Register all three at crate root. This closes the phase — Task 3 runs the full-suite + clippy phase gate.

Purpose: REP-01 SC3 — a composable Lfd operator plus a PDA estimator that recovers a known linear ODE's coefficients within tolerance.
Output: pda.rs (Lfd, principal_differential_analysis, PdaResult) + lib.rs registration + inline tests incl. harmonic-oscillator recovery.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-basis-system-completions/35-CONTEXT.md
@.planning/phases/35-basis-system-completions/35-RESEARCH.md

# Reuse analogs (verified on disk):
@fdars-core/src/matrix.rs
@fdars-core/src/helpers.rs
@fdars-core/src/smooth_basis.rs
@fdars-core/src/error.rs
@fdars-core/src/basis/pspline.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lfd struct + apply() (iterated finite-difference derivatives)</name>
  <files>fdars-core/src/pda.rs</files>
  <read_first>
    - fdars-core/src/helpers.rs line ~839 — gradient(y: &[f64], t: &[f64]) -> Vec<f64> auto-dispatches uniform 5-point / non-uniform 3-point. Apply it m times to a curve slice to build D^0..D^m.
    - fdars-core/src/matrix.rs — FdMatrix zeros/nrows/ncols/shape/column and column-major indexing data[(i,j)] = i + j*nrows.
    - fdars-core/src/smooth_basis.rs ~550 — differentiate_basis_columns iterates gradient per column; mirror that iterated-differentiation pattern for curve rows.
    - 35-RESEARCH.md "Lfd (Linear Differential Operator)" (struct design coefs: Vec<Vec<f64>>; apply algorithm; constant-coefficient broadcast when coefs[k].len()==1) and Pitfall 3 (constant-coefficient broadcasting).
  </read_first>
  <behavior>
    - Lfd { coefs: Vec<Vec<f64>> } where coefs.len() == operator order m; each coefs[k] is either length 1 (constant, broadcast) or length n_pts (grid-sampled).
    - apply(data, argvals): for each curve, compute D^0 x .. D^m x via iterated gradient on argvals; Lx(t_j) = D^m x(t_j) + sum_{k=0}^{m-1} beta_k(t_j) * D^k x(t_j); beta_k(t_j) = coefs[k][0] if coefs[k].len()==1 else coefs[k][j].
    - Constant operator sanity: for m=1 with coefs=[[c]] applied to a constant curve, D^1 const == 0 so Lx == c*const (within finite-difference tolerance).
    - argvals.len() != data.ncols() -> Err(InvalidDimension). Any coefs[k] length neither 1 nor n_pts -> Err(InvalidDimension). Never panic.
    - Returns FdMatrix of Lx values, same shape as data.
  </behavior>
  <action>
    Create pda.rs. Define pub struct Lfd { pub coefs: Vec<Vec<f64>> } with derive(Debug, Clone, PartialEq) + conditional serde. Implement apply(&self, data: &FdMatrix, argvals: &[f64]) -> Result<FdMatrix, FdarError>. Validate argvals.len()==data.ncols() and every coefs[k].len() in {1, n_pts}. m = self.coefs.len(). For each curve i: extract the row into a Vec<f64>, compute derivs[0]=curve, then derivs[k]=gradient(&derivs[k-1], argvals) for k=1..=m. Build Lx row: lx[j] = derivs[m][j] + sum_{k=0}^{m-1} beta_k(j) * derivs[k][j], with beta_k(j) broadcasting length-1 coefs (Pitfall 3). Write into the output FdMatrix column-major. Return it.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars &amp;&amp; TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel pda::tests::lfd 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Constant-operator test: m=1, coefs=[[2.0]] on a constant curve gives Lx ~ 2*const (within 1e-6).
    - Mismatched argvals length returns Err(InvalidDimension); no panic.
    - coefs[k] of a length other than 1 or n_pts returns Err(InvalidDimension).
    - apply returns an FdMatrix of the same shape as the input data.
  </acceptance_criteria>
  <done>Lfd::apply forms Lx via iterated finite-difference derivatives with constant-coefficient broadcasting and input validation; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PdaResult + principal_differential_analysis (pointwise SVD least squares)</name>
  <files>fdars-core/src/pda.rs</files>
  <read_first>
    - fdars-core/src/smooth_basis.rs ~611 — invert_penalized_system: the nalgebra SVD pseudoinverse pattern (to_dmatrix + SVD + singular-value threshold) to reuse for the pointwise normal-equations solve.
    - fdars-core/src/matrix.rs — FdMatrix column(j) gives the contiguous column slice for building the per-t design matrix.
    - fdars-core/src/basis/pspline.rs — PsplineFitResult derive + non_exhaustive pattern for PdaResult.
    - 35-RESEARCH.md "PDA (Principal Differential Analysis)" (pointwise normal equations X_j beta = y_j with y_j = -D^m x; SVD pseudoinverse; singular-design guard 1e-10*max_sv; n < order guard) and "Harmonic Oscillator Test" (n=20 curves x_i=A_i cos(wt)+B_i sin(wt), w=2pi, ~101 points; beta0~w^2, beta1~0) and Pitfall 4 (n < order).
  </read_first>
  <behavior>
    - PdaResult { coefficients: Vec<Vec<f64>>, order: usize, residuals: Option<FdMatrix> } — coefficients.len()==order; each inner Vec has length n_pts; non_exhaustive; residuals default None.
    - principal_differential_analysis(data, argvals, order): compute D^0..D^order for all curves; at each grid point t_j build design X_j (n x order, columns D^0..D^{order-1} at t_j) and target y_j = -D^order at t_j; solve via SVD pseudoinverse (threshold 1e-10*max singular value); coefficients[k][j] = beta_k(t_j).
    - Harmonic oscillator (w=2pi, order=2, >=101 uniform points, 20 curves): beta0(t_j) ~ w^2 (~39.478) and beta1(t_j) ~ 0, each within tolerance 1.0 at every interior grid point.
    - n < order+1 (too few curves for well-posed pointwise regression) -> Err(InvalidDimension). Mismatched argvals length -> FdarError. Never panic.
  </behavior>
  <action>
    In pda.rs define pub struct PdaResult { pub coefficients: Vec<Vec<f64>>, pub order: usize, pub residuals: Option<FdMatrix> } with derive(Debug, Clone, PartialEq) + non_exhaustive + conditional serde. Implement principal_differential_analysis(data: &FdMatrix, argvals: &[f64], order: usize) -> Result<PdaResult, FdarError>. Validate argvals.len()==data.ncols(), order>=1, and n_curves >= order+1 (Pitfall 4) else FdarError. Compute derivative FdMatrices D^0..D^order by applying helpers::gradient per curve row iteratively (share the row-derivative helper with Task 1). For each grid column j: build an n x order design matrix from D^0..D^{order-1} columns and the n-vector y_j = -(D^order column j); solve the least-squares normal system via nalgebra SVD pseudoinverse (mirror invert_penalized_system's threshold handling: drop singular values below 1e-10*max, giving a zero coefficient for a singular design). Store beta_k(t_j) into coefficients[k][j]. Leave residuals None (default; extensible via non_exhaustive). Return PdaResult.

    Add the harmonic-oscillator recovery test as an inline #[cfg(test)] case exactly as specified in RESEARCH (w=2pi, 20 curves x_i=A_i cos + B_i sin, ~101 points) asserting beta0 ~ w^2 and beta1 ~ 0 within tolerance; and a n<order+1 -> Err test.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars &amp;&amp; TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel pda 2>&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Harmonic-oscillator test passes: beta0(t_j) within 1.0 of (2pi)^2 and beta1(t_j) within 1.0 of 0 at every interior grid point.
    - n < order+1 returns Err(InvalidDimension); mismatched argvals length returns FdarError; no panic.
    - PdaResult.coefficients.len()==order, each inner length n_pts; struct is non_exhaustive.
  </acceptance_criteria>
  <done>principal_differential_analysis recovers a known linear ODE's coefficients via pointwise SVD least squares with singular-design and n<order+1 guards; harmonic-oscillator test green.</done>
</task>

<task type="auto">
  <name>Task 3: Register pda module + crate-root re-exports; phase-gate full suite + clippy</name>
  <files>fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/lib.rs — the pub mod declarations and crate-root re-export sections; add pda alongside.
  </read_first>
  <action>
    In lib.rs add pub mod pda; and pub use pda::{Lfd, PdaResult, principal_differential_analysis}; consistent with existing ordering. Do not touch other exports. Then run the phase gate: the FULL test suite and the full clippy gate; fix any warnings this phase introduced. This is the final plan — leave the crate green.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars &amp;&amp; TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel 2>&amp;1 | tail -15 &amp;&amp; TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - lib.rs contains pub mod pda; and pub use pda::{Lfd, PdaResult, principal_differential_analysis};.
    - fdars_core::Lfd, fdars_core::PdaResult, fdars_core::principal_differential_analysis callable at crate root.
    - Full cargo test suite green (no regressions in existing tests).
    - cargo clippy --all-targets --features linalg,parallel -- -D warnings clean.
  </acceptance_criteria>
  <done>pda module + Lfd/PdaResult/principal_differential_analysis crate-root re-exported; full suite + clippy phase gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library caller -> Lfd::apply / principal_differential_analysis | Numeric curve data + argvals + order; no network/file/deserialization surface. Dimension/rank violations rejected via FdarError. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-05 | Denial of Service | PDA underdetermined/singular design | low | mitigate | Guard n < order+1 with FdarError; SVD pseudoinverse thresholds tiny singular values so a rank-deficient design yields a zero coefficient instead of NaN/panic. |
| T-35-06 | Denial of Service | Lfd::apply argvals/coefs length mismatch | low | mitigate | Validate argvals.len()==ncols and coefs[k] length in {1, n_pts} before indexing; FdarError, no panic. |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | No new crate dependency (reuses existing nalgebra SVD + helpers::gradient). No package-install surface. |
</threat_model>

<verification>
- cargo test -p fdars-core --features linalg,parallel pda — Lfd apply + PDA harmonic-oscillator recovery + guard tests green.
- Full cargo test suite green (no regression in existing ~2268 tests).
- cargo clippy --all-targets --features linalg,parallel -- -D warnings — clean.
- git diff shows only the new pda.rs + two additive lib.rs lines.
</verification>

<success_criteria>
Lfd (with apply), principal_differential_analysis, and PdaResult exist in a new pda.rs, are crate-root re-exported, recover a known linear ODE's coefficients within tolerance, reject invalid/singular inputs via FdarError, and the full suite + clippy gate is green — satisfying REP-01 SC3 and closing the phase.
</success_criteria>

<output>
Create `.planning/phases/35-basis-system-completions/35-04-SUMMARY.md` when done
</output>
