---
phase: 37-specialized-fpca-variants
plan: 02
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - fdars-core/src/fpca_variants.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [FPCA-02-02, FPCA-02-04, FPCA-02-05]
estimate:
  tokens: 78000
  raw_tokens: 39000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "dynamical_correlation(x, y, argvals) returns a scalar f64 in [-1, 1]; =1 (within tol) when X==Y, =-1 when Y=-X, near 0 for independent samples"
    - "dynamical_correlation requires X and Y to share the same argvals grid and returns FdarError on mismatch"
    - "fsvd returns paired left/right singular functions at unit functional L2 norm plus singular values and per-sample scores, recovering a known rank-1 cross-covariance structure within a documented tolerance"
    - "fsvd applies a deterministic sign convention (largest-|magnitude| element of each left singular function positive) and flips the paired right function together"
    - "ssvd estimates loadings/scores via a sandwich-smoothed covariance and, at near-zero bandwidth, agrees with fdata_to_pc_1d within a documented tolerance (~1e-4)"
    - "All three functions reject invalid inputs (empty matrix, mismatched sample sizes, mismatched grids, ncomp out of range) with FdarError rather than panicking"
    - "cross_covariance, dynamical_correlation, fpca_der, fsvd, ssvd, and FsvdResult are all reachable from the crate root"
  artifacts:
    - "fdars-core/src/fpca_variants.rs (extended with dynamical_correlation, fsvd, ssvd)"
    - "extended crate-root re-export: pub use fpca_variants::{cross_covariance, dynamical_correlation, fpca_der, fsvd, ssvd, FsvdResult};"
  key_links:
    - "fsvd routes through cross_covariance (Plan 01) → Simpson-weighted → nalgebra SVD → unscale to unit L2 → sign fix"
    - "ssvd inlines the eigendecompose_cov sandwich pattern from pace_fpca.rs:165-237 (NOT a direct call — it is private) on a kernel-smoothed covariance"
    - "dynamical_correlation uses simpsons_weights and divides by domain_length in both the norm (step 3) and the inner product (step 4)"
---

<objective>
Expand the proven fpca_variants module (from Plan 01) with the three heavier FPCA variants: `dynamical_correlation` (scalar association, exact Dubin–Müller 4-step), `fsvd` (functional SVD / cross-FPCA, populating the FsvdResult defined in Plan 01), and `ssvd` (sandwich-smoother FPCA path). Add the crate-root smoke re-export integration test covering all five variants.

Purpose: Complete FPCA-02 by adding the variants that require careful normalization and sign conventions, building on the module skeleton, cross_covariance, and FsvdResult that Plan 01 landed and verified green.
Output: `fpca_variants.rs` extended with `dynamical_correlation`, `fsvd`, `ssvd`; `lib.rs` re-export block extended to all five variants + FsvdResult; smoke_reexports integration test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-37-specialized-fpca-variants/37-CONTEXT.md
@.planning/phases/phase-37-specialized-fpca-variants/37-RESEARCH.md
@.planning/phases/phase-37-specialized-fpca-variants/37-VALIDATION.md
@.planning/phases/phase-37-specialized-fpca-variants/37-01-PLAN.md

# Reused source (read the exact signatures + patterns before implementing):
@fdars-core/src/regression.rs
@fdars-core/src/fdata.rs
@fdars-core/src/helpers.rs
@fdars-core/src/pace_fpca.rs
@fdars-core/src/covariance.rs
@fdars-core/src/fpca_variants.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement dynamical_correlation (Dubin–Müller / DynCorr, exact 4-step)</name>
  <files>fdars-core/src/fpca_variants.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - .planning/phases/phase-37-specialized-fpca-variants/37-RESEARCH.md (Pattern 4 — the exact 4-step formula: per-curve integrated-mean centering → population centering → L2-norm standardization → integrated inner product averaged over subjects; and Pitfall 3 — divide by domain_length in BOTH step 3 and step 4)
    - fdars-core/src/helpers.rs:57-86 (simpsons_weights — sums to domain_length, NOT 1)
    - fdars-core/src/matrix.rs (FdMatrix indexing, .shape())
  </read_first>
  <behavior>
    - test_dyncorr_identical: dynamical_correlation(&data, &data, &argvals) ≈ 1.0 within 1e-10 (after double-centering + L2-normalization, std_xi == std_yi so the integrated self-product is 1).
    - test_dyncorr_negated: with neg = -data elementwise, dynamical_correlation(&data, &neg, &argvals) ≈ -1.0 within 1e-10.
    - test_dyncorr_range: for random-ish synthetic X, Y the result is within [-1.0, 1.0] (inclusive, allowing tiny fp slack e.g. 1e-9).
    - test_dyncorr_errors: nx != ny → FdarError; mx != my (curves) → FdarError; argvals.len() != mx → FdarError; nx < 2 → FdarError. Same-grid requirement enforced (single argvals used for both X and Y).
  </behavior>
  <action>
Implement `pub fn dynamical_correlation(x: &FdMatrix, y: &FdMatrix, argvals: &[f64]) -> Result<f64, FdarError>` per the locked CONTEXT decision (Dubin–Müller / fdapace DynCorr; range [-1,1]; shared grid required; scalar f64). Mark `#[must_use]`.

Validate at entry: `(nx, mx) = x.shape()`, `(ny, my) = y.shape()`; `nx == ny` else `FdarError::InvalidDimension`; `mx == my` else `InvalidDimension`; `argvals.len() == mx` else `InvalidDimension { parameter: "argvals", ... }`; `nx >= 2` else `InvalidDimension`. Compute `let w = crate::helpers::simpsons_weights(argvals);` and `let domain_length = argvals[mx - 1] - argvals[0];` (guard domain_length > 0 → else InvalidParameter).

Implement the exact 4 steps from RESEARCH Pattern 4 (do NOT reorder or drop a division — Pitfall 3):
- Step 1 (per-curve integrated-mean centering): for each curve i, `aver_xi = (Σ_j X[(i,j)]·w[j]) / domain_length`; `Xc1[(i,j)] = X[(i,j)] - aver_xi`. Same for Y.
- Step 2 (population centering): `Mx[j] = (Σ_i Xc1[(i,j)]) / nx`; `Xc2[(i,j)] = Xc1[(i,j)] - Mx[j]`. Same for Y.
- Step 3 (functional L2 standardization): `norm_xi = sqrt((Σ_j Xc2[(i,j)]^2 · w[j]) / domain_length)`; `std_xi[j] = Xc2[(i,j)] / norm_xi`, with the guard `if norm_xi < 1e-15 { use 0.0 for all j }` (division-by-zero → NaN mitigation). Same for Y.
- Step 4 (per-subject integrated inner product / domain_length, averaged): `z[i] = (Σ_j std_xi[j]·std_yi[j]·w[j]) / domain_length`; return `Σ_i z[i] / nx`.

Extend the crate-root re-export block in lib.rs to include `dynamical_correlation` (line becomes `pub use fpca_variants::{cross_covariance, dynamical_correlation, fpca_der, FsvdResult};` for now; fsvd/ssvd added in later tasks of this plan). Add inline `#[cfg(test)]` tests per the behavior above.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel test_dyncorr -- --test-threads=4</automated>
  </verify>
  <acceptance_criteria>
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel test_dyncorr` passes (identical, negated, range, errors).
    - dynamical_correlation(&data, &data, &argvals) is within 1e-10 of 1.0; with negated Y within 1e-10 of -1.0.
    - Result is always within [-1, 1] for the range test.
  </acceptance_criteria>
  <done>dynamical_correlation implements the exact 4-step DynCorr, requires a shared grid, returns a scalar in [-1,1] with the known-answer tests green, and is crate-root reachable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fsvd (functional SVD / cross-FPCA) populating FsvdResult</name>
  <files>fdars-core/src/fpca_variants.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - .planning/phases/phase-37-specialized-fpca-variants/37-RESEARCH.md (Pattern 2 — weighted cross-cov → thin SVD → unscale to unit L2 → sign fix → scores; and Pitfall 2 — flip left AND right function together)
    - fdars-core/src/regression.rs:180-201 (fix_svd_signs — the max-|abs|-element-positive convention to mirror)
    - fdars-core/src/regression.rs:370 (nalgebra SVD::new(mat.to_dmatrix(), true, true) call pattern)
    - fdars-core/src/helpers.rs:57-86 (simpsons_weights — one per grid)
    - fdars-core/src/fpca_variants.rs (cross_covariance from Plan 01 and the FsvdResult struct fields)
  </read_first>
  <behavior>
    - test_fsvd_unit_norm: each returned left_functions column k has Σ_s u_k[s]^2 · wx[s] ≈ 1.0 (within 1e-8); each right_functions column k has Σ_t v_k[t]^2 · wy[t] ≈ 1.0.
    - test_fsvd_rank1: for X[i,j]=α_i·sin(argvals_x[j]), Y[i,j]=α_i·cos(argvals_y[j]) with varying α_i, fsvd(...,1) yields one dominant singular value, left_functions[:,0] ∝ sin, right_functions[:,0] ∝ cos (within a documented tolerance after sign fixing), and the rank-1 cross-covariance reconstruction Σ_k s_k·u_k·v_k^T recovers the weighted cross-covariance within tolerance.
    - test_fsvd_errors: mismatched sample sizes nx != ny → FdarError; ncomp < 1 → FdarError; ncomp > min(p,q) is clamped or errors per documented behavior; empty matrix → FdarError.
  </behavior>
  <action>
Implement `pub fn fsvd(x: &FdMatrix, argvals_x: &[f64], y: &FdMatrix, argvals_y: &[f64], ncomp: usize) -> Result<FsvdResult, FdarError>` per the locked CONTEXT decision (paired samples matched n; weight cross-cov by √Simpson on both grids before thin-SVD; rescale singular functions to unit functional L2 norm; deterministic sign convention). Mark `#[must_use]`.

Validate: `(nx, p) = x.shape()`, `(ny, q) = y.shape()`; `nx == ny` else `FdarError::InvalidDimension`; `nx >= 2`; `argvals_x.len() == p`, `argvals_y.len() == q` else `InvalidDimension`; `ncomp >= 1` else `InvalidParameter`. Clamp effective ncomp to `min(ncomp, p, q)` (document the clamp in rustdoc) or error if ncomp > min(p,q) — pick clamp for robustness and document it.

Steps (RESEARCH Pattern 2):
1. `let c = cross_covariance(x, y)?;` (p×q, from Plan 01).
2. `let wx = simpsons_weights(argvals_x); let wy = simpsons_weights(argvals_y);` compute `sqrt_wx`, `sqrt_wy`.
3. Build weighted matrix `Cw[(s,t)] = sqrt_wx[s] · c[(s,t)] · sqrt_wy[t]` (p×q).
4. Thin SVD via nalgebra: `SVD::new(Cw.to_dmatrix(), true, true)` → U (p×k), singular values, V^T (k×q). Take the top `k = effective_ncomp` components.
5. Unscale singular functions back to unit functional L2: `u_k[s] /= sqrt_wx[s]` (guard sqrt_wx[s] > 1e-15), `v_k[t] /= sqrt_wy[t]`. After unscaling each u_k satisfies Σ_s u_k[s]^2·wx[s] ≈ 1.
6. Sign fix (mirror fix_svd_signs, Pitfall 2): for each component k find s_max = argmax_s |u_k[s]|; if u_k[s_max] < 0, negate the ENTIRE left column k AND the ENTIRE right column k together (preserves the singular value and u·C·v sign).
7. Scores: center X and Y per-grid (reuse fdata::center_1d), then `left_scores[(i,k)] = Σ_s Xc[(i,s)]·u_k[s]·wx[s]`, `right_scores[(i,k)] = Σ_t Yc[(i,t)]·v_k[t]·wy[t]`.
8. Assemble and return `FsvdResult { singular_values, left_functions (p×k), right_functions (q×k), left_scores (n×k), right_scores (n×k) }`.

Extend the lib.rs re-export block to include `fsvd`. Add inline `#[cfg(test)]` tests per the behavior above (unit-norm, rank-1 recovery, errors).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel test_fsvd -- --test-threads=4</automated>
  </verify>
  <acceptance_criteria>
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel test_fsvd` passes (unit_norm, rank1, errors).
    - Each singular function is unit functional L2 norm within 1e-8.
    - Rank-1 synthetic data recovers sin (left) and cos (right) singular functions within the documented tolerance after sign fixing.
    - fsvd is crate-root reachable and returns a fully-populated FsvdResult.
  </acceptance_criteria>
  <done>fsvd computes the Simpson-weighted cross-covariance SVD, unscales to unit-L2 singular functions, applies the paired sign convention, computes per-sample scores, and populates FsvdResult with green tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement ssvd (sandwich-smoother FPCA) + smoke re-export test + Wave-2 gate</name>
  <files>fdars-core/src/fpca_variants.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/pace_fpca.rs:165-237 (eigendecompose_cov — the W^{1/2}·Cov·W^{1/2} sandwich → symmetric_eigen → sort-descending/keep-positive → unscale by 1/sqrt_w → max-|abs|-positive sign fix pattern to INLINE, since it is private)
    - fdars-core/src/fdata.rs:358-395 (functional_covariance — empirical m×m covariance to smooth)
    - fdars-core/src/helpers.rs:247-253 (gaussian_kernel — NOTE: returns 0.0 when h < 1e-15, so bandwidth 0 is NOT identity; the near-zero-bandwidth dense limit must be handled explicitly)
    - fdars-core/src/regression.rs:287-400 (fdata_to_pc_1d — the dense reference the ssvd dense limit must match within ~1e-4)
    - .planning/phases/phase-37-specialized-fpca-variants/37-RESEARCH.md (Pattern 5 + Pitfall 4 — separable row-then-column Gaussian smoothing; dense-limit tolerance 1e-4)
  </read_first>
  <behavior>
    - test_ssvd_dense_limit: ssvd(&data, ncomp, &argvals, 1e-12) matches fdata_to_pc_1d(&data, ncomp, &argvals) on singular_values within relative 1e-4 (per Pitfall 4). CRITICAL: because gaussian_kernel returns 0 at bandwidth≈0, treat bandwidth <= a small epsilon as "no smoothing" and pass the EMPIRICAL covariance straight to the sandwich eigendecompose (identity smoother), so the dense limit is well-defined.
    - test_ssvd_orthonormality: the returned eigenfunctions are W-orthonormal — Σ_j φ_a[j]·φ_b[j]·w[j] ≈ δ_ab within 1e-6.
    - test_ssvd_errors: ncomp < 1 → FdarError; empty matrix (n==0) → FdarError; argvals.len() != m → FdarError.
    - smoke_reexports: an integration-style test in the same file asserting all five symbols resolve from the crate root — reference fdars_core::{cross_covariance, dynamical_correlation, fpca_der, fsvd, ssvd, FsvdResult} (compile-level reachability), e.g. via a test named `smoke_reexports` that calls each on tiny valid inputs and unwraps.
  </behavior>
  <action>
Implement `pub fn ssvd(data: &FdMatrix, ncomp: usize, argvals: &[f64], bandwidth: f64) -> Result<FpcaResult, FdarError>` per the locked CONTEXT decision (sandwich-smooth the empirical covariance then eigendecompose; reuse covariance/kernel machinery; NO new crate; dense/zero-bandwidth matches fdata_to_pc_1d within tolerance). Return `FpcaResult` (same struct as fdata_to_pc_1d). Mark `#[must_use]`. Per RESEARCH Open Question #2: use a raw `bandwidth: f64` (implicit Gaussian kernel), NOT a full CovKernel.

Validate: `(n, m) = data.shape()`; `n == 0` → FdarError; `m == 0` → FdarError; `argvals.len() == m` else InvalidDimension; `ncomp >= 1` else InvalidParameter; `bandwidth >= 0.0` else InvalidParameter.

Steps:
1. Center data per-grid and keep means: reuse `crate::fdata::center_1d` (or the center_columns pattern) — need `Xc` and `mean` for the FpcaResult.
2. Empirical covariance: `let emp = crate::fdata::functional_covariance(data)?;` (m×m, 1/(n-1)).
3. Smoothing: if `bandwidth <= 1e-10`, use `emp` directly (identity smoother — this is the dense limit; do NOT run gaussian_kernel which returns 0 at bandwidth 0). Otherwise apply a SEPARABLE row-then-column Gaussian kernel smoother over the argvals grid (RESEARCH Pattern 5 / Assumption A1): for each output (s,t), weight neighbors by `gaussian_kernel(|argvals[s]-argvals[s']|, bandwidth)` along rows then columns, normalizing each pass by the sum of kernel weights. Produce `smooth_cov` (m×m, symmetric by construction).
4. Sandwich eigendecompose — INLINE the pace_fpca eigendecompose_cov pattern (RESEARCH Anti-Pattern: it is a private fn, cannot be called): compute `w = simpsons_weights(argvals)`, `sqrt_w`, build `c_scaled[row+col*m] = sqrt_w[row]·smooth_cov[(row,col)]·sqrt_w[col]`, `DMatrix::from_column_slice(m,m,&c_scaled).symmetric_eigen()`, sort eigenvalues descending, keep positive ones up to ncomp, unscale eigenfunctions `phi_k[j] = v_k[j]/sqrt_w[j]` (guard sqrt_w[j] > 1e-15), apply the max-|abs|-element-positive sign fix.
5. singular_values: per FpcaResult convention, populate with the FPCA singular values consistent with fdata_to_pc_1d so the dense-limit comparison is apples-to-apples. NOTE: fdata_to_pc_1d's singular_values are SVD singular values of the weighted centered data; the eigenvalues of W^{1/2}·Cov·W^{1/2} relate as singular_value = sqrt(eigenvalue · (n-1)). Derive the exact relationship by reading fdata_to_pc_1d (regression.rs:287-400) and match its convention so test_ssvd_dense_limit passes at 1e-4; document the chosen convention in rustdoc.
6. scores: project centered data onto eigenfunctions with weights: `scores[(i,k)] = Σ_j Xc[(i,j)]·phi_k[j]·w[j]`.
7. Assemble `FpcaResult { singular_values, rotation (m×k eigenfunctions), scores (n×k), mean, centered: Xc, weights: w }`.

Rustdoc: document that ssvd is an alternative smoothed-covariance (sandwich) estimator to fdata_to_pc_1d's raw thin-SVD, that it reuses the pace_fpca sandwich pattern (inlined), and that bandwidth<=1e-10 is the no-smoothing dense limit.

Extend the lib.rs re-export block to the final form: `pub use fpca_variants::{cross_covariance, dynamical_correlation, fpca_der, fsvd, ssvd, FsvdResult};`. Add the `smoke_reexports` test and the ssvd behavior tests.

Wave-2 gate: after implementing, run `cargo fmt -p fdars-core` and confirm the full suite + clippy --all-targets are green (see verify). Use TMPDIR=/home/simonm/.cache/fdars-bench-tmp for linking. Fix any fmt/clippy drift before committing; this CODE commit must pass fmt + clippy + tests.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo fmt -p fdars-core --check && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel</automated>
  </verify>
  <acceptance_criteria>
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel test_ssvd` passes (dense_limit within relative 1e-4, orthonormality, errors).
    - `smoke_reexports` test passes: all five variants + FsvdResult resolve and run from the crate root.
    - `cargo fmt -p fdars-core --check`, `cargo clippy --all-targets --features linalg,parallel -- -D warnings`, and `cargo test -p fdars-core --features linalg,parallel` are all fully green.
    - No existing public signature changed (additive/non-breaking).
  </acceptance_criteria>
  <done>ssvd implements the sandwich-smoothed FPCA path (inlined pace_fpca eigendecompose pattern), matches fdata_to_pc_1d in the dense limit within 1e-4, produces W-orthonormal eigenfunctions; all five variants are crate-root reachable via smoke_reexports; whole crate + clippy --all-targets green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → public fn args | Only untrusted input is malformed function arguments (empty matrix, mismatched sample sizes, mismatched argvals grids, ncomp out of range, degenerate/zero-norm curves, negative bandwidth). No network / file / process I/O; no untrusted deserialization; no unsafe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-05 | Tampering (NaN propagation) | dynamical_correlation L2 normalization | low | mitigate | Guard `if norm_xi < 1e-15 { use zeros }` in step 3; guard domain_length > 0 |
| T-37-06 | Tampering (NaN propagation) | fsvd / ssvd unscale by 1/sqrt_w | low | mitigate | Guard `sqrt_w[j] > 1e-15` before dividing (mirrors pace_fpca.rs:210-213) |
| T-37-07 | Tampering (panic on bad dims) | fsvd / dynamical_correlation / ssvd | low | mitigate | Dimension + grid checks at entry return FdarError; no unchecked indexing |
| T-37-08 | Tampering (wrong dense-limit result) | ssvd bandwidth≈0 with gaussian_kernel returning 0 | low | mitigate | Treat bandwidth <= 1e-10 as identity smoother (pass empirical covariance straight through) rather than zeroing it |
| T-37-09 | Information disclosure | zero-row / degenerate input silently producing zeros | low | mitigate | Explicit n>=2 / ncomp>=1 checks before computation; near-degenerate covariance yields correct near-zero singular values (no extra guard needed) |

No threat reaches the block-on threshold (all low; pure in-process numerical library, no new dependency → no package-legitimacy gate).
</threat_model>

<verification>
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel` fully green (existing suite + all Phase 37 tests: test_dyncorr_*, test_fsvd_*, test_ssvd_*, smoke_reexports).
- `cargo clippy --all-targets --features linalg,parallel -- -D warnings` clean.
- `cargo fmt -p fdars-core --check` clean.
- All five variants (cross_covariance, fpca_der, dynamical_correlation, fsvd, ssvd) + FsvdResult reachable from crate root.
- No existing public signature changed.
</verification>

<success_criteria>
- FPCA-02-02 (fsvd), FPCA-02-04 (dynamical_correlation), FPCA-02-05 (ssvd) implemented, crate-root re-exported, covered by green inline tests matching VALIDATION.md test names (test_fsvd_unit_norm, test_fsvd_rank1, test_fsvd_errors, test_dyncorr_identical, test_dyncorr_negated, test_dyncorr_range, test_dyncorr_errors, test_ssvd_dense_limit, test_ssvd_orthonormality, test_ssvd_errors, smoke_reexports).
- ssvd dense limit agrees with fdata_to_pc_1d within 1e-4.
- Whole crate + clippy --all-targets clean; additive/non-breaking.
</success_criteria>

<output>
Create `.planning/phases/phase-37-specialized-fpca-variants/37-02-SUMMARY.md` when done.
</output>