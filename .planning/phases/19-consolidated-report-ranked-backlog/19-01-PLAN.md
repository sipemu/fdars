---
phase: 19-consolidated-report-ranked-backlog
plan: "19-01"
type: execute
wave: 1
requirements: [RPT-01, RPT-02]
audit_only: true
deliverables:
  - .planning/research/R-AUDIT-REPORT.md
  - .planning/research/R-BACKLOG.md
files_modified:
  - .planning/research/R-AUDIT-REPORT.md
  - .planning/research/R-BACKLOG.md
status: ready
---

# Phase 19 · Plan 01 — Consolidated Report & Ranked Backlog

**Goal:** Synthesize Phases 16–18 into RPT-01 (consolidated findings in `R-AUDIT-REPORT.md`) and RPT-02 (new value-ranked `R-BACKLOG.md`). Audit-only — zero `fdars-core/src/` edits.

## Task 1 — Consolidated report (RPT-01)
Append `## Phase 19 — Consolidated Report` to `.planning/research/R-AUDIT-REPORT.md`:
- `### §Methodology (Consolidated)` — packages + versions surveyed (35, as of 2026-08), the in/out-of-scope rule, the D-01 verdict rubric, the D-03 category rubric; note the `Rfssa` inventory-completeness caveat.
- `### §Consolidated Findings` — headline numbers (35 pkgs, 275 caps, 248 in-scope, 162 actionable gaps); gap counts by area and by category (table-stakes 18 / differentiator 144); the fdars-strengths summary (12 R-honest strengths, headliners explainability + streaming depth); the largest gap zones (Areas 7, 6, 5).

### Success checks (ROADMAP SC1)
- [ ] Methodology section (packages+versions, in/out rule, both rubrics) + Consolidated Findings (gap counts by area & category + strengths summary) present.

## Task 2 — GSD-ready ranked backlog (RPT-02)
Create NEW file `.planning/research/R-BACKLOG.md` (distinct from scikit-fda `BACKLOG.md`):
- Ranking methodology: `score = value/√effort` (value 1–5, effort S/M/L, severity P1/P2/P3), mirroring v0.14.0.
- Cluster the 162 actionable gaps into ~20–35 coherent GSD-ready items (candidate requirements/phases). Table-stakes rank high; big differentiator clusters (Fréchet/object-data, functional inference suite, FTS forecasting, sparse/PACE FPCA, FoF regression, concurrent regression) are milestone-sized.
- Master ranked table **strictly non-increasing** by score; ties broken by severity.
- Per item: 7-field promotion block (candidate phrasing · R-side reference + parity rows · fdars current gap · proposed direction incl. `fdars-core/src/` location · value+effort+severity+category · score · notes/deps).
- Do NOT propose building fdars' existing strengths (Phase 18).
- Completeness-gate statement (table strictly descending; every item has a block; top items non-cosmetic; table-stakes represented near the top).

### Success checks (ROADMAP SC2–SC4)
- [ ] `R-BACKLOG.md` documents the `value/√effort` methodology.
- [ ] Master ranked table strictly non-increasing, ties by severity.
- [ ] Every ranked item has a matching 7-field promotion block.

**Constraints:** append RPT-01 to existing `R-AUDIT-REPORT.md`; create `R-BACKLOG.md` fresh; do not overwrite Phases 16–18 or the archived scikit-fda files. Zero `fdars-core/src/` edits.

**Commit:** `docs(19): consolidated findings + R-BACKLOG.md ranked backlog (RPT-01/RPT-02)`
