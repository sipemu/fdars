---
phase: 93-release-preparation-readiness-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/Cargo.toml
  - CHANGELOG.md
  - fdars-core/CHANGELOG.md
  - documentation/ROADMAP-TO-1.0.md
  - README.md
autonomous: true
requirements: [REL-01, REL-02]

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "fdars-core/Cargo.toml declares version = \"0.43.0\" (REL-01)"
    - "BOTH CHANGELOG.md (root) and fdars-core/CHANGELOG.md carry a top ## [0.43.0] entry describing the golden-flake root cause (faer/nalgebra SVD backend divergence, NOT environmental), the least-invasive test-side cfg-guard fix (no dep, no tolerance, no src change), the suite-wide robustness audit (zero further fragile tests), and the new determinism-guardrail CI job (REL-01)"
    - "BOTH changelog entries state 0.43.0 supersedes the unpublished 0.41.0/0.42.0 (registry at 0.40.0) AND that the `git tag v0.43.0` -> crates.io publish is the deferred operator step (REL-01, Success Criterion 4)"
    - "The golden-flake Quality item in documentation/ROADMAP-TO-1.0.md is marked [x] with the stale 'env/BLAS/disk-pressure dependent' clause rewritten to the true cause (feature-config faer-vs-nalgebra backend divergence when compiled without linalg) and fix (test-side cfg-guard), citing Phases 90-92 (REL-01)"
    - "README.md dependency snippet reads fdars-core = { version = \"0.43\", ... } (REL-01)"
    - "All 6 release gates pass green: cargo fmt --check; clippy --all-targets --features linalg,parallel -D warnings; full cargo test --features linalg,parallel,serde; --features serde build; all 28 examples + doctests; cargo package (REL-02)"
    - "No src/ or algorithm change was made; no git tag pushed and no crates.io publish performed (operator step documented as remaining)"
  artifacts:
    - fdars-core/Cargo.toml
    - CHANGELOG.md
    - fdars-core/CHANGELOG.md
    - documentation/ROADMAP-TO-1.0.md
    - README.md
  key_links:
    - "Cargo.toml version 0.43.0 <-> cargo package (gate 6 reads it) <-> CHANGELOG [0.43.0] header"
    - "ROADMAP-TO-1.0 Quality [x] text <-> 90-DIAGNOSIS.md true root cause"
    - "full cargo test --features linalg,parallel,serde (gate 3) <-> the determinism proof enforced by the Phase-92 CI guardrail"
---

<objective>
Make fdars-core release-ready at 0.43.0 (milestone: Test Determinism & Release Hardening): bump the version, write the CHANGELOG `[0.43.0]` entries in BOTH changelogs, refresh the two stale docs (the 1.0 Quality checklist item + the README version), then run all six whole-crate release gates green — the full-suite `cargo test` standing as end-to-end proof the golden-flake fix holds.

Purpose: This is the FINAL phase of v0.43.0. It converts the completed determinism work (Phases 90-92) into a shippable, documented release state. NO code/algorithm change — docs + version + verification only.

Output: A version-bumped, changelog-documented, gate-verified working tree. The `git tag v0.43.0` -> crates.io publish is DEFERRED to the operator (not performed here).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md

## Build/disk hazards (MEMORY.md — mandatory for the gate run)
- Run `cargo fmt` before each commit — `--no-verify` commits otherwise leave fmt drift that fails CI fmt-check.
- The pre-commit hook runs the full cargo gate and TIMES OUT. Run each gate FOREGROUND, per-gate, with an explicit 600000ms (600s) tool timeout, then `git commit --no-verify`. Do NOT run the combined gate as one background job — it gets killed mid-run.
- Free `target/debug/{incremental,examples}` (`rm -rf`) before the long gate run: `target/` fills `/home`; doctests and `cargo package` link in a small `/tmp` tmpfs — watch space.
- Keep the `--features serde` build green (repaired in v0.40.0 — do not regress).
- Execute inline. fdars executor subagents stall on long cargo runs.
- Local `main` is ~24 commits ahead of `origin/main` — expected. Do NOT push. Do NOT tag (operator step).
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-release-preparation-readiness-verification/93-CONTEXT.md

# True root cause + fix (source of the changelog + ROADMAP-TO-1.0 corrected text)
@.planning/phases/90-golden-flake-root-cause-deterministic-fix/90-DIAGNOSIS.md
# Suite-wide audit outcome (zero further fragile tests)
@.planning/phases/91-suite-wide-robustness-sweep/91-AUDIT.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Version bump + BOTH changelogs + docs refresh — the full release-doc slice (REL-01)</name>
  <files>fdars-core/Cargo.toml, CHANGELOG.md, fdars-core/CHANGELOG.md, documentation/ROADMAP-TO-1.0.md, README.md</files>
  <read_first>
    - fdars-core/Cargo.toml (line 3: `version = "0.42.0"`)
    - CHANGELOG.md (lines 1-25: header + top `## [0.42.0] - 2026-09-08` entry — the style to match; prepend-at-top, plain `## [x.y.z] - date` headers, no bottom link-refs)
    - fdars-core/CHANGELOG.md (lines 1-20: Keep-a-Changelog header + top `## [0.42.0] - 2026-09-08` entry — DIFFERENT style from root)
    - documentation/ROADMAP-TO-1.0.md (lines 15-17: the `**Update (v0.42.0):**` note pattern; lines 47-54: the `## Test & quality debt` Quality item to flip + rewrite)
    - README.md (line 84: `fdars-core = { version = "0.41", default-features = false }`)
    - .planning/phases/90-golden-flake-root-cause-deterministic-fix/90-DIAGNOSIS.md (the true root cause + chosen fix — verbatim source for the corrected ROADMAP text and changelog wording)
  </read_first>
  <action>
Perform five scoped edits — docs/version only, NO src/ or algorithm change:

1. **Cargo.toml version bump.** In fdars-core/Cargo.toml replace `version = "0.42.0"` with `version = "0.43.0"` (line 3). This is the value gate 6 (`cargo package`) reads.

2. **Root CHANGELOG.md** — prepend a new `## [0.43.0] - 2026-09-09` entry directly above the existing `## [0.42.0] - 2026-09-08` entry (line 14), matching this file's prose style. Frame it as **Test Determinism & Release Hardening**. Content, in the root file's voice:
   - The long-standing co_cluster / svd_sign golden flake is **root-caused** as a deterministic faer-vs-nalgebra SVD backend divergence in `fdata_to_pc` when compiled without the `linalg` feature — explicitly NOT an environmental / disk-pressure / thread-scheduling flake (the historical hypothesis was overturned in Phase 90).
   - The **fix** is least-invasive: a test-side `#[cfg_attr(not(feature = "linalg"), ignore)]` guard on exactly the three affected golden tests — no new dependency, no tolerance relaxation, no `src/` change.
   - A **suite-wide robustness audit** (Phase 91) found zero further fragile tests (backend-fragility fully covered, RNG/thread-order robust under a thread-count sweep).
   - A new **`determinism-guardrail` CI job** (Phase 92: repeat loop + `RAYON_NUM_THREADS=1` + anti-silent-skip golden assertions) now gates the suite and the crates.io publish against regression.
   - **Supersession + deferred-operator note:** state that 0.43.0 supersedes the unpublished 0.41.0 and 0.42.0 (registry is still at 0.40.0 — one publish catches all three up), and that the operator-driven `git tag v0.43.0` -> crates.io publish (auto via `release.yml`) is the remaining step, not performed in any phase. Mirror the existing "Folds in the unpublished vX" supersession phrasing already in this file (near line 151).
   - This milestone is docs/tests/CI only — no numeric or behavioral change to `fdars-core`; MSRV unchanged (1.81 crate / 1.84 for `linalg`).

3. **Crate CHANGELOG.md** (fdars-core/CHANGELOG.md) — prepend an analogous `## [0.43.0] - 2026-09-09` entry above the top `## [0.42.0] - 2026-09-08` (line 8), following THIS file's Keep-a-Changelog style (it DIFFERS from root — use `### Fixed` / `### Added` / `### Notes` sections as fits). Same substantive content as (2): root cause, cfg-guard fix, robustness audit, CI guardrail, supersession + deferred-operator note. Do not copy the root prose verbatim — match this file's own conventions.

4. **documentation/ROADMAP-TO-1.0.md — flip AND rewrite the Quality item** (lines 49-54, under `## Test & quality debt`). Change `- [ ]` to `- [x]`, and REPLACE the stale parenthetical `(env/BLAS/disk-pressure dependent)` and the stale "either switch to tolerance comparison, or serialize" remediation clause with the TRUE diagnosis + fix: the failure is a deterministic feature-configuration artifact — the goldens are captured under the faer SVD backend (`--features linalg`) and diverge when compiled without `linalg` (nalgebra backend); resolved by a test-side `#[cfg_attr(not(feature = "linalg"), ignore)]` guard on the three tests (NOT tolerance, NOT serialization, NO src change), audited suite-wide and locked by a CI determinism guardrail. Cite Phases 90-92. Do NOT leave the wrong root-cause text checked off. ALSO add a `**Update (v0.43.0):**` note near line 17 (mirroring the existing `**Update (v0.42.0):**` line-17 pattern) recording that the Quality (golden-flake) blocker is cleared in v0.43.0.

5. **README.md** — on line 84 update the dependency snippet version `"0.41"` to `"0.43"` (leave `default-features = false` unchanged). This is the single stale version reference; no other guide rewrite.

Do not touch any file under `fdars-core/src/`. Do not add or remove dependencies.

<!-- planner-discipline-allow: env/BLAS/disk-pressure dependent -->
  </action>
  <acceptance_criteria>
    - fdars-core/Cargo.toml line 3 is exactly `version = "0.43.0"` and no `version = "0.42.0"` remains in that file.
    - CHANGELOG.md has a `## [0.43.0]` header positioned above `## [0.42.0]`, and its body contains the strings for the SVD backend divergence root cause, the cfg-guard fix, the robustness audit, the determinism-guardrail CI job, the 0.40.0-registry supersession fact, and the deferred `git tag v0.43.0` operator step.
    - fdars-core/CHANGELOG.md has a `## [0.43.0]` header above `## [0.42.0]` covering the same substantive points in its own Keep-a-Changelog style.
    - documentation/ROADMAP-TO-1.0.md: the golden-flake Quality item line begins `- [x]`; the substring `env/BLAS/disk-pressure dependent` is GONE; the item text names the faer-vs-nalgebra / no-linalg backend divergence and the cfg-guard fix and cites Phases 90-92; a `**Update (v0.43.0):**` note exists.
    - README.md line 84 reads `fdars-core = { version = "0.43", default-features = false }`.
    - `git diff --name-only` shows only the five files above; nothing under `fdars-core/src/`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q '^version = "0.43.0"' fdars-core/Cargo.toml && ! grep -q '^version = "0.42.0"' fdars-core/Cargo.toml && grep -q '## \[0.43.0\]' CHANGELOG.md && grep -q '## \[0.43.0\]' fdars-core/CHANGELOG.md && grep -q 'version = "0.43"' README.md && echo DOCS_OK</automated>
    <fails_when>no DOCS_OK (version not bumped to 0.43.0, a stale 0.42.0 remains, a [0.43.0] changelog header is missing from either file, or README still on 0.41/0.42)</fails_when>
  </verify>
  <verify>
    <automated>grep -nE '^- \[x\] \*\*`golden`-test flake' documentation/ROADMAP-TO-1.0.md | grep -q . && ! grep -q 'env/BLAS/disk-pressure dependent' documentation/ROADMAP-TO-1.0.md && grep -q '\*\*Update (v0.43.0):\*\*' documentation/ROADMAP-TO-1.0.md && echo ROADMAP_OK</automated>
    <fails_when>no ROADMAP_OK (Quality item not marked [x], the stale env/BLAS/disk-pressure clause still present, or the Update (v0.43.0) note missing)</fails_when>
  </verify>
  <verify>
    <automated>test -z "$(git diff --name-only -- fdars-core/src/)" && echo NO_SRC_CHANGE</automated>
    <fails_when>no NO_SRC_CHANGE (a file under fdars-core/src/ was modified — this must be docs/version only)</fails_when>
  </verify>
  <done>Version is 0.43.0; both changelogs carry a [0.43.0] entry with the true root cause, cfg-guard fix, robustness audit, CI guardrail, supersession, and deferred-operator note; the ROADMAP-TO-1.0 Quality item is [x] with corrected text and a v0.43.0 update note; README is on 0.43; no src/ change.</done>
  <reversibility rating="reversible">Pure documentation/version edits — fully revertible with git checkout, no persisted external state.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Run all 6 release gates green and record results (REL-02)</name>
  <files>.planning/phases/93-release-preparation-readiness-verification/93-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/93-release-preparation-readiness-verification/93-CONTEXT.md (the 6-gate list + Claude's-Discretion notes on gate 3 config and `cargo package --no-verify`)
    - .planning/phases/91-suite-wide-robustness-sweep/91-AUDIT.md (the green baseline: 3676 passed under `--features linalg,parallel,serde`; expected gate-3 result)
  </read_first>
  <action>
Run every gate FOREGROUND, per-gate, each with an explicit 600000ms tool timeout. Do NOT combine them into one background job (it gets killed mid-run). Free disk first: `rm -rf target/debug/incremental target/debug/examples` (target/ fills /home; /tmp tmpfs is small for doctest/package links). Run `cargo fmt` (not just --check) once up front so the commit carries no fmt drift, then run the gates. All commands run from repo root against crate `fdars-core`.

Gates (all must exit 0 / report 0 failed):
1. **Format:** `cargo fmt --check` (repo-wide).
2. **Clippy (mirror CI exactly):** `cargo clippy --all-targets --features linalg,parallel -- -D warnings`. CI lints test/bench code via `--all-targets`; a plain `-p` invocation misses test-code warnings. (Optionally also run CI's serde+`-A` variant if CI does; the `--all-targets --features linalg,parallel -- -D warnings` form is the gate of record.)
3. **Full test (the determinism proof):** `cargo test -p fdars-core --features linalg,parallel,serde`. Expect ~3676 passed, 0 failed, the 3 goldens green (matches the Phase-91 baseline). This is REL-02's evidence the flake fix holds end-to-end and is exactly the path the Phase-92 CI guardrail enforces. (Discretion: the `--no-default-features --features linalg` sequential config is a green bonus per Phase 90-91 — run it if disk/time allows, but the serde config satisfies the proof.)
4. **serde build guard:** `cargo build -p fdars-core --features serde` (must stay green — repaired v0.40.0).
5. **Examples + doctests:** `cargo build -p fdars-core --examples --features linalg,parallel` (all 28 examples compile) and `cargo test -p fdars-core --doc --features linalg,parallel` (doctests). Watch /tmp for doctest linking.
6. **Package:** `cargo package -p fdars-core` after Task 1's edits are committed (package is VCS-state sensitive). If the /tmp link hazard or verify build exhausts space, fall back to `cargo package -p fdars-core --no-verify` and document that choice in the summary (per CONTEXT Discretion).

Record each gate's exact command and pass/fail (with the test pass-count for gate 3) in the plan SUMMARY. Then `git commit --no-verify` the five Task-1 files with a message noting `docs(93): release-prep 0.43.0` — the `--no-verify` avoids the timing-out pre-commit hook, and the up-front `cargo fmt` avoids fmt drift.

Do NOT `git push`, do NOT `git tag`, do NOT `cargo publish`. Tagging/publishing is the deferred operator step (release.yml auto-publishes on a `v*` tag) — document it as the single remaining action in the summary.
  </action>
  <acceptance_criteria>
    - Gate 1 `cargo fmt --check` exits 0.
    - Gate 2 `cargo clippy --all-targets --features linalg,parallel -- -D warnings` exits 0.
    - Gate 3 full `cargo test --features linalg,parallel,serde` exits 0 with 0 failed and the 3 goldens green.
    - Gate 4 `cargo build --features serde` exits 0.
    - Gate 5 examples build + doctests exit 0.
    - Gate 6 `cargo package` (or `--no-verify`, documented) exits 0.
    - SUMMARY records each gate command + result; no `git push`/`git tag`/`cargo publish` was run; the operator tag/publish step is documented as remaining.
  </acceptance_criteria>
  <verify>
    <automated>cargo fmt --check && echo FMT_OK</automated>
    <fails_when>no FMT_OK / non-zero exit (formatting drift — gate 1 failed)</fails_when>
  </verify>
  <verify>
    <automated>cargo clippy --all-targets --features linalg,parallel -- -D warnings && echo CLIPPY_OK</automated>
    <fails_when>no CLIPPY_OK / non-zero exit (a clippy warning was promoted to an error — gate 2 failed)</fails_when>
  </verify>
  <verify>
    <automated>cargo test -p fdars-core --features linalg,parallel,serde 2>&1 | tee /tmp/claude-1000/gate3.log | grep -q 'test result: ok' && ! grep -q 'test result: FAILED' /tmp/claude-1000/gate3.log && echo TEST_OK</automated>
    <fails_when>no TEST_OK, or any 'test result: FAILED' line in output (the determinism proof failed — gate 3, the golden flake is not fixed)</fails_when>
  </verify>
  <verify>
    <automated>cargo build -p fdars-core --features serde && echo SERDE_BUILD_OK</automated>
    <fails_when>no SERDE_BUILD_OK / non-zero exit (serde feature build regressed — gate 4)</fails_when>
  </verify>
  <verify>
    <automated>cargo build -p fdars-core --examples --features linalg,parallel && cargo test -p fdars-core --doc --features linalg,parallel 2>&1 | grep -q 'test result: ok' && echo EXAMPLES_DOCS_OK</automated>
    <fails_when>no EXAMPLES_DOCS_OK / non-zero exit (an example failed to compile or a doctest failed — gate 5)</fails_when>
  </verify>
  <verify>
    <automated>cargo package -p fdars-core 2>&1 | tail -5; test "${PIPESTATUS:-$?}" = 0 && echo PACKAGE_OK || (cargo package -p fdars-core --no-verify && echo PACKAGE_NOVERIFY_OK)</automated>
    <fails_when>neither PACKAGE_OK nor PACKAGE_NOVERIFY_OK (crate cannot be packaged for publish — gate 6; the --no-verify fallback for the /tmp link hazard must be documented if used)</fails_when>
  </verify>
  <verify>
    <automated>if git tag --list | grep -qx 'v0.43.0'; then echo TAGGED_UNEXPECTED; else echo NO_TAG_OK; fi</automated>
    <fails_when>TAGGED_UNEXPECTED (a v0.43.0 tag was created — tagging is the deferred OPERATOR step and must NOT happen in this phase)</fails_when>
  </verify>
  <done>All 6 gates green (fmt clean, clippy -D warnings clean, full serde test 0-failed with the 3 goldens green, serde build green, 28 examples + doctests green, cargo package ok); the five Task-1 files committed with --no-verify after cargo fmt; no push, no v0.43.0 tag, no publish; SUMMARY records each gate result and names the operator tag/publish as the sole remaining step.</done>
  <reversibility rating="reversible">Verification-only plus a docs commit; no external state, no publish. Fully revertible.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree -> crates.io registry | A `git tag v0.43.0` push auto-triggers `release.yml` -> `cargo publish`. This phase MUST stay on the working-tree side of that boundary; crossing it is the deferred operator step. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-93-01 | Tampering | fdars-core/src/ (accidental algorithm change during "release prep") | medium | mitigate | Task 1 forbids src/ edits; gate `git diff --name-only -- fdars-core/src/` must be empty (verify NO_SRC_CHANGE). |
| T-93-02 | Elevation | premature `git tag v0.43.0` -> auto-publish of an unverified/unwanted version | high | mitigate | Task 2 explicitly forbids push/tag/publish; verify NO_TAG_OK asserts no v0.43.0 tag exists; operator step documented, never executed here. |
| T-93-03 | Repudiation | stale root-cause text left checked off in ROADMAP-TO-1.0 (misleading 1.0 record) | low | mitigate | Task 1 rewrites the clause + negative-greps `env/BLAS/disk-pressure dependent` out (verify ROADMAP_OK). |
| T-93-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase (docs + verification only); no legitimacy gate needed. |
</threat_model>

<verification>
Phase is verified when every `<automated>` gate above emits its OK sentinel: DOCS_OK, ROADMAP_OK, NO_SRC_CHANGE (Task 1); FMT_OK, CLIPPY_OK, TEST_OK, SERDE_BUILD_OK, EXAMPLES_DOCS_OK, PACKAGE_OK (or PACKAGE_NOVERIFY_OK, documented), NO_TAG_OK (Task 2). Gate 3's `test result: ok` with zero `FAILED` lines is the end-to-end proof the golden-flake fix holds under the full parallel serde config.
</verification>

<success_criteria>
- REL-01: version = 0.43.0; both changelogs carry a [0.43.0] entry (root cause + cfg-guard fix + robustness audit + CI guardrail + supersession + deferred-operator note); ROADMAP-TO-1.0 Quality item [x] with corrected text citing Phases 90-92; README on 0.43.
- REL-02: all 6 release gates green.
- No src/algorithm change; no tag/publish performed (operator step documented as the sole remaining action).
</success_criteria>

<artifacts>
## Artifacts this phase produces
- `fdars-core/Cargo.toml` — version bumped to 0.43.0.
- `CHANGELOG.md` (root) — `## [0.43.0]` entry.
- `fdars-core/CHANGELOG.md` (crate) — `## [0.43.0]` entry.
- `documentation/ROADMAP-TO-1.0.md` — Quality item cleared + corrected root-cause text + v0.43.0 update note.
- `README.md` — dependency snippet on 0.43.
- `.planning/phases/93-release-preparation-readiness-verification/93-01-SUMMARY.md` — the 6-gate result record + the documented remaining operator tag/publish step.
</artifacts>

<output>
Create `.planning/phases/93-release-preparation-readiness-verification/93-01-SUMMARY.md` when done.
</output>