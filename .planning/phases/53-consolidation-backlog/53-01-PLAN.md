---
phase: 53-consolidation-backlog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/GAP-AUDIT-REPORT.md
autonomous: true
requirements: [RPT-01]
must_haves:
  truths:
    - "GAP-AUDIT-REPORT.md exists and does NOT overwrite AUDIT-REPORT.md/R-AUDIT-REPORT.md."
    - "Contains a Methodology section (four ecosystems, capability-first, de-dup rule, already-considered rigor)."
    - "Contains per-ecosystem findings sections summarizing all four Phase 52 surveys with version pins."
    - "Contains a cross-ecosystem convergence analysis (which gaps recur across >=2 ecosystems)."
    - "Contains a reverse-parity strengths sweep (where fdars leads these ecosystems)."
  prohibitions:
    - "No fdars-core/src/ file is modified (audit-only fence)."
  artifacts:
    - .planning/research/GAP-AUDIT-REPORT.md
---

# Plan 53-01 — Consolidated Gap Report (RPT-01)

## Objective
Merge the four Phase 52 survey files into `.planning/research/GAP-AUDIT-REPORT.md`: methodology, per-ecosystem findings, cross-ecosystem convergence, reverse-parity strengths.

## Tasks
1. **Read inputs** — `read_first`: `survey-matlab.md`, `survey-julia.md`, `survey-tidyfun.md`, `survey-pyx.md`, `53-CONTEXT.md`, and the prior `AUDIT-REPORT.md`/`R-AUDIT-REPORT.md` for shape.
2. **Write report** — methodology, four per-ecosystem finding sections (with `pkg@version` pins and net-new counts), a cross-ecosystem convergence table, and a consolidated reverse-parity strengths section.
   - `verify`: `test -f .planning/research/GAP-AUDIT-REPORT.md` (fails_when: file absent); `grep -q Methodology`, `grep -qi convergence`, `grep -qi reverse-parity` (fails_when: non-zero exit).
3. **Fence check** — `git status --porcelain fdars-core/src/` empty (fails_when: any output → FENCE_VIOLATION).

## Artifacts this phase produces
`.planning/research/GAP-AUDIT-REPORT.md`
