---
phase: 53-consolidation-backlog
plan: 02
type: execute
wave: 2
depends_on: [53-01]
files_modified:
  - .planning/research/GAP-BACKLOG.md
autonomous: true
requirements: [RPT-02]
must_haves:
  truths:
    - "GAP-BACKLOG.md exists and does NOT overwrite BACKLOG.md/R-BACKLOG.md."
    - "Contains a Ranking Methodology section using value/sqrt(effort) with value 1-5 and effort S/M/L (S=1,M=2,L=3)."
    - "Every net-new gap from GAP-AUDIT-REPORT.md is a ranked, promotion-ready item block (candidate req/phase id, value, effort, score, reference baseline pkg@ver, rationale, source ecosystem)."
    - "Items are sorted strictly descending by score."
    - "Cross-ecosystem convergent gaps receive a documented score/priority boost."
  prohibitions:
    - "No fdars-core/src/ file is modified (audit-only fence)."
  artifacts:
    - .planning/research/GAP-BACKLOG.md
---

# Plan 53-02 — Ranked GSD-Ready Backlog (RPT-02)

## Objective
Produce `.planning/research/GAP-BACKLOG.md`: every solid net-new gap scored `value/√effort`, sorted, expressed as a promotion-ready item block.

## Tasks
1. **Read inputs** — `read_first`: `GAP-AUDIT-REPORT.md`, the four survey files, `53-CONTEXT.md`, prior `BACKLOG.md`/`R-BACKLOG.md` for scoring/section shape.
2. **Score & rank** — assign value (1–5) and effort (S/M/L) per gap; compute `value/√effort`; sort descending; write promotion-ready blocks. Apply the convergence boost.
   - `verify`: `test -f .planning/research/GAP-BACKLOG.md`; `grep -qi "value/√effort\|value/sqrt"`; ranked order present (fails_when: non-zero exit / not sorted).
3. **Fence check** — `git status --porcelain fdars-core/src/` empty.

## Artifacts this phase produces
`.planning/research/GAP-BACKLOG.md`
