---
phase: 53-consolidation-backlog
plan: 03
type: execute
wave: 3
depends_on: [53-02]
files_modified:
  - .planning/research/GAP-AUDIT-REPORT.md
autonomous: true
requirements: [RPT-03]
must_haves:
  truths:
    - "A de-dup + completeness gate section is appended to GAP-AUDIT-REPORT.md (or written as a gate block) that runs LAST."
    - "Independent re-verification: every GAP-BACKLOG.md row re-asserted absent from fdars-core/src/ AND absent from BACKLOG.md and R-BACKLOG.md."
    - "Every surveyed capability gap from Phase 52 is either ranked in GAP-BACKLOG.md or explicitly recorded out-of-scope with reasoning (flagged GPU + SAX/symbolic candidates recorded out-of-scope)."
    - "Gate emits an explicit PASS/FAIL verdict with assertions."
  prohibitions:
    - "No fdars-core/src/ file is modified (audit-only fence)."
  artifacts:
    - .planning/research/GAP-AUDIT-REPORT.md
---

# Plan 53-03 — De-dup & Completeness Gate (RPT-03, LAST)

## Objective
Append a completeness gate to `GAP-AUDIT-REPORT.md`: independently re-verify de-dup for every backlog row, confirm every surveyed gap is accounted for (ranked or explicitly out-of-scope), and emit a PASS/FAIL verdict.

## Tasks
1. **Read inputs** — `read_first`: `GAP-BACKLOG.md`, `GAP-AUDIT-REPORT.md`, the four surveys, `BACKLOG.md`, `R-BACKLOG.md`, PROJECT.md Validated list.
2. **Re-verify & account** — for each backlog row, independently grep `fdars-core/src/` + both backlogs to confirm net-new; build a gap-to-item coverage map covering all 9 surveyed candidates (7 ranked + 2 out-of-scope); write the gate verdict.
   - `verify`: `grep -qi "completeness gate\|PASS"` in `GAP-AUDIT-REPORT.md`; coverage map present (fails_when: non-zero exit).
3. **Fence check** — `git status --porcelain fdars-core/src/` empty.

## Artifacts this phase produces
Completeness-gate section within `.planning/research/GAP-AUDIT-REPORT.md`
