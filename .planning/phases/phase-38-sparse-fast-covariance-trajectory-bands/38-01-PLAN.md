---
phase: 38-sparse-fast-covariance-trajectory-bands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/irreg_fdata/face.rs
  - fdars-core/src/irreg_fdata/mod.rs
  - fdars-core/src/fpca_variants.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [SPARSE-01, SPARSE-01-01]
estimate:
  tokens: 62000
  raw_tokens: 34000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "face_covariance(ifd, grid, bandwidth) returns a symmetric PSD FdMatrix covariance surface for sparse/irregular data (SPARSE-01-01)"
    - "On dense-limit synthetic curves face_covariance recovers a known covariance surface within a documented tolerance (SPARSE-01-01)"
    - "Invalid inputs (empty sample, grid < 2 points, non-monotone grid, non-finite/non-positive bandwidth) return FdarError rather than panicking (SPARSE-01-01)"
    - "New symbol resolves at crate root: fdars_core::face_covariance"
    - "Existing public signatures (cov_irreg, pace_fpca) unchanged; full suite + clippy --all-targets green"
  artifacts:
    - "fdars-core/src/irreg_fdata/face.rs (new file with face_covariance + inline tests)"
    - "pub mod face; + pub use face::face_covariance in irreg_fdata/mod.rs"
    - "pub use irreg_fdata::face_covariance in lib.rs"
    - "gaussian_smooth_cov bumped to pub(crate) in fpca_variants.rs"
  key_links:
    - "face_covariance -> cov_irreg (raw kernel-smoothed covariance) -> gaussian_smooth_cov (S·Cov·Sᵀ sandwich) -> symmetric_eigen PSD projection -> reconstructed FdMatrix"
    - "irreg_fdata/mod.rs pub use -> lib.rs crate-root pub use (re-export chain resolves)"
---

<objective>
Create the new `fdars-core/src/irreg_fdata/face.rs` module and land `face_covariance` (SPARSE-01-01) end-to-end as the phase tracer: a FACE fast-sandwich covariance surface for sparse/irregular functional data, wired from raw covariance through PSD projection and re-exported to the crate root, proving the module compiles, the sandwich+PSD path works, and re-exports resolve.

Purpose: `face_covariance` is the foundational estimator the rest of the phase builds on — `mface_covariance` (Plan 02) calls it for diagonal blocks. Landing it as a fully-verified vertical slice de-risks the sandwich construction, the PSD-clip reconstruction, and the whole module-wiring/re-export chain before expansion.

Output: new `face.rs` (with `face_covariance` + inline `#[cfg(test)]` tests), the `pub(crate)` visibility bump to `gaussian_smooth_cov`, and the module + crate-root re-export lines.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-38-sparse-fast-covariance-trajectory-bands/38-CONTEXT.md
@.planning/phases/phase-38-sparse-fast-covariance-trajectory-bands/38-RESEARCH.md
@.planning/phases/phase-38-sparse-fast-covariance-trajectory-bands/38-VALIDATION.md

# Source files to read before editing (do NOT re-read; extract everything in one pass)
@fdars-core/src/irreg_fdata/smoothing.rs
@fdars-core/src/irreg_fdata/mod.rs
@fdars-core/src/irreg_fdata/kernels.rs
@fdars-core/src/fpca_variants.rs
@fdars-core/src/helpers.rs
@fdars-core/src/lib.rs
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01 subset)

NEW symbols and files landed by this plan:
- `fdars-core/src/irreg_fdata/face.rs` — NEW file
- `face_covariance(ifd: &IrregFdata, grid: &[f64], bandwidth: f64) -> Result<FdMatrix, FdarError>` — NEW public fn
- `pub mod face;` + `pub use face::face_covariance;` in `irreg_fdata/mod.rs`
- `pub use irreg_fdata::face_covariance;` (crate-root re-export block) in `lib.rs`
- visibility change: `gaussian_smooth_cov` in `fpca_variants.rs` from `fn` to `pub(crate) fn` (NOT a public-API change — intra-crate reuse for DRY, RESEARCH Finding 1)

Deferred to Plan 02: `mface_covariance`, `MfaceCovResult`, `face_trajectory`, and their re-export lines.
</artifacts_produced>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Create face.rs + wire module + implement face_covariance end-to-end</name>
  <files>fdars-core/src/irreg_fdata/face.rs, fdars-core/src/irreg_fdata/mod.rs, fdars-core/src/fpca_variants.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/irreg_fdata/smoothing.rs — `cov_irreg(ifd, s_grid, t_grid, bandwidth) -> FdMatrix` (NOT Result; uses `.expect()` internally so empty/short inputs would panic — validate BEFORE calling); `accumulate_cov_at_point` double-loop pattern; column-major write `cov[si + ti * ns]`.
    - fdars-core/src/fpca_variants.rs — `gaussian_smooth_cov(cov: &FdMatrix, argvals: &[f64], bandwidth: f64) -> FdMatrix` (separable row-then-column Gaussian smoother S·Cov·Sᵀ; currently private `fn`); the `ssvd` weighted-eigendecompose+PSD pattern (`simpsons_weights` → `sqrt_w` → `c_scaled[row + col*m] = sqrt_w[row]*cov*sqrt_w[col]` → `DMatrix::from_column_slice(m,m,&c_scaled).symmetric_eigen()` → sort desc → filter `lam > 0.0`). Note `gaussian_smooth_cov` uses the LOCAL `gaussian_kernel(dist, bw)` helper, not irreg_fdata's `kernel_gaussian(u)`.
    - fdars-core/src/irreg_fdata/mod.rs — module list (`pub mod kernels; pub mod smoothing;`) and `pub use` block; `IrregFdata` fields/methods (`n_obs`, `get_obs`, `argvals`, `offsets`).
    - fdars-core/src/helpers.rs — `pub fn simpsons_weights(argvals: &[f64]) -> Vec<f64>`.
    - fdars-core/src/matrix.rs — `FdMatrix::from_column_major(data, nrows, ncols) -> Result<..>`, `FdMatrix::zeros`, `Index` via `m[(row, col)]`.
    - fdars-core/src/error.rs — `FdarError::{InvalidDimension { parameter, expected, actual }, InvalidParameter { parameter, message }, ComputationFailed { operation, detail }}`.
    - fdars-core/src/lib.rs — line 266 area: existing `pub use pace_fpca::{pace_fpca, PaceFpcaConfig, PaceFpcaResult};` re-export block; line 230 `pub use irreg_fdata::{IrregFdata, KernelType};`.
  </read_first>
  <behavior>
    Write these inline `#[cfg(test)]` tests FIRST (RED), then implement until GREEN. Test names must match VALIDATION.md exactly:
    - `test_face_covariance_shape`: build a small sparse `IrregFdata` (~10 curves, ~5 points each on [0,1]) via `IrregFdata::from_lists`, call `face_covariance(&ifd, &grid, bw)` with an m-point grid (m≈11); assert `Ok`, result is `m×m`, symmetric (`|C[(i,j)] - C[(j,i)]| < 1e-9` for all i,j), and PSD (all eigenvalues of the returned matrix `>= -1e-9` via a `symmetric_eigen` check in the test).
    - `test_face_covariance_dense_limit`: build n≈30 curves ALL densely observed at the SAME m=51 uniform grid points on [0,1], drawn from a process with known covariance `C(s,t) = exp(-|s-t|)` (OU kernel — sample scores against a truncated eigenbasis or generate from the Cholesky of the known 51×51 kernel, seeded `StdRng::seed_from_u64`). Call `face_covariance(&ifd, &grid, 0.05)`; assert `max_{s,t} |C_hat[(si,ti)] - exp(-|s-t|)| < TOL`. Calibrate `TOL` during implementation (RESEARCH A2 suggests ~0.3; document the final constant in a rustdoc/comment and pick the smallest passing value).
    - `test_face_covariance_errors`: assert each returns `Err(FdarError::...)` (never panics): empty `IrregFdata` (0 curves) → `InvalidDimension`; grid of length 0 and length 1 → `InvalidDimension`; non-monotone grid (e.g. `[0.0, 0.5, 0.4]`) → `InvalidParameter`; bandwidth `0.0`, `-1.0`, `f64::NAN`, `f64::INFINITY` → `InvalidParameter`.
  </behavior>
  <action>
    Bump `gaussian_smooth_cov` in `fpca_variants.rs` from `fn gaussian_smooth_cov` to `pub(crate) fn gaussian_smooth_cov` (single-token visibility widening; do NOT touch its body or signature — per D reuse decision / RESEARCH Finding 1). This is intra-crate, not a public-API change.

    Create `fdars-core/src/irreg_fdata/face.rs` with a module doc comment (`//!`) describing the FACE kernel-sandwich estimator and documenting the divergence from `refund::face`: fdars uses a kernel-smoother sandwich (K-FACE) rather than penalized tensor-product splines (P-FACE), matched by capability not exact internals. Implement `face_covariance(ifd: &IrregFdata, grid: &[f64], bandwidth: f64) -> Result<FdMatrix, FdarError>`, `#[must_use]`, with rustdoc `# Errors` documenting the error paths. Steps in order:
    (1) VALIDATE before any compute (mirrors pace_fpca validation; `cov_irreg` does not validate and would panic via `.expect()`): `ifd.n_obs() == 0` → `InvalidDimension { parameter: "ifd", ... }`; `grid.len() < 2` → `InvalidDimension { parameter: "grid", ... }`; `!bandwidth.is_finite() || bandwidth <= 0.0` → `InvalidParameter { parameter: "bandwidth", ... }`; grid not strictly increasing (`grid.windows(2)` any `w[0] >= w[1]`) → `InvalidParameter { parameter: "grid", ... }`.
    (2) `let raw_cov = cov_irreg(ifd, grid, grid, bandwidth);` (reuse — do NOT re-derive the kernel accumulation).
    (3) `let smooth_cov = gaussian_smooth_cov(&raw_cov, grid, bandwidth);` (reuse the now-`pub(crate)` sandwich — same bandwidth for both passes per RESEARCH Pitfall 5; document this in rustdoc).
    (4) PSD projection mirroring the `ssvd`/`eigendecompose_cov` pattern: `let w = simpsons_weights(grid); let sqrt_w = w.iter().map(f64::sqrt)...`; build `c_scaled[row + col*m] = sqrt_w[row] * smooth_cov[(row,col)] * sqrt_w[col]`; `DMatrix::from_column_slice(m, m, &c_scaled).symmetric_eigen()`; collect `(eigenvalue, index)` pairs, sort descending, retain `lam > 0.0` (exact threshold per existing pattern; document that this clips estimation-noise negatives to zero).
    (5) RECONSTRUCT the PSD surface: `cov_data[i + j*m] += lam * phi[i] * phi[j]` where `phi[j] = eigenvectors[(j, idx)] / sqrt_w[j]` (guard `sqrt_w[j] > 1e-15`, else use raw component). Return `FdMatrix::from_column_major(cov_data, m, m).map_err(|e| FdarError::ComputationFailed { operation: "face_covariance", detail: e.to_string() })`.
    Add the inline `#[cfg(test)] mod tests { ... }` from `<behavior>`. Use seeded `StdRng::seed_from_u64` for any synthetic-data randomness.

    Wire the module: in `irreg_fdata/mod.rs` add `pub mod face;` alongside `pub mod kernels; pub mod smoothing;`, and add `pub use face::face_covariance;` to the re-export block. In `lib.rs` add a crate-root re-export near the existing `pub use pace_fpca::{...}` (line ~266): `pub use irreg_fdata::face_covariance;` under a `// Re-export FACE sparse covariance (Phase 38)` comment.

    Run `cargo fmt -p fdars-core` before committing. Commit the RED test slice and GREEN implementation as atomic commits (`test(38-01): ...`, `feat(38-01): ...`); code commits must pass fmt + clippy + tests — use `TMPDIR=/home/simonm/.cache/fdars-bench-tmp` and `--no-verify` only if the /tmp-exhaustion pre-commit hook blocks, after running the gates manually.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel irreg_fdata::face -- --test-threads=4</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p fdars-core --features linalg,parallel irreg_fdata::face::tests::test_face_covariance_shape` passes.
    - `cargo test -p fdars-core --features linalg,parallel irreg_fdata::face::tests::test_face_covariance_dense_limit` passes with a documented tolerance constant.
    - `cargo test -p fdars-core --features linalg,parallel irreg_fdata::face::tests::test_face_covariance_errors` passes (all invalid inputs return Err, none panic).
    - `grep -n 'pub(crate) fn gaussian_smooth_cov' fdars-core/src/fpca_variants.rs` matches exactly one line.
    - `grep -n 'pub mod face' fdars-core/src/irreg_fdata/mod.rs` and `grep -n 'pub use face::face_covariance' fdars-core/src/irreg_fdata/mod.rs` each match.
    - `grep -n 'pub use irreg_fdata::face_covariance' fdars-core/src/lib.rs` matches.
  </acceptance_criteria>
  <done>face.rs exists; face_covariance validates inputs, sandwiches cov_irreg through gaussian_smooth_cov, PSD-projects via symmetric_eigen, and returns a symmetric PSD m×m FdMatrix; dense-limit test recovers exp(-|s-t|) within tolerance; error tests pass; symbol resolves at crate root.</done>
</task>

<task type="auto">
  <name>Task 2: Full-gate green — fmt, clippy --all-targets, whole-crate suite</name>
  <files>fdars-core/src/irreg_fdata/face.rs</files>
  <read_first>
    - MEMORY.md pointer: CI lints test/bench code — a plain `-p ... -D warnings` false-greens; must use `--all-targets`. `--no-verify` commits skip fmt → run `cargo fmt` explicitly.
  </read_first>
  <action>
    Run `cargo fmt -p fdars-core` and confirm no drift. Run the full clippy gate `cargo clippy --all-targets --features linalg,parallel -- -D warnings` and fix any warnings in `face.rs` (including test-code warnings — unused vars, needless clones, etc.). Run the whole-crate test suite `cargo test -p fdars-core --features linalg,parallel` to confirm the new module did not break existing tests and existing public signatures (`cov_irreg`, `pace_fpca`) are unchanged (Success Criterion 5). Use `TMPDIR=/home/simonm/.cache/fdars-bench-tmp` for all commands. If pre-existing target/ bloat causes example LINK failures, `rm -rf target/debug/{incremental,examples}` (MEMORY.md) — that is not a code bug.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel</automated>
  </verify>
  <acceptance_criteria>
    - `cargo clippy --all-targets --features linalg,parallel -- -D warnings` exits 0 (no warnings).
    - `cargo test -p fdars-core --features linalg,parallel` — full suite green (all pre-existing tests still pass + new face tests).
    - `cargo fmt -p fdars-core -- --check` reports no diff.
  </acceptance_criteria>
  <done>Full clippy --all-targets gate clean, whole-crate suite green, no fmt drift; additive/non-breaking confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → `face_covariance` args | Only untrusted surface: malformed `IrregFdata`, grid, and bandwidth values crossing into a pure in-process numeric fn. No network/file/process I/O, no deserialization of untrusted data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-01 | Denial of Service | `face_covariance` input validation | low | mitigate | Validate empty sample, grid length < 2, non-monotone grid, non-finite/non-positive bandwidth at entry and return `FdarError` before calling `cov_irreg` (which panics via `.expect()` on bad dims). No panics on malformed input. |
| T-38-02 | Tampering | PSD reconstruction numerics | low | mitigate | Guard `sqrt_w[j] > 1e-15` before dividing; clip negative eigenvalues to 0; symmetric-by-construction sandwich. No `unsafe`; NaN cannot enter via validated finite bandwidth. |
| T-38-SC | Tampering | npm/pip/cargo installs | low | accept | No new crate dependency added (locked milestone constraint); nothing to install. |
</threat_model>

<verification>
- `face_covariance` returns a symmetric PSD covariance surface (shape test).
- Dense-limit synthetic recovery within documented tolerance (dense-limit test).
- All invalid inputs return `FdarError`, none panic (error test).
- Crate-root re-export resolves; existing signatures unchanged; full clippy --all-targets + whole-crate suite green.
</verification>

<success_criteria>
SPARSE-01-01 satisfied: `face_covariance` is a `Result`-returning, crate-root-re-exported entry point in `irreg_fdata/` that reuses `cov_irreg` + `gaussian_smooth_cov`, recovers a known covariance on dense-limit data within tolerance, and validates all invalid inputs. Additive/non-breaking (ROADMAP SC5): existing public signatures unchanged, full suite + `cargo clippy --all-targets --features linalg,parallel -- -D warnings` green.
</success_criteria>

<output>
Create `.planning/phases/phase-38-sparse-fast-covariance-trajectory-bands/38-01-SUMMARY.md` when done.
</output>
