---
phase: 16-r-ecosystem-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/R-AUDIT-REPORT.md
autonomous: true
requirements: [INV-01]
estimate:
  tokens: 55000
  raw_tokens: 30000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "R-AUDIT-REPORT.md exists in .planning/research/ (distinct file; the scikit-fda AUDIT-REPORT.md is untouched)"
    - "The report opens with a methodology preamble stating the knowledge+CRAN sourcing convention and the survey month (2026-08), so version citations are interpreted correctly downstream"
    - "§R-Inventory lists every enumerated capability capability-first (fit/predict/transform collapsed to one row), each row tagged with its source package(s) AND package version, across all 35 surveyed packages (per INV-01 core set + surfaced additions)"
    - "Capabilities are grouped into 9 named areas, each area carrying a per-area capability count (in-scope + out-of-scope) — satisfies ROADMAP SC1 + SC2"
  artifacts:
    - ".planning/research/R-AUDIT-REPORT.md"
  key_links:
    - "§R-Inventory row schema + area taxonomy is reused verbatim by 16-02's design-goal-filter count table and by Phase 17's parity matrix"
---

<objective>
Create the new file `.planning/research/R-AUDIT-REPORT.md` and author its Phase-16 header, a methodology preamble, and the full `§R-Inventory` section: a versioned, area-organized, capability-first inventory of the R FDA ecosystem lifted from the completed survey in `16-RESEARCH.md`. Covers INV-01.

Purpose: Produce the R side of the parity comparison — the versioned capability inventory Phase 17 will map fdars against. This mirrors the v0.14.0 §Phase 7 scikit-fda enumeration.
Output: `.planning/research/R-AUDIT-REPORT.md` with `## Phase 16 — R Ecosystem Inventory`, a methodology preamble, a Packages-Surveyed table, and the 9-area capability tables (each with a per-area count).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The completed survey — the executor lifts its tables mechanically. This IS the source content.
@.planning/phases/16-r-ecosystem-inventory/16-RESEARCH.md

# Resolved decisions — AUTHORITATIVE (data-source convention, area taxonomy, in/out rule, file-naming)
@.planning/phases/16-r-ecosystem-inventory/16-CONTEXT.md

# STRUCTURAL TEMPLATE ONLY — §Phase 7 (lines ~1125-1620) is the format to mirror for §R-Inventory.
# REFERENCE ONLY: do NOT overwrite or edit this file. R-AUDIT-REPORT.md is a NEW, separate file.
@.planning/research/AUDIT-REPORT.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Create R-AUDIT-REPORT.md with header, methodology preamble, and Area 1 as the proven end-to-end slice</name>
  <files>.planning/research/R-AUDIT-REPORT.md</files>
  <action>
    Create the NEW file `.planning/research/R-AUDIT-REPORT.md` (never overwrite the existing scikit-fda `AUDIT-REPORT.md` or `BACKLOG.md` — this is a distinct filename per the CONTEXT.md file-naming decision). Write, in order:

    (1) A top-level title `# fdars R-Ecosystem Gap Audit Report` and a one-paragraph note that later phases (17-19) append their own sections to this same file.

    (2) The section anchor `## Phase 16 — R Ecosystem Inventory` with a lead paragraph stating this section is the R side of the parity comparison and that deliverables are documentation only — zero `fdars-core/src/` edits.

    (3) A `### Methodology` preamble that states, once and explicitly (source these facts from 16-RESEARCH.md §Methodology and 16-CONTEXT.md "Data Source and Versioning"): the sourcing convention (model knowledge cross-checked against CRAN / pkgdown documentation; NO local R install, NO packageVersion runtime calls); the version-citation convention (each package's latest CRAN release as of the survey month, stated as "as of 2026-08"; uncertain versions flagged with a leading tilde or "latest CRAN, unverified"); and the capability-first collapse rule (one row per capability; fit/predict/transform and S3/S4 method variants collapsed; multi-package capabilities co-list all packages). Also define the capability-row schema table (columns: Capability, Task Group, Source Package(s) as pkg with version, Relevance, Confidence) — mirror the §Phase 7 "Capability-Row Schema" shape from the template.

    (4) The `### Packages Surveyed` table lifted verbatim from 16-RESEARCH.md "Packages Surveyed" (all 35 active packages with Version / CRAN Date / Primary Area / Status), immediately followed by the "Candidate packages considered and excluded" list (classiFunc/FRegSigCom/fpca ARCHIVED; rainbow in-but-out-of-scope; refund.shiny/mlr3fda/tidyfun excluded) — source it from 16-RESEARCH.md "Packages Considered and Excluded" so SC1's "any further packages surfaced during research" is auditable.

    (5) Open the `## §R-Inventory` heading and author ONLY Area 1 (Representation / Basis Systems / Smoothing) as the tracer slice: its description, its per-area count line reading exactly "In-scope count: 38   Out-of-scope count: 7", and its full capability table lifted verbatim from 16-RESEARCH.md "Area 1". This proves the exact end-to-end row format (versioned package tags, collapsed capability rows, per-area count) before the remaining 8 areas are expanded in Task 2.

    Reconcile stale prose while lifting: 16-RESEARCH.md's early Summary paragraph says "210+ capabilities" and "181 in-scope" — these are superseded drafts. The AUTHORITATIVE totals are the final §Design-Goal Filter count table: 275 total rows, 248 in-scope, 27 out-of-scope, 9 areas. Do NOT copy the stale 210/181 figures into the report; use the per-area count lines exactly as they appear in each area's header in 16-RESEARCH.md.
  </action>
  <verify>
    <automated>test -f .planning/research/R-AUDIT-REPORT.md && grep -q "## Phase 16 — R Ecosystem Inventory" .planning/research/R-AUDIT-REPORT.md && grep -q "as of 2026-08" .planning/research/R-AUDIT-REPORT.md && grep -q "## §R-Inventory" .planning/research/R-AUDIT-REPORT.md && grep -q "Area 1" .planning/research/R-AUDIT-REPORT.md && grep -Eq "In-scope count: 38" .planning/research/R-AUDIT-REPORT.md</automated>
  </verify>
  <done>R-AUDIT-REPORT.md exists with the Phase-16 header, a methodology preamble citing the 2026-08 survey-month and no-runtime convention, the 35-package survey table plus exclusions list, and Area 1's full versioned capability-first table with its per-area count. The scikit-fda AUDIT-REPORT.md is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Expand §R-Inventory to all 9 areas (Areas 2-9) with per-area counts</name>
  <files>.planning/research/R-AUDIT-REPORT.md</files>
  <action>
    Append Areas 2 through 9 to the `## §R-Inventory` section, each lifted verbatim from the corresponding area in 16-RESEARCH.md: Area 2 (Preprocessing / Registration, in 22 / out 0), Area 3 (Exploratory / Depth / Outlier, in 31 / out 7), Area 4 (ML — Regression + Classification + Clustering, in 59 / out 2; keep its three sub-headings Regression / Classification / Clustering), Area 5 (Inference / Testing, in 22 / out 3), Area 6 (Functional Time Series, in 24 / out 2), Area 7 (Density / Object Data / Manifold, in 24 / out 1), Area 8 (SPM / Control Charts, in 10 / out 2), Area 9 (FPCA — Sparse / Longitudinal / Specialized, in 18 / out 3).

    For each area preserve: the area description paragraph, the per-area count header line reading "In-scope count: N   Out-of-scope count: M", and the complete capability table (Capability / Task Group / Source Package(s) with version / Relevance / Confidence). Every row's Source Package(s) cell MUST carry a versioned package tag (pkg with vX.Y.Z) — this is the INV-01 "each capability tagged with source package AND version" requirement.

    Preserve the capability-first collapse (do NOT split any collapsed row back into fit/predict/transform variants) and the Relevance tags exactly as surveyed (16-02 depends on these tags being correct for its count table). Do NOT add fdars parity verdicts, gap categories, or fdars-strengths notes — those are Phase 17/18 material and are explicitly deferred (16-CONTEXT.md Deferred Ideas).

    After all 9 areas, add a short subsection carrying over 16-RESEARCH.md's "Architectural Responsibility Map" and "Common Pitfalls for Phase 17 Parity Mapping" (verbatim), since Phase 17 consumes both directly.
  </action>
  <verify>
    <automated>python3 - <<'PY'
import re
t=open('.planning/research/R-AUDIT-REPORT.md').read()
counts=re.findall(r'In-scope count:\s*(\d+)\s+Out-of-scope count:\s*(\d+)', t)
areas=[(38,7),(22,0),(31,7),(59,2),(22,3),(24,2),(24,1),(10,2),(18,3)]
assert len(counts)>=9, f"expected >=9 area count lines, got {len(counts)}"
got=[(int(a),int(b)) for a,b in counts[:9]]
assert got==areas, f"per-area counts mismatch: {got}"
ins=sum(a for a,_ in areas); outs=sum(b for _,b in areas)
assert (ins,outs)==(248,27), f"totals {ins}/{outs} != 248/27"
for pkg in ["fda (6.3.0)","fdasrvf (2.4.4)","refund (0.1-40)","frechet (0.3.0)","funcharts (1.8.1)"]:
    assert pkg in t, f"missing versioned pkg tag: {pkg}"
assert "Architectural Responsibility Map" in t
print("OK: 9 areas, per-area counts sum to 248 in / 27 out, versioned package tags present")
PY</automated>
  </verify>
  <done>All 9 areas present in §R-Inventory with matching per-area count headers summing to 248 in-scope / 27 out-of-scope; every capability row carries a versioned package source tag; no fdars parity/gap/strengths content leaked in; Architectural Responsibility Map plus Pitfalls carried over for Phase 17.</done>
</task>

</tasks>

<verification>
- `R-AUDIT-REPORT.md` exists and is a NEW file — `git status --short .planning/research/` shows it added, and `AUDIT-REPORT.md` / `BACKLOG.md` are NOT modified.
- Methodology preamble states the 2026-08 survey month, the no-local-R / no-packageVersion sourcing convention, and the capability-first collapse rule — once, up front.
- §R-Inventory contains 9 named areas, each with a per-area count line; the 9 per-area in-scope counts sum to 248 and out-of-scope to 27 (matching the survey).
- Every capability row is capability-first (fit/predict/transform collapsed) and carries source package(s) with version — INV-01 satisfied. (ROADMAP SC1 + SC2.)
</verification>

<success_criteria>
- INV-01 fully addressed: versioned, area-organized, capability-first inventory across the core R FDA ecosystem, each row source-package-and-version tagged.
- ROADMAP Phase-16 SC1 (capability-first versioned package-tagged inventory across the core ecosystem) and SC2 (named areas with per-area capability counts) are TRUE in the written report.
- Zero `fdars-core/src/` edits; the scikit-fda AUDIT-REPORT.md is untouched.
</success_criteria>

<output>
Create `.planning/phases/16-r-ecosystem-inventory/16-01-SUMMARY.md` when done.
</output>
