---
phase: 83-naming-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/gmm/subspace.rs
  - fdars-core/src/gmm/mod.rs
  - fdars-core/src/gmm/em.rs
  - fdars-core/src/gmm/cluster.rs
  - fdars-core/src/function_on_scalar_2d.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [API-04]
estimate:
  tokens: 55000
  raw_tokens: 28000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "AUD-14: public fn is named fun_hddc_cluster; funhddC_cluster no longer exists as a callable API symbol"
    - "AUD-15: public type is named Fosr2dResult; FosrResult2d no longer exists as a type"
    - "AUD-16: public type is named GmmFitResult; GmmResult no longer exists as a bare type name"
    - "The crate compiles and the full test suite passes with no numeric/behavioral change (pure rename)"
  artifacts:
    - fdars-core/src/gmm/subspace.rs
    - fdars-core/src/gmm/mod.rs
    - fdars-core/src/function_on_scalar_2d.rs
    - fdars-core/src/lib.rs
  key_links:
    - "crate-root re-exports in lib.rs (lines 396, 478-479) point at the new names"
    - "gmm/mod.rs barrel re-export (line 81) points at fun_hddc_cluster"
    - "doctest at subspace.rs:546 and module doc at subspace.rs:21 use the new name (compile-checked by cargo test)"
---

<objective>
Apply the three simple hard renames from the approved Phase 81 audit (per D: AUD-14, AUD-15, AUD-16) — no dispatcher redesign, just symbol renames with full call-site / re-export / doctest migration. Hard renames, no deprecated shims (still 0.x).

- AUD-14: fn `funhddC_cluster` → `fun_hddc_cluster` (fix camelCase-in-snake_case).
- AUD-15: type `FosrResult2d` → `Fosr2dResult` (adjective-before-noun house style).
- AUD-16: type `GmmResult` → `GmmFitResult` (disambiguate the single-K fit result from `GmmClusterResult`).

Purpose: These are the lowest-risk items in the highest-risk phase; landing them first shrinks the blast radius before the two grid-enum collapses (83-02, 83-03).
Output: Three renamed public symbols, all in-crate callers/tests/doctests migrated, whole-crate gates green. No new public types.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-naming-unification/83-CONTEXT.md

# Build/CI hazards (from project MEMORY) — read before running gates:
# - Pre-commit hook runs the full test suite and times out at ~30s. Run `cargo fmt` then
#   `git commit --no-verify`, and run the real gates out-of-band (this plan's <verify> blocks).
# - On spurious "No space left on device" / linker "linking with cc failed" during examples:
#   `rm -rf target/debug/{incremental,examples}` to free /home, then re-run.
# - `cargo test` may surface a PRE-EXISTING co_cluster/svd_sign golden flake under full parallel
#   runs. It is NOT a rename regression. Confirm by re-running the specific test binary in isolation.
# - Run all cargo commands from the workspace root: /home/simonm/projects/rust/fdars
</context>

<tasks>

<task type="tracer">
  <name>Task 1: AUD-14 — rename funhddC_cluster → fun_hddc_cluster (end-to-end)</name>
  <files>fdars-core/src/gmm/subspace.rs, fdars-core/src/gmm/mod.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/gmm/subspace.rs:21 (module doc bullet), :546 (doctest `use` line), :554 (def), :668 (error `operation:` string), :759+ (test fns / test callers)
    - fdars-core/src/gmm/mod.rs:81 (barrel `pub use subspace::{...}`)
    - fdars-core/src/lib.rs:478 (crate-root `pub use` line)
  </read_first>
  <action>
    Rename the public function `funhddC_cluster` to `fun_hddc_cluster` (per AUD-14). This is the tracer: it walks every layer a rename in this crate touches (definition → barrel re-export → crate-root re-export → doctest → module doc → in-crate test callers), proving the migration pattern end-to-end before the type renames in Tasks 2-3.
    Edit these reference kinds:
    1. Definition at subspace.rs:554 (`pub fn funhddC_cluster`).
    2. Module-doc intra-doc link at subspace.rs:21 (the ``[`funhddC_cluster`]`` bullet) — a stale intra-doc link fails `cargo doc`/doctest.
    3. Doctest `use` line at subspace.rs:546.
    4. In-crate test callers in the `#[cfg(test)]` block (the `funhddC_cluster(&data, ...)` call sites around :770-878).
    5. Barrel re-export in gmm/mod.rs:81.
    6. Crate-root re-export in lib.rs:478.
    Leave the `operation:` error-string literal at subspace.rs:668 and the internal `#[test] fn test_funhddC_*` names AS-IS — they are not API symbols and renaming them is out of scope; the grep gate below is written to ignore them.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && grep -rnE '\bfunhddC_cluster\s*[(<]|::funhddC_cluster\b|pub use[^;]*\bfunhddC_cluster\b|\[`funhddC_cluster`\]' fdars-core/src fdars-core/examples fdars-core/tests; test $? -ne 0 && grep -qE '\bpub fn fun_hddc_cluster\b' fdars-core/src/gmm/subspace.rs && cargo build -p fdars-core --features linalg,parallel 2>&1 | { ! grep -qE 'error(\[|:)'; }</automated>
    <fails_when>any callable/type/re-export/intra-doc-link reference to funhddC_cluster remains in src/examples/tests (the grep MATCHES, so the leading `grep; test $? -ne 0` guard fails), OR `pub fn fun_hddc_cluster` is absent, OR `cargo build` emits an error line.</fails_when>
  </verify>
  <acceptance_criteria>
    - `pub fn fun_hddc_cluster(...)` exists at the old definition site.
    - No API/re-export/intra-doc reference to `funhddC_cluster` remains anywhere in src/examples/tests.
    - `cargo build -p fdars-core --features linalg,parallel` succeeds.
  </acceptance_criteria>
  <done>fun_hddc_cluster is the sole public name; funhddC_cluster is gone from the public API surface; crate builds.</done>
</task>

<task type="auto">
  <name>Task 2: AUD-15 & AUD-16 — rename FosrResult2d → Fosr2dResult and GmmResult → GmmFitResult</name>
  <files>fdars-core/src/function_on_scalar_2d.rs, fdars-core/src/gmm/mod.rs, fdars-core/src/gmm/em.rs, fdars-core/src/gmm/cluster.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/function_on_scalar_2d.rs:69 (struct def), :95 (impl block), :474 & :599 (constructor / return), :617 & :626 (doc + fn param)
    - fdars-core/src/gmm/mod.rs:39 (struct def), :71 (`pub best: GmmResult` field)
    - fdars-core/src/gmm/em.rs:7 (`use super::{... GmmResult}`), :269/:277/:304(doc)/:319 (returns/constructs)
    - fdars-core/src/gmm/cluster.rs:5 (`use super::{... GmmResult}`), :19/:20/:175/:227 (Option<GmmResult> / &GmmResult)
    - fdars-core/src/lib.rs:396 (FosrResult2d re-export), :478-479 (GmmResult re-export)
  </read_first>
  <action>
    AUD-15: Rename the public struct `FosrResult2d` to `Fosr2dResult` (matches the `<Adjective>2d`/`Grid2d`/`Deriv2DResult` house style). Update: struct def (function_on_scalar_2d.rs:69), its `impl FosrResult2d` block (:95), the `Result<FosrResult2d, ...>` return + `Ok(FosrResult2d {` constructor (:474, :599), the doc-comment mention and fn param type at :617/:626, and the crate-root re-export in lib.rs:396.
    AUD-16: Rename the public struct `GmmResult` to `GmmFitResult` (single-K fit result; disambiguates from `GmmClusterResult`). Update: struct def (gmm/mod.rs:39), the `pub best: GmmResult` field type in the K-selection result (gmm/mod.rs:71), all `use super::{... GmmResult}` imports (em.rs:7, cluster.rs:5), every `GmmResult`/`Option<GmmResult>`/`&GmmResult`/`Result<GmmResult, _>` occurrence in em.rs (:269, :277, :319) and cluster.rs (:19, :20, :175, :227), the doc-comment at em.rs:304, and the crate-root re-export in lib.rs:479. Both are whole-word type renames — do NOT touch `GmmClusterResult` (a different type).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && grep -rnE '\bFosrResult2d\b|\bGmmResult\b' fdars-core/src fdars-core/examples fdars-core/tests; test $? -ne 0 && grep -qE '\bpub struct Fosr2dResult\b' fdars-core/src/function_on_scalar_2d.rs && grep -qE '\bpub struct GmmFitResult\b' fdars-core/src/gmm/mod.rs && cargo build -p fdars-core --features linalg,parallel 2>&1 | { ! grep -qE 'error(\[|:)'; }</automated>
    <fails_when>any reference to `FosrResult2d` or bare `GmmResult` remains in src/examples/tests (grep matches → guard fails), OR either new `pub struct` is absent, OR `cargo build` emits an error line. (`\bGmmResult\b` word-boundary does NOT match `GmmClusterResult`.)</fails_when>
  </verify>
  <acceptance_criteria>
    - `pub struct Fosr2dResult` and `pub struct GmmFitResult` exist at the old definition sites.
    - No reference to `FosrResult2d` or bare `GmmResult` remains anywhere in src/examples/tests.
    - `GmmClusterResult` is untouched.
    - `cargo build -p fdars-core --features linalg,parallel` succeeds.
  </acceptance_criteria>
  <done>Both types carry the new names crate-wide, re-exports updated, crate builds, GmmClusterResult unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Whole-crate gates — prove the three renames are complete and behavior-preserving</name>
  <files>fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/lib.rs:396, :478-479 (confirm re-export lines are consistent after Tasks 1-2)
    - project MEMORY hazard notes in this plan's <context> (pre-commit timeout, /home disk pressure, golden flake)
  </read_first>
  <action>
    Run the whole-crate gates that constitute the phase-completion proof. First `cargo fmt` to normalize, then the gates below. If `cargo test` shows a co_cluster or svd_sign golden failure, re-run ONLY that test binary in isolation (e.g. `cargo test -p fdars-core --features linalg,parallel --test <binary> <test_name>` or the relevant lib test) to confirm it is the pre-existing env flake and not a rename regression; a rename regression would surface as a compile error or a gmm/fosr/funhddc test failure, never as co_cluster/svd_sign. If /home disk pressure aborts the examples build with a spurious "No space left"/link failure, run `rm -rf target/debug/{incremental,examples}` and re-run. No source edits expected here beyond a possible `cargo fmt` reformat.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && cargo fmt --check && cargo clippy --all-targets --features linalg,parallel -- -D warnings && cargo test && cargo build --features serde && cargo build --examples</automated>
    <fails_when>`cargo fmt --check` non-zero; OR clippy non-zero / emits any warning or error line; OR `cargo test` prints "test result: FAILED" for a rename-relevant binary or exits non-zero for a reason other than the confirmed-in-isolation co_cluster/svd_sign golden flake; OR `cargo build --features serde` non-zero; OR `cargo build --examples` non-zero (all 28 examples must compile).</fails_when>
  </verify>
  <acceptance_criteria>
    - fmt clean, clippy clean under --all-targets --features linalg,parallel, full test suite green (modulo the confirmed pre-existing golden flake), serde build green, all 28 examples compile.
  </acceptance_criteria>
  <done>All whole-crate gates green; the three renames are proven complete and numerically inert.</done>
</task>

</tasks>

<verification>
- No public/re-export/intra-doc reference to `funhddC_cluster`, `FosrResult2d`, or bare `GmmResult` remains in src/examples/tests.
- `fun_hddc_cluster`, `Fosr2dResult`, `GmmFitResult` are the sole names for their symbols.
- Whole-crate gates green: `cargo fmt --check`, `cargo clippy --all-targets --features linalg,parallel -- -D warnings`, `cargo test`, `cargo build --features serde`, `cargo build --examples`.
- No numeric/behavioral change (renames only; no computation body touched).
</verification>

<success_criteria>
AUD-14, AUD-15, AUD-16 applied crate-wide with all call sites, tests, and doctests migrated; whole-crate gates green; blast radius reduced ahead of the grid-enum collapses.
</success_criteria>

<output>
Create `.planning/phases/83-naming-unification/83-01-SUMMARY.md` when done.
</output>
