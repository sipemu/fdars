---
phase: 42-object-data-fr-chet-regression
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - fdars-core/src/frechet/spaces/spherical.rs
  - fdars-core/src/frechet/spaces/network.rs
  - fdars-core/src/frechet/spaces/point_process.rs
  - fdars-core/src/frechet/spaces/mod.rs
  - fdars-core/src/frechet/mod.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [FRE-02-03, FRE-02-04, FRE-02-05]
estimate:
  tokens: 60000
  raw_tokens: 33000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "SphericalSpace geodesic distance of antipodal unit vectors [1,0] and [-1,0] equals π within 1e-12; identical vectors give 0 (FRE-02-03)."
    - "SphericalSpace Karcher mean of [1,0] and [cos 0.1, sin 0.1] on S¹ equals the great-circle midpoint [cos 0.05, sin 0.05] within 1e-6 (FRE-02-03)."
    - "SphericalSpace Karcher mean of three identical unit vectors recovers that vector within 1e-8; antipodally-balanced input returns ComputationFailed (FRE-02-03)."
    - "NetworkSpace Frobenius distance of identical 3-node Laplacians is 0; weighted mean of valid Laplacians has row-sums 0 within 1e-10 (FRE-02-04)."
    - "PointProcessSpace L2 distance of [1,0,0] and [0,1,0] equals √2; weighted mean of identical intensity vectors recovers the vector within 1e-10 (FRE-02-05)."
    - "dimension mismatch, empty objects, weight-length mismatch, and Karcher non-convergence return FdarError (never panic)."
  artifacts:
    - fdars-core/src/frechet/spaces/spherical.rs
    - fdars-core/src/frechet/spaces/network.rs
    - fdars-core/src/frechet/spaces/point_process.rs
  key_links:
    - "SphericalSpace, NetworkSpace, PointProcessSpace each `impl MetricSpace` with `type Object = Vec<f64>`."
    - "frechet/mod.rs and src/lib.rs re-export blocks extended (append-only) with all three new space structs."
---

<objective>
Deliver the three vector-valued `MetricSpace` backends: `SphericalSpace` (geodesic exp/log +
iterative Karcher mean), `NetworkSpace` (graph-Laplacian Frobenius), and `PointProcessSpace`
(intensity/count L2). Spherical is the only iterative backend and leads as the tracer.

Purpose: FRE-02-03 (spherical geodesic space), FRE-02-04 (network Laplacian space),
FRE-02-05 (point-process intensity space).

Output: `frechet/spaces/spherical.rs`, `network.rs`, `point_process.rs`; append-only
re-export additions in `frechet/spaces/mod.rs`, `frechet/mod.rs`, `src/lib.rs`.

Depends on 42-01: the `frechet/spaces/` module and its re-export blocks must already exist.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-object-data-fr-chet-regression/42-RESEARCH.md
@.planning/phases/42-object-data-fr-chet-regression/42-PATTERNS.md
@.planning/phases/42-object-data-fr-chet-regression/42-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan produces: `SphericalSpace`, `NetworkSpace`, `PointProcessSpace`. The generic
regression/ANOVA entry points and `pub(crate)` weight/Tₙ helpers are produced by plan 42-03.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: SphericalSpace — geodesic distance, exp/log maps, Karcher mean end-to-end</name>
  <files>fdars-core/src/frechet/spaces/spherical.rs, fdars-core/src/frechet/spaces/mod.rs, fdars-core/src/frechet/mod.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/frechet/space.rs (lines 51-121): struct + `new()` + `impl MetricSpace` analog (PATTERNS.md lines 32-99).
    - fdars-core/src/frechet/spaces/mod.rs, frechet/mod.rs, src/lib.rs (created/extended by 42-01): the re-export blocks to append to.
    - 42-RESEARCH.md §"Spherical Space" (lines 263-301): unit-vector object, geodesic distance `arccos(clamp(⟨a,b⟩,−1,1))`, exp map, log map with θ guards, Karcher gradient-descent algorithm (extrinsic init, max_iter=50, tol=1e-8), ComputationFailed on non-convergence/antipodal-balance.
    - 42-RESEARCH.md code example §"Karcher mean on sphere" (lines 551-586) and §"Common Pitfalls" 4 (lines 484-488) — division-by-zero guard at θ≈π.
    - 42-RESEARCH.md test oracles §Spherical (lines 649-655).
  </read_first>
  <action>
    Create `frechet/spaces/spherical.rs` with `pub struct SphericalSpace { pub d: usize }` (derive `Debug, Clone, PartialEq` + serde-gate, no `#[non_exhaustive]`). `SphericalSpace::new(d) -> Result<Self, FdarError>` validates `d >= 1`. Add file-level constants `MAX_ITER: usize = 50` and `TOL: f64 = 1e-8`.
    Private helpers: `geodesic_distance(a, b) -> f64` = `a·b` clamped to `[-1,1]` then `.acos()`; `exp_map(x, v, d) -> Vec<f64>` returning `x` if `‖v‖ < 1e-12` else `cos(‖v‖)·x + (sin(‖v‖)/‖v‖)·v`; `log_map(x, y, d) -> Result<Vec<f64>, FdarError>` with `θ = arccos(clamp(⟨x,y⟩,−1,1))`, returning the zero vector if `θ < 1e-12`, `ComputationFailed { operation: "SphericalSpace::weighted_frechet_mean", ... }` if `θ > π − 1e-8` (antipodal, non-unique log), else `(θ/sin θ)·(y − cos(θ)·x)`.
    `impl MetricSpace for SphericalSpace { type Object = Vec<f64>; ... }`: `distance` validates `a.len()==d && b.len()==d` (else `InvalidDimension`) then `geodesic_distance`; `weighted_frechet_mean` validates non-empty objects, `weights.len()==objects.len()`, per-object length `==d`, then runs the Karcher loop: extrinsic init `x = Σ wᵢ aᵢ` normalized (if `‖·‖ < 1e-14` → `ComputationFailed` for antipodally-balanced input); iterate up to `MAX_ITER`: gradient `g = Σ wᵢ log_map(x, aᵢ)`, if `‖g‖ < TOL` break (converged), else `x = exp_map(x, g)` re-normalized; if not converged after `MAX_ITER` → `ComputationFailed { detail: "Karcher mean did not converge in 50 iterations" }`.
    Rustdoc: document that unit-norm is validated only conceptually (callers must supply unit vectors; not checked per-call for performance), the extrinsic-mean initialization, and the divergence from R `frechet` 0.3.0's Karcher init/convergence criterion (42-RESEARCH.md lines 265-267, 299-300).
    Extend re-exports (append-only): `frechet/spaces/mod.rs` add `mod spherical;` + `pub use spherical::SphericalSpace;`; `frechet/mod.rs` add `SphericalSpace` to the `pub use spaces::{...}` list; `src/lib.rs` append `SphericalSpace` to the `pub use frechet::{...}` block.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel spherical 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Inline test `spherical_geodesic_antipodal_is_pi`: distance of `[1,0]` and `[-1,0]` equals `std::f64::consts::PI` within 1e-12.
    - Inline test `spherical_geodesic_identical_is_zero`: distance of a vector from itself < 1e-12.
    - Inline test `spherical_karcher_midpoint`: Karcher mean of `[1,0]` and `[cos(0.1), sin(0.1)]` equals `[cos(0.05), sin(0.05)]` within 1e-6.
    - Inline test `spherical_karcher_of_identical_recovers`: mean of three identical unit vectors recovers the vector within 1e-8.
    - Inline test `spherical_karcher_antipodal_balanced_fails`: mean of `[1,0]` and `[-1,0]` with equal weights returns `FdarError::ComputationFailed`.
    - `grep -n "SphericalSpace" fdars-core/src/lib.rs` returns a match inside the `pub use frechet::{...}` block.
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel spherical 2>&1 | tail -10` shows 0 failures.
  </acceptance_criteria>
  <done>`SphericalSpace` supports geodesic distance + exp/log maps + iterative Karcher mean end-to-end, with passing antipodal-π, great-circle-midpoint, and non-convergence oracles, and is re-exported.</done>
  <reversibility rating="reversible">Additive new file + append-only re-exports on unpublished API.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NetworkSpace — graph-Laplacian Frobenius backend</name>
  <files>fdars-core/src/frechet/spaces/network.rs, fdars-core/src/frechet/spaces/mod.rs, fdars-core/src/frechet/mod.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/frechet/space.rs (lines 51-121): struct + `new()` + `impl MetricSpace` analog.
    - fdars-core/src/frechet/spaces/spd.rs (Frobenius arm, from 42-01): the identical element-wise Frobenius distance + weighted-average mean to mirror.
    - 42-RESEARCH.md §"Network Space" (lines 304-313): Laplacian object, Frobenius distance, weighted-average mean, convex-cone preservation (no special guard), test oracles §Network (lines 658-663).
  </read_first>
  <behavior>
    - Test `network_distance_of_identical_is_zero`: Frobenius distance of two identical 3-node Laplacians is 0 within 1e-12.
    - Test `network_mean_of_identical_recovers`: weighted mean of identical Laplacians recovers the Laplacian within 1e-10.
    - Test `network_mean_preserves_row_sums`: weighted mean of two distinct valid 3-node Laplacians has each row summing to 0 within 1e-10.
    - Test `network_rejects_dimension_mismatch`: distance on wrong-length object returns `FdarError::InvalidDimension`.
  </behavior>
  <action>
    Create `frechet/spaces/network.rs` with `pub struct NetworkSpace { pub d: usize }` (derive `Debug, Clone, PartialEq` + serde-gate, no `#[non_exhaustive]`). `NetworkSpace::new(d) -> Result<Self, FdarError>` validates `d >= 1`. `impl MetricSpace` with `type Object = Vec<f64>`: `distance` = element-wise Frobenius norm over `d*d` flat Laplacians after dimension validation; `weighted_frechet_mean` validates non-empty objects, weight-length match, per-object length, then returns the element-wise weighted average `M = (Σ wᵢ Lᵢ) / Σ wᵢ` (with `NUMERICAL_EPS` guard). Do NOT validate Laplacian structure per-call (document that valid inputs are the caller's responsibility, and that a non-negative weighted combination stays on the Laplacian convex cone).
    Rustdoc: document the Frobenius-on-Laplacian metric and the R `frechet` 0.3.0 divergence note (42-RESEARCH.md lines 308, 313).
    Extend re-exports (append-only): `frechet/spaces/mod.rs` add `mod network;` + `pub use network::NetworkSpace;`; `frechet/mod.rs` and `src/lib.rs` append `NetworkSpace`.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel network 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - The four `<behavior>` tests exist and pass.
    - `grep -n "NetworkSpace" fdars-core/src/lib.rs` returns a match inside the `pub use frechet::{...}` block.
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel network 2>&1 | tail -8` shows 0 failures.
  </acceptance_criteria>
  <done>`NetworkSpace` implements Frobenius distance + weighted-average mean with passing distance/mean/row-sum oracles, and is re-exported.</done>
  <reversibility rating="reversible">Additive new file + append-only re-exports on unpublished API.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PointProcessSpace — intensity/count L2 backend</name>
  <files>fdars-core/src/frechet/spaces/point_process.rs, fdars-core/src/frechet/spaces/mod.rs, fdars-core/src/frechet/mod.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/frechet/space.rs (lines 51-121): struct + `new()` + `impl MetricSpace` analog.
    - fdars-core/src/frechet/spaces/network.rs (from Task 2): the closest structural analog (simple linear-combination space).
    - 42-RESEARCH.md §"Point-Process Space" (lines 317-326): intensity vector of length `m`, L2 distance, weighted-average mean; test oracles §Point-Process (lines 666-671).
  </read_first>
  <behavior>
    - Test `point_process_distance_of_identical_is_zero`: L2 distance of two identical vectors is 0 within 1e-12.
    - Test `point_process_distance_orthonormal_is_sqrt2`: L2 distance of `[1,0,0]` and `[0,1,0]` equals `2f64.sqrt()` within 1e-12.
    - Test `point_process_mean_of_identical_recovers`: weighted mean of identical intensity vectors recovers the vector within 1e-10.
    - Test `point_process_rejects_dimension_mismatch`: distance on wrong-length object returns `FdarError::InvalidDimension`.
  </behavior>
  <action>
    Create `frechet/spaces/point_process.rs` with `pub struct PointProcessSpace { pub m: usize }` (derive `Debug, Clone, PartialEq` + serde-gate, no `#[non_exhaustive]`). `PointProcessSpace::new(m) -> Result<Self, FdarError>` validates `m >= 1`. `impl MetricSpace` with `type Object = Vec<f64>`: `distance` = L2 norm `sqrt(Σ (aᵢ − bᵢ)²)` over length-`m` vectors after dimension validation; `weighted_frechet_mean` validates non-empty objects, weight-length match, per-object length `==m`, then returns the element-wise weighted average `M = (Σ wᵢ aᵢ) / Σ wᵢ` (with `NUMERICAL_EPS` guard). Do not enforce non-negativity of intensities (document that as a caller responsibility, validate only dimensions).
    Rustdoc: document the L2-on-intensity metric and the R `frechet` 0.3.0 divergence note (42-RESEARCH.md lines 321, 326).
    Extend re-exports (append-only): `frechet/spaces/mod.rs` add `mod point_process;` + `pub use point_process::PointProcessSpace;`; `frechet/mod.rs` and `src/lib.rs` append `PointProcessSpace`.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel point_process 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - The four `<behavior>` tests exist and pass.
    - `grep -n "PointProcessSpace" fdars-core/src/lib.rs` returns a match inside the `pub use frechet::{...}` block.
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel point_process 2>&1 | tail -8` shows 0 failures.
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy -p fdars-core --all-targets --features linalg,parallel -- -D warnings 2>&1 | tail -5` clean for the touched files.
  </acceptance_criteria>
  <done>`PointProcessSpace` implements L2 distance + weighted-average mean with passing √2 and recovery oracles, and is re-exported.</done>
  <reversibility rating="reversible">Additive new file + append-only re-exports on unpublished API.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → Spherical/Network/PointProcess public fns | Untrusted numeric vectors (`Vec<f64>`) + dimension cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-06 | Tampering | `log_map` divides by `sin(θ)` → division-by-zero at θ≈π | medium | mitigate | Guard `θ > π − 1e-8` → `ComputationFailed` before dividing |
| T-42-07 | Denial of Service | Karcher mean non-convergence loops forever | medium | mitigate | Hard `MAX_ITER = 50` cap; `ComputationFailed` on non-convergence |
| T-42-08 | Tampering | `arccos` of out-of-range dot product → NaN | low | mitigate | Clamp `⟨a,b⟩` to `[-1,1]` before `acos` |
| T-42-09 | Denial of Service | Empty objects / weight-length mismatch | low | mitigate | `InvalidDimension` at function entry |
| T-42-SC | Tampering | npm/pip/cargo installs | high | accept | No new dependencies (RESEARCH audit: none) |
</threat_model>

<verification>
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel spherical network point_process 2>&1 | tail -12` — all backend tests green.
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings 2>&1 | tail -5` — clean.
- `cargo fmt -p fdars-core` per commit.
</verification>

<success_criteria>
- FRE-02-03: `SphericalSpace` geodesic + Karcher mean with antipodal-π and midpoint oracles.
- FRE-02-04: `NetworkSpace` Frobenius distance + Laplacian-preserving mean.
- FRE-02-05: `PointProcessSpace` L2 distance + weighted-average mean.
- All three re-exported; existing re-exports and 42-01's additions untouched.
- No existing public signature changed.
</success_criteria>

<output>
Create `.planning/phases/42-object-data-fr-chet-regression/42-02-SUMMARY.md` when done.
</output>
