---
phase: 42-object-data-fr-chet-regression
plan: 03
type: execute
wave: 3
depends_on: ["42-01", "42-02"]
files_modified:
  - fdars-core/src/frechet/regression.rs
  - fdars-core/src/frechet/anova.rs
  - fdars-core/src/frechet/mod.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [FRE-02-06, FRE-02-07]
estimate:
  tokens: 66000
  raw_tokens: 37000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "frechet_global_reg_space over SpdMatrixSpace(Frobenius) with all-identical responses predicts that exact matrix at any xout within 1e-8 (FRE-02-06)."
    - "frechet_local_reg_space over SpdMatrixSpace(Frobenius) returns a predicted object per xout row with no panic (FRE-02-06)."
    - "frechet_anova_space over a homogeneous SPD sample yields p_value_permutation > 0.05 and is bit-exactly reproducible for a fixed seed (FRE-02-07)."
    - "frechet_anova_space over a well-separated 2-group SPD sample (identity vs 4·identity) yields p_value_permutation < 0.05 (FRE-02-07)."
    - "ALL existing density Fréchet regression + ANOVA tests still pass unchanged after the pub(crate) helper extraction (regression gate)."
    - "existing public signatures of frechet_global_reg, frechet_local_reg, frechet_anova are byte-for-byte unchanged (additive only)."
    - "weight-length / empty-object / label-count mismatches on the generic entry points return FdarError (never panic)."
  artifacts:
    - "pub(crate) compute_global_weights in fdars-core/src/frechet/regression.rs"
    - "pub(crate) compute_local_weights in fdars-core/src/frechet/regression.rs"
    - "pub(crate) compute_tn_generic in fdars-core/src/frechet/anova.rs"
    - "frechet_global_reg_space + frechet_local_reg_space in fdars-core/src/frechet/regression.rs"
    - "frechet_anova_space in fdars-core/src/frechet/anova.rs"
  key_links:
    - "frechet_global_reg + frechet_local_reg delegate to the extracted compute_global_weights/compute_local_weights (density path bit-identical output)."
    - "frechet_anova delegates to compute_tn_generic (generic core; density path unchanged)."
    - "generic entry points call space.weighted_frechet_mean per xout row / compute_tn_generic per permutation."
    - "frechet/mod.rs and src/lib.rs re-export the three generic entry-point functions (append-only)."
---

<objective>
Lift the density-specific weight and Tₙ machinery into `pub(crate)` helpers that both the
existing density path and new generic entry points consume, then add the generic
`frechet_global_reg_space`, `frechet_local_reg_space`, and `frechet_anova_space` — demonstrated
and validated with the `SpdMatrixSpace` (Frobenius) backend. The refactor is INTERNAL: the
existing `frechet_global_reg`, `frechet_local_reg`, and `frechet_anova` public signatures stay
byte-for-byte identical and their existing tests are the regression gate.

Purpose: FRE-02-06 (generic global + local Fréchet regression over Euclidean predictors for a
non-density backend) and FRE-02-07 (generic Fréchet-ANOVA over a non-density object space).

Output: `pub(crate) compute_global_weights` / `compute_local_weights` in `regression.rs`,
`pub(crate) compute_tn_generic` in `anova.rs`, the three generic entry points, and append-only
re-exports.

Depends on 42-01 (uses `SpdMatrixSpace` + `SpdMetric::Frobenius` as the demo/validation
backend) and 42-02 (re-export blocks already extended; append-only additions here).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-object-data-fr-chet-regression/42-RESEARCH.md
@.planning/phases/42-object-data-fr-chet-regression/42-PATTERNS.md
@.planning/phases/42-object-data-fr-chet-regression/42-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan produces: `pub(crate) compute_global_weights`, `pub(crate) compute_local_weights`,
`pub(crate) compute_tn_generic`, `frechet_global_reg_space`, `frechet_local_reg_space`,
`frechet_anova_space`. Together with plans 42-01 and 42-02, this completes the full phase
symbol set: SpdMatrixSpace, SpdMetric, CorrelationMatrixSpace, SphericalSpace, NetworkSpace,
PointProcessSpace, the three generic entry points, and the three pub(crate) helpers.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Extract global weight helper + frechet_global_reg_space (SPD Frobenius) end-to-end</name>
  <files>fdars-core/src/frechet/regression.rs, fdars-core/src/frechet/mod.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/frechet/regression.rs (lines 1-148): `validate_reg_input`, the existing `frechet_global_reg` (weight block lines 94-141 to extract), imports lines 11-16 (PATTERNS.md §regression.rs, lines 184-325).
    - fdars-core/src/frechet/spaces/spd.rs (from 42-01): `SpdMatrixSpace` + `SpdMetric::Frobenius` used for the demo/validation test.
    - 42-RESEARCH.md §"Global weight helper" (lines 350-395): the `compute_global_weights` signature, the delegated `frechet_global_reg`, the `frechet_global_reg_space` pattern, and the signed-weight handling (linear-combination spaces take signed weights natively; constant-response oracle lines 673-681).
    - 42-PATTERNS.md §"New generic entry-point pattern" (lines 296-325) and §"#[must_use]" (lines 536-543).
  </read_first>
  <action>
    Extract the Petersen–Müller global weight computation from `frechet_global_reg` (the X̄ means, sample covariance Σ̂ + 1e-6 ridge, `cholesky_factor`, and the per-xout-row signed weights `sᵢ = (1 + (Xᵢ−X̄)ᵀ Σ̂⁻¹(xout−X̄)) / n`) into `pub(crate) fn compute_global_weights(predictors: &FdMatrix, xout: &FdMatrix) -> Result<(Vec<Vec<f64>>, Vec<f64>), FdarError>` returning `(weights_per_row, x_bar)`. Do NOT change any formula. Rewrite `frechet_global_reg` to call `compute_global_weights` then run its existing `signed_quantile_average` loop over `weights_per_row[r]` — its public signature and output must be byte-identical.
    Add `#[must_use = "expensive regression — store or use the returned prediction"] pub fn frechet_global_reg_space<S: MetricSpace>(space: &S, predictors: &FdMatrix, responses: &[S::Object], xout: &FdMatrix) -> Result<Vec<S::Object>, FdarError>`: validate `responses.len() == predictors.nrows()` and `predictors.nrows() > 0` and `xout.ncols() == predictors.ncols()` (else `InvalidDimension`), call `compute_global_weights`, and for each xout row push `space.weighted_frechet_mean(responses, &weights_per_row[r])?`. Signed (possibly negative) weights are passed directly — correct for linear-combination spaces (SPD Frobenius/Correlation/Network/PointProcess); rustdoc-note that `SphericalSpace` should clip negatives (documented divergence, 42-RESEARCH.md lines 385-395).
    Add `MetricSpace` to the `regression.rs` imports (`use crate::frechet::space::{signed_quantile_average, MetricSpace};`). Extend re-exports append-only: `frechet/mod.rs` add `pub use regression::frechet_global_reg_space;`; `src/lib.rs` append `frechet_global_reg_space` to the `pub use frechet::{...}` block.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel frechet_global_reg 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pub(crate) fn compute_global_weights" fdars-core/src/frechet/regression.rs` returns a match.
    - Existing density regression test `global_tracks_known_relationship` still passes (regression gate).
    - Inline test `spd_global_reg_constant_response_predicts_constant`: `frechet_global_reg_space` over `SpdMatrixSpace::new(2, SpdMetric::Frobenius)` with all responses equal to a fixed 2×2 SPD matrix A predicts A (Frobenius distance < 1e-8) at an arbitrary xout row.
    - Inline test `spd_global_reg_rejects_response_count_mismatch`: mismatched `responses.len()` vs predictor rows returns `FdarError::InvalidDimension`.
    - `grep -n "frechet_global_reg_space" fdars-core/src/lib.rs` returns a match inside the `pub use frechet::{...}` block.
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel frechet_global_reg 2>&1 | tail -12` shows 0 failures (both the density and generic tests).
  </acceptance_criteria>
  <done>`compute_global_weights` is extracted, `frechet_global_reg` delegates to it with bit-identical output (existing tests pass), and `frechet_global_reg_space` predicts a constant SPD response end-to-end.</done>
  <reversibility rating="reversible">Internal helper extraction + additive generic fn on unpublished API; density path proven identical by existing tests.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Extract local weight helper + frechet_local_reg_space</name>
  <files>fdars-core/src/frechet/regression.rs, fdars-core/src/frechet/mod.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/frechet/regression.rs (lines 150-254): existing `frechet_local_reg` — kernel weights + local moments μ₁/μ₂ + Cholesky solve + signed weights + zero-sum guard (PATTERNS.md §"Local weight block", lines 229-278).
    - fdars-core/src/frechet/regression.rs (Task 1 result): `compute_global_weights`, `frechet_global_reg_space` — mirror the generic entry-point shape.
    - 42-RESEARCH.md §"Local weight helper" (lines 397-411): the `compute_local_weights(predictors, x0, bandwidth, n, p)` signature returning the normalized per-row weight vector.
  </read_first>
  <action>
    Extract the local-linear weight computation from `frechet_local_reg` (product Gaussian kernel via `gaussian_kernel`, local moments μ₁/μ₂ with 1e-6 ridge, `cholesky_solve` for `a = μ₂⁻¹μ₁`, signed weights `sᵢ = Kᵢ(1 − (Xᵢ−x₀)ᵀa)`, normalize by `Σ w` with the `NUMERICAL_EPS` zero-sum guard → `ComputationFailed`) into `pub(crate) fn compute_local_weights(predictors: &FdMatrix, x0: &[f64], bandwidth: f64, n: usize, p: usize) -> Result<Vec<f64>, FdarError>`. No formula change. Rewrite `frechet_local_reg` to call this per xout row; its public signature and output stay byte-identical.
    Add `#[must_use = "..."] pub fn frechet_local_reg_space<S: MetricSpace>(space: &S, predictors: &FdMatrix, responses: &[S::Object], xout: &FdMatrix, bandwidth: f64) -> Result<Vec<S::Object>, FdarError>`: validate response/predictor alignment, xout columns, and `bandwidth > 0` (else `InvalidParameter`); for each xout row compute `compute_local_weights` and push `space.weighted_frechet_mean(responses, &weights)?`. Rustdoc-note the same signed-weight divergence for spherical.
    Extend re-exports append-only: `frechet/mod.rs` add `frechet_local_reg_space` to the `pub use regression::{...}` line; `src/lib.rs` append `frechet_local_reg_space`.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel frechet_local_reg 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pub(crate) fn compute_local_weights" fdars-core/src/frechet/regression.rs` returns a match.
    - All existing `frechet_local_reg` density tests still pass (regression gate).
    - Inline test `spd_local_reg_returns_object_per_xout`: `frechet_local_reg_space` over `SpdMatrixSpace(Frobenius)` returns exactly `xout.nrows()` predicted objects, each length `d*d`, with no panic.
    - Inline test `spd_local_reg_rejects_nonpositive_bandwidth`: `bandwidth = 0.0` returns `FdarError::InvalidParameter`.
    - `grep -n "frechet_local_reg_space" fdars-core/src/lib.rs` returns a match inside the `pub use frechet::{...}` block.
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel frechet_local_reg 2>&1 | tail -12` shows 0 failures.
  </acceptance_criteria>
  <done>`compute_local_weights` is extracted with `frechet_local_reg` delegating (existing tests pass), and `frechet_local_reg_space` returns a predicted SPD object per xout row.</done>
  <reversibility rating="reversible">Internal helper extraction + additive generic fn on unpublished API.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Generify compute_tn + frechet_anova_space</name>
  <files>fdars-core/src/frechet/anova.rs, fdars-core/src/frechet/mod.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/frechet/anova.rs (lines 1-98): imports, the concrete `compute_tn` (lines 32-97) with its full Tₙ body (partition, pooled/group Fréchet mean+variance, σ̂ₗ², Fn/Un/Tn) — PATTERNS.md §anova.rs, lines 333-421.
    - fdars-core/src/frechet/anova.rs (lines 118-191): existing `frechet_anova` validation + seeded permutation loop `StdRng::seed_from_u64(seed.wrapping_add(perm as u64))` (PATTERNS.md lines 381-407).
    - fdars-core/src/frechet/mean.rs (lines 40-83): confirms `frechet_mean`/`frechet_variance` are already generic over `S: MetricSpace` (RESEARCH lines 346, 439).
    - 42-RESEARCH.md §"Generic ANOVA helper" (lines 413-443) + code example §"Generic ANOVA after refactor" (lines 590-623); ANOVA oracles (lines 683-689).
  </read_first>
  <action>
    Rename `compute_tn` to `pub(crate) fn compute_tn_generic<S: MetricSpace>(space: &S, objects: &[S::Object], labels: &[usize], k: usize) -> Result<(f64, f64, f64, Vec<f64>, f64), FdarError>` — the body is unchanged (it already calls the generic `frechet_mean`/`frechet_variance` and `space.distance`). Update `frechet_anova` to call `compute_tn_generic(&space, &objects, ...)` with its `WassersteinDensitySpace` — no public-signature change. Update the permutation loop to call `compute_tn_generic`.
    Add `#[must_use = "returns the Fréchet ANOVA result; examine the p-values"] pub fn frechet_anova_space<S: MetricSpace>(space: &S, objects: &[S::Object], group_labels: &[usize], n_perm: usize, seed: u64) -> Result<FrechetAnovaResult, FdarError>`: mirror `frechet_anova`'s validation minus the `argvals`/`FdMatrix` checks (validate non-empty objects, `group_labels.len() == objects.len()`, ≥2 distinct groups, contiguous 0..k labels — else `InvalidDimension`/`InvalidParameter`); default `n_perm` to 999 when 0; compute observed Tₙ via `compute_tn_generic`, the asymptotic χ²(k−1) p-value via `chi_square_sf`, and the seeded-permutation p-value via the identical `StdRng::seed_from_u64(seed.wrapping_add(perm as u64))` loop; assemble and return the existing `FrechetAnovaResult` struct.
    Add `MetricSpace` to the anova.rs imports (it already imports it via `super::{FrechetAnovaResult, MetricSpace}` — confirm; add if missing). Extend re-exports append-only: `frechet/mod.rs` add `pub use anova::frechet_anova_space;`; `src/lib.rs` append `frechet_anova_space`.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel frechet_anova 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pub(crate) fn compute_tn_generic" fdars-core/src/frechet/anova.rs` returns a match; `grep -c "fn compute_tn\b" fdars-core/src/frechet/anova.rs` shows the old concrete `compute_tn` is gone (renamed, not duplicated).
    - Existing density ANOVA test `anova_flags_shifted_groups` still passes (regression gate).
    - Inline test `spd_anova_homogeneous_not_significant`: `frechet_anova_space` over `SpdMatrixSpace(Frobenius)` on a 2-group all-identical SPD sample yields `p_value_permutation > 0.05`.
    - Inline test `spd_anova_seed_reproducible`: two calls with the same `seed` return bit-exactly equal `p_value_permutation`.
    - Inline test `spd_anova_separated_significant`: a 2-group SPD sample (group A = identity, group B = 4·identity) yields `p_value_permutation < 0.05`.
    - `grep -n "frechet_anova_space" fdars-core/src/lib.rs` returns a match inside the `pub use frechet::{...}` block.
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel frechet 2>&1 | tail -12` shows 0 failures (whole frechet module, density + generic).
    - `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings 2>&1 | tail -5` clean.
  </acceptance_criteria>
  <done>`compute_tn` is generified to `compute_tn_generic`, `frechet_anova` delegates (existing tests pass), and `frechet_anova_space` gives non-significant/significant + seed-reproducible results over the SPD backend.</done>
  <reversibility rating="reversible">Rename to generic + additive generic fn on unpublished API; density ANOVA path proven identical by existing tests.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → generic regression/ANOVA entry points | Untrusted predictors, responses (`&[S::Object]`), labels, seed cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-10 | Tampering | Negative Petersen–Müller weights fed to a Karcher-based `weighted_frechet_mean` | medium | mitigate | Signed weights are safe for linear-combination spaces (SPD Frobenius demo); rustdoc-note spherical must clip negatives |
| T-42-11 | Tampering | Response/predictor/label length mismatch | medium | mitigate | Validate alignment at entry → `InvalidDimension`/`InvalidParameter`, never index past bounds |
| T-42-12 | Denial of Service | Zero-sum local weights (bandwidth too small) | low | mitigate | Existing `NUMERICAL_EPS` guard in `compute_local_weights` → `ComputationFailed` |
| T-42-13 | Repudiation | Non-reproducible ANOVA p-value | low | mitigate | Seeded `StdRng::seed_from_u64(seed.wrapping_add(perm))`; seed-reproducibility test asserts bit-exact equality |
| T-42-SC | Tampering | npm/pip/cargo installs | high | accept | No new dependencies (RESEARCH audit: none) |
</threat_model>

<verification>
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel frechet 2>&1 | tail -15` — full frechet module (density regression gate + generic tests) green.
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --all-targets 2>&1 | tail -10` — whole crate green (density path unbroken).
- `TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings 2>&1 | tail -5` — clean.
- `cargo fmt -p fdars-core` per commit + whole-crate sweep at end.
</verification>

<success_criteria>
- FRE-02-06: `frechet_global_reg_space` + `frechet_local_reg_space` work over the SPD Frobenius backend (constant-response oracle passes).
- FRE-02-07: `frechet_anova_space` gives non-significant/significant + seed-reproducible results over the SPD backend.
- The `pub(crate)` helper extraction leaves all existing density `frechet_global_reg`/`frechet_local_reg`/`frechet_anova` tests passing and their public signatures unchanged.
- All three generic entry points re-exported from `frechet/mod.rs` and the crate root.
</success_criteria>

<output>
Create `.planning/phases/42-object-data-fr-chet-regression/42-03-SUMMARY.md` when done.
</output>
