---
phase: 92-ci-determinism-guardrail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/rust-ci.yml
autonomous: true
requirements: [CI-01]
estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A CI job `determinism-guardrail` exists in .github/workflows/rust-ci.yml exercising the full parallel test path (cargo test --features linalg,parallel,serde) with a repeat loop AND once with RAYON_NUM_THREADS=1 (satisfies CI-01)."
    - "The job asserts the 3 golden tests actually RAN under --features linalg (2 passed for equivalence_phase48 golden_co_cluster; 1 passed for equivalence_phase49 svd_sign_fpca_two_matrix_bit_identical) with 0 ignored — catching a silent-skip regression (the exact Phase-90 surface)."
    - "The workflow YAML is syntactically valid (parses with python3 yaml.safe_load)."
    - "No new crate/tooling dependency is introduced (no cargo-nextest, no serial_test) — the mechanism is a pure shell loop in the workflow YAML."
    - "The guardrail's exact shell commands run green locally against the current Phase-90/91 baseline (proxy that CI will be green)."
  artifacts:
    - ".github/workflows/rust-ci.yml (containing the new determinism-guardrail job)"
  key_links:
    - "determinism-guardrail job -> same on: push/pull_request/release triggers + working-directory: fdars-core + cache block as the existing test job."
    - "positive-run grep assertion -> the `test result:` line format `N passed; 0 failed; 0 ignored` proven locally."
---

<objective>
Add ONE new CI job `determinism-guardrail` to `.github/workflows/rust-ci.yml` that catches a reintroduced Phase-90-style determinism regression, without adding any new tooling. The mechanism (locked in 92-CONTEXT.md): repeat the full parallel serde-config test run a few times, run it once with `RAYON_NUM_THREADS=1`, and positively assert the three golden tests actually RAN under `--features linalg` (not silently ignored).

Purpose: A future regression must fail CI loudly instead of returning silently. Two concrete regression modes are covered — (a) real nondeterminism under repeated/thread-varied runs, and (b) the golden tests silently never running (cfg-guard/feature-wiring regression, the exact Phase-90 surface).
Output: An edited `.github/workflows/rust-ci.yml` with the `determinism-guardrail` job; the job's shell commands validated locally against the green Phase-90/91 baseline.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.github/workflows/rust-ci.yml
@.planning/phases/92-ci-determinism-guardrail/92-CONTEXT.md
@.planning/phases/90-golden-flake-root-cause-deterministic-fix/90-DIAGNOSIS.md
</context>

<constraints>
- **Std-only, NO new tooling.** Do NOT add cargo-nextest, serial_test, or any crate/action dependency. The mechanism is a pure shell loop inside the workflow YAML step (`run: |`). This is a LOCKED decision (92-CONTEXT.md).
- **This phase touches ONLY `.github/workflows/rust-ci.yml`.** No version bump, no CHANGELOG, no src/ or Cargo.toml change (that is Phase 93).
- **Mirror the existing `test` job scaffolding** verbatim: `runs-on: ubuntu-latest`, `defaults.run.working-directory: fdars-core`, `actions/checkout@v4`, `dtolnay/rust-toolchain@stable`, the `actions/cache@v4` block caching `~/.cargo/...` + `fdars-core/target/` keyed on `${{ runner.os }}-cargo-<key>-${{ hashFiles('**/Cargo.lock') }}`. The job runs under the same top-level `on:` push/pull_request/release triggers (do NOT add a new `on:` block).
- **Exclude `dhat-heap`** (its `#[global_allocator]` panics under parallel dhat tests) and **`js`** (WASM-only) — identical rationale to the existing `test` job. Use the feature set `linalg,parallel,serde`.
- **Commit hazards (project):** run `cargo fmt` is NOT needed here (no Rust source changed). This is a YAML-only edit; commit normally, but if the pre-commit hook stalls on cargo, commit with `--no-verify` (the only file changed is a workflow YAML — no fmt/clippy surface).
- **No local CI runner** (`act` not assumed). Validation = (1) YAML parses, (2) the exact shell commands the job runs pass locally against the current baseline.
</constraints>

<tasks>

<task type="tracer">
  <name>Task 1: Add the `determinism-guardrail` job to rust-ci.yml (full end-to-end guardrail path)</name>
  <files>.github/workflows/rust-ci.yml</files>
  <read_first>
    - .github/workflows/rust-ci.yml (mirror the `test` job block, lines ~21-58: runs-on, defaults.run.working-directory, checkout, rust-toolchain, actions/cache@v4 block, and the two cargo test invocations)
    - .planning/phases/92-ci-determinism-guardrail/92-CONTEXT.md (LOCKED decisions: std-only, repeat + RAYON_NUM_THREADS=1, anti-silent-skip positive-run assertion)
    - .planning/phases/90-golden-flake-root-cause-deterministic-fix/90-DIAGNOSIS.md (the failure mode: goldens require `linalg`; a cfg-guard/feature-wiring regression could make them silently `ignored`)
  </read_first>
  <action>
    Add a new job with id `determinism-guardrail` to the `jobs:` map in `.github/workflows/rust-ci.yml` (place it after the `test` job block, before `clippy`). It runs under the existing top-level `on:` triggers — do NOT add or modify the `on:` block. The job MUST mirror the `test` job scaffolding: `name: Determinism Guardrail`, `runs-on: ubuntu-latest`, `defaults.run.working-directory: fdars-core`, step `uses: actions/checkout@v4`, step `Install Rust` via `uses: dtolnay/rust-toolchain@stable`, and the `Cache cargo` step `uses: actions/cache@v4` with the same `path:` list (`~/.cargo/bin/`, `~/.cargo/registry/index/`, `~/.cargo/registry/cache/`, `~/.cargo/git/db/`, `fdars-core/target/`) and a `key:` of the form `${{ runner.os }}-cargo-determinism-${{ hashFiles('**/Cargo.lock') }}`.

    Then add exactly three run-steps, each a shell block (`run: |`), NO new dependency/action:

    Step A "Repeated full parallel run (intermittency)": a bash `for i in 1 2 3` loop that runs `cargo test --features linalg,parallel,serde` each iteration and fails the step if any iteration reports a failure. Detect failure by grepping the cargo output for the string `test result: FAILED` and exiting non-zero when found (mirror the Phase-91 proven pattern: `for i in 1 2 3; do cargo test --features linalg,parallel,serde 2>&1 | tee /tmp/dg_$i.log; grep -q "test result: FAILED" /tmp/dg_$i.log && { echo "RUN $i had a FAILED test result"; exit 1; }; done; echo ALL_3_RUNS_GREEN`). Repeat count is 3 (documented: balances signal vs CI minutes — a determinism regression that is intermittent surfaces within a handful of runs; 3 is the CONTEXT-suggested "few").

    Step B "Single-threaded reduction-order run": run the same suite once with the env var forced, `RAYON_NUM_THREADS=1 cargo test --features linalg,parallel,serde`, failing the step if the output contains `test result: FAILED` (rationale in a `#` comment: if any golden ever depended on parallel reduction order, single-threaded reduction would drift it — this catches that class).

    Step C "Assert golden tests actually ran (anti-silent-skip)": run each golden target under `--features linalg,parallel` and assert a POSITIVE run count with zero ignored, so a cfg-guard/feature-wiring regression that makes them silently `ignored` fails the step. Use the proven output-line format `test result: ok. N passed; 0 failed; 0 ignored`:
      - `cargo test -p fdars-core --features linalg,parallel --test equivalence_phase48 -- golden_co_cluster` MUST produce a line matching `2 passed` AND that line must NOT contain a nonzero-ignored count. Assert with two greps against a captured log: one confirming `test result: ok. 2 passed` is present, and one confirming NO line matches `[1-9][0-9]* ignored` (i.e. a positive ignored count). Fail the step (exit 1) if the `2 passed` assertion is absent OR a positive-ignored line is present. Emit a sentinel `PHASE48_GOLDENS_RAN` on success.
      - `cargo test -p fdars-core --features linalg,parallel --test equivalence_phase49 -- svd_sign_fpca_two_matrix_bit_identical` MUST produce a line matching `1 passed` with no positive-ignored line; same assertion shape; emit `PHASE49_GOLDEN_RAN` on success.
    Do NOT guard/assert the PACE sibling `svd_sign_pace_eigenfunctions_single_matrix_bit_identical` — per 90-DIAGNOSIS it is intentionally unguarded and runs under every config; it is out of scope for these assertions.

    Keep the YAML minimal and readable; add short `#` comments naming WHY each step exists (intermittency / reduction-order / anti-silent-skip). Do NOT inline any Rust or reproduce other jobs.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/rust-ci.yml')); print('YAML_OK')" && python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/rust-ci.yml')); j=d['jobs']['determinism-guardrail']; assert j['runs-on']=='ubuntu-latest'; assert j['defaults']['run']['working-directory']=='fdars-core'; blob=open('.github/workflows/rust-ci.yml').read(); assert 'RAYON_NUM_THREADS=1' in blob; assert 'linalg,parallel,serde' in blob; assert 'golden_co_cluster' in blob and 'svd_sign_fpca_two_matrix_bit_identical' in blob; assert 'nextest' not in blob and 'serial_test' not in blob; print('JOB_SHAPE_OK')"</automated>
    <fails_when>non-zero exit or missing `YAML_OK`/`JOB_SHAPE_OK` — the workflow YAML is malformed, the `determinism-guardrail` job is absent/mis-scaffolded, the RAYON/feature/golden strings are missing, or a forbidden `nextest`/`serial_test` dependency slipped in.</fails_when>
  </verify>
  <reversibility rating="reversible">Pure additive workflow YAML edit; revert by deleting the job block.</reversibility>
  <done>
    - `.github/workflows/rust-ci.yml` parses as valid YAML.
    - A `determinism-guardrail` job exists with `runs-on: ubuntu-latest`, `working-directory: fdars-core`, `actions/checkout@v4`, `dtolnay/rust-toolchain@stable`, and an `actions/cache@v4` block.
    - The job contains: a 3× repeat loop of `cargo test --features linalg,parallel,serde`; a `RAYON_NUM_THREADS=1` run of the same; and positive-run assertions for the two golden targets (`2 passed`/`1 passed`, zero ignored).
    - No `nextest`/`serial_test`/new-action dependency present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Locally validate the guardrail's exact shell commands against the green Phase-90/91 baseline</name>
  <files>.github/workflows/rust-ci.yml</files>
  <read_first>
    - .github/workflows/rust-ci.yml (the `determinism-guardrail` job added in Task 1 — the shell commands to replay)
    - .planning/phases/90-golden-flake-root-cause-deterministic-fix/90-DIAGNOSIS.md (proven invocations + the expected `N passed; 0 failed; 0 ignored` result-line format)
  </read_first>
  <action>
    There is no local CI runner, so validate the job as a proxy by replaying its EXACT shell commands locally against the current (green) baseline. Run each guardrail command in `fdars-core/` with an explicit 600s tool timeout and confirm green:
    1. The repeat loop: `for i in 1 2 3; do cargo test -p fdars-core --features linalg,parallel,serde 2>&1 | grep -q "test result: FAILED" && { echo "RUN $i FAILED"; exit 1; }; done; echo ALL_3_GREEN` (reuses the Phase-91 proven command verbatim; note it uses `-p fdars-core` locally — CI uses `working-directory: fdars-core` so the `-p` flag is redundant but harmless).
    2. The single-threaded run: `RAYON_NUM_THREADS=1 cargo test -p fdars-core --features linalg,parallel,serde 2>&1 | grep -c "test result: FAILED"` MUST print `0`.
    3. The anti-silent-skip assertions: run the two golden targets and confirm the `test result:` line shows `2 passed; 0 failed; 0 ignored` (phase48) and `1 passed; 0 failed; 0 ignored` (phase49) — the exact format proven at plan time.
    Do NOT modify any Rust or Cargo files. If any command is red, the job YAML or the baseline is wrong — fix the YAML (Task 1) accordingly; the baseline itself is known-green from Phases 90/91. Batch the cargo runs; each `cargo test` invocation gets its own explicit 600s timeout (do not chain all runs in one unbounded background job — fdars cargo runs exceed default stream watchdogs).
  </action>
  <verify>
    <automated>cd fdars-core && RAYON_NUM_THREADS=1 cargo test -p fdars-core --features linalg,parallel,serde 2>&1 | grep -c "test result: FAILED" | grep -qx 0 && cargo test -p fdars-core --features linalg,parallel --test equivalence_phase48 -- golden_co_cluster 2>&1 | grep -Eq "test result: ok\. 2 passed; 0 failed; 0 ignored" && cargo test -p fdars-core --features linalg,parallel --test equivalence_phase49 -- svd_sign_fpca_two_matrix_bit_identical 2>&1 | grep -Eq "test result: ok\. 1 passed; 0 failed; 0 ignored" && echo GUARDRAIL_LOCAL_GREEN</automated>
    <fails_when>non-zero exit or missing `GUARDRAIL_LOCAL_GREEN` — a single-threaded run failed (count != 0), or a golden target did not report the exact `2 passed`/`1 passed` with `0 ignored` line (goldens failed OR were silently ignored), meaning the guardrail would start red in CI.</fails_when>
  </verify>
  <reversibility rating="reversible">Read-only validation (runs tests); no artifact changes beyond any YAML fix already covered by Task 1.</reversibility>
  <done>
    - The 3× repeat loop runs locally with `ALL_3_GREEN` (no `test result: FAILED`).
    - The `RAYON_NUM_THREADS=1` run reports 0 failed test-result lines.
    - Both golden targets report `2 passed; 0 failed; 0 ignored` (phase48) and `1 passed; 0 failed; 0 ignored` (phase49).
    - Conclusion recorded: the CI job's shell will be green against the current baseline.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow → runner shell | Workflow YAML supplies shell commands executed on the GitHub Actions runner. This phase adds only fixed, literal shell (no user/external input interpolated), so no injection surface. |
| repo push/PR → CI trigger | The new job runs on the same push/pull_request/release events as existing jobs; no new secret or permission is used. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-92-01 | Tampering | determinism-guardrail shell steps | low | accept | Steps run only fixed literal cargo/grep commands with no interpolated external input; no `${{ github.event.* }}` values are passed into shell. No mitigation needed beyond avoiding untrusted interpolation. |
| T-92-02 | Denial of Service | CI minutes (repeat loop) | low | mitigate | Repeat count fixed at 3 + one single-threaded run — bounded, small; documented tradeoff (signal vs minutes) in the step comment. |
| T-92-03 | Repudiation | silent-skip regression | medium | mitigate | Positive-run assertions (`2 passed`/`1 passed`, zero ignored) make a golden that never runs fail the gate loudly — the core anti-silent-skip mitigation (the exact Phase-90 surface). |
| T-92-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install tasks in this phase (std-only, no new tooling — locked decision); no `## Package Legitimacy Audit` needed. |
</threat_model>

<artifacts>
## Artifacts this phase produces

- **`.github/workflows/rust-ci.yml` — new `determinism-guardrail` job** containing:
  - A 3× repeat loop of `cargo test --features linalg,parallel,serde` (catches intermittent nondeterminism).
  - A `RAYON_NUM_THREADS=1` single-threaded run of the same suite (catches parallel-reduction-order sensitivity).
  - Positive-run assertions that the three golden tests (`golden_co_cluster` ×2 in `equivalence_phase48`, `svd_sign_fpca_two_matrix_bit_identical` in `equivalence_phase49`) actually RAN under `--features linalg` (each with `0 ignored`) — the anti-silent-skip guard for the exact Phase-90 failure surface.
- No new crate/tooling dependency (no cargo-nextest, no serial_test) — pure shell in the workflow YAML.
</artifacts>

<verification>
- Workflow YAML parses cleanly (`python3 -c "import yaml; yaml.safe_load(open('.github/workflows/rust-ci.yml'))"`).
- The `determinism-guardrail` job is structurally correct (runs-on, working-directory, checkout, toolchain, cache) and contains the repeat loop, the `RAYON_NUM_THREADS=1` run, and the two golden positive-run assertions.
- No forbidden dependency (`nextest`/`serial_test`) present in the file.
- The job's exact shell commands run green locally against the current Phase-90/91 baseline.
</verification>

<success_criteria>
- CI-01 satisfied: a `determinism-guardrail` CI job exercises the full parallel test path with a repeat loop + a single-threaded run, and asserts the three golden tests actually ran under `linalg`.
- A reintroduced determinism regression (real nondeterminism OR silently-skipped goldens) would fail this gate.
- The change is confined to `.github/workflows/rust-ci.yml`; no new tooling; YAML valid; commands green locally.
</success_criteria>

<output>
Create `.planning/phases/92-ci-determinism-guardrail/92-01-SUMMARY.md` when done.
</output>
