---
phase: 28-depth-measure-long-tail
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - fdars-core/src/depth/extremal.rs
  - fdars-core/src/depth/erl.rs
  - fdars-core/src/depth/linf.rs
  - fdars-core/src/depth/mod.rs
  - fdars-core/src/depth/dispatch.rs
  - fdars-core/src/lib.rs
autonomous: true
requirements: [DEPTH-01]
estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "extremal_depth_1d, extreme_rank_length_depth_1d, linfinity_depth_1d each return Result<Vec<f64>, FdarError> over a column-major FdMatrix (per DEPTH-01)"
    - "functional_depth(data, DepthMethod::Extremal|ExtremeRankLength|LInfinity) dispatches to the new functions; existing variants + signature unchanged"
    - "On synthetic data with a central curve plus injected outliers, each of extremal / ERL / L∞ ranks the central curve deepest and outliers shallowest (within tolerance) in inline tests"
    - "Extremal depth ordering is deterministic — tied (d_level, mass) values break by original index (stable tie-break), asserted with a known tied input"
    - "L∞ depth ranks the curve closest to the sample (smallest mean sup-norm distance) deepest"
    - "Empty matrix returns FdarError for every new entry point; extremal and TVD-style n<3 guard, ERL n<2 guard return FdarError (never panic)"
    - "No new crate dependency added; cargo clippy --all-targets --features linalg,parallel -- -D warnings stays green"
  artifacts:
    - fdars-core/src/depth/extremal.rs
    - fdars-core/src/depth/erl.rs
    - fdars-core/src/depth/linf.rs
  key_links:
    - "depth/mod.rs + lib.rs re-export the 3 functions; dispatch.rs enum + match wire Extremal/ExtremeRankLength/LInfinity variants"
---

<objective>
Add the three independent fdaoutlier rank/distance depth measures: extremal depth (`extremal.rs`, rank-ordering), extreme-rank-length depth (`erl.rs`, lexicographic rank-vector ordering), and L∞ depth (`linf.rs`, pairwise sup-norm inversion). Each fully wired as a `DepthMethod` variant + crate-root re-export.

The additive integration path was proven end-to-end in wave 1; these three follow the same pattern. They share no measure files, but all extend the same three wiring files (`mod.rs`, `dispatch.rs`, `lib.rs`), so they land together in this wave (co-located wiring keeps the crate compiling).

Purpose: Match fdaoutlier's rank/distance depth catalog (DEPTH-01).
Output: `extremal.rs`, `erl.rs`, `linf.rs` (1 fn each), 3 new DepthMethod variants + arms, mod.rs + lib.rs re-exports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-depth-measure-long-tail/28-CONTEXT.md
@.planning/phases/28-depth-measure-long-tail/28-RESEARCH.md
@.planning/phases/28-depth-measure-long-tail/28-PATTERNS.md
@fdars-core/src/depth/band.rs
@fdars-core/src/depth/dispatch.rs
@fdars-core/src/depth/mod.rs
</context>

<artifacts_produced>
## Artifacts this plan produces
- `extremal_depth_1d(&FdMatrix, &FdMatrix) -> Result<Vec<f64>, FdarError>` (extremal.rs) + private `column_ranks` helper
- `extreme_rank_length_depth_1d(&FdMatrix, &FdMatrix) -> Result<Vec<f64>, FdarError>` (erl.rs) + private `column_ranks` helper
- `linfinity_depth_1d(&FdMatrix, &FdMatrix) -> Result<Vec<f64>, FdarError>` (linf.rs)
- DepthMethod variants: `Extremal`, `ExtremeRankLength`, `LInfinity`
- files: extremal.rs, erl.rs, linf.rs
(See 28-01-PLAN.md for the full phase artifact list.)
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: extremal.rs — extremal depth (pointwise-depth min-level + mass ordering)</name>
  <files>fdars-core/src/depth/extremal.rs, fdars-core/src/depth/mod.rs, fdars-core/src/depth/dispatch.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/depth/dispatch.rs (sort-with-tie-breaker pattern `partial_cmp(...).unwrap_or(Equal).then(a.cmp(&b))`; Band n<2 guard arm to mirror for the n<3 guard)
    - 28-RESEARCH.md §"Measure Definitions" item 7 (extremal depth 4-step algorithm) + Code Examples Pattern 3 + Pattern 6 (column_ranks helper) + Common Pitfall 3 (stable sort)
    - 28-PATTERNS.md §"src/depth/extremal.rs" (sequential O(n·m) pattern, sort tie-breaker, n≥3 guard) + §"Pointwise rank with tie-averaging helper"
  </read_first>
  <behavior>
    - Test: central curve gets depth n/n (deepest); an extreme magnitude outlier gets depth 1/n (shallowest).
    - Test: with a deliberately tied (d_level, mass) pair, the ordering is deterministic across runs (tie-break by original index) — same output on repeated calls.
    - Test: depths lie in (0, 1]; the deepest equals 1.0.
    - Test: empty matrix → Err; n<3 → Err (never panic).
  </behavior>
  <action>
    Create `fdars-core/src/depth/extremal.rs` with module doc and imports `use crate::matrix::FdMatrix; use crate::error::FdarError;` (sequential — no `iter_maybe_parallel!`; extremal is O(n·m + n log n)).

    Add a private `fn column_ranks(data: &FdMatrix, col: usize) -> Vec<f64>` (1-based average-tie ranks matching R `rank(ties.method="average")`, per RESEARCH Pattern 6): collect `(value, idx)`, `sort_unstable_by` with `partial_cmp(...).unwrap_or(Equal)`, walk tie groups assigning `(k+1 + j)/2` average rank.

    Implement `#[must_use] pub fn extremal_depth_1d(data_obj: &FdMatrix, data_ori: &FdMatrix) -> Result<Vec<f64>, FdarError>`. Use `let (n, m) = (data_ori.nrows(), data_ori.ncols());`. Entry guard: `n == 0 || m == 0` → InvalidDimension. n<3 guard → InvalidDimension (expected "at least 3 curves for extremal depth"), mirroring the Band guard.
      Step 1: for each column t, `column_ranks`, then pointwise depth `D[i][t] = 1.0 - (2.0*rank - n as f64 - 1.0).abs() / n as f64`.
      Step 2: `d_level[i] = min_t D[i][t]`; `mass[i] = (#{t: (D[i][t]-d_level[i]).abs() < eps}) as f64 / m as f64`.
      Step 3: sort an index vec `0..n` by `d_level` ascending, then `mass` descending, then original index ascending — use `partial_cmp(...).unwrap_or(Equal).then(...).then(a.cmp(&b))` (Pitfall 3: deterministic).
      Step 4: `depth[order[k]] = (k + 1) as f64 / n as f64`; return `Ok(depth)`.

    WIRE: mod.rs `pub mod extremal;` + `pub use extremal::extremal_depth_1d;`. dispatch.rs: add `extremal_depth_1d` to import; add `Extremal` variant (`/// Extremal depth (Narisetty & Nair 2016). Requires n ≥ 3.`); add match arm with `if n < 3` InvalidDimension guard calling `extremal_depth_1d(data, data)?`. lib.rs: add `extremal_depth_1d,` to the depth re-export block.

    Inline `#[cfg(test)] mod tests` implementing the behaviors.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel depth::extremal</automated>
  </verify>
  <acceptance_criteria>
    - Signature `pub fn extremal_depth_1d(&FdMatrix, &FdMatrix) -> Result<Vec<f64>, FdarError>` present; private `column_ranks` present.
    - `DepthMethod::Extremal` dispatches via `functional_depth` with an n<3 guard.
    - Inline test asserts central curve depth == 1.0 and outlier depth == 1/n, plus deterministic tie ordering.
    - `cargo test -p fdars-core --features linalg,parallel depth::extremal` exits 0.
    - `cargo clippy --all-targets --features linalg,parallel -- -D warnings` green.
  </acceptance_criteria>
  <done>Extremal depth computes correct rank-ordered depths deterministically, dispatches through DepthMethod::Extremal, and is crate-root re-exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: erl.rs — extreme-rank-length depth (lexicographic rank-vector ordering)</name>
  <files>fdars-core/src/depth/erl.rs, fdars-core/src/depth/mod.rs, fdars-core/src/depth/dispatch.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/depth/band.rs (`iter_maybe_parallel!` outer-loop pattern to mirror for the O(n²·m) pairwise comparison)
    - 28-RESEARCH.md §"Measure Definitions" item 8 (ERL 5-step algorithm) + Code Examples Pattern 4 + Pattern 6 (column_ranks) + Common Pitfall 4 (depth-assignment off-by-one)
    - 28-PATTERNS.md §"src/depth/erl.rs" (O(n²) parallel outer loop, lexicographic comparison via zip/find)
  </read_first>
  <behavior>
    - Test: the least-extreme (central) curve is not more extreme than any other → highest ERL depth.
    - Test: an outlier is more extreme than nearly all others → ERL depth near 0.
    - Test: depths in (0, 1]; deepest == 1.0 (or 1 - 0/n per the chosen normalization).
    - Test: empty matrix → Err; n<2 → Err.
  </behavior>
  <action>
    Create `fdars-core/src/depth/erl.rs` with module doc and imports `use crate::iter_maybe_parallel; use crate::matrix::FdMatrix; use crate::error::FdarError;` + `#[cfg(feature = "parallel")] use rayon::iter::ParallelIterator;`.

    Add the private `column_ranks` helper (same as extremal.rs — define per-file per PATTERNS.md decision to avoid coupling).

    Implement `#[must_use] pub fn extreme_rank_length_depth_1d(data_obj: &FdMatrix, data_ori: &FdMatrix) -> Result<Vec<f64>, FdarError>`. `let (n, m) = (data_ori.nrows(), data_ori.ncols());`. Entry guard + n<2 guard → FdarError.
      Step 1: for each column t, `column_ranks` → `r[i][t]` in 1..=n.
      Step 2: two-sided transform `R[i][t] = f64::min(r[i][t], (n + 1) as f64 - r[i][t])`.
      Step 3: for each i, `sorted_r[i]` = ascending sort of the transformed rank vector (length m).
      Step 4 (O(n²·m), parallel outer i): count j where curve i is NOT more extreme than j; i is more extreme than j iff at the first lex position where `sorted_r[i]` and `sorted_r[j]` differ (`abs diff > EPSILON`), `sorted_r[i] < sorted_r[j]` (via `.iter().zip(...).find(...).map(...).unwrap_or(false)`). `depth[i] = not_more_extreme_count as f64 / n as f64`. Mind Pitfall 4 off-by-one — a curve counts itself in `not_more_extreme` (i vs i is not more extreme), giving deepest == 1.0.

    WIRE: mod.rs `pub mod erl;` + `pub use erl::extreme_rank_length_depth_1d;`. dispatch.rs: import + `ExtremeRankLength` variant + match arm with `if n < 2` guard calling `extreme_rank_length_depth_1d(data, data)?`. lib.rs: add `extreme_rank_length_depth_1d,`.

    Inline `#[cfg(test)] mod tests`.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel depth::erl</automated>
  </verify>
  <acceptance_criteria>
    - Signature `pub fn extreme_rank_length_depth_1d(&FdMatrix, &FdMatrix) -> Result<Vec<f64>, FdarError>` present.
    - `DepthMethod::ExtremeRankLength` dispatches via `functional_depth` with an n<2 guard.
    - Inline test asserts central curve deepest (ERL == 1.0) and outlier shallowest.
    - `cargo test -p fdars-core --features linalg,parallel depth::erl` exits 0.
    - `cargo clippy --all-targets --features linalg,parallel -- -D warnings` green.
  </acceptance_criteria>
  <done>ERL depth computes correct lexicographic rank-vector depths, dispatches through DepthMethod::ExtremeRankLength, and is crate-root re-exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: linf.rs — L∞ depth (pairwise sup-norm distance inversion) + dispatcher</name>
  <files>fdars-core/src/depth/linf.rs, fdars-core/src/depth/mod.rs, fdars-core/src/depth/dispatch.rs, fdars-core/src/lib.rs</files>
  <read_first>
    - fdars-core/src/depth/band.rs (MEI `iter_maybe_parallel!` loop — identical outer structure, different inner)
    - 28-RESEARCH.md §"Measure Definitions" item 9 (L∞ formula `1/(1+mean_j max_t |Xi-Xj|)`) + assumption A4 (n=1 → depth 1.0, no n≥2 guard)
    - 28-PATTERNS.md §"src/depth/linf.rs" (core loop, n≥1 guard — no n≥2 guard)
  </read_first>
  <behavior>
    - Test: the curve closest to the sample (smallest mean sup-norm distance) gets the highest L∞ depth.
    - Test: a far magnitude outlier gets a lower L∞ depth than the central curve.
    - Test: depths in (0, 1]; monotonically decreasing in mean L∞ distance.
    - Test: empty matrix → Err; n=1 self-depth returns Ok([1.0]) (well-defined, no n<2 guard).
  </behavior>
  <action>
    Create `fdars-core/src/depth/linf.rs` with module doc and imports matching erl.rs (`iter_maybe_parallel` + conditional rayon).

    Implement `#[must_use] pub fn linfinity_depth_1d(data_obj: &FdMatrix, data_ori: &FdMatrix) -> Result<Vec<f64>, FdarError>`. `let (nobj, nori, m) = (data_obj.nrows(), data_ori.nrows(), data_obj.ncols());`. Entry guard only: `nobj == 0 || nori == 0 || m == 0` → InvalidDimension. NO n<2 guard (A4: valid for n=1). Per object i (parallel): accumulate `mean_dist += max_t |data_obj[(i,t)] - data_ori[(j,t)]|` over j, `/= nori`, depth `= 1.0 / (1.0 + mean_dist)`. Rustdoc: formula, `[CITED: fdaoutlier::linfinity_depth]`, note it is average sup-norm distance to ALL curves (not to the pointwise median).

    WIRE: mod.rs `pub mod linf;` + `pub use linf::linfinity_depth_1d;`. dispatch.rs: import + `LInfinity` variant (`/// L-infinity depth (fdaoutlier). Valid for n ≥ 1.`) + match arm with NO extra guard calling `linfinity_depth_1d(data, data)?`. lib.rs: add `linfinity_depth_1d,`.

    Inline `#[cfg(test)] mod tests` implementing the behaviors (include the n=1 → [1.0] case since self-depth of one curve against itself has mean_dist 0).
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel depth::linf</automated>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel depth::</automated>
  </verify>
  <acceptance_criteria>
    - Signature `pub fn linfinity_depth_1d(&FdMatrix, &FdMatrix) -> Result<Vec<f64>, FdarError>` present.
    - `DepthMethod::LInfinity` dispatches via `functional_depth` (no n<2 guard).
    - Inline test asserts closest-to-sample curve deepest and n=1 self-depth returns [1.0].
    - `cargo test -p fdars-core --features linalg,parallel depth::linf` exits 0.
    - `cargo test -p fdars-core --features linalg,parallel depth::` (whole module, incl. wave-1 measures) exits 0.
    - `cargo clippy --all-targets --features linalg,parallel -- -D warnings` green.
  </acceptance_criteria>
  <done>L∞ depth computes correct sup-norm-inversion depths, dispatches through DepthMethod::LInfinity, is crate-root re-exported, and the whole depth module (waves 1+2) passes green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → depth fn | Untrusted `FdMatrix` dimensions cross here; validated at entry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-03 | Denial of Service | extremal/erl/linf entry points | low | mitigate | Empty / min-n guard returns FdarError before indexing (no panic) |
| T-28-04 | Tampering | f64 sort in column_ranks / extremal ordering | low | mitigate | `partial_cmp(...).unwrap_or(Ordering::Equal)` (NaN-safe) + index tie-break for determinism |
| T-28-05 | Tampering | rank normalization division by n | low | mitigate | n==0 guarded at entry; division by nori guarded by non-empty check |
| T-28-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages added this phase |
</threat_model>

<verification>
- `cargo test -p fdars-core --features linalg,parallel depth::` exits 0 (extremal, erl, linf, dispatch + wave-1 measures).
- `cargo clippy --all-targets --features linalg,parallel -- -D warnings` green.
- No new dependency in `fdars-core/Cargo.toml`.
</verification>

<success_criteria>
- 3 new functions (extremal / ERL / L∞) callable, `Result`-returning, crate-root re-exported.
- 3 new DepthMethod variants dispatch correctly; existing variants + signature unchanged.
- Central curve deepest / outliers shallowest on synthetic data for each measure.
- Error paths return FdarError, never panic; extremal ordering deterministic.
- Full suite + clippy green.
</success_criteria>

<output>
Create `.planning/phases/28-depth-measure-long-tail/28-02-SUMMARY.md` when done
</output>
