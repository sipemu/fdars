---
phase: 50-additive-api-surface-consolidation
plan: 03
type: execute
wave: 3
depends_on: [50-02]
files_modified:
  - fdars-core/src/dim.rs
  - fdars-core/src/lib.rs
  - fdars-core/src/depth/mod.rs
  - fdars-core/src/depth/modal.rs
  - fdars-core/src/depth/fraiman_muniz.rs
  - fdars-core/src/depth/random_projection.rs
  - fdars-core/src/depth/random_tukey.rs
  - fdars-core/src/depth/tests.rs
  - fdars-core/src/fdata.rs
  - fdars-core/src/regression.rs
  - fdars-core/tests/validate_against_r.rs
  - fdars-core/tests/equivalence_phase50.rs
autonomous: true
requirements: [API-01, API-02, API-03]
estimate:
  tokens: 66000
  raw_tokens: 33000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "A shared `Dim` enum (Dim::One / Dim::Two, #[non_exhaustive]) exists in src/dim.rs and is re-exported at the crate root, shared by the depth and fdata dispatchers (API-02)"
    - "Exactly 5 unified `Dim`-dispatchers ship: depth::modal, depth::fraiman_muniz, depth::random_projection, depth::random_tukey, and fdata::mean — each `fn name(<same args>, dim: Dim)` that forwards to the `_1d` primitive for both arms (the `_2d` path never diverged); each equals its `_1d`/`_2d` outputs (API-02)"
    - "ONLY the 5 redundant `_2d` shims are `#[deprecated(since=\"0.30.0\")]` (modal_2d, fraiman_muniz_2d, random_projection_2d, random_tukey_2d, mean_2d); NO `_1d` is ever deprecated (workhorse, called by depth/dispatch.rs); regression, deriv, geometric_median, functional_spatial, band, and genuine `_nd` algorithms are UNTOUCHED (API-02)"
    - "Item #3 is DOC-ONLY: a rustdoc note on the canonical FpcaResult vocabulary (scores/rotation/mean/weights/singular_values/centered) + `fitted` accepted; NO field rename (API-01)"
  artifacts:
    - fdars-core/src/dim.rs
    - fdars-core/tests/equivalence_phase50.rs
  key_links:
    - "src/dim.rs::Dim → re-exported at crate root → used by depth::{modal,fraiman_muniz,random_projection,random_tukey} + fdata::mean"
    - "each unified dispatcher → forwards to its `_1d` primitive (both Dim arms); `_2d` shim deprecated as redundant"
    - "equivalence_phase50.rs dispatch-equality asserts ← modal(a,b,h,Dim::One)==modal_1d(a,b,h), ==modal_2d (with #[allow(deprecated)]), etc. for all 5 pairs"
    - "depth/dispatch.rs calls `_1d` only → stays warning-free with ZERO #[allow(deprecated)] churn (verify)"
---

<objective>
Item #4 — unified `Dim` dispatch for the cleanly-unifiable `_1d`/`_2d` pairs, plus item #3 (doc-only).
Add a shared `Dim` enum (`src/dim.rs`, re-exported at the crate root, mirroring the crate's existing
`DepthMethod` dispatcher idiom in depth/dispatch.rs) and exactly 5 unified dispatchers, then
`#[deprecated]` ONLY the 5 redundant `_2d` shims. This reuses the `#[deprecated]`-under-`-D warnings`
hygiene discipline established in plan 50-02.

CRITICAL SCOPE (RESEARCH item #4 — do NOT re-expand; the "unify 30+ fns" framing is wrong): reading every
signature this session, the cleanly-unifiable set is small and honest:
  - depth: modal, fraiman_muniz, random_projection, random_tukey — 4 pairs where `_2d` is a thin shim with
    an IDENTICAL signature that already just calls `_1d`. functional_spatial + kernel_functional_spatial are
    EXCLUDED (arity mismatch: `_1d` takes `argvals`, `_2d` does not). band/modified_band/extremal/etc. have
    no `_2d` (nothing to unify).
  - fdata: mean — 1 pair (mean_2d body is literally `mean_1d(data)`). deriv (3 vs 5 args, different return)
    and geometric_median (4 vs 5 args) are EXCLUDED — genuinely divergent, LEAVE ALONE.
  - regression: ZERO pairs — only `_1d` exists, no `_2d` counterpart. DO NOTHING.
  => exactly 5 unified dispatchers + 5 deprecated `_2d` shims.
NEVER deprecate any `_1d` (the workhorse primitive, called directly by depth/dispatch.rs and user code).
NEVER touch the genuine `_nd` ALGORITHMS (pca_nd, karcher_mean_nd, karcher_covariance_nd, srsf_transform_nd,
srsf_inverse_nd) — they are different multivariate/shape algorithms, not dimension conveniences.

NOTE (scope reconciliation): the RESEARCH prose summary says "6 pairs (5 depth + 1 fdata)", but its own
per-family table enumerates 4 clean depth pairs (functional_spatial is PARTIAL/excluded). The
source-verified count is 5 dispatchers = 4 depth + 1 fdata. This plan follows the authoritative table.

Purpose: give one ergonomic `name(…, dim: Dim)` entry point per unifiable pair and retire the redundant
`_2d` shims, without deprecating any primitive. Output: src/dim.rs + 5 dispatchers + 5 `_2d` deprecations
+ dispatch-equality goldens + the item-#3 doc note, all green under both configs, examples, wasm, -D warnings.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/50-additive-api-surface-consolidation/50-CONTEXT.md
@.planning/phases/50-additive-api-surface-consolidation/50-RESEARCH.md
@fdars-core/src/depth/dispatch.rs
@fdars-core/src/depth/mod.rs
@fdars-core/src/fdata.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add src/dim.rs (Dim enum) + 5 unified dispatchers; extend equivalence_phase50.rs with dispatch-equality goldens</name>
  <files>fdars-core/src/dim.rs, fdars-core/src/lib.rs, fdars-core/src/depth/modal.rs, fdars-core/src/depth/fraiman_muniz.rs, fdars-core/src/depth/random_projection.rs, fdars-core/src/depth/random_tukey.rs, fdars-core/src/depth/mod.rs, fdars-core/src/fdata.rs, fdars-core/tests/equivalence_phase50.rs</files>
  <read_first>
    - fdars-core/src/depth/dispatch.rs (the DepthMethod/functional_depth match idiom to mirror stylistically)
    - fdars-core/src/depth/modal.rs (modal_1d:17, modal_2d:44 — identical sig `(data_obj,&FdMatrix, data_ori,&FdMatrix, h:f64) -> Vec<f64>`)
    - fdars-core/src/depth/fraiman_muniz.rs (fraiman_muniz_1d:32/_2d:43 — `(…, scale: bool)`); random_projection.rs (_1d:32/_2d:57 — `(…, nproj: usize)`); random_tukey.rs (_1d:11/_2d:36 — `(…, nproj: usize)`)
    - fdars-core/src/fdata.rs (mean_1d:167, mean_2d:184 — `(data: &FdMatrix) -> Vec<f64>`)
    - fdars-core/src/lib.rs (the `mod` declaration region — add `pub mod dim;` alphabetically; and the crate-root re-export lines for depth/fdata names)
  </read_first>
  <behavior>
    - modal(a, b, h, Dim::One) == modal_1d(a, b, h)  AND  == modal_2d(a, b, h)
    - fraiman_muniz(a, b, s, Dim::Two) == fraiman_muniz_1d(a, b, s)  AND  == fraiman_muniz_2d(a, b, s)
    - random_projection(a, b, n, Dim::One) == random_projection_1d(a, b, n)  (deterministic seed path — reuse the same fixture/seed the existing depth tests use so the RNG-projection output is reproducible)
    - random_tukey(a, b, n, Dim::Two) == random_tukey_1d(a, b, n)
    - mean(data, Dim::One) == mean_1d(data)  AND  == mean_2d(data)
  </behavior>
  <action>
    Create src/dim.rs with a tiny shared enum (mirror the RESEARCH snippet):

      /// Dimensionality selector for the unified depth/fdata dispatchers.
      #[derive(Debug, Clone, Copy, PartialEq, Eq)]
      #[non_exhaustive]
      pub enum Dim { /// 1D functional data (curves).
                     One,
                     /// 2D functional data (surfaces, flattened column-major).
                     Two }

    Add `pub mod dim;` to lib.rs (alphabetical) and re-export `Dim` at the crate root so depth and fdata
    share one type. Then add exactly 5 unified dispatchers, each `#[must_use]`, each forwarding to its
    `_1d` primitive for BOTH Dim arms (the `_2d` path never diverged, so `Dim::One | Dim::Two => name_1d(…)`;
    the `dim` param makes intent explicit and gives a single future seam if 2D ever diverges):
      - src/depth/modal.rs:            pub fn modal(data_obj, data_ori, h: f64, dim: Dim) -> Vec<f64>
      - src/depth/fraiman_muniz.rs:    pub fn fraiman_muniz(data_obj, data_ori, scale: bool, dim: Dim) -> Vec<f64>
      - src/depth/random_projection.rs:pub fn random_projection(data_obj, data_ori, nproj: usize, dim: Dim) -> Vec<f64>
      - src/depth/random_tukey.rs:     pub fn random_tukey(data_obj, data_ori, nproj: usize, dim: Dim) -> Vec<f64>
      - src/fdata.rs:                  pub fn mean(data: &FdMatrix, dim: Dim) -> Vec<f64>
    Import `crate::dim::Dim` in each file. Export the 5 new names from src/depth/mod.rs (add to the existing
    `pub use module::{…}` lines) and re-export at the crate root in lib.rs alongside the existing depth/fdata
    names (do NOT remove the `_1d`/`_2d` names — they stay exported). Each dispatcher body only forwards to
    `_1d`, so it does NOT reference the (soon-to-be-deprecated in Task 2) `_2d` — no `#[allow(deprecated)]`
    needed in the dispatcher bodies.

    Extend equivalence_phase50.rs with dispatch-equality tests: for each of the 5 pairs assert
    `assert_eq!(unified(…, Dim::One), name_1d(…))`. These do NOT yet reference `_2d`, so no
    `#[allow(deprecated)]` needed here yet (the `==_2d` assert is added in Task 2 after deprecation, with the
    allow). Use the SAME deterministic fixture/seed as the existing depth `*_2d_delegates` tests so RNG paths
    (random_projection/random_tukey) are reproducible. Run `cargo fmt -p fdars-core`; commit `--no-verify`.
    Prefix every cargo command with `TMPDIR=/home/simonm/.cache/fdars-bench-tmp`.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --test equivalence_phase50 dispatch && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --features linalg,parallel && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    src/dim.rs exists with `Dim`, re-exported at crate root. The 5 unified dispatchers compile and their
    `Dim::One` outputs assert_eq! their `_1d` outputs. `_1d`/`_2d` names still exported. Crate builds; clippy
    --all-targets -D warnings clean (no deprecation yet).
  </acceptance_criteria>
  <done>Dim enum + 5 unified dispatchers shipped and proven equal to their _1d primitives; nothing deprecated yet.</done>
  <reversibility rating="reversible">Additive module + 5 fns; revert deletes them.</reversibility>
</task>

<task type="auto">
  <name>Task 2: #[deprecated] the 5 `_2d` shims + hygiene (allow the in-crate test/validate callers); add ==_2d equality goldens</name>
  <files>fdars-core/src/depth/modal.rs, fdars-core/src/depth/fraiman_muniz.rs, fdars-core/src/depth/random_projection.rs, fdars-core/src/depth/random_tukey.rs, fdars-core/src/fdata.rs, fdars-core/src/depth/tests.rs, fdars-core/tests/validate_against_r.rs, fdars-core/tests/equivalence_phase50.rs</files>
  <read_first>
    - the 5 `_2d` fn definitions (modal.rs:44, fraiman_muniz.rs:43, random_projection.rs:57, random_tukey.rs:36, fdata.rs:184)
    - fdars-core/src/depth/dispatch.rs (VERIFY it imports/uses only `_1d` forms — random_projection_1d_seeded, fraiman_muniz_1d, etc. — so it stays warning-free with zero #[allow]; if it does reference any `_2d`, add #[allow(deprecated)] there too)
    - in-crate `_2d` callers to silence (from this session's grep):
        modal_2d → src/depth/tests.rs:336 (test_modal_2d_delegates)
        fraiman_muniz_2d → src/depth/tests.rs:326 (test_fraiman_muniz_2d_delegates), tests/validate_against_r.rs:2284
        random_projection_2d → src/depth/tests.rs:355 (test_random_projection_2d_returns_valid)
        random_tukey_2d → (no in-crate caller found — re-exports only; verify with grep)
        mean_2d → src/fdata.rs:1080 (test_mean_2d_delegates), tests/validate_against_r.rs:2515 and :2518
      (lib.rs / prelude.rs / depth/mod.rs `pub use` lines are re-exports and do NOT emit deprecation warnings — leave them.)
  </read_first>
  <action>
    Attach `#[deprecated(since = "0.30.0", note = "redundant with `<unified>(…, Dim::Two)`; body just \
    forwards to `<name>_1d`")]` to each of the 5 `_2d` shims (modal_2d, fraiman_muniz_2d,
    random_projection_2d, random_tukey_2d, mean_2d). Do NOT change their bodies or signatures — they keep
    forwarding to `_1d` (their call target is not deprecated, so the shim body needs no #[allow]). Do NOT
    deprecate any `_1d`.

    Then silence every in-crate `_2d` caller so `clippy --all-targets -D warnings` stays green (reusing the
    50-02 hygiene discipline). Put `#[allow(deprecated)]` on the SPECIFIC test fn (not a blanket module allow):
      - src/depth/tests.rs: test_modal_2d_delegates, test_fraiman_muniz_2d_delegates,
        test_random_projection_2d_returns_valid (and any other test fn a grep shows calling a deprecated `_2d`).
      - src/fdata.rs: test_mean_2d_delegates.
      - tests/validate_against_r.rs: the test fn(s) wrapping :2284 (fraiman_muniz_2d) and :2515/:2518 (mean_2d).
    First run `grep -rn '<symbol>' fdars-core/src fdars-core/tests fdars-core/examples` for each of the 5
    `_2d` symbols to produce the EXACT current caller set (line numbers may have shifted) and annotate each
    enclosing test fn once. Confirm depth/dispatch.rs uses only `_1d` (expected: no #[allow] needed there).

    Extend equivalence_phase50.rs: for each of the 5 pairs add the `assert_eq!(unified(…, Dim::Two),
    name_2d(…))` check, and put `#[allow(deprecated)]` on those dispatch-equality test fn(s) (they now
    reference the deprecated `_2d`). Run `cargo fmt -p fdars-core`; commit `--no-verify`. Prefix every cargo
    command with `TMPDIR=/home/simonm/.cache/fdars-bench-tmp`.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel --test equivalence_phase50 dispatch && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel depth && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    The 5 `_2d` shims are `#[deprecated(since="0.30.0")]` (bodies unchanged); no `_1d` is deprecated. Every
    in-crate `_2d` caller is `#[allow(deprecated)]` on its specific test fn; depth/dispatch.rs confirmed
    warning-free (uses `_1d`). equivalence_phase50.rs asserts unified(…,Dim::Two)==name_2d(…) for all 5.
    clippy --all-targets -D warnings clean.
  </acceptance_criteria>
  <done>5 `_2d` shims deprecated; all in-crate callers allowed; ==_2d goldens green; -D warnings clean.</done>
  <reversibility rating="reversible">Attributes + allows only; revert removes the deprecations.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Item #3 doc-only canonical-vocabulary note; full phase gate (both configs + 28 examples + wasm + fmt)</name>
  <files>fdars-core/src/regression.rs</files>
  <read_first>
    - fdars-core/src/regression.rs:1-3 (module doc `//!` header — where the canonical-vocabulary note goes) and the FpcaResult struct (fields scores/rotation/mean/weights/singular_values/centered)
  </read_first>
  <action>
    Item #3 is DOC-ONLY (field renames are breaking → deferred to APIB-01). Add a short rustdoc note to the
    regression.rs module-level `//!` header (or as a `///` note on FpcaResult) documenting the canonical FPCA
    result vocabulary — `scores`, `rotation`, `mean`, `weights`, `singular_values`, `centered` — and stating
    that `fitted` (on FtsmResult / BoostFosrResult / FosrResult / FosrFpcResult) is the accepted response-field
    name across result structs, and that the `FpcPredictor` trait (explain_generic) already provides the
    cross-model unification layer so no field rename is needed. Optionally add `#[doc(alias = "fitted_values")]`
    on the `fitted` fields ONLY if trivially additive and it does not require touching multiple modules — this
    is a nice-to-have, not required; the doc note is the deliverable. Do NOT rename any field.

    Then run the FULL phase gate to close out Phase 50: both feature configs green, all 28 examples build,
    wasm compiles, clippy --all-targets -D warnings clean, fmt clean. Prefix every cargo command with
    `TMPDIR=/home/simonm/.cache/fdars-bench-tmp`. If the 28-example build fails with a LINK / disk error, run
    `rm -rf target/debug/{incremental,examples}` and retry. Run `cargo fmt -p fdars-core`; commit `--no-verify`.
  </action>
  <verify>
    <automated>TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --features linalg,parallel && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo test -p fdars-core --no-default-features --features linalg && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --examples --features linalg,parallel && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo build -p fdars-core --target wasm32-unknown-unknown --features js --no-default-features && TMPDIR=/home/simonm/.cache/fdars-bench-tmp cargo clippy --all-targets --features linalg,parallel -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    regression.rs carries the canonical-vocabulary doc note (no field renamed). Full suite green under BOTH
    --features linalg,parallel AND --no-default-features --features linalg. All 28 examples build. wasm
    compiles. clippy --all-targets -D warnings clean. fmt clean; committed --no-verify. No new dependency.
  </acceptance_criteria>
  <done>Item #3 doc note landed; whole-phase both-config + examples + wasm + -D-warnings gate green.</done>
  <reversibility rating="reversible">Doc-only + gate run; revert deletes the note.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Additive enum + dispatchers + deprecation attributes + doc note; no I/O, no untrusted input, no new dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-06 | Tampering | unified dispatcher output vs _1d/_2d | medium | mitigate | equivalence_phase50.rs asserts unified(…,Dim::One)==_1d AND unified(…,Dim::Two)==_2d for all 5 pairs (deterministic fixture/seed for RNG paths) |
| T-50-07 | Elevation of Privilege | accidental deprecation of a `_1d` workhorse | high | mitigate | plan deprecates ONLY the 5 `_2d` shims; task read_first + acceptance_criteria explicitly forbid deprecating any `_1d`; depth/dispatch.rs (calls `_1d`) verified warning-free |
| T-50-08 | Denial of Service | -D warnings false-red from deprecated `_2d` callers | medium | mitigate | every in-crate `_2d` caller `#[allow(deprecated)]` on its specific test fn; per-symbol grep confirms the exact set; clippy --all-targets gate enforces |
| T-50-09 | Denial of Service | 28-example build fills /home (target/) | medium | mitigate | TMPDIR prefix; `rm -rf target/debug/{incremental,examples}` retry per MEMORY |
</threat_model>

<verification>
- equivalence_phase50.rs dispatch goldens: unified(…,Dim::One)==_1d AND unified(…,Dim::Two)==_2d (assert_eq!) for all 5 pairs, under --features linalg,parallel.
- Full suite green: `--features linalg,parallel` AND `--no-default-features --features linalg`.
- All 28 examples build; wasm32-unknown-unknown compiles with --features js.
- `clippy --all-targets --features linalg,parallel -- -D warnings` clean (no deprecated-use errors; no `_1d` deprecated).
- `cargo fmt -p fdars-core` clean; commits via `git commit --no-verify`.
- No field renamed (item #3 doc-only); genuine `_nd` algorithms untouched; regression/deriv/geometric_median/functional_spatial untouched; no new dependency.
</verification>

<success_criteria>
- src/dim.rs with `Dim` (Dim::One/Dim::Two) exists and is re-exported at the crate root.
- 5 unified dispatchers (depth::modal/fraiman_muniz/random_projection/random_tukey, fdata::mean) ship and equal their _1d/_2d outputs.
- Only the 5 `_2d` shims are `#[deprecated(since="0.30.0")]`; no `_1d` deprecated; _nd algorithms untouched.
- Item #3 canonical-vocabulary doc note added; no field rename.
- Both feature configs + 28 examples + wasm + clippy --all-targets -D warnings green (whole-phase gate).
</success_criteria>

<output>
Create `.planning/phases/50-additive-api-surface-consolidation/50-03-SUMMARY.md` when done, mirroring the
Phase-49 SUMMARY format (frontmatter with provides/affects/key-files/key-decisions/coverage). Record:
(1) the source-verified scope reconciliation — 5 dispatchers (4 depth + 1 fdata), NOT 6, because
functional_spatial is arity-excluded; (2) that only the 5 `_2d` shims were deprecated and NO `_1d`
(depth/dispatch.rs verified warning-free); (3) that item #3 was doc-only (no field rename); (4) that the
whole-phase both-config + 28-example + wasm + -D-warnings gate is green, closing API-01/02/03.
</output>
