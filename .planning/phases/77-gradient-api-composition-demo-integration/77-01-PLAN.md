---
phase: "77"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/src/autodiff.rs
  - fdars-core/src/lib.rs
  - fdars-core/src/prelude.rs
  - fdars-core/tests/autodiff_reexports.rs
autonomous: true
requirements: [DIF-04]
estimate:
  tokens: 70000
  raw_tokens: 47000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "grad(f, x) returns (value, gradient) where gradient.len() == x.len(); on Sum(xi^2) it matches 2*xi to <=1e-12 (SC #1, DIF-04)"
    - "A composed objective over >=2 Phase-76 differentiable ops has its grad-gradient matching central finite differences <=1e-6 on spanning full-rank curves (SC #2, DIF-04)"
    - "The full differentiable surface (Scalar, Dual, diff, grad, soft_dtw_distance_generic, amplitude_distance_at_warp_generic, project_scores_generic) is reachable via BOTH fdars_core::<symbol> (crate root) and fdars_core::prelude::* (SC #3, DIF-04)"
    - "The autodiff module doctest calls grad over a composed objective and asserts; cargo test --doc exits 0 (SC #4, DIF-04)"
    - "Additive/non-breaking: no existing signature changed, no new [dependencies] entry, MSRV 1.81 (DIF-04 locked)"
  artifacts:
    - "fdars-core/src/autodiff.rs (new grad fn + optional jacobian/directional_derivative + module doctest)"
    - "fdars-core/src/lib.rs (crate-root re-exports of autodiff + generic ops)"
    - "fdars-core/src/prelude.rs (prelude re-exports of the differentiable surface)"
    - "fdars-core/tests/autodiff_reexports.rs (external-crate reachability test for both import paths)"
  key_links:
    - "grad seeds each of m inputs in turn (m forward passes) and collects a length-m gradient; seeding index k maps to gradient[k]"
    - "The composed objective must instantiate the generic ops at Dual so tangents flow through the whole chain"
    - "project_scores_generic is ALREADY re-exported at lib.rs:582 - do NOT duplicate it in lib.rs; only ADD the missing symbols"
---

<objective>
Ship the ergonomic public gradient entry point (`grad`) over the `Scalar`-generic differentiable subset, a worked end-to-end composition demo (also the module doctest) proving forward-mode AD flows through a composition of >=2 Phase-76 differentiable ops, and full crate-root + prelude re-exports of the differentiable surface. This is the LAST phase of milestone v0.39.0 (DIF-04); it completes GAP-08 and exhausts the v0.31.0 GAP-BACKLOG.

Purpose: A user can call ONE public `(value, gradient)` entry point over the differentiable subset and get back both the objective value and its length-m gradient, and can reach every differentiable symbol from both the crate root and the prelude.

Output: A new `grad` fn (plus optional `jacobian`/`directional_derivative`) in `autodiff.rs`; a module doctest composing the ops and calling `grad`; inline tests for `grad` correctness (<=1e-12 closed form) and the composed-gradient finite-difference cross-check (<=1e-6); crate-root + prelude re-exports; an external-crate reachability test exercising both import paths.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-gradient-api-composition-demo-integration/77-CONTEXT.md
@.planning/phases/77-gradient-api-composition-demo-integration/77-VALIDATION.md

# Substrate + re-export patterns (read these before editing)
@fdars-core/src/autodiff.rs
@fdars-core/src/prelude.rs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end tracer - grad multi-input gradient API, proven against a closed-form objective</name>
  <files>fdars-core/src/autodiff.rs</files>
  <read_first>
    - fdars-core/src/autodiff.rs - the Scalar trait, Dual{value,tangent}, Dual::seed/constant/extract, and the existing single-input diff (lines 419-435). grad is the multi-input generalization of diff.
    - Note Dual::seed(v) sets tangent=1.0; Dual::constant(v) sets tangent=0.0; extract() returns (value, tangent).
  </read_first>
  <behavior>
    - grad over f(x) = Sum(xi^2) at x = [1.0, 2.0, 3.0] returns value = 14.0 and gradient = [2.0, 4.0, 6.0], each to <=1e-12.
    - Returned gradient length equals input length (here 3).
    - grad over a single-element input [3.0] with f = x0^2 returns (9.0, [6.0]) - agrees with the existing single-input diff.
    - (If jacobian is added) jacobian over f(x)=[x0*x1, x0+x1] at [2.0,3.0] returns values [6.0,5.0] and a 2x2 Jacobian [[3.0,2.0],[1.0,1.0]] to <=1e-12.
  </behavior>
  <action>
    In fdars-core/src/autodiff.rs, after the existing diff fn (line ~435, before the tests module), add a public multi-input gradient entry point implementing DIF-04 SC #1.

    Signature (exact): pub fn grad&lt;F: Fn(&amp;[Dual]) -&gt; Dual&gt;(f: F, x: &amp;[f64]) -&gt; (f64, Vec&lt;f64&gt;). Mark it #[must_use].

    Algorithm - forward-mode, m forward passes (one per input): let m = x.len(); allocate gradient = vec![0.0; m]; keep value = 0.0. For each k in 0..m, build the input vector duals: Vec&lt;Dual&gt; where element k is Dual::seed(x[k]) (tangent 1.0, the variable being differentiated) and every other element j is Dual::constant(x[j]) (tangent 0.0); call f(&amp;duals); extract (v, t) via .extract(); set gradient[k] = t; on the first pass (k==0) also set value = v (the primal is identical across passes since only tangents differ). Return (value, gradient). Handle m == 0 by returning (f(&amp;[]).value, Vec::new()) - evaluate f once so an empty input still yields the constant objective value; do not index x.

    Document with a /// doc comment: explain it seeds each of the m inputs in turn and collects the length-m gradient, and that it is the multi-input generalization of diff. Add a runnable rust doctest inside this fn's doc that constructs grad(|x| x[0]*x[0] + x[1]*x[1], &amp;[3.0, 4.0]), asserts value approx 25.0 and gradient approx [6.0, 8.0] (this is a per-fn doctest; the composed-objective module doctest lands in Task 3). Use explicit paths use fdars_core::autodiff::{grad, Dual, Scalar}; in the doctest.

    OPTIONAL (include both only if they add no risk and stay within the additive/no-new-dep constraint - cheap wrappers over the same seeding loop):
    - pub fn jacobian&lt;F: Fn(&amp;[Dual]) -&gt; Vec&lt;Dual&gt;&gt;(f: F, x: &amp;[f64]) -&gt; (Vec&lt;f64&gt;, Vec&lt;Vec&lt;f64&gt;&gt;) - same m-pass seeding; on pass k, f(&amp;duals) returns Vec&lt;Dual&gt; of length n (the outputs); collect output values from pass 0 and, for every pass k, push t of each output into column k of an n x m Jacobian (row = output, col = input). Mark #[must_use].
    - pub fn directional_derivative&lt;F: Fn(&amp;[Dual]) -&gt; Dual&gt;(f: F, x: &amp;[f64], direction: &amp;[f64]) -&gt; (f64, f64) - ONE forward pass seeding each input j with tangent direction[j] (Dual { value: x[j], tangent: direction[j] }); return f(&amp;duals).extract(). Assert direction.len() == x.len() via debug_assert. Mark #[must_use].

    Do NOT change any existing signature (diff, Dual, Scalar stay untouched). Do NOT add a use of any crate not already imported. Do NOT add anything to Cargo.toml [dependencies].
  </action>
  <verify>
    <automated>cargo test -p fdars-core --features linalg,parallel grad 2>&1 | tail -20; grep -n 'pub fn grad' fdars-core/src/autodiff.rs</automated>
    <fails_when>grad is absent, the Sum(xi^2) gradient does not match [2,4,6] to 1e-12, the returned gradient length != input length, or an existing signature/dependency was changed.</fails_when>
  </verify>
  <acceptance_criteria>
    - grep -n 'pub fn grad' fdars-core/src/autodiff.rs matches the multi-input signature over Fn(&amp;[Dual]) -&gt; Dual returning (f64, Vec&lt;f64&gt;).
    - An inline test in autodiff.rs asserts grad of Sum(xi^2) at [1,2,3] returns (14.0, [2.0,4.0,6.0]) to <=1e-12 and that gradient.len() == 3.
    - cargo test -p fdars-core --features linalg,parallel grad exits 0.
    - No line was added to fdars-core/Cargo.toml [dependencies].
  </acceptance_criteria>
  <done>grad returns (value, length-m gradient); the closed-form Sum(xi^2) gradient matches 2*xi to <=1e-12; the per-fn doctest asserts; committed.</done>
  <reversibility rating="reversible">New additive public fn; can be removed without touching existing code.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expansion - composed-objective demo + finite-difference cross-check (SC #2)</name>
  <files>fdars-core/src/autodiff.rs</files>
  <read_first>
    - fdars-core/src/metric/soft_dtw.rs:157 - pub fn soft_dtw_distance_generic&lt;S: Scalar&gt;(x: &amp;[S], y: &amp;[S], gamma: f64) -&gt; S.
    - fdars-core/src/regression.rs:232 - pub fn project_scores_generic&lt;S: Scalar&gt;(curve: &amp;[S], mean: &amp;[f64], rotation: &amp;FdMatrix, weights: &amp;[f64], ncomp: usize) -&gt; Vec&lt;S&gt; (gradient is the constant rotation[(j,k)]*weights[j]).
    - fdars-core/src/regression.rs:169 (doctest) - the fdata_to_pc_1d(&amp;data, ncomp, &amp;argvals) -&gt; FpcaResult construction pattern for building a trained FpcaResult in a test.
    - fdars-core/src/alignment/differentiable.rs:147-166 - the setup() pattern: grid on [0.1,0.9] deliberately avoids SRSF derivative zeros; use spanning curves and avoid zero-derivative sample points per 76's guidance.
    - MEMORY note "recovery-tests-need-fullrank-design": use n much greater than m spanning pseudo-random curves; never loosen tolerance.
  </read_first>
  <behavior>
    - The composed scalar objective J(curve) = soft_dtw_distance_generic(&amp;curve, &amp;reference, gamma) + lambda * Sum_k project_scores_generic(&amp;curve, mean, rotation, weights, ncomp)[k]^2, evaluated at f64, equals a hand-composed f64 reference to <=1e-12 (parity of the composition itself).
    - grad(|c| J(c), &amp;curve_f64) returns a length-m gradient whose every component matches central finite differences (h = 1e-6) to <=1e-6, on a SPANNING full-rank curve built from random combos of sin(pi*t), cos(2*pi*t), sin(3*pi*t) on a grid that avoids SRSF derivative zeros.
    - The returned objective value is finite and greater than 0.
  </behavior>
  <action>
    Add an inline test (in autodiff.rs's tests module, near grad) that builds the composed-objective demo and cross-checks its grad-gradient against central finite differences, implementing DIF-04 SC #2.

    Build the fixtures at f64: choose m (e.g. 24) and a grid argvals on [0.1, 0.9] (spanning, avoids SRSF singularities - mirror alignment/differentiable.rs setup()). Build a training data matrix of n much greater than m (e.g. n=40) SPANNING curves as seeded random combos of sin(pi*t), cos(2*pi*t), sin(3*pi*t) (use StdRng::seed_from_u64 for determinism, matching the crate's per-thread seeding convention), call fdata_to_pc_1d(&amp;data, ncomp, &amp;argvals) (ncomp e.g. 3) to get a trained FpcaResult, and extract its mean, rotation, weights. Build a reference curve and an input curve (another spanning combo, derivative nonzero on the grid). Pick gamma (e.g. 0.1) and lambda (e.g. 1.0).

    Define the composed objective as a closure over the input dual vector: let objective = |c: &amp;[Dual]| -&gt; Dual { let sdtw = soft_dtw_distance_generic(c, &amp;reference_duals, gamma); let scores = project_scores_generic(c, &amp;mean, &amp;rotation, &amp;weights, ncomp); let mut acc = Dual::constant(0.0); for s in &amp;scores { acc += *s * *s; } sdtw + Dual::constant(lambda) * acc } - where reference_duals: Vec&lt;Dual&gt; = reference.iter().map(|&amp;r| Dual::constant(r)).collect() (reference is a constant; gradient flows only through c). This composes soft-DTW (op 1) and FPCA-score projection (op 2) into ONE scalar.

    Call let (value, gradient) = grad(objective, &amp;curve);. Assert value.is_finite() and value greater than 0.0 and gradient.len() == m.

    Central-FD cross-check: define an f64 objective let f64_obj = |c: &amp;[f64]| -&gt; f64 { let d = soft_dtw_distance_generic::&lt;f64&gt;(c, &amp;reference, gamma); let sc = project_scores_generic::&lt;f64&gt;(c, &amp;mean, &amp;rotation, &amp;weights, ncomp); d + lambda * sc.iter().map(|s| s*s).sum::&lt;f64&gt;() }. For each j in 0..m, perturb curve[j] by +/- h (h = 1e-6), compute (f64_obj(plus) - f64_obj(minus)) / (2h), and assert (gradient[j] - fd_j).abs() &lt; 1e-6. Also assert (value - f64_obj(&amp;curve)).abs() &lt; 1e-12 (composition parity at f64).

    Do not loosen the 1e-6 tolerance; if a component fails, the curve is likely low-rank or hits an SRSF zero-derivative point - fix the fixture (more spanning curves / shift the grid), never the tolerance (MEMORY: recovery-tests-need-fullrank-design). Additive-only: add no new dependency; do not change existing signatures.
  </action>
  <verify>
    <automated>cargo test -p fdars-core --features linalg,parallel compos 2>&1 | tail -25; cargo test -p fdars-core --features linalg,parallel autodiff 2>&1 | tail -10</automated>
    <fails_when>The composed grad-gradient deviates from central finite differences by more than 1e-6 on any component, the f64 composition parity exceeds 1e-12, or the objective value is non-finite / not positive.</fails_when>
  </verify>
  <acceptance_criteria>
    - An inline test (name containing compos) composes soft_dtw_distance_generic AND project_scores_generic into one scalar objective, calls grad, and asserts every gradient component matches central FD <=1e-6.
    - The test uses a spanning full-rank training set (n much greater than m) and a grid avoiding SRSF derivative zeros; tolerance is exactly 1e-6 (not loosened).
    - cargo test -p fdars-core --features linalg,parallel autodiff exits 0.
  </acceptance_criteria>
  <done>The composed-objective gradient matches central finite differences <=1e-6 on spanning curves, and f64 composition parity holds <=1e-12; committed.</done>
  <reversibility rating="reversible">Test-only addition.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Expansion - crate-root + prelude re-exports, reachability test, and composed-objective module doctest (SC #3, SC #4)</name>
  <files>fdars-core/src/lib.rs, fdars-core/src/prelude.rs, fdars-core/tests/autodiff_reexports.rs, fdars-core/src/autodiff.rs</files>
  <read_first>
    - fdars-core/src/lib.rs:76 (pub mod autodiff; - keep it), :271 (alignment re-export block), :582 (pub use regression::{ ... project_scores_generic ... } - ALREADY re-exports project_scores_generic; do NOT duplicate), :633 (metric re-export block).
    - fdars-core/src/prelude.rs - the pub use crate::&lt;module&gt;::{...}; style (e.g. lines 45-48 metric, 98-105 alignment, line 18 regression). Match this exactly.
    - fdars-core/src/autodiff.rs module doc (lines 1-39) - the existing doctest style; the composed-objective module doctest goes here (or on grad's doc, per CONTEXT SC #4).
    - MEMORY "ci-clippy-all-targets-gate": the clippy gate must run --all-targets, so the new integration test is linted too.
  </read_first>
  <action>
    (a) Crate-root re-exports in fdars-core/src/lib.rs - additive, matching existing style:
    - Add a new re-export block near the autodiff module: pub use autodiff::{diff, grad, Dual, Scalar}; (add jacobian/directional_derivative to this list IF they were added in Task 1). Keep pub mod autodiff; at line 76.
    - Add soft_dtw_distance_generic to the existing pub use metric::{ ... }; block (line ~633) - insert the identifier in alphabetical position among the soft_dtw_* entries.
    - Add amplitude_distance_at_warp_generic to the existing pub use alignment::{ ... }; block (line ~271) - insert in alphabetical position (near amplitude_distance).
    - Do NOT re-add project_scores_generic to lib.rs (already present at line 582).

    (b) Prelude re-exports in fdars-core/src/prelude.rs - additive, matching the pub use crate::&lt;module&gt;::{...}; style:
    - Add pub use crate::autodiff::{diff, grad, Dual, Scalar}; (+ jacobian/directional_derivative if added) under a comment line describing the differentiable core (v0.39.0 DIF-04).
    - Add pub use crate::metric::soft_dtw_distance_generic; (or fold into the existing metric block at lines 45-48).
    - Add pub use crate::alignment::amplitude_distance_at_warp_generic; (or fold into the existing alignment block at lines 98-105).
    - Add pub use crate::regression::project_scores_generic; to the prelude (the prelude does not yet re-export it - add near the regression block at line ~18).

    (c) Reachability test (SC #3): a crate cannot use fdars_core::... its own name from an inline unit test, so add an INTEGRATION test file fdars-core/tests/autodiff_reexports.rs (external-crate view - the true test of crate-root + prelude reachability). At the top declare two explicit use groups: use fdars_core::{Scalar, Dual, diff, grad, soft_dtw_distance_generic, amplitude_distance_at_warp_generic, project_scores_generic}; (crate-root) and, in a separate module or fn, use fdars_core::prelude::*;. Add #[test] fn reexports_reachable() that touches every symbol at least once (e.g. call let (_v, g) = grad(|x| x[0]*x[0], &amp;[1.0]); assert_eq!(g.len(), 1); bind diff as a fn value; name the generic ops via type-annotated fn-pointer lets; construct a Dual and a value implementing Scalar) so none is dead code. Gate the file with #![cfg(all(feature = "linalg", feature = "parallel"))] to match the run command. Add a #[test] that uses the prelude::* path to reference grad/Dual/Scalar too, proving BOTH paths.

    (d) Module doctest (SC #4): add a runnable rust example to the autodiff module doc (top of autodiff.rs) OR to grad's doc. It MUST compose >=2 Phase-76 ops (soft_dtw_distance_generic + project_scores_generic) into ONE scalar objective and call grad over the input curve, then assert (e.g. gradient length == m, value finite and positive). Import via use fdars_core::prelude::*; (exercising the prelude path in-doc). Build a small trained FpcaResult inline via fdata_to_pc_1d (mirror the regression.rs:169 doctest) - keep it self-contained, small, and green under cargo test --doc. Keep the existing module doctest (lines 32-39) intact - add the composed example alongside.

    Additive-only: change no existing signature; add nothing to Cargo.toml [dependencies]. The doctest satisfies SC #4 - an examples/ file is OPTIONAL and out of scope for this task.
  </action>
  <verify>
    <automated>cargo test -p fdars-core --features linalg,parallel --doc autodiff 2>&1 | tail -20; cargo test -p fdars-core --features linalg,parallel reexports 2>&1 | tail -15; grep -c 'soft_dtw_distance_generic\|amplitude_distance_at_warp_generic\|pub use autodiff' fdars-core/src/lib.rs</automated>
    <fails_when>The module doctest fails or is absent under cargo test --doc, the reexports_reachable integration test fails to compile (a symbol is not reachable via crate root or prelude), or an existing signature/dependency was changed.</fails_when>
  </verify>
  <acceptance_criteria>
    - fdars-core/src/lib.rs contains pub use autodiff::{diff, grad, Dual, Scalar}; (or a superset including jacobian/directional_derivative) AND soft_dtw_distance_generic in the metric block AND amplitude_distance_at_warp_generic in the alignment block; project_scores_generic still present exactly once.
    - fdars-core/src/prelude.rs re-exports diff, grad, Dual, Scalar, soft_dtw_distance_generic, amplitude_distance_at_warp_generic, project_scores_generic.
    - fdars-core/tests/autodiff_reexports.rs exists, references the surface via crate root AND prelude::*, and cargo test ... reexports exits 0.
    - cargo test -p fdars-core --features linalg,parallel --doc autodiff exits 0 (the composed module doctest runs and asserts).
  </acceptance_criteria>
  <done>The full differentiable surface is reachable via crate root AND prelude (external-crate test compiles + passes); the composed module doctest runs green under cargo test --doc; committed.</done>
  <reversibility rating="reversible">Additive re-exports + test + doctest; removable without touching implementations.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library API -> caller | This is a pure numeric Rust library with no I/O, no network, no untrusted input, no deserialization on this path. `grad` takes an in-process closure and an `&[f64]`; all data is caller-owned in-process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-77-01 | Tampering | grad seeding index -> gradient[k] mapping | low | mitigate | Task 1 closed-form test (Sum(xi^2) -> [2,4,6] to 1e-12) and Task 2 finite-difference cross-check (<=1e-6) pin the gradient values; a wrong seeding index fails both. |
| T-77-02 | Denial of Service | m == 0 empty-input path in grad | low | mitigate | grad handles m == 0 by evaluating f once and returning an empty gradient, never indexing x - no panic on empty input. |
| T-77-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase (additive Rust-only, no new [dependencies]); the no-new-dependency constraint is enforced by an acceptance criterion. No legitimacy gate required. |
</threat_model>

<verification>
Full-suite gates (run before /gsd-verify-work):
- `cargo test -p fdars-core --features linalg,parallel` (unit + integration: grad, compos, reexports)
- `cargo test -p fdars-core --features linalg,parallel --doc` (the new composed module doctest + existing autodiff doctests green)
- `cargo clippy --all-targets --features linalg,parallel -- -D warnings` (lints the new integration test too - MEMORY: ci-clippy-all-targets-gate)
- `cargo fmt` clean

Additive/no-new-dep check: `git diff fdars-core/Cargo.toml` touches nothing under [dependencies]; no existing public signature changed (diff, Dual, Scalar, the three generic ops all unchanged).
</verification>

<success_criteria>
- SC #1: grad(f, x) returns (value, length-m gradient); Sum(xi^2) matches 2*xi to <=1e-12.
- SC #2: composed-objective gradient over >=2 Phase-76 ops matches central finite differences <=1e-6 on spanning full-rank curves.
- SC #3: the full differentiable surface is reachable from BOTH fdars_core::<symbol> and fdars_core::prelude::* (external-crate test compiles + passes).
- SC #4: cargo test --doc green (the composed module doctest runs and asserts).
- Additive/non-breaking; no new crate dependency; MSRV 1.81.
</success_criteria>

<output>
Create `.planning/phases/77-gradient-api-composition-demo-integration/77-01-SUMMARY.md` when done.
</output>
