---
phase: 85-release-preparation-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fdars-core/Cargo.toml
  - Cargo.lock
  - README.md
  - CHANGELOG.md
autonomous: true
requirements: [REL-01]

estimate:
  tokens: 70000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "fdars-core reports version 0.41.0 (per REL-01, D: version bump 0.40.0→0.41.0)"
    - "CHANGELOG has a [0.41.0] breaking-changes entry covering API-01/02/03/04 + Added, with migration notes (per REL-01)"
    - "No stray 0.40 version reference remains in tracked README/docs (per REL-01 docs refresh)"
    - "All whole-crate release gates pass green: fmt, clippy, test, serde build, examples build, package sanity (per REL-01)"
    - "No git tag is created and no real cargo publish is run — operator's final step (per CONTEXT HARD BOUNDARY)"
  artifacts:
    - fdars-core/Cargo.toml
    - Cargo.lock
    - README.md
    - CHANGELOG.md
  key_links:
    - "fdars-core/Cargo.toml version string ↔ README dependency lines ↔ CHANGELOG [0.41.0] heading all agree on 0.41.0"
    - "CHANGELOG [0.41.0] entry ↔ Phases 81–84 SUMMARYs (removed forms, seals, non_exhaustive list, renames, Added deliverables)"
---

<objective>
Prepare `fdars-core` for a 0.41.0 release and VERIFY release-readiness. Bump the crate version 0.40.0 → 0.41.0, refresh version strings in tracked docs, author a breaking-changes-framed CHANGELOG `[0.41.0]` entry documenting all cleanups landed in Phases 81–84, and confirm every whole-crate gate is green (fmt, clippy, test, serde build, all 28 examples, package sanity).

Purpose: This is the FINAL phase of milestone v0.41.0 — the first breaking release after a long additive run. It makes the crate publishable and produces the operator-verifiable evidence that the breaking changes are complete and consistent. It satisfies REL-01.

Output: Bumped `fdars-core/Cargo.toml` + refreshed `Cargo.lock`, version-consistent `README.md`, a new `CHANGELOG.md [0.41.0]` section, and a verified-green release-readiness state.

**HARD BOUNDARY — DO NOT tag or publish.** This plan MUST NOT run `git tag v0.41.0` and MUST NOT run a real `cargo publish`. The tag push → crates.io publish (release.yml) is the FINAL OPERATOR-DRIVEN step and is explicitly out of scope. Only `cargo publish --dry-run` / `cargo package` (packaging sanity, no upload) is permitted.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-release-preparation-verification/85-CONTEXT.md
@.planning/REQUIREMENTS.md

# Source of the breaking-change list for the CHANGELOG (Phases 81–84 outcomes)
@.planning/phases/81-api-audit-deprecated-removal
@.planning/phases/82-surface-sealing-non-exhaustive
@.planning/phases/83-naming-unification
@.planning/phases/84-stability-deliverables

# Files this plan edits
@fdars-core/Cargo.toml
@README.md
@CHANGELOG.md
</context>

<execution_notes>
Environment hazards (from MEMORY — apply throughout):
- **Commit hazard:** the pre-commit hook runs the full test suite and times out at 30s → every normal `git commit` fails with a bogus error. For commits in this plan: run `cargo fmt` first, then `git commit --no-verify`. Verify gates OUT OF BAND (they are run explicitly in Task 3), not via the hook.
- **Disk pressure:** `/home` runs hot (~94%); `target/` can reach 100+GB and a full `cargo test`/`cargo build --examples` may die at link time with a bogus "No space left" / "linking with cc failed". If that happens, free space with `rm -rf target/debug/{incremental,examples}` and re-run — this is an environment issue, NOT a code regression.
- Run all cargo commands from the workspace root `/home/simonm/projects/rust/fdars`.
- **No code changes to `fdars-core/src/`** in this plan — the breaking changes already landed in Phases 81–83 and are proven by green gates + 28 examples/doctests. This plan documents + re-verifies them.
</execution_notes>

<tasks>

<task type="auto">
  <name>Task 1: Bump version 0.40.0 → 0.41.0 and refresh version strings</name>
  <files>fdars-core/Cargo.toml, README.md, Cargo.lock</files>
  <read_first>
    - fdars-core/Cargo.toml line 3: `version = "0.40.0"` (the crate version to bump)
    - README.md line 44: `fdars-core = "0.40"` and line 84: `fdars-core = { version = "0.40", default-features = false }`
  </read_first>
  <action>
    Bump the crate version per REL-01. Edit `fdars-core/Cargo.toml` line 3 from `version = "0.40.0"` to `version = "0.41.0"`. Edit `README.md`: the dependency line at ~line 44 (`fdars-core = "0.40"` → `fdars-core = "0.41"`) and the featured-dependency line at ~line 84 (`version = "0.40"` → `version = "0.41"`).

    Then grep the tracked tree for any OTHER `0.40` version references in docs/examples and bump each to `0.41` where it denotes the fdars-core crate version. Use `grep -rn '0\.40' --include='*.md' --include='*.rs' --include='*.toml' .` and inspect each hit. DO NOT change: the CHANGELOG historical `[0.40.0]`/`[0.39.0]`/earlier headings and their body text (those are permanent release history), the `rust-version = "1.81"` MSRV, any dependency pin unrelated to the fdars-core version, or Cargo.lock transitive pins beyond fdars-core's own entry.

    Refresh `Cargo.lock` by running a build so the `fdars-core` lock entry updates to 0.41.0: `cd /home/simonm/projects/rust/fdars && cargo build -p fdars-core` (a normal build rewrites the lock; `cargo update` is not needed). Confirm the lock's `[[package]] name = "fdars-core"` block now shows `version = "0.41.0"`.

    Commit hazard applies (see execution_notes): `cargo fmt` then `git commit --no-verify`.
  </action>
  <acceptance_criteria>
    - `fdars-core/Cargo.toml` line 3 reads `version = "0.41.0"`.
    - Both README dependency lines reference `0.41` (none reference `0.40` as the fdars-core crate version).
    - `Cargo.lock` fdars-core entry reads `version = "0.41.0"`.
    - No unrelated version pins, the `[0.40.0]` CHANGELOG history, or the `1.81` MSRV were touched.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && grep -qF 'version = "0.41.0"' fdars-core/Cargo.toml && grep -c '0\.40' README.md | grep -qx 0 && grep -q 'name = "fdars-core"' Cargo.lock && awk '/name = "fdars-core"/{f=1} f&&/^version = /{print; exit}' Cargo.lock | grep -qF '"0.41.0"'</automated>
    <fails_when>fdars-core/Cargo.toml does not show version 0.41.0, OR any `0.40` string remains in README.md, OR the Cargo.lock fdars-core entry is not 0.41.0 (version mismatch across surfaces).</fails_when>
  </verify>
  <done>Crate version is 0.41.0 in Cargo.toml, Cargo.lock, and README; no stray 0.40 fdars-core version reference remains in README (per REL-01).</done>
</task>

<task type="auto">
  <name>Task 2: Author CHANGELOG [0.41.0] breaking-changes entry + fix preamble</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md lines 1–9: the preamble (currently claims "additive and non-breaking … except where noted") and the current top entry `## [0.40.0] - 2026-09-07`.
    - .planning/phases/85-release-preparation-verification/85-CONTEXT.md §"CHANGELOG [0.41.0] entry" — the AUTHORITATIVE content spec for this entry (the exact removed forms, seals, non_exhaustive list, renames, and Added items).
    - The Phases 81–84 SUMMARYs (referenced in <context>) — the source of truth for the concrete symbols; cross-check the CONTEXT list against them.
  </read_first>
  <action>
    Insert a new `## [0.41.0] - 2026-09-07` section ABOVE the existing `## [0.40.0] - 2026-09-07` section (keepachangelog + SemVer, newest-first). This is the FIRST breaking release after the additive run — lead the section with a clear **BREAKING** framing sentence explaining the release settles the public API ahead of a future 1.0 (breaking is API SHAPE ONLY — no numeric/behavioral change; MSRV unchanged at 1.81 crate / 1.84 linalg). Group the content by keepachangelog section headings, sourcing every item from CONTEXT §CHANGELOG (authoritative) cross-checked against the Phase 81–84 SUMMARYs:

    - **Removed** (API-01): the 6 deprecated forms `mean_2d`, `fanova`, `random_tukey_2d`, `random_projection_2d`, `fraiman_muniz_2d`, `modal_2d` and their crate-root/prelude re-exports. Include migration guidance: use `mean(…, Dim::Two)`, `fanova_seeded(…, 42)`, and `<depth>(…, Dim::Two)` for the depth forms.
    - **Changed — sealing** (API-02): `sort_nan_safe` and `solve_gaussian_pub` are now `pub(crate)` (previously unintentionally `pub`).
    - **Changed — non-exhaustive** (API-03): `#[non_exhaustive]` added to 10 public enums (`PeerPenalty`, `LambdaChoice`, `LambdaMethod`, `DesignCriterion`, `OptimalityKind`, `ExtrapolationPolicy`, `ImputationMethod`, `SelectionCriterion`, `BasisType`, `BasisCriterion`) and 2 result structs (`OptimBandwidthResult`, `KnnCvResult`). Note the downstream impact: exhaustive `match` / struct-literal construction on these now requires a wildcard arm / `..`.
    - **Changed — naming** (API-04): renamed `funhddC_cluster`→`fun_hddc_cluster`, `FosrResult2d`→`Fosr2dResult`, `GmmResult`→`GmmFitResult`; collapsed `deriv_1d`/`deriv_2d`→`deriv(…, DerivDomain)` returning `DerivResult`, and `lp_self_1d`/`lp_cross_1d`/`lp_self_2d`/`lp_cross_2d`→`lp_self`/`lp_cross(…, LpDomain)`. Provide a per-rename migration note.
    - **Added**: `documentation/STABILITY.md` (semver + MSRV policy), `documentation/ROADMAP-TO-1.0.md` (1.0 gap checklist); new public enums `DerivDomain`, `LpDomain` and the new result type `DerivResult`.

    Then FIX the preamble (lines ~4–7): it currently asserts the span is "additive and non-breaking … except where noted". Update it so it no longer characterizes the whole span as additive — state that 0.41.0 is an explicitly breaking API-stabilization release (API shape only) while earlier entries in the span remained additive. Do NOT alter the `[0.40.0]` and earlier entry bodies.

    Commit hazard applies: `cargo fmt` (no-op for markdown but keeps the flow uniform) then `git commit --no-verify`.
  </action>
  <acceptance_criteria>
    - A `## [0.41.0] - 2026-09-07` section exists immediately above `## [0.40.0]`.
    - It is BREAKING-framed and contains Removed / Changed / Added subsections covering API-01 (6 removed forms), API-02 (2 seals), API-03 (12 non_exhaustive items), API-04 (renames + deriv/lp collapses), and the Added deliverables, each with migration notes.
    - The preamble no longer claims the span is uniformly additive/non-breaking.
    - No pre-existing entry body ([0.40.0] and earlier) was modified.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && awk '/## \[0\.41\.0\]/{f=1} /## \[0\.40\.0\]/{f=0} f' CHANGELOG.md > /tmp/cl41.txt && test -s /tmp/cl41.txt && for t in mean_2d fanova random_tukey_2d fraiman_muniz_2d modal_2d sort_nan_safe solve_gaussian_pub non_exhaustive funhddC_cluster Fosr2dResult GmmFitResult DerivDomain LpDomain STABILITY ROADMAP-TO-1.0; do grep -qiF "$t" /tmp/cl41.txt || { echo "MISSING in [0.41.0] entry: $t"; exit 1; }; done && grep -qiE 'break' /tmp/cl41.txt</automated>
    <fails_when>The `[0.41.0]` section is absent/empty, OR any of the enumerated removed/sealed/renamed/added symbols is missing from the entry, OR the entry is not breaking-framed.</fails_when>
  </verify>
  <done>CHANGELOG.md has a complete, breaking-framed [0.41.0] entry covering API-01/02/03/04 + Added with migration notes, and the preamble no longer claims the span is additive (per REL-01).</done>
</task>

<task type="auto">
  <name>Task 3: Verify whole-crate release-readiness (all gates green, NO tag/publish)</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - .planning/phases/85-release-preparation-verification/85-CONTEXT.md §Verification and the KNOWN FLAKE note — the authoritative gate list and flake-handling protocol.
  </read_first>
  <action>
    Run every whole-crate release gate from the workspace root `/home/simonm/projects/rust/fdars` and confirm each is green. This is the load-bearing task — it produces the release-readiness evidence for REL-01. Run, in order:

    1. `cargo fmt --check`
    2. `cargo clippy --all-targets --features linalg,parallel -- -D warnings` (must lint test/bench code too — the `--all-targets` is REQUIRED per MEMORY ci-clippy-all-targets-gate, a bare `-p` gate produces a false green).
    3. `cargo test` (lib + integration + doctests).
    4. `cargo build --features serde`.
    5. `cargo build --examples` (all 28 must build).
    6. Packaging sanity: `cargo publish -p fdars-core --dry-run --features linalg,parallel` (falls back to `cargo package -p fdars-core --no-verify` if the dry-run is too slow under disk pressure). This confirms the crate is packageable — it does NOT upload.

    **KNOWN FLAKE handling (per CONTEXT):** under a full parallel `cargo test`, `golden_co_cluster_parallel` + `golden_co_cluster_below_threshold` (test `equivalence_phase48`) and `svd_sign_fpca_two_matrix_bit_identical` (test `equivalence_phase49`) may fail env/BLAS/disk-dependently yet PASS in isolation. If AND ONLY IF one of these three is the failing test, re-run its binary alone: `cargo test --test equivalence_phase48` and/or `cargo test --test equivalence_phase49`. If the isolated re-run is green, the suite counts as passing for release purposes (already logged in ROADMAP-TO-1.0.md — NOT a v0.41.0 regression). Any OTHER test failure is a real gate failure and must block.

    **Disk-pressure handling:** if any build/test dies at link time with "No space left" / "linking with cc failed", run `rm -rf target/debug/{incremental,examples}` to free space and re-run — that is an environment issue, not a regression.

    **HARD BOUNDARY:** DO NOT run `git tag v0.41.0`. DO NOT run a real `cargo publish` (no `--dry-run` flag). Only the dry-run / package sanity is permitted. Leave tagging and publishing to the operator.

    No commit is required for this task (it modifies nothing) — record the gate results in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `cargo fmt --check` exits 0.
    - `cargo clippy --all-targets --features linalg,parallel -- -D warnings` exits 0 with no warnings.
    - `cargo test` passes (or the only failures are the three named golden flakes AND each passes on isolated per-binary re-run).
    - `cargo build --features serde` exits 0.
    - `cargo build --examples` builds all 28 examples (exit 0).
    - `cargo publish --dry-run` (or `cargo package`) exits 0 with no packaging error.
    - No git tag created; no real publish performed.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/simonm/projects/rust/fdars && cargo fmt --check && cargo clippy --all-targets --features linalg,parallel -- -D warnings && cargo build --features serde && cargo build --examples && cargo package -p fdars-core --no-verify</automated>
    <fails_when>Any of fmt/clippy/serde-build/examples-build/package exits non-zero. (cargo test is run separately in-action so the golden-flake isolation protocol can be applied; a NON-flake test failure fails this gate. A packaging error fails this gate.) A git tag or real publish having been run is itself a boundary violation and fails the task.</fails_when>
  </verify>
  <done>All whole-crate release gates are green (with the three named golden flakes handled per the isolated-rerun protocol), the crate packages cleanly, and NO tag/publish was performed — release-readiness is verified for the operator (per REL-01).</done>
</task>

</tasks>

<verification>
Phase-level release-readiness check (goal-backward for REL-01):
- Version consistency: `grep 'version = "0.41.0"' fdars-core/Cargo.toml` present; README has no `0.40` fdars-core reference; Cargo.lock fdars-core entry is 0.41.0.
- CHANGELOG: a breaking-framed `[0.41.0]` entry exists above `[0.40.0]` covering API-01/02/03/04 + Added with migration notes; preamble corrected.
- Gates: fmt, clippy (`--all-targets --features linalg,parallel -D warnings`), test (golden-flake exception via isolated re-run), `--features serde` build, all 28 examples build, packaging sanity — all green.
- Boundary: NO `git tag v0.41.0` and NO real `cargo publish` were run.
</verification>

<success_criteria>
- [ ] `fdars-core/Cargo.toml`, `Cargo.lock`, and `README.md` all report 0.41.0; no stray 0.40 fdars-core reference remains.
- [ ] `CHANGELOG.md` has a complete, breaking-framed `[0.41.0] - 2026-09-07` entry (Removed/Changed/Added) with migration notes; preamble no longer claims the span is uniformly additive.
- [ ] All whole-crate gates verified green: fmt, clippy `--all-targets --features linalg,parallel -D warnings`, test, `--features serde` build, all 28 examples build, packaging sanity.
- [ ] Known golden flakes, if they fired, confirmed green on isolated per-binary re-run (not counted as regressions).
- [ ] No git tag created and no real crates.io publish performed — the operator's final step is left untouched.
</success_criteria>

<artifacts>
## Artifacts this phase produces
- `fdars-core/Cargo.toml` — version bumped to 0.41.0.
- `Cargo.lock` — fdars-core entry refreshed to 0.41.0.
- `README.md` — dependency version strings bumped to 0.41.
- `CHANGELOG.md` — new breaking-framed `[0.41.0]` section + corrected preamble.
- Verified-green release-readiness state (evidence recorded in the plan SUMMARY): all whole-crate gates pass; crate packages cleanly; tag/publish deliberately NOT performed (operator's final step).
</artifacts>

<output>
Create `.planning/phases/85-release-preparation-verification/85-01-SUMMARY.md` when done.
</output>
