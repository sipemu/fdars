# fdars

## What This Is

fdars is a mature Rust functional-data-analysis (FDA) library (crate `fdars-core`, v0.24.0; planning ahead of the crate release) with broad algorithm coverage — regression, classification, clustering, depth measures, elastic shape analysis, seasonal decomposition, statistical process monitoring, and model explainability — plus WASM/JS and R bindings. The **v0.14.0 audit milestone** (shipped 2026-08-09) proactively reviewed execution performance and mapped functionality gaps against Python's scikit-fda, producing a consolidated report and a prioritized, GSD-ready backlog. Subsequent implementation milestones executed the highest-leverage items from that backlog top-first — v0.15.0 (top-4 quick wins), v0.16.0 (elastic feasibility + parity), v0.17.0 (registration parity + elastic-FPCA performance) — which exhausted the actionable scikit-fda backlog. Milestone **v0.18.0** pivoted the comparison yardstick to the richer, older **R FDA ecosystem** (`fda`, `fda.usc`, `refund`, `fdapace`, `roahd`, `ftsa`, `frechet`, and more): a fresh audit mapping fdars' gaps against R packages, producing `R-BACKLOG.md` (26 ranked R-parity items). Milestone **v0.19.0** began executing that backlog — the functional-inference suite (`inference/` module: two-sample tests + FLM inference), closing the biggest R-parity table-stakes gap.

## Core Value

A comprehensive, fast Rust functional-data-analysis library that closes the highest-leverage capability and performance gaps against scikit-fda — driven by the evidence-backed v0.14.0 audit backlog, top items first.

## Current Milestone: v0.28.0 Spectral Functional Time Series & Object-Data Fréchet Regression

**Goal:** Draw the two now-unblocked score-1.00 (L-effort) items from the v0.18.0 `R-BACKLOG.md` — spectral/frequency-domain functional time series (FTS-03) and object-data Fréchet regression across specific metric spaces (FRE-02) — each by adding `fdars-core/src/` code, additively and non-breaking, extending a foundation shipped in v0.27.0.

**Target features:**
- **FTS-03 (rank 22, score 1.00, P3 differentiator, L-effort):** spectral functional time series — dynamic principal component analysis (DPCA) via spectral methods, spectral-density-operator estimation, functional VAR/VMA process simulation, and functional ARMA (FARMA) simulation. New `fts/spectral.rs` reusing the existing `rustfft` dependency (spectral density + DPCA) plus a FARMA/VAR simulator in `simulation.rs`; builds on the shipped FTS-01/FTS-02 ACF/forecasting foundation. R baseline: `freqdom` / `ftsa`. Covers ~9 absent Area-6 gaps.
- **FRE-02 (rank 23, score 1.00, P3 differentiator, L-effort):** object-data Fréchet regression across specific object spaces — covariance-matrix responses (Frobenius / power / log-Cholesky metrics), correlation matrices, spherical data (geodesic exp/log maps), network responses, and point-process responses, each with its Fréchet-ANOVA analog. Extends `frechet/` with per-space metric + geodesic operations as pluggable metric backends for the shipped FRE-01 solver. R baseline: `frechet` 0.3.0. Covers ~8 absent Area-7 gaps.

**Key context:** Implementation milestone — additive/non-breaking, reuse-first, `Result`-returning, inline `#[cfg(test)]` tests, crate-root re-exports, zero changes to existing public signatures, **no new crate dependency** (FTS-03 reuses `rustfft`; FRE-02 plugs into the FRE-01 solver), numeric outputs only. Phase numbering continues from v0.27.0 (39/40) → Phases 41/42 (two independent, disjoint code areas: `fts/spectral.rs` + `simulation.rs` vs `frechet/` metric backends). FTS-03 builds on the shipped FTS-01/FTS-02; FRE-02 depends on the shipped FRE-01 Fréchet-regression solver framework. Any-order/parallel execution; any divergence from the R baseline documented in rustdoc.

**Last shipped:** v0.27.0 Functional Time Series & Fréchet Regression — FTS-01 (functional time-series forecasting — new `fts/forecast.rs`: `ftsm`/`ftsm_forecast`/`ftsm_forecast_multistep`/`ftsm_update`/`fplsr`, Phase 39) and FRE-01 (Fréchet/object-data regression — new `frechet/`: `MetricSpace`/`WassersteinDensitySpace`, `frechet_mean`/`frechet_variance`, `wasserstein2_distance`, global/local Fréchet regression, `frechet_anova`, Phase 40) — the two score-1.33 (L-effort) `R-BACKLOG.md` items, exhausting the 1.33 tier. Both delivered, verified 5/5 each, milestone audit PASSED: whole-crate suite **2460 lib + 172 doc tests green**, `cargo clippy --all-targets --features linalg,parallel` clean, `cargo fmt --check` clean, no new dependency. **Released as crate `fdars-core` 0.27.0 on crates.io** (tag `v0.27.0`). See the Validated section below and [`milestones/v0.27.0-ROADMAP.md`](milestones/v0.27.0-ROADMAP.md).

**Next (after v0.28.0):** the 1.00 L-effort tier still holds REG-06 (boosting/Bayesian functional regression), REP-02 (FEM/PDE smoothing on irregular 2D/3D domains), and CLUS-02 (functional co-clustering / funLBM) — score 0.67, L-effort. Those form the following tier once v0.28.0 exhausts the 1.00 tier's dependency-satisfied items.

<details>
<summary>v0.27.0 milestone goal (shipped 2026-08-22)</summary>

**Goal:** Draw the two score-1.33 (L-effort) items from the v0.18.0 `R-BACKLOG.md` — functional time-series forecasting (FTS-01) and Fréchet / object-data regression (FRE-01) — each by adding `fdars-core/src/` code, additively and non-breaking.

**Target features:**
- **FTS-01 (rank 20, score 1.33, P2 differentiator, L-effort):** functional time-series forecasting — `ftsm` (FPCA-based model), FPC-score-regression forecasting, `fplsr` (functional PLS forecasting), dynamic forecast updating, iterative multi-step forecasting. New `fts/forecast.rs`, reusing `fdata_to_pc_1d` + `scoring.rs` + FTS-02's `fts/acf.rs`. R baseline: `ftsa`.
- **FRE-01 (rank 21, score 1.33, P2 differentiator, L-effort):** metric-space (object-data) regression — global/local Fréchet regression, Fréchet mean/variance, 1D Wasserstein distance, density-response Fréchet regression, Fréchet ANOVA. New `frechet/`, sharing DENS-01's quantile/Wasserstein machinery. R baseline: `frechet`.

**Key context:** Implementation milestone — additive/non-breaking, reuse-first, no new crate dependency, numeric outputs only. Phase numbering continued from v0.26.0 (37/38) → Phases 39/40 (independent, disjoint code areas). Released as crate `fdars-core` 0.27.0 on crates.io (tag `v0.27.0`).

</details>

<details>
<summary>v0.26.0 milestone goal (shipped 2026-08-21)</summary>

**Goal:** Draw the two remaining top-ranked (score 1.73, M-effort) items from the v0.18.0 `R-BACKLOG.md` — completing the FPCA long tail (FPCA-02) and adding fast sparse-covariance estimation (SPARSE-01) — each by adding `fdars-core/src/` code, additively and non-breaking.

**Target features (from R-BACKLOG.md):**
- **FPCA-02 (rank 18, score 1.73, P3 differentiator, M-effort):** specialized FPCA variants — `fpca_der`, `fsvd` (functional SVD / cross-FPCA), cross-covariance surfaces, dynamical/functional correlation, and the sandwich-smoother / sparse-SVD FPCA path. New `fpca_variants.rs`, reusing `fdata_to_pc_1d` + `covariance.rs`. R baseline: `fdapace` / `refund`.
- **SPARSE-01 (rank 19, score 1.73, P3 differentiator, M-effort):** the FACE fast-sandwich covariance estimator for sparse functional data, the multivariate `mfaces` extension, and integrated fitted-trajectory-with-confidence-band output. Extended `irreg_fdata/` (new `face.rs`), building on `cov_irreg` and integrating with the shipped PACE `pace_fpca`. R baseline: `face` / `mfaces` / `fdapace`.

**Key context:** Implementation milestone — additive/non-breaking, reuse-first, no new crate dependency, numeric outputs only. Phase numbering continued from v0.25.0 → Phases 37/38 (independent, disjoint code areas).

</details>

<details>
<summary>v0.25.0 milestone goal (shipped 2026-08-21)</summary>

**Goal:** Draw the next three top-ranked items (all score 1.73, M-effort) from the v0.18.0 `R-BACKLOG.md` — broadening fdars' functional-time-series diagnostics, representation layer, and density-FDA families — each by adding or extending `fdars-core/src/` modules, additively and non-breaking.

**Target features (from R-BACKLOG.md):**
- **FTS-02 (rank 14, score 1.73, P2 differentiator, M-effort):** **functional serial-dependence tooling** — L2-norm functional ACF/PACF with strong-white-noise confidence bands, a functional stationarity test, long-run covariance via a kernel-sandwich estimator, and a functional differencing operator. New `fts/acf.rs`, reusing `helpers` quadrature + `covariance.rs`. R baseline: `ftsa` / `fdaACF`.
- **REP-01 (rank 16, score 1.73, P2 differentiator, M-effort):** **basis-system completions** — `monomial_basis`, `exponential_basis`, `power_basis`, `polygonal_basis` factories (with penalty matrices) in `basis/`; a `MultiFunData` multivariate/multi-domain container (new `multi_fdata.rs`); a composable `Lfd`/linear-differential-operator object; and a `principal_differential_analysis` estimator. R baseline: `fda` / `funData` / `tf`.
- **DENS-01 (rank 17, score 1.73, P3 differentiator, M-effort):** **density object-data FDA** — the log-quantile-density (LQD) transform + inverse, LQD-FPCA for probability densities (reuse `fdata_to_pc_1d` in LQD space), a 1D Wasserstein Fréchet mean (quantile-average barycenter), and density normalization/regularization. New `density_fda.rs`. R baseline: `fdadensity`.

**Key context:** Implementation milestone — additive/non-breaking, reuse-first, no new crate dependency, numeric outputs only. Phase numbering continued from v0.24.0 → Phases 34/35/36 (independent, disjoint code areas).

</details>

<details>
<summary>v0.24.0 milestone goal (shipped 2026-08-20)</summary>

**Shipped:** v0.24.0 Functional Regression & Clustering Breadth — REG-04 (additive functional regression + variable selection, Phase 31), REG-05 (flexible mixed-effects regression, Phase 32), CLUS-01 (model-based/density functional clustering, Phase 33) — the three top-ranked P2 differentiators from `R-BACKLOG.md` (score 1.73 each). All delivered, verified 5/5, integrated (2268-test suite green, `cargo clippy --all-targets` clean). Real `fdars-core/src/` code, additive/non-breaking, no new dependency. See [`milestones/v0.24.0-ROADMAP.md`](milestones/v0.24.0-ROADMAP.md).

</details>

## Requirements

### Validated

<!-- Inferred from existing codebase (see .planning/codebase/). These already ship and are relied upon. -->

- ✓ Scalar-on-function regression (linear `fregre_lm`, functional logistic) — existing
- ✓ Functional PCA (`FpcaResult`, 1D + 2D FOSR with tensor-product penalty) — existing
- ✓ Classification (LDA, QDA, kNN, kernel, DD) with FPC-space fitting — existing
- ✓ Depth measures (Fraiman-Muniz, modal, band, random projection) + streaming depth — existing
- ✓ Elastic curve registration & shape analysis (elastic regression, elastic FPCA, PDO curves) — existing
- ✓ Seasonal analysis (period detection, peak finding, STL decomposition) — existing
- ✓ Statistical process monitoring (control charts, outlier detection) — existing
- ✓ Model explainability (PDP, SHAP, LIME, ALE, importance) via generic `FpcPredictor` trait — existing
- ✓ Clustering (GMM) and irregular functional data support — existing
- ✓ Core infrastructure: column-major `FdMatrix`, `Result`-based error handling, feature-gated rayon parallelism, ~1,650 tests, 8 criterion benchmarks, 28 examples, WASM/JS + R bindings — existing
- ✓ **Static hot-path analysis** — zero-cost per-module bottleneck-candidate map (complexity in N/M, allocation hotspots incl. 8 `to_dmatrix()` SVD copies + 14 `from_column_slice` basis sites, parallelism gaps, feature-gate annotations) in `.planning/research/AUDIT-REPORT.md` — Validated in Phase 2 (PERF-01)
- ✓ **FPCA/SVD & allocation audit** — criterion 6-cell N×M grid + elastic-FPCA cells and a dhat allocation baseline (feature-gated `dhat-heap`) quantify the `FdMatrix→DMatrix` SVD-copy as ~0.14–0.17% of wall-clock: SVD compute dominates (~99.8%). Report carries the SVD-vs-copy split, a **Phase-6 GO** verdict (faer-vs-nalgebra comparison warranted), and a GSD-ready backlog in `.planning/research/AUDIT-REPORT.md` — Validated in Phase 4 (PERF-03, PERF-04)
- ✓ **Parallelism gap assessment** — criterion rayon thread-scaling (heavy `karcher_mean` + light `StreamingFraimanMuniz::depth_batch` sentinels, RAYON_NUM_THREADS ∈ {1,2,4,8}) plus payback-threshold N per target (karcher N≤10, streaming N≈50), a 5-candidate safe-to-parallelize list with source anchors, the default unaccelerated-path cost (rayon-off ~10×), and a GSD-ready backlog (P5-1..P5-4) in `.planning/research/AUDIT-REPORT.md` §Phase 5. Governor unpinned → multi-thread cells flagged LOW-CONFIDENCE. Zero `fdars-core/src/` edits (audit-only). — Validated in Phase 5 (PERF-05)
- ✓ **Conditional SVD library comparison** — criterion 7-cell N×M grid measuring faer `thin_svd` (zero-copy `MatRef` view) vs nalgebra `SVD::new` (clone-then-SVD) at fdars' real FPCA sizes, with a `svd_equivalence` integration test confirming agreement within 1e-10. faer measured **1.8–4.1× faster** (primary cell N=500,M=200: 1.8×), zero-copy conversion costs ~3.5–7.7 ns. Report §Phase 6 carries the GO verdict, comparison table, faer adoption/maintenance-risk note, and a GSD-ready backlog item **P6-1** (swap to faer `thin_svd`, P2/S-effort borderline with explicit downgrade condition). Zero `fdars-core/src/` edits (audit-only). — Validated in Phase 6 (PERF-06)
- ✓ **scikit-fda capability enumeration** — versioned (skfda **0.10.1**, runtime-verified `__version__`), area-organized capability inventory in `.planning/research/AUDIT-REPORT.md` §Phase 7: six areas (representation, preprocessing, exploratory, ML, inference, misc) enumerated capability-first (fit/predict/transform collapsed per row, Pitfall 9 avoided), plus a one-page Design-Goal Filter separating **129 in-scope** (numeric algorithms + API-ergonomics) from **32 out-of-scope** (plotting + IO) = **161 total**. This is the scikit-fda side of the parity comparison Phase 8 will map fdars against. Zero `fdars-core/src/` edits (audit-only). — Validated in Phase 7 (GAP-01)
- ✓ **Capability parity matrix & categorization** — the fdars-vs-scikit-fda parity comparison in `.planning/research/AUDIT-REPORT.md` §Phase 8: a single verdict rubric (present/partial/absent, D-01) and category rubric (table-stakes/differentiator/out-of-scope, D-03) driving **six area parity tables** (141 capability rows, 59 present / 19 partial / 63 absent), mapped by capability not API name with 83 "searched fdars for:" notes. Known-bug rows carry "present — accuracy NOT verified" flags citing fix commits (`2fb6d3c9` #33, `6ed62398` #34, `ec17d138` GMM). Separates **82 actionable in-scope gaps** from 32 out-of-scope (plotting/IO). A **reverse-parity strengths sweep** documents 30 fdars-only capabilities (explainability, SPM, seasonal, streaming depth + 26 more from a full module-map walk). A drafted **UNRANKED** gap backlog (21 entries + a D-02a numerical-accuracy validation item) feeds Phase 9 value ranking. Verified 9/9 must-haves; zero `fdars-core/src/` edits (audit-only). — Validated in Phase 8 (GAP-02, GAP-03, GAP-04)
- ✓ **Benchmark confirmation** — the top performance suspects were measured with real criterion numbers across the audit: FPCA/SVD grid (Phase 4, SVD ~99.8% of wall-clock), rayon thread-scaling (Phase 5, ~4.73× at 8 threads), faer-vs-nalgebra SVD (Phase 6, 1.8–4.1× faster), and elastic-alignment cost (top bottleneck: N=500,M=50 → 37.82 s; M=200 infeasible). — Validated across Phases 4–6
- ✓ **Consolidated audit report** — `.planning/research/AUDIT-REPORT.md` (2,646 lines) flipped to **"Complete — audit milestone v0.14.0"**: `## Methodology (Consolidated)` (feature-flag matrix + infra-vs-code triage rule), `## Consolidated Findings` with five performance findings (PF-1..PF-5, each linked to a real bench artifact), gap findings (82 in-scope / 32 out-of-scope excluded), and an fdars-strengths summary (30 capabilities, 4 SC3 headliners). All 8 prior Phase sections preserved. Verified 7/7 must-haves; zero `fdars-core/src/` edits (audit-only). — Validated in Phase 9 (RPT-01, RPT-02)
- ✓ **GSD-ready prioritized backlog** — `.planning/research/BACKLOG.md` (873 lines): `score = value / sqrt(effort)` ranking methodology (5-level value scale, S/M/L effort, P1/P2/P3 severity), a master **Ranked Backlog of 32 items** sorted 4.00 → 0.58 (strictly non-increasing), and 34 fully-worked 7-field item blocks (8 performance + 24 gap + ACC-VALIDATE). Completeness Gate PASSED (5 P1 items, top-10 non-cosmetic, strictly descending). Ready to promote via `/gsd-new-milestone`. — Validated in Phase 9 (RPT-03)
- ✓ **Spline interpolation** — `spline_interpolate(data, argvals, query_points, order) -> Result<FdMatrix, FdarError>` in `helpers.rs`: fits an order-k B-spline per curve over the existing `basis/` system (knots → `bspline_basis` → SVD pseudoinverse → evaluate at query points) and returns a new `FdMatrix`; re-exported at the crate root. 5 inline tests (exact reproduction + off-grid cubic accuracy within 1e-10, plus 3 validation paths); existing linear-interpolation path retained (additive). — Validated in Phase 10 (FEAT-01)
- ✓ **Functional summary statistics** — five public functions over `FdMatrix` in `fdata.rs`, all `Result`-returning and re-exported at the crate root: `functional_variance`/`functional_std` (Bessel-corrected pointwise, std²==var by construction), `functional_covariance` (symmetric M×M with `checked_mul` overflow guard), `depth_based_median` and `trim_mean` (via `fraiman_muniz_1d` self-depth). 7 inline tests against hand-computed references + error paths. — Validated in Phase 10 (FEAT-02)
- ✓ **Parallel CV folds** — `fclassif_cv`'s fold loop in `classification/cv.rs` now runs via `iter_maybe_parallel!(0..nfold).map(...).collect()` (indexed collect preserves order), parallel under the `parallel` feature and bit-for-bit identical to sequential; a `test_fclassif_cv_parallel_matches_sequential` inline test proves determinism. No new deps. — Validated in Phase 11 (PERF-01)
- ✓ **faer FPCA SVD** — `fdata_to_pc_1d` in `regression.rs` computes its SVD via faer `Svd::new_thin` on a zero-copy `MatRef::from_column_major_slice` under `#[cfg(feature = "linalg")]` (eliminating the `to_dmatrix()` copy), retains the nalgebra path under `#[cfg(not(feature = "linalg"))]`, and reconciles singular-vector signs via a shared `fix_svd_signs` helper. A `#[cfg(all(test, feature = "linalg"))]` `test_faer_svd_matches_nalgebra` proves numerical parity within 1e-8·σ₁. No new deps. — Validated in Phase 11 (PERF-02)
- ✓ **Banded elastic alignment (opt-in)** — new `karcher_mean_with_band` / `elastic_self_distance_matrix_with_band` / `elastic_cross_distance_matrix_with_band` (`band_frac: Option<f64>`) in `alignment/`, re-exported at the crate root; `None` = exact unbanded (existing behavior preserved), `Some(f)` = banded (4–6× faster, makes N=500,M=200 feasible). Existing signatures untouched (non-breaking); 9 equivalence tests. — Validated in Phase 12 (PERF-03)
- ✓ **Missing-value imputation** — `impute_missing_values(data, argvals, method) -> Result<FdMatrix, FdarError>` + `ImputationMethod{Linear,Mean,Constant(f64)}` in `helpers.rs`; NaN detection, all-NaN + zero-column guards, boundary extension. — Validated in Phase 13 (FEAT-03)
- ✓ **Composable extrapolation policy** — `ExtrapolationPolicy{Boundary,Exception,Fill(f64),Periodic}` threaded non-breakingly through both interpolation paths via `fdata_interpolate_with_policy` and `spline_interpolate_with_policy`. — Validated in Phase 13 (FEAT-04)
- ✓ **Functional scoring metrics** — new `scoring.rs`: `functional_mae/mse/mape/msle/explained_variance(y_true, y_pred, argvals) -> Result<f64, FdarError>`, Simpson-integrated with domain guards (MAPE zero-denominator, MSLE log domain). — Validated in Phase 13 (FEAT-05)
- ✓ **Least-squares shift registration** — `least_squares_shift_registration(data, argvals, max_shift) -> Result<ShiftRegistrationResult, FdarError>` in new `alignment/shift.rs`: per-curve rigid horizontal shift to the cross-sectional sample mean (`mean_1d`) via golden-section L2 minimization (Simpson-weighted, `linear_interp` boundary-clamped resampling), returning `ShiftRegistrationResult { registered_data, shifts }`; crate-root re-exported. Fills the "simplest registration method" gap. — Validated in Phase 14 (FEAT-06, v0.17.0)
- ✓ **Registration-quality scores** — `least_squares_score`, `pairwise_correlation_score` (centered functional Pearson), `sobolev_least_squares_score(…, lambda)` in `alignment/quality.rs`, all `Result<f64, FdarError>`, standalone-energy form (documented divergence from scikit-fda's ratio-based scorers). — Validated in Phase 14 (FEAT-07, v0.17.0)
- ✓ **Parallel elastic-FPCA** — the three per-curve loops in `elastic_fpca.rs` (`shooting_vectors_from_psis`, `build_augmented_srsfs`, `svd_scores_and_eigenvalues`) parallelized via `iter_maybe_parallel!` collect-then-assign; the light score-fill guarded by `SCORES_PARALLEL_THRESHOLD = 50` (outer-if); bit-identical to sequential (tested `parallel` ON and OFF), no signature changes. — Validated in Phase 15 (PERF-04, v0.17.0)
- ✓ **R-ecosystem capability inventory** — `.planning/research/R-AUDIT-REPORT.md` §Phase 16: versioned, capability-first inventory of 35 R FDA packages, 275 capabilities across 9 areas, design-goal-filtered to 248 in-scope. — Validated in Phase 16 (INV-01/INV-02, v0.18.0)
- ✓ **fdars-vs-R parity matrix + categorization** — §Phase 17: 250 in-scope R capabilities mapped present/partial/absent (88/49/113), 162 actionable gaps categorized 18 table-stakes / 144 differentiator. — Validated in Phase 17 (GAP-01/GAP-02, v0.18.0)
- ✓ **Reverse-parity strengths sweep** — §Phase 18: 42-module walk; 12 R-honest fdars strengths (headliners: model explainability for functional models, streaming depth — no R FDA package touches either). — Validated in Phase 18 (GAP-03, v0.18.0)
- ✓ **R-ecosystem consolidated report + ranked backlog** — §Phase 19 + `.planning/research/R-BACKLOG.md`: 26 GSD-ready value-ranked items (`score = value/√effort`, 5.00→0.67), completeness gate PASS. — Validated in Phase 19 (RPT-01/RPT-02, v0.18.0)
- ✓ **Two-sample functional inference** — new `fdars-core/src/inference/` module: `t_perm_test`, `f_perm_test` (permutation), `two_sample_mean_test` (Hotelling T² on a shared FPC basis), `mean_scb` + `scb_two_sample_test` (Degras SCB), plus `TestResult`. First standalone inference surface (Area 5 was 0/22). 999-perm default, seeded; reuses `function_on_scalar`/`spm::stats`/`tolerance::degras`. — Validated in Phase 20 (INF-01, v0.19.0)
- ✓ **Functional-linear-model inference** — `flm_f_test` (overall-significance F), `flm_gof_test` (Ramsey-RESET residual lack-of-fit), `oneway_anova_vstat` (Simpson-integrated V-stat, Satterthwaite scaled-χ² p-value) added alongside the unchanged permutation `fanova`; self-contained F/χ² survival fns in `inference/dist.rs` (no new dep). — Validated in Phase 21 (INF-02, v0.19.0)
- ✓ **Depth-measure long tail** — 9 canonical batch depth measures added to `depth/`: hypograph/modified-hypograph/epigraph indices (HI/MHI/EI), half-region & modified half-region depth (HRD/MHRD), extremal depth, extreme-rank-length depth (ERL), L∞ depth, and total-variation depth with MSSI — each a `Result`-returning fn registered in the `DepthMethod` dispatcher; `TvdMssResult { tvd, mss }` pinned as a forward contract for OUT-01. Batch only (excludes streaming depth). Additive/non-breaking, no new dep. — Validated in Phase 28 (DEPTH-01, v0.23.0)
- ✓ **Outlier-detector suite** — `tvdmss` (two-stage TVD+MSSI, consuming DEPTH-01's `total_variation_depth_1d`), `muod` (Fast-MUOD regression-vs-mean indices), `sequential_transform_outliers` (T0/T1/T2/D1 cumulative + functional boxplot), and the `depthgram` statistic in `outliers.rs` — numeric outputs reusing the MS-plot / outliergram machinery + a shared `iqr_fence` helper. Additive/non-breaking, no new dep. — Validated in Phase 29 (OUT-01, v0.23.0)
- ✓ **Interval Testing Procedure family** — new `inference/itp.rs`: `itp_one_pop`, `itp_two_pop`, `itp_flm` — B-spline/Fourier basis-wise permutation tests with the Pini & Vantini interval-wise closure adjustment (`pval_correct`) for domain-selective adjusted p-values, reusing the INF-01 permutation pattern + `basis/` projection. Additive/non-breaking, no new dep. — Validated in Phase 30 (INF-03, v0.23.0)

- ✓ **Constant basis + AIC smoothing selection** — `constant_basis` (m×1 intercept column) in `basis/constant.rs`; `CvCriterion::Aic` + `aic_smoother` (kernel bandwidth) and `smooth_basis_aic` (basis roughness penalty), reusing the GCV hat-matrix trace. `CvCriterion` now `#[non_exhaustive]`. — Validated in Phase 22 (T-01, v0.20.0)
- ✓ **Functional boxplot + depth dispatcher** — `functional_depth(data, DepthMethod)` self-depth dispatcher + canonical López-Pintado–Romo `functional_boxplot` (numeric central-region/whisker/outlier outputs, `FunctionalBoxplotResult`) in `depth/dispatch.rs`, wrapping the existing depth functions. — Validated in Phase 23 (T-02, v0.20.0)
- ✓ **Concurrent / varying-coefficient regression (dense)** — `concurrent_regression(response, predictors, argvals, bandwidth, kernel) -> Result<ConcurrentRegrResult, FdarError>` in new `concurrent_regression.rs`, crate-root re-exported: pointwise OLS β(t) per grid column (reusing `smoothing::solve_gaussian_pub` + ridge stabilizer) then local-linear kernel smoothing (`smoothing::local_linear`); result `{ beta_curve (p×m), intercept, fitted, residuals, argvals }` with time-varying intercept. NaN/Inf-bandwidth + underdetermined-`n≤p` guards; 9 inline tests (recovery, monotone roughness, residual consistency, error paths). Additive/non-breaking. — Validated in Phase 24 (REG-01, v0.21.0)
- ✓ **Functional GLM (exponential family)** — `functional_glm(data, y, family, scalar_covariates, ncomp, max_iter, tol) -> Result<FunctionalGlmResult, FdarError>` (+ `predict_functional_glm`) in new `scalar_on_function/glm.rs`, crate-root re-exported: IRLS over `fdata_to_pc_1d` FPC scores generalizing the `functional_logistic` loop, with `#[non_exhaustive] GlmFamily { Binomial, Poisson, Gamma, Gaussian }` (canonical link + variance each). `Binomial` reproduces `functional_logistic` within 1e-6 (logistic signature unchanged). Per-family response-domain + non-finite guards; dispersion-scaled SEs (Gaussian/Gamma); inline Lanczos `ln_gamma` (no new dependency). 12 inline tests. Code review caught + fixed a Gamma IRLS-weight-inversion correctness bug (μ² not 1/μ²) with a strengthened recovery test. Additive/non-breaking. — Validated in Phase 25 (REG-02, v0.21.0)
- ✓ **PACE sparse FPCA** — `pace_fpca(&IrregFdata, &PaceFpcaConfig) -> Result<PaceFpcaResult, FdarError>` in new `pace_fpca.rs`, crate-root re-exported: smoothed mean + `cov_irreg`-surface symmetric eigendecomposition + **newly-implemented** BLUP/conditional-expectation FPC scores + fitted trajectories with Ω prediction-variance bands. Reuses `irreg_fdata`/`cov_irreg`/`mean_irreg` + nalgebra eig + `linalg::cholesky_solve`; local z-quantile (no new dep). 15 inline tests. Code review fixed 1 blocker (NaN-mean → all-NaN) + 4 warnings. (Backlog's `spm::partial::conditional_expectation` reference was inaccurate — the BLUP scorer was implemented, not orchestrated.) Additive/non-breaking. — Validated in Phase 26 (FPCA-01, v0.22.0)
- ✓ **Elastic multinomial regression** — `elastic_multinomial(data, y:&[usize], argvals, ncomp_beta, lambda, max_iter, tol) -> Result<ElasticMultinomialResult, FdarError>` (+ `predict_elastic_multinomial`) in `elastic_regression/logistic.rs`, crate-root re-exported: one-vs-rest over the unchanged binary `elastic_logistic` (K fits, row-normalized class posteriors, argmax labels). Completes fdars' elastic-regression family. 11 inline tests (K-class recovery, K=2↔binary agreement, 6 guards). Code review fixed 2 blockers (serde-feature compile break; normalization Inf/NaN) + 3 warnings. Additive/non-breaking (binary path unchanged), no new dep. — Validated in Phase 27 (REG-03, v0.22.0)
- ✓ **Additive functional regression + variable selection** — new `scalar_on_function/additive.rs`: `fam` (one-pass NW additive smooth over FPC scores — FPC-uncorrelatedness makes backfitting unnecessary), `fregre_gkam` (iterative L2-distance kernel backfitting), `fregre_gsam` (spectral, FAM-equivalent under identity link), `variable_selection` (group-lasso with 5-fold CV λ-selection), `permutation_test_fam` (seeded, 999-default), `history_index` (marginal-integration lagged-window). Reuses `smoothing.rs` + `fdata_to_pc_1d`; crate-root re-exported. Code review caught + fixed a broken monotonic-MSE λ-selection + a raw-gradient singular fallback (2 blockers) + 5 warnings. 28 tests + 6 doctests. Additive/non-breaking, no new dep. — Validated in Phase 31 (REG-04, v0.24.0)
- ✓ **Flexible mixed-effects regression** — extended `famm.rs` with `dense_flmm` (denseFLMM over FPC scores, reusing the existing REML-EM `fit_scalar_mixed_model` per component), `multi_famm` (D independent per-dimension FPCAs), `fast_fmm` (massively-univariate per-gridpoint fit + running-mean smoothing + Wald inference), and wired `fof_re_regression`/`predict_fof_re` into `fof_regression.rs`; 6 famm helpers promoted private→pub(crate). Documented R divergences (FPC-score not basis-coeff; running-mean not mgcv). Code review fixed inert config fields (fastFMM max_iter/tol) + 4 warnings. 41 famm + 16 fof tests. Additive/non-breaking, no new dep. — Validated in Phase 32 (REG-05, v0.24.0)
- ✓ **Model-based & density functional clustering** — `funhddC_cluster` (per-group AkBk subspace covariance EM in new `gmm/subspace.rs`, reusing gmm E-step + k-means++), plus `dbscan_fd` (density over `distance.rs` L2, noise=`Option<usize>` None), `kcfc_cluster` (per-cluster FPCA reassignment), `funfem_cluster` (Fisher-EM discriminative subspace), `align_cluster_fd` (elastic k-means with Karcher-mean templates, reusing `alignment/`) in new `clustering_advanced.rs`; test-only `adjusted_rand_index` helper. Code review fixed an E-step complement-clamp + false-convergence-after-reinit (2 blockers) + 3 warnings. 39 clusterer tests + 6 doctests. Additive/non-breaking, no new dep. — Validated in Phase 33 (CLUS-01, v0.24.0)
- ✓ **Functional serial-dependence tooling** — new `fts/acf.rs`: L2-norm functional ACF/PACF with strong-white-noise confidence bands, a functional stationarity test, long-run covariance via a kernel-sandwich estimator, and a functional differencing operator; reuses `helpers` quadrature + `covariance.rs`. 25 tests, clippy `--all-targets` clean, no new dep. Additive/non-breaking. — Validated in Phase 34 (FTS-02, v0.25.0)
- ✓ **Basis-system completions** — `monomial_basis`/`exponential_basis`/`power_basis`/`polygonal_basis` factories (with penalty matrices) returning a `BasisSystem` in `basis/`; a `MultiFunData` multivariate/multi-domain container (new `multi_fdata.rs`); a composable `Lfd`/linear-differential-operator object; and a `principal_differential_analysis` estimator (new `pda.rs`) — all crate-root re-exported. 2363 tests + 163 doctests, clippy `--all-targets` clean, no new dep. — Validated in Phase 35 (REP-01, v0.25.0)
- ✓ **Density object-data FDA** — new `density_fda.rs`: the log-quantile-density (LQD) transform + inverse, LQD-FPCA for probability densities (reusing `fdata_to_pc_1d` in LQD space), a 1D Wasserstein Fréchet mean (quantile-average barycenter), and density normalization/regularization. 22 tests, clippy `--all-targets` clean, no new dep. — Validated in Phase 36 (DENS-01, v0.25.0)
- ✓ **Specialized FPCA variants** — new `fpca_variants.rs`: `fpca_der` (FPCA of differentiated curves, reusing `deriv_1d`+`fdata_to_pc_1d`), `fsvd` (functional SVD / cross-FPCA via Gram-matrix symmetric eigendecomposition — robust where nalgebra's general SVD fails to converge — returning `FsvdResult`), `cross_covariance` (sample-centered p×q surface), `dynamical_correlation` (exact Dubin–Müller / fdapace DynCorr scalar in [-1,1]), and `ssvd` (sandwich-smoother FPCA matching `fdata_to_pc_1d` in the dense limit). All crate-root re-exported, documented R divergences. 19 tests, clippy `--all-targets` clean, no new dep. — Validated in Phase 37 (FPCA-02, v0.26.0)
- ✓ **Sparse fast covariance & trajectory bands** — new `irreg_fdata/face.rs`: `face_covariance` (FACE kernel-sandwich covariance over `cov_irreg` + symmetric-eigen PSD projection, reusing the Phase-37 `gaussian_smooth_cov` via a `pub(crate)` bump), `mface_covariance` + `MfaceCovResult` (multivariate `mfaces` block covariance with cross-variable blocks + `block()` accessor), and `face_trajectory` (fitted trajectories + pointwise bands, thin `pace_fpca` delegation). Documented K-FACE-vs-P-FACE divergence. 9 tests, clippy `--all-targets` clean, no new dep. — Validated in Phase 38 (SPARSE-01, v0.26.0)
- ✓ **Functional time-series forecasting** — new `fts/forecast.rs`: `ftsm` (FPCA-based functional time-series model via `fdata_to_pc_1d` + per-component Yule-Walker AR with AIC order selection), `ftsm_forecast` / `ftsm_forecast_multistep` (iterative-plug-in FPC-score AR forecast → reconstructed curves; h=1 bit-identical by delegation), `ftsm_update` (dynamic update projecting new obs onto frozen loadings, no FPCA refit), and `fplsr` (lag-1 per-point PLS forecasting reusing `fregre_pls`). Reuses `fdata_to_pc_1d` + `scoring.rs` + FTS-02's `fts/acf.rs` patterns; deterministic, additive/non-breaking, no new dep. R baseline `ftsa`. 23 tests. — Validated in Phase 39 (FTS-01, v0.27.0)
- ✓ **Fréchet / object-data regression + statistics** — new `frechet/` module: a `MetricSpace` trait (distance + weighted-Fréchet-mean solver) with a `WassersteinDensitySpace` backend, `wasserstein2_distance` (quantile-L2), `frechet_mean`/`frechet_variance`, `frechet_global_reg` (Petersen–Müller global linear weights) + `frechet_local_reg` (local-linear Gaussian kernel) predicting conditional density responses via a signed quantile average + sort-based isotonic projection (no osqp), and `frechet_anova` (Dubey–Müller Tₙ with seeded-permutation + asymptotic χ²(k−1) p-values). Shares DENS-01's `density_fda.rs` quantile/Wasserstein machinery; additive/non-breaking, no new dep. R baseline `frechet`. 23 tests. — Validated in Phase 40 (FRE-01, v0.27.0)
- ✓ **Spectral functional time series** — new `fts/spectral.rs`: `spectral_density` (Bartlett-weighted DFT over lagged autocovariance operators via `rustfft` → per-frequency Hermitian operator), `dpca` (per-frequency eigen-filters + Simpson-metric-consistent dynamic scores), `dpca_reconstruct` (inverse dynamic filtering, monotone-decreasing reconstruction error); plus `sim_fvarma`/`sim_farma` seeded operator-kernel simulators in `simulation.rs`. Reuses `rustfft` + FTS-01/02 autocovariance; documented `freqdom`/`ftsa` divergences (Re-only eigendecomposition, 1/2π omission). Additive, no new dep. 18 tests. — Validated in Phase 41 (FTS-03, v0.28.0)
- ✓ **Object-data Fréchet regression across metric spaces** — new `frechet/spaces/`: `SpdMatrixSpace`/`SpdMetric` (Frobenius/power-α/log-Cholesky), `CorrelationMatrixSpace`, `SphericalSpace` (geodesic exp/log + Karcher mean), `NetworkSpace` (graph-Laplacian), `PointProcessSpace` (intensity L2) — each `impl MetricSpace`; plus generic `frechet_global_reg_space`/`frechet_local_reg_space`/`frechet_anova_space` reusing extracted `pub(crate)` `compute_global_weights`/`compute_local_weights`/`compute_tn_generic` (density path bit-identical, non-breaking). R baseline `frechet` 0.3.0. Additive, no new dep. 36 tests. — Validated in Phase 42 (FRE-02, v0.28.0)

### Active

<!-- v0.28.0 in progress — the two dependency-satisfied score-1.00 (L-effort) R-BACKLOG items (FTS-03 + FRE-02), Phases 41/42. Remaining 1.00/0.67 tier (REG-06, REP-02, CLUS-02) deferred to a following milestone. -->

- [ ] **FTS-03 — Spectral functional time series** (Phase 41): DPCA via spectral methods, spectral-density-operator estimation, functional VAR/VMA simulation, FARMA simulation. New `fts/spectral.rs` + `simulation.rs` FARMA/VAR simulator, reusing `rustfft`; builds on shipped FTS-01/FTS-02. R baseline `freqdom`/`ftsa`.
- [ ] **FRE-02 — Object-data Fréchet regression** (Phase 42): covariance-matrix (Frobenius/power/log-Cholesky), correlation-matrix, spherical (geodesic exp/log), network, and point-process response spaces — each with a Fréchet-ANOVA analog — as pluggable metric backends for the shipped FRE-01 solver. Extends `frechet/`. R baseline `frechet` 0.3.0.

### Out of Scope

<!-- Explicit boundaries with reasoning. -->

- Implementing the R-parity gaps found — deferred to future milestones; v0.18.0 is audit-only to keep scope bounded and decisions evidence-driven
- Plotting/visualization parity with R packages (`rainbow` bagplots, `roahd`/`fdaoutlier` graphical outputs, `fda` plot methods) — a numeric Rust library does not need base-R/ggplot-style rendering; the *numeric* underpinnings (e.g. outliergram/MS-plot statistics) may be in scope, but the plots themselves are not
- Data/IO parity (R dataset loaders, `read`/`write` round-trips) — out of scope, consistent with the v0.14.0 audit fence
- Re-auditing scikit-fda — its actionable backlog is exhausted (v0.15.0–v0.17.0); R is now the sole comparison yardstick for this milestone

## Context

- Brownfield: fdars is at v0.14.0 with a complete codebase map in `.planning/codebase/`. Architecture is a modular monolith with a layered design (public API → domain modules → shared infra → external deps).
- Existing benchmarking foundation: criterion 0.5 is already a dev-dependency with 8 benchmarks and HTML reports, so the "real benchmarks" work extends existing infrastructure rather than starting from scratch.
- Known perf anti-patterns already documented in `.planning/codebase/ARCHITECTURE.md`: dense matrix copy in SVD (FdMatrix ↔ nalgebra DMatrix round-trips), unvalidated slice access in hot loops, NaN-handling inconsistency.
- Stack: Rust 2021, MSRV 1.81 (1.84 for `linalg`), nalgebra 0.33, rayon 1.10, rustfft 6.2, faer 0.23 (optional), anofox-regression 0.4.
- Comparison target scikit-fda is a Python library — gap analysis is API/capability comparison, not a runtime benchmark against Python.

## Constraints

- **Scope**: Audit-only milestone — deliverables are a report + backlog, not code changes to `fdars-core`.
- **Baseline**: scikit-fda is the sole functionality-gap yardstick for this milestone.
- **Tech stack**: Benchmarks use the existing criterion 0.5 harness; performance reasoning must respect the column-major `FdMatrix` layout and feature-gated parallelism model.
- **Output**: Backlog items must be GSD-ready (phrased as candidate requirements/phases) so they can be promoted into future milestones.

## Current State

- **Shipped: v0.27.0 Functional Time Series & Fréchet Regression** (2026-08-22) — 2 phases (39–40), 6 plans, both complete & verified (5/5 must-haves each), milestone audit **PASSED** (13/13 requirements, integration PASS). The two score-1.33 (L-effort) `R-BACKLOG.md` items, exhausting the 1.33 tier: **FTS-01** functional time-series forecasting (new `fts/forecast.rs` — `ftsm`/`ftsm_forecast`/`ftsm_forecast_multistep`/`ftsm_update`/`fplsr`) and **FRE-01** Fréchet/object-data regression (new `frechet/` — `MetricSpace`/`WassersteinDensitySpace`, `frechet_mean`/`frechet_variance`, `wasserstein2_distance`, `frechet_global_reg`/`frechet_local_reg`, `frechet_anova`). Both reuse-first (fts reuses `fdata_to_pc_1d`+`scoring.rs`+`fts/acf.rs`; frechet reuses `density_fda.rs`), additive/non-breaking, **no new dependency**. Executed inline (worktree/executor-stall memory); each phase independently re-verified. Notable divergences documented in rustdoc: signed-weight regression uses a sort-based isotonic projection instead of R `frechet`'s osqp QP; Fréchet ANOVA σ̂ₗ² estimator flagged `[ASSUMED]` (permutation p-value is the primary, robust inference); Wasserstein-barycenter density round-trip carries a documented ~0.1–0.15 W₂ reconstruction floor. **Full suite 2460 lib + 172 doc tests green, `cargo clippy --all-targets --features linalg,parallel` clean, `cargo fmt --check` clean.** **Released as crate `fdars-core` 0.27.0 on crates.io** (bumped 0.24.0 → 0.27.0, tag `v0.27.0` pushed → `release.yml` `cargo publish` succeeded); `CHANGELOG.md` added. The 0.27.0 crate folds in all additive code shipped since 0.24.0 (the v0.25.0/v0.26.0 milestones were never published as separate crate versions).

- **Shipped: v0.22.0 PACE Sparse FPCA & Elastic Multinomial** (2026-08-19) — 2 phases (26–27), both complete & verified (5/5 each), milestone audit passed (2/2). FPCA-01: `pace_fpca` unified PACE sparse FPCA (new `pace_fpca.rs`) — the final P1 table-stakes item, with a newly-implemented BLUP conditional-expectation scorer + Ω bands. REG-03: `elastic_multinomial` one-vs-rest (extends `elastic_regression/logistic.rs`), completing the elastic family. Both reuse-first, additive/non-breaking, **no new dependency**. Code review caught + fixed real bugs both phases (NaN-mean guard, BLUP/band consistency; serde-feature compile break, normalization Inf/NaN). **Full suite 2107 lib tests green, clippy `--all-targets` clean, serde-feature build verified.** **P1 table-stakes tier now exhausted** — future milestones draw differentiators (DEPTH-01, OUT-01, INF-03, …). Released as crate `fdars-core` 0.22.0 on crates.io (tag v0.22.0).

- **Shipped: v0.21.0 Functional Regression Completeness** (2026-08-17) — both phases **complete & verified** (5/5 must-haves each), milestone audit passed (2/2). Phase 24 (REG-01): dense concurrent/varying-coefficient regression (`concurrent_regression` + `ConcurrentRegrResult`, new `concurrent_regression.rs`), pointwise-OLS-then-kernel-smooth β(t). Phase 25 (REG-02): functional GLM (`functional_glm` + `GlmFamily{Binomial,Poisson,Gamma,Gaussian}` + `FunctionalGlmResult`, new `scalar_on_function/glm.rs`), IRLS over FPC scores generalizing `functional_logistic`. Both additive/non-breaking, reuse-first, **no new dependency**. Code review caught + fixed real bugs both phases (Phase 24: NaN/Inf-bandwidth + `n≤p` guards; Phase 25: Gamma IRLS-weight inversion, Poisson factorial overflow, predict/covariate dimension guards). **Full suite 2081 lib + doctests green, clippy `--all-targets` clean.** **Released as crate `fdars-core` 0.21.0 on crates.io** (bumped 0.20.0 → 0.21.0, tag `v0.21.0` pushed, `release.yml` publish succeeded).
- **Shipped: v0.20.0 Table-Stakes Quick Wins** (2026-08-16) — 2 phases (22–23), 2 plans, both VERIFICATION passed (independently re-verified), milestone audit passed (2/2). The two top-ranked (score 5.00, P1 table-stakes, S-effort) R-backlog items, each wrapping existing code: **T-01** `constant_basis` + AIC smoothing selection (`CvCriterion::Aic`/`aic_smoother`/`smooth_basis_aic`), **T-02** `functional_depth(data, DepthMethod)` dispatcher + López-Pintado–Romo `functional_boxplot`. Additive/non-breaking, no new dependency; **full suite 2061 lib tests green, clippy `--all-targets` clean**. New files `basis/constant.rs`, `depth/dispatch.rs`. **Release pending** — crate at 0.19.0 → bump 0.20.0 + push + tag (operator-driven).
- **Shipped: v0.19.0 Functional Inference Suite** (2026-08-16) — 2 phases (20–21), 2 plans, both VERIFICATION passed (independently re-verified), milestone audit passed (2/2). First implementation milestone promoted from the v0.18.0 `R-BACKLOG.md`: gives fdars its first standalone functional-inference surface, closing the two P1 table-stakes items in R-parity Area 5 (previously 0/22 present). New `fdars-core/src/inference/` module (7 files, 8 public entry points): INF-01 two-sample tests (`t_perm_test`, `f_perm_test`, `two_sample_mean_test`, `mean_scb`, `scb_two_sample_test` + `TestResult`) + INF-02 FLM inference (`flm_f_test`, `flm_gof_test`, `oneway_anova_vstat`). 29 inline tests; **full suite 2039 lib tests green, clippy `--all-targets` clean**; additive/non-breaking (existing signatures incl. `fanova` unchanged); **no new crate dependency** (self-contained χ²/F survival fns). INF-03 (ITP) deferred to v2. **Release pending** — crate is at 0.17.0 (v0.18.0 was audit-only); this milestone shipped real code, so a version bump + PR + tag is the next ship step (operator-driven).
- **Shipped: v0.18.0 R-Ecosystem Gap Audit** (2026-08-15) — 4 phases (16–19), 5 plans, all VERIFICATION passed, milestone audit passed (7/7 requirements, cross-phase document flow coherent). Audit-only: **zero `fdars-core/src/` edits** — the R-ecosystem analog of v0.14.0. Deliverables: `.planning/research/R-AUDIT-REPORT.md` (35-package versioned inventory → 250-row parity matrix → 12 R-honest strengths → consolidated findings) + `.planning/research/R-BACKLOG.md` (26 value-ranked, promotion-ready items). **Headline findings:** the R ecosystem is far broader than scikit-fda (275 caps vs 161); **162 actionable gaps** (18 table-stakes / 144 differentiator); fdars is present on only 88/250 in-scope capabilities but leads on explainability + streaming depth. **Biggest table-stakes deficits:** functional inference (Area 5, 0 present), Fréchet/object-data & density regression (Area 7, 0 present), functional time series forecasting (Area 6, 2/25). Distinct deliverables from the archived scikit-fda `AUDIT-REPORT.md`/`BACKLOG.md` (untouched). `Rfssa` inventory-completeness caveat documented. Crate stays at 0.17.0 — no code changes.
- **Shipped: v0.17.0 Registration Parity & Elastic-FPCA Performance** (2026-08-12) — 2 phases (14–15), 3 plans, all VERIFICATION passed, milestone audit passed (3/3 requirements, integration clean, full suite green: 2727 tests `linalg,parallel` / 2718 default); released via PR #41 (crate 0.17.0 on crates.io, tag v0.17.0). FEAT-06 `least_squares_shift_registration` + `ShiftRegistrationResult` (new `alignment/shift.rs`), FEAT-07 three registration-quality scores (`alignment/quality.rs`), PERF-04 parallelized elastic-FPCA loops (bit-identical, N≥50 guard) — all additive/non-breaking. Phase-14 review caught + fixed 1 CI-blocker + 3 warnings (incl. cosine→centered-Pearson correctness fix); Phase-15 review clean. Both VALIDATION.md remain `draft` (Nyquist TODO).
- **Shipped: v0.16.0 Elastic Feasibility + Parity Quick Wins** (2026-08-12) — 2 phases, all VERIFICATION passed, milestone audit passed (4/4 requirements, integration clean, 2663 tests green); released via PR #40 (crate 0.16.0, tag v0.16.0). PERF-03 opt-in banded elastic alignment (large grids feasible), FEAT-03 imputation, FEAT-04 ExtrapolationPolicy (both interp paths), FEAT-05 five scoring metrics — all additive/non-breaking. Phase-13 review caught + fixed 2 real critical edge-case bugs before completion.
- **Shipped: v0.15.0 Top-Backlog Quick Wins** (2026-08-11) — 2 phases, 4 plans, all VERIFICATION passed, milestone audit passed (4/4 requirements, integration clean); PR #38. First real `fdars-core/src/` code delivered from the audit backlog: FEAT-01 (spline interpolation) + FEAT-02 (functional summary statistics) in phase 10; PERF-01 (parallel CV folds via `iter_maybe_parallel!`) + PERF-02 (faer `thin_svd` FPCA backend under `linalg`) in phase 11 — both perf wins equivalence-tested. Full suite 1948 tests green, clippy clean. 3 advisory code-review items carried forward as tech debt (weakened MEWMA test assertion, `fix_svd_signs` NaN no-op, over-broad test name); both phase VALIDATION.md files remain `draft` (Nyquist coverage TODO).
- **Shipped: v0.14.0 audit milestone** (2026-08-09) — 9 phases, 21 plans, all VERIFICATION passed, milestone audit passed (13/13 requirements, integration sound). Zero `fdars-core/src/` edits.
- **Deliverables:** `.planning/research/AUDIT-REPORT.md` (consolidated report — methodology, 5 performance findings, 82 in-scope gaps, 30 fdars strengths) and `.planning/research/BACKLOG.md` (32-item value-ranked backlog, `score = value/√effort`, promotion-ready 7-field blocks).
- **Headline findings:** SVD dominates FPCA wall-clock (~99.8%); faer `thin_svd` 1.8–4.1× faster than nalgebra; elastic alignment is the top bottleneck (infeasible at N=500,M=200 on the default path); 82 actionable capability gaps vs scikit-fda 0.10.1 (36 table-stakes, 46 differentiator); 30 fdars-exclusive strengths.
- `fdars-core` crate itself remains at v0.14.0 — the audit made no code changes.

## Next Milestone Goals

v0.28.0 (in progress) draws the two dependency-satisfied score-1.00 (L-effort) items — FTS-03 + FRE-02. Once those ship, continue drawing from `.planning/research/R-BACKLOG.md` via `/gsd-new-milestone` — the remaining lower tier:
- **REG-06** (score 0.67, L) — boosting / Bayesian functional regression (FDboost, GAMLSS, Gibbs/VB FOSR).
- **REP-02** (score 0.67, L) — FEM/PDE smoothing on irregular 2D/3D domains (fdaPDE).
- **CLUS-02** (score 0.67, L) — functional co-clustering (funLBM latent-block) + slope-heuristic selection.

**Release status:** `fdars-core` **0.27.0 published to crates.io** (tag `v0.27.0`, 2026-08-22) — the version bump folded in all code shipped since 0.24.0. v0.28.0 will be the next release after FTS-03 + FRE-02 land.

## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| scikit-fda as the gap-analysis baseline (not R fda.usc/fda) | Single, modern Python reference keeps the comparison focused | ✓ Validated — v0.14.0 (Phases 7–8: 161-row inventory → 141-row parity matrix, 82 actionable gaps) |
| Audit-only milestone (report + backlog, no production code) | Bounds scope; makes future implementation decisions evidence-driven | ✓ Validated — v0.14.0 (all 9 phases zero `fdars-core/src/` edits; deliverables shipped) |
| Performance measured via static analysis + real benchmarks | Static pass finds candidates cheaply; criterion numbers confirm the real bottlenecks | ✓ Validated — Phases 1–6 (SVD dominates FPCA ~99.8%, copy ~0.15%; faer 1.8–4.1× over nalgebra) |
| Gap-analysis breadth decided by findings ("start broad, deep-dive where warranted") | Avoids over-investing in low-value areas before knowing where the gaps are | ✓ Validated — v0.14.0 (6 areas enumerated broadly; 82 gaps categorized table-stakes/differentiator) |
| Backlog phrased as GSD-ready requirements/phases | Lets findings flow straight into `/gsd-new-milestone` without rework | ✓ Validated — v0.14.0 (32-item ranked backlog, 34 seven-field promotion-ready blocks) |
| Performance changes must be additive + equivalence-tested (behavior preserved) | Perf wins should never risk numerical correctness; keep non-`linalg` path intact | ✓ Validated — v0.15.0 (PERF-01 bit-for-bit vs sequential; PERF-02 faer≈nalgebra within 1e-8·σ₁; no new deps) |
| First backlog milestone kept to top-4 quick wins (2 capability, 2 perf) | Bounds scope; proves the audit→implementation pipeline before larger items | ✓ Validated — v0.15.0 (4/4 requirements shipped + verified in 2 phases) |
| Registration scores use standalone-energy form (not scikit-fda's ratio-based improvement metrics) | The backlog/CONTEXT intent was absolute quality diagnostics over the registered set; documented the divergence in rustdoc | ✓ Validated — v0.17.0 (Phase 14; review confirmed not silently reverted; WR-01 fix centered the Pearson score) |
| Elastic-FPCA `:764` light loop guarded by an N≥50 threshold (outer-if) rather than unconditional parallelization | A single-multiply body only pays back the parallel dispatch at scale; guard avoids small-N regression | ✓ Validated — v0.17.0 (Phase 15; `SCORES_PARALLEL_THRESHOLD=50`, bit-identical equivalence ON/OFF) |
| Milestone-complete does planning archival only; the crate release (version bump + PR + tag) is a separate ship step | Keeps planning cadence decoupled from the release flow; `main` is not branch-protected, so the release PR + tag can be driven directly | ✓ Validated — v0.17.0 (released via PR #41; tag v0.17.0 → `release.yml` auto-published crate 0.17.0 to crates.io) |
| Pivot the gap-analysis yardstick from scikit-fda to the R FDA ecosystem for v0.18.0 | scikit-fda's actionable backlog was exhausted (v0.15.0–v0.17.0); R is older/broader and surfaces the next tier of high-value gaps | ✓ Validated — v0.18.0 (35 R packages, 275 caps vs scikit-fda's 161; 162 actionable gaps found) |
| R-audit deliverables use distinct filenames (`R-AUDIT-REPORT.md`/`R-BACKLOG.md`), never overwriting the scikit-fda audit files | Preserves the v0.14.0 audit as historical record; keeps the two yardsticks separable | ✓ Validated — v0.18.0 (both scikit-fda files untouched across all 4 phases) |
| Reverse-parity strengths must be re-vetted against R specifically, not copied from the scikit-fda sweep | R is far broader — SPM/conformal/elastic have R analogs (`funcharts`/`conformalInference.fd`/`fdasrvf`); an uncritical copy would overstate fdars' moat | ✓ Validated — v0.18.0 (Phase 18: 30 scikit-fda strengths → 12 R-honest; genuine moat = explainability + streaming depth) |
| First R-backlog implementation milestone = the two P1 table-stakes inference items (INF-01/INF-02), not the 5.00 quick wins | Inference (Area 5) was fdars' single biggest table-stakes deficit (0/22 present) — the highest-value gap despite M-effort; closes a whole capability class | ✓ Validated — v0.19.0 (2/2 shipped; new `inference/` module, 2039 tests green) |
| New inference built by reuse, not new dependencies (self-contained χ²/F survival fns; lift existing permutation/Hotelling/Degras machinery) | Avoids a `statrs` API-surface addition + package-legitimacy review; keeps the perm/Hotelling/SCB code paths battle-tested | ✓ Validated — v0.19.0 (χ²/F SF in `inference/dist.rs`; `fanova`/`hotelling_t2`/`scb_mean_degras` reused unchanged) |
| Orchestrator independently re-verifies executor code (cargo test + clippy) before marking phases passed | Background agents were unreliable this session; trust-but-verify catches lost/partial agent work | ✓ Validated — v0.19.0 (both phases re-run green by orchestrator, not just trusting the executor summary) |
| Sandwich-smoother FPCA/covariance variants reuse existing kernel smoothing rather than implementing R's penalized tensor-product splines | Milestone constraint = reuse-first, no new dependency; kernel-sandwich matches the capability (symmetric PSD smoothed covariance) without a P-spline subsystem; divergence documented in rustdoc | ✓ Validated — v0.26.0 (ssvd + face_covariance/mface_covariance all kernel-sandwich; `gaussian_smooth_cov` shared pub(crate) across `fpca_variants`→`irreg_fdata/face`) |
| Compute `fsvd` cross-covariance SVD via the smaller Gram matrix's symmetric eigendecomposition, not nalgebra's general `SVD::new` | nalgebra's general SVD (and its own `recompose()`) failed to reconstruct a near-rank-deficient weighted cross-covariance (err ~0.10), producing wrong singular functions; the Gram-eigen path is robust and feature-agnostic | ✓ Validated — v0.26.0 (rank-1 recovery + both p>q/p<q branches tested green) |
| v0.26.0 autonomous run executes phases inline (not via gsd-executor subagents) and skips the git tag | Repo memory: worktree base diverged from origin/HEAD (forces sequential) + executor subagents stall on long fdars cargo builds; a `v*` tag push triggers crates.io publish while Cargo.toml is still 0.24.0 (crate release is a deferred operator step, matching v0.25.0's no-tag precedent) | ✓ Validated — v0.26.0 (both phases 5/5 verified via inline execution + independent verifier re-runs; no phantom publish) |
| Signed-weight Fréchet regression uses a sort-based isotonic projection, not R `frechet`'s osqp quadratic program | Milestone constraint = no new crate dependency; Petersen–Müller/local-linear weights can be negative so `wasserstein_barycenter` (which rejects negative weights) cannot be reused for regression — a signed quantile average + sort-based monotone projection is the zero-dependency equivalent (documented divergence) | ✓ Validated — v0.27.0 (global/local regression track a known predictor→density relationship within tolerance; negative-weight inputs return Ok with a finite monotone density) |
| Fréchet ANOVA reports a seeded permutation p-value as primary and the asymptotic χ²(k−1) as secondary, with σ̂ₗ² flagged `[ASSUMED]` | The exact Dubey–Müller σ̂ₗ² variance-estimator form is not fully specified in public R `frechet::DenANOVA` sources; the permutation p-value is robust to that assumption, and the in-crate `chi_square_sf` avoids a `statrs` dependency | ✓ Validated — v0.27.0 (flags shifted groups p<0.05, ignores homogeneous p>0.05, seed-reproducible) |
| v0.27.0 autonomous run executes phases inline and skips the git tag (same as v0.25.0/v0.26.0) | Same repo-memory constraints (worktree base divergence + executor cargo-build stalls) and the same deferred-release rationale (Cargo.toml still 0.24.0; a `v*` tag push would publish a phantom version) | ✓ Validated — v0.27.0 (both phases 5/5 verified inline; milestone audit PASSED 13/13; no phantom publish) |

## Evolution

This document evolves at phase transitions and milestone boundaries.

**After each phase transition** (via `/gsd-transition`):
1. Requirements invalidated? → Move to Out of Scope with reason
2. Requirements validated? → Move to Validated with phase reference
3. New requirements emerged? → Add to Active
4. Decisions to log? → Add to Key Decisions
5. "What This Is" still accurate? → Update if drifted

**After each milestone** (via `/gsd-complete-milestone`):
1. Full review of all sections
2. Core Value check — still the right priority?
3. Audit Out of Scope — reasons still valid?
4. Update Context with current state

---
*Last updated: 2026-08-22 — v0.28.0 Spectral Functional Time Series & Object-Data Fréchet Regression **started** (`/gsd-new-milestone`): the two now-unblocked score-1.00 (L-effort) `R-BACKLOG.md` items — FTS-03 (spectral functional time series — DPCA/spectral-density/VAR-VMA/FARMA, new `fts/spectral.rs` + `simulation.rs`, reusing `rustfft`, builds on shipped FTS-01/FTS-02, Phase 41) and FRE-02 (object-data Fréchet regression — covariance/correlation/spherical/network/point-process metric backends plugging into the shipped FRE-01 solver, extends `frechet/`, Phase 42). Both P3 differentiators, additive/non-breaking, reuse-first, **no new dependency**; phase numbering continues 39/40 → 41/42; independent/disjoint code areas. Prior: v0.27.0 Functional Time Series & Fréchet Regression **shipped** (audit PASSED 13/13, milestone archived): the two score-1.33 (L-effort) `R-BACKLOG.md` items — FTS-01 (functional time-series forecasting — `ftsm`/`ftsm_forecast`/`ftsm_forecast_multistep`/`ftsm_update`/`fplsr`, new `fts/forecast.rs`, reusing `fdata_to_pc_1d` + `scoring.rs` + FTS-02's `fts/acf.rs`, Phase 39, verified 5/5) and FRE-01 (Fréchet/object-data regression — `MetricSpace`/`WassersteinDensitySpace`, `frechet_mean`/`frechet_variance`, `wasserstein2_distance`, global/local Fréchet regression, `frechet_anova`, new `frechet/`, sharing DENS-01's `density_fda.rs` quantile machinery, Phase 40, verified 5/5). Exhausts the 1.33 tier; additive/non-breaking, reuse-first, **no new dependency**. Documented divergences: signed-weight regression via sort-based isotonic projection (not R's osqp QP); Fréchet ANOVA σ̂ₗ² `[ASSUMED]` (permutation p-value primary); Wasserstein-barycenter round-trip ~0.1–0.15 W₂ floor. Executed inline (worktree/executor-stall memory), each phase independently re-verified; whole-crate **2460 lib + 172 doc tests green, `cargo clippy --all-targets --features linalg,parallel` clean, `cargo fmt --check` clean**. **Released as crate `fdars-core` 0.27.0 on crates.io** (bumped 0.24.0 → 0.27.0, tag `v0.27.0` pushed → `release.yml` `cargo publish` succeeded); `CHANGELOG.md` added. The 0.27.0 crate folds in all additive code shipped since 0.24.0 (the v0.25.0/v0.26.0 milestones were never published as separate crate versions). Prior: v0.26.0 FPCA Breadth & Sparse Covariance **shipped** (audit PASSED 8/8, milestone archived): FPCA-02 (specialized FPCA variants — new `fpca_variants.rs`: `fpca_der`/`fsvd`/`cross_covariance`/`dynamical_correlation`/`ssvd`, Phase 37, verified 5/5) and SPARSE-01 (FACE/mfaces sparse covariance + trajectory bands — new `irreg_fdata/face.rs`: `face_covariance`/`mface_covariance`/`face_trajectory`, Phase 38, verified 5/5) — the two remaining top-ranked `R-BACKLOG.md` items (score 1.73, M-effort), exhausting the 1.73 tier. Both additive/non-breaking, reuse-first, no new dependency; executed inline (worktree/executor-stall memory), each phase code-reviewed (coverage gaps closed) + independently re-verified; whole-crate **2414 lib + doc tests green, `cargo clippy --all-targets` clean, `cargo fmt --check` clean**. Crate still at 0.24.0 — version bump + tag/publish for v0.23.0/v0.24.0/v0.25.0/v0.26.0 is a deferred operator ship-time step (no `v0.26.0` git tag created by the autonomous run, matching the v0.25.0 no-tag precedent). Prior: v0.26.0 **started** (`/gsd-new-milestone`) (`/gsd-new-milestone`): the two remaining top-ranked `R-BACKLOG.md` items (score 1.73, M-effort) — FPCA-02 (specialized FPCA variants — FPCAder/FSVD/cross-covariance/dynamical-correlation/sandwich-ssvd, extend `regression.rs`, Phase 37) and SPARSE-01 (FACE/mfaces fast sparse covariance + trajectory bands, extend `irreg_fdata/`, Phase 38). Both P3 differentiators complementing the shipped PACE FPCA-01; additive/non-breaking, reuse-first, no new dependency. Phase numbering continues 34–36 → 37–38. Prior: v0.25.0 Serial Dependence, Representation & Density Breadth **shipped** (audit PASSED, milestone archived): FTS-02 (functional serial-dependence tooling — new `fts/acf.rs`, Phase 34, verified 5/5), REP-01 (basis-system completions — `basis/` factories + `multi_fdata.rs` + `pda.rs`, Phase 35, verified 13/13), DENS-01 (density object-data FDA — new `density_fda.rs`, Phase 36, verified 7/7) — the next three top-ranked `R-BACKLOG.md` items (score 1.73 each, M-effort). All independent, additive/non-breaking, reuse-first, no new dependency; code-review fixes applied each phase (1 critical + 6 warnings + several info across the milestone); whole-crate **2385 lib + 166 doc tests green, `cargo clippy --all-targets` clean, `cargo fmt --check` clean**. Crate still at 0.23.0 — version bump + tag/publish for v0.23.0/v0.24.0/v0.25.0 is a deferred operator ship-time step (no `v0.25.0` git tag created by the autonomous run). Prior: v0.24.0 Functional Regression & Clustering Breadth **shipped** (audit passed, milestone archived): REG-04 (additive functional regression + variable selection, Phase 31), REG-05 (flexible mixed-effects regression, Phase 32), CLUS-01 (model-based/density functional clustering, Phase 33) — the three top-ranked P2 differentiators from `R-BACKLOG.md` (score 1.73 each). All independent, additive/non-breaking, reuse-first, no new dependency; each verified 5/5 with code-review fixes applied (5 blockers + 12 warnings across the milestone), whole-crate 2268-test suite + `cargo clippy --all-targets` green. Crate still at 0.23.0 — version bump + tag/publish for v0.23.0 and v0.24.0 is a deferred operator ship-time step. Prior: v0.23.0 Depth, Outliers & Interval Inference (DEPTH-01 + OUT-01 + INF-03, crate 0.23.0, tag v0.23.0); v0.22.0 PACE Sparse FPCA & Elastic Multinomial (crate 0.22.0); v0.21.0–v0.19.0 as their versions; v0.18.0 R-Ecosystem Gap Audit (produced `R-BACKLOG.md`). Full per-phase history below.*

*Phase 9 (Consolidated Report & Prioritized Backlog) complete — **audit milestone v0.14.0 done**: AUDIT-REPORT.md finalized (Methodology + Consolidated Findings: 5 perf findings PF-1..5, 82 in-scope gaps, 30 fdars strengths) and BACKLOG.md finalized (32-item Ranked Backlog by `value/sqrt(effort)`, 34 seven-field blocks, Completeness Gate PASSED); verified 7/7; zero src edits (RPT-01/02/03). Backlog ready to promote via `/gsd-new-milestone`. Prior: Phase 8 (Capability Parity Matrix & Categorization) — six area parity tables (141 rows, 59 present / 19 partial / 63 absent) mapping fdars vs scikit-fda 0.10.1 by capability, both rubrics (D-01 verdict, D-03 category), 82 actionable in-scope gaps separated from 32 out-of-scope, a 30-row reverse-parity strengths sweep, and a drafted UNRANKED gap backlog (21 entries + D-02a accuracy-validation item); known-bug rows accuracy-flagged; verified 9/9; zero src edits (GAP-02/03/04). Prior: Phase 7 scikit-fda capability enumeration (GAP-01, skfda 0.10.1, 129 in-scope / 32 out-of-scope); Phase 6 conditional SVD comparison (PERF-06, faer 1.8–4.1× faster, P6-1); Phase 5 parallelism gap assessment (PERF-05); Phase 4 FPCA/SVD & allocation audit (PERF-03/04, Phase-6 GO); Phase 2 static hot-path map (PERF-01); Phase 1 benchmark apparatus + baselines (PERF-02).*
